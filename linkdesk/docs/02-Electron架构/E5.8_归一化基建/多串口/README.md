# 多串口——Phase 6 档案（#26-#30）

> 2026-08-17 立案（用户拍板升格正式 Phase）。**设计真相源 → [01-多串口设计.md](01-多串口设计.md)**——现状摸底（七层单口假设）+ 五决策定死 + 五任务改动文件/设计要点/验收映射 + 破坏性变更窗口论证。
> 清单条目 → `E5.8-执行清单.md` Phase 6（#26-#30，一个任务 = 一次 commit + 独立验收）。

## 一句话

SerialService 单口字段组 → `Map<portName, PortState>`；推送载荷加 portName（对象化）；插件按口过滤。消灭三个单口 hack（数据错标/字符串正则挖口名/string 载荷）。

## 执行前必读

1. 本目录 [01-多串口设计.md](01-多串口设计.md)（五决策 D1-D5 已拍板定死）
2. 前置依赖：Phase 1.5 broadcast 通道 + Phase 4 契约生成（#28 载荷变更走生成流程）
3. 消费方清单 = 三处（SerialContext / index.tsx / useIpcEvent 映射表）+ serialHandlers.test.ts——一次性切换，不留旧通道尾巴
4. 破坏性变更窗口 = E5.8（上架前无第三方存量插件）——论证见设计文档 §5

## 目录

```
多串口/
├── README.md           ← 本文件——开工说明
└── 01-多串口设计.md     ← 设计真相源（现状摸底 + 目标架构 + 五任务细节 + 风险回归）
```
