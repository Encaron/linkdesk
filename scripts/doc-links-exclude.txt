# 断链门禁——整树排除清单（E6#96f）
#
# 格式：<仓库相对路径前缀>	<理由>        （分隔符 = 制表符；理由必填，无理由 = 配置错误）
# 行首 `#` = 注释。
#
# 🔴 豁免是债，债必须挂账。**这里每一条都等于「这棵树里的死链我们不修了」**——
#    所以只有「这棵树永久冻结、再也不会搬动文件」才配进来。
#    如果你要在这份名单里加一条，先回答：这棵树将来还会被改吗？会 ⇒ 不许排除，去修。
#
# 排除的判据（三选一，逐条写在理由里）：
#  ① 名字自带 `_暂定` / `_待执行` —— 立项时就是冻结态，从未转正
#  ② 项目文档明文记载「已封存」（MEMORY / CLAUDE.md）
#  ③ 记录的是**已永久删除**的架构，改了就等于篡改历史

docs/01-Tauri_P1至P5.5/	Tauri 时代遗留。已迁移到 Electron，仓库结构（filter-repo 单根合并）也换过一轮，此树记录的是另一套技术栈与另一套目录布局，无真身可指。
docs/02-Electron架构/E2_底层加固与侧栏扩展_暂定/	名字自带 `_暂定`——立项即冻结，从未转正。
docs/02-Electron架构/E3_多WebView与壳收尾_暂定/	名字自带 `_暂定`——立项即冻结，从未转正。
docs/02-Electron架构/E4_文件树与编辑器_暂定/	名字自带 `_暂定`——立项即冻结，从未转正。
docs/02-Electron架构/E5_核心归一化与壳重构_待执行/	名字自带 `_待执行`——该轮被 E5.6/E5.7/E5.8 取代，未按原样执行。
docs/02-Electron架构/E5.5_多WebView恢复/	记录的是**已废弃的 Per-Tab WebView**（E5.7 极简 Pool 取代）。其断链全是 `linkdesk/src/...` ——单根合并前的套娃布局，**该目录已被永久删除**，无真身；硬改成今天的路径 = 篡改历史记录（它记的是当时的样子）。
docs/02-Electron架构/E5.6_Pool模型重构/	MEMORY / E5.6 清单明文记载「**已封存**」。
docs/02-Electron架构/E5.7_极简Pool/	MEMORY / CLAUDE.md 明文记载「**已封存**」。
docs/02-Electron架构/E5.8_归一化基建/	MEMORY / CLAUDE.md 明文记载「**已封存**」。

# ── E6#99（L7 第 7.2 轮）：18 只发货插件源码外移独立仓 ⇒ 已封存任务档案里的源码引用永久失效 ──
#
# 判据 = 上面第 ③ 条（记录的是**已永久删除**的架构，改了就等于篡改历史）+ 第 ② 条（所属层已收官/封站）。
# 这 9 份是**单档排除**（不是整树）——它们所在的两棵树里还有活文档，绝不能整树放行。
# 🔴 为什么是「排除」而不是「回填」：这些档案记的是**当时那一刻**文件在哪（「#30c 改了 plugins/marketplace/
#    src/…/DetailView.tsx」），把它改写成插件仓路径 **不是回填，是篡改历史记录**（同 E5.5 那条的理由）。
#    它们的源码目标现在住 `Encaron/linkdesk-plugin-<id>`，但**档案里那句话的时态是过去**，不该被改写。
# ⚠️ 这条口子只对**这 9 份**开：任何**活文档**（03-插件制造 / 各层规范 / CLAUDE.md / decisions）里
#    出现指向 `plugins/<已外移 id>/…` 的链接，门禁**照旧红**——发现即修，不许往这里加。
# 🔵 回收条件：若哪天这些档案被正式归档/移出 docs/（例如并入历史区），这些行应同笔删除（债随树走）。
docs/02-Electron架构/E3.5-软件生态美化/串口监视器UI整改/01-全量审计.md	E3.5 已收官的任务档案；其 `plugins/serial-monitor/…` 引用随该插件外移（E6#99）永久失效——改写成插件仓路径 = 篡改当时的审计记录。
docs/02-Electron架构/E6_插件生态与发布/03-插件市场/08-信任与安全.md	L3 市场层已收官；其中 `plugins/marketplace/…` 引用随该插件外移（E6#99）永久失效。
docs/02-Electron架构/E6_插件生态与发布/03-插件市场/13-详情页HTML对账任务档案.md	L3.5 已收官的任务档案；`plugins/marketplace/…` 引用随外移永久失效（同上理由）。
docs/02-Electron架构/E6_插件生态与发布/03-插件市场/14-市场图标系统任务档案.md	L3.5 已收官的任务档案；`plugins/marketplace/…` 引用随外移永久失效（同上理由）。
docs/02-Electron架构/E6_插件生态与发布/03-插件市场/16-市场区健康审视任务档案.md	L3.5 已收官的任务档案；`plugins/marketplace/…` 引用随外移永久失效（同上理由）。
docs/02-Electron架构/E6_插件生态与发布/03-插件市场/18-通知系统全账与设计定案.md	L3.5 已收官的设计定案档；其中 35 处 `plugins/{marketplace,editor,file-tree}/…` 引用随外移永久失效（同上理由）。
docs/02-Electron架构/E6_插件生态与发布/插件规范化层/00-整理档案.md	L3.7 已收官（六轮全收官）；`plugins/marketplace/…` 引用随外移永久失效。
docs/02-Electron架构/E6_插件生态与发布/插件规范化层/02-元数据链路归一.md	L3.7 已收官；`plugins/marketplace/…` 引用随外移永久失效。
docs/02-Electron架构/E6_插件生态与发布/插件规范化层/03-官方插件补齐.md	L3.7 已收官；`plugins/{marketplace,settings,lang-defaults}/…` 引用随外移永久失效。
