# E5.7 执行清单——极简Pool模型

> 2026-08-12。**从双Pool（SidebarPool + MainPool + OverlayWindow）迁移至极简Pool（1 BrowserWindow + 1 WebContentsView 100%×100%）。**
> **🔥 进度唯一真相源。** 做完一个勾一个。
> **目标：** 统一入口 → 壳 DOM 迁入 → SidebarPool 合并 → OverlayWindow 消除 → Zone 分解 → 浮层归一化 → 分隔线 CSS 化 → 突破边界 → 崩溃恢复 → 清理 → Registry 主进程化 → API 补全 → 硬编码消灭 → 缩放/ESLint → E6 前置 → 全量回归。
>
> **当前进度：18 个子任务已勾（#0a-#0e + #1-#4 + #12/#12.5 提前 + #5/#6/#8/#9 + #10/#11/#13）——Phase 0 + Phase 1 + Phase 2 + **Phase 3 完成**（#7 TabBarZone 取消）。#6 补丁 2026-08-14：图标栏拖拽换位补回（commit 9848333a）；#10 并入 E4V#48 跨容器拖放（经用户确认）。**
>
> 📖 **架构设计：** [01-极简Pool设计.md](01-极简Pool设计.md)
> 📖 **Zone 分解：** [Zone系统/Zone分解设计.md](Zone系统/Zone分解设计.md)
> 📖 **浮层归一化：** [浮层系统/浮层归一化设计.md](浮层系统/浮层归一化设计.md)
> 📖 **脱出窗口：** [脱出窗口/脱出窗口设计.md](脱出窗口/脱出窗口设计.md)
> 📖 **崩溃恢复：** [崩溃恢复/单Pool崩溃恢复设计.md](崩溃恢复/单Pool崩溃恢复设计.md)
> 📖 **清理方案：** [清理/清理方案.md](清理/清理方案.md)
> 📖 **Registry 主进程化：** [Registry主进程化/Registry主进程化设计.md](Registry主进程化/Registry主进程化设计.md)
> 📖 **API 变更：** [API补全/API变更清单.md](API补全/API变更清单.md)
> 📖 **硬编码清扫：** [硬编码消灭/硬编码清扫方案.md](硬编码消灭/硬编码清扫方案.md)
> 📖 **回归矩阵：** [回归验证/全量回归矩阵.md](回归验证/全量回归矩阵.md)
> 📖 **任务迁移映射：** [迁移/E5.6到E5.7任务映射.md](迁移/E5.6到E5.7任务映射.md)

---

## 关键路径

```
Phase 0：E5.6 归档（串行——文档标记）
Phase 1：统一入口（串行——PoolLayout v2 + PoolZoneShell 骨架）
Phase 2：壳 DOM 迁入 Pool（串行——Phase 1 后）
Phase 3：SidebarPool 合并（串行——Phase 1 后）
Phase 4：OverlayWindow 消除（串行——Phase 3 后）
Phase 5：Zone 分解（串行——Phase 2 后）
Phase 6：浮层收尾（串行——Phase 4 后；#25/#26 已提前至 Phase 4）
Phase 7：分隔线简化（串行——Phase 3 后）
Phase 8：突破边界——脱出窗口（Phase 5 后）
Phase 9：崩溃恢复（串行——Phase 1 后即可，推荐 Phase 5 后）
Phase 10：清理死代码（串行——Phase 5 后，确认新模型稳定）
Phase 11：Registry 主进程化（Phase 10 后）
Phase 12：API 补全（Phase 1 后即可开始）
Phase 13：硬编码消灭（Phase 12 后）
Phase 14：缩放联动 + ESLint（Phase 1 后）
Phase 15：E6 前置（IPC 骨架，不等 E6 返工）
Phase 16：全量回归（最后）
```

---

## 现状速查

| 文件 | 当前状态 | E5.7 动作 |
|:--|:--|:--|
| `electron/window-manager.ts` | 双Pool WCV 管理 + 多实例遗留 | → 简化为单 WCV 管理 |
| `electron/overlay-window.ts` | OverlayWindow 类 ~400 行 | → 整文件删除 |
| `electron/pool-layout.ts` | PoolLayout v1 + poolId | → 升级 v2 全量字段 |
| `src/pool/pool-main.tsx` | zone 路由 + RENDERERS 注册表 | → 单入口 PoolZoneShell |
| `src/pool/sidebar/` | SidebarRenderer + 子组件 | → 迁入 zones/SidebarZone.tsx |
| `src/pool/main/MainRenderer.tsx` | 693 行超级胶水 | → 分解为 MainZone + PanelZone |
| `src/App.tsx` | 壳 DOM（TitleBar/IconBar/TabBar/StatusBar） | → 迁入 zones/ |
| `src/hooks/useWebViewSync.ts` | ~270 行 per-tab 遗留 | → 整文件删除 |
| `electron/preload-pool.ts` | window.linkdesk.* + pool.onLayout | → 保留，去 poolId 逻辑 |

---

# Phase 0：E5.6 归档

> 不动代码。标记文档，让后续 AI 知道方向已变。

- [x] **E5.7#0a** E5.6 执行清单顶部加废弃声明 → 指向本清单（执行注 2026-08-13：废弃声明已存在——建 E5.7 分支时已加在 E5.6-执行清单.md:3-10，本次核验后勾选）
- [x] **E5.7#0b** E5.6 设计文档（01-Pool模型设计 / 多Pool扩展预留）顶部加 "已被极简Pool取代" 声明（执行注 2026-08-13：两份文档顶部已有"🔴 已废弃——2026-08-12 架构切换至极简Pool"声明（01-Pool模型设计.md:3-6 / 多Pool扩展预留.md:3-8），含继承说明与 E5.7 文档链接——语义即"已被极简Pool取代"，核验后勾选）
- [x] **E5.7#0c** 检查 e5.6 分支未提交改动——`electron/overlay-window.ts` focusable 键盘交互修复已提交（e5.6 收尾 commit）
- [x] **E5.7#0d** 新建 `e5.7` 分支（从 e5.6 切出）——所有 E5.7 改动在新分支
- [x] **E5.7#0e** 🔴 基线噪音记录——e5.6 交接时 `npm run check` 有 4 项存量问题，非本次引入：eslint 3 error（`ShellPluginComponent.tsx:59` useMemo 条件调用 / `SidePanel.tsx:1283` 手写 path replace / monaco `configureWorkerFactory` 内调 useWorkerFactory）+ vitest 1 失败（`DataConverter` UTF-8 跨 realm Uint8Array 相等）。三个 eslint 文件都在 E5.7 清理方案删除/重写名单上（ShellPluginComponent git rm、SidePanel 迁 zones），随 Phase 10 自然消灭；DataConverter 测试在 Phase 16 回归时修。（执行核验 2026-08-13：npm run check 实测与记录一致——tsc 零错误 / eslint 3 error 逐一相符 / vitest 1 failed 399/400 通过）

---

# Phase 1：统一入口——PoolLayout v2 + PoolZoneShell

> 目标：pool.html 单 zone。PoolLayout 升级全量字段。唯一根组件 PoolZoneShell。
> 📖 PoolLayout v2 完整类型 → [01-极简Pool设计.md §7.2](01-极简Pool设计.md#72-e57-的-poollayout全量唯一)
> ✅ 完成标准：tsc 零错误 + 应用可启动（壳 DOM 原样正常、Pool 渲染骨架占位）+ pool:ready / onLayout 正常

- [x] **E5.7#1** `src/core/types/poolLayout.ts`——version 1→2，加全量字段
  - 新增 `titleBar` / `iconBar` / `statusBar` / `rightSidebar?` / `panel?`（🔴 2026-08-13 审计删 `tabBar` 字段——#7 TabBarZone 取消，tab bar 从 `groups[].tabs` 渲染，无需独立字段；同审计删 `topBar`——#23 TopBarZone 取消，顶栏是规范文档已移除部件）
  - 保留 `groups` / `root?` / `sidebar` / `creatableViews?`
  - 删 `poolId`——只有一个 Pool
  - （执行注 2026-08-13：commit 0039ecda。v2 全量字段 + TitleBarLayout/IconBarLayout/StatusBarLayout/rightSidebar/panel 新类型齐备。"删 poolId" 无代码对象——grep 核实 electron/ + src/core/types/ 零 poolId 命中（E5.6 代码从未实现设计稿的 version:1/poolId，纯设计文档概念），此事实已记入 poolLayout.ts 头注释）
- [x] **E5.7#2** `src/pool/pool-main.tsx`——去 zone 路由
  - 删 `RENDERERS` 注册表 + `?zone=` 解析
  - 改为直接渲染 `<PoolZoneShell layout={layout} />`
  - `pool:ready` + `pool.onLayout` 不变
  - （执行注 2026-08-13：commit 911a8e9f。RENDERERS/zone 解析/无效 zone fallback 全删；pool:ready + onLayout 链未动；v2 无空对象初始值 → layout 初始 null + 首推到达前渲染 null（缓冲回放保证 ready 后立即到达））
- [x] **E5.7#3** `src/pool/PoolZoneShell.tsx`——新建唯一根组件
  - flex column 全窗口布局 + zone 条件渲染
  - 🔴 占位策略：Phase 1 版用占位 div——zone 组件在 Phase 2/3/5 逐个替换占位（每 Phase 结束必须 tsc 零错误 + 应用可启动；不允许 import 尚不存在的 Zone 组件）
  - 📖 代码骨架 → [01-极简Pool设计.md §6.1](01-极简Pool设计.md#61-poolzoneshell唯一根组件)
  - （执行注 2026-08-13：commit 6982c33f。flex column 骨架 + index.css 占位样式（全 CSS 变量）；sidebar/panel/rightSidebar 按 layout 字段条件渲染；各占位 div 注释标注 Phase 2/3/5 替换任务号；FloatingLayerHost #25 注释预留未 import）
- [x] **E5.7#4** pushLayout 适配——两端同步改
  - `electron/window-manager.ts`——`pushLayout(layout)` 去 `poolId` 参数（单 WCV 直推）
  - 壳侧 `syncLayout()`（App.tsx / usePoolSync.ts）——构建完整 PoolLayout v2（🔴 syncLayout 实现在壳渲染进程，不在 window-manager）
  - （执行注 2026-08-13：commit 26b8af7c。window-manager pushLayout(layout) 单直推——destroyPool/rebuildPool/getPoolView/getAllPoolViews 同步去 zone；pool-handlers 8 个 handler 去 zone 参数；preload-shell pool API 同步；App.tsx bounds 单推 main（保留 SplitHandles 留缝 gap 数学）；usePoolSync 构建完整 v2 单推。#12 已按中间态说明随本任务提前执行，见下）

---

# Phase 2：壳 DOM 迁入 Pool

> 目标：TitleBar / IconBar / StatusBar 变 React Zone。壳 App.tsx 保留全部状态逻辑（tabState/syncLayout/pushLayout/Registry/命令执行），只删 UI DOM 渲染。
> 🔴 2026-08-13 审计：#7 TabBarZone 取消——tab bar 留在 MainZone 内（每 panel 自带 GroupTabBar，E5.6#16.5"自包含"教训 + 拖拽协调不拆），吸收进 Phase 5 #20。
> 📖 Zone 职责表 → [Zone分解设计.md](Zone系统/Zone分解设计.md)

- [x] **E5.7#5** `src/pool/zones/TitleBarZone.tsx`——新建
  - `-webkit-app-region: drag` + no-drag 按钮区 + 窗口控制 IPC
  - 🔴 前置：`preload-pool.ts` 补 `window` 命名空间（minimize/maximize/unmaximize/close/toggleDevTools/isMaximized/onMaximizeChange——从 preload-shell.ts 同款搬入。2026-08-13 审计：preload-pool 现无此 API，TitleBarZone 在池内要用）
  - 📖 → [Zone分解设计.md §2.1](Zone系统/Zone分解设计.md#21-titlebarzone)
  - （执行注 2026-08-14：commit 13ea6d32。数据流定案=壳推全量（显示文本铁律）：TitleBarLayout 扩 logoUrl（壳 getAssetPath——Path B 池不碰 core）/menuGroups（壳做 group 分组+flattenGroupItems 展平+递归 t() 翻译+order 排序）/slots（when 壳侧 ContextKeyService 过滤）/windowControls 4 tooltip（壳 t()）；usePoolSync 增 ContextKeyService.onDidChangeContext + i18n languageChanged 订阅→layoutVersion 重推（壳 TitleBar 重渲染的池等价物）。TitleBarZone 哑渲染：fixed 下拉（WCV 满窗无裁剪，不需要 OverlayPortal——下拉在 30px 拖拽区之下满足硬约束 #18）+ 递归 children 子面板（hover 100/150ms 同壳时序）+ 外部点击/Escape 关闭 + ─ □ × 最大化状态。诚实注记：①壳 TitleBar 本就不渲染 title 文本（仅 Logo）——照搬零丢失；②壳下拉无快捷键无 when 灰显（checkWhen=false）——池同；③设计稿 §2.1 的 Alt 调出隐藏菜单栏壳未实现——池也未实现（远期）；④菜单注册表无 onDidChange——插件卸载后菜单按钮需等下次 pushLayout 才消失（壳 TitleBar 同样依赖重渲染，非新回归））
- [x] **E5.7#6** `src/pool/zones/IconBarZone.tsx`——新建
  - 42px 图标列 + 激活高亮 + `icon:selected` 事件
  - 📖 → [Zone分解设计.md §2.2](Zone系统/Zone分解设计.md#22-iconbarzone)
  - （执行注 2026-08-14：commit 8c2ae85c。数据流=壳推全量：IconBarLayout 扩 icons（壳 getViewPlugins+iconOrder 排序+getIconLocation 过滤——无 iconBar 声明不渲染，壳 filter 同款；icon 序列化为判别联合 lucide/codicon/img/emoji，池 PoolPluginIcon 哑渲染——LUCIDE_MAP 与壳 PluginIcon 同步维护）+activePluginId（壳侧双重守卫：侧栏展开+活动容器属于该插件）+hamburgerVisible/hamburger.navLabel/title/groups（menuStyle hamburger/both；groups 壳 MenuRenderer showGroups+showKeybindings+checkWhen 语义——与 titlebar 关键差异=不展平父项"文件/查看"保留 children hover 子面板；快捷键 formatKeyLabel、when 灰显壳侧已判）+navLabel t("导航")。点击→池 events.emit("icon:selected")→主进程 plugin:emit→壳 plugin:push→App 新增桥接 effect→shellEvents→MainContent 开标签。onDidRegister/onDidUnregister→layoutVersion 重推（Phase 5h Step 1 同款——安装插件图标即时出现）。共享组件提取 3 件：MenuItemList（#5 TitleBarZone 内提取，汉堡复用）/PoolPluginIcon/executePoolCommand。诚实注记：①拖拽换位——原设计 §2.2"可选——远期"，2026-08-14 按零丢失铁律补回（见下方 #6 补丁执行注）；②底部齿轮菜单（MenuId.ExtensionGear）推迟 Phase 4 #14——过渡期点击齿轮无动作，设置从 文件→打开设置 可达；③壳 icon-btn 48×48 在 42px 列溢出——池按设计修正 42×42；④汉堡子面板壳仅 2 层，池递归 N 层（实际数据 ≤2 无感）；⑤css 归一化：active 径向高亮 rgba→color-mix(var(--accent) 12%)，汉堡阴影→var(--context-menu-shadow)）
  - （#6 补丁执行注 2026-08-14：commit 9848333a——零丢失铁律。设计 §2.2"可选——远期"全清单无任务认领=静默丢功能，经用户确认现补。数据流（#13 同款乐观本地+commit，真相源在壳）：池 IconBarZone 增壳 IconBar.tsx 状态机照搬——mousedown 记起点+5px 阈值防误触+mousemove findTarget（整列落点——壳只认 icon-bar-top、底部图标不可作落点=壳实现遗漏，注记⑤补全）+mouseup 全序 splice→setLocalIcons 立即渲染+events.emit("icon:reordered", ids)→主进程 plugin:emit→壳 plugin:push→App 新增桥接 effect→shellEvents→E5#7d 订阅持久化 iconOrder→usePoolSync 新增订阅重推权威序→池 localIcons 对齐（拖拽期间 draggingRef 忽略推送防闪跳）。拖影 42px（鼠标 −21 居中，壳 −24=48/2）+drop 指示线 28×2 accent+原按钮 opacity 0.4；拖影阴影 rgba(0,0,0,0.35)→var(--context-menu-shadow) 归一化。诚实注记⑥：E4V#48 视图跨容器拖放（文件树条目拖到图标栏→ViewContainerService.moveView）未迁——清单无任务（与拖拽换位同类静默丢失），drop 源 SectionStack 要 #10 SidebarZone 才进池，建议并入 #10——待用户定夺。验证：tsc 0 / eslint 3 错误+960 警告零新增 / vitest 399/400（基线 DataConverter）/ 4px 网格 ✓）
- [ ] **E5.7#7** ~~`src/pool/zones/TabBarZone.tsx`~~ 🔴 已取消（2026-08-13 审计）——tab bar 留在 MainZone 内，吸收进 #20
  - 取消原因：E5.6#16.5 的架构教训——"TabBar+内容+分隔线自包含在 panel 内"是治"拖分隔线 TabBar 不跟着动"的唯一解；独立 TabBarZone 横带在多 group 分屏时与面板几何错位（需双份 computeLayout），且 MainRenderer 拖拽协调（tabBarRefs + computeSplitZone + preview portal）依赖 tab bar 和 panel 同组件——拆开 = 重接线 15 轮 bug 验证过的活代码
  - 连带：#1 的 PoolLayout `tabBar` 字段删除；拖出窗口检测（useDragDetach，Phase 8 #33）改由 MainZone 接入
  - 📖 取消论证 → [Zone分解设计.md §2.4](Zone系统/Zone分解设计.md#24-tabbarzone)（墓碑）
- [x] **E5.7#8** `src/pool/zones/StatusBarZone.tsx`——新建
  - 22px 状态栏 + 左右分区
  - 📖 → [Zone分解设计.md §2.8](Zone系统/Zone分解设计.md#28-statusbarzone)
  - （执行注 2026-08-14：commit 3232cc8e。数据流=壳推全量（显示文本铁律）：StatusBarLayout 扩 items（壳三源合并=贡献 getStatusBarContributions+动态 getDynamicStatusBarItems+eventEntries 按 alignment 拆 __shell_left__/__shell_right__+壳固定项语言/主题——title 硬编码中文改 t()，硬约束 #2 顺带修正）+dividerBefore 壳侧算好（左区每项除整区首个都有分隔——组间+组内；右区仅组内除首个——壳 StatusBar 渲染语义照搬）+component 标记（getViewPlugin().statusBarComponent——serial-monitor TX/RX 计数，取代该插件全部静态项，壳 renderPluginStatusBar 同款）+chordLabel（壳 CHORD_CHANGED 逻辑迁 usePoolSync——pending/failed 两态字符串+3s failedKey 自动清除定时器，按键名技术标识符不走 i18n）+notif（通知中心纯数据：_seenIds 模块级 Set 从 NotificationCenter 迁 usePoolSync，formatTimeAgo/buildSourceGroups/getNotifIconClass 壳侧照搬；unread/bellTitle/panelTitle/clearLabel/emptyLabel/dismissTitle/groups 全壳 t() 后序列化）。订阅→layoutVersion 重推：onDidChangeStatusBar/tab:focused/subscribeToasts/statusbar:update。池侧 2 新文件：StatusBarZone.tsx 哑渲染（switch 分左右、分隔线、chord、铃铛+面板 fixed 定位——OverlayPortal 不迁，outside-click+Escape 行为等价）+PoolStatusBarComponent.tsx（插件状态栏组件 glob 6 路径懒加载+resolvePath 运行时 3 路径试错，同 PluginComponent 模式）。池→壳事件回传 4 通道：notif:panel（setToastsSuppressed+标记已读+手动重推——标记已读不 fire toast 事件）/notif:dismiss/notif:clearAll/notif:action（ToastAction.onClick 是壳侧闭包不可序列化→壳侧执行+关闭，语义照搬 a.onClick();dismissToast(n.id)）。诚实注记：①NotificationCenter 迁入本任务——Zone分解设计 §2.8 右区明确列"通知"，且全清单无其他任务覆盖，零丢失铁律要求吸收；②面板阴影 0 8px 32px rgba(0,0,0,0.35) 保留壳原值——--context-menu-shadow 层级太浅不配 420px 面板，Phase 6 #26 常量表可补面板级 token（CSS 头注记）；③壳固定项 lang/theme 空 label 回退 id 显示"lang/theme"文本（壳 StatusBar {item.label || item.id} 行为照搬，非新回归）；④未迁入死类 .status-dot/.status-traffic/.status-error/@keyframes statusFadeIn——壳 StatusBar.tsx 与插件均不渲染（grep 零消费））
- [x] **E5.7#9** `src/App.tsx`——删壳 UI DOM 渲染，保留状态逻辑
  - 删：TitleBar/IconBar/TabBar/StatusBar/MainContent 的 DOM 渲染代码
  - 🔴 **保留（"壳=唯一真相源"不变）：** tabState / syncLayout / pushLayout / shellEvents / CommandRegistry / MenuRegistry / KeybindingRegistry / ConfigurationRegistry / loader.ts 插件加载 / 命令注册与执行
  - 壳渲染进程 = 纯状态持有者——无可见 DOM（被 WCV 完全覆盖）
  - 壳与池的分工：壳想（状态），池画（渲染）
  - （执行注 2026-08-14：commit c5407294。App.tsx 删 TitleBar/WindowControls/IconBar/SplitHandles/MainContent/StatusBar 渲染 + zoneBounds + menuStyle + editorAreaRef + TITLE_BAR_HEIGHT 偏移——MainContent 全套状态机搬入（useTabManager 解构/icon:selected 消费/TabActions u1-u7/布局恢复/handleFocusTab/coreCallbacks/handleTabAction/usePoolSync/持久化三件套）。诚实注记：①布局恢复 effect 加 ready 守卫——原 MainContent 在 ready 门控 JSX 内挂载（initAll 完成后才 mount），迁 App 后 effect 首轮 mount 就跑，不守卫则 E5#7h3 恢复早于 LayoutService 初始化；②LayoutEngine 保留最小 feed（setContainerSize 满窗零偏移）——#12.5 删的是 bounds 推流，zone 几何真相源（usePoolSync 读 sidebar 宽/SidePanel setZoneWidth 折叠/#13 拖拽 commit）仍在壳，#31 整删时连带删此 effect；③SidePanel 隐藏挂载保留——icon:selected→sidebar:containerChanged/toggled 管线是壳侧栏状态唯一入口，活到 #10 SidebarZone 接管（旧注释"Pool 崩溃 fallback"不准确——§9.2 恢复由主进程 WCV 重建+pushLayout 重放覆盖）；④usePoolSync sidebarWidth 参数删除——LayoutEngine getBounds 内部直读 + onDidChangeLayout→layoutVersion 重推（折叠/展开/resize 后重推）；⑤删 16 文件（清理方案 §3"已被 zones 替代则删除"）：MainContent/TitleBar/IconBar/StatusBar/WindowControls/NotificationCenter + 壳视图三件套（池 ShellViewRenderer 已有 welcome/plugin-detail/output 路由）；SplitHandles.tsx 留 #31 整删（清单明确归属）；PluginDetailView.css 保留（PluginDetailPoolView 复用）；⑥eslint 984→960 warning——纯死文件 warning 消灭零新增，3 error 基线不变，vitest 399/400（基线 DataConverter），4px 网格 ✓）

---

# Phase 3：SidebarPool 合并 → SidebarZone

> 目标：SidebarRenderer 从独立 WCV 变同 DOM React 组件。

- [x] **E5.7#10** `src/pool/zones/SidebarZone.tsx`——新建（🔴 职责 = 哑渲染 SidebarLayout 全量字段，不只是 PluginComponent 包装器）
  - 替代 `src/pool/sidebar/SidebarRenderer.tsx`——**行为零丢失**，验收逐项对照 SidebarRenderer 现有 6 项：① collapsed 折叠态（▶ 展开按钮）② 空状态文案 ③ mergeHeaderWhenSingle 标题合并 ④ toolbar/section 角色分离（toolbar 粘顶在滚动容器外）⑤ SectionStack 折叠/拖排/PaneSash resize ⑥ header 右键菜单 + ◀ 折叠按钮
  - visible=false → display:none；section 内容用 `<PluginComponent>`——容器结构（header/toolbar/section stack）在 zone 内渲染
  - 🔴 API 过渡：侧栏 action（toggleSidebarCollapse / sidebarAction）暂用现有 `linkdesk.pool.*` 通道，Phase 12 归位命名——Phase 3 结束时侧栏按钮必须可用（防"点不动"回归）
  - 📖 → [Zone分解设计.md §2.3](Zone系统/Zone分解设计.md#23-sidebarzone)
  - （执行注 2026-08-14：commit 2535cb2b。SidebarZone.tsx/css 新建 + PoolZoneShell 接入替换占位；E4V#48 跨容器拖放经用户确认并入本任务（清单原无此任务，与 #6 拖拽换位同类的静默丢失——零丢失铁律）。差异如实记录：① visible=false 用 display:none 包裹（非 E5.6 null 卸载）——保持挂载视图状态不丢 ② .side-panel-title 类名修正（E5.6 用了不存在的 .side-panel-header-title）③ 空状态两行文案（主+提示）壳 t() 推送（SidebarLayout +4 字段 emptyText/emptyHint/expandTooltip/collapseTooltip）④ 无 as-any。宿主状态机：原隐藏挂载 SidePanel 语义迁入 App（icon:selected 容器解析/同容器 toggle 折叠/异容器展开切换 + view:toggleCollapse/resetPosition/toggleVisibility 三命令事件），折叠真相源 = LayoutEngine zone 宽（≤48）——不持 collapsedRef 防池◀按钮改宽后状态脱节；preCollapseWidth 恢复；usePoolSync toggleSidebarCollapse 改转发 sidebar:toggleFromPool（ShellEvents +void 事件，三条折叠路径归一 doCollapse）；隐藏挂载 SidePanel 删除。E4V#48：viewDragProtocol.ts 自定义 MIME + PoolSectionStack handleDragStart setData + IconBarZone onDragOver/Leave/Drop + .view-drop-target 描边——dataTransfer 替代壳 getDraggingView 共享状态（池内唯一同步通道）。门禁：tsc 0 / eslint 3+960 基线零新增 / vitest 399/400 基线 / grid ✓）
- [x] **E5.7#11** `src/pool/sidebar/`——目录删除，组件迁移
  - PoolSectionStack + PoolToolbarSlot → `src/pool/shared/`（RightSidebarZone 同构复用——折叠/拖排/sash resize/toolbar 粘顶是 E5.6#11g/#11f/#16.7k 打磨的活代码，禁止重写）
  - SidebarRenderer.tsx 删除（被 #10 SidebarZone 替代）
  - （执行注 2026-08-14：commit 103bda62。两组件 git mv → shared/（import 改 ./PluginComponent、./viewDragProtocol；../../core 与 ../../components 深度不变零改动）；SidebarRenderer.tsx 删除——其 2 条 lint warning 随删（33:16 as-any + 83:20 role 比较）→ 基线 960→958 如实记录。门禁：tsc 0 / eslint 3+958 / vitest 399/400 基线）
- [x] **E5.7#12** `electron/window-manager.ts`——删 SidebarPool WCV 创建 + 可见性管理（toggleSidebarPool / destroyPool('sidebar')）
  - 🔴 中间态说明：Phase 1 #2 去 zone 路由后，SidebarPool WCV 已无内容可加载（黑条）——本任务物理删除 WCV 即消除中间态。若 Phase 1/2 验证时黑条干扰，可把本任务提前至 Phase 1 与 #4 一起做
  - （执行注 2026-08-13：已按本条中间态说明提前至 Phase 1 与 #4 一起做——commit 26b8af7c。SidebarPool WCV 物理删除：createSidebarPool/toggleSidebarPool 删除，main.ts 只建 MainPool，pool:toggle-sidebar-pool handler + preload-shell 暴露删除，App.tsx 调用删除；SidePanel 壳 DOM 保留隐藏挂载作唯一 Pool 崩溃 fallback（设计 §9.2））
- [x] **E5.7#12.5** MainPool bounds 换主——壳推流删除，主进程接管（🔴 2026-08-13 审计：机制换主无人认领，新增本任务）
  - 主进程增：`createMainPool` 时 `setBounds(窗口内容区)` + `win.on('resize')` → `setBounds` 跟随（模式可抄 [脱出窗口设计.md](脱出窗口/脱出窗口设计.md) 的同款 resize 处理）
  - 壳删：App.tsx 的 updateSize / LayoutEngine bounds 推流 / TITLE_BAR_HEIGHT 偏移数学（E5.6 专属——titlebar 在壳 DOM；E5.7 titlebar 是池内 zone，WCV 满窗零偏移）
  - 死链删：`pool:set-bounds` handler（plugin-view-handlers.ts:144）+ preload-shell.ts:509 暴露 + window-manager `updatePoolBounds`（🔴 零调用死代码，不是"简化"）
  - 验证：窗口拉伸/最大化后 Pool 满窗、无残影、无 30px 错位
  - （执行注 2026-08-14：🔴 提前执行注——按 2026-08-13 审计建议"Phase 2 验证时占位 zone 位移干扰可提前"，经用户确认提前到 Phase 2 与 #5/#6/#8/#9 一起做（WCV 满窗后 zone 渲染位置即最终位置，避免先建 zone 再换 bounds 的二次适配）。commit a04374eb：window-manager 增 syncPoolBounds（窗口内容区 x0/y0）+ 构造函数注册 resize 跟随（dispose 注销成对——rebuildPool 不重复注册）+ createMainPool 创建即 setBounds+setVisible；壳侧 pool:set-bounds handler + preload-shell setBounds 暴露 + App 推流数学（gap/偏移）全删。两处拆分如实记录：① checklist 原话"壳删 updateSize / LayoutEngine 推流"——本 commit 只删推流，updateSize/LayoutEngine 订阅保留到 #9（zoneBounds 壳 DOM 消费方还在，提前删则 MainContent 因 zoneBounds.main 空 unmount、tab 状态逻辑断裂）；② updatePoolBounds 已不存在——Phase 1 #4 已随 zone 去参删除，非本任务遗漏。中间态提示：满窗 WCV 会盖住壳 TitleBar/IconBar/StatusBar——占位 titleBar/iconBar/statusBar zone 顶上，真实顶栏/图标栏/状态栏暂不可见，Phase 2 内 #5/#6/#8 逐个替换占位后恢复）
- [x] **E5.7#13** 侧栏分隔线 CSS 化——4px div + `cursor: col-resize` + mousedown 拖拽
  - 🔴 拖拽期间：池内乐观本地 width（rAF 节流 mousemove），**mouseup 一次性 commit 到壳** → pushLayout 回执（回执与本地一致，无跳变；真相源仍在壳）
  - 🔴 isDragging guard：拖拽期间忽略 pushLayout 的 sidebar.width 旧值（防外部 layout 推送闪跳），拖完放开
  - 🔴 E5.6#22m 防护传承（useDragHandle 活代码，禁止丢）：① body cursor lock——拖拽期间锁 `document.body` cursor/userSelect（快速拖拽脱离 handle 不跳回箭头）② 窗口外释放——`mousemove` 里 `buttons === 0` 视为 `mouseup`（窗口外释放时 `mouseup` 到不了本 window）
  - hover 高亮：`--separator` → `--separator-hover`（transition 0.15s，HandleLine 现有行为传承）
  - 无 IPC setBounds（E5.6 跨 WCV 老路彻底消失）
  - 📖 → [01-极简Pool设计.md §5.3](01-极简Pool设计.md#53-分隔线css-原地分离)
  - （执行注 2026-08-14：commit a50dcf3f。分隔线渲染在 SidebarZone 的 zone 包装内（可见性条件块——visible=false 整体 display:none，分隔线随之消失，审计不变量自动成立）；拖拽状态机 IconBarZone #6 同款模式——乐观本地宽 rAF 节流 + mouseup 一次性 commit `sidebarAction({action:"setSidebarWidth",width})` → 壳 resizeZone 钳制（与 E5 壳 useDragHandle 同款语义）→ onDidChangeLayout → pushLayout 回执。回执判定 = commit 后第一条非拖前宽的推送（壳钳制后值可能 ≠ 本地宽——以壳权威为准；拖回原位时按值相等判回执，防永久待回执）。钳制界壳推：SidebarLayout +minWidth/maxWidth（LayoutEngine dock 声明 170/600）——池本地钳制与壳公式一致零硬编码。折叠态拖拽期间本地宽实时派生（壳 collapsed 重推滞后，折叠→拖宽中途分支实时切换）；.resizing 类关 150ms 过渡（rAF 帧帧改宽防橡皮筋）。#22m 防护：body cursor/userSelect 锁存前值恢复 + buttons===0 窗口外释放。门禁：tsc 0 / eslint 3+958 基线 / vitest 399/400 基线 / grid ✓。补丁 2026-08-14（Phase 3 自检）：+moved 标记——无位移点击（mousedown+mouseup 未动）不 commit，对齐 E5 壳 no-op 语义（否则折叠态 28px 误点分隔线被钳到 170 意外展开））

---

# Phase 4：OverlayWindow 消除

> 目标：ContextMenu / QuickPick / Toast / Dialog 全部 CSS position:fixed。删 OverlayWindow。
> 🔴 2026-08-13 审计定性：OverlayWindow 当前是空壳（overlay-main.tsx 全占位符 + 壳侧 overlay.push 零调用）——本 Phase 的迁移源是**壳 DOM 真浮层**（components/shared/ContextMenu 等），不是 src/overlay/；删除零行为损失。
> 📖 完整设计 → [浮层归一化设计.md](浮层系统/浮层归一化设计.md)

- [ ] **E5.7#25** `src/pool/zones/FloatingLayerHost.tsx`——新建（🔴 自 Phase 6 提前——#14 前置：portal root 不存在 = 菜单点不动）
  - `position: fixed; inset: 0; pointer-events: none` 容器 + 4 个 portal root
  - 📖 代码骨架 → [浮层归一化设计.md §2](浮层系统/浮层归一化设计.md#2-floatinglayerhost浮层容器)
- [ ] **E5.7#26** z-index 层级标准化——`src/constants.ts` 定义 `Z_INDEX` 常量表（🔴 自 Phase 6 提前——#14-17 引用）
  - 100 splitHandle / 200 panelResize / 500 dragPreview / 1000 dropZone / 2000 toast / 3000 contextMenu / 4000 quickPick / 5000 dialog / 6000 tooltip
  - 全量 grep 替换裸数字 → 常量引用
- [ ] **E5.7#14** ContextMenu——portal 到 `#context-menu-root`
  - `window.linkdesk.menu.show(menuId, {x, y}, context)` API 不变
  - 聪慧→哑数据流保留：菜单项解析在壳渲染进程（Registry 所在）/ 渲染在 Pool FloatingLayerHost（哑）/ executeCommand 在壳渲染进程
  - 简化：删 originPool 追踪（单 Pool）；🔴 主题/语言机制拆开写——主题：菜单在 Pool 文档内，继承 Pool 文档 CSS 变量（壳→Pool theme:changed 广播保留，不再单独推给 OverlayWindow）；语言：显示文本铁律（壳侧 t() 解析后字符串推送，池不初始化 i18n）
  - 定位用 clientX/Y 直接定位（无屏幕坐标转换）+ 边界翻转，🔴 翻转钳制 menuTop ≥ 30（TitleBarZone drag 区下沿——硬约束 18，防 OS 截鼠标事件）
  - 🔴 键盘导航：菜单打开后聚焦菜单容器，↑↓ 移动 + Enter 选择 + Escape 关闭（防 E5.6 focusable:false 键盘回归）
  - 📖 → [浮层归一化设计.md §4](浮层系统/浮层归一化设计.md#4-contextmenu)
- [ ] **E5.7#15** QuickPick / CommandPalette——portal 到 `#quick-pick-root`
  - 居中面板 + backdrop + 自动聚焦 + 150ms 防抖
  - 📖 → [浮层归一化设计.md §5](浮层系统/浮层归一化设计.md#5-quickpick--commandpalette)
- [ ] **E5.7#16** Toast——`<ToastHost>` 右下角
  - `position: fixed; bottom: 25px; right: 12px`（状态栏 22px + 3px gap）
  - 栈排列 + CSS 进出动画 + pointer-events: none
  - 📖 → [浮层归一化设计.md §6](浮层系统/浮层归一化设计.md#6-toast)
- [ ] **E5.7#17** Dialog / Modal——portal 到 `#dialog-root`
  - backdrop + 居中 + 焦点陷阱 + Escape
  - 📖 → [浮层归一化设计.md §7](浮层系统/浮层归一化设计.md#7-dialog--modal)
- [ ] **E5.7#18** 旧浮层代码清理——删除对象分两类（🔴 2026-08-13 审计：OverlayWindow 是空壳，真浮层在壳 DOM）
  - 空壳类（删了零行为损失）：`src/overlay/` 整目录（overlay-main.tsx 四浮层全占位 + debugBorder 临时码）、`overlay.html`、`electron/preload-overlay.ts`
  - 真浮层类（迁移源——渲染部分删除）：`components/shared/ContextMenu.tsx`、`components/shared/CommandPalette.tsx`（渲染部分）、App.tsx:427-447 QuickPick 渲染、`components/ToastContainer.tsx`
  - 🔴 壳侧保留清单（聪慧部分）：QuickPickService（LanguagePicker/ThemeBrowser/开发者命令在用）、菜单解析、executeCommand
- [ ] **E5.7#19** `electron/overlay-window.ts`——整文件删除 + window-manager 删 OverlayWindow 创建/管理
  - 🔴 main.ts 连带删除：`resize/move → overlayWindow.syncBounds()` 监听器（⚠️ 与 #12.5 的 MainPool resize 跟随是两个监听器，删时分清）+ `overlay:forward-to-overlay` / `overlay:forward-to-shell` 两个中继
  - preload-shell.ts `overlay` 命名空间暴露删除（并入清理方案 §2 preload-shell 行）

---

# Phase 5：Zone 分解——MainRenderer 瘦身

> 目标：693 行 MainRenderer 分解为独立 Zone 文件。

- [ ] **E5.7#20** `src/pool/zones/MainZone.tsx`（主区）——从 MainRenderer 提取（🔴 2026-08-13 审计：吸收 #7 TabBarZone；EditorZone 更名 MainZone——主区装任何插件标签页，不叫 Editor）
  - 职责 = MainRenderer 693 行**行为零丢失**提取，验收逐项对照现有行为 13 项：① computeLayout 分屏面板绝对定位平级渲染（B22 防护，key=groupId 不变）② 每 panel 自带 GroupTabBar（pool/shared/ 复用——tab bar 留在 panel 内）③ keep-alive CSS display 切换 ④ `shellRendered → ShellViewRenderer` 分支（welcome/插件详情/输出）⑤ 空态"没有打开的标签页" ⑥ 单面板 flex fallback（叶子数=1 不绝对定位）⑦ 分隔线拖拽——乐观本地 sizes + mouseup commit tabAction（#13 同款模式）+ 双击 50/50 重置 + hover accent ⑧ 同组拖拽乐观重排 dragLocalTabs ⑨ 跨 group 移动 / 拖到面板边缘分屏（drop zone Glassmorphism）⑩ drag preview portal（document.body）⑪ creatableViews [+] 动态菜单 ⑫ ErrorBoundary 用池侧版（🔴 现 import `components/shared/ErrorBoundary` 是壳目录——迁 `src/pool/shared/`）⑬ zIndex 9999/99999 → `Z_INDEX.dropZone` / `Z_INDEX.dragPreview` 常量
  - 内联逻辑原样提取（useDragReorder 275 行 15 轮 bug 验证——不重写）；useSplitResize / useTabDropPreview 拆分是可选重构，非本任务
  - 拖出窗口检测：Phase 8 #33 useDragDetach 建成后在此接入（原 #7 占位迁此）
  - 📖 职责 → [Zone分解设计.md §2.5](Zone系统/Zone分解设计.md#25-mainzone)
- [ ] **E5.7#21** `src/pool/zones/PanelZone.tsx`——新建（greenfield 骨架——🔴 2026-08-13 审计）
  - PanelTabBar 28px + keep-alive + 视图动态加载占位（🔴 2026-08-13 审计：不建 PANEL_VIEWS 静态表——写死 pluginId 违反插件独立铁律 + 硬约束 10；动态加载归 #63.7，PluginComponent 同款）
  - 🔴 骨架优先：无壳侧生产者（bottom-panel 贡献路由 / panel 数据填充归 Phase 12 #63.7）——本任务只建渲染骨架，`layout.panel` 无数据时条件渲染永假
  - 高度拖拽：顶部 resize handle 用 #13 同款模式（乐观本地 + mouseup commit，真相源在壳）
  - 插件贡献面板视图（E5.6 的 registerPanelView 动态注册）→ Phase 12，本任务只有静态表
  - 📖 → [Zone分解设计.md §2.6](Zone系统/Zone分解设计.md#26-panelzone)
- [ ] **E5.7#22** `src/pool/zones/RightSidebarZone.tsx`——新建（greenfield 骨架）
  - 和 SidebarZone 同构（复用 pool/shared/ 的 PoolSectionStack / PoolToolbarSlot——#11 已迁入），默认隐藏
  - 左侧 4px resize handle——#13 同款模式（乐观本地 + mouseup commit）
  - 无壳侧生产者（rightSidebar 数据填充归 Phase 12）——本任务只建骨架
  - 📖 → [Zone分解设计.md §2.7](Zone系统/Zone分解设计.md#27-rightsidebarzone)
- [ ] **E5.7#23** ~~`src/pool/zones/TopBarZone.tsx`~~ 🔴 已取消（2026-08-13 审计）——与 V3-部件命名规范 ② 顶栏（Phase 4 已移除部件）重名；面包屑不在区域清单、无数据生产者
  - 连带：#1 的 PoolLayout `topBar` 字段删除；面包屑若做 → 并入 MainZone 每 panel（同 GroupTabBar 自包含原则）
  - 📖 论证 → [Zone分解设计.md §2.9](Zone系统/Zone分解设计.md#29-topbarzone)（墓碑）
- [ ] **E5.7#24** `src/pool/main/MainRenderer.tsx`——删除（已分解入 MainZone #20）
  - 🔴 删除前对照 #20 验收 13 项，逐项在 MainZone 找到对应实现；`src/pool/main/` 随之整目录删除（panelViews 在 pool/views/ 不受影响）

---

# Phase 6：浮层收尾

> #25（FloatingLayerHost）/ #26（Z_INDEX 常量表）已提前至 Phase 4（#14 前置——2026-08-13 审计：portal root 不存在则菜单点不动）。

- [ ] **E5.7#27** pointer-events 管理——浮层显示时各自 `pointer-events: auto`；Toast 容器始终 none
  - 🔴 2026-08-13 审计：原稿"Toast 始终 none"字面执行 = 通知本体也穿透 → 关闭/操作按钮点不了。现网正确行为照搬 ToastContainer.css:24/27/58——容器 none / `.toast-container *` auto / 退场 none
  - 机制已定死（浮层归一化设计.md:59——容器默认穿透、浮层各自 auto）；现网样板 SelectBox.css:147 / ColorPicker.css:91 / ProgressBar.css:19-23 各写各的——执行 = 归一化进 FloatingLayerHost，非新发明

---

# Phase 7：分隔线简化

> 🔴 2026-08-13 审计：分隔线实现已随 #13/#20/#21 提前完成——本 Phase 从"实现"改为"验证 + Z_INDEX 常量应用 + 壳侧残留清理"。**分隔线不变量：分隔线活在区域的可见性条件块内——两个区域都在时自动出现，区域消失分隔线消失**（见 01-极简Pool设计 §5.3）。

- [ ] **E5.7#28** Sidebar ↔ MainZone 分隔线——🔴 审计改定位：验证 #13 产物（实现已在 Phase 3 完成，本任务不新写代码）
  - 验证：分隔线在 `sidebar?.visible &&` 块内（侧栏消失分隔线消失）；拖拽 + commit + isDragging guard + cursor lock + 窗口外释放全部可用
  - 壳侧旧实现确认：SplitHandles.tsx 已删（清理方案 §3）
- [ ] **E5.7#29** MainZone 分屏分隔线——🔴 审计改定位：验证 #20 ⑦ 产物（实现已随 MainZone 提取完成）
  - 残余增量：handle zIndex 裸数字 100 → `Z_INDEX.splitHandle`（#20 ⑬ 只覆盖 dropZone/dragPreview，未覆盖 handle）
  - B22 防护保持：面板绝对定位，key=groupId 不变；单面板 flex fallback 零 handle（自动出现不变量）
- [ ] **E5.7#30** PanelZone resize handle——🔴 审计改定位：验证 #21 产物（高度拖拽已随骨架完成）
  - 增量明确：`cursor: row-resize`（#13 是 col-resize）+ 顶部 4px div + `Z_INDEX.panelResize`（200）常量应用
- [ ] **E5.7#31** 壳侧分隔线体系零残留确认（🔴 审计改清单——OverlayWindow 空壳无真实分隔线代码，真残留是壳侧三件套）
  - SplitHandles.tsx 整删（清理方案 §3）
  - useDragHandle 已迁 `src/pool/hooks/`（清理方案 §3）
  - 🔴 LayoutEngine 整删——`core/services/LayoutEngine.ts` + `core/__tests__/LayoutEngine.test.ts`（zone 布局 E5.7 由池内 flex 接管；#12.5 只删了 bounds 推流用法，文件本体无人认领）；连带清消费者 import：App.tsx / SidePanel / SplitHandles / ShellEvents / usePoolSync / App.css 注释
  - 🔴 同簇死代码（2026-08-14 Phase 3 自检补记——SidePanel 卸载后成死簇，随 LayoutEngine 一起扫）：`SidePanel.tsx`（零挂载点，订阅永不注册）+ `components/shared/SectionStack.tsx`/`ToolbarSlot.tsx`（唯一消费者 SidePanel）+ `components/shared/viewDragState.ts`（getDraggingView 零读者，setDraggingView 唯一写者是已死的壳 SectionStack——E4V#48 池版已用 dataTransfer MIME 替代）
  - grep 验证：`SplitHandles` / `LayoutEngine` / 壳侧 `useDragHandle` 零残留（池侧新写法除外）

---

# Phase 8：突破边界——脱出窗口 + 漂移面板

> 🔴 **2026-08-13 审计 + 用户决策：整个 Phase 8 推迟到上架后 v1.3——版本更新点，不在 E5.7 范围。** 路线图槽位：05-版本更新/00-README.md `v1.3 悬浮窗 A（可拖出标签页）`。E5.7 发布 = 单窗口——window:* IPC 硬编码 mainWindow、Phase 10 单 Pool 简化、#36 单窗口崩溃恢复全部成立，无需多窗口改造。
> 📖 完整设计（保留为 v1.3 设计稿 + 审计发现）→ [脱出窗口设计.md](脱出窗口/脱出窗口设计.md)

- ~~E5.7#32~~ 🔴 推迟 v1.3——`electron/detached-window.ts`（`createDetachedWindow` / `closeDetachedWindow`）
- ~~E5.7#33~~ 🔴 推迟 v1.3——`src/pool/hooks/useDragDetach.ts`（拖拽出窗检测）
- ~~E5.7#34~~ 🔴 推迟 v1.3——漂移面板（PanelZone `⤢` → `detachPanel()`）——与拖出共享多窗口机械，不单独做
- ~~E5.7#35~~ 🔴 推迟 v1.3——新窗口 API `window.linkdesk.window.open(layout)`——多窗口基础设施整体后移

> 🔴 **v1.3 开工前必读（2026-08-13 审计发现，详见脱出窗口设计.md 头部）：**
> ① **状态迁移空白**——拖出 = 重挂载（插件状态全在 pool renderer，跨 WCV 不可转移）；Hot Exit 落盘兜底 + pluginState 恢复模板是必修课。VS Code 对照：普通编辑器模型可转移（1.85+）、webview 只能销毁重建
> ② **壳想池画数据流**——tabState 在壳渲染进程；detach 流程 = pool 手势 → 主进程 → 壳 tabState 更新 → 壳 pushLayout 两窗口；主进程禁止直接 buildLayout
> ③ **VS Code 机制**——原生 HTML5 拖拽 + "释放时判断"（isWindowDraggedOver 标志 + 释放点 bounds 防误触发），拖出再拖回 = 完全取消；手写体系下 e.screenX/screenY 可用，照抄该语义（"过线 30px 立即 detach"不可取）
> ④ **多窗口连带**——window:* / pool:ready / broadcast / keyboard-router 按 sender 窗口路由；崩溃恢复覆盖脱出窗口（崩 → 关窗 + tab 回归主窗口）

---

# Phase 9：崩溃恢复——单点 + 快速重建

> 📖 完整设计 → [单Pool崩溃恢复设计.md](崩溃恢复/单Pool崩溃恢复设计.md)

- [ ] **E5.7#36** `electron/crash-recovery.ts`——新建
  - `app.on('render-process-gone')` → 判断 Pool WCV → 重建 WCV（BrowserWindow + 壳渲染进程存活）
  - 壳渲染进程崩溃分支——`webContents === BrowserWindow.webContents` → 全窗口重建（销毁旧窗 → `createMainWindow()` → workspace 持久化恢复 tabState → lastLayout 兜底回放）
  - 保存/回放 `lastLayout` 快照（每次 pushLayout 时保存）
  - 🔴 2026-08-13 审计补 3 条验收：① 壳崩重建**不可直接复用 createMainWindow**——内部无条件 `ipcMain.on`（overlay 中继等）+ 建 WCV，二次调用 = IPC 重复注册抛异常 + 双份 WCV；全部 IPC 注册 once-guard 或拆 createWindow/registerIpc ② `details.reason === 'clean-exit'` 跳过 + before-quit 守卫（应用退出也触发 render-process-gone，不拦 = 退出过程中建 WCV）③ pool:ready 等待挂 did-fail-load / 超时 → 重试并走 #39b 崩溃计数
  - 📖 代码 → [单Pool崩溃恢复设计.md §2](崩溃恢复/单Pool崩溃恢复设计.md#2-实现)
- [ ] **E5.7#37** 心跳——5s ping / 10s 超时 → forcefullyCrashRenderer → 触发重建
  - 🔴 2026-08-13 审计补：preload-pool.ts 池命名空间现无 ping/pong——需补 `pool.onPing` 注册回 pong；与 E2a 壳心跳（`app:heartbeat` 30s → 原生对话框）**并行互不替代**——壳持 tabState，壳心跳保留
- [ ] **E5.7#38** Hot Exit 实现
  - Monaco 脏内容异步落盘 → 重建后恢复 + dirty 标记
  - 🔴 2026-08-13 审计已确认未实现（src 零实现）——本任务新增实现
  - 🔴 写盘路径：Monaco 在 pool renderer（沙箱无 fs）——脏内容落盘走现有 filesystem 命名空间或主进程 handler，禁止渲染进程直写 %APPDATA%
  - 📖 → [单Pool崩溃恢复设计.md §3.2](崩溃恢复/单Pool崩溃恢复设计.md#32-hot-exit-机制)
- [ ] **E5.7#39** 内存监控——🔴 2026-08-13 审计改定位：改造现有 `checkMemoryPressure`（window-manager.ts:278——已是 30s 采样 + 1GB 阈值 + toast）→ 单 Pool 版（删多 WebView PID 集合，直接匹配 pool WCV）；API 用 `app.getAppMetrics()`（getProcessMemoryInfo 在 Process 上，不在 WebContents）
- [ ] **E5.7#39b** 连续崩溃防护——10s 内 3 次 → 停止重建 → 静态错误页

---

# Phase 10：清理死代码

> 📖 完整清单 → [清理方案.md](清理/清理方案.md)

- [ ] **E5.7#40** 删 per-tab 遗留文件
  - ✅ `src/hooks/useWebViewSync.ts` 已删（2026-08-13 审计——树中已无，仅注释残留）
  - `git rm src/components/ShellPluginComponent.tsx`
  - `git rm plugin-view.html` + `plugin-shell.html`（后者是孤儿——主进程零引用）
  - `git rm src/plugin-shell-main.tsx`（两页入口，页删后成孤儿）
  - 清理残留引用（grep 确认零残留）
- [ ] **E5.7#41** `electron/window-manager.ts`——删多实例 + 双Pool 逻辑
  - 删 `pluginViews` Map / `rekeyInstance` / `findInstanceInGrace` / `graceTimers` / `scheduleViewDestroy` / `cancelViewDestroy` / `getAllInstanceIds` / `cleanupCrashedView`
  - 删 `sidebarPoolView` 管理（SidebarPool 已合并）
  - 删 `createPool()` / `destroyPool()` / `rebuildPool()` / `updatePoolBounds()`
  - 保留：单 WCV 创建 + `pushLayout()` + `toggleDevTools()`
- [ ] **E5.7#42** `electron/plugin-view-registry.ts`——删多实例路由方法
  - 删 `registerPlugin` / `unregisterPlugin` / `setBounds` / `setVisible` / `scheduleDestroy` / `cancelDestroy` / `reloadPlugin` / `getView` / `isRegistered` / `getAllInstanceIds` / `getInstanceIdsForPlugin` / `rekeyInstance`
- [ ] **E5.7#43** IPC handlers + bridge 清理
  - `electron/ipc/plugin-view-handlers.ts`——删 9 个 handler（plugin-view:create 等）
  - `electron/ipc-bridge.ts`——broadcast 改单 Pool `webContents.send()`；`requestToPlugin(instanceId)` → `requestToPlugin(tabId)`
  - `electron/keyboard-router.ts`——删 `getAllInstanceIds()` 遍历 → 单 Pool 直推
- [ ] **E5.7#44** preload 清理
  - `preload-pool.ts`——删 `pluginInstance` 命名空间 + `notifyReady`
  - `preload-shell.ts`——删 `pluginViews` 命名空间（10 个方法）
  - `preload-shell.ts`——langDef / encoding 临时池侧补丁删除（E5.6#11.5"MainPool 不存在期间 editor/file-tree 在壳渲染"；E5.7 壳不再渲染插件 → 死代码）；commands/workspace 按壳自身使用逐项判断（2026-08-13 审计）
  - `git rm electron/preload-plugin.ts`（per-tab 插件 WebView preload——E5.7 无消费者）
  - `git rm electron/preload-overlay.ts`（OverlayWindow preload——随 #19 删除）
  - 同步 event-system.ts / preload-shell.ts 头注释里的 preload-plugin 引用
- [ ] **E5.7#45** ESLint 机械防线——`no-restricted-syntax` 拦截 `OverlayWindow` / `sidebarPoolView` / `pluginViews` / `instanceId` 字面量
- [ ] **E5.7#45.5** 仓库根杂物归位（2026-08-13 审计——清理方案 §6）
  - `git rm src-tauri/`（只剩 target 编译产物）+ `impeccable.zip`
  - `memory/` 2 个笔记归档 docs/；`shared/`（5 处引用全在 electron 侧）并入 `electron/constants.ts` 后删目录
  - `src/core/services/pathUtils.ts` + `assetPath.ts` 迁 `src/core/utils/`（纯函数——壳目录规范 §1）

---

# Phase 11：Registry 主进程化——消灭跨进程数据隔离

> 📖 完整设计 → [Registry主进程化设计.md](Registry主进程化/Registry主进程化设计.md)
> **根因：** TS 模块在 Electron 多进程各自实例化——壳注册的 Registry 数据主进程读不到。E5.7 虽然单 Pool，但主进程/renderer 仍是两个进程，`plugins:call` 代理补丁依然存在。

- [ ] **E5.7#46** 审计——全量 Registry 数据流摸底
  - grep `src/core/registry/` + `src/core/services/FileAssociationService.ts` 下模块级可变状态 → Registry 清单（2026-08-13 审计：FileAssociation 在 services/ 不在 registry/——漏掉会漏掉 #50 要迁的对象）
  - 标注每个的写入方/读取方/当前状态（✅ 已代理 / ⚠️ 未知 / 🔴 已知 bug）
  - 产出 Registry×进程 矩阵表 → 存入本目录
- [ ] **E5.7#47** 设计——主进程 plugin.json 预加载方案
  - 启动时序 / loader.ts 增量同步 / preload 直接 IPC / 迁移优先级 / 回退方案
  - 🔴 写入方唯一性定死（2026-08-13 审计）：静态数据→主进程唯一写入方（扫盘 + 热装/卸重扫 + pool 运行时注册直接 IPC 汇入）；JS 函数→所在进程内——禁止双写
  - 📖 方案骨架 → [Registry主进程化设计.md §2](Registry主进程化/Registry主进程化设计.md#2-方案主进程-pluginjson-预加载)
- [ ] **E5.7#48** `electron/plugin-manifest-loader.ts`——新建
  - 扫描 `plugins/builtin/` + `plugins/user/` → 读 plugin.json → 调各 register 函数
  - 扫描路径复用 `src/core/pluginPaths.ts`（PLUGIN_SUBDIRS + pluginJsonGlobPatterns——硬约束 12），dev/packaged 两态路径策略写清（2026-08-13 审计）
  - 注册幂等——主进程预加载 + 壳 loader.ts 双注册：重复注册覆盖不叠加
  - `electron/main.ts` `app.whenReady()` 中调用（createMainWindow 之前）
  - 错误隔离——单插件解析失败不阻断其他
- [ ] **E5.7#49** LangDef + Protocol 迁移
  - `preload-pool.ts`——langDef/protocol 从 `plugins:call` 改直接 IPC
  - `IpcBridgeHandler.ts`——删对应 case
  - 🆕 新建 `electron/ipc/registry-handlers.ts`——langDef:get / protocol:listProtocols 主进程侧注册（直接 IPC 的接收端；设计 §4"合并到统一的 registry-handlers"本任务认领，2026-08-13 审计）
  - 壳侧 `loader.ts` 注册移除（loader.ts:610 registerLangDef 等——写入方收敛主进程，2026-08-13 审计）
  - 验证：编辑器打开 .py → LSP 启动；串口协议下拉正常
- [ ] **E5.7#50** FileAssociation 迁移
  - `plugin-manifest-loader.ts` 加 `contributes.fileAssociations` 解析
  - preload 改直接 IPC + IpcBridgeHandler/PROXY_CHANNELS 删 case
  - 壳侧 `loader.ts` 注册移除（loader.ts:591 registerFileAssociation——写入方收敛主进程，2026-08-13 审计）
  - FileDecorationRegistry **不迁**——provider 是 JS 函数，真源与消费方同进程（E5.7 = Pool），不进 IPC（2026-08-13 审计：原稿"plugins:call 永久正确"是 E5.6 残影）
- [ ] **E5.7#51** 删死文件——`electron/ipc/lang-def-handlers.ts` + `protocol-handlers.ts`
  - 连带：main.ts:20-21 import + main.ts:85-86 注册调用删除，grep 零残留（2026-08-13 审计）
- [ ] **E5.7#52** ESLint 机械防线——`no-restricted-imports`：`electron/` 下禁 import 壳侧 Registry 模块（`import type` 除外）
- [ ] **E5.7#53** 全量回归验证——LangDef / Protocol / FileDecoration / FileAssociation / 壳侧自身功能（命令执行/侧栏/tabState）不受影响 / npm run check

---

# Phase 12：API 补全

> 📖 完整清单 → [API补全/API变更清单.md](API补全/API变更清单.md)
> 目标：消灭插件 `import @src/core`——每个命名空间补全对应几处 import。
>
> 🔴 **E5.7 结构性依赖：** 插件 React 模块现在**只在 Pool 渲染进程导入**（壳不再渲染任何插件）。插件模块顶层的副作用注册（`registerCommand` 32 处等）发生在 Pool 进程——但 CommandRegistry 在壳渲染进程。**插件注册必须全部走 `window.linkdesk.*` IPC，否则命令/菜单/快捷键在壳侧注册表里是空的。** 本 Phase 不只是清理 import——是极简Pool 的功能正确性前提。E5.6 时代的 `plugins:call` 代理是补丁，本 Phase 是根治。

- [ ] **E5.7#54** `pool.*` 命名空间简化
  - 删 `poolId`/`zone` 参数（ready() 不再带 zone）
  - 🔴 保留 `sidebarAction` / `tabAction` 意图通道——GroupTabBar/MainRenderer/SidebarRenderer/WelcomePoolView ~15 处调用，tabs.* 覆盖不了 pinTab/拖拽/分屏（2026-08-13 审计：原稿"只留 onLayout + ready"会删掉活代码）
- [ ] **E5.7#55** `linkdesk.workspace`——验证 6 方法已在位（E5.6#11.5a 已补 preload-pool.ts:250-263）+ 残余清理（2026-08-13 审计：原"消 7 处 import"数字过期——plugins/ 实测零 workspace import）
- [ ] **E5.7#56** `linkdesk.commands.registerCommand`——IPC 版已存在（preload-pool.ts:114）→ 剩余：marketplace/src/index.tsx:15 迁移 window.linkdesk.commands（2026-08-13 审计：实测全仓库插件运行时 import 仅此 1 文件 2 处）
- [ ] **E5.7#57** ~~`linkdesk.fileAssociation`~~ 已并入 Phase 11 #50（getPluginFor 已存在 preload-pool.ts:462，"改直接 IPC"Phase 11 认领——2026-08-13 审计删除本任务）
- [ ] **E5.7#58** `linkdesk.viewContainer`——no-op 桩换真 IPC 查询（现 preload-pool.ts:506-511 registerView 空函数 + getView 返回 null；改 getViews/getViewContainer 问壳侧注册表）
- [ ] **E5.7#59** `linkdesk.events`——验证 on/emit 已在位（event-system.ts:33-67，off = on 返回的 dispose）+ 残余清理（2026-08-13 审计：原"消 3 处 import"数字过期）
- [ ] **E5.7#60** `linkdesk.decorations`（现网名——插件已调用 FileTreeDecoration.ts:28）——registerProvider 按需补；provider 与消费方同在 Pool（Phase 11 §3 已标）（2026-08-13 审计：API 文档名 fileDecoration 与现网不符，按现网名）
- [ ] **E5.7#61** `linkdesk.protocol`——验证 listProtocols/getActiveProtocolId/setActiveProtocolId 已在位（preload-pool.ts:535，Phase 11 #49 改直接 IPC）；registerParser/getParsers/getDefaultFor 按需——现网零插件 import（2026-08-13 审计）
- [ ] **E5.7#62** 零散命名空间验证——现网 6 个已在位（keybindings:325 / clipboard:243 / langDef:515 / encoding:496 / search:469 / contextKey:386），sidebar/data 现网无此命名空间（按需删条目）（2026-08-13 审计：原"消 8 处 import"数字过期）
- [ ] **E5.7#63** `linkdesk.quickPick` 命名空间——`show(opts)` → `Promise<selected>`（现网 preload-pool 无此命名空间，真新建）
  - 🔴 机制（2026-08-13 审计）：E5.7 壳无 UI——picker 必须池内渲染（FloatingLayerHost 的 QuickPickHost）；preload.show() ↔ 池内 UI 桥接走 contextBridge 函数代理（池 main 世界注册 hostFn → 隔离世界存储调用）
- [ ] **E5.7#63.5** 🔴 `filesystem` 路径守卫——主进程 file-handlers 零路径校验（插件可 `filesystem:remove('C:\\任意路径')`）。normalizePath 归一化 + 危险目录写拒绝 + workspace 外写操作用户确认 dialog + 越界尝试日志。📖 设计 → [01-极简Pool设计.md §8.1](01-极简Pool设计.md#81-filesystem-路径守卫沙箱唯一故意开口)
  - 🔴 preload-pool.ts:217 注释"主进程校验路径"与事实相反（2026-08-13 审计实测：file-handlers.ts 14 个 handler 零校验）——执行时删除该注释或改为真实描述
- [ ] **E5.7#63.6** IPC 通道名集中化——新建 `electron/ipc/channels.ts`，main + preload 双端 import
  - 2026-08-13 审计：`filesystem:readTextFile` / `config:get` / `plugins:call` 等 50+ 通道名在 `ipcMain.handle(...)` 与 `ipcRenderer.invoke(...)` 各写一遍裸字符串，无联动——改一端另一端静默失效
  - 时机：Phase 12 全部命名空间补全/重注册后（#54–#63.5）——通道名收敛为常量，此后改名只改一处
  - 验证：grep 确认 `ipcMain.handle` / `ipcRenderer.(invoke|send|on)` 参数不再含裸字符串（除 channels.ts 定义处）
- [ ] **E5.7#63.7** PanelZone 数据生产者三件套（2026-08-13 审计——Zone分解设计 §2.6 承诺"归 Phase 12"无人认领，本任务补上）
  - ① 壳侧 bottom-panel 贡献路由——ViewContainerService `location:"panel"` 查询 → `panel.views[]` 入 pushLayout
  - ② PanelZone 动态组件加载——PluginComponent 同款，按 pluginId + render 动态 import（禁止静态 PANEL_VIEWS 表——插件独立铁律 + 硬约束 10）
  - ③ 面板高度持久化（workspace.json）
  - 验证：第三方插件 plugin.json 声明 `location:"panel"` 视图 → 面板出现其标签，池代码零改动

---

# Phase 13：硬编码消灭

> 📖 完整方案 → [硬编码消灭/硬编码清扫方案.md](硬编码消灭/硬编码清扫方案.md)

- [ ] **E5.7#64** `MenuRegistry.ts`——MenuId enum → string + MENU_SLOTS 常量
- [ ] **E5.7#65** factoryRole 类型——闭合 union → string
- [ ] **E5.7#66** `plugin.schema.json` 枚举全量审计——4 处 enum→string / 9 处保留 / 1 处加值（2026-08-13 审计：实测 17 个 enum，doc 数字过期且 4+9+1=14 自相矛盾——执行时重数）
- [ ] **E5.7#67** `tabIdentity.ts`——FALLBACK_META 清理
- [ ] **E5.7#68** `PoolZoneShell.tsx`——壳级硬编码 pluginId 抽常量
- [ ] **E5.7#69** `plugin-file-service.ts`——主进程硬编码插件子目录 → 扫描所有子目录
  - 🔴 连带（2026-08-13 审计）：`electron/protocol.ts:69-71` plugin:// 协议解析同样写死 builtin/user 两分支——不同步开放 = 新子目录插件装上后加载不了
  - 连带：`src/core/pluginPaths.ts` PLUGIN_SUBDIRS 两端同步（dev 模式 Vite glob）；安装目标 `'user'`（plugin-file-service.ts:118/182）收敛为命名常量
- [ ] **E5.7#70** `IpcBridgeHandler.ts`——壳级硬编码参数化（2026-08-13 审计实测：一处 :247 `type || "editor"` 未知类型兜底；执行时全文件复核原稿"两处"）
- [ ] **E5.7#71** `MainZone.tsx`——SHELL_VIEWS 裸字符串 → 常量表
- [ ] **E5.7#72** `plugin.schema.json`——additionalProperties: false → true（2026-08-13 审计：执行时先确认根级是否存在——grep 到的 3 处全在嵌套层 113/129/147）
- [ ] **E5.7#73** `dialog.openFile` 暴露
- [ ] **E5.7#74** `ConfigurationRegistry.ts`——renderHint/uiHint 闭合 union → string
- [ ] **E5.7#75** `ipc-bridge.ts`——PROXY_CHANNELS 注册门诊断日志
- [ ] **E5.7#76** `preload-pool.ts`——configuration 设置页专用方法注释标注
- [ ] **E5.7#77** 全量 grep 确认——零 pluginId 硬编码

---

# Phase 14：缩放联动 + ESLint

- [ ] **E5.7#78** `IpcRelay<T>`——缓冲+回放模式归一化（2026-08-13 审计：归一化 3 处——preload-pool `_layoutBuffer` 1 / preload-shell `_bridgeRequestBuffer`+`_readyBuffer` 2；随 Phase 10 删除消失 3 处——preload-overlay `_commandBuffer` 随 #19/#44、preload-plugin 2 处随 #44 git rm——不归一化）
- [ ] **E5.7#79** 缩放联动——单 Pool 简化版
  - 🔴 2026-08-13 审计：zoom 现网零实现（唯一痕迹 event-system.ts:110 PUSH_CHANNELS 死通道名）——"简化"是相对 E5.6 纸面设计（映射表 3.5 全"适配搬"），实际从零建
  - Ctrl+/- 在 PoolZoneShell 直接处理（无跨进程转发；池侧 preload webFrame.setZoomFactor 直接生效）
  - 🔴 持久化路径定死：pushLayout 壳→池单向，池内 Ctrl+/- 改的 zoom 回不去壳——池侧 localStorage 或意图通道回壳，二选一执行时定死（默认 localStorage，最简）
  - zoom 状态持久化 + Monaco 独立缩放
- [ ] **E5.7#80** ESLint 升级——`no-core-import-in-plugin` warn → error
  - 修复所有现存 warning（来自 Phase 12 消灭的 import）
  - 🔴 升级前先给规则补测试豁免：filename 含 `__tests__`/`.test.`/`.spec.` 跳过（vitest 单进程，多 WebView 理由不成立）——否则 FileTreeClipboard.test.ts:8 运行时 import ContextKeyService（B 类已记录例外）直接炸 check
  - 🔴 规则现无 importKind 判断——`import type` 同样会报。升级时定死：纯类型 import 豁免（类型擦除后零运行时耦合）——豁免后 FoldersView.tsx:10 / FileTreeModel.ts:11-12 等纯类型行不算 warning，无需"修复"

---

# Phase 15：E6 前置——插件分发地基（IPC 骨架）

- [ ] **E5.7#81** `PluginInstallService`——IPC 骨架
  - 2026-08-13 审计：现网链路已通（E3a #31）——SearchView.tsx:25 lk.pluginManager.install → preload-pool.ts:291 plugins:call('install', path) → IpcBridgeHandler:405 → plugin-file-service.ts:100（消毒已有：distribution 强制 user / core 强制 false）。非"建骨架"——真实差距 = 包装：校验/进度/版本处理；安装只收 path（preload-pool.ts:291）
- [ ] **E5.7#82** `loader.ts`——E6 打包格式分支
  - 分支点 = 插件入口解析：dev 走 Vite import.meta.glob（loader.ts:55-102 构建时展开）vs E6 打包走 dist/plugins/<id>.js 目录扫描 + 动态 import——两套入口映射合一；plugin:// 协议对打包格式的适用性一并验证
- [ ] **E5.7#83** IPC 广播——安装/卸载通知到唯一 Pool
  - 🔴 2026-08-13 审计：plugin:installed/plugin:uninstalled 全仓零 emit——死通道（仅 event-system.ts:108-109 警示列表留名）。映射表"全部Pool→唯一Pool 微调"不成立——广播今天不存在，是新建
  - 新建 emitter：主进程 plugin-file-service install/uninstall 完成处发事件（Phase 11 单写入者一致）→ IpcBridge.broadcast 到唯一 Pool
  - 分发管道已通一半：ipc-bridge.ts:321/336 poolView 推送已有 + preload-pool.ts:55 监听已有 → #83 只缺"发"这一头
  - 消费方验证：marketplace 装完改走全局事件（内部 mini-emitter 徽标逻辑 marketplaceShared.ts:82 保留）

---

# Phase 16：全量回归

> 📖 验证矩阵 → [回归验证/全量回归矩阵.md](回归验证/全量回归矩阵.md)

- [ ] **E5.7#84** 场景 1：侧栏交互——图标切换/折叠展开/拖宽/视图切换
- [ ] **E5.7#85** 场景 2：编辑器——打开/切标签/关标签/脏标记/F12
- [ ] **E5.7#86** 场景 3：分屏——左右/上下/拖拽分屏/合屏/深度限制
- [ ] **E5.7#87** 场景 4：浮层——右键菜单（文件树/编辑器/标签页）+ 命令面板 + Toast + Dialog
- [ ] **E5.7#88** 场景 5：底部面板——打开/切换/新建/拖高/Toast 位置
- [ ] **E5.7#89** 场景 6：串口监视器——多会话/收发/右键/协议下拉
- [ ] **E5.7#90** 场景 7：插件热重载——修改代码/重载/新装
- [ ] **E5.7#91** 场景 8：崩溃恢复——kill renderer → 重建 + layout 恢复 + Hot Exit
- ~~E5.7#92~~ 🔴 推迟 v1.3——场景 9 脱出窗口（拖出/独立编辑/关闭回归/漂移面板）：Phase 8 已整体推迟（#32-#35），E5.7 发布不测；回归矩阵场景 9 保留为 v1.3 验证稿（2026-08-13 审计）
- [ ] **E5.7#93** 场景 10：收尾检查——tsc 零错误 + ESLint 零新增 + vitest 基线 + grep 零残留 + 进程数 = 3（主 + 壳 + Pool） + F5 刷新

---

## 迁移统计

| 指标 | E5.6 已完成 | E5.7 目标 | 变动 |
|:--|:--|:--|:--|
| BrowserWindow | 2 (Main + Overlay) | 1 | −1 |
| WebContentsView | 3 (Sidebar + Main + Overlay) | 1 | −2 |
| 渲染进程 | 4 | 2（壳状态 + Pool 渲染） | −2 |
| 插件代码改动 | 0 | 0 | 0 |
| 净删代码 | ~480（E5.5→E5.6） | ~1,400（E5.6→E5.7） | |
| PoolLayout version | 1 | 2 | 全量字段 |
| 任务数 | 86 主任务（16 Phase） | 94 主任务（16 Phase） | 继承 E5.6 未完成部分 |

---

> 📖 **架构设计：** [01-极简Pool设计.md](01-极简Pool设计.md)
> 📖 **任务迁移映射：** [迁移/E5.6到E5.7任务映射.md](迁移/E5.6到E5.7任务映射.md)
> **← 上一 Phase：** E5.6 双Pool（已封存，执行到 51%）
> **→ 下一 Phase：** E6 插件生态与发布
