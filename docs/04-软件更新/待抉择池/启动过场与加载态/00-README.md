# 04 待抉择池 · 软件启动过场与加载态

> 2026-09-26 立案（用户）。**状态：待拍板**——只登记设计与现状读码，**未开工、零代码改动**。
> 归属：**04-软件更新 / 软件本体**（主进程建窗 + 壳启动管线 + 池哑渲染，**零插件参与**——`theme-defaults` 不用改）。
> 本项按用户要求**建子夹成套**（设计文档 + HTML 参考图），同 [`../关于LinkDesk标签页重设计/`](../关于LinkDesk标签页重设计/00-README.md) 的规格。
> 🔴 **显式声明：这不是新能力**——没有新 API、没有新 IPC 通道、没有新宿主面；改的是**启动时序与过场观感**（`check-design-flow` ③ 判的是 `docs/02-Electron架构/` 下的新能力文档，本项不在其射程；此处仍按惯例声明，免得下一个人来猜）。
> 🔴 **用户拍板（2026-09-26 二轮）：三案合并立项，不拆单**——「如果只有一个加载态的修改，难免有点少」。故 ④A 主题快照先行 + ④B 首推延后 / 池首帧过场 + ④C 内容加载骨架合为一项，④D 插件并行分波加载作**可选**工作流随行（见 01-设计 §4.4）。

| 文件 | 是什么 |
|:--|:--|
| [01-设计.md](./01-设计.md) | 设计文档：**现状启动六幕读码（带 `file:line`，下一棒必读 §二）** · 设计原则 · 合并方案 ④A/④B/④C/（④D 可选）· 落点清单 · 跨仓后果 · 用户视角验收 · 待拍板 · 给下一棒的话 |
| [mockups/01-启动过场-参考图.html](./mockups/01-启动过场-参考图.html) | HTML 参考图（**复杂场景**型）：Frame 0 新旧时序对照条 + Frame 1 首帧（主题快照 + 品牌过场）+ Frame 2 整帧出现 + Frame 3 内容就位中 + Frame 4 就绪终态 + 骨架规格卡；可切亮/暗 |

## 立案缘起（一句话账）

用户在「关于页重设计」的参考图里看到 Frame 3 的加载骨架（[`../关于LinkDesk标签页重设计/mockups/01-关于标签页-重设计.html`](../关于LinkDesk标签页重设计/mockups/01-关于标签页-重设计.html)），反问：**启动软件的时候是不是没有加载态？** AI 读码核实（[01-设计 §二](./01-设计.md) 六幕时间线，全部 `file:line` 依据）证实：现状启动是「**纯黑 → 半截框架 → 逐个蹦 → 变色 → 标签涌入 → 内容逐个就位**」，而全产品唯一的加载态是 PluginComponent 里一行居中「加载中...」纯文字（`src/pool/shared/plugin-component/PluginComponent.tsx:205`）。用户随即拍板：不做单点补丁，**三案合并成一个启动过场专项**。

## 依赖与碰撞

- 🔴 **E3f #51「防启动白屏」是本项的前身决策**：OS 层窗背景写死 `#1e1e1e` 就是它干的（`electron/main.ts:489`）。本项**继承它的目标（不白屏）、替换它的手段**（写死暗色 → 按持久化主题 seed）——动这条代码时别把「防白屏」本身当废话删了。
- **主题真相源不挪**：仍是 `theme-defaults` 插件贡献（`contributes.themes`）+ ConfigurationService（`app.theme`）。④A 的快照只是 **localStorage 镜像**，权威管线照旧在 `initAll` Step 5 落地——**快照层永远不许长逻辑**（01-设计 §九 对下一棒的硬话）。
- **Path B 哑渲染边界**：池首帧过场与骨架组件都是**纯展示**，不得 import `@src/core/*` 运行时（`PoolZoneShell.tsx:20` 的红线对新增组件同样生效）。
- **零新 i18n（设计目标）**：骨架无文字、过场无文字，`aria-label` 复用既有 key「加载中...」⇒ **不触发 `lang-defaults` 发版**。若开工时忍不住要加文案，先回本文件改这条并补跨仓后果段。
- 硬约束 16 已照办：mockup 绘制前已调 `html-blueprint` skill（复杂场景型；**非竞标**，故无多 skill 联动，基线 = 壳 token 体系）。
