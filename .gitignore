# === 产物 / 依赖（容器版 + 工程版并集，2026-09-04 仓库结构上移后归并为一份） ===
node_modules/
dist/
dist-electron/
target/
release/
__pycache__/
tsconfig.tsbuildinfo
*.log
.DS_Store
Thumbs.db

# E5.7#81：插件卸载坟场——运行时产物，不入库
plugins/.disabled/
# E5.8#22.6：@linkdesk/contracts 验收示例——本地安装产物，不入库
contracts-example/node_modules/
contracts-example/package-lock.json
contracts/*.tgz
# contracts/ 自身零依赖（files 白名单只 d.ts）——包内 lockfile 是本地 npm install 的残留，
# 且它把 version 抄了第二份（违反「版本号不手写第二份」），故不入库
contracts/package-lock.json
# E6#6：@linkdesk/plugin-sdk——npm pack 本地 tarball 不入库
packages/plugin-sdk/*.tgz
# E6#4b/#5b：@linkdesk/plugin-sdk 作者形态验收工程——本地安装产物与构建产物不入库
plugin-sdk-example/node_modules/
plugin-sdk-example/package-lock.json
plugin-sdk-example/*.linkdesk-plugin
plugin-sdk-example/dist/
# E6#15：插件工作区独立 build 的本地 zip——canonical 发货件收 bundled-plugins/（要提交），本地副本忽略防双份
# 2026-09-05 塌平后单根：plugins/<id>/<id>.linkdesk-plugin
plugins/*/*.linkdesk-plugin

# === agent / 本机工具文件（保留在磁盘但不入库） ===
.claude/
# 2026-09-15 用户拍板：非 Claude 工具（ZCode / Codex）共用同一批技能——`.agents/skills` 是
#   `.claude/skills` 的本机副本（28MB），`.codex` / `.zcode` 是各自 CLI 的按项目状态
#   （钩子配置 / plan 产物）。三者一律只留磁盘，绝不入库。
.agents/
.codex/
.zcode/
# impeccable 设计 hook 本地运行时（ignore 决策存储 + 扫描缓存）
.impeccable/
# Codex CLI persona/profile（Codex 用）
SOUL.md
USER.md
# AI 会话记忆工具产物（本机工具状态）
memory/
release2/

# E6#100c：`sync-official-catalog` 的**候选产物**——它是「要人过目 + 用户点头」的中间态；
# 真身 = 官方目录仓库里那份 marketplace.json。留在壳仓入库 = 第二份真相源，两份必分叉。
scratch/
