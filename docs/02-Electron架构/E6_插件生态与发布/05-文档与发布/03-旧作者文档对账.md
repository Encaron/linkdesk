# docs/03-插件制造 旧作者文档对账——E6 真相核稿矩阵

> 🔵 **非新能力（2026-09-05 立案）**：本文 = 文档对账**任务清单**（E6#58 的矩阵档案），零新增 `window.linkdesk.*` / `contributes.*` 面——只是把 12 篇既有作者文档逐篇核到 E6 现实并定处置。核稿中引用的字段/路径均为既有契约与已落地事实。
> 对应任务：E6#58（[E6-执行清单.md](../E6-执行清单.md) 第 5.1 轮）。
> 相邻档案：[01-文档清单.md](01-文档清单.md)（作者文档总账，58b 修正对象）· [01-插件独立构建/02-linkdesk-plugin格式规范.md](../01-插件独立构建/02-linkdesk-plugin格式规范.md)（E6 打包格式真相源）。

---

## 一、背景——为什么立这个任务

`docs/03-插件制造/` 是**作者文档区**：
- `plugin.schema.json` 的「作者文档区拷贝」就住这（3 拷贝同步 gate 角色，**非遗留，永不动**）；
- E6#40 计划把新《插件开发指南》建到 `docs/03-插件制造/13-插件开发指南.md`（同夹共居；⚠️ **2026-09-11 路径订正**——原写 `11-` 已被 `11-主题制作.md` 占用，`12-README说明区媒体契约.md` 亦占用 ⇒ 空位 = **13**）；
- 但现存的 **12 篇散文文档大多停在 E6 之前的世界**。

**2026-09-05 全夹扫描实据**（入库基线）：

| 文件 | 陈旧命中 | 实锤样例 |
|:--|:--|:--|
| 04-插件分发格式.md | **18** 🔴 | 「LinkDesk 用**目录分发**，没有 VSIX 式单一文件」——与 E6 zip 单文件 `.linkdesk-plugin` **直接相反**；引用 `plugins/builtin|user` |
| 06-plugin.json规范.md | 17 | 引用 `plugins/{builtin,user}` 双层；字段表缺 E6 时代（render 改写 `views/*.bundle.js` / distribution 遗留标注 / JSONC #55） |
| 10-如何造一个设置插件.md | 8 | 引用 `plugins/{builtin,user}` 双层 |
| 02-插件生命周期.md | 5 | 引用 `plugins/{builtin,user}` 双层 |
| 08-ViewContainer-视图容器API.md | 2 | 引用 `plugins/user` |
| 00-README.md | 2 | 引用 `plugins/user` |
| 05-插件UI写法规约.md | 1 | 引用 `plugins/builtin` |
| 11-主题制作.md | 1 | 引用 `plugins/user` |
| 07-插件间通信.md | 0 | — |
| 01-插件API契约.md | 0 | — |
| 03-插件contributes规范.md | 0 | — |
| 09-插件目录规范.md | 0（待细核） | 2026-08-05 早于塌平，目录树可能仍带双层段（E5 时代产物） |

> 命中词：`目录分发` / `没有 VSIX` / `plugins/builtin` / `plugins/user`。零命中的篇（01/03/07）也须逐段读——它们引用 `window.linkdesk.*` / `linkdesk://`，是契约类文档，主要风险 = API 面与 E6 现实（#54 @linkdesk/ui、例外表清零）的偏差，不像路径类那么好 grep。

---

## 二、处置三档

1. **改写更新**（多数）——就地更新到 E6 真相：单根 `plugins/<id>` / zip 单文件分发（或指向 02 格式规范）/ `@linkdesk/ui` / 当前 schema 字段。
2. **收编进 E6 档案后标废弃**——被 [01-插件独立构建](../01-插件独立构建/) 对应档案彻底取代、不宜再独立陈述的篇：正文标注「已收编 → [指向]」，内容留短摘要或不留（收编对象预判：09-目录规范 → 06-builtin-user-语义规范 + 09-塌平决策；04-分发格式 分发语义 → 02 格式规范 §打包）。
3. **原样保留**——E6 真相核后仍对的篇，只补「已核 2026-09-05」戳。

## 三、执行清单（对账进行时勾选）

- [x] 12 篇逐篇读 + 每篇在下方矩阵写「已过时点 + 处置」定稿（处置进矩阵「处置」列）——✅ 2026-09-06 矩阵 §四 12 行全定稿
- [x] `04-插件分发格式.md` —— 最高优先（18 处，与 E6 zip 分发直接相反，会带偏新 AI）——✅ 整篇重写（zip 单文件分发 §一/装卸 §二/core:true flag §三/版本纪律 #15n §四/资源声明式 §五）
- [x] `09-插件目录规范.md` —— 细核塌平后目录树是否仍带双层段——✅ 目录树无双层段；改「作者必调 getAssetPath」句法为「相对路径声明 → 壳 getPluginAssetPath 解析」（L17/47/76/92）
- [x] 全夹 grep 收口：`plugins/builtin` = 0、`plugins/user` = 0、`目录分发/没有 VSIX` = 0——✅ 唯一残留 = plugin.schema.json 遗留字段 self-note（「已无 plugins/builtin、user 之分」= 合法废弃声明）；04「目录分发时代已结束」= 否定史述；`.disabled/` 命中 = 源码树 app 插件卸载当前语义（合法）
- [x] 58b：修正 01-文档清单.md §三（`10-插件脚手架.md` 不存在，实际 = `10-如何造一个设置插件.md`）+ 收 11-插件开发指南 进总账——✅ 同批完成
- [ ] 58d：plugin.schema.json 三拷贝 gate 跑绿（本次对账不碰它，核完确认 gate 仍绿）——本次零触碰，`npm run check` 收尾确认
- [ ] 验证：新 AI 通读 docs/03-插件制造 写插件，不踩已死指令——机械门禁已过（逐篇 grep 收口 0）；冷读复验随时可做

---

## 四、矩阵（对账工作底稿——每篇处置定稿后回填）

| # | 文档 | 扫描命中 | 已过时点（对账后定稿） | 处置 |
|:--|:--|:--|:--|:--|
| 00 | README | 2 | `plugins/user/my-plugin`（塌平单根）；能力表「设置 UI」行 settings-demo 悬空（已下架）；静态资源格 `getAssetPath()` 暗示作者调用（实为壳解析）；缺分发入口 | 改写更新（已落）——L57 单根 + 官方设置 + 资源声明式 + `.linkdesk-plugin` 分发注 + header 核戳 |
| 01 | 插件API契约 | 0 | §五允许 import 仍列 `@src/components/shared/*`（E6#54c 已收 `@linkdesk/ui`、仓库插件零引用）；§三 3.1 发布态「仅 npm pack 本地验收」过时（`@linkdesk/contracts@0.1.0` 2026-09-04 已真发，E6#2.5b 0c434bdb1） | 改写更新（已落）——§五共享控件 bullet 收归 @linkdesk/ui；发布态改真发事实 |
| 02 | 插件生命周期 | 5 | `plugins/{builtin,user}` 双层（§一 双加载路径、§九 装卸语）；目录分发已死 | 改写更新（已落）——header 核戳；§一「repo 源码树 HMR vs 安装态 bundle」双路径表；§九 9.1 zip 装卸 / 9.4 双分支卸载 / 9.5 重装 / 9.6 词汇纪律；§十一 VS Code 对照行 |
| 03 | contributes规范 | 0 | 逐段核零残留——contributes 面无目录/分发语义，E5.8#41 方案A 后未变 | 原样保留 + E6 核戳（header 已补） |
| 04 | 插件分发格式 | 18 🔴 | 「目录分发、没有 VSIX 式单一文件」= 与 E6 zip 单文件**直接相反**；`plugins/{builtin,user}` | 改写更新（已落）——整篇重写：§一 `.linkdesk-plugin` zip 单文件（SDK 产物）；§二 zip→`{userData}/plugins/<id>` 装卸两路 + 双代码根表；§三 core:true = 防误删 flag（无内置/用户类）；§四 版本纪律（#15n 内容变更必 bump）；§五 资源声明式壳解析 |
| 05 | UI写法规约 | 1 | `plugins/builtin/editor` 路径（L420） | 改写更新（子代理已落）——→ `plugins/editor` + header 核戳 |
| 06 | plugin.json规范 | 17 | `plugins/{builtin,user}` 双层；「内置插件 vs 用户插件」分类；字段表缺 E6 时代（distribution 遗留标注 / readme 行 / E6 单根示例） | 改写更新（已落）——§插件只有一类（core:true=防误删 flag 表 + distribution 遗留「第三方勿填」）；目录结构约定 → plugins/<id> + {userData}/plugins；icon/factoryRole/校验 全扫 |
| 07 | 插件间通信 | 0 | 逐段核零残留——三通信机制（events/commands/数据管道）E5.8 后未变 | 原样保留 + E6 核戳（header 已补） |
| 08 | ViewContainer API | 2 | `plugins/user` 引用；视图容器例的插件路径指旧双层 | 改写更新（子代理已落）——示例路径转 Git 插件语境（`git/plugin.json`）+ header 核戳 |
| 09 | 插件目录规范 | 待细核 | 目录树无双层段 ✅；但「读取资源必须走 `getAssetPath()`」把壳内部机制写成作者必调（实为 `getPluginAssetPath(pluginId,path)` 壳解析）；L39 分发物说法 | 改写更新（已落）——§静态资源改「相对路径声明 → 壳 getPluginAssetPath 解析、作者不 import 路径工具」+ 分发 = SDK zip |
| 10 | 造设置插件 | 8 | `plugins/builtin/settings`；settings-demo（已下架）多处活引用（编排层/CSS 命名空间范例）；distribution 字段行；「白名单控件」走 @src/components | 改写更新（已落）——官方 `plugins/settings` 参考 + 共享控件收 @linkdesk/ui + 造法一/二 + CSS 死命名空间清（`.my-settings`）+ 内置设置→官方设置归一 + header 核戳 |
| 11 | 主题制作 | 1 | `plugins/user` 引用；资产机制名 getAssetPath → getPluginAssetPath 跨文档对齐 | 改写更新（已落）——路径清（子代理）+ 机制名对齐（4 处）+ header 核戳 |
| — | plugin.schema.json | —（gate 拷贝） | 已是 2026-09-05 塌平同步态（distribution 遗留标注 / core 语义 / readme 行俱在） | 不动（58d 保留） |

> **附记（58 收尾）：** 顺带清掉官方 `plugins/settings/src/views/SettingsView.tsx` L265 源码注释残留「settings-demo = 右下角胶囊」——settings-demo 已下架，改「官方套 = 顶部平铺条；第三方套可自选右下角胶囊等」（docs 已写 10 篇「已下架」，代码侧同步不留死引用）。

---

> **→ 上一文档：** `02-发布流水线.md`
> **→ 关联：** [E6#58 清单条目](../E6-执行清单.md)
