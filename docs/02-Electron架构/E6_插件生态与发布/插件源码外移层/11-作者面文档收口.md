# 作者面文档收口与上手动线——03-插件制造 的宗旨化

> **任务号：** E6#105（**第 7.8 轮**——🔵 编号 7.8 = 插入序；**执行序在 7.7 全层验收之前**）· **前置：** 7.0 落笔的判据（`#97a/97f/97g`）＋ [10-作者文档族对账.md](10-作者文档族对账.md)（内容先改对）

> **非新能力声明（`check-design-flow` §8.4 ③）：** 本档是**文档收口的规划与执行清单**，不含新能力设计——
> 文中出现的 `window.linkdesk.*`（§六.1 拟放进脚手架 `AGENTS.md` 的**铁律原文示例**）与
> `contributes.configuration`（§九 任务表里**引用既有贡献点**描述要补的那篇文档）**都是既有公开 API 与既有 manifest 字段**，
> 其设计前置在各自既定轮次里早已落笔，本档只是把它们**写进文档 / 教给作者**。⇒ **无需新能力设计前置**。
> （2026-09-14 补：本档早先因「未被任何门禁扫到」而没写这句——`check-design-flow` ③ 只扫 **git 未提交**的
> `docs/02-Electron架构/*.md`，没人改它时它是绿的。7.6 轮顺手订正一处过期指令时被这道闸扫到，同笔补上。）

---

## 〇·〇 本轮的宗旨（🔴 用户 2026-09-14 亲口定，一切判据从这句出发）

> **「03-插件制造 最主要的目的是让插件作者知道如何制作某一个插件……换而言之，这个文件夹的目的就是让插件作者从零开始快速学习如何制作一个插件。」**

用户点名的**教学大纲**（作者必须学会的六件事）：

| # | 作者必须知道 | 现状 |
|:--:|:--|:--|
| ① | **图标栏 / 侧栏（侧拉）/ 主区** —— 有哪些区域、各能放什么、怎么声明 | ⚠️ **散而无图**——`00-README` 有"贡献类型"表、`08-ViewContainer` 讲容器 API、`03-contributes` 讲字段，**但没有一篇"区域地图"** |
| ② | **怎么用我提供的这些 UI**（共享组件） | 🟡 `13` 第 6 步 + `05-UI写法规约`——有，但缺**组件速查** |
| ③ | **不同区域之间怎么联系互动**（侧栏选中 → 主区切换 → 状态栏更新） | ⚠️ **缺"区域间"这个视角**（`07`/`14` 讲的是通信机制，不是区域联动） |
| ④ | **插件与其他插件之间怎么联系互动** | ✅ `07-插件间通信.md` 覆盖 |
| ⑤ | **怎么在设置里搞它的配置项** | 🟡 `10-如何造一个设置插件` + `03-contributes` 的 `configuration`——有，但"我的插件加一条配置项"的最小做法要显式 |
| ⑥ | **主题怎么做** | 🟡 `11-主题制作` 薄 + 真知识散在 `外观主题化` 22 篇设计文档 |

**🔴 口号（可测验收标准，用户 2026-09-14 定）：**

```
AI 10 分钟制作出一个最小插件
AI 30 分钟制作出一个视图插件
AI  1 天  制作出一个高难度插件
```

> 📌 这是既有标准的**扩展**：memory `e6-10min-first-plugin`（2026-08-09）只立了「10 分钟」一档，并写明「**E6 成功的唯一标准**：第三方作者 10 分钟内写出第一个插件并跑起来。VS Code 赢在 `yo code` 5 分钟，不是 Extension API 优雅」。**本轮把它扩成三档，并把"时间"变成可测判据。**

**🔴 因此本轮的产物形态是「教学动线」，不是「文档集合」：**
- `03-插件制造` 的入口应是**导览**（按"我要做什么/我是哪一档"分流），不是文档列表；
- 判据不是"收齐了多少篇"，而是**"这三档时间能不能达标"**（§十 终局验收）。

---

> **立案：** 2026-09-14 用户拍板。**用户原话要点：**
> ①「你可以**再建一个轮次专门整理 03-插件制造**……归纳一下建立子文件夹，然后**索引他们的地址**」；
> ②「散落到 E5.8 那些**其实是当时做任务的规划、是布置任务**；假如有作者想做主题插件确实是问答困难 ⇒ **你读一下相关源文档，回到 03-插件制造 专门建一个子文件夹**给想做主题插件的人看，**里面写一两个 md 告诉他们怎么做**」；
> ③「API 速查表也一样，**集中到这里统一让插件作者去看**——**不一定作者自己看，可能是作者的 AI 去看**，那样它就知道该翻哪个文件夹」；
> ④「`E6#` 任务号**是给我自己的 AI 找历史用的**，对插件作者是**无法解析的坐标**⇒ 想办法解决」；
> ⑤「**不是真的全量搬进来**——目的是**让作者从零快速学会做一个插件**」（本轮 §〇·〇）；
> ⑥「接下来**在第七层多立任务**，把插件制造这一方面**说详细搞详细**——脚手架怎么更新到什么版本、里面放什么、文档 npm、以及 03-插件制造 的**全量更新**」。
> **🔵 先解决用户第 4 点的疑虑（看起来的冲突）**：他说"集中到 03-插件制造"（按目录），我说"边界该按读者"——**不冲突，是同一件事的两步**：**按读者判定 → 按目录落地**。
> **🔴 一条重要结论（实测代价决定）**：**老 15 篇正文不搬**（归纳靠索引实现）——搬动的收益是"顺序感"，代价是 **~90 个外部文件引用重算**（6 个门禁脚本、**16 个 memory 文件**、**9 个已封存期档案 ⇒ 必然断链**、1 处源码注释）；而用户真正要的"**可发现性/学得会**"靠**导览 + 新写的缺篇零成本**拿到。**且"不是全量搬进来"已由用户明确拍板。**

---

## 〇、先解决一个看起来的冲突（用户第 4 点疑虑）

用户问：「我说"把散落的都集中到 03-插件制造"（按目录），跟你说的"边界不该按目录、该按读者"，会不会冲突？」

**不冲突——它们是同一件事的两步：**

| 步 | 做什么 | 判据 |
|:--:|:--|:--|
| **1** | **按读者判定归属**：这篇是给插件作者看的，还是给壳开发者/内部进度看的？ | 「一个**陌生作者**读它有用吗？」有用 ⇒ 作者面 |
| **2** | **按目录落地**：判为作者面的 ⇒ **收进 `03-插件制造/`**；判为内部的 ⇒ 留在原处（各层档案不动） | 目录成为作者面的**物理全集** |

⇒ **判定按读者，落地按目录。** 做完之后 `03-插件制造/` 就成了"作者面"这个名字的唯一落点——这正是归一化要的（**一个概念只写一次**：作者面文档只在一个目录里）。

---

## 一、现状体检（2026-09-14 实测，全部回源）

### 1.1 十五篇平铺 + 一篇 schema

```
docs/03-插件制造/
  00-README.md   01-插件API契约.md   02-插件生命周期.md   03-插件contributes规范.md
  04-插件分发格式.md   05-插件UI写法规约.md   06-plugin.json规范.md   07-插件间通信.md
  08-ViewContainer-视图容器API.md   09-插件目录规范.md   10-如何造一个设置插件.md
  11-主题制作.md   12-README说明区媒体契约.md   13-插件开发指南.md   14-数据管道命令约定.md
  plugin.schema.json
```

### 1.2 🔴 作者面文档散在至少四处（这是「问答困难」的根因）

| 位置 | 篇目 | 为什么是作者面 |
|:--|:--|:--|
| `docs/03-插件制造/` | 15 篇 | 主集（唯一"专门为作者造"的目录） |
| `docs/02-Electron架构/E5.8_归一化基建/外观主题化/` | **22 篇**（含 `02-变量契约` / `05-主题数据模型` / `06-主题API契约` / `10-混搭设计` / `09-命令面`…） | **写主题插件的作者必读** |
| `docs/02-Electron架构/E6_插件生态与发布/` 的 `01-插件独立构建/` · `02-插件开发工具链/` · `05-文档与发布/00-第三方作者旅程.md` | zip 格式真相源 / 脚手架 / 本地预览 / 发布流水线 / 真机调试 / 作者旅程 | 作者动线 |
| `packages/plugin-sdk/README.md`（API 速查表）· `contracts/linkdesk.d.ts` · `02-Electron架构/E5.8_归一化基建/契约生成/命名空间矩阵.md` | API 面 | 作者查 API |

⚠️ **但要分清那 22 篇的性质**（用户 ② 已点明）：「**那是当时做任务时我对它的规划、是布置任务**」—— 它们里有**设计总纲 / 代码规划 / 缺陷修复档案 / 根治收尾规划**（`07-代码规划` `12-主题缺陷修复档案` `13-主题系统重构档案` … `21-玻璃饱和度量程语义修复档案`）。**这些是"过程与决策"，不是"怎么做"手册。**

⇒ **结论：不搬那 22 篇，而是从它们里提炼出面向作者的手册**（§三）。**设计文档（为什么这么做）与作者手册（怎么做）是不同读者、不同问题，不算重复**——但**字段级契约必须只留一个真源 = `theme.schema.json`**（手册讲"怎么做"，schema 讲"字段是什么"）。

### 1.3 🔴 出界的 48 条链接（决定"能不能发 npm 包"）

实测 `03-插件制造/` 里指向目录外的链接：

| 目标 | 条数 | 性质 |
|:--|:--:|:--|
| `../02-Electron架构/…` | **39** | **混装**——既有真作者面（外观主题化 / 格式规范 / 工具链 / 作者旅程 / 命名空间矩阵），也有纯内部（`E6-执行清单.md` / `03-插件市场/15-…任务档案.md` / `文件整理层/06-…` / `开发管理/壳目录规范.md`） |
| 相对路径噪音（`](../)`） | 7 | 需逐条看 |
| `../开发管理/壳目录规范.md` | 2 | **内部**（壳开发者的规范） |

⇒ **发 npm 包（只装本目录）会让这 48 条全部悬空；GitHub 上有整个仓库所以不会** —— 这就是为什么在线锚选 GitHub 是对的（见 §六）。

### 1.4 🔴 130 处内部任务号（对作者是"无法解析的坐标"）

实测 `03-插件制造/` 里 `E6#` / `E5.8#` / `E5#` / `E4V#` 的命中：**总计 130 处，分布 14 篇**。最密的：

| 篇 | 处数 |
|:--|:--:|
| `03-插件contributes规范.md` | 19 |
| `01-插件API契约.md` | 17 |
| `05-插件UI写法规约.md` | 15 |
| `06-plugin.json规范.md` | 14 |
| `02-插件生命周期.md` | 13 |
| `09-插件目录规范.md` | 7 |
| `10-如何造一个设置插件.md` | 6 |
| `08-ViewContainer-视图容器API.md` | 6 |
| `07-插件间通信.md` | 5 |
| `00-README.md` | 4 |
| `04-插件分发格式.md` | 4 |
| `11-主题制作.md` | 3 |
| `12-README说明区媒体契约.md` | 1 |
| `13-插件开发指南.md` | 1 |

**为什么这是缺陷（用户 ④ 已确认口径）**：这些号是**给本项目的 AI 找历史用的**；对一个陌生作者的 AI，`E6#18g` 是**它无法解析的坐标**——它会想"E6 是什么、要不要去查"，**这正是硬约束级 AI 友好要消灭的"追踪成本"**（memory `ai-friendliness-three-layers` 的核心理念：**让 AI 不需要追踪**）。

### 1.5 🔴 搬动正文的引用面 ≈ 90 个文件（决定"全量搬进子文件夹"该不该做）

实测引用 `03-插件制造/<文件>` 的**仓内**文件（不含本目录自身）：

| 类别 | 数量 | 能不能机械修 |
|:--|:--:|:--|
| 机械门禁脚本（`check-api-contracts` / `check-contributes` / `check-plugin-schema-sync` / `check-font-scale-audit` / `check-lsp-args-base` / `audit-i18n`） | 6 | ✅ 但要连 `--self-test` 一起改 |
| **memory 文件**（不进 git、**无门禁兜底**） | **16** | ⚠️ 手工，最危险（L0.16 那轮的账：memory 20 文件逐个修） |
| **已封存的期档案执行清单**（E2 / E3 / E3.6 / E4 / E5 / E5.5 / E5.6 / E5.7 / E5.8） | 9 | 🔴 **按规矩不该改**（历史档案，改它 = 改写历史）⇒ 会永久断链 |
| 源码里的注释引用（`src/core/react/events/ShellEvents.ts`） | 1 | ✅ |
| 总纲/入口（`CLAUDE.md` / 根 `README.md` / `.claude/skills/design-flow`） | 3 | ✅ |
| 其余 docs（E6 各层档案、04/开发管理…） | ~50 | ✅ 大部分机械 |

引用最密的篇：**`09-插件目录规范.md`（约 38 条）** · **`plugin.schema.json`（约 30 条）** · `06-plugin.json规范.md`（约 22 条） · `12-README说明区媒体契约.md`（约 18 条）。

**⇒ 关键判断：搬动正文的收益是"审美 + 顺序感"，而"可发现性"（用户真正要的）可以零成本拿到（靠索引）。搬动的代价却是 ~90 处引用重算 + 9 个封存档案的既定断链。**

---

## 二、决策（收口原则）

| # | 决策 | 理由 |
|:--|:--|:--|
| **Y1** | **判定按读者、落地按目录**（§〇） | 解决用户第 4 点的疑虑 |
| **Y2** | 🔴 **老 15 篇正文不搬**（原地不动），**归纳靠 `00-README.md` 真索引实现** | 索引给的是"该看哪篇"，正是可发现性；搬迁给的是"文件在哪"，代价 ~90 处引用 + 9 处必然断链。**可发现性零成本可得，顺序感不值这个价** |
| **Y3** | **新建子文件夹只装"新增/重写的作者面文档"**（`主题/` 等），按内容量建（≥2 篇才建夹） | 用户 ② 明确要"专门建一个子文件夹"给主题作者 |
| **Y4** | **主题作者手册 = 从 22 篇设计文档提炼新写**（不是搬运） | 用户 ② 原话「那是布置任务 / 读一下源文档 / 写一两个 md 告诉他们怎么做」；设计过程 ≠ 作者手册 |
| **Y5** | **字段级契约只留一个真源**（`schema`），手册只讲"怎么做"并指向 schema | 避免"同一个概念写两份"（戒律五） |
| **Y6** | **130 处内部任务号 = 剥离或转写**；对外面禁止裸任务号（配门禁） | 用户 ④ 确认口径；AI 友好 |
| **Y7** | **在线锚 = GitHub 的 `03-插件制造/`**（用户 ⑥ 选 (a)），**指向作者面入口而非单篇** | 48 条出界链接在 GitHub 上全部有效 |
| **Y8** | **文档 npm 包 = 按收口后的"作者面清单"发布**（生成物，非手工副本） | 复用 `@linkdesk/contracts` 的"真源 + 生成物 + `--check`"模式 |
| **Y9** | 若用户日后仍要物理归纳（把 15 篇搬进子文件夹）⇒ **单列任务**，含"封存档案豁免口径 + memory 16 文件逐个修 + 门禁 self-test 同改" | **知情选择**：代价已量化，不阻挠但要记账 |

---

## 三、主题作者子文件夹（用户 ② 点名的那件事）

**目标产物：** `docs/03-插件制造/主题/`（≥2 篇）

| 篇 | 内容 | 素材来源（读，不搬） |
|:--|:--|:--|
| `01-做一个主题插件.md` | 面向作者的动线：主题插件长什么样 → 写 `theme.json` → 注册 → 本地预览 → 发布。**以"我要做一个主题"的视角组织**，不是按内部设计章节组织 | `外观主题化/05-主题数据模型.md`（配方 Recipe / 配色变体 Colorway 的**作者可理解表述**）+ `11-主题制作.md`（现有作者文档，可能需重写）+ `02-Electron架构/…/02-本地预览环境.md` |
| `02-主题字段速查.md` | **只做索引与指向**：字段清单 → 指向 `theme.schema.json`（单一真源）；常用配方示例 | `theme.schema.json`（**引，不抄**）+ `外观主题化/02-变量契约.md` |

**做法铁律（三条）：**

1. 🔴 **读设计文档，不搬设计文档** —— 22 篇里含 `代码规划` / 多份 `缺陷修复档案` / `根治收尾规划`，那些是过程记录，搬过去只会污染作者面。
2. 🔴 **字段不做第二真源** —— 手册里出现的字段说明要么**引 schema**，要么**引 `09-插件目录规范`**；**禁止抄一份字段表**（抄了就等着漂移）。
3. 🔴 **剥离内部坐标** —— 新写的两篇里**零任务号**（用户 ④）；要表达"为什么这么设计"就写人话，不写 `E5.8#xx`。

---

## 四、API 速查表归拢（用户 ③）

**现状**：API 面散在 `packages/plugin-sdk/README.md`（速查表，由 `generate-api-cheatsheet.mjs` 生成）+ `contracts/linkdesk.d.ts` + `02-Electron架构/…/契约生成/命名空间矩阵.md`。

**处置**：**在 `03-插件制造/01-插件API契约.md` 里做"总入口"**，指向上述三处（**引用不复制**）——因为三处各有分工，复制哪一处都会立刻漂移：

| API 面 | 真源 | 读者怎么用 |
|:--|:--|:--|
| 速查表（45 命名空间 / 242 方法） | `packages/plugin-sdk/README.md`（`generate-api-cheatsheet.mjs --check` 守） | 作者 npm 页面就看得到 |
| 类型契约 | `contracts/linkdesk.d.ts`（`contracts:check` 守） | 编辑器里 `import type` 直接补全 |
| 命名空间矩阵（四面覆盖） | `02-Electron架构/…/契约生成/命名空间矩阵.md`（`check-namespace-matrix.mjs` 守） | 想确认"这个能力在哪些面可用" |

---

## 五、内部任务号处置（用户 ④）

**口径（待用户确认其一）：**

| 方案 | 做法 | 代价 |
|:--|:--|:--|
| **甲（推荐）** | **剥离**：正文里删掉 `E6#xx` 这类坐标；要保留"为什么"就把理由写成**人话**；确需留档的移进**本目录之外的**内部档案 | 130 处逐条判断，工作量大但产物干净 |
| 乙 | **隔离**：正文删任务号，但把"历史注记"收进每篇末尾一个显式折叠块（如 `<!-- 内部注记 -->`），并声明"本节仅供本项目维护者" | 保留可追溯性，但作者/AI 仍会读到 |
| 丙 | **容忍 + 声明**：顶部加一句"文中 `E6#xx` 是本项目内部进度坐标，读者可忽略" | 零工作，但**没解决追踪成本**，只是告诉读者"别管它" |

**推荐甲**：理由是你 AI 友好的原话——「**你要的不是 AI 更聪明，你要的是代码不给 AI 犯错的机会**」。丙是"告诉 AI 别犯错"，甲是"不给它犯错的机会"。

**配门禁（用户 ⑥ 同意）**：`docs/03-插件制造/**` 出现裸 `E6#` / `E5.8#` / `E5#` / `E4V#` ⇒ 红。今天实测 130 处 ⇒ **立起来立刻红，说明尺子对，不是尺子严**（同 L3.6 门禁的做法）。

---

## 六、脚手架更新（用户 ⑤「脚手架怎么更新、更新到什么版本、里面放什么」）

**只加一个文件，不加 15 篇文档。**

### 6.1 新增 `AGENTS.md`（脚手架模板 → 每个新插件天生带）

必须回答**四件事**（用户原话：「光有个脚手架，那个 AI 哪知道该干什么」）：

```markdown
# <插件名>

## 1. 这是什么
<一句话：本工程是 LinkDesk 插件；LinkDesk 是什么（一句）>

## 2. 铁律（内联——这几条几乎不变，所以内联代价最小）
- 颜色走 var(--xxx)，禁止硬编码 hex
- 界面文字走 t()，禁止硬编码中文
- 系统能力只走 window.linkdesk.*，禁止 import 壳源码
- 插件身份（pluginId）显式声明，发布后不可改

## 3. 规则去哪找
- 在线：<GitHub 的 03-插件制造/ 入口>（Y7）
- 离线：npm i @linkdesk/plugin-docs（Y8）
- 字段级：plugin.json 的 $schema（已在 node_modules）← 必然到达
- 完整清单：03-插件制造/00-README.md 的索引

## 4. 下一步干什么（动线，用脚手架自己的 script 名）
npm run dev → npm run dev:real → npm run validate → npm run build → npm run publish
```

### 6.2 版本与改法

- **改的是 npm 包 `create-linkdesk-plugin`** ⇒ bump + publish + `release:mark`（`npm-publish-every-update` 纪律）。~~与 `#102a`（CI 模板）· `#103a`（git init）合并成一笔版本~~ → 🔴 **2026-09-14 订正：那一笔已经发出去了**（`0.1.3` 已 bump ＋ 已 publish ＋ 已 `release:mark`）⇒ **本轮动模板必须 bump `0.1.4`**，别往已发布的版本号里塞东西。**发布命令必须显式 `--registry=https://registry.npmjs.org`**（npm 11 在 workspace 里会忽略本包 `.npmrc`，不带会指向只读镜像 npmmirror 并报成「没登录」——坑的全文见 [01 号档案 §七](../02-插件开发工具链/01-create-linkdesk-plugin脚手架.md)）。
- 🔴 **模板 README 的存量空指针要处置**：`template/README.md:48` 现在写着「完整作者文档见 LinkDesk 仓库的 `docs/03-插件制造/`」——**没 URL，而且 README 是市场详情页的数据源（读者是用户不是 AI）**。⇒ 作者面指针**挪进 `AGENTS.md`**，README 只保留面向用户的说明。
- ⚠️ **别的 AI 工具读不同文件**（Claude 读 `CLAUDE.md`、Cursor 读 `.cursorrules`…）：插件仓**只押 `AGENTS.md`**（内容本来就短，不做两级），或按需加一行软指针——**待用户定**。

---

## 七、文档 npm 包（`@linkdesk/plugin-docs`，名字待定）

**形态（复用 `@linkdesk/contracts` 的成熟模式，零新机制）：**

```
真源      docs/03-插件制造/**（含新子文件夹）        ← 手工只改这一份
  ↓ 生成脚本（新，对标 generate-contract.mjs）
产物      packages/plugin-docs/docs/**
  ↓ 门禁（新，对标 contracts:check）
--check   与磁盘字节比对（链接重写处除外，规则需显式登记）
```

- **命名待定**：`@linkdesk/plugin-docs` vs `@linkdesk/author-docs`。
- **第五根作者轴**：进 `scripts/npm-release-state.json` + `version-bump` skill §〇 多轴表加一行；自动获得 `check-npm-release` 黄灯 + `release:mark` 三关。**升版判据最简：内容变即 PATCH**。
- 🔴 **前置 = §五 的内部符号剥离 + §二 Y8 的作者面清单收口** —— **否则包里全是断链和无法解析的坐标**（48 条出界链接 + 130 处任务号）。**所以它排在本轮靠后，不是第一步。**

---

## 八、两条门禁（用户 ⑥ 同意）

| 门禁 | 判据 | 今天跑会怎样 |
|:--|:--|:--|
| **无内部符号** | `docs/03-插件制造/**` 出现裸 `E6#`/`E5.8#`/`E5#`/`E4V#` ⇒ exit 1 | 🔴 **立刻红 130 处** ⇒ 清完才绿（**同 L3.6 的做法：先立尺，再清账**） |
| **出界链接白名单化** | `03-插件制造` 的出界链接必须出现在"作者面清单"里，否则红 | 红 —— 白名单从 §五/§二 的判定表来 |

两条都要**自带 `--self-test` 并接进 `npm run check`**（memory `gate-selftest-must-be-wired`：「加带 `--self-test` 的尺子时，自测同批接进 check」——否则自测自己就是假门禁）。

---

## 九、任务分解（E6#105a-m）

> **排序原则：** 先对账（知道自己缺什么）→ 再补缺篇（教学生产线的主体）→ 再收口（索引/符号/门禁）→ 最后发布（脚手架 + npm 包）。

| 子项 | 内容 | 对应 | 依赖 |
|:--|:--|:--|:--|
| **105a** | **教学大纲对账 + 作者面判定**：§〇·〇 六项逐项查"现有哪篇覆盖 / 缺什么"；48 条出界链接 + 散在五处的作者文档**逐条判"作者面 / 内部"**⇒ 产出**作者面清单 + 缺篇清单** | 大纲总纲 | 无 |
| **105b** | **`00-README.md` 改成导览**（不是文档列表）：按「**我要做什么**」（写最小插件 / 写视图 / 写主题 / 上架 / 查 API）＋「**我是哪一档**」（10 分钟 / 30 分钟 / 1 天）分流；每行 = 看哪篇 + 一句话 | §〇·〇 形态 | 105a |
| **105c** | **三层动线落到 `13-插件开发指南.md`**：现有九步动线**已经存在**，本轮给它加**三档入口**——① 10 分钟档（最小可跑）② 30 分钟档（视图插件：区域 + 贡献点 + 组件）③ 1 天档（高难度：数据管道 / 跨插件 / LSP / Canvas），每档一条**"跟做"最小路线** | 口号三档 | 105a |
| **105d** | 🔴 **补缺篇①「区域地图」**（用户大纲 ① 点名"知道图标栏、知道侧拉、知道主区"）：**新写一篇**——图标栏 / 侧栏 / 主区 / 底部面板各区是什么、能放什么、怎么声明、彼此什么关系。现状是**散而无图**（`00-README` 贡献类型表 + `08-ViewContainer` 容器 API + `03-contributes` 字段，但没有一张"区域总览"） | 大纲 ① | 105a |
| **105e** | 🔴 **补缺篇③「区域间互动」**（用户大纲 ③）：侧栏选中 → 主区切换 → 状态栏更新这类**区域联动**的作者视角。现有 `07-插件间通信` / `14-数据管道命令约定` 讲的是**通信机制**，不是**区域联动**——两回事，缺的是后者 | 大纲 ③ | 105a |
| **105f** | **补缺篇②「组件速查」**（用户大纲 ②"怎么用我提供的这些 UI"）：`@linkdesk/ui` 组件清单速查（按钮 / 对话框 / 右键菜单 / 选择器 / 图标 / Markdown…）；🔴 **引 SDK 与 `05-UI写法规约`，不抄组件表** | 大纲 ② | 无 |
| **105g** | **补缺篇⑤「我的插件加一条配置项」**（用户大纲 ⑤）：最小做法显式化（`contributes.configuration` → 设置页自动渲染 → 读取）。现有 `10-如何造一个设置插件` 讲的是**做一个设置插件**，不是**给自己的插件加一条配置** | 大纲 ⑤ | 无 |
| **105h** | **主题作者子文件夹**（大纲 ⑥）：读 `外观主题化` 22 篇 → 在 `03-插件制造/主题/` **新写 2 篇**。🔴 三条铁律：读设计文档**不搬**（那 22 篇含代码规划与多份修复档案 = 过程记录）；**字段只引 `theme.schema.json`，禁抄表**；**零任务号** | 大纲 ⑥ | 105a |
| **105i** | **API 速查表归拢**（用户 ③）：`01-插件API契约.md` 做**总入口**，指向三处真源（SDK README 速查表 / `contracts/linkdesk.d.ts` / 命名空间矩阵）——**引用不复制**（三处各有门禁守，复制即漂移） | 用户 ③ | 无 |
| **105j** | **内部任务号处置**：**130 处**（14 篇；最密 `03-contributes` 19 / `01-API契约` 17 / `05-UI规约` 15 / `06-plugin.json` 14）；推荐**甲＝剥离** | 用户 ④ | 105a |
| **105k** | **脚手架更新**（用户 ⑥）：模板新增 `AGENTS.md` 回答**四件事**（这是什么 / 铁律内联 / 规则去哪找 / **下一步动线**）；**`template/README.md:48` 的存量空指针处置**；与 `#102a`/`#103a` **合并成一笔 npm 版本** | 用户 ⑥ | 105c（指针目标要存在） |
| **105l** | **文档 npm 包**（复用 `@linkdesk/contracts` 的"真源 + 生成物 + `--check`"模式）：生成脚本 + **第五根作者轴**登记；🔴 **前置硬依赖 = 105a/105j**（否则包内全是断链 + 无法解析的坐标） | 用户 ⑥ | 105a/105j |
| **105m** | **两条门禁**（用户 ⑥ 同意）：① `03-插件制造/**` 裸任务号 ⇒ 红（**今天跑会红 130 处 ⇒ 先立尺再清账**）② 出界链接白名单化；两条都自带 `--self-test` 并接进 `npm run check` | 用户 ⑥ | 105a |

---

## 十、判据（本轮过不过）

### 10.1 终局判据 = 口号本身（可测，这是本轮存在的理由）

- [ ] 🔴 **10 分钟档**：拿 `npm create linkdesk-plugin` 的产物 → **一个 AI 不问任何人**，10 分钟内做出**最小可跑插件**
- [ ] 🔴 **30 分钟档**：同条件，30 分钟内做出**视图插件**（进得了区域、声明得了贡献点、用得上共享组件）
- [ ] 🔴 **1 天档**：同条件，1 天内做出**高难度插件**（数据管道 / 跨插件通信 / Canvas 或 LSP 级别的）
- [ ] ⚠️ **计时必须诚实**：把"找文档的时间"算进去（那正是本轮要优化的东西）；跑不通就记下**卡在哪一步**——**卡点即缺篇清单，直接喂回 105a**

### 10.2 大纲落点判据（用户点名六项，每项必须有家）

- [ ] ① 图标栏 / 侧栏 / 主区 → **105d 区域地图**可指
- [ ] ② 共享 UI 怎么用 → **105f 组件速查**可指
- [ ] ③ 区域间互动 → **105e** 可指
- [ ] ④ 插件间互动 → 现有 `07-插件间通信` 可指（**引用即可，不重写**）
- [ ] ⑤ 配置项 → **105g** 可指
- [ ] ⑥ 主题怎么做 → **105h 主题子文件夹**可指
- [ ] **导览可用**：一个不懂 LinkDesk 的人（或 AI）只读 `00-README.md`，能**自己选对入口**（"我要做主题" → 翻到主题夹）

### 10.3 收口与发布判据

- [ ] **130 处内部任务号清零**（或按用户选定的方案处置）；**新门禁会红/会绿双向实证**
- [ ] 脚手架 `AGENTS.md` 回答了**四件事**，且动线的 script 名与模板 `package.json` **逐字一致**
- [ ] 模板 `README.md:48` 的作者面空指针已挪出（README 不再承载作者文档指针）
- [ ] 文档 npm 包：**真源 ≠ 产物 ⇒ `--check` 红**（负控）；**包内出界链接 = 0**；第五根轴已登记
- [ ] **不 bump 软件**（纯文档/工具链；脚手架与文档包各自 bump）；`npm run check` 全绿
- [ ] 🔴 **老 15 篇正文未被搬动**（Y2）——`git status` 里不应出现 `03-插件制造/*.md` 的改名记录

---

> **← 总档案：** [00-整理档案.md](00-整理档案.md) · **上游：** [10-作者文档族对账.md](10-作者文档族对账.md)（先把内容改对）· [09-命名规范.md](09-命名规范.md)（N6 版本号）· [07-脚手架与工作区.md](07-脚手架与工作区.md)（容器与 git init）
