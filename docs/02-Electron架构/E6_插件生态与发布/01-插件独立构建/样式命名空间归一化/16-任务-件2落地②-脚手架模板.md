# 任务 `#109h-b②`：件 2 落地·**脚手架模板**——第 1.14② 轮

> **一句话**：把脚手架模板的示例类名改成 `{{pluginName}}-starter*`，**让新插件一建出来就合规**——否则作者拿到手的第一个工程就会红在自己的 CI 上。
> **前置**：[① SDK 判据腿](15-任务-件2落地①-SDK判据腿.md) ✅（模板必须先于任何仓改名——新产物不能一边出生一边违规）。
> **规模**：模板 2 文件 ＋ `check-scaffold` 断言 1 条 ＋ `create-linkdesk-plugin` **发版**。
> **提交轴**：`packages/create-linkdesk-plugin`（发版）＋ 壳仓 `scripts/check-scaffold.mjs`。
>
> **非新能力声明（`check-design-flow` §8.4 ③）：** 本档是**示例代码与门禁断言的同步改动**，不新增作者面能力。⇒ **无需新能力设计前置**。

---

## 一、改什么（**实测**：模板里只有 2 个文件提到示例类名）

| 文件 | 处 | 现状 |
|:--|--:|:--|
| `packages/create-linkdesk-plugin/template/src/index.css` | **5** | `.starter`（:7）· `.starter__title`（:12）· `.starter__text`（:17）· `.starter__hint`（:22）· `.starter__hint code`（:28，**后代选择器**） |
| `packages/create-linkdesk-plugin/template/src/index.tsx` | **6** | `className="starter"`（:23）· `starter__title`（:24）· `starter__text`（:25）· `starter__hint` ×3（:26 / :29 / :32） |

**改成**：`{{pluginName}}-starter` / `{{pluginName}}-starter__title` / `{{pluginName}}-starter__text` / `{{pluginName}}-starter__hint`（＋后代选择器那句同步）。

> ✅ **零新机制**：占位符 `{{pluginName}}` 已存在，且 CLI 的 `replacePlaceholders()` 是**递归替换模板目录下所有文件**（`index.js:165–178`，`pluginName` = 用户的插件名 = `pluginId`）。⇒ 不需要给 CLI 加任何逻辑。

🔴 **同时改的那一条**：`template/plugin.json` 里给 `pluginId` 补一句「**不得以 `ldk-` 开头**」的说明（拍板 Q4）——它是**实例注释**（JSONC），改了不影响 schema。

⚠️ **检查一下模板里还有没有别的裸类名/关键帧**：跑 `node scripts/plugin-css-prefix-audit.mjs <生成物目录>`（工具在 [①](15-任务-件2落地①-SDK判据腿.md) 交付）——**以工具读数为准**，上表是 2026-09-15 的实测快照。

---

## 二、`check-scaffold.mjs` 补一条断言

**新增「断言 11：生成物零裸类名/关键帧违规」**——**不许在断言里重写判据**：调用 [①](15-任务-件2落地①-SDK判据腿.md) 的审计工具（或直接 import SDK 的同一条实现），断言「违规数 == 0」。理由与断言 10 同款：

> 🔴 **判据必须与门禁同源**（文件头那句「两处各写一份正则 = 必然漂移」）。这里再抄一份 = 给下一次漂移埋钉子。

**断言必须能被负控推翻**：`--self-test` 里加一例——把 `template/src/index.css` 的 `.starter` 临时改回无前缀（桩模板）⇒ 断言**红**；还原 ⇒ 绿。**恒真的断言等于没有断言**（照断言 10 的先例：**真变异实测**，不靠推理）。

---

## 三、发版（`create-linkdesk-plugin` PATCH）

- bump `packages/create-linkdesk-plugin/package.json` ＋ `CHANGELOG` 同笔（硬约束 22）。
- 刷 lock：`npm install --package-lock-only`（否则 `check:lockfile-sync` 红）。
- `git add` 后、`npm publish` **前**真跑一次 `npx lefthook run pre-commit`（runbook 坑④：尾空行）。
- `cd packages/create-linkdesk-plugin && npm publish --registry=https://registry.npmjs.org`（**不要** `npm publish <folder>`，坑②）。
- ⚠️ **`npm publish` 要用户点头**；货架约 3 分钟复制延迟后 `npm run release:mark`。
- **货架保真**（不是看版本号就算）：从真货架真跑一次生成，**解包核生成物里零裸类名**：
  `npm create linkdesk-plugin@latest <tmp> -- --no-git` ⇒ 跑审计工具 ⇒ 违规 0。

---

## 四、判据（逐条给读数）

- [ ] `node scripts/check-scaffold.mjs` 绿（含新断言 11）；`--self-test` 绿（含新负控）
- [ ] 真生成一次（仓外临时目录）⇒ 新类名（`<id>-starter*`）在产物 CSS/TSX 里齐、`starter` 裸名 **0**
- [ ] `npm create linkdesk-plugin@latest` **从真货架**跑出来的产物同样 0 违规（§三）
- [ ] `check:lockfile-sync` 绿 · `check:npm-release` **黄灯灭**（本格**要**发版，与 ① 不同）
- [ ] bump ＋ `CHANGELOG` 段非空（`check-changelog-section` 绿）
- [ ] `npm run check` **EXIT=0**（贴末尾三行）

## 五、负控

| # | 注入 | 期望 |
|:--:|:--|:--|
| NC1 | 模板 CSS 里把 `{{pluginName}}-starter` 改回 `.starter` | 断言 11 🔴 红（报点含文件 ＋ 类名 ＋「应以 `<id>-` 开头」） |
| NC2 | 同上，还原 | 绿（且逐字节还原） |
| NC3 | 模板 TSX 只改 CSS 不改渲染点（反向） | 断言 11 **不红**（它只管 CSS）+ **token 判据红** ⇒ 证「渲染点判据」是另一条腿（本格不引入，但要知道边界） |

## 六、提交与收尾

- 提交信息：`feat:E6#109h-b② 脚手架模板示例类名带 pluginId 前缀 + check-scaffold 断言 11（与判据同源）`。
- 收尾两件：交接.md 顶部追加 ＋ 清单轮次进度与格子勾选（**含 §四 全部框**）。
- 🔴 下一格是 **③ `file-tree` 改名**（[17 号通用作业书](17-任务-件2落地③-⑦-改名轮通用作业书.md)）。

## 七、禁区

1. **不许改模板的视觉/语义**（只改类名标识符；颜色、间距、字号一字不动——硬约束 16 不触发，若发现必须改视觉 ⇒ 停下先调 design skill）。
2. **不许给 CLI 加新占位符**（`{{pluginName}}` 已够）。
3. **不许把判据复制进 `check-scaffold.mjs`**（同源）。
4. **不许跳过货架保真**（「本地生成绿」不等于「作者拿到的是新版」——7.9 那次 npx 缓存暗礁就是这么踩的）。
