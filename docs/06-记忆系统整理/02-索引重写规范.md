# 记忆系统整理 · 索引重写规范

> 对象：`MEMORY.md`（**每次对话自动加载进上下文**，是本记忆库里最贵的文件）。
> 时机：批次 6g——所有合并完成后一次性重写。

---

## 一、为什么必须重写

| 问题 | 现状 | 后果 |
|:--|:--|:--|
| 覆盖不全 | 引用 181 / 250 | **68 份孤儿**——AI 永远撞不见 |
| 行密度过高 | 22K / 126 行，单行塞 5+ 链接 | 加载贵、检索难 |
| 状态块过重 | 顶部 17 行全是 L3.7 细节 | 每轮对话都背一遍历史细节 |
| 与工程文档重复 | 大量 `../docs/...` 长链接 + 摘要 | 记忆库不该镜像工程文档 |

---

## 二、目标结构（8 个分区）

```
# LinkDesk Memory 索引

## 🔴 当前时点（≤ 10 行）
   - 进度真相源指针（工程 E6-执行清单）
   - 软件版本 / 当前层 / 下一层
   - 云端状态（推不推）
   - 封站/暂缓项

## 🔥🔥🔥 新 AI 进场先读（≤ 15 行）
   硬约束级、必读清单、易踩红线

## 🏛 架构与身份（≤ 8 行）
   圆形大厅 / 核心无知 / 通用容器 / 准入标准

## 📜 进化史（≤ 5 行）
   → evolution-chronicle.md 一份 + v2-lessons

## 🐛 bug 与教训（≤ 8 行）
   → bug-atlas.md + 跨时代 bug 类

## 🔌 插件制造（≤ 10 行）
   → plugin-authoring-manual + 三份硬约束独立文件

## 🎨 设计 / UI / 主题（≤ 12 行）

## ⚙️ 工具 / 发布 / 工作法（≤ 15 行）
   → toolbox-sop / version-and-release / ai-workflow-canon

## 📦 04-出厂制造（≤ 5 行）
```

**行数预算：≤ 100 行。** 超出的内容一律下沉到对应记忆文件，索引只留指针。

---

## 三、每一行的格式（两段式）

```markdown
- [短标题](文件名.md) — 一句话钩子（≤ 40 字，只说"什么时候该点开它"）
```

**规则：**
1. **一行一个文件**，禁止一行塞多个链接（现状最大的可读性问题）。
2. 钩子写**触发条件**，不写内容摘要——例：「**加文件前必读**」优于「壳目录规范说明」。
3. 工程文档（`../docs/...`）**最多保留 3 条**（进度真相源 / E6 清单 / version-bump skill），其余删掉——记忆库引用工程文档会随工程移动而腐烂。
4. 同族文件合并后**只出现合并文件**，原文件名不得再出现。
5. 🔥 标记只保留三级以内（`🔥🔥🔥` 封顶），禁止 `🔥🔥🔥🔥🔥🔥`。

---

## 四、重写后的自检（机械可验）

```bash
cd "C:\Users\fengy\.claude\projects\e--linkdesk\memory"
node -e "
const fs=require('fs');
const md=fs.readFileSync('MEMORY.md','utf8');
const linked=new Set([...md.matchAll(/\]\(([A-Za-z0-9._-]+\.md)\)/g)].map(m=>m[1]));
const all=fs.readdirSync('.').filter(f=>f.endsWith('.md')&&f!=='MEMORY.md');
const orphan=all.filter(f=>!linked.has(f));
const broken=[...linked].filter(f=>!fs.existsSync(f));
console.log('行数', md.split('\n').length, '| 孤儿', orphan.length, '| 断链', broken.length);
console.log('孤儿:', orphan);
console.log('断链:', broken);
"
```

**通过判据：** 行数 ≤ 100 · 孤儿 = 0 · 断链 = 0。

---

## 五、记忆文件自身的书写规范（重写索引时一并统一）

- frontmatter 保留 `name` / `description` / `type`；`originSessionId` 可留可删（无检索价值）。
- `description` 必须写**触发条件**而非内容摘要——它是未来 AI 决定"要不要读"的唯一依据。
- 文件顶部若来自合并，加一行：`> 本文件合并自：a.md / b.md / c.md`
- 禁止在记忆文件里复制工程文档全文——**只留结论 + 指针**。这条是本批整理最重要的长期收益。
