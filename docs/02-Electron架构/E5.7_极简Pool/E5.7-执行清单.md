# E5.7 执行清单——极简Pool模型

> 🔴 **2026-08-16 封存——E5.8 已开战。** 本清单不再追加任务，新工程进度唯一真相源 → [E5.8-执行清单](../E5.8_归一化基建/E5.8-执行清单.md)。
>
> 2026-08-12。**从双Pool（SidebarPool + MainPool + OverlayWindow）迁移至极简Pool（1 BrowserWindow + 1 WebContentsView 100%×100%）。**
> **🔥 进度唯一真相源。** 做完一个勾一个。
> **目标：** 统一入口 → 壳 DOM 迁入 → SidebarPool 合并 → OverlayWindow 消除 → Zone 分解 → 浮层归一化 → 分隔线 CSS 化 → 突破边界 → 崩溃恢复 → 清理 → Registry 主进程化 → API 补全 → 硬编码消灭 → 缩放/ESLint → 静态卫生清零 → E6 前置 → 全量回归。
>
> **当前进度：113 个子任务已勾（#0a-#0e + #1-#4 + #12/#12.5 提前 + #5/#6/#8/#9 + #10/#11/#13/#13.5 + #25/#26 + #14 + #15 + #16 + #17 + #18 + #19 + #20 + #21 + #22 + #24 + #27 + #27.5 + #28-#31 + #31.5 + #31.6/#31.7 + #36 + #37 + #38 + #39 + #39b + #40-#45.5 + #45.6 + #46 + #47 + #48 + #49 + #50 + #51 + #52 + #53 + #45.7 + #55 + #59 + #61 + #62 + #54 + #56 + #63.8 + #63.5 + #63 + #58 + #60 + #63.7 + #94-#102 + #81 + #82 + #83 + #84 + #85 + #86 + #87 + #88 + #89 + #90 + #91 + #93）——Phase 0 + Phase 1 + Phase 2 + **Phase 3 完成** + **Phase 4 完成 🎉** + **Phase 5 完成 🎉（#7/#23 取消）——Zone 分解收官：MainZone/PanelZone/RightSidebarZone 三 zone 挂载，占位时代终结，MainRenderer 整删（diff 零丢失证据），占位 CSS 段全删** + **Phase 6 完成 🎉（#27 验证收官——pointer-events 归一化随 08-14 根级 opt-in 已提前落地，零代码变更；⚠️ 连带发现 ProgressBar 挂壳 App.tsx → #27.5 摸底证伪"迁池"前提，用户决策改写死链整删——ProgressBar + NotificationService 进度/DND/过滤死面全删，现代进度 API 走 #16 toast 桥已通）** + **Phase 7 完成 🎉（#28-#31 分隔线简化收官——#28/#29/#30 全部验证零变更（#13/#20/#21/#26 提前消化），#31 死簇 7 文件整删 + LayoutEngine 保留（清单"整删"前提过时——双活链证据：折叠真相源 + 拖拽钳制），#31.5 LayoutEngine 瘦身完成——死肉 5 方法 + floating/mode/undockable/visible 字段全删，活肉侧栏宽度权威 6 方法保留，测试 16→9 段随删（getZone 订正为活体——usePoolSync:729-730 钳制界推送））**。#6 补丁 2026-08-14：图标栏拖拽换位补回（commit 9848333a）；#10 并入 E4V#48 跨容器拖放（经用户确认）；#13.5 侧栏容器持久化（用户验收发现 + 选 B——对标 VS Code 恢复上次侧栏）；#25 FloatingLayerHost + #26 Z_INDEX 常量表就绪（Phase 4 开路）；#14 ContextMenu portal 归一化 + #15 QuickPick/CommandPalette 聪慧→哑桥 + #16 Toast 聪慧→哑桥 + #17 Dialog 聪慧→哑桥 + #18 旧浮层代码清理 + #19 OverlayWindow 空壳整删（Phase 4 迁移件 6/6——OverlayWindow 消除全部收官）；#14/#17 补丁 2026-08-14：池侧浮层 pointer-events 继承穿透——FloatingLayerHost 容器 none 沿 DOM 继承，ContextMenu 菜单本体 / DialogHost backdrop+panel 漏设 auto → 池侧右键菜单点不动+无 hover、对话框点不动（用户现网验收发现）。同日升级根级 opt-in：FloatingLayerHost 4 个 portal root 统一 pointer-events:auto（hosts 关闭时 return null 零 DOM → root 零 hit area，穿透不受影响），各浮层 CSS 的 auto 移除（Toast 例外：直挂容器自设保留）——陷阱反转：忘设 = 正常工作。；快捷键路由补丁 2026-08-14：E5.5#7 的 before-input-event 只挂插件 WebView——极简Pool 时代焦点永远在池上，Ctrl+Shift+P 等全局快捷键全灭（用户现网验收发现——壳被池全覆盖，双池时代"点壳区域"绕行法失效）。attachKeyboardRouting 提取导出 + window-manager.createPoolView 工厂处挂载（初始 + rebuildPool 崩溃恢复全覆盖）——Phase 10 #43 的挂载侧提前消化，删 registry 遍历留给 #43。#27.5 执行 2026-08-15（前提证伪后改写死链整删，见任务执行注）；#31.5 执行 2026-08-15（LayoutEngine 瘦身——死肉删活肉留，见任务执行注）；**Phase 7.5 完成 🎉（#31.6/#31.7 池开发预览——浏览器 mock，Codex UI 设计通道；dev/ 三文件 + preview.html 仅 vite dev 入口，生产构建零污染实测）**；**Phase 9 完成 🎉（#36 两分支重建 + 审计 3 补强 / #37 心跳 5s ping 10s 超时 / #38 Hot Exit 文件版备份 / #39 内存监控单 Pool 化 + 死链修活 / #39b 连续崩溃防护静态错误页——崩溃恢复五件套全部落地，见各任务执行注）**。现网实测 2026-08-15 六项全过：Hot Exit 杀应用重开内容恢复带 ● / 池崩重建布局原样 / 3 连崩静态错误页 / 壳崩全窗口重建 / 心跳假死 10s 强制重建 / 内存压力 toast（测试方法见 #39 执行注）。**Phase 10 完成 🎉 2026-08-15（#40 整删 → #41-#45.5 全落地，门禁见各任务执行注）**。**Phase 10 UI 回归 Bug 修复 2026-08-15：Bug A（串口接收区右键菜单空 + 行号命令切走切回才出现）——React 18 eager state 根因：createTab 返回值从 setState updater 内读出，fiber 有 pending lane 时 updater 延后执行 → 返回恒空串 → tab:focused 不发 → activeEditor 断链 → when:"activeEditor == 'serial-monitor'" 过滤全灭。修复 useTabManager 7 函数 eager 预计算 + 第三 emit 入口（commit a5b40553 + 埋点清除 2e2b18bb，用户验证通过）；Bug C（命令面板执行池注册命令无效果——日志钉死：打中壳侧占位 handler"尚未绑定"warn）——loader 元数据命令只注册占位 handler，真实 handler 在池 preload _poolCommands，壳侧执行永远到不了池。修复：Command placeholder 标记 + 壳→池转发桥（executeInPool pending map 10s 超时 → events.emit commands:executeRequest → plugin:push 广播 → 池 preload 订阅 executeLocal 执行 → invoke commands:executeResult → resolvePoolExecution 回传——5 文件：CommandRegistry/loader/preload-pool/ipc-bridge/IpcBridgeHandler，零新 API 面，无 preload 桥时回退诊断 warn 行为不变；门禁 tsc 0×2 / eslint 2+859 零新增 / vitest 393 基线 1 败 / grid ✓ / pool-css ✓，待用户验证）；**Bug C 补全 2026-08-15（用户验收三症状：①"系统消息独立显示"等运行时命令面板不可见 ②消息回显开关后标题不变 ③重启后串口标签右键空+面板空，关重开才恢复）——根因两处：E5.6#11.5h 迁移把 registerCommand 元数据面砍成 (id, handler)（title/category/when 丢失 → 面板不可见 + 静态标题），App 恢复 effect 不发 tab:focused（重启后 activeEditor null → when 过滤全灭）。修复为架构级通用能力：池 registerCommand(id, handler, meta?) → commands:register IPC → 壳 registerPoolCommandMetadata（已有条目只更新 title/category/when 不碰 handler；缺失条目建 placeholder 运行时条目——插件未在 plugin.json 声明的命令同样可见可执行）+ unregisterCommands 同步（只删运行时条目，loader 元数据幸存）→ 类型化 linkdesk-api 文档注释（未来插件零壳改动直接受益）；restoreLayout 返回聚焦标签（eager 预计算，Bug A 教训）+ App 恢复 effect emit tab:focused（Bug D）。串口插件恢复 E5.6 语义：16 命令全带 meta（send when:"false" 纯程序化）+ 7 toggle effect 动态标题照抄 f9cf8c35^ + SEND_MODE_TEXT/HEX 常量归一化（规避自定义 ESLint BinaryExpression>Literal 拦截）。门禁 tsc 0×2 / eslint 2+854（零新增，比基线少 5）/ vitest 393 基线 1 败 / grid ✓ / pool-css ✓，✅ 用户验证 2026-08-16 无误）；**Bug C 补全·症状 4 2026-08-15（用户验收：串口已打开，面板里仍无"暂停接收"）——根因：togglePause 的 when 带 "&& sourceOpen"（plugin.json 三处，E5.5 设计——没开串口不显示暂停），而池化后插件直接调 lk.serial.openPort 绕开壳 handleToggleOpen，壳 isOpen 只在两条路径更新：AppInitializer 启动串口态 + serial-system 监听器——后者 d3b28380 只补了 invokeBeforeClose 的关闭分支（/Port closed|关闭/），打开消息（serial-service.ts "---- 已打开串行端口 X ----"）无人认领 → 壳 isOpen 恒 false → sourceOpen 恒 false → when 过滤全灭。修复：监听器补开分支（/已打开/ → setIsOpen(true)，关闭分支不变；"串口打开失败"消息不含"已打开"零误匹配——三条 system 消息原文逐一核对）。门禁 tsc 0×2 / eslint 2+854 零新增 / vitest 393 基线 1 败 / grid ✓ / pool-css ✓，✅ 用户验证 2026-08-16 无误（开串口搜"暂停"出现 + 标题翻转）**；**Bug C 补全·收尾 2026-08-15（用户批准 1+2）**：①SerialSourceContext 死代码审计——handleToggleOpen 系列 + context + 上游 state 全仓零消费者（isOpen 活体除外）→ 登记死代码任务 #45.6（Phase 10 收尾后续报，未执行，待用户拍板）；②命令元数据真相源书面声明——"壳 registry = 显示真相源，池 _poolCommands = 执行真相源"写入 CommandRegistry.ts / preload-pool.ts / linkdesk-api.ts 三处注释（防下个 AI 误判哪份是权威）**；**#45.6 执行 2026-08-15（用户拍板"可以"）**：SerialSourceContext 死簇整删落地——App.tsx −100 行（三 handler/六 state/两 ref/三 effect/Provider 空 div/轮询常量/import）+ SourceStateContext.tsx git rm + v3Api 导出摘除；isOpen + serial-system 监听器原样保留（sourceOpen 链——症状 4 修通的 when 过滤继续有效）；过期注释订正（sidebar.tsx 已不存在 + useConfiguration 插件已零使用）。门禁 tsc 0×2 / eslint 2+848 反降 6（死簇携带的 any 警告随行消失，零新增）/ vitest 393 基线 1 败 / grid ✓ / pool-css ✓，用户验证通过（sourceOpen 链回归无异常）**；**#46 执行 2026-08-15（Phase 11 开战——用户拍板"开始吧"）**：Registry×进程 矩阵落盘（17 模块全量摸底，见任务执行注 + [Registry×进程矩阵.md](Registry主进程化/Registry×进程矩阵.md)）——#51 删除前提实证成立（两 handler 零调用 + 读主进程空实例）、marketplace 活 bug 补强 #56、CardRegistry/DataDispatch 遗留空壳待拍板、FileDecoration 注册方缺失归 #60。零代码变更（纯审计文档）**；**#47 执行 2026-08-15**：预加载方案骨架落盘——设计 §2 扩为 5 小节（时序/数据流+builtin 特例/增量同步/直连通道表/优先级+回退），§4 文件清单补 registry-handlers.ts + loader.ts + App.tsx，矩阵勘误（ProtocolRegistry 写入方仅 ensureBuiltinProtocols——grep 实证）。零代码变更（纯设计文档）**；**#48 执行 2026-08-15**：plugin-manifest-loader.ts 落地——主进程三表预加载 + 装/卸/重装全清全重扫通路 + builtin 协议汇入主进程实例（写入方唯一性第一次以代码形态存在；消费端 #49/#50 切换）。连带：preload-shell notifyManifestChanged / loader 三成功路径通知 / electron tsc include 补 global.d.ts。门禁 tsc 0×2 / eslint 2+848 零新增 / vitest 393 基线 1 败 / grid ✓ / pool-css ✓**；**#49 执行 2026-08-15**：LangDef + Protocol 直连切换——registry-handlers.ts（4 通道）+ preload-pool 去包装 + IpcBridgeHandler 5 case 删 + 壳侧三处写入移除（loader/App/lifecycle）+ 测试契约钉"壳不写"。壳 bundle 自此零 import 两 Registry。门禁 tsc 0×2 / eslint 2+848 零新增 / vitest 393 基线 1 败 / grid ✓ / pool-css ✓，✅ 用户验证 2026-08-16 无误**；**#50 执行 2026-08-15**：FileAssociation 直连——registry-handlers 直答 + PROXY_CHANNELS 摘通道（同 commit 防重复注册抛异常）+ 壳侧写入清零（loader/lifecycle）+ IpcBridgeHandler case 删。壳侧 FileAssociationService 零消费者实证，池消费面零改动。门禁 tsc 0×2 / eslint 2+848 零新增 / vitest 393 基线 1 败 / grid ✓ / pool-css ✓**；**#51 执行 2026-08-15**：git rm 两死 handler（删除前提 #46 实证 + #49 已摘 import/注册）。grep 全仓零残留。门禁 tsc 0×2 / eslint 2+848 零新增 / vitest 393 基线 1 败**；**#52 执行 2026-08-15**：ESLint 机械防线落地——electron/ 禁 import 壳侧注册表模块（registry/ 整目录 + FileAssociationService/ViewContainerService），白名单只放行 plugin-manifest-loader + registry-handlers 写入/读取对，import type 放行。scratch 文件实测 pattern 语义六项全过（两种深度 + 无后缀 + type 放行 + 白名单外放行），主块 CardRegistry 规则 electron/ 下并存生效。门禁 tsc 0×2 / eslint 2+848 零新增 / vitest 393 基线 1 败 / grid ✓ / pool-css ✓**；**#53 执行 2026-08-15（Phase 11 收官）**：全量回归——npm run check 基线 850（2+848）；五提交面审计 FileDecoration/壳功能零触碰；壳侧三表 import 归零实证；池消费链端到端核对（EditorView:114 / ControlPanel:41-80 / FoldersView:376+SearchView:139）；回归摘除 core/index.ts:73 桶 re-export 口子（registerBuiltinProtocols 零消费方但诱壳侧空实例回潮）。门禁 tsc 0×2 / eslint 2+848 零新增 / vitest 393 基线 1 败 / grid ✓ / pool-css ✓，✅ 用户验证 2026-08-16 无误。**Phase 11 完成 🎉（#47-#53 七任务——Registry 主进程化：静态声明三表唯一写入方收敛主进程 + 直连 IPC + ESLint 机械防线）**；**#45.7 执行 2026-08-15（#46 矩阵发现 ④ 摸底定性 → 用户拍板整删，同日落地）**：CardRegistry + DataDispatch 死簇整删——git rm 两文件 + lifecycle 摘 unregisterPluginCards + 测试摘第 5 段 + eslint 摘 CardRegistry 规则/例外块 + CLAUDE.md 硬约束 #3 原则化（不引用已删模块）+ 矩阵文档记定性结论。死因：CardRegistry = Phase 5 柱子 5 骨架（卡片工作台插件从未存在于此树，20+ commit 只搬家）；DataDispatch = 被"独立 RingBuffer 多消费者"设计决策取代的 Pub/Sub。门禁 tsc 0×2 / eslint 2+844 反降 4 / vitest 392 基线 1 败（393→392 恰删 1 测试）/ grid ✓ / pool-css ✓ / 残留 grep 仅迁移注释**；**#55 执行 2026-08-15（Phase 12 开战）**：workspace 验证 8 方法在位 + 六通道壳侧闭合；审计口径修正（plugins/ 残留 1 处 type import 漏算）→ FoldersView 摘除换本地 WorkspaceFolderDto（#97 后换 ambient）。门禁 tsc 0×2 / eslint 2+843 反降 1 / vitest 392 基线 1 败 / grid ✓ / pool-css ✓**；**#59 执行 2026-08-15**：events 全链验证——event-system {on,emit}+dispose / preload 双端暴露（pool:803 shell:308）/ API 文档 167-171 在列 / 插件值 import 归零。零代码变更（纯验证）。连带：FileEntry type import 旧路径（services re-export）+ FileDecoration type import 分别记给 #62/#60**；**#61 执行 2026-08-15**：protocol 验证零代码变更——三方法在位（preload-pool.ts:762-773，任务注 535 行号漂移）+ 插件 ProtocolRegistry 值 import 归零 + registerParser 系列按需跳过（零插件消费）。连带发现记 #97：linkdesk-api.ts 文档面缺 protocol 等 11 命名空间（window.linkdesk ambient 类型仍是 Record<string, any>，类型接线整体归 #97 契约类型任务）**；**#62 执行 2026-08-15（验证类四连收官）**：六命名空间实体核对在位（clipboard/keybindings/contextKey/search/encoding/langDef 全真 IPC）+ sidebar/data 按需删条目 = 零删除（三处文档无过期条目）+ 插件 value import 归零 + FileEntry 旧路径收尾 4 处直指 types/fileEntry 正源 + FileService re-export 摘除（#59 连带，桶消费零实证）。门禁 tsc 0×2 / eslint 2+843 零新增 / vitest 392 基线 1 败 / grid ✓ / pool-css ✓**；**#54 执行 2026-08-15**：pool.* 简化——poolId 全仓零活体（仅历史注释）；zone 残留链 3 处全摘（_poolZone 死链 + ready() + 主进程转发 + 壳 onReady zone 过滤，零调用方）；sidebarAction/tabAction 意图通道保留。门禁 tsc 0×2 / eslint 2+843 零新增 / vitest 392 基线 1 败 / grid ✓ / pool-css ✓**；**#56 执行 2026-08-15**：marketplace 迁移——双进程执行归一化（壳 preload 补 commands 命名空间 + _executeShellLocal 执行桥 + registerShellLocalCommand 真实条目；池侧 Bug C 补全半程不变，两半程幂等汇合）；插件文件三处 @src import 全摘（registerCommand/MenuId/getLoadedPluginManifests）+ count 隐性 bug 修（池进程恒 0 → useMarketplacePlugins）。**里程碑：插件侧 @src 值 import 全仓归零——Phase 12 结构性目标达成 🎉。** 门禁 tsc 0×2 / eslint 2+842 反降 1 / vitest 392 基线 1 败 / grid ✓ / pool-css ✓，待用户验证 marketplace 齿轮菜单三命令 + 主视图插件计数。**用户实测：禁用/卸载点击无效 → be19e6b1 修复**——handler 保留旧壳直注册 (_token,...args) 签名，两半程 infra 均先剥 token 再调 handler，{ pluginId } 被当 _token 吞掉 → args[0] 恒 undefined 静默 no-op。改 (...args)（file-tree 池侧同款约定），✅ 用户复验通过 2026-08-16。**；**#58 执行 2026-08-15**：viewContainer 桩换真 IPC——摸底发现桩下隐藏真消费方（FoldersView/SerialSettingsView 动态标题 registerView + getView——审计只 grep src 漏插件目录）；contextBridge 克隆边界最小 Electron 实验实证（嵌套函数过桥不抛、invoke 真克隆必抛）→ 写方向池侧白名单剥壳；四方法真 IPC（getViewContainer/getViews/getView/registerView 问壳注册表）+ 四代理通道 + 四 handler case；元数据更新经壳注册表 → onDidChangeActiveViews → 重推布局闭环（marketplace:updateBadge 先例链路）。连带：①#19e 广播通道名连字符/驼峰不符自落地零触发——改 camelCase 对齐 marketplace 订阅；②#19e payload 只剥 render → toViewDto（actions/pinnedContent 防未来克隆抛）。API 契约补 §3.23 + 表格行。门禁 tsc 0×2 / eslint 2+841 反降 1 / vitest 403 基线 1 败 / grid ✓ / pool-css ✓，**用户验证通过 2026-08-16**（三查询全通 + 动态标题 _testfiles 实显侧栏）**；**#60 执行 2026-08-16**：decorations 注册表池内化——摸底钉死设计定性（provider 是 JS 函数不可跨进程，真源与消费方同池，不进 IPC；现链是 E5.6 残影：池 getDecoration 问壳恒空注册表恒 null）。实现：preload-pool 池内本地注册表（_decoProviders Map + _decoListeners Set，contextBridge 函数代理存 provider——#58 实验实证可用）+ 四方法真实现（registerProvider 幂等覆盖+注册即全量刷新 / unregisterProvider / getDecoration 池内直答同步契约 / onDidChange 本地 Set）；壳链整删——ipc-bridge 摘通道 / IpcBridgeHandler 删 case+广播+三处 unsub / src/core/registry/FileDecorationRegistry.ts git rm（桶行随删）；#59 连带 FileTreeModel @src type import 换本地接口；API 契约 §3.24 + 矩阵定性更新。门禁 tsc 0×2 / eslint 2+840 反降 1 / vitest 403 基线 1 败 / grid ✓ / pool-css ✓。**用户验证曝刷新链 bug（2026-08-16，已修）**：注册后已加载节点徽标不出现——消费侧 refresh 从未被真实触发过（旧壳广播链恒静默）。根因三处：①attach 回调只清缓存 + fire——已加载节点不再走 getChildren（装饰器只在新节点创建时调）→ 徽标永不重查；②SidebarZone keep-alive 不重挂载——侧栏切走切回无济于事；③FileTreeNode memo 浅比较全等跳过——item 引用未变，装饰变更不可见。修复：全量重查已加载节点（配置批量读）+ decoration 单独走 prop（新引用破 memo）。**用户验证通过 2026-08-16（注册即全图 T / 折叠展开保持 / 注销即消失 / 侧栏切走切回保持）**；**#63.7 执行 2026-08-16（PanelZone 数据生产者三件套——用户批准"可以"）**：三件套全落地（①usePoolSync buildPanelViewMetas——location:"panel" 容器×活跃视图两级展平含 renderPath，views 非空才推 panel 字段；②PanelZone 空 div → PluginComponent 动态加载 + createTooltip 壳推；③LayoutEngine 新 panel zone（bottom dock 220 钳制 120-600）+ resizeZoneHeight + App 事件桥/panelActiveViewId/恢复/防抖保存 + LayoutService panel 字段）。连带：_recalculate 多 bottom zone 堆叠循环改写（原同 y 重叠隐患随 panel 加入暴露）。门禁 tsc 0×2 / eslint 2+840 零新增 / vitest 404 基线 1 败（+1 resizeZoneHeight 新测试）/ grid ✓ / pool-css ✓。✅ 用户验证通过 2026-08-16（panel-demo 已入仓库声明 location:"panel"——底部面板输出/待办实测正常）** + **Phase 14.5 完成 🎉（静态卫生清零 #94-#102 九任务收官）**（eslint errors 历史首次清零 → 规则收窄 → 字符串比较分类 + tabAction wire 契约 → wire DTO 契约归口 + preload/API 边界 any 清零 → 插件侧 + 壳 src 剩余 any 清零 → exhaustive-deps 22 处逐个修 → active-guard 5 处 + 硬编码中文 4 处清零 → v3Api 零消费整删 + v3 selector 升 error → vitest 断言修复 + 门禁收口——**eslint 全仓 0+0 历史首次全零 🎉 + vitest 409/409 历史首次全绿 🎉 + npm run check 门禁收口 --max-warnings 0 自证通过**，执行注见各任务）；**#86 执行 2026-08-16（Phase 16 场景 3 分屏收官——七行代码侧核对 + 「乐观本地+提前释放」类 bug 两实例当场修复：①分隔线松手回闪（localSizesRef 提前释放 → pendingSplitsRef 回执对齐，commit 35ed5817）②标签排序从未提交+回闪（findOtherContainer el===ownContainer 死判断 → 排除源组 + 回执对齐，commit 3878b54c）+ 池 console 入 protocol-debug.log 诊断基建。门禁 tsc 0×2 / eslint 0+0 / vitest 433/433 / grid ✓ / pool-css ✓。✅ 用户实机验证 2026-08-16：场景 3 七行全过 🎉，执行注见任务条目）**；**#87 执行 2026-08-16（Phase 16 场景 4 浮层收官——十行代码侧核对零代码变更：右键菜单三处/边界翻转/消失四路径/命令面板 150ms 防抖/Toast 5s+栈序/Dialog 焦点陷阱/键盘导航/语言纯净全链路在位。门禁 tsc 0×2 / eslint 0+0 / vitest 433/433 / grid ✓ / pool-css ✓（实跑自证）。✅ 用户实机验证 2026-08-16：正常——场景 4 十行全过 🎉，执行注见任务条目）**；**#88 执行 2026-08-16（场景 5 底部面板——用户拍板直接勾掉：②切换 ④拖高 ⑤Toast 位置已实现且 #63.7 验收已实测；①Ctrl+J 显隐命令 ③[+] 新建两行如实记未实现——已知未做项，决策轨迹见 memory 面板定案。执行注见任务条目）**；**#89 执行 2026-08-16（场景 6 串口监视器收官——五行代码侧核对全通 + 回归风险 3 bug 防护在位；用户验收曝侧栏绿灯 bug 当场修复：换端口顶替时 isOpen 恒 true 不产生 :isOpen 事件，useSerialConnection 补 :sourceName 键订阅换灯（isOpen/sourceName 合并单状态），commit e3b30770。✅ 用户复验 2026-08-16 无更多 bug，执行注见任务条目）**；**#90 执行 2026-08-16（场景 7 插件热重载——四行代码侧核对零代码变更；①改代码保存不即时 HMR 为有意设计（vite watch ignored plugins/ 防装卸 churn），实际语义「保存 → F5 → 更新」如实记录，用户验收接受。✅ 用户实机验证 2026-08-16：正常，执行注见任务条目）**；**#91 执行 2026-08-16（场景 8 崩溃恢复——五行代码侧核对零代码变更；用户拍板直接勾掉：五行均在 2026-08-15 Phase 9 现网实测全过（六项），不重测。执行注见任务条目）**；**#93 执行 2026-08-16（场景 10 收尾检查——门禁实跑全绿 + grep 六项零残留（仅迁移注释零活代码）+ 用户验证进程数 = 3 + F5 正常。✅ 用户实机验证 2026-08-16：正常——**Phase 16 全量回归收官 🎉（#86-#91 + #93，#92 推迟 v1.3）——18 Phase 全部完成**，执行注见任务条目）**
>
> 📖 **架构设计：** [01-极简Pool设计.md](01-极简Pool设计.md)
> 📖 **Zone 分解：** [Zone系统/Zone分解设计.md](Zone系统/Zone分解设计.md)
> 📖 **浮层归一化：** [浮层系统/浮层归一化设计.md](浮层系统/浮层归一化设计.md)
> 📖 **脱出窗口：** [脱出窗口/脱出窗口设计.md](脱出窗口/脱出窗口设计.md)
> 📖 **崩溃恢复：** [崩溃恢复/单Pool崩溃恢复设计.md](崩溃恢复/单Pool崩溃恢复设计.md)
> 📖 **清理方案：** [清理/清理方案.md](清理/清理方案.md)
> 📖 **Registry 主进程化：** [Registry主进程化/Registry主进程化设计.md](Registry主进程化/Registry主进程化设计.md)
> 📖 **API 变更：** [API补全/API变更清单.md](API补全/API变更清单.md)
> 📖 **硬编码清扫：** [硬编码消灭/硬编码清扫方案.md](硬编码消灭/硬编码清扫方案.md)
> 📖 **静态卫生清零：** [静态卫生清零/静态卫生清零方案.md](静态卫生清零/静态卫生清零方案.md)
> 📖 **回归矩阵：** [回归验证/全量回归矩阵.md](回归验证/全量回归矩阵.md)
> 📖 **任务迁移映射：** [迁移/E5.6到E5.7任务映射.md](迁移/E5.6到E5.7任务映射.md)

---

## 关键路径

```
Phase 0：E5.6 归档（串行——文档标记）
Phase 1：统一入口（串行——PoolLayout v2 + PoolZoneShell 骨架）
Phase 2：壳 DOM 迁入 Pool（串行——Phase 1 后）
Phase 3：SidebarPool 合并（串行——Phase 1 后）
Phase 4：OverlayWindow 消除（串行——Phase 3 后）
Phase 5：Zone 分解（串行——Phase 2 后）
Phase 6：浮层收尾（串行——Phase 4 后；#25/#26 已提前至 Phase 4）
Phase 7：分隔线简化（串行——Phase 3 后）
Phase 7.5：池开发预览（开发工具——浏览器 mock；Phase 7 后，独立于主链，生产构建零污染）
Phase 8：突破边界——脱出窗口（Phase 5 后）
Phase 9：崩溃恢复（串行——Phase 1 后即可，推荐 Phase 5 后）
Phase 10：清理死代码（串行——Phase 5 后，确认新模型稳定）
Phase 11：Registry 主进程化（Phase 10 后）
Phase 12：API 补全（Phase 1 后即可开始）
Phase 13：硬编码消灭（Phase 12 后）
Phase 14：缩放联动 + ESLint（Phase 1 后）
Phase 14.5：静态卫生清零（Phase 14 后——tsc/ESLint/vitest 卫生 + 门禁收口；排序铁律：先删死文件再修警告）
Phase 15：E6 前置（IPC 骨架，不等 E6 返工）
Phase 16：全量回归（最后）
```

---

## 现状速查

| 文件 | 当前状态 | E5.7 动作 |
|:--|:--|:--|
| `electron/window-manager.ts` | 双Pool WCV 管理 + 多实例遗留 | → 简化为单 WCV 管理 |
| `electron/overlay-window.ts` | OverlayWindow 类 ~400 行 | → 整文件删除 |
| `electron/pool-layout.ts` | PoolLayout v1 + poolId | → 升级 v2 全量字段 |
| `src/pool/pool-main.tsx` | zone 路由 + RENDERERS 注册表 | → 单入口 PoolZoneShell |
| `src/pool/sidebar/` | SidebarRenderer + 子组件 | → 迁入 zones/SidebarZone.tsx |
| `src/pool/main/MainRenderer.tsx` | 693 行超级胶水 | → 分解为 MainZone + PanelZone |
| `src/App.tsx` | 壳 DOM（TitleBar/IconBar/TabBar/StatusBar） | → 迁入 zones/ |
| `src/hooks/useWebViewSync.ts` | ~270 行 per-tab 遗留 | → 整文件删除 |
| `electron/preload-pool.ts` | window.linkdesk.* + pool.onLayout | → 保留，去 poolId 逻辑 |

---

# Phase 0：E5.6 归档

> 不动代码。标记文档，让后续 AI 知道方向已变。

- [x] **E5.7#0a** E5.6 执行清单顶部加废弃声明 → 指向本清单（执行注 2026-08-13：废弃声明已存在——建 E5.7 分支时已加在 E5.6-执行清单.md:3-10，本次核验后勾选）
- [x] **E5.7#0b** E5.6 设计文档（01-Pool模型设计 / 多Pool扩展预留）顶部加 "已被极简Pool取代" 声明（执行注 2026-08-13：两份文档顶部已有"🔴 已废弃——2026-08-12 架构切换至极简Pool"声明（01-Pool模型设计.md:3-6 / 多Pool扩展预留.md:3-8），含继承说明与 E5.7 文档链接——语义即"已被极简Pool取代"，核验后勾选）
- [x] **E5.7#0c** 检查 e5.6 分支未提交改动——`electron/overlay-window.ts` focusable 键盘交互修复已提交（e5.6 收尾 commit）
- [x] **E5.7#0d** 新建 `e5.7` 分支（从 e5.6 切出）——所有 E5.7 改动在新分支
- [x] **E5.7#0e** 🔴 基线噪音记录——e5.6 交接时 `npm run check` 有 4 项存量问题，非本次引入：eslint 3 error（`ShellPluginComponent.tsx:59` useMemo 条件调用 / `SidePanel.tsx:1283` 手写 path replace / monaco `configureWorkerFactory` 内调 useWorkerFactory）+ vitest 1 失败（`DataConverter` UTF-8 跨 realm Uint8Array 相等）。三个 eslint 文件都在 E5.7 清理方案删除/重写名单上（ShellPluginComponent git rm、SidePanel 迁 zones），随 Phase 10 自然消灭；DataConverter 测试在 Phase 16 回归时修。（执行核验 2026-08-13：npm run check 实测与记录一致——tsc 零错误 / eslint 3 error 逐一相符 / vitest 1 failed 399/400 通过）

---

# Phase 1：统一入口——PoolLayout v2 + PoolZoneShell

> 目标：pool.html 单 zone。PoolLayout 升级全量字段。唯一根组件 PoolZoneShell。
> 📖 PoolLayout v2 完整类型 → [01-极简Pool设计.md §7.2](01-极简Pool设计.md#72-e57-的-poollayout全量唯一)
> ✅ 完成标准：tsc 零错误 + 应用可启动（壳 DOM 原样正常、Pool 渲染骨架占位）+ pool:ready / onLayout 正常

- [x] **E5.7#1** `src/core/types/poolLayout.ts`——version 1→2，加全量字段
  - 新增 `titleBar` / `iconBar` / `statusBar` / `rightSidebar?` / `panel?`（🔴 2026-08-13 审计删 `tabBar` 字段——#7 TabBarZone 取消，tab bar 从 `groups[].tabs` 渲染，无需独立字段；同审计删 `topBar`——#23 TopBarZone 取消，顶栏是规范文档已移除部件）
  - 保留 `groups` / `root?` / `sidebar` / `creatableViews?`
  - 删 `poolId`——只有一个 Pool
  - （执行注 2026-08-13：commit 0039ecda。v2 全量字段 + TitleBarLayout/IconBarLayout/StatusBarLayout/rightSidebar/panel 新类型齐备。"删 poolId" 无代码对象——grep 核实 electron/ + src/core/types/ 零 poolId 命中（E5.6 代码从未实现设计稿的 version:1/poolId，纯设计文档概念），此事实已记入 poolLayout.ts 头注释）
- [x] **E5.7#2** `src/pool/pool-main.tsx`——去 zone 路由
  - 删 `RENDERERS` 注册表 + `?zone=` 解析
  - 改为直接渲染 `<PoolZoneShell layout={layout} />`
  - `pool:ready` + `pool.onLayout` 不变
  - （执行注 2026-08-13：commit 911a8e9f。RENDERERS/zone 解析/无效 zone fallback 全删；pool:ready + onLayout 链未动；v2 无空对象初始值 → layout 初始 null + 首推到达前渲染 null（缓冲回放保证 ready 后立即到达））
- [x] **E5.7#3** `src/pool/PoolZoneShell.tsx`——新建唯一根组件
  - flex column 全窗口布局 + zone 条件渲染
  - 🔴 占位策略：Phase 1 版用占位 div——zone 组件在 Phase 2/3/5 逐个替换占位（每 Phase 结束必须 tsc 零错误 + 应用可启动；不允许 import 尚不存在的 Zone 组件）
  - 📖 代码骨架 → [01-极简Pool设计.md §6.1](01-极简Pool设计.md#61-poolzoneshell唯一根组件)
  - （执行注 2026-08-13：commit 6982c33f。flex column 骨架 + index.css 占位样式（全 CSS 变量）；sidebar/panel/rightSidebar 按 layout 字段条件渲染；各占位 div 注释标注 Phase 2/3/5 替换任务号；FloatingLayerHost #25 注释预留未 import）
- [x] **E5.7#4** pushLayout 适配——两端同步改
  - `electron/window-manager.ts`——`pushLayout(layout)` 去 `poolId` 参数（单 WCV 直推）
  - 壳侧 `syncLayout()`（App.tsx / usePoolSync.ts）——构建完整 PoolLayout v2（🔴 syncLayout 实现在壳渲染进程，不在 window-manager）
  - （执行注 2026-08-13：commit 26b8af7c。window-manager pushLayout(layout) 单直推——destroyPool/rebuildPool/getPoolView/getAllPoolViews 同步去 zone；pool-handlers 8 个 handler 去 zone 参数；preload-shell pool API 同步；App.tsx bounds 单推 main（保留 SplitHandles 留缝 gap 数学）；usePoolSync 构建完整 v2 单推。#12 已按中间态说明随本任务提前执行，见下）

---

# Phase 2：壳 DOM 迁入 Pool

> 目标：TitleBar / IconBar / StatusBar 变 React Zone。壳 App.tsx 保留全部状态逻辑（tabState/syncLayout/pushLayout/Registry/命令执行），只删 UI DOM 渲染。
> 🔴 2026-08-13 审计：#7 TabBarZone 取消——tab bar 留在 MainZone 内（每 panel 自带 GroupTabBar，E5.6#16.5"自包含"教训 + 拖拽协调不拆），吸收进 Phase 5 #20。
> 📖 Zone 职责表 → [Zone分解设计.md](Zone系统/Zone分解设计.md)

- [x] **E5.7#5** `src/pool/zones/TitleBarZone.tsx`——新建
  - `-webkit-app-region: drag` + no-drag 按钮区 + 窗口控制 IPC
  - 🔴 前置：`preload-pool.ts` 补 `window` 命名空间（minimize/maximize/unmaximize/close/toggleDevTools/isMaximized/onMaximizeChange——从 preload-shell.ts 同款搬入。2026-08-13 审计：preload-pool 现无此 API，TitleBarZone 在池内要用）
  - 📖 → [Zone分解设计.md §2.1](Zone系统/Zone分解设计.md#21-titlebarzone)
  - （执行注 2026-08-14：commit 13ea6d32。数据流定案=壳推全量（显示文本铁律）：TitleBarLayout 扩 logoUrl（壳 getAssetPath——Path B 池不碰 core）/menuGroups（壳做 group 分组+flattenGroupItems 展平+递归 t() 翻译+order 排序）/slots（when 壳侧 ContextKeyService 过滤）/windowControls 4 tooltip（壳 t()）；usePoolSync 增 ContextKeyService.onDidChangeContext + i18n languageChanged 订阅→layoutVersion 重推（壳 TitleBar 重渲染的池等价物）。TitleBarZone 哑渲染：fixed 下拉（WCV 满窗无裁剪，不需要 OverlayPortal——下拉在 30px 拖拽区之下满足硬约束 #18）+ 递归 children 子面板（hover 100/150ms 同壳时序）+ 外部点击/Escape 关闭 + ─ □ × 最大化状态。诚实注记：①壳 TitleBar 本就不渲染 title 文本（仅 Logo）——照搬零丢失；②壳下拉无快捷键无 when 灰显（checkWhen=false）——池同；③设计稿 §2.1 的 Alt 调出隐藏菜单栏壳未实现——池也未实现（远期）；④菜单注册表无 onDidChange——插件卸载后菜单按钮需等下次 pushLayout 才消失（壳 TitleBar 同样依赖重渲染，非新回归））
- [x] **E5.7#6** `src/pool/zones/IconBarZone.tsx`——新建
  - 42px 图标列 + 激活高亮 + `icon:selected` 事件
  - 📖 → [Zone分解设计.md §2.2](Zone系统/Zone分解设计.md#22-iconbarzone)
  - （执行注 2026-08-14：commit 8c2ae85c。数据流=壳推全量：IconBarLayout 扩 icons（壳 getViewPlugins+iconOrder 排序+getIconLocation 过滤——无 iconBar 声明不渲染，壳 filter 同款；icon 序列化为判别联合 lucide/codicon/img/emoji，池 PoolPluginIcon 哑渲染——LUCIDE_MAP 与壳 PluginIcon 同步维护）+activePluginId（壳侧双重守卫：侧栏展开+活动容器属于该插件）+hamburgerVisible/hamburger.navLabel/title/groups（menuStyle hamburger/both；groups 壳 MenuRenderer showGroups+showKeybindings+checkWhen 语义——与 titlebar 关键差异=不展平父项"文件/查看"保留 children hover 子面板；快捷键 formatKeyLabel、when 灰显壳侧已判）+navLabel t("导航")。点击→池 events.emit("icon:selected")→主进程 plugin:emit→壳 plugin:push→App 新增桥接 effect→shellEvents→MainContent 开标签。onDidRegister/onDidUnregister→layoutVersion 重推（Phase 5h Step 1 同款——安装插件图标即时出现）。共享组件提取 3 件：MenuItemList（#5 TitleBarZone 内提取，汉堡复用）/PoolPluginIcon/executePoolCommand。诚实注记：①拖拽换位——原设计 §2.2"可选——远期"，2026-08-14 按零丢失铁律补回（见下方 #6 补丁执行注）；②底部齿轮菜单（MenuId.ExtensionGear）推迟 Phase 4 #14——过渡期点击齿轮无动作，设置从 文件→打开设置 可达；③壳 icon-btn 48×48 在 42px 列溢出——池按设计修正 42×42；④汉堡子面板壳仅 2 层，池递归 N 层（实际数据 ≤2 无感）；⑤css 归一化：active 径向高亮 rgba→color-mix(var(--accent) 12%)，汉堡阴影→var(--context-menu-shadow)）
  - （#6 补丁执行注 2026-08-14：commit 9848333a——零丢失铁律。设计 §2.2"可选——远期"全清单无任务认领=静默丢功能，经用户确认现补。数据流（#13 同款乐观本地+commit，真相源在壳）：池 IconBarZone 增壳 IconBar.tsx 状态机照搬——mousedown 记起点+5px 阈值防误触+mousemove findTarget（整列落点——壳只认 icon-bar-top、底部图标不可作落点=壳实现遗漏，注记⑤补全）+mouseup 全序 splice→setLocalIcons 立即渲染+events.emit("icon:reordered", ids)→主进程 plugin:emit→壳 plugin:push→App 新增桥接 effect→shellEvents→E5#7d 订阅持久化 iconOrder→usePoolSync 新增订阅重推权威序→池 localIcons 对齐（拖拽期间 draggingRef 忽略推送防闪跳）。拖影 42px（鼠标 −21 居中，壳 −24=48/2）+drop 指示线 28×2 accent+原按钮 opacity 0.4；拖影阴影 rgba(0,0,0,0.35)→var(--context-menu-shadow) 归一化。诚实注记⑥：E4V#48 视图跨容器拖放（文件树条目拖到图标栏→ViewContainerService.moveView）未迁——清单无任务（与拖拽换位同类静默丢失），drop 源 SectionStack 要 #10 SidebarZone 才进池，建议并入 #10——待用户定夺。验证：tsc 0 / eslint 3 错误+960 警告零新增 / vitest 399/400（基线 DataConverter）/ 4px 网格 ✓）
- [ ] **E5.7#7** ~~`src/pool/zones/TabBarZone.tsx`~~ 🔴 已取消（2026-08-13 审计）——tab bar 留在 MainZone 内，吸收进 #20
  - 取消原因：E5.6#16.5 的架构教训——"TabBar+内容+分隔线自包含在 panel 内"是治"拖分隔线 TabBar 不跟着动"的唯一解；独立 TabBarZone 横带在多 group 分屏时与面板几何错位（需双份 computeLayout），且 MainRenderer 拖拽协调（tabBarRefs + computeSplitZone + preview portal）依赖 tab bar 和 panel 同组件——拆开 = 重接线 15 轮 bug 验证过的活代码
  - 连带：#1 的 PoolLayout `tabBar` 字段删除；拖出窗口检测（useDragDetach，Phase 8 #33）改由 MainZone 接入
  - 📖 取消论证 → [Zone分解设计.md §2.4](Zone系统/Zone分解设计.md#24-tabbarzone)（墓碑）
- [x] **E5.7#8** `src/pool/zones/StatusBarZone.tsx`——新建
  - 22px 状态栏 + 左右分区
  - 📖 → [Zone分解设计.md §2.8](Zone系统/Zone分解设计.md#28-statusbarzone)
  - （执行注 2026-08-14：commit 3232cc8e。数据流=壳推全量（显示文本铁律）：StatusBarLayout 扩 items（壳三源合并=贡献 getStatusBarContributions+动态 getDynamicStatusBarItems+eventEntries 按 alignment 拆 __shell_left__/__shell_right__+壳固定项语言/主题——title 硬编码中文改 t()，硬约束 #2 顺带修正）+dividerBefore 壳侧算好（左区每项除整区首个都有分隔——组间+组内；右区仅组内除首个——壳 StatusBar 渲染语义照搬）+component 标记（getViewPlugin().statusBarComponent——serial-monitor TX/RX 计数，取代该插件全部静态项，壳 renderPluginStatusBar 同款）+chordLabel（壳 CHORD_CHANGED 逻辑迁 usePoolSync——pending/failed 两态字符串+3s failedKey 自动清除定时器，按键名技术标识符不走 i18n）+notif（通知中心纯数据：_seenIds 模块级 Set 从 NotificationCenter 迁 usePoolSync，formatTimeAgo/buildSourceGroups/getNotifIconClass 壳侧照搬；unread/bellTitle/panelTitle/clearLabel/emptyLabel/dismissTitle/groups 全壳 t() 后序列化）。订阅→layoutVersion 重推：onDidChangeStatusBar/tab:focused/subscribeToasts/statusbar:update。池侧 2 新文件：StatusBarZone.tsx 哑渲染（switch 分左右、分隔线、chord、铃铛+面板 fixed 定位——OverlayPortal 不迁，outside-click+Escape 行为等价）+PoolStatusBarComponent.tsx（插件状态栏组件 glob 6 路径懒加载+resolvePath 运行时 3 路径试错，同 PluginComponent 模式）。池→壳事件回传 4 通道：notif:panel（setToastsSuppressed+标记已读+手动重推——标记已读不 fire toast 事件）/notif:dismiss/notif:clearAll/notif:action（ToastAction.onClick 是壳侧闭包不可序列化→壳侧执行+关闭，语义照搬 a.onClick();dismissToast(n.id)）。诚实注记：①NotificationCenter 迁入本任务——Zone分解设计 §2.8 右区明确列"通知"，且全清单无其他任务覆盖，零丢失铁律要求吸收；②面板阴影 0 8px 32px rgba(0,0,0,0.35) 保留壳原值——--context-menu-shadow 层级太浅不配 420px 面板，Phase 6 #26 常量表可补面板级 token（CSS 头注记）；③壳固定项 lang/theme 空 label 回退 id 显示"lang/theme"文本（壳 StatusBar {item.label || item.id} 行为照搬，非新回归）；④未迁入死类 .status-dot/.status-traffic/.status-error/@keyframes statusFadeIn——壳 StatusBar.tsx 与插件均不渲染（grep 零消费））
- [x] **E5.7#9** `src/App.tsx`——删壳 UI DOM 渲染，保留状态逻辑
  - 删：TitleBar/IconBar/TabBar/StatusBar/MainContent 的 DOM 渲染代码
  - 🔴 **保留（"壳=唯一真相源"不变）：** tabState / syncLayout / pushLayout / shellEvents / CommandRegistry / MenuRegistry / KeybindingRegistry / ConfigurationRegistry / loader.ts 插件加载 / 命令注册与执行
  - 壳渲染进程 = 纯状态持有者——无可见 DOM（被 WCV 完全覆盖）
  - 壳与池的分工：壳想（状态），池画（渲染）
  - （执行注 2026-08-14：commit c5407294。App.tsx 删 TitleBar/WindowControls/IconBar/SplitHandles/MainContent/StatusBar 渲染 + zoneBounds + menuStyle + editorAreaRef + TITLE_BAR_HEIGHT 偏移——MainContent 全套状态机搬入（useTabManager 解构/icon:selected 消费/TabActions u1-u7/布局恢复/handleFocusTab/coreCallbacks/handleTabAction/usePoolSync/持久化三件套）。诚实注记：①布局恢复 effect 加 ready 守卫——原 MainContent 在 ready 门控 JSX 内挂载（initAll 完成后才 mount），迁 App 后 effect 首轮 mount 就跑，不守卫则 E5#7h3 恢复早于 LayoutService 初始化；②LayoutEngine 保留最小 feed（setContainerSize 满窗零偏移）——#12.5 删的是 bounds 推流，zone 几何真相源（usePoolSync 读 sidebar 宽/SidePanel setZoneWidth 折叠/#13 拖拽 commit）仍在壳，#31 整删时连带删此 effect；③SidePanel 隐藏挂载保留——icon:selected→sidebar:containerChanged/toggled 管线是壳侧栏状态唯一入口，活到 #10 SidebarZone 接管（旧注释"Pool 崩溃 fallback"不准确——§9.2 恢复由主进程 WCV 重建+pushLayout 重放覆盖）；④usePoolSync sidebarWidth 参数删除——LayoutEngine getBounds 内部直读 + onDidChangeLayout→layoutVersion 重推（折叠/展开/resize 后重推）；⑤删 16 文件（清理方案 §3"已被 zones 替代则删除"）：MainContent/TitleBar/IconBar/StatusBar/WindowControls/NotificationCenter + 壳视图三件套（池 ShellViewRenderer 已有 welcome/plugin-detail/output 路由）；SplitHandles.tsx 留 #31 整删（清单明确归属）；PluginDetailView.css 保留（PluginDetailPoolView 复用）；⑥eslint 984→960 warning——纯死文件 warning 消灭零新增，3 error 基线不变，vitest 399/400（基线 DataConverter），4px 网格 ✓）

---

# Phase 3：SidebarPool 合并 → SidebarZone

> 目标：SidebarRenderer 从独立 WCV 变同 DOM React 组件。

- [x] **E5.7#10** `src/pool/zones/SidebarZone.tsx`——新建（🔴 职责 = 哑渲染 SidebarLayout 全量字段，不只是 PluginComponent 包装器）
  - 替代 `src/pool/sidebar/SidebarRenderer.tsx`——**行为零丢失**，验收逐项对照 SidebarRenderer 现有 6 项：① collapsed 折叠态（▶ 展开按钮）② 空状态文案 ③ mergeHeaderWhenSingle 标题合并 ④ toolbar/section 角色分离（toolbar 粘顶在滚动容器外）⑤ SectionStack 折叠/拖排/PaneSash resize ⑥ header 右键菜单 + ◀ 折叠按钮
  - visible=false → display:none；section 内容用 `<PluginComponent>`——容器结构（header/toolbar/section stack）在 zone 内渲染
  - 🔴 API 过渡：侧栏 action（toggleSidebarCollapse / sidebarAction）暂用现有 `linkdesk.pool.*` 通道，Phase 12 归位命名——Phase 3 结束时侧栏按钮必须可用（防"点不动"回归）
  - 📖 → [Zone分解设计.md §2.3](Zone系统/Zone分解设计.md#23-sidebarzone)
  - （执行注 2026-08-14：commit 2535cb2b。SidebarZone.tsx/css 新建 + PoolZoneShell 接入替换占位；E4V#48 跨容器拖放经用户确认并入本任务（清单原无此任务，与 #6 拖拽换位同类的静默丢失——零丢失铁律）。差异如实记录：① visible=false 用 display:none 包裹（非 E5.6 null 卸载）——保持挂载视图状态不丢 ② .side-panel-title 类名修正（E5.6 用了不存在的 .side-panel-header-title）③ 空状态两行文案（主+提示）壳 t() 推送（SidebarLayout +4 字段 emptyText/emptyHint/expandTooltip/collapseTooltip）④ 无 as-any。宿主状态机：原隐藏挂载 SidePanel 语义迁入 App（icon:selected 容器解析/同容器 toggle 折叠/异容器展开切换 + view:toggleCollapse/resetPosition/toggleVisibility 三命令事件），折叠真相源 = LayoutEngine zone 宽（≤48）——不持 collapsedRef 防池◀按钮改宽后状态脱节；preCollapseWidth 恢复；usePoolSync toggleSidebarCollapse 改转发 sidebar:toggleFromPool（ShellEvents +void 事件，三条折叠路径归一 doCollapse）；隐藏挂载 SidePanel 删除。E4V#48：viewDragProtocol.ts 自定义 MIME + PoolSectionStack handleDragStart setData + IconBarZone onDragOver/Leave/Drop + .view-drop-target 描边——dataTransfer 替代壳 getDraggingView 共享状态（池内唯一同步通道）。门禁：tsc 0 / eslint 3+960 基线零新增 / vitest 399/400 基线 / grid ✓）
- [x] **E5.7#11** `src/pool/sidebar/`——目录删除，组件迁移
  - PoolSectionStack + PoolToolbarSlot → `src/pool/shared/`（RightSidebarZone 同构复用——折叠/拖排/sash resize/toolbar 粘顶是 E5.6#11g/#11f/#16.7k 打磨的活代码，禁止重写）
  - SidebarRenderer.tsx 删除（被 #10 SidebarZone 替代）
  - （执行注 2026-08-14：commit 103bda62。两组件 git mv → shared/（import 改 ./PluginComponent、./viewDragProtocol；../../core 与 ../../components 深度不变零改动）；SidebarRenderer.tsx 删除——其 2 条 lint warning 随删（33:16 as-any + 83:20 role 比较）→ 基线 960→958 如实记录。门禁：tsc 0 / eslint 3+958 / vitest 399/400 基线）
- [x] **E5.7#12** `electron/window-manager.ts`——删 SidebarPool WCV 创建 + 可见性管理（toggleSidebarPool / destroyPool('sidebar')）
  - 🔴 中间态说明：Phase 1 #2 去 zone 路由后，SidebarPool WCV 已无内容可加载（黑条）——本任务物理删除 WCV 即消除中间态。若 Phase 1/2 验证时黑条干扰，可把本任务提前至 Phase 1 与 #4 一起做
  - （执行注 2026-08-13：已按本条中间态说明提前至 Phase 1 与 #4 一起做——commit 26b8af7c。SidebarPool WCV 物理删除：createSidebarPool/toggleSidebarPool 删除，main.ts 只建 MainPool，pool:toggle-sidebar-pool handler + preload-shell 暴露删除，App.tsx 调用删除；SidePanel 壳 DOM 保留隐藏挂载作唯一 Pool 崩溃 fallback（设计 §9.2））
- [x] **E5.7#12.5** MainPool bounds 换主——壳推流删除，主进程接管（🔴 2026-08-13 审计：机制换主无人认领，新增本任务）
  - 主进程增：`createMainPool` 时 `setBounds(窗口内容区)` + `win.on('resize')` → `setBounds` 跟随（模式可抄 [脱出窗口设计.md](脱出窗口/脱出窗口设计.md) 的同款 resize 处理）
  - 壳删：App.tsx 的 updateSize / LayoutEngine bounds 推流 / TITLE_BAR_HEIGHT 偏移数学（E5.6 专属——titlebar 在壳 DOM；E5.7 titlebar 是池内 zone，WCV 满窗零偏移）
  - 死链删：`pool:set-bounds` handler（plugin-view-handlers.ts:144）+ preload-shell.ts:509 暴露 + window-manager `updatePoolBounds`（🔴 零调用死代码，不是"简化"）
  - 验证：窗口拉伸/最大化后 Pool 满窗、无残影、无 30px 错位
  - （执行注 2026-08-14：🔴 提前执行注——按 2026-08-13 审计建议"Phase 2 验证时占位 zone 位移干扰可提前"，经用户确认提前到 Phase 2 与 #5/#6/#8/#9 一起做（WCV 满窗后 zone 渲染位置即最终位置，避免先建 zone 再换 bounds 的二次适配）。commit a04374eb：window-manager 增 syncPoolBounds（窗口内容区 x0/y0）+ 构造函数注册 resize 跟随（dispose 注销成对——rebuildPool 不重复注册）+ createMainPool 创建即 setBounds+setVisible；壳侧 pool:set-bounds handler + preload-shell setBounds 暴露 + App 推流数学（gap/偏移）全删。两处拆分如实记录：① checklist 原话"壳删 updateSize / LayoutEngine 推流"——本 commit 只删推流，updateSize/LayoutEngine 订阅保留到 #9（zoneBounds 壳 DOM 消费方还在，提前删则 MainContent 因 zoneBounds.main 空 unmount、tab 状态逻辑断裂）；② updatePoolBounds 已不存在——Phase 1 #4 已随 zone 去参删除，非本任务遗漏。中间态提示：满窗 WCV 会盖住壳 TitleBar/IconBar/StatusBar——占位 titleBar/iconBar/statusBar zone 顶上，真实顶栏/图标栏/状态栏暂不可见，Phase 2 内 #5/#6/#8 逐个替换占位后恢复）
- [x] **E5.7#13** 侧栏分隔线 CSS 化——4px div + `cursor: col-resize` + mousedown 拖拽
  - 🔴 拖拽期间：池内乐观本地 width（rAF 节流 mousemove），**mouseup 一次性 commit 到壳** → pushLayout 回执（回执与本地一致，无跳变；真相源仍在壳）
  - 🔴 isDragging guard：拖拽期间忽略 pushLayout 的 sidebar.width 旧值（防外部 layout 推送闪跳），拖完放开
  - 🔴 E5.6#22m 防护传承（useDragHandle 活代码，禁止丢）：① body cursor lock——拖拽期间锁 `document.body` cursor/userSelect（快速拖拽脱离 handle 不跳回箭头）② 窗口外释放——`mousemove` 里 `buttons === 0` 视为 `mouseup`（窗口外释放时 `mouseup` 到不了本 window）
  - hover 高亮：`--separator` → `--separator-hover`（transition 0.15s，HandleLine 现有行为传承）
  - 无 IPC setBounds（E5.6 跨 WCV 老路彻底消失）
  - 📖 → [01-极简Pool设计.md §5.3](01-极简Pool设计.md#53-分隔线css-原地分离)
  - （执行注 2026-08-14：commit a50dcf3f。分隔线渲染在 SidebarZone 的 zone 包装内（可见性条件块——visible=false 整体 display:none，分隔线随之消失，审计不变量自动成立）；拖拽状态机 IconBarZone #6 同款模式——乐观本地宽 rAF 节流 + mouseup 一次性 commit `sidebarAction({action:"setSidebarWidth",width})` → 壳 resizeZone 钳制（与 E5 壳 useDragHandle 同款语义）→ onDidChangeLayout → pushLayout 回执。回执判定 = commit 后第一条非拖前宽的推送（壳钳制后值可能 ≠ 本地宽——以壳权威为准；拖回原位时按值相等判回执，防永久待回执）。钳制界壳推：SidebarLayout +minWidth/maxWidth（LayoutEngine dock 声明 170/600）——池本地钳制与壳公式一致零硬编码。折叠态拖拽期间本地宽实时派生（壳 collapsed 重推滞后，折叠→拖宽中途分支实时切换）；.resizing 类关 150ms 过渡（rAF 帧帧改宽防橡皮筋）。#22m 防护：body cursor/userSelect 锁存前值恢复 + buttons===0 窗口外释放。门禁：tsc 0 / eslint 3+958 基线 / vitest 399/400 基线 / grid ✓。补丁 2026-08-14（Phase 3 自检）：+moved 标记——无位移点击（mousedown+mouseup 未动）不 commit，对齐 E5 壳 no-op 语义（否则折叠态 28px 误点分隔线被钳到 170 意外展开））
- [x] **E5.7#13.5** 侧栏容器持久化——启动恢复上次侧栏（🔴 2026-08-14 用户验收发现：启动无侧栏是 E5 旧壳既定行为忠实照搬，非回归——electron 分支铁证：setSidebarView 全仓 2 处（初始 null + 卸载置 null）零恢复路径，workspace 只存 {tabs,cards}。用户确认选 B：对标 VS Code 恢复上次 Activity Bar 选择）
  - 持久化：icon:selected 选中容器时 `setPluginStateValue(APP_PLUGIN_ID, "activeSidebarPlugin", pluginId)`——iconOrder 同款机制 PluginStateService（归一化不新发明）
  - 恢复：ready 后一次性 effect 重放 `shellEvents.emit("icon:selected", persisted)`——与点击图标同一路径（u1 容器校验/折叠展开全走状态机，零第二套选择逻辑）
  - 🔴 守卫：插件须仍在 getViewPlugins()（图标栏同源）——已卸载/禁用则静默无侧栏（回退旧行为）；一次性 ref 防 StrictMode 双跑重放（u1 同图标会当 toggle 处理）
  - 🔴 不持久化折叠态/宽度（本任务范围 = 容器选择；折叠态持久化为可选后续）
  - （执行注 2026-08-14：commit 见下。App.tsx 四处：import +getViewPlugins/getPluginStateValue；u1 选中容器后 `setPluginStateValue(APP_PLUGIN_ID, "activeSidebarPlugin", pluginId)`（iconOrder 同款——persist 的是 pluginId 非 cid，恢复重放 icon:selected 复用 u1 的 pluginId→容器映射，零第二套选择逻辑）；sidebar:containerChanged 订阅后 +恢复 effect（ready 守卫 + getViewPlugins 守卫 + sidebarRestoreDoneRef 一次性防 StrictMode 双跑重放——u1 同图标会当 toggle 折叠，双跑 = 启动即意外折叠）。卸载/禁用插件不清理持久值——恢复时 getViewPlugins 守卫静默跳过（回退旧行为）；重装后自动恢复选择。门禁：tsc 0 / eslint 3+958 基线 / vitest 399/400 基线 / grid ✓）

---

# Phase 4：OverlayWindow 消除

> 目标：ContextMenu / QuickPick / Toast / Dialog 全部 CSS position:fixed。删 OverlayWindow。
> 🔴 2026-08-13 审计定性：OverlayWindow 当前是空壳（overlay-main.tsx 全占位符 + 壳侧 overlay.push 零调用）——本 Phase 的迁移源是**壳 DOM 真浮层**（components/shared/ContextMenu 等），不是 src/overlay/；删除零行为损失。
> 📖 完整设计 → [浮层归一化设计.md](浮层系统/浮层归一化设计.md)

- [x] **E5.7#25** `src/pool/zones/FloatingLayerHost.tsx`——新建（🔴 自 Phase 6 提前——#14 前置：portal root 不存在 = 菜单点不动）
  - `position: fixed; inset: 0; pointer-events: none` 容器 + 4 个 portal root
  - 📖 代码骨架 → [浮层归一化设计.md §2](浮层系统/浮层归一化设计.md#2-floatinglayerhost浮层容器)
  - （执行注 2026-08-14：commit 见下。按设计 §2 骨架照抄：容器 inline style（position:fixed / inset:0 / zIndex:2000 / pointerEvents:none——默认穿透，浮层显示时各自设 auto）+ 4 个 portal root（context-menu-root / quick-pick-root / dialog-root / tooltip-root）。ToastHost 未渲染——#16 才建（设计骨架是终态，#25 范围 = 容器 + root，不 import 尚不存在的组件）。PoolZoneShell 接入：import + Row 3 后挂载（fixed 脱离 flex 流）+ 占位注释换真组件 + 头注释占位策略补 Phase 4 行。zIndex 2000 为骨架裸数字——#26 建 Z_INDEX 常量表后换 Z_INDEX.toast 引用（本文件是替换点之一，已注记）。门禁：tsc 0 / eslint 3+958 基线 / vitest 399/400 基线 / grid ✓）
- [x] **E5.7#26** z-index 层级标准化——`src/constants.ts` 定义 `Z_INDEX` 常量表（🔴 自 Phase 6 提前——#14-17 引用）
  - 100 splitHandle / 200 panelResize / 500 dragPreview / 1000 dropZone / 2000 toast / 3000 contextMenu / 4000 quickPick / 5000 dialog / 6000 tooltip
  - 全量 grep 替换裸数字 → 常量引用
  - （执行注 2026-08-14：commit 见下。constants.ts +Z_INDEX 表（设计 §3 照抄）。替换 4 处：FloatingLayerHost zIndex 2000 → Z_INDEX.toast；MainRenderer 9999 → dropZone / 99999 → dragPreview / 分隔线手柄 10 → splitHandle（SplitHandles 壳侧同名层级已死——此手柄是表内 splitHandle 的活体，#20 验收⑬ 提前消化）。终检 grep 残留 9 处全部有交代不替换：①overlay-main.tsx 9999——#18 整目录删 ②SplitHandles.tsx 100——零引用死代码随 #31 扫 ③④⑤⑥ 组件内部层级 z-index 1/2（KeybindingSettingsView/IconBarZone/SidebarSection css+tsx——非全局浮层级，不在表内）⑦⑧ GroupTabBar backdrop 999 + PlusMenu 1001——dropdown/popover 级不在表内（#14 ContextMenu 迁移后 backdrop 消亡、PlusMenu 归层）⑨ CSS --z-* 变量体系保留（CSS 不能 import TS 常量；E5#98 平衡值随组件迁移逐个换 Z_INDEX 引用）。门禁：tsc 0 / eslint 3+958 基线 / vitest 399/400 基线 / grid ✓）
- [x] **E5.7#14** ContextMenu——portal 到 `#context-menu-root`
  - `window.linkdesk.menu.show(menuId, {x, y}, context)` API 不变
  - 聪慧→哑数据流保留：菜单项解析在壳渲染进程（Registry 所在）/ 渲染在 Pool FloatingLayerHost（哑）/ executeCommand 在壳渲染进程
  - 简化：删 originPool 追踪（单 Pool）；🔴 主题/语言机制拆开写——主题：菜单在 Pool 文档内，继承 Pool 文档 CSS 变量（壳→Pool theme:changed 广播保留，不再单独推给 OverlayWindow）；语言：显示文本铁律（壳侧 t() 解析后字符串推送，池不初始化 i18n）
  - 定位用 clientX/Y 直接定位（无屏幕坐标转换）+ 边界翻转，🔴 翻转钳制 menuTop ≥ 30（TitleBarZone drag 区下沿——硬约束 18，防 OS 截鼠标事件）
  - 🔴 键盘导航：菜单打开后聚焦菜单容器，↑↓ 移动 + Enter 选择 + Escape 关闭（防 E5.6 focusable:false 键盘回归）
  - 📖 → [浮层归一化设计.md §4](浮层系统/浮层归一化设计.md#4-contextmenu)
  - （执行注 2026-08-14：commit 见下。调研结论二条——①设计 §4.4 的 `menu.show`/`context-menu:show` IPC 管线 E5.6 从未建（全仓零匹配），现役 `lk.menu.getItems` IPC 已是同一聪慧→哑三段式（解析壳/渲染池/执行壳）——按现有管线演进而非重造零行为收益的管线；②originPool 全仓已零匹配（单 Pool 后自然消亡）。改动：OverlayPortal +`rootId` prop（归一化 portal 目标，root 不存在回退 body——SettingsView 等壳侧消费者迁移期兼容）；ContextMenu 传 `rootId="context-menu-root"` + wrapper zIndex Z_INDEX.contextMenu + 透明 backdrop（contextMenu-1，吞第一击=点击即关不激活下层——设计 §4.2）+ 主/子菜单 inline zIndex + menuTop≥30 钳制（初始定位 + 翻转修正两处——设计 §4.3）+ 打开后聚焦容器（menuReady effect，visibility 可见后 focus——focus() 对 hidden 元素静默失败）+ 删池侧 t()（标签壳侧推送原文渲染——设计 §4.4 语言铁律）；IpcBridgeHandler menu:getItems 加壳侧 i18n.t 解析（label/title/children.label——title 含命令动态标题亦走 key 回退安全；children 判字符串用 instanceof Object 而非 typeof——ESLint no-restricted-syntax 对小写字面量比较报 pluginId 误报，保 3+958 基线）；GroupTabBar.css 删死代码簇 group-tab-context-*（backdrop/menu/item/divider 零 TSX 引用——E5.6#16.7k 归一化前身，含 `#fff` 硬编码违例 1 处）+ 旧注释"WCV 视口在 TitleBar 下方→硬约束 18 不适用"随簇删除（E5.7 不成立：Pool 100%×100%，TitleBarZone 池内渲染 drag 区）；GroupTabBar.tsx 注释行号刷新。PlusMenu zIndex 1001 归层推迟 #20（GroupTabBar 未挂载 + 设计 §3 表内无 dropdown 级——不擅自加层）。门禁：tsc 0 / eslint 3+958 基线 / vitest 399/400 基线 / grid ✓）
- [x] **E5.7#15** QuickPick / CommandPalette——portal 到 `#quick-pick-root`
  - 居中面板 + backdrop + 自动聚焦 + 150ms 防抖
  - 📖 → [浮层归一化设计.md §5](浮层系统/浮层归一化设计.md#5-quickpick--commandpalette)
  - （执行注 2026-08-14：聪慧→哑桥全链 9 步——①类型 `src/core/types/poolQuickPick.ts`（PoolQuickPickItem：key/searchText/label/category/detail/keybinding/buttons DTO；函数无法过 IPC，故 DTO 化）；②QuickPickService +`serialize`（必填——编译器驱动 5 消费方全部迁移）+`onItemAction`；③preload-shell +pushQuickPick/onQuickPickAction（模块顶层 pool:quickpick-action 监听，对标 sidebar-action 模式）；④plugin-view-handlers +pool:quickpick-show/action 两中继（count 7→9）；⑤window-manager +pushQuickPick（pool:quickpick webContents.send）；⑥preload-pool +缓冲回放（对标 pool:layout，只留最后一份——浮层单例态）+linkdesk.quickPick{onShow,select,highlight,close,itemAction}；⑦池 `QuickPickHost.tsx/.css` 哑渲染（设计 §5.1：backdrop rgba(0,0,0,0.3) 点即关 / 面板 top 15vh 居中 400px 宽 max 60vh / 自动聚焦 50ms / 150ms 防抖本地过滤不 IPC / ↑↓Enter Esc / 高亮变化回传 / 退场 100ms 动画 + closingRef 重开竞态守卫；fuzzyScore 壳副本——#18 删壳后归一）；⑧FloatingLayerHost #quick-pick-root 挂载 host；⑨App.tsx 桥 effect（onChange→serialize→push；动作回传按 key 重解析——select 对标 handleSelect 先 onSelect 后 onClose；close 只 st.onClose 由消费方 hide；查表分发绕开 no-restricted-syntax 小写字面量误报）+ 5 消费方 serialize（CommandPalette 齿轮→buttons DTO+onItemAction→快捷键设置；ThemeBrowser/LanguagePicker detail/category 走 i18n.t 查表——壳侧解析铁律；devtools 提取 searchOf/keyOf/labelOf/detailOf 四 helper render 与 serialize 共用归一；命令式 showQuickPick 改走 QuickPickService——旧自建 DOM root 在 WCV 后面不可见，插件 API 必须经桥）。旧壳渲染/订阅保留至 #18（隐身无害，诊断参照）。门禁：tsc 0 / eslint 3+956（基线 958 −2：ThemeBrowser/devtools 归一去重）/ vitest 399/400 基线 / grid ✓）
- [x] **E5.7#16** Toast——`<ToastHost>` 右下角
  - `position: fixed; bottom: 30px; right: 6px`（现网实测——回归矩阵场景 5 已订正：状态栏 22px + 8px gap；设计旧草图 25px/12px 作废）
  - 栈排列 + CSS 进出动画 + pointer-events: none
  - 📖 → [浮层归一化设计.md §6](浮层系统/浮层归一化设计.md#6-toast)
  - （执行注 2026-08-14：聪慧→哑桥全链 8 步——①`src/core/types/poolToast.ts`（PoolToastItem：id/message/iconClass/sourceText/actions DTO；onClick 闭包不过 IPC——actions 序列化为位置序号 actionId "0"/"1"，壳侧重解析执行，消费方零改动）；②toast.ts +`getToastIconClass`（ToastContainer 局部函数迁入归一——serialize 与渲染共用一处定义；查表绕开 no-restricted-syntax switch case 误报）+`serializeToasts`（显示文本铁律：sourceText 壳侧 i18n.t 解析）+`runToastAction`（按 id+actionId 重解析 onClick 后 dismiss——对标 NotificationItem 动作行为零差异）；③preload-shell +pushToast/onToastAction（模块顶层 pool:toast-action 监听，对标 sidebar-action 模式）；④plugin-view-handlers +pool:toast-show/action 两中继（count 9→11）；⑤window-manager +pushToast（pool:toast webContents.send）；⑥preload-pool +缓冲回放（对标 pool:quickpick，只留最后一份——全量快照语义）+linkdesk.toast{onShow,dismiss,action}；⑦池 `ToastHost.tsx/.css` 哑渲染（suppressed→整体 null 对标壳 / 退出检测 prevIds diff+map 缓存+opacity transitionend 清理对标 E3.5 #TO01/#TO05 / 双 rAF 入场 / 中键 dismiss+双击展开+chevron+动作按钮全行为保留 / 展开池本地态 / z-index 由 FloatingLayerHost 统一持有；MIDDLE_BUTTON/OPACITY_PROPERTY 标识符比较绕字面量误报——壳同类两处留 #18 删除）；⑧FloatingLayerHost 挂载 ToastHost（设计 §2 常驻自隐藏）+ App.tsx 桥 effect（subscribeToasts+subscribeToastSuppressed→push 全量快照；动作查表分发 dismiss→dismissToast / action→runToastAction）。位置/动画按现网实测（bottom 30px/right 6px/gap 4px/入场 300ms 退场 200ms）——设计 §6.1-6.3 已同步订正，回归矩阵场景 5 口径一致。旧壳 ToastContainer 保留至 #18（隐身无害）。门禁：tsc 0 / eslint 3+956 基线 / vitest 399/400 基线 / grid ✓）
- [x] **E5.7#17** Dialog / Modal——portal 到 `#dialog-root`
  - backdrop + 居中 + 焦点陷阱 + Escape
  - 📖 → [浮层归一化设计.md §7](浮层系统/浮层归一化设计.md#7-dialog--modal)
  - （执行注 2026-08-14：聪慧→哑桥全链 9 步——①`src/core/types/poolDialog.ts`（open 判别联合：`{open:false}` 关闭态；函数不过 IPC——Promise resolve 闭包留壳）；②DialogService **零改动**（renderer 注册本就是壳侧机制——桥本身就是 renderer）；③preload-shell +pushDialog/onDialogAction（模块顶层 pool:dialog-action 监听）；④plugin-view-handlers +pool:dialog-show/action 两中继（count 11→13）；⑤window-manager +pushDialog；⑥preload-pool +缓冲回放（只留最后一份——单例态）+linkdesk.dialogHost{onShow,confirm,cancel}（命名 dialogHost——dialog 命名空间已是插件侧 dialog.confirm/alert API）；⑦池 `DialogHost.tsx/.css` 哑渲染（设计 §7.1 规格：backdrop rgba(0,0,0,0.5) 点击关 / 居中 50%/50% translate / minWidth 300 maxWidth 80vw maxHeight 80vh——壳旧值 0.4/320px/420px 按设计规格为准；§7.2 焦点陷阱 Tab 循环 + Escape 条件关闭 alert 除外 + backdrop 条件关闭 alert 除外——现网壳无陷阱，此为清单要求的设计规格落实；Enter 确认；打开聚焦面板本身而非按钮——Enter 确认不误触取消按钮；零 useTranslation——按钮文案壳侧 t() 解析，显示文本铁律端态；无退场动画对标壳即时卸载）；⑧FloatingLayerHost #dialog-root 挂载 host；⑨App.tsx 桥 effect（registerDialogRenderers 桥接——ConfirmDialog 的注册卸下防双注册覆盖，组件留壳至 #18；单一待决对话框对标壳单 state 后开覆盖先开；**先推关闭再 settle**——settle 后消费方可能立即再开，stale close 推在 settle 后会把新开对话框关掉）。旧壳 ConfirmDialog 保留至 #18（inert——state 恒 null）。门禁：tsc 0 / eslint 3+956 基线 / vitest 399/400 基线 / grid ✓）
- [x] **E5.7#18** 旧浮层代码清理——删除对象分两类（🔴 2026-08-13 审计：OverlayWindow 是空壳，真浮层在壳 DOM）
  - 空壳类（删了零行为损失）：`src/overlay/` 整目录（overlay-main.tsx 四浮层全占位 + debugBorder 临时码）、`overlay.html`、`electron/preload-overlay.ts`
  - 真浮层类（迁移源——渲染部分删除）：`components/shared/ContextMenu.tsx`、`components/shared/CommandPalette.tsx`（渲染部分）、App.tsx:427-447 QuickPick 渲染、`components/ToastContainer.tsx`
  - 🔴 壳侧保留清单（聪慧部分）：QuickPickService（LanguagePicker/ThemeBrowser/开发者命令在用）、菜单解析、executeCommand
  - （执行注 2026-08-14：真浮层类全清——13 文件 −1556 行。①ToastContainer.tsx/.css + ConfirmDialog.tsx/.css 整删（App 唯一消费者；ConfirmDialog 的 re-export showConfirm 零消费——全走 DialogService 直连）。②CommandPalette.tsx 组件删——showCommandPalette 迁 `core/commands/commandPalette.ts`（coreCommands 静态 import 替代动态 import）。③QuickPick.tsx 整删——showQuickPick + quickpick.show 注册迁 `core/commands/quickPickCommand.ts` 走 ensureCoreCommands 注册模式（原模块级 registerCommand 依赖 import 图存活——四个 importer 全删即静默死亡；provider id "linkdesk" 字面量 → APP_PLUGIN_ID 归一，硬约束 10 违例顺手消灭）。④QuickPickState 删 render* 五个 slot props（ReactNode import 死）——五消费方 render* 参数全部剥除（CommandPalette/devtools/ThemeBrowser/LanguagePicker——serialize 是唯一渲染路径，#15 后已无消费方）。⑤ThemeBrowser/LanguagePicker 组件部分删（零渲染方）——只剩命令式 showThemePicker/showLanguagePicker（settingsCommands 动态 import 不变；文件名保留避免 churn——已非组件，诚实注记）。⑥App.tsx 删 quickPickState + 旧 onChange 订阅 + 三处渲染（ToastContainer/QuickPick 1086-1104/ConfirmDialog）+ 四个 import。⑦index.css 壳 palette 块整删 265 行（.ctx-overlay/.palette-*/.keybinding-pill/.palette-item-*——池 QuickPickHost.css 自带完整副本，serial-monitor 注释过时无 className 消费；--z-overlay/--z-overlay-backdrop 变量保留——ColorPicker/ProgressBar/SettingsView/StatusBarZone 活消费）。保留：ContextMenu.tsx（4 消费方 SidePanel/SettingsView/GroupTabBar/SidebarZone——#14 已原地迁移为池渲染件，清单"删除对象"与审计不符，执行以审计为准）、OverlayPortal（HamburgerMenu + ContextMenu rootId 机制）、QuickPickService/菜单解析/executeCommand。空壳类（src/overlay/、overlay.html、preload-overlay.ts）唯一引用方是 overlay-window.ts——并入 #19 原子删除（本任务删则中间态 broken）。诚实注记：getSearchText/getKey 仅 getKey 被桥消费（动作回传重解析 item）——字段保留（#18 范围=渲染部分）。门禁：tsc 0 / eslint 3+950（基线 956 −6：死文件警告随删——净减非回归）/ vitest 399/400 基线 / grid ✓）
- [x] **E5.7#19** `electron/overlay-window.ts`——整文件删除 + window-manager 删 OverlayWindow 创建/管理
  - 🔴 main.ts 连带删除：`resize/move → overlayWindow.syncBounds()` 监听器（⚠️ 与 #12.5 的 MainPool resize 跟随是两个监听器，删时分清）+ `overlay:forward-to-overlay` / `overlay:forward-to-shell` 两个中继
  - preload-shell.ts `overlay` 命名空间暴露删除（并入清理方案 §2 preload-shell 行）
  - （执行注 2026-08-14：原子删除链 8 处——①`electron/overlay-window.ts` 整删（OverlayWindow 类）；②`electron/preload-overlay.ts` 整删（唯一消费者 overlay-window.ts:67）；③`overlay.html` git rm + vite.config rollupOptions.input 摘 overlay 入口（根目录 HTML 只剩 index.html + pool.html——清理方案 §4 口径）；④`src/overlay/` 整目录 git rm（overlay-main.tsx + 空 components/——四浮层全占位符零行为，审计相符，#18 并入的空壳类在此交付）；⑤main.ts 四删：import / overlayWindow 变量 / 创建+resize/move syncBounds 监听+两中继 / closed 时 destroy（⚠️ window-manager.ts:48 syncPoolBounds 是 #12.5 MainPool 跟随——两个监听器区分无误，保留）；⑥preload-shell.ts 删 `overlay` 命名空间（壳侧 overlay.push 零调用 + overlay:result 壳侧无监听器——审计相符，中继是死管）；⑦window-manager.ts:485 注释引用 overlay-window.ts syncBounds 删（#12.5 注释自足）。终检 grep "OverlayWindow|preload-overlay|overlay:forward|overlay:render|overlay:result" src/ electron/ 零残留。门禁：tsc 0 / eslint 3+949（基线 956 −7：死文件警告随删——净减非回归）/ vitest 399/400 基线 / grid ✓）

---

# Phase 5：Zone 分解——MainRenderer 瘦身

> 目标：693 行 MainRenderer 分解为独立 Zone 文件。

- [x] **E5.7#20** `src/pool/zones/MainZone.tsx`（主区）——从 MainRenderer 提取（🔴 2026-08-13 审计：吸收 #7 TabBarZone；EditorZone 更名 MainZone——主区装任何插件标签页，不叫 Editor）
  - 职责 = MainRenderer 693 行**行为零丢失**提取，验收逐项对照现有行为 13 项：① computeLayout 分屏面板绝对定位平级渲染（B22 防护，key=groupId 不变）② 每 panel 自带 GroupTabBar（pool/shared/ 复用——tab bar 留在 panel 内）③ keep-alive CSS display 切换 ④ `shellRendered → ShellViewRenderer` 分支（welcome/插件详情/输出）⑤ 空态"没有打开的标签页" ⑥ 单面板 flex fallback（叶子数=1 不绝对定位）⑦ 分隔线拖拽——乐观本地 sizes + mouseup commit tabAction（#13 同款模式）+ 双击 50/50 重置 + hover accent ⑧ 同组拖拽乐观重排 dragLocalTabs ⑨ 跨 group 移动 / 拖到面板边缘分屏（drop zone Glassmorphism）⑩ drag preview portal（document.body）⑪ creatableViews [+] 动态菜单 ⑫ ErrorBoundary 用池侧版（🔴 现 import `components/shared/ErrorBoundary` 是壳目录——迁 `src/pool/shared/`）⑬ zIndex 9999/99999 → `Z_INDEX.dropZone` / `Z_INDEX.dragPreview` 常量
  - 内联逻辑原样提取（useDragReorder 275 行 15 轮 bug 验证——不重写）；useSplitResize / useTabDropPreview 拆分是可选重构，非本任务
  - 拖出窗口检测：~~Phase 8 #33~~ useDragDetach 🔴 已推迟 v1.3（2026-08-13 决策，脱出窗口设计.md）——MainZone 留接入点注记（原 #7 占位迁此）
  - 📖 职责 → [Zone分解设计.md §2.5](Zone系统/Zone分解设计.md#25-mainzone)
  - （执行注 2026-08-14：commit 4ef4230d。行为零丢失提取——693 行仅改：头注释 / 组件名 MainRenderer→MainZone / ErrorBoundary import 切池侧版 / useDragDetach 占位注记改 v1.3。13 项逐项对照：①②③④⑤⑥⑦⑧⑨⑩⑪原样提取（computeLayout 绝对定位平级 key=groupId / per-panel GroupTabBar / keep-alive display 切换 / shellRendered 分支 / 空态 t("没有打开的标签页") / 单面板 flex fallback / 分隔线乐观+mouseup commit+双击 50/50+hover accent / dragLocalTabs / 跨组+分屏 drop zone / body portal preview / creatableViews [+]。⑫ pool/shared/ErrorBoundary.tsx 新建（i18n 相对路径同深度直接可用——pool-main 模块级已初始化），4 个 pool/shared 消费方（PluginComponent/PoolStatusBarComponent/PoolSectionStack/PoolToolbarSlot）同簇违规一并切换，壳侧 3 消费方保留壳版。⑬ Z_INDEX 已由 #26 提前消化——提取时确认 splitHandle/dropZone/dragPreview 三处无裸数字。挂载：PoolZoneShell 占位→MainZone groups/root/creatableViews；.zone-main 死 CSS 删。门禁：tsc 0 / eslint 3+966（+17 = MainZone 复制 MainRenderer 的临时双份计数，源文件仍在树上，#24 删后回落 949）/ vitest 399/400 基线 / grid ✓）
- [x] **E5.7#21** `src/pool/zones/PanelZone.tsx`——新建（greenfield 骨架——🔴 2026-08-13 审计）
  - PanelTabBar 28px + keep-alive + 视图动态加载占位（🔴 2026-08-13 审计：不建 PANEL_VIEWS 静态表——写死 pluginId 违反插件独立铁律 + 硬约束 10；动态加载归 #63.7，PluginComponent 同款）
  - 🔴 骨架优先：无壳侧生产者（bottom-panel 贡献路由 / panel 数据填充归 Phase 12 #63.7）——本任务只建渲染骨架，`layout.panel` 无数据时条件渲染永假
  - 高度拖拽：顶部 resize handle 用 #13 同款模式（乐观本地 + mouseup commit，真相源在壳）
  - 插件贡献面板视图（E5.6 的 registerPanelView 动态注册）→ Phase 12，本任务只有静态表
  - 📖 → [Zone分解设计.md §2.6](Zone系统/Zone分解设计.md#26-panelzone)
  - （执行注 2026-08-14：commit 06e59262。骨架全机械落地：PanelTabBar 28px（标签 80px 固定不 shrink + 列表 overflow-x 滚动 + [+] 滚动区外固定最右）+ keep-alive display 切换空壳（动态加载 #63.7）+ 顶部 4px resize handle #13 同款全机械（乐观本地高 + rAF 节流 + didMove no-op + mouseup/buttons===0 commit + commitPending 回执对齐 + body row-resize/userSelect 锁）+ Z_INDEX.panelResizeHandle（#26 常量表——清单 #30 写 panelResize 是旧名，实为 panelResizeHandle）。钳制界：PanelLayout 扩 minHeight/maxHeight 可选字段（#13 同款壳推，Phase 12 推送前无界）。池→壳通道 events.emit（#6 同款）：panel:viewSelected/panel:createView/panel:resize——Phase 12 #63.7 消费前安全 no-op。未建 PANEL_VIEWS 静态表。PoolZoneShell 挂载替换占位）
- [x] **E5.7#22** `src/pool/zones/RightSidebarZone.tsx`——新建（greenfield 骨架）
  - 和 SidebarZone 同构（复用 pool/shared/ 的 PoolSectionStack / PoolToolbarSlot——#11 已迁入），默认隐藏
  - 左侧 4px resize handle——#13 同款模式（乐观本地 + mouseup commit）
  - 无壳侧生产者（rightSidebar 数据填充归 Phase 12）——本任务只建骨架
  - 📖 → [Zone分解设计.md §2.7](Zone系统/Zone分解设计.md#27-rightsidebarzone)
  - （执行注 2026-08-14：commit 06e59262。SidebarZone 同构骨架——复用 PoolSectionStack/PoolToolbarSlot（#11）；左侧 4px handle #13 同款机械（方向翻转：宽 = 拖前宽 + startX - clientX）；宽度 commit → events.emit "rightSidebar:resize"（不借 setSidebarWidth——壳 handler 左栏专属语义无 containerId 参数）；视图 reorder/setCollapsed → pool.sidebarAction（按 containerId 泛化通道，Phase 12 容器注册后直达）。差异注记：①折叠按钮/collapsed 态 Phase 12 接入（右栏无壳侧容器）②RightSidebarZone.css 自包含（.side-panel* 与 SidebarZone.css 同源拷贝——zone 零跨 zone import 纪律；.sidebar-pane-* 由 PoolSectionStack 自带 import））
- [ ] **E5.7#23** ~~`src/pool/zones/TopBarZone.tsx`~~ 🔴 已取消（2026-08-13 审计）——与 V3-部件命名规范 ② 顶栏（Phase 4 已移除部件）重名；面包屑不在区域清单、无数据生产者
  - 连带：#1 的 PoolLayout `topBar` 字段删除；面包屑若做 → 并入 MainZone 每 panel（同 GroupTabBar 自包含原则）
  - 📖 论证 → [Zone分解设计.md §2.9](Zone系统/Zone分解设计.md#29-topbarzone)（墓碑）
- [x] **E5.7#24** `src/pool/main/MainRenderer.tsx`——删除（已分解入 MainZone #20）
  - 🔴 删除前对照 #20 验收 13 项，逐项在 MainZone 找到对应实现；`src/pool/main/` 随之整目录删除（panelViews 在 pool/views/ 不受影响）
  - （执行注 2026-08-14：commit 06e59262。#20 13 项对照 = diff 零丢失证据——MainRenderer vs MainZone 差异仅 6 处（头注释/组件名 MainRendererProps→MainZoneProps/ErrorBoundary import 切池侧版/"MainRenderer reads"注释改 MainZone/useDragDetach 注记新增），逻辑字节级一致。src/pool/main/ 整目录删除（仅 MainRenderer.tsx 一个文件）。eslint 3+966 → 3+949 基线精确回落——#20 的 +17 双份计数随源文件删除归零）

---

# Phase 6：浮层收尾

> #25（FloatingLayerHost）/ #26（Z_INDEX 常量表）已提前至 Phase 4（#14 前置——2026-08-13 审计：portal root 不存在则菜单点不动）。

- [x] **E5.7#27** pointer-events 管理——浮层显示时各自 `pointer-events: auto`；Toast 容器始终 none
  - 🔴 2026-08-13 审计：原稿"Toast 始终 none"字面执行 = 通知本体也穿透 → 关闭/操作按钮点不了。现网正确行为照搬 ToastContainer.css:24/27/58——容器 none / `.toast-container *` auto / 退场 none
  - 机制已定死（浮层归一化设计.md:59——容器默认穿透、浮层各自 auto）；现网样板 SelectBox.css:147 / ColorPicker.css:91 / ProgressBar.css:19-23 各写各的——执行 = 归一化进 FloatingLayerHost，非新发明
  - （执行注 2026-08-15：零代码变更——验证通过，机制已随 08-14 根级 opt-in 补丁提前落地。逐项：①FloatingLayerHost 容器 none + 4 root auto 单点（陷阱反转：忘设=正常工作；头注释 + 设计文档 §2 均已更新）②ContextMenu.css:16 / DialogHost.css / QuickPickHost.css 头注释明示"不在此写——root 根级提供"，CSS 内零 pointer-events 残留 ③ToastHost.css 三态精确：容器 none:26 / \* auto:30 / 退场 none:60 ④三处现网样板判定：SelectBox.css:147 = .selectbox-disabled 禁用态（非浮层用途，合法保留）；ColorPicker.css:91 = 色相滑块装饰光标（合法保留）；ProgressBar.css:19-23 = 自有容器 none/\* auto（Toast 例外同款——直挂自有容器非 portal root，合法保留）。⚠️ 连带发现（已报告用户 → 2026-08-15 用户决策补入 #27.5）：ProgressBar 唯一挂载点 = 壳 App.tsx:1070——E5.7 壳零可见 DOM（WCV 满窗覆盖）→ 市场安装进度卡当前不可见）
- [x] **E5.7#27.5** 🔴 进度条死链整删（2026-08-15 #27 连带发现 → 执行前摸底**证伪原前提** → 用户决策改写：原"ProgressBar 迁池——市场进度条复活"作废——实测 showProgress 全仓库零调用方，ProgressBar 零数据生产者，不是"在跑但看不见"而是死代码；现代进度 API = notification.show(progress:true) 持久 toast 走 #16 桥池内已能显示。给零生产者渲染器建 8 步桥 = 新增死代码，违反原则 4）
  - 删：`components/ProgressBar.tsx/.css`（git rm）+ App.tsx 挂载（:1070）+ import（:5/:12）+ 调试后门（:277-280 __showProgress/__setDoNotDisturb/__setSourceFilter——DevTools 调试钩子零消费者）
  - 删 NotificationService 死面：进度 API（showProgress/cancelProgress/getProgresses/subscribeProgress + ProgressHandle/ProgressItem 类型 + 内部状态 5 件 _progresses/_progressCounter/_progressHandles/_onDidChangeProgress）+ DND/来源过滤（setDoNotDisturb/isDoNotDisturb/setSourceFilter/getSourceFilter + _dnd/_sourceFilters——零消费者，pushToast 过滤分支从未生效）
  - 留：NotificationService 保持 toast 统一入口（pushToast 直通 re-export + toast.* 全家 re-export）——ProfileService/useMemoryMonitor/lifecycle/loader/core.index 消费方零改动
  - 真进度 UI 留未来：哪天有插件真需要百分比进度——API 面（notification.show progress:true + update/finish/cancel）已现成走 toast 桥；要 VS Code 式进度条 UI 届时再建桥（样式 git history 30 秒找回）
  - 验证：grep 零残留 / tsc 0 / eslint 基线 / vitest 基线
  - （执行注 2026-08-15：按改写版执行完毕。删：ProgressBar.tsx/.css git rm（143+80 行）；App.tsx 三处——import（:5/:12 残留清）、调试后门（:277-279 三条 __showProgress/__setDoNotDisturb/__setSourceFilter 删——__pushToast/__clearDismissed 保留：活 API 的调试钩子）、挂载（:1069）。NotificationService.ts 手术：进度 API 面 6 件 + DND/来源过滤 4 函数 2 状态全删——文件保留为 toast 统一入口（pushToast 直通 re-export + toast.* 全家），头注释重写记录死链删史 + 现网进度路径（E3j #76 持久 toast 走 #16 桥）。消费方零改动验证：ProfileService/useMemoryMonitor/lifecycle/loader/core.index 的 import 面全部命中保留面。门禁：tsc 0 / eslint 3+935（基线 3+938 回落 3——死文件带走警告，非回归）/ vitest 399/400 基线 / grid ✓ / pool-css ✓）

---

# Phase 7：分隔线简化

> 🔴 2026-08-13 审计：分隔线实现已随 #13/#20/#21 提前完成——本 Phase 从"实现"改为"验证 + Z_INDEX 常量应用 + 壳侧残留清理"。**分隔线不变量：分隔线活在区域的可见性条件块内——两个区域都在时自动出现，区域消失分隔线消失**（见 01-极简Pool设计 §5.3）。

- [x] **E5.7#28** Sidebar ↔ MainZone 分隔线——🔴 审计改定位：验证 #13 产物（实现已在 Phase 3 完成，本任务不新写代码）
  - 验证：分隔线在 `sidebar?.visible &&` 块内（侧栏消失分隔线消失）；拖拽 + commit + isDragging guard + cursor lock + 窗口外释放全部可用
  - 壳侧旧实现确认：SplitHandles.tsx 已删（清理方案 §3）
  - （执行注 2026-08-15：零代码变更——验证通过。①分隔线在可见性条件块内 ✓：SidebarZone.tsx:172-183 renderZone 统一包裹（visible=false → zone display:none；collapsed/空态/正常三分支全经 renderZone → 分隔线随 zone 消失——审计不变量成立）②拖拽机械全在场 ✓：窗口级 mousemove/mouseup + rAF 节流 + didMoveRef 无位移不 commit + buttons===0 窗口外释放 + finishDrag 一次性 commit + commitPendingRef 回执对齐 + body cursor/userSelect 锁（#13 同款 = #6 模式）③壳侧旧实现确认：⚠️ 清单假设"SplitHandles.tsx 已删"与实际不符——仍在 src/components/，但零 import 方（唯一消费 useDragHandle 的是它自己）= 死代码，整删归 #31。连带核实：useDragHandle 亦未"已迁 src/pool/hooks/"（#31 措辞与实际不符——池侧 #13 是全新机械非迁移），实际 = 死代码（唯一消费方 SplitHandles 已死），#31 整删）
- [x] **E5.7#29** MainZone 分屏分隔线——🔴 审计改定位：验证 #20 ⑦ 产物（实现已随 MainZone 提取完成）
  - 残余增量：handle zIndex 裸数字 100 → `Z_INDEX.splitHandle`（#20 ⑬ 只覆盖 dropZone/dragPreview，未覆盖 handle）
  - B22 防护保持：面板绝对定位，key=groupId 不变；单面板 flex fallback 零 handle（自动出现不变量）
  - （执行注 2026-08-15：零代码变更——验证通过，清单"残余增量"过时：该增量已由 #26 提前消化（#26 注记「分隔线手柄 10→splitHandle——此手柄是活体，#20 验收⑬ 提前消化」）。逐项：①onDividerMouseDown 198-259 乐观本地 localSizesRef + forceUpdate / mouseup 一次性 commit updateSplitSizes / 5% 双向钳制 ②onDoubleClick 576-584 双击 50/50 ③hover accent 585-594 带 dividerDragRef 拖拽守卫（拖拽中不闪 accent）④cursor col/row 双方向 565 ⑤zIndex 566 = Z_INDEX.splitHandle（constants.ts:18 = 100）⑥B22 防护：面板 key={p.groupId} 绝对定位平级 537-538；handles 只渲染于多面板分支 554 → 单面板 flex fallback 599+ 零 handle（自动出现不变量成立）⑦池内零裸数字 100 残留（GroupTabBar.tsx:354 zIndex 1001 = PlusMenu dropdown 级，非分隔线，有交代））
- [x] **E5.7#30** PanelZone resize handle——🔴 审计改定位：验证 #21 产物（高度拖拽已随骨架完成）
  - 增量明确：`cursor: row-resize`（#13 是 col-resize）+ 顶部 4px div + `Z_INDEX.panelResize`（200）常量应用
  - （执行注 2026-08-15：零代码变更——验证通过，增量已随 #21 提前消化（清单常量名 panelResize 是旧名，实为 panelResizeHandle——#21 注记）。逐项：①cursor row-resize ✓（PanelZone.css:22 静态 + 拖拽 body 锁 :133 双处）②顶部 4px div ✓（css:20 + tsx:142 注释）③zIndex = Z_INDEX.panelResizeHandle（constants.ts:19 = 200）tsx:146 无裸数字 ④#13 同款全机械 ✓：didMoveRef 无位移不 commit:37/71 + commitPendingRef 回执对齐:42 + finishDrag 一次性 commit + events.emit "panel:resize":74（Phase 12 #63.7 消费前安全 no-op）+ 窗口级 mousemove/mouseup + buttons===0 窗口外释放:84 + rAF 节流:89）
- [x] **E5.7#31** 壳侧分隔线体系零残留确认（🔴 审计改清单——OverlayWindow 空壳无真实分隔线代码，真残留是壳侧三件套）
  - SplitHandles.tsx 整删（清理方案 §3）
  - useDragHandle 已迁 `src/pool/hooks/`（清理方案 §3）
  - 🔴 LayoutEngine 整删——`core/services/LayoutEngine.ts` + `core/__tests__/LayoutEngine.test.ts`（zone 布局 E5.7 由池内 flex 接管；#12.5 只删了 bounds 推流用法，文件本体无人认领）；连带清消费者 import：App.tsx / SidePanel / SplitHandles / ShellEvents / usePoolSync / App.css 注释
  - 🔴 同簇死代码（2026-08-14 Phase 3 自检补记——SidePanel 卸载后成死簇，随 LayoutEngine 一起扫）：`SidePanel.tsx`（零挂载点，订阅永不注册）+ `components/shared/SectionStack.tsx`/`ToolbarSlot.tsx`（唯一消费者 SidePanel）+ `components/shared/viewDragState.ts`（getDraggingView 零读者，setDraggingView 唯一写者是已死的壳 SectionStack——E4V#48 池版已用 dataTransfer MIME 替代）
  - grep 验证：`SplitHandles` / `LayoutEngine` / 壳侧 `useDragHandle` 零残留（池侧新写法除外）
  - （执行注 2026-08-15：死簇 7 文件整删——SplitHandles.tsx + useDragHandle.ts（互死：SplitHandles 是 useDragHandle 唯一 importer，SplitHandles 零 importer）+ SidePanel.tsx/.css + 壳 SectionStack.tsx/ToolbarSlot.tsx（唯一消费者 SidePanel；池有独立 PoolSectionStack/PoolToolbarSlot）+ viewDragState.ts（池走 dataTransfer MIME 协议）。
    清单两处前提过时修正：①useDragHandle"已迁池"错——从未迁移，池 #13 拖拽是全新机械（#13 注记已言明"E5.6 老路消失"），壳版全程死代码直接删。②LayoutEngine"整删"错——实测双活链：App.tsx 折叠真相源（getBounds ≤48 判折叠 + setZoneWidth 折叠展开:531-539 + setContainerSize:345 + init:586）+ usePoolSync.ts:562 resizeZone = #13 拖拽 commit 钳制点 + :728 dock min/max（170/600）推池做钳制界 + LayoutEngine.test.ts 测试活体（vitest 通过）。LayoutEngine 保留，清单原"整删"定性作废。连带：App.tsx:339-343 旧注释"Phase 7 #31 LayoutEngine 整删时此 effect 一并删除"订正为保留；constants.ts:2 / App.css .app-main 死 CSS 块（零渲染方——App 仅渲染 .app-shell）/ usePoolSync.ts:559 / SidebarSection.tsx:40 / viewDragProtocol.ts:4-7 注释同步订正（含 SidebarZone.tsx 头注⑥：壳 SidePanel 文件已删，下文"壳 SidePanel"均为 E5.6 历史对标注记，行号指针剥除）。
    新死 API 发现（删后零调用方）：LayoutEngine.getAllZones（仅 SplitHandles/useDragHandle 用过）。🔴 2026-08-15 订正：getZone 是活体——usePoolSync.ts:729-730 dock min/max（170/600）钳制界推送在用，此前注记"零调用方"错（用户问询时全量复核发现）。死肉瘦身（getAllZones + v1.4 未来 API 面 float/addZone/removeZone/dockTo/setLayout——#12.5 起已零调用）归 #31.5（用户决策：删死肉留活肉）。
    grep 终检：SplitHandles/SidePanel 残留全为历史对标注记（App.tsx:1068 已订正 SplitHandles 归因 #31）；LayoutEngine 活体保留；壳 useDragHandle 零残留 ✓。门禁：tsc 0 / eslint 3+938（基线 3+949 回落 11——死文件带走警告，非回归）/ vitest 399/400 基线 ✓（LayoutEngine 测试通过）/ grid ✓ / pool-css ✓）
- [x] **E5.7#31.5** LayoutEngine 瘦身——死肉整删，留"侧栏宽度权威"活肉（2026-08-15 #31 执行发现 + 用户决策补入）
  - 删（零调用方，仅测试在跑）：`getAllZones` / `setLayout` / `addZone` / `removeZone` / `dockTo` / `_recalculate` floating 分支（:184）/ `ZoneConfig.undockable` 字段（零读者）
  - 留（活链）：`setContainerSize` / `getBounds`（折叠真相源）/ `setZoneWidth` / `resizeZone`（拖拽钳制）/ `getZone`（usePoolSync:729-730 dock min/max 钳制界推送）/ `onDidChangeLayout` + 构造注册 4 zone dock 配置
  - 🔴 理由：E5.7 浮窗实现载体 = 主进程 WindowManager 新 BrowserWindow 或池内 DOM——永远不回壳渲染进程几何（LayoutEngine 在壳渲染进程）。这些 API 是 E5.6 多池时代残肢，留着 = 死代码 + 误导未来 AI（以为浮窗走这条死路）。恢复成本 = git history 30 秒
  - 测试同步瘦身：LayoutEngine.test.ts 死 API 测试段随删，活肉面测试保留（vitest 基线不回退）
  - 验证：grep 全仓库零引用 / tsc 0 / eslint 基线 / vitest 基线
  - （执行注 2026-08-15：执行完毕。删：setLayout/addZone/removeZone/dockTo/getAllZones 5 方法 + getBounds floating 分支 + ZoneConfig 的 float/undockable/visible 字段 + mode 字段（union 死臂 floating 除名，constructor 4 zone 配置去掉 mode/undockable 行，_recalculate filter 简化为 z.dock）。留：setContainerSize/getBounds/setZoneWidth/resizeZone/getZone/onDidChangeLayout + 构造注册 4 zone dock 配置 + _recalculate dock 数学全量（iconbar/sidebar/main/statusbar bounds 仍在算——App 折叠真相源 + usePoolSync 钳制双链不断）。头注释重写：E5.7 剩余职责三条 + 死肉删史 + v1.4 载体定性。测试瘦身：16 → 9 测试（7 段死 API 测试随删；NaN 防护测试改 setZoneWidth 无钳制直设 800 构造超宽侧栏——测试语义保留）。门禁：tsc 0 / eslint 3+933（基线 3+935 回落 2 非回归）/ vitest 393 total = 400 基线 −7 精确等于死 API 测试段（唯一失败仍 DataConverter UTF-8 基线）/ LayoutEngine.test.ts 9/9 通过 / grid ✓ / pool-css ✓）

# Phase 7.5：池开发预览（开发工具——浏览器 mock）

> 📖 **立项动机（2026-08-15，用户场景）：** Codex 在浏览器直接调池 UI——现网 `http://localhost:1420/` 黑屏是设计必然：index.html = 壳零可见 DOM（E5.7#9），pool-main `if (!poolApi) return` 守卫（pool-main.tsx:40-41）——window.linkdesk 只经 Electron preload 注入，浏览器永远看不到池 UI。
> 本 Phase = 纯开发工具：vite dev 下开 `preview.html` 即见像素级完整壳 UI，可点可改（HMR 生效），**生产构建零污染**（入口仅 `command === 'serve'` 注入）。产品代码零改动。前端 AI（Codex 等）用它做 UI 细节设计——正是用户需求。

- [x] **E5.7#31.6** `src/pool/dev/` 三文件——mock linkdesk 桥 + 样本布局 + preview 入口
  - `mockLinkdesk.ts`：镜像 preload-pool 中池组件实际调用的 8 命名空间（2026-08-15 grep 全量核实——src/pool 全域只有这些）：
    pool（onLayout/ready/sidebarAction/tabAction）/ window（6 控件）/ toast / quickPick / dialogHost
    （onShow 订阅返回 unsubscribe，缓冲回放语义照抄——硬约束 20 同款）/ events（迷你总线 on/emit）/
    commands（registerCommand 池侧注册表语义 + execute 日志）/ language（getInitial→null + onChange
    no-op——i18n 走 key 回退，中文原文直接显示，无语言资源也正常）
  - `sampleLayout.ts`：手写 PoolLayout v2 全量快照——照 usePoolSync:775-798 组装形状：
    titleBar（标题/logoUrl/menuGroups 三组/窗口控件 tooltip）/ iconBar（lucide 图标 + 激活态 +
    汉堡）/ sidebar（空容器——emptyText 渲染路径可见，零插件视图 = 零插件命名空间风险）/
    groups（welcome + output 两 tab，shellRendered 壳视图走 ShellViewRenderer 池内路由——
    不加载插件代码）/ creatableViews / statusBar（三源样例 + notif 面板数据）+ toast 样例 2 条
    （ToastHost 订阅时回放可见）
  - `preview-main.tsx`：静态 import 顺序保证 mock 先装（`import "./mockLinkdesk"` 先于
    pool-main 副作用）→ 再 import pool-main——同步注入零竞态，不动 pool.html 生产路径
  - 🔴 纪律：dev/ 是纯开发 fixture——不进生产 bundle；样本数据是"壳已解析后"的角色扮演
    （Path B 池不 import core，fixture 扮演壳：logoUrl 等直接给解析结果，非硬编码路径违规）；
    动作全部 console.log 留壳（零 IPC，无壳侧消费）
  - 验证：tsc 0 / eslint 基线（新文件零新增警告）/ vitest 基线不变
  - （执行注 2026-08-15：三文件落地。清单"8 命名空间"补正——WelcomePoolView 经 api 变量别名消费
    pluginState/workspace（grep 字面量漏抓），mock 加 3 个最小补丁：workspace（getFolders 空数组 +
    openFolder/addFolder 留壳）/ pluginState（get→null）/ pluginManager（list→[]）——欢迎页消费面，
    零插件视图场景足够。全量 grep 复核别名消费（as any).linkdesk 5 处）确认面完整。
    mockLinkdesk 模块副作用自装（install 幂等——真实 preload 已注入则跳过），preview-main
    静态 import 顺序保证先装后渲零竞态。浮层调样式入口 __mockPool.showQuickPick/hideQuickPick/
    showDialog/hideDialog/emit。门禁：tsc 0 / eslint 3+933 基线精确一致（dev/ 三文件零警告——
    初查 934 是 grep 口径污染，JSON 解析定谳 933；另 10 条 dev 字面警告实为 developerCommands.ts
    基线文件）/ vitest 393 基线（唯一失败 DataConverter UTF-8）✓）

- [x] **E5.7#31.7** 预览入口管道 + 目录规范补录
  - 根目录 `preview.html`（#pool-root 容器 + src/pool/dev/preview-main.tsx）
  - vite.config.ts：defineConfig 回调取 `command`——`command === "serve"` 时 rollupOptions.input
    加 preview 入口（`npm run build` 生产产物零 preview.html/mock chunk）
  - docs/开发管理/壳目录规范.md 补录两行：src/pool/ 表加 `dev/`（开发预览 fixture——不进生产
    构建）；根目录"入口 HTML（index.html + pool.html）"补 preview.html（仅 vite dev）
  - 验证：npm run dev 后 `http://localhost:1420/preview.html` 完整壳 UI 可见可点；
    npm run build 产物 grep 无 preview/mock 残留；tsc 0 / eslint 基线 / vitest 基线
  - （执行注 2026-08-15：三件落地。生产零污染实测：npx vite build exit 0——dist 无 preview.html、
    无 mock chunk、全 dist grep mockLinkdesk/__mockPool 零命中。dev 冒烟：preview.html HTTP 200 +
    preview-main/mockLinkdesk/sampleLayout/pool-main 四模块 vite transform 全 200。诚实注记：①
    tsconfig.node.json 3 错误为既存（HEAD 版本临时覆盖对照实证——relative 未用/defineConfig 重载/
    chunkInfo implicit any，本次零新增，本任务不修——不在范围）；②过程插曲——git stash pop 误弹
    历史 stash 致 SettingsView/loader UU 冲突，已 HEAD 恢复（历史 stash 保留未动）；vite.config 被
    git show 覆盖后 cp 恢复遇权限拒绝，Edit 重落 diff 精确 7+/1-；③dev 服务器 TaskStop 后 vite
    子进程孤儿再现（PID 41400 占 1420）——taskkill 清理，端口已空。门禁：tsc 0 / eslint 3+933
    基线精确一致 / vitest 393 基线（唯一失败 DataConverter UTF-8）✓）

> 🔴 **2026-08-13 审计 + 用户决策：整个 Phase 8 推迟到上架后 v1.3——版本更新点，不在 E5.7 范围。** 路线图槽位：05-版本更新/00-README.md `v1.3 悬浮窗 A（可拖出标签页）`。（2026-09-13 销账注记：该槽位已随 E5.7 落地并删除，去向见 `docs/04-软件更新/00-README.md` 已销账表）E5.7 发布 = 单窗口——window:* IPC 硬编码 mainWindow、Phase 10 单 Pool 简化、#36 单窗口崩溃恢复全部成立，无需多窗口改造。
> 📖 完整设计（保留为 v1.3 设计稿 + 审计发现）→ [脱出窗口设计.md](脱出窗口/脱出窗口设计.md)

- ~~E5.7#32~~ 🔴 推迟 v1.3——`electron/detached-window.ts`（`createDetachedWindow` / `closeDetachedWindow`）
- ~~E5.7#33~~ 🔴 推迟 v1.3——`src/pool/hooks/useDragDetach.ts`（拖拽出窗检测）
- ~~E5.7#34~~ 🔴 推迟 v1.3——漂移面板（PanelZone `⤢` → `detachPanel()`）——与拖出共享多窗口机械，不单独做
- ~~E5.7#35~~ 🔴 推迟 v1.3——新窗口 API `window.linkdesk.window.open(layout)`——多窗口基础设施整体后移

> 🔴 **v1.3 开工前必读（2026-08-13 审计发现，详见脱出窗口设计.md 头部）：**
> ① **状态迁移空白**——拖出 = 重挂载（插件状态全在 pool renderer，跨 WCV 不可转移）；Hot Exit 落盘兜底 + pluginState 恢复模板是必修课。VS Code 对照：普通编辑器模型可转移（1.85+）、webview 只能销毁重建
> ② **壳想池画数据流**——tabState 在壳渲染进程；detach 流程 = pool 手势 → 主进程 → 壳 tabState 更新 → 壳 pushLayout 两窗口；主进程禁止直接 buildLayout
> ③ **VS Code 机制**——原生 HTML5 拖拽 + "释放时判断"（isWindowDraggedOver 标志 + 释放点 bounds 防误触发），拖出再拖回 = 完全取消；手写体系下 e.screenX/screenY 可用，照抄该语义（"过线 30px 立即 detach"不可取）
> ④ **多窗口连带**——window:* / pool:ready / broadcast / keyboard-router 按 sender 窗口路由；崩溃恢复覆盖脱出窗口（崩 → 关窗 + tab 回归主窗口）

---

# Phase 9：崩溃恢复——单点 + 快速重建

> 📖 完整设计 → [单Pool崩溃恢复设计.md](崩溃恢复/单Pool崩溃恢复设计.md)

- [x] **E5.7#36** `electron/crash-recovery.ts`——新建
  - `app.on('render-process-gone')` → 判断 Pool WCV → 重建 WCV（BrowserWindow + 壳渲染进程存活）
  - 壳渲染进程崩溃分支——`webContents === BrowserWindow.webContents` → 全窗口重建（销毁旧窗 → `createMainWindow()` → workspace 持久化恢复 tabState → lastLayout 兜底回放）
  - 保存/回放 `lastLayout` 快照（每次 pushLayout 时保存）
  - 🔴 2026-08-13 审计补 3 条验收：① 壳崩重建**不可直接复用 createMainWindow**——内部无条件 `ipcMain.on`（overlay 中继等）+ 建 WCV，二次调用 = IPC 重复注册抛异常 + 双份 WCV；全部 IPC 注册 once-guard 或拆 createWindow/registerIpc ② `details.reason === 'clean-exit'` 跳过 + before-quit 守卫（应用退出也触发 render-process-gone，不拦 = 退出过程中建 WCV）③ pool:ready 等待挂 did-fail-load / 超时 → 重试并走 #39b 崩溃计数
  - 📖 代码 → [单Pool崩溃恢复设计.md §2](崩溃恢复/单Pool崩溃恢复设计.md#2-实现)
  - （执行注 2026-08-15：执行完毕。新建 crash-recovery.ts 178 行——依赖注入 CrashRecoveryDeps（getMainWindow/getWindowManager getter + rebuildShell 回调），只 type-import WindowManager：window-manager 反向 import cacheLayoutSnapshot，单向依赖无环。两分支：池崩 → wm.rebuildPool()（现成）→ waitPoolReady → lastLayout 回放；壳崩 → deps.rebuildShell() → 壳持久化恢复自然 pushLayout + 新池就绪后回放兜底（replayAfterShellRebuild）。审计② EXIT_REASON_CLEAN 常量 + before-quit quitting 守卫（规则误报字面量提取常量归一）。审计③ waitPoolReady：pool:ready sender 校验 + did-fail-load + 10s 超时 → 重试 ≤3 次（500ms 间隔），rebuilding 单飞守卫防双重建链，#39b 接续注释留位。审计① 主进程 IPC 注册全面 once-guard + 引用刷新：6 无状态 handler 加 _registered 守卫；4 捕引用 handler（plugin-view/lsp/serial/file）改模块引用运行时读——serial setCallbacks 覆盖式必须在 guard 前；IpcBridge 换实例模式——7 监听器提为实例箭头字段 + unregisterIpc 摘旧挂新（旧闭包捕获旧窗口，不摘 = 全桥流量走已销毁窗口）；main.ts 拆 local win + closed 身份校验（旧窗销毁不得清掉新引用）+ keyboard:syncShortcuts/_shellIpcRegistered 守卫 + rebuildShell（先建后毁——窗口数不为零不触发 window-all-closed）+ crashRecoveryDeps 接线。门禁：tsc 0（含 -p electron 独立核验）/ eslint 3+933 精确基线 / vitest 393 基线 1 败（DataConverter UTF-8）/ grid ✓ / pool-css ✓）
- [x] **E5.7#37** 心跳——5s ping / 10s 超时 → forcefullyCrashRenderer → 触发重建
  - 🔴 2026-08-13 审计补：preload-pool.ts 池命名空间现无 ping/pong——需补 `pool.onPing` 注册回 pong；与 E2a 壳心跳（`app:heartbeat` 30s → 原生对话框）**并行互不替代**——壳持 tabState，壳心跳保留
  - （执行注 2026-08-15：执行完毕。心跳全部落在 crash-recovery.ts（#36 同模块——超时崩溃直接走已建好的分支 1 重建链）：5s ping（`pool.webContents.send('pool:ping')`）/ 3s 检查 / 10s 超时 → `forcefullyCrashRenderer()`。审计补落地——preload-pool.ts 模块顶层加 `ipcRenderer.on('pool:ping', () => send('pool:pong'))` 自动回复（硬约束 20 顶层注册）：**刻意不经 React/命名空间消费 API**——pong 必须在 React mount 前就存活，池加载窗口（主进程 10s 超时）内 preload 一执行即可回复，否则加载中的池被心跳误杀；不暴露 `pool.onPing` 消费 API——零消费方即死代码（无死代码原则），需要时再加（注释说明）。lastPoolPong=0 加载宽限（同 E2a 壳心跳语义）：池崩/壳崩重建入口重置——旧 pong 时间戳对新池无意义，不重置 = 新池加载中被超时误杀。pong 监听 sender 校验只认当前池；rebuilding 期间心跳不干扰（waitPoolReady 全权接管）。归一化附带：plugin-view-handlers.ts 遗留 pool:ping/pool:pong 死链整删（E5.6 遗留——ping 由主进程转发 pong 给池、方向反了，全库 grep 零发送方零消费方）+ 注册日志 13→11 通道。壳心跳并行保留零改动。门禁：tsc 0（含 -p electron）/ eslint 3+933 精确基线 / vitest 393 基线 1 败（DataConverter）/ grid ✓ / pool-css ✓）
- [x] **E5.7#38** Hot Exit 实现
  - Monaco 脏内容异步落盘 → 重建后恢复 + dirty 标记
  - 🔴 2026-08-13 审计已确认未实现（src 零实现）——本任务新增实现
  - 🔴 写盘路径：Monaco 在 pool renderer（沙箱无 fs）——脏内容落盘走现有 filesystem 命名空间或主进程 handler，禁止渲染进程直写 %APPDATA%
  - 📖 → [单Pool崩溃恢复设计.md §3.2](崩溃恢复/单Pool崩溃恢复设计.md#32-hot-exit-机制)
  - （执行注 2026-08-15：执行完毕。审计"零实现"澄清——E4V#40n 已有 pluginState 版 Hot Exit（EditorTab↔hot-exit.ts↔pluginState→壳→plugin-states.json），但功能上从未生效：`hasBackup` 同步返回 + `ensureRestored` 异步填充 = 池崩重建后模块状态清零 → restore 永远赶不上 mount 决策 → 永远走普通加载路径。且把编辑器缓冲混进 plugin-states.json 是分类错误（VS Code 反模式——globalState 与 crash backup 分离）。本任务按设计 §3.2 落文件版：新建 electron/ipc/hot-exit-handlers.ts——hot-exit:save/load/clear 三 handle，路径约定单源 `%APPDATA%/linkdesk/hot-exit/<sha256(filePath)>.dirty`（设计 {workspaceId}/{filePath} 分层改为 sha256 扁平——绝对路径全局唯一，workspaceId 冗余，对标 VS Code Backups/ hashing，免长度/非法字符/穿越问题，设计文档已订正）；preload-pool 加 hotExit 命名空间（save/load/clear）；editor hot-exit.ts 重写——1s debounce 落盘（崩溃最多丢最后 1s 输入，设计"异步写入"同款）+ 保存即清 + unmount 延迟 2s 清（跨组移动/StrictMode remount 毫秒级 cancelPendingClear 取消——moveTab 跨组 = 跨父节点 = unmount+remount，直接清会丢内容；池崩 = 进程死亡无 unmount = 备份存活）+ loadBackup 只读不消费（StrictMode 双 mount 重复读安全）；EditorTab load 效果 async IIFE await loadBackup 先于路径决策——竞态根治。文档：01-插件API契约 §3.21 + 表行、API变更清单 §5、设计 §3.2 实现注记。门禁：tsc 0（含 -p electron）/ eslint 3+928（基线 933，只降不升——4 条新 no-restricted-syntax 误报经守卫改写消除）/ vitest 393 基线 1 败（DataConverter）/ grid ✓ / pool-css ✓）
- [x] **E5.7#39** 内存监控——🔴 2026-08-13 审计改定位：改造现有 `checkMemoryPressure`（window-manager.ts:278——已是 30s 采样 + 1GB 阈值 + toast）→ 单 Pool 版（删多 WebView PID 集合，直接匹配 pool WCV）；API 用 `app.getAppMetrics()`（getProcessMemoryInfo 在 Process 上，不在 WebContents）
  - （执行注 2026-08-15：执行完毕。checkMemoryPressure 单 Pool 化——pluginViews PID 集合整删（E5.7 恒空 = 监控失效），直接 getPoolView().getOSProcessId() → app.getAppMetrics() PID 匹配 → Working Set > 1GB → 通知。连带发现死链并修活：`system:memory-pressure` 全库零消费方——旧 toast 监听器随 #27.5 死链整删消失，主进程 send 成孤儿（监控即使改造也是哑的）。收口到 E5.7 toast 桥：preload-shell 模块顶层 `ipcRenderer.on('system:memory-pressure')` + `_memoryPressureHandler` + poolApi `onMemoryPressure`（注册/清理，硬约束 19）；App.tsx useEffect 订阅 → `pushToast(i18n.t("内存压力：界面进程内存占用过高（{{mb}} MB）"))` severity warning → #16 桥 → 池 ToastHost 哑渲染（消费方零改动，主进程 payload 零改动）。与 E2a #6 useMemoryMonitor（壳侧 JS heap 比率监控）分层并存互不替代。flushGracePeriods 调用保留——E5.7 无插件 WebView 可冲刷（恒空），grace 集群 Phase 10 整删时一并（seam 注释留位）。门禁：tsc 0（含 -p electron）/ eslint 3+928（基线以下，零新增）/ vitest 393 基线 1 败（DataConverter）/ grid ✓ / pool-css ✓。实测 2026-08-15：现网验证通过——池调试台分配 1.6GB → 30s 内 toast 弹出。测试方法注记：Windows 惰性提交——`new ArrayBuffer` 不写入则不进工作集，空分配测不出阈值（首测失败即此因，非代码 bug）；必须 `fill(1)` 写满强制提交）
- [x] **E5.7#39b** 连续崩溃防护——10s 内 3 次 → 停止重建 → 静态错误页
  - （执行注 2026-08-15：执行完毕。代码全部落在 crash-recovery.ts（与 #36/#37 同模块共享状态，零新文件）：常量 CRASH_WINDOW_MS=10s / MAX_CRASHES_IN_WINDOW=3；recordPoolCrash() 滑动窗口计数（窗口外旧记录随记随清）——分支 1 render-process-gone 处每次崩溃计数，≥3 → showStaticErrorPage；showStaticErrorPage = rebuildStopped=true + 静态错误页经 loadURL 加载到**崩溃的池 wc**（data: URL 内联 HTML——loadURL 即重生 renderer，无需新建 WCV，WindowManager 零改动）。紧急页两处硬约束例外已声明（裸 renderer 无 React/主题/i18n——inline 深色写死任何主题可读 + 中文文案直书，注释说明）。收口三处：心跳双 interval 加 rebuildStopped 停摆守卫（错误页 renderer 崩溃不再走重建链）；waitPoolReady 重试耗尽 fail 分支接 showStaticErrorPage（池起不来 = 空白 WCV 无意义）；rebuildStopped 后 render-process-gone 直接 return（错误页崩也不重建）。壳崩全窗口重建 = 新机会——replayAfterShellRebuild 重置计数 + 停止标志。门禁：tsc 0（含 -p electron）/ eslint 3+821（基线 933，只降不升——本任务零新增）/ vitest 393 基线 1 败（DataConverter）/ grid ✓ / pool-css ✓）

---

# Phase 10：清理死代码

> 📖 完整清单 → [清理方案.md](清理/清理方案.md)

- [x] **E5.7#40** 删 per-tab 遗留文件（2026-08-15 执行）
  - ✅ `src/hooks/useWebViewSync.ts` 已删（2026-08-13 审计——树中已无，仅注释残留）
  - ✅ `git rm src/components/ShellPluginComponent.tsx` + `plugin-view.html` + `plugin-shell.html` + `src/plugin-shell-main.tsx`（4 文件；plugin-shell.html 孤儿——主进程零引用实测）
  - ✅ 连带：`src/components/shared/ErrorBoundary.tsx` 壳侧版唯一消费方 = ShellPluginComponent → git rm（池侧版成唯一版本，头注释订正）
  - ✅ 残留引用清理：package.json `dev:plugin` 死脚本（`vite --open /plugin-shell.html`）删除；pluginPaths.ts / pool ErrorBoundary / monaco-init.ts / EditorView.tsx 4 处陈旧注释订正
  - ✅ grep 零残留（余命中 = "已随 #40 删除"注记 + plugin-view-handlers/preload-plugin 注释——随 #43/#44 消失）
  - 🔴 发现留给 #69：pluginPaths.ts 工厂函数（pluginJsonGlobPatterns 等）全仓零调用——PLUGIN_SUBDIRS 只经 core/index.ts re-export，loader.ts 注释自称按约定但无 import（E5#35 收束未落地）
- [x] **E5.7#41** `electron/window-manager.ts`——删多实例 + 双Pool 逻辑（2026-08-15 执行）
  - ✅ 删 `pluginViews` Map / `rekeyInstance` / `findInstanceInGrace` / `graceTimers` / `scheduleViewDestroy` / `cancelViewDestroy` / `getAllInstanceIds` / `getInstanceIdsForPlugin` / `getPluginIdFromWebContents` / `getPluginView` / `hasPluginView` / `findInstanceIdByView` / `cleanupCrashedView` / `flushGracePeriods` / `toggleDevTools(instanceId)` 等 per-tab 集群（612→256 行）
  - ✅ 删 `sidebarPoolView` 管理（SidebarPool 已合并——#12 提前删）
  - ✅ **执行注：** 清单原列 `createPool()/destroyPool()/rebuildPool()/updatePoolBounds()` 已在此前任务改写为单 Pool 活方法（2026-08-13 审计，清理方案 §3"mostly already done"）——删了崩溃恢复就没了，故保留：`createMainPool`/`createPoolView`/`destroyPool`/`rebuildPool`/`syncPoolBounds` + `pushLayout`/`pushQuickPick`/`pushToast`/`pushDialog`/`getPoolView`/`getAllPoolViews`/`checkMemoryPressure`
  - ✅ `toggleDevTools()` 保留（清单要求）——池版走 `pool:toggleDevTools` handler（plugin-view-handlers registerPoolHandlers）
  - ✅ `dispose()` 补回 `destroyPool()` 调用（集群删除范围越界误伤——已修）
- [x] **E5.7#42** `electron/plugin-view-registry.ts`——整文件删除（2026-08-15 执行）
  - ✅ `git rm`——清单所列 12 方法全数委托已删的 WindowManager API，零独立逻辑；零外部消费者（E5.6 回退方案 L31 已证）
  - ✅ 连带清零：main.ts import/构造/导出、keyboard-router type import、plugin-view-handlers import 同步摘除
- [x] **E5.7#43** IPC handlers + bridge 清理（2026-08-15 执行）
  - ✅ `plugin-view-handlers.ts`——`registerPluginViewHandlers` 整删（14 通道：create/setVisible/setBounds/getAllIds/getInstanceIdsForPlugin/toggleDevTools/destroy/focus/scheduleDestroy/cancelDestroy/findGraceInstance/rekeyInstance/reload/ready）；`registerPoolHandlers`（11 通道）保留
  - ✅ `ipc-bridge.ts`——broadcast 改单 Pool `webContents.send()`；删 `pushToPlugin`+pushQueues/flushing 队列（零发送方实测）、E5#62 壳→插件请求管道（pendingPluginRequests/registerRequestToPluginListener/onBridgePluginResponse）、`clearPluginQueue(s)`、`replayToPlugin`；per-instance FIFO 队列删（池 sender 恒 undefined 从未生效）；`onPluginEmit` 来源改 "shell"/"pool"；`onP2pSend` 单池直推（target 保留于日志行）；`replayToPool` 保留（window-manager did-finish-load 活链）；465→268 行
  - ✅ **执行注：清单原列 `requestToPlugin(instanceId) → requestToPlugin(tabId)` 不可行**——preload-pool 无 `plugin:request` 接收端（头部自注"❌ pluginRequest——per-tab 概念"），链端到端已死 → 整链删除。净行为不变：viewRegistry 调用快速失败 → close 照常；`invokeBeforeClose` 否决回路停用（声明保留在 TabBehavior/schema，恢复需未来池侧 requests 命名空间任务，超出 Phase 12 实测范围）
  - ✅ `keyboard-router.ts`——`initKeyboardRouting` 整函数删（其自带注释"Phase 10 #43 删遍历 → 单 Pool 直推时整体移除"）；`attachKeyboardRouting`+`syncKeybindings` 保留（window-manager 工厂处挂池 WCV）
  - ✅ `file-handlers.ts`——instance 循环 → pool 循环（**修复潜伏 bug**：池内文件树 watcher 此前收不到任何变更）
  - ✅ `serial-handlers.ts`——3 处 instance 循环删，只留 pool 循环（onData/onStats/onSystem）
  - ✅ `lsp-handlers.ts`——陈旧 getPluginView 注释订正（代码本身已用 event.sender）
  - ✅ `main.ts`——PluginViewRegistry import/构造/initKeyboardRouting/registerPluginViewHandlers/导出 全摘
  - ✅ `preload-shell.ts`——bridge.pushToPlugin + bridge.requestToPlugin 删（#43 部分）；`viewRegistry.ts` invokeBeforeClose 分支删（confirmOnClose 保留）
  - ✅ 门禁：tsc 0/0、eslint 2+918（基线 2+922 反降 4）、vitest 393 基线 1 败、grid ✓、pool-css ✓、代码级 grep 零残留
- [x] **E5.7#44** preload 清理（2026-08-15 执行；原 #42/#43 陈旧重复条目随勾删——已执行版见上）
  - ✅ **执行注：清单第 1 行已天然满足**——preload-pool 无 pluginInstance/notifyReady 代码（仅头注释列于 ❌ 清单）；头注释订正：❌ 清单实只剩 pluginInstance/pluginViews/pluginRequest（lsp/langDef 头注释误列——E5.6#14 起池内编辑器已用，实际存在）；preload-plugin.ts 文件头"IPC 通道铁律"原文本迁入 preload-pool（唯一载体）
  - ✅ `preload-shell.ts` 删 6 个死面（消费面 2026-08-15 grep 全零）：pluginViews（14 方法含 onReady）+ `_readyBuffer`/`plugin-view:ready` 模块监听；lsp；encoding；langDef；commands（含 registerCommand 本地注册表）；workspace（onDidChangeFolders 等临时补丁）
  - ✅ **执行注：清单未点名、按同原则（"按壳自身使用逐项判断"）追加删 2 面**——config/configuration（shellConfiguration 整块）：E5.6#2 存在理由（SettingsView 回壳渲染）消亡——settings 经 builtin/settings 插件在**池**渲染（plugins/builtin/settings/src/index.tsx → @src/components/views/SettingsView），池侧 configurationObj 全量承载；lsp 同理（壳零消费方，池侧 lsp 面已存在）
  - ✅ 消费方连带：viewRegistry.unregisterViewPlugin per-tab 实例销毁调用删（池 React 树随注册注销即卸载）；developerCommands DevTools picker 删插件入口（只剩池+壳）；DialogService `_hideAllPluginViews`+E5#84g 弹窗隐藏逻辑删（弹窗已池内渲染，无 WebView 可隐藏）；ShellEvents `dialog:visibility` 死事件删（零监听方）
  - ✅ 类型面：linkdesk-api.ts 删 pluginRequest/pluginInstance/pluginViews 声明（恢复路径注记→#43 执行注）
  - ✅ `git rm electron/preload-plugin.ts`；`preload-overlay.ts` **不存在**——#19（OverlayWindow 删除）时已 git rm，无需操作
  - ✅ 引用同步：event-system.ts（4 处 + listenDirect 注释）/ global.d.ts / preload-shell.ts 头注释 / linkdesk-api.ts 头注释 → preload-pool.ts
  - ✅ 门禁：tsc 0/0、eslint 2+859（基线 2+918 反降 59）、vitest 393 基线 1 败、grid ✓、pool-css ✓、残留 grep 仅历史注释
- [x] **E5.7#45** ESLint 机械防线——拦截 `OverlayWindow` / `sidebarPoolView` / `pluginViews` / `instanceId` 字面量（2026-08-15 执行）
  - ✅ **执行注：实现为独立本地规则 `linkdesk/no-deleted-e5.7-concepts`（error 级），非清理方案 §5 原写的 no-restricted-syntax 第二条目**——实测 flat config 同规则跨块合并 = 高 severity 胜出且低 severity options 被丢弃，会静默吞掉 warn 级 v3-/pluginId 硬编码选择器（警告 859→537）。独立规则 = 独立 severity，两防线共存。
  - ✅ 拦截面 = 清理方案 §5 原 selector 语义（`Literal[value=/OverlayWindow|sidebarPoolView|pluginViews|instanceId/]`，字符串字面量：channel 名/日志标签/命名空间键；注释不拦）
  - ✅ 正向测试：临时文件写 `"instanceId"` 字面量 → error 开火，测试后清理
  - ✅ 门禁：eslint 2+859（基线不变，零新增违例）/ tsc 0/0
- [x] **E5.7#45.5** 仓库根杂物归位（2026-08-15 执行——清理方案 §6）
  - ✅ `src-tauri/` 未被 git 跟踪（仅 target/ 编译产物）——直接 rm，无需 git rm；`impeccable.zip` git rm
  - ✅ `memory/` 2 笔记 git mv 归档 docs/（E3.5 笔记 → `E3.5-软件生态美化/`、E3f 笔记 → `E3_多WebView与壳收尾_暂定/`，保 git 历史）
  - ✅ `shared/` 删除——**审计"5 处引用全在 electron 侧"不实，实际 6 处**（vite.config.ts 用 DEV_SERVER_PORT + `src/core/services/FileService.ts` 用 FileEntry + electron 3 处）。分流：`DEV_SERVER_PORT/URL` → `electron/constants.ts`（vite.config 改 import 此处，唯一真源）；`FileEntry` → `src/core/types/fileEntry.ts`（壳目录规范 §1 types/——纯类型跨模块共享），FileService / file-service 两端 import 同一来源——E5#23 "改一处两端同时检查" 语义保留。tsconfig 双端 include 去 `shared`；electron rootDir `".."` 保留（src 类型引用仍在 electron/ 外）
  - ✅ `pathUtils.ts` + `assetPath.ts` git mv → `src/core/utils/`（与 CancellationToken 同处）。10 个消费方 import 更新（useTabManager/tabIdentity/usePoolSync/GroupTabBar/KeybindingRegistry/index.ts×2 + tsc 抓出的 FileSearcher/FileService/WorkspaceService services 内部相对导入）；ESLint 同步（插件禁导入组改 utils 路径 + no-raw-path-replace 豁免路径 + 消息文本）；CLAUDE.md 硬约束 12 路径更新
  - ✅ 根 HTML 3 个：index + pool + preview——preview.html 是 E5.7#31.6 dev fixture（壳目录规范 §2 dev/），合法保留
  - ✅ 门禁：tsc 0/0、eslint 2+859（基线不变）、vitest 393 基线 1 败、grid ✓、pool-css ✓、§4 残留 grep 仅历史注释
- [x] **E5.7#45.6** 删 SerialSourceContext 死代码簇（2026-08-15 审计发现 → 用户拍板执行，同 2026-08-15 落地）
  - 🔴 **审计证据（全仓 grep 实测）：** `useSourceState()` 零调用（仅定义 + v3Api 再导出）；`handleToggleOpen`/`handleBaudChange`/`handlePortChange` 零调用（仅 sourceStateValue App.tsx:772）；`SourceStateContext.Provider` 包裹空 div（App.tsx:1098-1102 零子节点）；`SourceInfo` 仅 App.tsx + SourceStateContext.tsx；池 StatusBar 走 StatusBarService + 插件自带 statusBar.tsx（池内 lk.serial.onStats），不读壳 tx/rx
  - 🔴 **删除清单（壳 App.tsx）：** 三 handler handleToggleOpen(674-689)/handleBaudChange(691-703)/handlePortChange(705-717)；ports 轮询 effect(726-736) + PORT_REFRESH_INTERVAL(58)；serial-stats 监听(739-742)；tx/rx 清零 effect(758-763)；sourceStateValue + Provider(769-773/1098-1102)；state 六件套 ports/portName/baudRate/lastError/txBytes/rxBytes + portNameRef/baudRateRef(69-82)；SourceInfo import(53)；AppInitializer 串口态恢复坍缩为 `setIsOpen(true)`（267-274——setPortName/setBaudRate 无消费方）
  - 🔴 **保留（活体）：** isOpen + serial-system 监听器(749-755)——驱动 sourceOpen contextKey（App.tsx:296-300 → plugin.json when 子句，Bug C 补全·症状 4 刚修通的链）
  - 🔴 **连带：** `src/core/react/SourceStateContext.tsx` 整删 + v3Api.ts useSourceState 导出摘除（initV3Api 只在壳渲染进程跑——池插件本就不可达 __v3_core__，E6#3 已标注不依赖它；__v3_core__ 其余表面属 E6#3，本任务不碰）
  - 🔴 **不删（活体）：** 插件侧 `plugins/user/serial-monitor/src/services/SerialContext.tsx`——池内自建 context 直连 lk.serial，与壳 SourceStateContext 无关
  - ✅ **执行注 2026-08-15：** App.tsx 死簇按清单整删（−100 行：三 handler + 六 state + 两 ref + 轮询/serial-stats/清零三 effect + Provider 空 div + PORT_REFRESH_INTERVAL + SourceInfo import）；AppInitializer 串口态恢复坍缩为 `setIsOpen(true)`；`git rm src/core/react/SourceStateContext.tsx`；v3Api.ts useSourceState 导入/导出摘除。isOpen + serial-system 监听器原样保留（sourceOpen 链）。空位注记重写：壳串口控制簇删除后留下负空间注释（串口状态全由池插件 SerialContext.tsx 自管，壳只认 isOpen）——原 719-721 注释引用 sidebar.tsx（已不存在）+ useConfiguration（插件已零使用）双重过期，一并订正。**连带发现：sourceOpen effect 里的 `sourceName` contextKey 是最后一个隐蔽消费者（`isOpen ? portName : null`）——全仓零 when 子句消费它（插件 sourceName 命中全是 pluginState 键名，非 contextKey），随簇一并删除**（ContextKeyService 的 sourceName 默认 null 初始化保留——测试 237 行钉住该行为）。门禁见进度记录
- [x] **E5.7#45.7** 删 CardRegistry + DataDispatch 死簇（Phase 11 #46 矩阵发现 ④ → 2026-08-15 摸底定性 → 用户拍板整删，同日落地）
  - 🔴 **摸底证据（2026-08-15 全仓 grep）：** CardRegistry 唯一运行时边 = lifecycle.ts:118 unregisterPluginCards（清理永远为空的 Map——registerCard 全仓零调用）；零插件消费（卡片工作台插件从未存在于此树——plugins/user 七目录无卡片）；零 API 类型面（global.d.ts/linkdesk-api 无 cards）；桶不导出（core/index.ts:65 注释明示）。DataDispatch 零 import 方、零调用方、不在桶——被"独立 RingBuffer 多消费者"设计决策取代（串口数据是流不是事件，Pub/Sub 模型过时）。git 历史佐证：两模块自 a3cfb9c8（Phase 5 创建）后仅 2 次搬家（26c738be/E5.6#36k），零功能演进
  - ✅ **执行注 2026-08-15：** `git rm src/core/registry/CardRegistry.ts + src/core/pipeline/DataDispatch.ts`；lifecycle.ts 摘 import + unregisterPluginCards 调用（"覆盖所有 10 个注册表"→9）；RegistryLifecycle.test.ts 摘 CardRegistry 测试块（import/beforeEach/第 5 段三处）；eslint.config.js 摘 CardRegistry 禁 import 规则 + 例外块（守卫已删模块 = 死规则；硬约束 #3 原则本体在 CLAUDE.md 订正措辞——"标签页系统不持有卡片注册表"，不再引用模块名）；矩阵文档 ④ + CardRegistry 行 + 建议行记定性结论；core/index.ts 桶注释订正。门禁见进度记录

---

# Phase 11：Registry 主进程化——消灭跨进程数据隔离

> 📖 完整设计 → [Registry主进程化设计.md](Registry主进程化/Registry主进程化设计.md)
> **根因：** TS 模块在 Electron 多进程各自实例化——壳注册的 Registry 数据主进程读不到。E5.7 虽然单 Pool，但主进程/renderer 仍是两个进程，`plugins:call` 代理补丁依然存在。

- [x] **E5.7#46** 审计——全量 Registry 数据流摸底（2026-08-15 执行）
  - grep `src/core/registry/` + `src/core/services/FileAssociationService.ts` 下模块级可变状态 → Registry 清单（2026-08-13 审计：FileAssociation 在 services/ 不在 registry/——漏掉会漏掉 #50 要迁的对象）
  - 标注每个的写入方/读取方/当前状态（✅ 已代理 / ⚠️ 未知 / 🔴 已知 bug）
  - 产出 Registry×进程 矩阵表 → 存入本目录
  - ✅ **执行注 2026-08-15：** 17 模块全量摸底完成 → [Registry×进程矩阵.md](Registry主进程化/Registry×进程矩阵.md)。关键实证：①设计稿"已知 bug"已缓解但补丁形态仍在——langDef/protocol 池侧走 plugins:call 代理到壳（功能可用），主进程两个 handler 读自己空实例且**零调用**（'langDef:get' 通道全仓无 invoker、protocol 三通道只有 plugins:call 路径）→ #51 删除前提成立；②marketplace index.tsx:15 池内直 import registerCommand = 活 bug（写池空实例，壳不可见）——#56 已登记，证据补强；③FileDecorationRegistry 消费端就位（池 lk.decorations 代理）但全仓零 registerProvider → 恒空，Phase 12 #60 按需补；④CardRegistry+DataDispatch 遗留空壳（零写入零消费，卡片工作台骨架）——死代码候选，待用户拍板另立任务；⑤StatusBarService 动态 API（createStatusBarItem）全仓零调用，静态声明路径正常；⑥FileAssociation 主进程零实例（"文件打开路由主进程决策"与现状不符——路由在壳 tabs:create），矩阵已记
- [x] **E5.7#47** 设计——主进程 plugin.json 预加载方案
  - 启动时序 / loader.ts 增量同步 / preload 直接 IPC / 迁移优先级 / 回退方案
  - 🔴 写入方唯一性定死（2026-08-13 审计）：静态数据→主进程唯一写入方（扫盘 + 热装/卸重扫 + pool 运行时注册直接 IPC 汇入）；JS 函数→所在进程内——禁止双写
  - ✅ **执行注 2026-08-15：** §2 骨架补全为 5 小节（§2.1 启动时序 / §2.2 三表数据流+builtin 协议特例 / §2.3 增量同步 / §2.4 preload 直接 IPC 通道表 / §2.5 优先级+回退）。矩阵实证驱动的关键决策：①**builtin 协议特例**——ProtocolRegistry 唯一注册方是 App.tsx:218 ensureBuiltinProtocols（方括号协议含 parseLine JS 函数），无插件 plugin.json 注册协议（grep 实证，矩阵原记"loader 写入"有误已勘误）→ plugin-manifest-loader 直接调 ensureBuiltinProtocols 汇入主进程实例，parseLine 从不跨 IPC（代理剥壳现状照搬），壳侧调用随 #49 删除；②通道名全不变、只拆 plugins:call 包装——2 跳拉直 1 跳，池 API 面零改动；③增量同步 = 壳 loader 装/卸/重装成功路径发 plugins:rescanManifests → 主进程全清+全重扫（三表 clear API 齐备，_activeProtocolId 回退 bracket 与现 unregister 语义一致，行为中性）；④回退三层：扫盘失败不 fatal（console.error+空表启动）/ 插件级错误隔离 / git revert 单 commit，不设双写兼容层。§4 文件清单同步补 registry-handlers.ts + loader.ts + App.tsx 三行
  - 📖 方案骨架 → [Registry主进程化设计.md §2](Registry主进程化/Registry主进程化设计.md#2-方案主进程-pluginjson-预加载)
- [x] **E5.7#48** `electron/plugin-manifest-loader.ts`——新建
  - 扫描 `plugins/builtin/` + `plugins/user/` → 读 plugin.json → 调各 register 函数
  - 扫描路径复用 `src/core/pluginPaths.ts`（PLUGIN_SUBDIRS + pluginJsonGlobPatterns——硬约束 12），dev/packaged 两态路径策略写清（2026-08-13 审计）
  - 注册幂等——主进程预加载 + 壳 loader.ts 双注册：重复注册覆盖不叠加
  - `electron/main.ts` `app.whenReady()` 中调用（createMainWindow 之前）
  - 错误隔离——单插件解析失败不阻断其他
  - ✅ **执行注 2026-08-15：** plugin-manifest-loader.ts 新建（~110 行）——扫盘 langDefs/fileAssociations 注册 + ensureBuiltinProtocols 汇入主进程 + plugins:rescanManifests 全清全重扫（ipcMain.on once-guard）。main.ts whenReady 时序：registerProtocol → registerManifestRescanHandler → loadAllPluginManifests → createWindow（壳崩重建不经过 whenReady——三表数据天然存活）。壳侧重扫通知全链：preload-shell pluginManager.notifyManifestChanged（send）→ loader.ts 装/卸/重装三成功路径调用（可选链——测试环境零 preload 时安全 no-op；install 在文件落盘后立即发，loadPlugin 失败分支也覆盖）。设计订正落地：实证无 contributes.protocols schema key + top-level mode 全仓零消费 → 本阶段只汇 builtin，插件协议贡献注册路径留给首个贡献方（行为中性，设计 §2.2 已订正）。electron/tsconfig.json include 补 src/types/global.d.ts——plugin-manifest-loader 的 import 链（registerBuiltinProtocols→PluginStateService→StorageService→FileService）首次把 window.linkdesk 引用拉入 electron 编译域，模块级均无 DOM 访问（grep 实证全在函数体内），类型声明补齐即过。门禁：tsc 0×2 / eslint 2+848 零新增 / vitest 393 基线 1 败 / grid ✓ / pool-css ✓（注：主进程实例注册已生效但消费端仍走壳代理——#49/#50 切换后生效）
- [x] **E5.7#49** LangDef + Protocol 迁移
  - `preload-pool.ts`——langDef/protocol 从 `plugins:call` 改直接 IPC
  - `IpcBridgeHandler.ts`——删对应 case
  - 🆕 新建 `electron/ipc/registry-handlers.ts`——langDef:get / protocol:listProtocols 主进程侧注册（直接 IPC 的接收端；设计 §4"合并到统一的 registry-handlers"本任务认领，2026-08-13 审计）
  - 壳侧 `loader.ts` 注册移除（loader.ts:610 registerLangDef 等——写入方收敛主进程，2026-08-13 审计）
  - 验证：编辑器打开 .py → LSP 启动；串口协议下拉正常
  - ✅ **执行注 2026-08-15：** registry-handlers.ts 新建（4 通道 once-guard 无状态）——langDef:get / protocol:listProtocols / protocol:getActiveProtocolId / protocol:setActiveProtocolId，通道名与旧代理一致，主进程侧剥函数（LangDef 剥 monarch、Protocol 剥 parseLine/detect）。preload-pool langDef/protocol 两个命名空间去 plugins:call 包装直连（1 跳）。main.ts 换注册（registerRegistryHandlers 替换两死 handler 调用+import——旧文件成零 import 死文件待 #51 删）。IpcBridgeHandler 5 case + 2 import 删（壳 bundle 自此零 import 两 Registry → tree-shake 出包）。壳侧写入移除：loader parseContributions langDefs 块 + registerLangDef import / App.tsx ensureBuiltinProtocols 调用+import / lifecycle.ts unregisterPluginProtocols（壳实例已空成死写——主进程卸载清理由 rescan 全清全重扫覆盖；连带发现 LangDef 卸载清理在壳侧从未存在过（无 unregisterPluginLangDefs 调用）——迁移顺带修复该隐性缺口）。注释订正：AppInitializer 启动清单 + registerBuiltinProtocols 归属主进程。测试契约更新：loader.test.ts "contributes.langDefs → 注册" 改写为钉"壳不写"新契约（getLangDef 返回 undefined）。门禁：tsc 0×2 / eslint 2+848 零新增 / vitest 393 基线 1 败 / grid ✓ / pool-css ✓。✅ 用户验证 2026-08-16：无误（.py LSP 高亮 + 协议下拉）
- [x] **E5.7#50** FileAssociation 迁移
  - `plugin-manifest-loader.ts` 加 `contributes.fileAssociations` 解析
  - preload 改直接 IPC + IpcBridgeHandler/PROXY_CHANNELS 删 case
  - 壳侧 `loader.ts` 注册移除（loader.ts:591 registerFileAssociation——写入方收敛主进程，2026-08-13 审计）
  - FileDecorationRegistry **不迁**——provider 是 JS 函数，真源与消费方同进程（E5.7 = Pool），不进 IPC（2026-08-13 审计：原稿"plugins:call 永久正确"是 E5.6 残影）
  - ✅ **执行注 2026-08-15：** plugin-manifest-loader 的 fileAssociations 解析在 #48 已一并落地（本任务验证即用）。registry-handlers.ts 加 fileAssociation:getPluginFor 直答（主进程实例）；PROXY_CHANNELS 摘除该通道（否则 IpcBridge 重复注册同通道抛异常——同 commit 摘除，注册顺序 registry-handlers 先于 IpcBridge 构造）；IpcBridgeHandler case + getPluginFor import 删；壳侧写入移除（loader fileAssociations 块 + registerFileAssociation import / lifecycle unregisterPluginFileAssociations——死写，rescan 覆盖）。preload-pool 通道名本就直连（非 plugins:call 包装），零代码改动只更新注释——三表迁移后壳侧 FileAssociationService 零消费者（grep 实证），池消费面（FoldersView:376/SearchView:139）零改动。门禁：tsc 0×2 / eslint 2+848 零新增 / vitest 393 基线 1 败 / grid ✓ / pool-css ✓
- [x] **E5.7#51** 删死文件——`electron/ipc/lang-def-handlers.ts` + `protocol-handlers.ts`
  - 连带：main.ts:20-21 import + main.ts:85-86 注册调用删除，grep 零残留（2026-08-13 审计）
  - ✅ **执行注 2026-08-15：** git rm 两文件——删除前提在 #46 矩阵实证（零调用 + 读主进程空实例 + protocol-handlers 头注释自封废弃 fallback），#49 已替换 main.ts 的 import/注册调用（两文件自此零 import），本任务纯删除。grep 全仓零残留（exit 1 无匹配）。矩阵里"死 handler"两行自此彻底消失。门禁：tsc 0×2 / eslint 2+848 零新增 / vitest 393 基线 1 败
- [x] **E5.7#52** ESLint 机械防线——`no-restricted-imports`：`electron/` 下禁 import 壳侧 Registry 模块（`import type` 除外）
  - ✅ **执行注 2026-08-15：** eslint.config.js 新增 electron/**/*.ts 块——`@typescript-eslint/no-restricted-imports` 两组 pattern：`**/src/core/registry/*`（整目录 16 模块全拦——比设计 §5 名单更完整，CardRegistry/StatusBarService/QuickPickService 同为渲染进程状态表，同属空实例陷阱）+ `**/src/core/services/FileAssociationService*` / `ViewContainerService*`；`allowTypeImports: true` 放行纯类型。白名单 override：plugin-manifest-loader.ts + registry-handlers.ts（唯一写入方/读取方对）。主块 CardRegistry 规则在 electron/ 下双规则同报（无替换吞并——两个规则名不同，同时生效）。语义 scratch 文件实测六项全过：两种深度（globstar 跨 `..` 段）+ 无 .js 后缀 + 白名单外模块放行 + import type 放行（硬约束 15 实证，不留猜）。门禁：tsc 0×2 / eslint 2+848 零新增 / vitest 393 基线 1 败 / grid ✓ / pool-css ✓
- [x] **E5.7#53** 全量回归验证——LangDef / Protocol / FileDecoration / FileAssociation / 壳侧自身功能（命令执行/侧栏/tabState）不受影响 / npm run check
  - ✅ **执行注 2026-08-15：** npm run check = 850（2+848 基线）。五提交面审计（git show --stat 73327d3f..44c4196e）：FileDecoration/tab/sidebar/命令文件零触碰——壳侧运行时只改 App.tsx（删 1 调用）/IpcBridgeHandler（删 6 case）/loader（删 2 注册块 + 3 重扫通知）/lifecycle（删 2 死写）。壳侧三表运行时 import 实证归零（仅测试 + 模块自身定义残存）。池消费链端到端核对：EditorView:114 lk.langDef.get ← langDef:get ← 主进程实例 ← python/editor plugin.json langDefs；ControlPanel:41/42/80 lk.protocol.* ← protocol:* 直连 ← ensureBuiltinProtocols 主进程实例（唯一消费者，读写闭环）；FoldersView:376/SearchView:139 lk.fileAssociation.getPluginFor ← 主进程实例 ← editor plugin.json fileAssociations。**回归发现并摘除一个口子：core/index.ts:73 桶 re-export registerBuiltinProtocols——零现存消费方但诱惑壳侧未来 import → 空实例回潮（写入方唯一性最后一道缝隙），摘除+注释。** 门禁：tsc 0×2 / eslint 2+848 零新增 / vitest 393 基线 1 败 / grid ✓ / pool-css ✓。✅ 用户验证 2026-08-16：无误（.py LSP / 协议下拉 / 双击路由全过）

---

# Phase 12：API 补全

> 📖 完整清单 → [API补全/API变更清单.md](API补全/API变更清单.md)
> 目标：消灭插件 `import @src/core`——每个命名空间补全对应几处 import。
>
> 🔴 **E5.7 结构性依赖：** 插件 React 模块现在**只在 Pool 渲染进程导入**（壳不再渲染任何插件）。插件模块顶层的副作用注册（`registerCommand` 32 处等）发生在 Pool 进程——但 CommandRegistry 在壳渲染进程。**插件注册必须全部走 `window.linkdesk.*` IPC，否则命令/菜单/快捷键在壳侧注册表里是空的。** 本 Phase 不只是清理 import——是极简Pool 的功能正确性前提。E5.6 时代的 `plugins:call` 代理是补丁，本 Phase 是根治。

- [x] **E5.7#54** `pool.*` 命名空间简化
  - 删 `poolId`/`zone` 参数（ready() 不再带 zone）
  - 🔴 保留 `sidebarAction` / `tabAction` 意图通道——GroupTabBar/MainRenderer/SidebarRenderer/WelcomePoolView ~15 处调用，tabs.* 覆盖不了 pinTab/拖拽/分屏（2026-08-13 审计：原稿"只留 onLayout + ready"会删掉活代码）
  - ✅ **执行注 2026-08-15：** 全仓摸底后精准摘除——poolId 全仓零活体（仅历史注释，E5.7#1 审计已证"从未实现"）；zone 残留链 3 处全摘：preload-pool.ts:61 `_poolZone` 死链（window-manager.ts:127/129 已无 ?zone= 路由，恒解析 ''）+ `ready()` 摘 zone 参数 + plugin-view-handlers.ts:34 主进程转发摘参数 + preload-shell.ts:345 `onReady(zone, cb)` 摘 zone 过滤（零调用方 grep 实证，保留 onReady(cb) 作池就绪监听 API 面）。poolLayout.ts:97 布局 zone 字段是布局区概念（sidebar/main/panel）非路由参数——保留。sidebarAction/tabAction 意图通道原样保留（preload-pool:606-608 + 壳 355-363 + handlers 57-70 三端一致）。dev mock 同形（makeLogger 无参）。门禁：tsc 0×2 / eslint 2+843 零新增 / vitest 392 基线 1 败 / grid ✓ / pool-css ✓
- [x] **E5.7#55** `linkdesk.workspace`——验证 6 方法已在位（E5.6#11.5a 已补 preload-pool.ts:250-263）+ 残余清理（2026-08-13 审计：原"消 7 处 import"数字过期——plugins/ 实测零 workspace import）
  - ✅ **执行注 2026-08-15：** 验证通过——preload-pool.ts:388-399 workspace 命名空间实有 **8 方法**（6 IPC：getFolders/getActive/setActive/openFolder/addFolder/removeFolder + 2 事件订阅 onDidChangeFolders/onDidChangeActive），壳侧 IpcBridgeHandler:152-178 六通道全在位，端到端闭合。审计口径修正：plugins/ 非零 workspace import——FoldersView.tsx:10 残留 1 处 **type import**（审计只算了值 import）。已摘：FoldersView 换本地 `WorkspaceFolderDto` 结构接口（形状与 WorkspaceService.ts:23 对齐 + 注释标注——preload "不 import src 构建边界"同款原则；#97 wire DTO 契约类型落地后换 ambient 全局类型）。插件侧 lk.workspace.* 调用 7 处（FoldersView:120/189/195/280/328/357 + updateTitle）零改动。门禁：tsc 0×2 / eslint 2+843 反降 1 / vitest 392 基线 1 败 / grid ✓ / pool-css ✓
- [x] **E5.7#56** `linkdesk.commands.registerCommand`——IPC 版已存在（preload-pool.ts:114）→ 剩余：marketplace/src/index.tsx:15 迁移 window.linkdesk.commands（2026-08-13 审计：实测全仓库插件运行时 import 仅此 1 文件 2 处）
  - ✅ **执行注 2026-08-15：** 摸底推翻审计前提——文件注释（68-71 行）声明"壳 loader 加载故不走 lk.commands"仍成立（initPluginLoader→loadPluginComponent 壳进程执行入口模块，App.tsx:227 实证），且壳 preload 无 commands 命名空间。迁移方案 = **双进程执行归一化**：①插件文件零 @src import（registerCommand + MenuId + getLoadedPluginManifests 三处全摘）——注册走 `lk.commands?.registerCommand?.(id, handler, meta)`（serial-monitor 同款可选链），菜单 slot 用字符串字面量"marketplaceItemGear"（E5.6#11.5h 先例）；②**壳 preload 补 commands 命名空间**——handler 存 _shellCommands 地图（contextBridge 页面世界代理，隔离世界可调用——preload-pool _poolCommands 同款机制）+ `_executeShellLocal` 执行桥 + 新通道 commands:registerShell（ipc-bridge PROXY_CHANNELS 回壳）→ CommandRegistry.registerShellLocalCommand 注册真实条目（handler = 壳桥，清 placeholder；与 registerPoolCommandMetadata 已有条目分支幂等汇合）——**壳启动注册保留 → 侧栏持久化恢复场景下齿轮命令不退化**（纯池侧注册方案会让该场景 10s 超时，摸底时识破）；③serial-monitor 零波及实证（注册在组件 effect 内池专属，壳从不挂载）；④连带修隐性 bug：count 改走 useMarketplacePlugins（原 getLoadedPluginManifests 在池进程解析到空 loader 实例恒 0——InstalledListView 同款数据源归一化）。**里程碑：插件侧 @src 值 import 全仓归零（grep 实证，含 @src/pluginLoader）——Phase 12 结构性目标达成。** 门禁：tsc 0×2 / eslint 2+842 反降 1 / vitest 392 基线 1 败 / grid ✓ / pool-css ✓
- [ ] **E5.7#57** ~~`linkdesk.fileAssociation`~~ 已并入 Phase 11 #50（getPluginFor 已存在 preload-pool.ts:462，"改直接 IPC"Phase 11 认领——2026-08-13 审计删除本任务）
- [x] **E5.7#58** `linkdesk.viewContainer`——no-op 桩换真 IPC 查询（现 preload-pool.ts:506-511 registerView 空函数 + getView 返回 null；改 getViews/getViewContainer 问壳侧注册表）
  - ✅ **执行注 2026-08-15：** 摸底发现桩下隐藏真消费方（2026-08-13 审计只 grep src 漏掉插件目录）：FoldersView.tsx:338-370（getView + 双 registerView——工作区名动态标题 / SEARCH 注册）+ SerialSettingsView.tsx:73（registerView 动态标题"收发设置 — 会话名"）——no-op 桩 = 动态标题静默失效（池侧栏标题从不更新，零报错）。contextBridge 克隆边界最小 Electron 实验实证：对象内嵌套函数过桥不抛错（代理）、React 元素 $$typeof Symbol 被消化——但 ipcRenderer.invoke 走真结构化克隆，render/actions/pinnedContent 函数字段必抛 DataCloneError → **写方向必须池侧白名单剥壳**。实现：preload-pool viewContainer 四方法真 IPC（getViewContainer/getViews/getView/registerView——问壳侧 ViewContainerService 注册表）+ 写方向白名单 toViewMetaDto（15 公开元数据字段）+ ipc-bridge 四代理通道（viewContainer:*）+ IpcBridgeHandler 四 case + 读方向 toViewDto（剥函数三字段 + _pluginId/_renderPath 内部标记）。元数据更新语义闭环：壳注册表更新 → onDidChangeActiveViews → usePoolSync layoutVersion bump → 重推布局 → 池侧栏动态标题实时刷新（marketplace:updateBadge 先例同款链路）。**连带修复（同任务）：** ①#19e 广播通道名 "view-container:changed"（连字符）与 marketplace 订阅 "viewContainer:changed"（驼峰）不符——自落地起订阅方零触发，改驼峰对齐；②#19e payload 只剥 render → 改走 toViewDto（actions/pinnedContent 未来含 React 节点时 events.emit IPC 克隆会抛）。API 契约补 §3.23 + §二表格行（此前 viewContainer 无文档面）。门禁：tsc 0×2 / eslint 2+841 反降 1（桩的 any 随替换消失）/ vitest 403 基线 1 败 / grid ✓ / pool-css ✓，**用户验证通过 2026-08-16**：控制台三查询全通——getViewContainer("explorer") 容器描述符 / getViews("explorer") 两视图 DTO（无 render/actions/pinnedContent 字段）/ getView("folders") DTO；getView("不存在") → undefined 全链通；**动态标题实证**——FoldersView 池侧 registerView 经桥更新壳注册表成功（title 变 "_testfiles" 工作区名）且侧栏 UI 实显（布局重推闭环）
- [x] **E5.7#59** `linkdesk.events`——验证 on/emit 已在位（event-system.ts:33-67，off = on 返回的 dispose）+ 残余清理（2026-08-13 审计：原"消 3 处 import"数字过期）
  - ✅ **执行注 2026-08-15：** 全链验证通过，零代码变更（纯验证任务）——event-system.ts createEventSystem 返回 { on, emit }，on 返回 dispose（= off 语义）✓；preload-pool.ts:803 暴露 events ✓；preload-shell.ts:308 spread 暴露 ✓；linkdesk-api.ts:167-171 文档面在列（on 返回 dispose 已定型）✓。插件侧 CoreEvents/Emitter 值 import 全仓归零（grep 命中全是注释——theme-sync/MiniEmitter/FoldersView 的历史注释）✓。残余清理 = 无。**连带发现记给 #62：** FileExcludeFilter.ts:9 + FileTreeModel.ts:11 的 FileEntry type import 走 services/FileService re-export（FileService.ts:23）——合法但路径陈旧（#45.5 已迁 types/fileEntry，正源路径应直指），#62 收；FileTreeModel.ts:12 FileDecoration type import 归 #60。
- [x] **E5.7#60** `linkdesk.decorations`（现网名——插件已调用 FileTreeDecoration.ts:28）——registerProvider 按需补；provider 与消费方同在 Pool（Phase 11 §3 已标）（2026-08-13 审计：API 文档名 fileDecoration 与现网不符，按现网名）
  - ✅ **执行注 2026-08-16：** 摸底钉死设计定性——Registry主进程化设计.md §3：FileDecorationRegistry 不迁（provider 是 JS 函数不可跨进程，真源与消费方同池，不进 IPC）；现链是 E5.6 残影（池 getDecoration → 壳恒空注册表代理 → 恒 null；onDidChange 经壳广播恒不触发）。实现：**注册表池内化**——preload-pool 模块级池内本地注册表（`_decoProviders` Map + `_decoListeners` Set，同渲染进程 contextBridge 函数代理存 provider——E5.7#58 实验实证可用）+ 四方法真实现：registerProvider（幂等覆盖 + 订阅 provider 变更事件 + 注册即全量刷新拾徽标——VS Code 同款）/ unregisterProvider / getDecoration（池内直答零 IPC，按注册顺序第一个非空，同步契约跳过 Promise——原壳注册表同款语义；provider 抛异常自愈剔除——插件卸载后代理目标销毁）/ onDidChange（本地 Set，返回退订）。**壳链整删（连带）：** ipc-bridge 摘 decorations:getDecoration 代理通道 / IpcBridgeHandler 删 case + decorations:changed 广播 + _decorationsUnsub 三处 / src/core/registry/FileDecorationRegistry.ts git rm（全仓零注册方，桶 index.ts 导出行随删）。**#59 连带收尾：** FileTreeModel.ts 的 FileDecoration @src type import 换本地接口（对齐 §3.24）；FoldersView/FileTreeDecoration 注释订正（引用已删模块）；壳目录规范 registry 例表摘名。API 契约补 §3.24 + §二表格行；API变更清单行更新；矩阵定性更新（行 3/30/66——恒空代理 → 已删池内化）。mockLinkdesk 不补（dev 预览无消费方，viewContainer 同款缺席先例；插件侧 `lk.decorations?.` 可选链守卫零影响）。门禁：tsc 0×2 / eslint 2+840 反降 1（壳注册表携带的 1 条警告随文件整删消失，零新增）/ vitest 403 基线 1 败 / grid ✓ / pool-css ✓。**用户验证曝刷新链 bug（2026-08-16，已修）**：注册后已加载节点徽标不出现——消费侧 refresh 从未被真实触发过（旧壳广播链恒静默，现网首曝）。根因三处：①FileTreeDecoration.attach 回调只清缓存（_invalidateRoots）+ fire——已加载节点不再走 getChildren（装饰器只在新节点创建时调）→ 徽标永不重查（用户"每点一个文件才出一个 T"= reveal→getChildren 新节点路径的偶然触发）；②SidebarZone keep-alive（display:none）不重挂载视图——侧栏切走切回无济于事；③FileTreeNode React.memo 浅比较全等跳过——item 引用未变，装饰变更对 memo 不可见（发现前修复不完整）。修复：attach 回调改全量重查已加载节点（roots 递归 children!==null，colors/badges 配置每批只读一次，null 清空——注销即消失无需折叠再展开，折叠子树也在内）+ decoration 单独走 prop（每次重查新引用破 memo，有装饰行重渲染，无装饰行保持跳过）。门禁：tsc 0×2 / eslint 2+840 零新增 / vitest 403 基线 1 败 / grid ✓ / pool-css ✓。**用户验证通过 2026-08-16**（注册即全图 T / 折叠展开保持 / 注销即消失 / 侧栏切走切回保持——VS Code 同款语义全部达标）
- [x] **E5.7#61** `linkdesk.protocol`——验证 listProtocols/getActiveProtocolId/setActiveProtocolId 已在位（preload-pool.ts:535，Phase 11 #49 改直接 IPC）；registerParser/getParsers/getDefaultFor 按需——现网零插件 import（2026-08-13 审计）
  - ✅ **执行注 2026-08-15：** 纯验证零代码变更——三方法在位（preload-pool.ts:762-773 实际位置，任务注 535 行号已漂移）：listProtocols/getActiveProtocolId/setActiveProtocolId 直连 protocol:* 三通道（#49 迁移产物，#53 已端到端核对 ControlPanel:41/42/80 读写闭环）。插件侧 ProtocolRegistry 值 import 全仓归零（grep 命中仅 ControlPanel.ts:17 历史注释）✓。registerParser/getParsers/getDefaultFor 按需跳过（零插件消费）。**连带发现记 #97：** linkdesk-api.ts 文档面 15 命名空间缺 protocol（及 workspace/langDef/fileAssociation/encoding/search/keybindings/viewContainer/decorations/filesystem/quickPick）——但 global.d.ts 的 window.linkdesk ambient 类型是 `Record<string, any>`（linkdesk-api.ts 是独立文档模块，未接线）——文档面补全 + 类型接线整体归 #97 契约类型任务，本任务不抢先。
- [x] **E5.7#62** 零散命名空间验证——现网 6 个已在位（keybindings:325 / clipboard:243 / langDef:515 / encoding:496 / search:469 / contextKey:386），sidebar/data 现网无此命名空间（按需删条目）（2026-08-13 审计：原"消 8 处 import"数字过期）
  - ✅ **执行注 2026-08-15：** ①六命名空间实体核对在位（行号漂移：clipboard:382-386 / keybindings:464-472 / contextKey:537-543 / search:698-711 / encoding:725-732 / langDef:745-748）——全部真 IPC 实现非桩（clipboard:* 三通道 / plugins:call 八方法代理 / contextKey 本地 store+IPC 双写 / search:searchFiles 带 signal 本地消费注释 / encoding:* 三通道 / langDef:get 直连——#49 产物）✓。②sidebar/data 现网无（preload-pool grep 全命中均为回调参数名）——按需删条目 = 零删除：E5.7 清单/映射文档/API变更清单三处均无过期条目（E5.5/E5.6 冻结文档不动）；API变更清单表格行更新为"6/6 验证在位 + 按需跳过"✓。③插件侧 6 命名空间 value import 归零（SearchView.tsx:10 FileSearchResult/SearchMatch 是 type import A 类；FileTreeClipboard.test.ts:8 ContextKeyService 是 B 类文档化例外）✓。④**#59 连带收尾：** FileEntry type import 旧路径 4 处（services/FileExcludeFilter.ts:9 + services/FileTreeModel.ts:11 + 2 测试）从 services/FileService re-export 直指 types/fileEntry 正源（#45.5 迁入处）；FileService.ts:23 re-export 摘除——内部使用保留（41/85 行签名），桶（@src/core barrel）消费方 grep 零实证，re-export 保留会诱插件走服务路径（#53 桶陷阱同款）。门禁：tsc 0×2 / eslint 2+843 零新增 / vitest 392 基线 1 败 / grid ✓ / pool-css ✓
- [x] **E5.7#63** `linkdesk.quickPick` 命名空间——`show(opts)` → `Promise<selected>`（现网 preload-pool 无此命名空间，真新建）
  - 🔴 机制（2026-08-13 审计）：E5.7 壳无 UI——picker 必须池内渲染（FloatingLayerHost 的 QuickPickHost）；preload.show() ↔ 池内 UI 桥接走 contextBridge 函数代理（池 main 世界注册 hostFn → 隔离世界存储调用）
  - ✅ **执行注 2026-08-15：** 池内本地桥落地（清单机制照做——零 IPC 零新通道，比壳往返方案快一轮且 #63.6 方向一致）——preload-pool `quickPick.show(opts)`（暴露函数执行于隔离世界）→ 存储的 hostFn(req, resolve) 经 contextBridge 函数参数代理跨世界（resolve 以代理函数进 main 世界，Promise 全程隔离世界）；QuickPickHost 双数据源 `pluginReq ?? 壳 data`，仲裁：last-wins（新 show 顶旧 resolve undefined）+ 壳优先（壳推 open:true 顶插件；插件期间壳推 open:false 只清壳数据不跑退场动画——动画会剥掉插件面板的 .show class）；key = 原数组 index → 选中 resolve **原对象**（身份保持零序列化往返）；show 先于 QuickPickHost mount → 缓冲，registerHost 回放（硬约束 20 同款模式）。**连带更名：** 池渲染桥 `quickPick` → `quickPickHost`（E5.7#15 遗留名），插件 API `quickPick` 真新建——dialog/dialogHost 同款归一；preload-pool / mockLinkdesk / QuickPickHost 三处同语义。v1 面：items(label/description→category/detail) + placeholder/prefix（无 buttons 无 itemAction）；插件标签不 t()（插件自带数据壳不翻译）；壳侧执行半程调 show no-op（API 契约 §3.22 书面注明）。en.json 补 1 key。测试 +10（QuickPickHost.test.tsx：pluginItemToDto 映射 2 / Enter resolve 原对象身份 / Escape undefined / last-wins / 壳顶插件 / 壳退场不波及插件 / 插件回落壳数据 / 壳模式回归 2）。门禁：tsc 0×2 / eslint 零新增（preload-pool 38=38 stash 基线实证）/ vitest 402 基线 1 败 / grid ✓ / pool-css ✓，待用户验证
  - 🔧 **验收补丁 2026-08-15（用户实测三 Bug）：** ①**last-wins 漏仲裁**——一次粘贴连续两个 show 是同一同步块，React 批处理两次调用间无渲染，pluginReqRef 渲染镜像滞后 → 旧请求漏 settle 永久 pending（vitest act() 冲刷掩盖）。修复：registerHost 内 ref 同步写最新请求。②**身份保持不可行**——contextBridge 每跳结构化克隆（实证 `x === items[0]` false），"resolve 原对象"设计推理错误。修复：结算契约改 key 回传——池侧 settle(key: string | null)，preload（Promise 所在地）映射 opts.items[Number(key)]（null → undefined）；API 契约 §3.22 改结构化副本声明（VS Code IPC 同款）。③**窗口失焦时 show 键盘动作全落空**——50ms 焦点定时器在失焦窗口拿不到真实焦点（DevTools 外挂窗口粘贴实测：Enter/Escape 无效，此前"Escape 成功"实为 blur 关闭代答）。修复：window focus 事件重聚焦输入框（插件定时器失焦 show 的真实边角同补）。测试 +2（同步批无渲染回归 / 窗口重聚焦），Enter 断言改 toEqual 克隆契约，总 12 段。门禁见补丁提交（4c29a02d），**用户复验通过 2026-08-15**：点击→副本契约 JSON 相等 true / last-wins 旧应 undefined 4/4（粘贴即出，Bug ①修复实证）/ Enter 新结果+选中菠萝 / Escape undefined——两处 `[object Object]` 为用户流程失误（忘 alt+tab，点击落点选中条目），非代码问题；Enter 偶发落空为三窗口 alt+tab 轮转落点不定所致（停靠 DevTools 后确定性通过）
- [x] **E5.7#63.5** 🔴 `filesystem` 路径守卫——主进程 file-handlers 零路径校验（插件可 `filesystem:remove('C:\\任意路径')`）。normalizePath 归一化 + 危险目录写拒绝 + workspace 外写操作用户确认 dialog + 越界尝试日志。📖 设计 → [01-极简Pool设计.md §8.1](01-极简Pool设计.md#81-filesystem-路径守卫沙箱唯一故意开口)
  - 🔴 preload-pool.ts:217 注释"主进程校验路径"与事实相反（2026-08-13 审计实测：file-handlers.ts 14 个 handler 零校验）——执行时删除该注释或改为真实描述
  - ✅ **执行注 2026-08-15：** 新文件 electron/services/filesystem-guard.ts（file-service 同级）——`guardPoolWrite` 四步：①归一化 `normalizePath(path.resolve(raw))`（../ 穿越消除 + 分隔符/盘符统一，复用 src/core/utils/pathUtils——electron 首例 utils import，非 #52 规则范围）②危险目录拒绝——任意盘根精确匹配（`/^[A-Z]:\/$/`）+ C:/Windows / Program Files (x86) 前缀匹配，POSIX 列表（/ 仅精确）③workspace 内判定——读 userData/workspace-folders.json（壳 WorkspaceService._persistFolders 每次 add/remove 写入，文件即真相源零同步开销；读失败按界外处理安全方向）④workspace 外确认 dialog——主进程原生 showMessageBox（main.ts:295 心跳弹窗同款先例，主进程无 i18n 文案硬编码，未来主进程 i18n 化随迁），"允许"记会话级放行目录 Set（防连发保存弹窗轰炸；main 进程级跨壳重建保留），"拒绝"抛错回传插件。**守卫只对池 sender 生效**——isPoolSender 经 getAllPoolViews 比对（壳渲染进程受信直通：自己的 StorageService 写 settings/workspace 不能被 dialog 打断——设计 Layer 3 "检查调用者 webContents"落地）；windowManager 缺失按不受信处理（安全方向）。5 写通道接线：writeTextFile/writeBinaryFile/createDir(isDirTarget 放行目录=目标本身)/copy(只查 dest 写侧，src 读侧放行)/remove；读通道全放行（设计原文）。越界 console.error 三处（危险拒绝/越界请求/用户拒绝）。preload-pool.ts:356 注释订正（原"受限——主进程校验路径"与事实相反 → 现在落地为真实描述）。**连带发现：** preload-pool filesystem.stat 暴露但主进程零注册（invoke 恒 reject）——非本任务范围。门禁：tsc 0×2 / eslint 零新增（新文件 0 警告——`process.platform !== 'win32'` 裸字面量触发 pluginId 硬编码误报，按规则提示改 PLATFORM_WIN32 大写常量规避）/ vitest 392 基线 1 败 / grid ✓ / pool-css ✓，**UI 验证通过 2026-08-15**：工作区外写弹确认→允许写入 ✅ + 同目录二次写零弹窗（会话放行记忆）✅ + 拒绝路径报「路径守卫拒绝」文件不落盘 ✅ + 危险目录 C:/Windows 零确认直拒 ✅ + 工作区内文件树操作/编辑器保存零弹窗 ✅ + 壳设置写入零弹窗 ✅
- [x] **E5.7#63.6** IPC 通道名集中化——新建 `electron/ipc/channels.ts`，main + preload 双端 import
  - 2026-08-13 审计：`filesystem:readTextFile` / `config:get` / `plugins:call` 等 50+ 通道名在 `ipcMain.handle(...)` 与 `ipcRenderer.invoke(...)` 各写一遍裸字符串，无联动——改一端另一端静默失效
  - 时机：Phase 12 全部命名空间补全/重注册后（#54–#63.5）——通道名收敛为常量，此后改名只改一处
  - 验证：grep 确认 `ipcMain.handle` / `ipcRenderer.(invoke|send|on)` 参数不再含裸字符串（除 channels.ts 定义处）
  - ✅ **执行 2026-08-16：** channels.ts 落地——126 通道常量按命名空间分组嵌套（`IPC.filesystem.readTextFile`）+ `filesystemChanged(watcherId)` 工厂（动态通道）。18 文件全量替换：main 侧（main.ts / window-manager / ipc-bridge PROXY_CHANNELS 36 通道数组全转常量 / keyboard-router / crash-recovery / plugin-manifest-loader / event-system / 9 个 ipc handlers）+ preload 双侧；theme:changed extraHandlers 对象键 → 计算属性 `[IPC.theme.changed]:`（裸替换会造 `{ IPC.x.y: fn }` 语法错误）。**范围外保留（DATA 非通道参数）：** plugin:push payload 数据名（lang:changed / plugin:installed / settings:requestGroup 等 events.on 数据名——经 broadcast 包装分发，与 IPC 通道是两层，不混入通道表）。**连带发现 ×2（死 IPC 不进真相源）：** ① preload-pool `filesystem:stat` 暴露但主进程零 handler（invoke 恒 reject）——src/plugins 零消费者实证后摘除；② `preload-pool-ready` 发送零监听者——死发送摘除。动态通道 4 站（file-handlers 2 广播 + preload 双侧）转工厂。门禁：tsc 0×2 / eslint 2+837（基线 840 −3）/ vitest 404 基线 1 败 / grid ✓ / pool-css ✓ / 验收 grep 全库零裸字符串（仅剩 `'plugins:*'` `'serial:*'` 两条通配注释）
- [x] **E5.7#63.7** PanelZone 数据生产者三件套（2026-08-13 审计——Zone分解设计 §2.6 承诺"归 Phase 12"无人认领，本任务补上）
  - ① 壳侧 bottom-panel 贡献路由——ViewContainerService `location:"panel"` 查询 → `panel.views[]` 入 pushLayout
  - ② PanelZone 动态组件加载——PluginComponent 同款，按 pluginId + render 动态 import（禁止静态 PANEL_VIEWS 表——插件独立铁律 + 硬约束 10）
  - ③ 面板高度持久化（workspace.json）
  - 验证：第三方插件 plugin.json 声明 `location:"panel"` 视图 → 面板出现其标签，池代码零改动
  - 执行 2026-08-16：三件套全落地。① buildPanelViewMetas（usePoolSync）——getViewContainers("panel") 容器按 order 排序 + getActiveViews 视图按 order 排序两级展平 → PanelViewMeta[]（含 renderPath，与侧栏同源 loader._renderPath）；views 非空才推 panel 字段（无面板贡献时 layout.panel 缺省 = 池条件渲染永假，零 DOM——与骨架期行为一致）。② PanelZone 内容区空 div → PluginComponent（renderPath O(1) 查找，自带 ErrorBoundary+Suspense；keep-alive 平级渲染 display 切换，isActive = id === activeViewId）；[+] 按钮 tooltip = panel.createTooltip（壳 t("新建面板视图") 推送——显示文本铁律）。③ 高度真相源 = LayoutEngine 新 panel zone（bottom dock：默认 220，钳制 120-600，order 1 在 statusbar 之上——_recalculate 多 bottom zone 堆叠循环改写：从窗口底边向上逐层，修了原"同 y 重叠"隐患）+ resizeZoneHeight（拖拽 + 恢复同路钳制）；App.tsx panel:viewSelected/panel:resize 事件桥（panel:createView 归 Phase 12 面板创建，保持 no-op）；panelActiveViewId App state + LayoutService panel {height, activeViewId} 持久化（恢复经 resizeZoneHeight 防坏值越界 + 100ms 防抖保存与上次落盘比对去抖 + beforeunload 读活值防冲掉——syncWriteLayout 整体替换缓存的坑）。**注：清单原文"workspace.json"实际落盘 layout.json（LayoutService "layout" key——与标签页布局同文件），文件名以实况为准。** 门禁：tsc 0×2 / eslint 2+840 零新增（3 条新增已灭：两处 typeof x === "number" 命中自定义规则 Literal 拦截 → Number.isFinite 单守卫；面板恢复 effect 多余 eslint-disable 删）/ vitest 404 基线 1 败（+1 = resizeZoneHeight 新测试；LayoutEngine 测试 9→10 段，默认布局测试重订 5 zone 坐标：panel y356 h220 + statusbar y576——堆叠不变量锁定）/ grid ✓ / pool-css ✓。**✅ 实机验证通过 2026-08-16**：panel-demo 演示插件落地（commit 59646010——首个 location:"panel" 容器：输出 + 待办两视图）→ 用户实测验收全过（面板出现 / 标签切换 / 待办 keep-alive / 拖拽 / 重启恢复）。池代码零改动即消费第三方面板贡献——插件独立铁律实证
  - ✅ **重装契约修复 2026-08-16（用户验收发现 + 契约定案）：** 用户实测「卸载→重启→重装」流程：serial-monitor 侧栏立即出现，panel-demo 底部面板不出现、需再重启一次。protocol-debug.log 钉死根因——每次重装打两行 `❌ view 未找到匹配模块 ... render="/@fs/..."`。链条：panel-demo 无 entry（纯 views 贡献，lang-defaults 同款合法形态）→ loadPlugin 运行时分支 pluginRoot 赋值绑死在 `if (manifest.entry)` → 重装瞬间 glob 查不到（卸载移目录 + Vite 重扫异步）→ 兜底三段式第三段 `else if (pluginRoot)` 不可达 → 两 view 静默注册失败 → buildPanelViewMetas 空 → 不推 panel 字段。serial-monitor 好 = 有 entry 走同兜底成功；重启后好 = glob 重展开命中。**契约定案（用户拍板）：「可卸载 ⇒ 可重装」是插件性定义本身（万物皆插件——不可卸载的不是插件）——与 glob 内外/entry 有无/core 标志全无关（core:true 只是默认值，翻 core:false 即入可卸载集）。** 修复归一化式：抽 `resolveRuntimePluginRoot(pluginId)` 唯一入口（dev 拼 /@fs/{abs} · prod 拼 linkdesk://{id} 不查盘；失败 reject，各调用点保留自己的错误语义 toast/warn+continue/静默跳过）+ pluginRoot 赋值移出 entry 分支（isRuntime 即赋，失败降级 undefined 诚实走 ❌）+ 原 5 处拼装收敛 1 函数（entry 根/entryUrl/statusBar 根/views 兜底/themes fetch——3 调用点全换）。重装路径自此与插件形状无关。门禁：tsc 0×2 / eslint 2+837 零新增 / vitest 404 基线 1 败 / grid ✓ / pool-css ✓，**✅ 用户实测通过 2026-08-16**（同款流程复验：panel-demo 重装面板立即出现零重启；serial-monitor 回归无异常）
  - ✅ **卸载/重装对称审计 + i18n 残留修复 2026-08-16（用户拍板执行）：** 两侧对账 13 项全对称（视图容器/标签页组件/命令含池运行时条目/菜单顶栏/快捷键/配置/主题引擎+注册表/图标语言注册表 RegistryBase 自动注销/状态栏/日志频道/图标排序/标签页关闭/主进程三表 notifyManifestChanged 重扫）——契约执行链完整闭环，entryless 洞（上文修复）是唯一断点。2 处「不对称」定案有意设计：插件设置数据保留（重装恢复设置，对标 VS Code）+ 元数据缓存保留（卸载后 marketplace 详情仍可浏览）。**唯一真不对称：** registerLanguageBundle 写两份命名空间（translation + pluginId），H6 清理只删 pluginId → translation 死键永久残留（卸载后 t(key) 仍显旧译文 + 语言切换广播把死键推给池）。修复：新模块 `src/pluginLoader/i18nResources.ts`（loader 内嵌版拆出——lifecycle 静态 import 零循环，单测独立覆盖）——逐插件留存原始 data，卸载时 translation 命名空间按剩余插件**整份重建**（逐键 removeResource 会误删他插件同顶层键子键——碰撞正确性，Map 插入序=注册序保持）；lifecycle 消费端 2b H6 循环换直接调用；loader 摘内嵌实现改 import。已知边界记入代码注释：add/removeResourceBundle 不触发 react-i18next 重渲染——当前语言已渲染界面旧译文保持到下次语言切换（资源层即时干净）。测试 +4（i18nResources.test.ts：双命名空间写入/碰撞重建/注册序保持/未知插件+重装重入）。门禁：tsc 0×2 / eslint 2+837 零新增 / vitest 408 基线 1 败 / grid ✓ / pool-css ✓，待用户实测（语言插件卸载→重装）
- [x] **E5.7#63.8** 命令 handler 签名全仓归一化——token 从 handler 合同删除（2026-08-15 用户拍板立项）
  - 背景：#56 齿轮菜单静默 no-op 根因 = 两套 handler 调用约定并存——壳直注册路径 CommandRegistry 传 token 进 handler（`(_token, ...args)` 样板 21 处）vs 跨进程路径 infra 先剥 token（`(...args)` 样板）。grep 实证：21 处签名、**零处真实消费 token**（装饰性参数）——删掉零功能损失
  - 改法：`executeCommand(id, token?, ...args)` 外层签名保留（未来取消语义入口），进 handler 前统一剥 token；`Command` 类型 handler 签名 `(token?, ...args)` → `(...args)`；21 处壳侧 handler 签名全改
  - 效果：全仓唯一 handler 样板 `(...args)`（VS Code 同款约定）——第三方作者按直觉写永不踩静默 no-op；"剥 token"概念全仓只有两处对应实现（壳 CommandRegistry / 池 preload executeCommand）且语义一致
  - 验证：grep 全仓 `(_token` 归零 + 命令面板/齿轮菜单/快捷键执行回归
  - ✅ **执行注 2026-08-15：** 21 处签名全改——CommandRegistry 4 处（头注释决策句 / `Command.handler` 类型 / registerShellLocalCommand 桥 / executeCommand 剥 token 点）+ coreCommands 11 + quickPickCommand 1 + settingsCommands 2 + shellMenus 3 + tabCommands 2 + App.tsx 1。剥 token 收敛为两处同语义实现（壳 CommandRegistry / 池 preload executeCommand）+ dev mock 同步（三处注释互指）。执行前实证：21 签名零 body 消费 token（`_token.` grep 零命中）——行为严格保持（任何 handler 读 args[0] 前后一致）。executeCommand 外层 token 槽位保留改 `_token` 占位（IpcBridgeHandler commands:execute 透传 undefined 依赖槽位；未来取消语义入口）。门禁：tsc 0×2 / eslint 63=63 零新增（9 文件 stash 基线对比实证）/ vitest 392 基线 1 败 / grid ✓ / pool-css ✓，待用户验证命令面板/齿轮菜单/快捷键回归

---

# Phase 13：硬编码消灭

> 📖 完整方案 → [硬编码消灭/硬编码清扫方案.md](硬编码消灭/硬编码清扫方案.md)

- [x] **E5.7#64** `MenuRegistry.ts`——MenuId enum → string + MENU_SLOTS 常量
  - ✅ **执行注 2026-08-16：** MenuId 闭合 enum → `type MenuId = string` + `MENU_SLOTS` 常量表（12 槽位、每槽 JSDoc 保留）——第三方作者可声明任意字符串新注册点，壳零改动消费（插件独立铁律）；壳内置槽位走常量表（拼写错 = tsc 报错，字符串数据面不变）。消费文件 8 个全收敛：coreCommands（type MenuId 保留）+ developerCommands + settingsCommands + shellMenus + HamburgerMenu + usePoolSync + RegistryLifecycle.test + SettingsView（本地 MENU_SETTING_GEAR 常量删除）。4 处 `as any` cast 摘除（loader.ts:423 / IpcBridgeHandler 两处 / SettingsView:424）。边界保持：池侧 menuId 字符串直传（Path B 字符串即桥契约）、插件侧字符串声明（不 import 壳模块）。门禁：tsc 0×2 / eslint 2+833（−4 = 摘掉的 4 条 no-explicit-any，零新增）/ vitest 408 基线 1 败 / grid ✓ / pool-css ✓
- [x] **E5.7#65** factoryRole 类型——闭合 union → string
  - ✅ **执行注 2026-08-16：** 唯一闭合点在 types.ts:117 `factoryRole?: "settings" | "marketplace"` → `string`（注释补记 #65 开放语义）。FactorySlots.ts 的 FactoryRole 与 schema factoryRole 本就是 string——第三方角色名自 #65 起全链路零受限（schema → TS → FactorySlots 查表）。
- [x] **E5.7#66** `plugin.schema.json` 枚举全量审计——4 处 enum→string / 9 处保留 / 1 处加值（2026-08-13 审计：实测 17 个 enum，doc 数字过期且 4+9+1=14 自相矛盾——执行时重数）
  - ✅ **执行注 2026-08-16：** 实测 17 处 enum 重分类——**5 开放**（type 废弃字段 / pluginRole / permissions / menus.propertyNames / configuration.uiHint）+ **1 加值**（iconSource + "lucide"，file-tree 插件现网在用而 schema 缺失——iconUtils.ts 注释实证）+ **11 保留**（iconBar top|bottom / distribution builtin|user / statusBar align / mode text|binary / iconLocation / viewRole / 配置 type / 配置 enum 字段 / 容器 location / view role / showActions——全部壳行为定义）。方案原稿 4+9+1 修正点：① menus propertyNames 原稿漏计——#64 已开 MenuId，schema 不删 enum = 新菜单注册点被拒（自相矛盾）② distribution 改判保留——壳政策字段（安装时 plugin-file-service 强制写 'user'，第三方选值无意义）③ permissions 按原稿开放（Phase 6 白名单首批准入，主进程解释）。docs/03-插件制造 副本自带"仅供参考、权威在 public/schemas"注记不跟进。JSON 校验通过，17→12 enum。
- [x] **E5.7#67** `tabIdentity.ts`——FALLBACK_META 清理
  - ✅ **执行注 2026-08-16：** FALLBACK_META 硬编码表整删（terminal/workspace/settings/marketplace/oled/editor/output 7 个插件 ID 字面量——硬约束 10 违例）。执行前实证：零插件声明 tabBehavior.identityField 的误判不成立——**editor 早已声明 filePath**（plugins/builtin/editor/plugin.json:13），terminal/workspace/oled 在本树已无对应插件（死条目），null 条目本就是 no-op。唯一生产行为依赖是 workspace 的 workspaceName——生产侧走 card-workspace 插件自声明（契约 = schema tabBehavior.identityField），测试侧改在 beforeEach 用 mock 注册表声明（registerViewPlugin + clearRegistry）。getMeta 第 3/4 步兜底统一归 null。门禁：tsc 0×2 / eslint 2+833 零新增 / vitest 408 基线 1 败（useTabManager 28/28 ✓）/ grid ✓ / pool-css ✓
- [x] **E5.7#68** `PoolZoneShell.tsx`——壳级硬编码 pluginId 抽常量
  - ✅ **执行注 2026-08-16：** 实证零残留——方案原稿前提已过期（"硬编码现网在 pool-main.tsx 随 #22 迁入"为 2026-08-13 快照，其后 E5.7 任务已消灭）。grep 全 src/pool：PoolZoneShell.tsx / pool-main.tsx 零 pluginId 字面量；eslint no-restricted-syntax 在 MainZone 的 11 处警告全是误报（`node.type === "leaf"` / `direction === "horizontal"` / `zone === "left"` 等通用字符串比较，非 pluginId）；剩余字面量两处均非 #68 范围——dev/sampleLayout.ts 夹具数据（fixture 本身就是硬编码样本）+ ShellViewRenderer.tsx 的 shellType 路由（"welcome"/"plugin-detail"/"output" 是壳视图类型字符串非 pluginId，归 #71）。无代码改动。
- [x] **E5.7#69** `plugin-file-service.ts`——主进程硬编码插件子目录 → 扫描所有子目录
  - 🔴 连带（2026-08-13 审计）：`electron/protocol.ts:69-71` plugin:// 协议解析同样写死 builtin/user 两分支——不同步开放 = 新子目录插件装上后加载不了
  - 连带：`src/core/pluginPaths.ts` PLUGIN_SUBDIRS 两端同步（dev 模式 Vite glob）；安装目标 `'user'`（plugin-file-service.ts:118/182）收敛为命名常量
  - ✅ **执行注 2026-08-16：** 新增 `scanPluginSubdirs()` 导出函数（plugin-file-service.ts）——readdir 全子目录、排除 `.` 开头（.disabled 坟场）、顺序 = SUBDIR_PRIORITY（builtin>user 同名冲突政策）> 其他字母序。消费方三处收敛：_findPluginDir（卸载/读 manifest/resolvePath 三用）/ listPluginDirs / **protocol.ts 协议解析同源复用**（原 builtin→user→根兜底三分支 → 扫描循环 + 根兜底保留，语义不变）。政策常量 SUBDIR_PRIORITY + INSTALL_SUBDIR 命名化（安装/重装目标/消毒写 distribution 三处）。pluginPaths.ts PLUGIN_SUBDIRS 实证零运行时消费（glob 工厂函数全仓无调用——glob 早已字面量化）→ 改注释定位为「静态打包范围，非运行期白名单」。node 仿真验证排序/排除/缺目录三场景 ✓。门禁：tsc 0×2 / eslint 2+832（−1 = 消灭的白名单字面量）/ vitest 408 基线 1 败 / grid ✓ / pool-css ✓，✅ 2026-08-16 用户实测通过（新子目录插件可见 + 装→卸→重装链路）
- [x] **E5.7#70** `IpcBridgeHandler.ts`——壳级硬编码参数化（2026-08-13 审计实测：一处 :247 `type || "editor"` 未知类型兜底；执行时全文件复核原稿"两处"）
  - ✅ **执行注 2026-08-16：** 全文件复核实证——唯一硬编码 = tabs:create 的 `type || "editor"`（原稿"两处"过期，grep 全插件 ID 字面量仅此一处）。抽壳政策常量 `DEFAULT_TAB_TYPE = "editor"` + JSDoc（硬约束 10 白名单例外：大写常量模式；为什么是编辑器——tabs:create 语义 = "打开点什么"，编辑器是唯一无参数可开的通用内容容器，对标 VS Code 文本编辑器 fallback）。行为零变化。门禁：tsc 0×2 / eslint 2+832 零新增 / vitest 408 基线 1 败 / grid ✓ / pool-css ✓
- [x] **E5.7#71** `MainZone.tsx`——SHELL_VIEWS 裸字符串 → 常量表
  - ✅ **执行注 2026-08-16：** 实证落点迁移——方案原稿路径过期（"SHELL_VIEWS 现网在 MainContent.tsx:46 随 #20 迁入 MainZone"）：grep 全仓 SHELL_VIEWS 零命中（旧表已随迁移整删），MainZone.tsx 实证零裸字符串（:463 只做 `tab.shellRendered` 分支路由到 ShellViewRenderer）。现网裸字符串在 `src/pool/views/ShellViewRenderer.tsx` switch（"welcome"/"plugin-detail"/"output" 三 case）。常量表落 ShellViewRenderer.tsx 模块级 `SHELL_VIEWS = { Welcome, PluginDetail, Output } as const`——池侧 Path B 不得 value-import @src/core，契约在池内本地声明（字符串即契约），JSDoc 标注壳侧同义集合 hooks/tabIdentity.ts SHELL_RENDERED_TYPES（两侧独立声明，新增壳视图需同步）。sampleLayout.ts 字面量保留（dev 夹具数据，夹具本身就是硬编码样本）。门禁：tsc 0×2 / eslint 2+832 零新增 / vitest 408 基线 1 败 / grid ✓ / pool-css ✓
- [x] **E5.7#72** `plugin.schema.json`——additionalProperties: false → true（2026-08-13 审计：执行时先确认根级是否存在——grep 到的 3 处全在嵌套层 113/129/147）
  - ✅ **执行注 2026-08-16：** 根级确认存在（allOf 之后、文件倒数第 2 行）→ true——第三方可在 plugin.json 顶层加自定义字段，schema 不拒。嵌套层 additionalProperties: false 全保留（壳定义的对象形状——statusBar 条目/themes/views 等，额外字段壳不读、拒掉提前暴露错误）。JSON 校验 ✓。
- [x] **E5.7#73** `dialog.openFile` 暴露
  - ✅ **执行注 2026-08-16：** 实证现状——主进程 handler（dialog-handlers.ts IPC.dialog.open，title/filters/directory，取消→null）与 preload 运行时 `dialog.open` 早已存在（preload-pool + preload-shell 双面），缺的是 E5.5#10q 规格的插件侧规范名 `openFile` + 类型声明（api/types.ts 无 dialog、global.d.ts 是 Record<string, any>）。补两处：preload-pool.ts dialog 命名空间加 `openFile(opts?)` 类型化签名（与 open 同通道，安全由主进程控制）+ linkdesk-api.ts 加 `DialogOpenOptions` 接口、`open` 从可选 any → 类型化必选（两 preload 恒暴露，实证安全）、加 `openFile` 类型声明。marketplace SearchView 现网消费走 `(window as any)` 零影响。E5.7#44 已删 preload-plugin.ts——插件 API 唯一载体是 preload-pool（任务前身 preload-plugin.ts 落点自动迁移）。门禁：tsc 0×2 / eslint 2+830（基线 832 −2 = open 去 any 化）/ vitest 408 基线 1 败 / grid ✓ / pool-css ✓，✅ 2026-08-16 用户实测通过（openFile 弹原生选择器，取消返回 null）
- [x] **E5.7#74** `ConfigurationRegistry.ts`——renderHint/uiHint 闭合 union → string
  - ✅ **执行注 2026-08-16：** 两处闭合 union 开为 string（`renderHint?: "color"|"action"` / `uiHint?: "fontFamily"|"fontSize"|"color"|"file"|"directory"`），JSDoc 注明已知值仅文档化、不构成类型白名单（插件独立铁律——第三方声明新 hint 不被壳 TS 拒绝）。实证降级链安全：SettingsView:549 uiHint switch `default: break` → 落 type 默认渲染；schema 侧 uiHint 枚举已在 #66 开放（TS/schema 两端一致）。SettingsView 本地 props 声明（:62-63）本就是 string——壳消费面零改动。门禁：tsc 0×2 / eslint 2+830 零新增 / vitest 408 基线 1 败 / grid ✓ / pool-css ✓
- [x] **E5.7#75** `ipc-bridge.ts`——PROXY_CHANNELS 注册门诊断日志
  - ✅ **执行注 2026-08-16：** 实证前提过期——原稿（E5.6#64a）"handleProxy 收到不在 PROXY_CHANNELS 的 invoke → 静默 undefined"：E5.7 架构已无 handleProxy 成员检查（按通道独立 ipcMain.handle，登记即门），grep 全 electron/ 零成员检查残留。两侧漏登记后果实证均非静默：① 池 invoke 未登记通道 → Electron "No handler registered" 拒绝（调用方 promise reject）；② 已登记但壳 IpcBridgeHandler switch 无 case → 壳 :357 `default: throw "未知的 bridge channel"`（plugins:call :584 同款）。E5.7 等价诊断 = 注册门重复自检：PROXY_CHANNELS 重复登记原只抛 Electron 原生 "second handler" 错（不可定位），入口加 Set 预检给出可定位错误（堵加新 API 方法的复制粘贴漂移——原稿担忧的场景）；门链两侧行为文档化在 registerProxyHandlers 注释。门禁：tsc 0×2 / eslint 2+830 零新增 / vitest 408 基线 1 败 / grid ✓ / pool-css ✓
- [x] **E5.7#76** `preload-pool.ts`——configuration 设置页专用方法注释标注
  - ✅ **执行注 2026-08-16：** 落点自动迁移（preload-plugin.ts 已随 E5.7#44 删除——插件 API 唯一载体 preload-pool.ts）。配置对象现网 13 方法实证分两组：通用 4（get/set/getSchema/onChange）+ 设置页专用 9（getConfigurationContributions/inspectConfiguration/getUserSettings/onDidChangeConfiguration/onPluginLifecycleChange/consumeSettingsGroup/onRequestSettingsGroup/consumeScrollToSetting/onRequestScrollToSetting——原稿"7 个"过期，E5.7 增 scrollToSetting 双通道等）。加 E5.7#76 分隔注释块（设置页专用用途枚举 + 通用插件请用 get/set/getSchema/onChange）+ 对象头注释索引句。getSchema 归通用组——api 类型层与 get/set/onChange 同段声明。门禁：tsc 0×2 / eslint 2+830 零新增 / vitest 408 基线 1 败 / grid ✓ / pool-css ✓
- [x] **E5.7#77** 全量 grep 确认——零 pluginId 硬编码
  - ✅ **执行注 2026-08-16：** 六组 grep 全过（方案 §7 原稿两条 + E5.6#66 四条，均按现网架构等价执行）：① `pluginId ===/==/!== "字面量"`（含 `.pluginId ===` 变体）src/electron/ → 仅 src/pluginLoader/__tests__/loader.test.ts 2 处测试夹具（"test-plugin"）；② `pluginId: "xxx"` 表/对象字面量 → 仅 __tests__ 夹具 + src/pool/dev/sampleLayout.ts dev 样例数据（夹具本身就是硬编码样本，零生产代码）；③ `"serial-monitor"|"editor"|"terminal"` 于 src/core + src/pluginLoader + electron/ → 零（命中全在测试/插件自身代码/注释——插件代码写自己 ID 是插件侧强制允许面）；④ `instanceId` 源码 → 零代码（仅 2 处注释描述已删链路）；⑤ `PLUGIN_ICON_PATH`/`BOTTOM_ICONS`/`switch(pluginId)` → 零（types.ts:148 仅注释提历史）；⑥ plugins/ 运行时 import @src/core（排除 import type/__tests__）→ 零。长期防线：eslint no-restricted-syntax 机械拦截（当前 2+830 全基线，Phase 13 期间零新增）。Phase 13 全 13 任务完结（#64-#77，10 个代码任务 + 2 个实证零残留 + 1 个纯验证），全程门禁绿。

---

# Phase 14：缩放联动 + ESLint

- [x] **E5.7#78** `IpcRelay<T>`——缓冲+回放模式归一化（2026-08-13 审计：归一化 3 处——preload-pool `_layoutBuffer` 1 / preload-shell `_bridgeRequestBuffer`+`_readyBuffer` 2；随 Phase 10 删除消失 3 处——preload-overlay `_commandBuffer` 随 #19/#44、preload-plugin 2 处随 #44 git rm——不归一化）
  - ✅ 2026-08-16 执行注：归一化实际 2 处——审计列的第 3 处 `_readyBuffer` 已随 E5.7#44（3f51ec1a，08-15，审计次日）per-tab 死面全删整消——`IpcRelay<T>` 落 electron/ipc-relay.ts（preload 共用工具，与 event-system.ts 同排布；push + onReady 回放全部 + unsubscribe 回缓冲态，30 行，E5.6#67a 原规格）。替换：preload-pool `_layoutBuffer` 三件套 → `_layoutRelay`；preload-shell `_bridgeRequestBuffer`+handler 双件套 → `_bridgeRequestRelay`（onRequest/onLayout 各收敛为一行 `relay.onReady(cb)`）
  - keep-last 缓冲不并入：审计后（#15/#16/#17，08-15）新建的 quickpick/toast/dialog 缓冲语义不同（单例快照——`length=0` 后 push，只留最后一份），IpcRelay 是 replay-all——两语义并入需 mode 开关，违背"30 行小工具"初衷，保留手写（ipc-relay.ts 头注释已注记）
  - 门禁：tsc 0×2 / eslint 2+828（基线 2+830，−2 零新增）/ vitest 408+1 基线败 / grid ✓ / pool-css ✓
- [x] **E5.7#79** 缩放联动——单 Pool 简化版
  - 🔴 2026-08-13 审计：zoom 现网零实现（唯一痕迹 event-system.ts:110 PUSH_CHANNELS 死通道名）——"简化"是相对 E5.6 纸面设计（映射表 3.5 全"适配搬"），实际从零建
  - ✅ 2026-08-16 用户拍板设计（讨论定案）：三壳级命令走快捷键注册表声明——`view.zoomIn`/`view.zoomOut`/`view.zoomReset` 默认键位 `ctrl+=`（= Ctrl+加号，Shift+= 同物理键）/`ctrl+-`/`ctrl+0`。用户可改绑、冲突检测可见、归一化（不埋组件 onKeyDown）——原"池侧 PoolZoneShell 直接处理"方案作废
  - 执行链：keyboard-router（主进程 before-input-event）拦截 → 壳命令 handler → 主进程 `webContents.setZoomFactor(池 WCV)`
  - 🔴 持久化决策点消解：执行链在壳侧 → 壳天然持有 zoom 值 → 落 `window.zoomLevel` 配置项（VS Code 同款），StorageService + 设置页全现成——localStorage / 意图通道两方案均不需要
  - Ctrl+wheel 不进键位（wheel 非键盘 chord）：编辑器独立缩放 = editor 插件 `mouseWheelZoom` 设置项（VS Code 的 editor.mouseWheelZoom 同款语义）——设置归设置页，不归快捷键面板
  - 执行坑预记：Monaco 默认把 ctrl+= 绑给 editor.action.fontZoomIn → 全局注册后需解绑，否则编辑器焦点下双 handler 打架（VS Code 同款处理）
  - ✅ 2026-08-16 执行注：执行链全落地——shellKeybindings CORE_KEYBINDINGS +4 条（ctrl+= / ctrl+shift+= 同物理键 / ctrl+- / ctrl+0，sync 主进程全自动）→ 壳命令（coreCommands，title 放大/缩小/重置缩放 category 视图，中英双语已入 lang-defaults：Zoom In / Zoom Out / Reset Zoom）→ 读写配置 `window.zoomLevel`（-8..8 默认 0）→ onApply 换算 factor=1.2^level → preload-shell `window.setZoom` → main.ts 应用 `getPoolView().setZoomFactor` + 缓存 `_lastZoomFactor`（壳崩重建 createMainPool 后重放，防闪 100%）。开机恢复：启动 applyAllConfigurations 自动跑 onApply，零额外接线
  - 物理键归一化：键位串 "+" 是分隔符无法表达 Shift+= 上档 → KeybindingRegistry + keyboard-router 两侧 `keyboardInputToKeyString` 同时归一化 "+"→"="（两侧必须同改——注释已注记）
  - Ctrl+wheel 现状核实：editor 插件 `mouseWheelZoom` 配置已存在（plugin.json:122 默认 true）且已接 Monaco 原生选项——半程现成零改动，设计定案 ratify
  - Monaco 解绑：EditorView 同 id addAction 替换（keybindings: []）摘除 fontZoomIn/Out/Reset——主进程对 autoRepeat 放行，长按 Ctrl+=/- 的重复帧会到编辑器，不摘除则双 handler 打架（VS Code workbench 同款手法）
  - no-restricted-syntax 误报：`typeof === "number"` 触发字符串比较启发式（selector 泛化）→ Number.isFinite 改写——Phase 14.5 规则收窄候补已注记
  - ✅ 用户实测通过（2026-08-16）：Ctrl+=/-/0 缩放生效 + 重启持久化 + 编辑器焦点下长按不打架 + 中英切换标题——全部正常
  - 门禁：tsc 0×2 / eslint 2+828 零新增 / vitest 408+1 基线败 / grid ✓ / pool-css ✓
- [x] **E5.7#80** ESLint 升级——`no-core-import-in-plugin` warn → error
  - 执行注（2026-08-16）：规则先行补两项豁免（eslint-local-rules.js noCoreImportInPlugin.create）再翻 error——
    - 测试文件豁免：filename 含 `__tests__`/`.test.`/`.spec.` → `return {}`（vitest 单进程与壳同 JS 堆，多 WebView 静默失效理由不成立；FileTreeClipboard.test.ts 运行时 import ContextKeyService 为已记录 B 类例外）
    - 纯类型豁免：`importKind === "type"` 或全部 specifier 内联 type → 跳过（类型擦除后零运行时耦合——FoldersView/FileTreeModel/marketplace 等 4 行纯类型 import 无需"修复"）
  - 现存违规枚举 7 处：3 处测试文件 + 4 处 `import type`——全部落入豁免，零行修复
  - 规则三态 stdin 实证：值 import → error 生效 / `import type` → 静默 / 测试文件 → 静默（import-x 解析报错为虚拟文件 artifact，与本规则无关）
  - eslint.config.js:173 warn→error（注释同步更新）
  - 门禁：tsc 0×2 / eslint 2+821（828→821 恰为豁免的 7 条消失，零新增）/ vitest 408+1 基线败 / grid ✓ / pool-css ✓

---

# Phase 14.5：静态卫生清零——tsc/ESLint/vitest + 门禁收口

> 📖 完整方案 → [静态卫生清零/静态卫生清零方案.md](静态卫生清零/静态卫生清零方案.md)
> 背景：2026-08-15 用户指示——E6 上架前整洁环境；审计对齐后"全做"（用户拍板）。
> 🔴 排序铁律：Phase 10-14 之后——先删死文件再修警告（#40 git rm ShellPluginComponent.tsx = error 2 所在文件，先修后删 = 白修）。审计快照数字执行时重跑（方案 §1）。

- [x] **E5.7#94** 重跑审计快照 + 3 errors 清零
  - 执行注（2026-08-16）：
    - 快照重跑（方案 §1 表已更新为双列）：tsc 0×2 / @ts-ignore 0 处 / eslint **0 errors** + 821 warnings / vitest 408+1 基线败 / grid ✓ / pool-css ✓
    - error 1 `monaco-init.ts:52`——假阳性清零：import 别名改名 `useWorkerFactory as configureWorkerFactory` + 局部适配器改名 `setupWorkerFactory`（适配器不能删——monaco-vscode-api 传 logger 参数，需适配成 workerLoaders 配置）+ 行 94 引用同步——零 disable 注释
    - error 2 `ShellPluginComponent.tsx:59`——文件已随 #40 整删（6eea96d5），本项只验证 ✓
    - error 3 `loader.ts:1274`——手写 `replace(/\\/g,'/')` → `normalizePath`（顺带盘符大写归一，硬约束 12 合规）
  - 门禁：tsc 0×2 / eslint 0 errors + 821 warnings（errors 历史首次清零）/ vitest 408+1 基线败 / grid ✓ / pool-css ✓
- [x] **E5.7#95** `no-restricted-syntax` 规则收窄 + 真违规清零 + 升 error
  - 执行注（2026-08-16）：
    - 实现：独立本地规则 `linkdesk/no-plugin-id-hardcode`（error）——同 #45 理由（flat config 同规则跨块合并高 severity 胜出、低 severity options 被丢弃）。no-restricted-syntax 保留 v3- 两 selector（warn）——升 error 的 E3i #68a TODO 待 #101 v3Api 清零后兑现，两防线共存
    - 代码式实现（非 selector 声明式）——可选链 `data?.pluginId` 是 ChainExpression 包裹（声明式需为它写 6 条变体），代码式统一解包；三种形态同拦：二元比较（名字含 plugin 的 Identifier/MemberExpression）+ `switch (pluginId) case` + `pluginIds.includes()` 盲点
    - 真违规枚举：grep 真值 = 规则命中 = **5 处**（零误报零漏报）——方案 §3.2 "20-40 处" 预估被 Phase 12/13 超前消化。5 处 = loader.test.ts ×2（测试夹具 → `TEST_PLUGIN_ID` 常量，7 处归一）+ serial-monitor ×3（插件自识 ID → `SERIAL_MONITOR_PLUGIN_ID` 共享常量 utils/pluginId.ts——插件写死自己的 ID 不违反铁律：铁律禁的是壳/core/pluginLoader 写死，插件自知两侧都在作者控制下）
    - 五态 stdin 实证：`t.pluginId === "editor"` → error / `label === "typescript"` → 静默（合法 tag 判别）/ `switch (p.pluginId) case` → error / `pluginIds.includes()` → error / `data?.pluginId !==` → error
    - 🔴 **#96 执行者注意：** 宽 selector 已摘除——剩余字符串比较不再有 eslint 枚举通道，改 grep 枚举（方案 §3.2 三类归口设计不变）
    - 门禁：tsc 0×2 / eslint 0 errors + 515 warnings（821→515 全来自宽 selector 摘除，零新增）/ vitest 408+1 基线败 / grid ✓ / pool-css ✓
- [x] **E5.7#96** 剩余字符串比较分类归口（方案 §3.2 三类，不做 294 个孤立 patch）
  - 执行注（2026-08-16）：
    - **类 1 跨堆协议值：** 全仓 grep 审计——layout/菜单/图标栏 wire 值已全在 poolLayout.ts union 定型（池侧零裸字面量比较，E5.7#1 遗产）。唯一未定型缝 = **池→壳 tabAction 协议**（载荷全链路 any）。新建 `src/core/types/ipc/tabActions.ts`（决策点 1 定死：不复活 shared/——#45.5 已删；electron rootDir ".." + 池 Path B allowTypeImports 三方 import type 全通）：PoolTabAction 12 变体 union + TabSplitDirection。壳（App.handleTabAction/usePoolSync）+ 池（MainZone/GroupTabBar）双端 import type 接线
    - **契约定型逼出 3 个真缝**（tsc 抓出）：① App createTab 的 `{ groupId } as any` 是死字段（CreateTabOptions 无 groupId）→ union 摘除 + 透传 workspaceName（欢迎页最近视图发的 workspaceName 原来被静默丢弃）；② MainZone moveTab 的 targetGroupId 可为 null 透传 → 提前空守卫；③ splitTab 的 "down"/"up"/"left"/"right" 六值分支是 E5.6 遗留（右键菜单现走壳命令 core.splitRight 不经此通道）→ 收窄为 horizontal/vertical 两值，死分支随 tsc 移除，zone 过滤 center/null
    - **类 2 closed union 判别（审计结论，保持字面量）：** 壳 src 132 处小写字面量比较逐一过目——typeof 检查 / `source === "user"`（union）/ `sendMode === "hex"`（union）/ `dialogType === "directory"`（union）/ `zone === "left"`（DropZone）/ `dragging.current === "sv"` / `activeField === "first"` 等组件态判别全部保持；`renderHint === "color"` 也是 **E5.7#74 有意开集**（插件独立铁律：第三方声明新 hint 不被壳 TS 类型拒绝，SettingsView 有降级回退）——原计划的 union 收窄取消，不修
    - **类 3 可疑处：** 除 tabAction 外零新增——ContextKeyService `ident === "true"/"false"/"in"` 是解析器输入判别（string 域合法）、DataConverter `encoding === "gbk"` 是编码名开放域检查，均保持
    - 门禁：tsc 0×2 / eslint 0 errors + 509 warnings（515→509 恰为契约定型消掉的 6 处参数 any）/ vitest 408+1 基线败 / grid ✓ / pool-css ✓
- [x] **E5.7#97** wire DTO 契约类型 + preload/API 边界 any 清零
  - 契约类型先行（一处定义三处消费）——preload 三兄弟 + linkdesk-api.ts 函数签名接上（方案 §3.1 分布：47/39/36/34 处）
  - 🔴 落点与 #45.5 冲突：契约三方消费（electron/壳/池）——推荐 shared/ 保留为纯类型/协议常量目录；或另立 src/core/types/ipc/（池侧 import type 不受 Path B 拦）。执行时与 #45.5 协调定死（方案 §7）
  - 无法定型的（Monaco 内部/动态模块加载）落 `unknown` + 守卫，不落 any
  - ✅ **执行注 2026-08-16：** 契约归口 + 双 preload 边界 any 清零 + global.d.ts 收窄兑现 E5#89（`Record<string, any>` → `LinkDeskAPI`——#61 连带的"文档面未接线"就此闭合）。**决策点 1：** 契约落点定死 `src/core/types/ipc/`（不复活 shared/——#45.5 已删；三方消费实证：electron rootDir ".." type import ✓ / 池 Path B allowTypeImports ✓ / 壳直 import ✓）。**契约文件 7 新**：keyboard/sidebarActions/serial/dialogs/events/bridge/poolActions + 归口 4 处双份定义（KeyboardInput 原 keyboard-router↔KeybindingRegistry / OpenPortConfig 原 serial-service↔preload / DialogOpenOptions 原 api↔dialog-handlers / PluginPushData 原 event-system 本地）。**linkdesk-api.ts 全量重写**——19 命名空间扩为 31（补 quickPick/quickPickHost/toast/dialogHost/fileAssociation/search/decorations/encoding/viewContainer/langDef/lsp/protocol——#61 记的 11 缺口全闭合）+ settings 9 法转 required（池注入真相源）+ plugins 读面 3 法标 `?` 壳独有 + pool 命名空间双端合一 + keyboardEventToKeyString 改纯数据形参（contextBridge 克隆丢 DOM 属性）。**保留 any 豁免（3+2 处，理由注明）**：commands execute/executeCommand/registerCommand 的 `...args: any[]`（VS Code registerCommand 同宽——`(line: string) => void` 可赋 any[] 不可赋 unknown[]，逆变铁证）+ configuration.get/pluginState.get 的 `<T = any>`（48 处无参调用方）。**global.d.ts 收窄 → 65 个 tsc 错误**全部作为真实消费方诚实收窄（非压制）：events.on 载荷 unknown → 订阅方逐通道 guard（App 4/usePoolSync 4/usePluginIpcEvent 边界 cast）；event-system on 泛型化 `<T>`（listenDirect 同款模式，解决 preload 双端 8 处逆变）；loader pluginsApi 一处守卫（壳 preload 全量面断言）；IpcBridgeHandler 闭包窄化失效 → 捕获 bridge 局部引用 + commands:execute 显式 token 槽位（CancellationToken 归位）；FilePathInput 死分支整删（dialog.open 契约 string|null 不再有 {path} 形状）；mockLinkdesk `satisfies Partial<LinkDeskAPI>` 边界断言（刻意残缺面 = 响亮失败设计，全文件零 any）。门禁：tsc 0×2 / eslint 0+406（**no-explicit-any 472→369，−103**，零新增）/ vitest 408 基线 1 败 / grid ✓ / pool-css ✓
- [x] **E5.7#98** 插件侧 + 壳 src 剩余 any 清零
  - 大头：FileTreeContextMenu 31 / FoldersView 23 / FileTree 17 / ExtensionItem 14 / loader.ts 15……（方案 §3.1 全表）
  - 原则同 #97：unknown 兜底；改插件时铁律照查（memory plugin-import-exceptions）
  - ✅ **执行注 2026-08-16：** 全仓 any 369→1（仅剩 v3Api.ts:63 一处 `__v3_core__` 按计划归 #101）。**决策点 4 兑现：plugins/user 全覆盖**——serial-monitor 等 user 树插件全部修完，零 eslint ignore 豁免（#102 门禁 --max-warnings 0 不允许豁免存在）。**原则落地：** unknown 兜底（catch 全部 errMsg idiom `e instanceof Error ? e.message : String(e)`；useIpcEvent/ShellEvents/QuickPickService 异质容器存 unknown 消费侧窄化）；注解可有可无处直接删（EditorBreadcrumb/SearchView 上下文已定型）；as any 断言全数消灭（eslint-disable 注解随行删除，不止压制）；契约缺口修正源不压制（FileExcludeFilter setter 补 null 签名——内部字段本就 FileExcludeFilter|null；InitDeps manifest 归口 PluginManifest——测试替身补 name/version；useTabManager tab:create wire 载荷窄化 CreateTabOptions）；死代码整删（revert.test 的 `(getConfigurationValue as any).mockClear?.()` 恒 no-op——mock 是普通箭头函数无 mockClear，?. 吞掉，行为零变化）。**monaco 0.55 实证归口**（editor 插件已全清零）：顶层 monaco.typescript 命名空间是 TS 语言服务正源（完整类型 + editor.main.js:167 运行时别名 languages.typescript 同对象——免 cast）；getTypeScriptWorker/DefinitionInfo 库签名丢精度处窄化 cast `as TsDef[] | undefined`。**异质/私有穿刺窄化**：ClipboardProviderRegistry 测试 protected unregisterAll → 两步 cast `as unknown as InternalRegistry`（单点，TS2352 免 via）；Electron.Item icon 本版 typings 必填而运行时可选 → 按需装配 + 调用点窄化；FileTreeDnD webkit File.path 私有字段 → `{ path?: string }` 窄化；queryLocalFonts/MonacoEnvironment 等 Chromium 专有全局 → 窄声明/declare global（monaco.d.ts 已定型直接删 cast）。**壳 pool 引用** useRef<any> 3 处 → `NonNullable<LinkDeskAPI["pool"]> | null`（GroupTabBar/MainZone/usePoolSync 统一）。门禁：tsc 0×2 / eslint 0+38（406→38——22 exhaustive-deps 归 #99 + 5 active-guard + 4 中文归 #100 + 6 no-restricted-syntax 基线 + 1 v3Api 归 #101，**零新增**）/ vitest 408 基线 1 败 / grid ✓ / pool-css ✓
- [x] **E5.7#99** exhaustive-deps 25 处逐个修（方案 §3.3 清单——含 plugins/user/serial-monitor 12 处，归属见方案 §7 决策点 4）
  - 🔴 禁止机械加 deps——可能引发死循环/闭包过期（React 老坑）；每处先判断"真实依赖还是误报"，真实依赖补上后验证不循环
  - ✅ **执行注 2026-08-16：** 22 处逐个修完（方案预估 25，eslint 全量盘点实测 22——以实测为准），**零机械加 deps**，四处分类各按判断落：
  - **① 真依赖补上（16）：** SerialContext 6× `[s]`（s=window.linkdesk?.serial 稳定对象——identity 不变零重跑）；EditorTab handleSave `t`；serial index 604/712 `t`（drain 重启/纯 formatter 重建无害）；statusBar `key`（fullKey 真依赖）；useSendData 壳池两份 `[ctxRef, callbacksRef]`（refs 稳定零重跑）；FileTree `rerender`（useCallback [] 稳定，同文件 134 行已有先例）；useDragReorder `onDropCopySplit`（211/213 真用）
  - **② 摘除多余 deps（4）：** App coreCallbacks 摘 `forceCloseTab, t` / handleTabAction 摘 `focusTab, splitTab`（eslint 判定 body 零引用）；index 445 摘 `timestampFormat`（CM6 appendLine 经 refs 读取）；MainZone 417 memo body 改直读 `groups`（270 行同 render 已同步 ref——render 期读 ref 的并发撕裂隐患顺带消除）
  - **③ ref 桥（2）：** index 805 p2p 用 `appendLineRef.current`（appendLine 入 deps = 每次设置变更重复注册 p2p.on——API 无 unsubscribe，死循环实锤）；EditorView 新建 `onChangeRef`（onChange 入 deps = 回调身份变化重建编辑器）
  - **④ justified disable（3，均注 E5.7#99 理由）：** App 292 mount-once 初始化管线（t 重跑 = 重注册配置 + 重复 initAll——硬约束 13 竞态面）；EditorTab 223 per-tab 一次性加载（t 入 deps = 语言切换重载文件 → 新建 model 丢 undo/光标）；EditorView 293 编辑器创建即定型（value/readOnly 仅初始创建读取 + options 走 optionsRef+updateOptions 同步——入 deps = options 身份变化重建编辑器丢 undo/光标）
  - **连带：** App forceCloseTab 死变量整删——摘 deps 后 tsc TS6133 逼出（零消费实证，eslint 判定正确性二次印证）
  - 门禁：tsc 0×2 / eslint 0+16（exhaustive-deps 22→0 清零；余 16 = #100 9 处 + #101 7 处存量，**零新增**）/ vitest 408 基线 1 败 / grid ✓ / pool-css ✓
- [x] **E5.7#100** active-guard 5 处 + 硬编码中文 4 处
  - `ContextMenu.tsx:140/166` + `OverlayPortal.tsx:36/49` + `serial-monitor SearchBar.tsx:25`（归属见方案 §7 决策点 4）——硬约束 14（#59c Bug 2）加 `if (!open) return` 守卫
  - 中文 4 处 t() 包裹（editor index.tsx:13 / DiffEditor.tsx:94 / ExtensionItem.tsx:96 / UninstalledListView.tsx:69）
  - ✅ **执行注 2026-08-16：** 两规则归零，零 disable、零机械套规则（读规则源码后逐处按语义落）：
  - **ContextMenu 2 处：** 活跃守卫 = `visible = menuReady`（入场动画完成前菜单 visibility:hidden 不可交互——两阶段渲染的现成真相源，menuPos/menuReady 声明提前）。统一失焦/键盘导航两 effect 加 `if (!visible) return` + visible 入 deps——行为差仅入场 1 帧无监听（隐藏态本就不可交互）
  - **OverlayPortal 3 处（含 trapFocus 硬约束 14 grep 原则同补）：** 加 `open?: boolean` prop 默认 true——防御性守卫（现 4 消费方全部条件挂载零行为差；未来"挂载但隐藏"消费方自动受保护）。mousedown/Escape/trapFocus 三 effect 同守卫 + open 入 deps
  - **SearchBar：** 单 effect 拆二——显示态挂导航/关闭监听（`if (!visible) return` 守卫，规则满足）；隐藏态只挂 Ctrl+F 打开分支（onOpen 不在规则回调名单——守卫语义即所在分支）。原单监听器按 visible 分支的行为逐键搬移零变化（Ctrl+F 焦点/选择、Escape、Enter 三条路径逐一核对）
  - **中文 4 处 t() 包裹**（key=中文原文，硬约束 2）：editor 空态"编辑器（双击文件打开）"（useTranslation 补入 index.tsx）/ DiffEditor"加载对比…" / ExtensionItem"管理" / UninstalledListView"安装"（三文件补 react-i18next import）
  - 门禁：tsc 0×2 / eslint 0+7（active-guard 5→0 + 中文 4→0 清零；余 7 全部归 #101 v3Api，零新增）/ vitest 408 基线 1 败 / grid ✓ / pool-css ✓
- [x] **E5.7#101** v3Api 消费方排查
  - grep v3Api 全仓消费方：零消费 → 归 Phase 10 式整删；有消费 → 改名（v3 = 旧应用名遗骨，6 处告警全在 v3Api.ts）
  - 删除后 v3- selector 降噪（#95 升 error 前置）
  - ✅ **执行注 2026-08-16：** 消费方 grep 定生死——**零活消费 → 整删**。**审计证据：** ①壳代码消费方仅 App.tsx（import + initV3Api() 调用，零读取方）；②运行时 `window.__v3_core__` 读取方全仓为零（src/plugins/electron 三棵树 grep 干净——池插件本就不可达壳 window 全局，#45.6 执行注已定性）；③构建侧 build-plugins.mjs 引用 __v3_core__ 但**不在构建链**（npm run build = tsc -b && vite build，插件走 vite 多入口 dist/plugins；build:plugins/dev:plugins 是旧运行时插件模型的独立脚本——E6#3 已接管改造方向"不依赖 __v3_core__"，本任务不碰）；④v3_ 存储 key 早已迁移（linted 代码 grep 零残留——覆盖审计文档的旧担心已过时）。**落地：** v3Api.ts git rm（6 v3 告警 + 全仓最后 1 处 any 随文件消灭——no-explicit-any 归零 🎉）+ App.tsx 摘 import/调用（留注释说明删除理由）+ AppInitializer 头注释同步。**E3i #68a 兑现：** v3 两 selector warn→error（#95 注记的前置条件满足）。**里程碑：eslint 全仓 0 errors 0 warnings——历史首次全零。** 门禁：tsc 0×2 / eslint 0+0 / vitest 408 基线 1 败 / grid ✓ / pool-css ✓
- [x] **E5.7#102** vitest 断言修复 + 门禁收口
  - `DataConverter.test.ts:59` toEqual 对 Uint8Array 比引用不比内容（方案 §3.5 根因）→ `expect(Array.from(bytes)).toEqual([0x41])`
  - 🔴 门禁升级（方案 §6）：npm run check = tsc 0 + `eslint --max-warnings 0` + vitest 全绿 + grid + pool-css；CLAUDE.md 提交自检同步（去掉"基线接受"口径）
  - 自证：故意插一条 warning 验证门禁红，再移除（机械防线自证）
  - ✅ **执行注 2026-08-16：** **①断言修复**——`expect(Array.from(bytes)).toEqual([0x41])`（FAIL 输出钉死根因："expected Uint8Array[ 65 ] to deeply equal Uint8Array[ 65 ]"——jest toEqual 对 TypedArray 比引用不比内容，方案 §3.5 预估实锤）。**②门禁收口**——package.json check 改写：`tsc --noEmit && tsc -p electron/tsconfig.json --noEmit && grid && pool-css && eslint src/ plugins/ electron/ --max-warnings 0 && vitest run`（双工程 tsc 0 + eslint 零警告才过——"基线接受"口径彻底清除，红灯必须修到绿灯）；CLAUDE.md 提交自检 #1 + 开发命令注释同步改写（"npm run check 全绿——双工程 tsc 零错误 + ESLint --max-warnings 0 + vitest 全绿 + 网格/pool-css。无基线接受"）。**③自证通过**——UninstalledListView 临时插裸中文 JSX（no-hardcoded-chinese warn 级）→ `npm run check` 红：`✖ 1 problem (0 errors, 1 warning)` + `ESLint found too many warnings (maximum: 0)`，&& 链在 eslint 处断（vitest 未执行）→ 移除探针 → 复绿。**里程碑双 🎉：eslint 全仓 0+0 + vitest 409/409 历史首次全绿（1 基线败点随 #102 消灭）。** 门禁：tsc 0×2 / eslint 0+0 --max-warnings 0 / vitest 409/409 / grid ✓ / pool-css ✓

---

# Phase 15：E6 前置——插件分发地基（IPC 骨架）

- [x] **E5.7#81** `PluginInstallService`——IPC 骨架
  - 2026-08-13 审计：现网链路已通（E3a #31）——SearchView.tsx:25 lk.pluginManager.install → preload-pool.ts:291 plugins:call('install', path) → IpcBridgeHandler:405 → plugin-file-service.ts:100（消毒已有：distribution 强制 user / core 强制 false）。非"建骨架"——真实差距 = 包装：校验/进度/版本处理；安装只收 path（preload-pool.ts:291）
  - ✅ **执行注 2026-08-16：** 摸底修正审计行号（迁移漂移）——现网链 = SearchView → preload-pool.ts:553 `pluginManager.install(path)`（plugins:call 代理）→ IpcBridgeHandler.ts:469 → loader.ts installPlugin（文件操作走 filesystem bridge）；**原审计指的 plugin-file-service.ts:100 安装实现实为全仓零调用方死代码**（装/卸/重装唯一实现 = 壳 loader）。四件落地：
  - **① 校验前置**（loader.ts installPlugin 重写 + 纯函数 `validateInstallManifest` 导出）：manifest 先读先验——原实现只在消毒时 parse，malformed JSON 会**先复制出半装目录再报错**；pluginId 安全格式正则 `^[A-Za-z0-9][A-Za-z0-9._-]*$`（安装目录名 = pluginId，不再用源目录 basename——目录名 ≠ pluginId 是潜伏错位，resolvePath 按 id 找目录；路径穿越字符直通文件系统是安全面）；**pluginId 裁决 = manifest.pluginId 优先、缺省目录名兜底**（lang-defaults/panel-demo 等 manifest 无 pluginId 字段——loader 惯例目录名即 id，强制要求会误拒合法插件，2026-08-16 连修）；version 必填（版本处理前置）；name 缺省回退 pluginId
  - **② 版本处理**（纯函数 `resolveVersionConflict` 导出）：目标已存在 → 读盘比对（semverUtils.compareVersions 复用）——同版/旧版拒绝且消息含双方版本号；新版拒绝升级并提示先卸载再装（**不覆盖**：Windows 文件锁，卸载 cp+rm 9 轮教训；真升级流程归 E6 PluginUpdateService）；已存在但版本读不到 → 保守拒绝（原行为是任何已存在即拒绝，零放宽）
  - **③ 进度事件**：`plugin:installProgress` { stage: validating/copying/loading/done/error } 壳 events.emit → 主进程 onPluginEmit 广播 → 池；SearchView 订阅 → 安装按钮实时阶段文案（校验中/复制中/加载中）；event-system PUSH_CHANNELS 告警名单补入（走 plugin:push 分发须用 events.on）
  - **④ 失败可见**：SearchView 不再静默吞错（原 catch {} 空吞）——`r.error` 弹 dialog.alert；成功 pushToast `已安装：{name} v{version}`（对齐卸载 toast 先例）；契约收窄 linkdesk-api.ts `PluginInstallResult`（success/pluginId/version/needRestart/error）+ mockLinkdesk 对齐返回形状
  - **死簇整删**：electron/services/plugin-file-service.ts 安装/卸载/重装三方法 + 随行私有助手 readManifestSync/isCorePlugin + INSTALL_SUBDIR 常量（零调用方 grep 实证）——本文件回归纯主进程直答面（目录扫描/manifest 读取/路径解析）
  - 测试：loader.test.ts +13（validateInstallManifest 8：合法三元组/manifest.pluginId 优先/目录名兜底/兜底安全校验/name 回退/非对象/pluginId 非字符串/路径穿越×5/缺 version；resolveVersionConflict 5：未安装放行/版本读不到/同版/旧版/新版）——vitest 409→422
  - 门禁：tsc 0×2 / eslint 0+0 --max-warnings 0 / vitest 422/422 / grid ✓ / pool-css ✓。✅ 用户验证 2026-08-16：五场景无误（夹具 `D:\linkdesk-verify\` 保留可复验）
- [x] **E5.7#82** `loader.ts`——E6 打包格式分支
  - 分支点 = 插件入口解析：dev 走 Vite import.meta.glob（loader.ts:55-102 构建时展开）vs E6 打包走 dist/plugins/<id>.js 目录扫描 + 动态 import——两套入口映射合一；plugin:// 协议对打包格式的适用性一并验证
  - 执行注（commit 877a8b28）：**两套映射合一**——resolveViewModule(pluginId, entryPath, runtimeRoot?) 单函数：glob 命中（dev 与打包产物同一张表）→ Vite chunk，未命中 → 动态 import（dev /@fs 源码 / prod linkdesk:// 预构建 chunk）；loadPluginComponent 与运行时分支两调用点收敛至此——pluginId → 模块语义全局唯一，只有 URL 形状随环境变。**E6 分支点**——runtimeEntryPath(manifest, pluginId, isDev) 纯函数：dev 返回 manifest.entry（entryless → null，生命周期契约不绑 entry），prod 一律 `<pluginId>.js`（vite.config 多入口产物名 = 插件目录名，entry 源码路径在打包格式中不参与解析）；loadPlugin 运行时分支改走此函数。**协议适用性验证**：① chunk 真身实证——dist/plugins/builtin/*.js 的 import 全是相对 `../../assets/*.js`，未逃出 plugins/ 层级 → E6 把 dist/plugins + dist/assets 平铺进 resources/plugins 即自洽（协议 CORS 安全网 + MIME 表 .js/.css/.wasm/字体/图片全覆盖在列）；② 路径解析抽纯函数 src/core/utils/linkdeskProtocolPath.ts + 7 单测（穿越 ../ 与 ..\\ → 403 / 根命中 / E6 chunk 子目录透明命中——linkdesk://{id}/{id}.js 解析方无需知道 builtin/user / assets 共享 chunk 命中 / 子目录覆盖根命中锁定原语义 / 404）——protocol.ts 委托（403/404 响应与 diag 文案逐字节保留）；③ 端到端打包实测推迟 E6（0/37，无打包基建）。**推迟 E6**：statusBar 多路径尝试 / views render 兜底 / themes fetch 在 prod 仍按源码路径（.tsx）——与入口同源的预构建迁移随 E6 打包任务补齐（#82 只立入口分支点）。测试：runtimeEntryPath 4 + linkdeskProtocolPath 7 → vitest 422→433。门禁：tsc 0×2 / eslint 0+0 --max-warnings 0 / vitest 433/433 / grid ✓ / pool-css ✓
- [x] **E5.7#83** IPC 广播——安装/卸载通知到唯一 Pool
  - 🔴 2026-08-13 审计：plugin:installed/plugin:uninstalled 全仓零 emit——死通道（仅 event-system.ts:108-109 警示列表留名）。映射表"全部Pool→唯一Pool 微调"不成立——广播今天不存在，是新建
  - 新建 emitter：主进程 plugin-file-service install/uninstall 完成处发事件（Phase 11 单写入者一致）→ IpcBridge.broadcast 到唯一 Pool
  - 分发管道已通一半：ipc-bridge.ts:321/336 poolView 推送已有 + preload-pool.ts:55 监听已有 → #83 只缺"发"这一头
  - 消费方验证：marketplace 装完改走全局事件（内部 mini-emitter 徽标逻辑 marketplaceShared.ts:82 保留）
  - 执行注（commit 617ac377）：**审计注勘误**——#81 后装/卸/重装唯一实现已迁壳 loader，原注"主进程 plugin-file-service 完成处"过时；Phase 11 单写入者原则以生命周期总线落点——新消费端加在 lifecycle.ts initLifecycleConsumers（消费端 6），不碰 loader 三函数（生命周期总线契约：新增消费端不改生产者）。**发射头**：onDidInstall reason ∈ {install, reinstall} → emit plugin:installed { pluginId, version, reason }；onDidUninstall reason = uninstall → emit plugin:uninstalled { pluginId, reason }——enable/disable/startup 是状态切换非装卸，过滤不进。链：壳 events.emit → 主进程 onPluginEmit → broadcast → 池 events.on（与 plugin:installProgress 同链；PUSH_CHANNELS 警示名单两通道早已在列，零改动）。API 契约补 linkdesk-api.ts PluginInstallResult 注释块两行。**消费方验证**：marketplace 装完已走全局事件——useMarketplacePlugins 订阅 plugin-lifecycle:changed（同 broadcast 链，marketplaceShared.ts:110）刷新全列表；徽标 mini-emitter（updateAllBadges + _dataListeners，marketplaceShared.ts:77-88）原样保留零改动；SearchView 安装结果走 install() 返回值直答（#81）。结论：marketplace 零代码变更；新通道复活为按插件消费契约（pluginId 载荷）——现有消费方无按插件需求，未来插件/E6 消费。门禁：tsc 0×2 / eslint 0+0 --max-warnings 0 / vitest 433/433 / grid ✓ / pool-css ✓

---

# Phase 16：全量回归

> 📖 验证矩阵 → [回归验证/全量回归矩阵.md](回归验证/全量回归矩阵.md)

- [x] **E5.7#84** 场景 1：侧栏交互——图标切换/折叠展开/拖宽/视图切换
  - 代码侧核对 2026-08-16（全量回归矩阵场景 1 八行全过 + 一处缺口当场修复）：
  - **①图标切换**：IconBarZone 点击 → events.emit icon:selected → 主进程 onPluginEmit 广播 → 壳 App u1（containerId 解析 / 同图标 toggle / 异图标切换 + 持久化 activeSidebarPlugin）→ sidebar:toggled / containerChanged → setState → pushLayout 回池。链完整。
  - **②折叠/展开**：池 ◀/▶ 按钮 → usePoolSync toggleSidebarCollapse → sidebar:toggle → App doCollapse（zone 宽真相源 ≤48 = 折叠 / preCollapseWidth 恢复）→ onDidChangeLayout 重推 → 池 collapsed 派生。链完整。**缺口修复**——矩阵要求的 Ctrl+B 全仓零注册（命令+键位均无，仅 dev sampleLayout mock 数据有）→ shellKeybindings 补 ctrl+b + shellMenus 注册 workbench.action.toggleSidebarVisibility（handler emit sidebar:toggle 复用池按钮同链——命令只做入口，真相源仍在 zone 宽，零第二套状态）；事件名 toggleFromPool → toggle 中性化（双生产者：池按钮 + 壳命令，ShellEvents/usePoolSync/App 三文件）。
  - **③拖宽**：SidebarZone 4px 分隔线 window-level mousemove/mouseup（移出分隔线不丢事件）+ rAF 节流 + 乐观本地宽 + mouseup commit setSidebarWidth → layoutEngine.resizeZone 钳制（壳推 minWidth/maxWidth）。链完整。
  - **④视图切换 keep-alive**：SidebarZone display:none 不卸载（PluginComponent 状态保持）。链完整。
  - **⑤图标栏显隐**：与 Ctrl+B 等效（同 ② 路径——壳持显隐真相源，池零状态）。
  - **⑥单容器 mergeHeaderWhenSingle**：effectiveTitle 派生（SidebarZone:224-227）在位。
  - **⑦sticky 吸顶**：header position sticky 在位。
  - **⑧F2/Delete**：FileTreeKeyboard:216/226 在位（池键盘拦截直接执行，不经壳）。
  - 门禁：tsc 0×2 / eslint 0+0 --max-warnings 0 / vitest 433/433 / grid ✓ / pool-css ✓
  - **用户实机验证 2026-08-16（两败两修）**：①③⑤-⑨ 全过。**②Ctrl+B 初测无效**——静态链路全查通（注册/同步/拦截/解析无破绽），实证定性 = 旧实例未重启（注册提交 09633dc5 后未重启应用即测）；重启 + 诊断埋点后 protocol-debug.log 三级实证全绿（sync `ctrl+b in table: true` → 拦截转发 → 壳 handler 命中），用户复验通过，埋点已清（commit a962a279 半程 + 本次清除）。**④keep-alive 失败**（文件树折叠态切容器丢失）——根因：壳只推活动容器 views，池切容器即重挂载 → FoldersView 重跑 syncRoots。修复：SidebarLayout 新增 containers[] 全容器清单（真相源在壳，插件卸载自然淘汰）+ SidebarZone 全部容器常驻挂载 display:none 切换（含折叠态不再提前 return——折叠也不丢状态），用户复验通过。**场景 1 八行全过 🎉**
- [x] **E5.7#85** 场景 2：编辑器——打开/切标签/关标签/脏标记/F12
  - 用户大致实机验证 2026-08-16 正常（零代码变更）。代码侧八行核对：
  - **①打开文件/LSP**：FoldersView:403 openFile 桥 → 壳建标签；editor 插件 EditorView + navigation-bridge + monaco-init 在位。
  - **②切标签 Monaco 不重载**：MainZone:464 标签页 `display:none` keep-alive（同 #84 侧栏模式）。
  - **③关标签脏确认**：closeTab（useTabManager:930-949）——dirty（tab.dirty 或 label ● 前缀双机制，E5#52）→ showConfirm → 取消阻断 / 确认走 reduceForceCloseTab（Bug A eager 修复在位）。
  - **④脏标记**：GroupTabBar:286 dirty dot + label ● 前缀双机制。
  - **⑤F12/Ctrl+Click**：editor navigation-bridge 在位（用户实机已验）。
  - **⑥主题字体颜色**：main.tsx 零 monaco 静态 import——E5.6#2 竞态已清（记忆背书 + grep 实证）。
  - **⑦同名标签**：标题由 openTab opts 传入（用户实机已验不混淆）。
  - **⑧斜体/正体**：矩阵「单例斜体」说法在 E5.7 由 preview/pinned 机制承载（useTabManager:49 + GroupTabBar:266 `!pinned → preview` 类 + CSS:100 斜体；E5#92 显式 opt-IN）——斜体 = 未固定预览 = 再开替换，正体 = 已固定 = 新增；singleton = 打开时去重（GroupTabBar:279 注释）。语义等价，机制名已迁。
  - 门禁：零代码变更，无门禁重跑（工作区即门禁全绿态）。
- [x] **E5.7#86** 场景 3：分屏——左右/上下/拖拽分屏/合屏/深度限制
  - 代码侧核对 2026-08-16（全量回归矩阵场景 3 七行 + **两处缺口当场修复**——本场景是「乐观本地+提前释放」类 bug 的集中爆发点）：
  - **①左右/上下分屏**：GroupTabBar 左键 mousedown → useDragReorder（5px 阈值拎起、垂直位移>15px 切 split 模式）→ computeSplitZone 逐面板 detectDropZone（VS Code 同款 25% 边缘阈值）→ drop-glass-zone 毛玻璃预览 → mouseup onDropSplit → pool.tabAction splitTab（direction 池侧归一化）→ App handleTabAction → splitTabAt → reduceSplitTabAt（zone→左右侧位）。链完整。
  - **②拖拽分屏线**：onDividerMouseDown → 窗口级 mousemove 逐 branch 本地宽（钳制 min 5%，forceUpdate 实时）→ mouseup updateSplitSizes 带 branchIndex（B35 精确定位）→ reduceUpdateSplitSizes。链完整。**缺口修复 A（用户实机报回闪）**——松手立刻删 localSizesRef → 回执前渲染壳侧旧尺寸一帧再跳回（闪两下）。修复：回执对齐（SidebarZone #13 同款）——pendingSplitsRef{preDrag, committed} 保留覆盖到壳 pushLayout 回执（推送≠拖前或==提交才释放），迟到旧推送不清覆盖。顺带补 handle mousedown button===0 守卫（右键/中键按住拖不再误触发 resize——SidebarZone 同款守卫漏迁）+ branchIndices memo 扩双表（key→idx + idx→branch 反查）。commit 35ed5817。
  - **③合屏**：拖 tab 回原组 → findOtherContainer → moveTab → reduceMoveTab 源组变空自动摘 leaf（标签不丢）+ TabContext 菜单命令。链完整。
  - **④深度限制**：MAX_TREE_DEPTH=4（splitTree:32），reduceSplitTabAt/reduceSplitTab 双入口 treeDepth≥4 静默拒绝。链完整。
  - **⑤设置 tab 拖拽**：core.openSettings → openTab 常规 tab 池渲染；TitleBarZone app-region 拖拽区独立成 zone 与标签栏零重叠，无 draggable:false 特例——E5.5 时代冲突点随架构消失。链完整。
  - **⑥标签栏排序**：computeInsertIndex（tab 中点 + scrollLeft 补偿 + 同组偏移）→ 插入指示器 + dragLocalTabs 乐观本地重排 → 松手 onReorder → reduceReorderTab。链完整。**缺口修复 B（探针实证：排序从未提交）**——findOtherContainer 的 `el===ownContainer` 是死判断（ownContainer=区根元素非标签栏），同组松手被误判为"移到本组"→ moveTab 同组 no-op → onReorder 全程零触发（排序仅乐观预览+弹回，用户此前"定格"均为跨组/阈值内假象）。修复：① findOtherContainer 排除源组（sourceGroupRef）——本组松手走 onReorder 真提交，跨组仍走 moveTab；② 排序回执对齐（分隔线同款）——onReorder 记 pendingReordersRef{preDragIds, committedIds, tabs}，getEffectiveTabs pending 顶替壳推送，effect 等回执（推送序==提交序或≠拖前序才释放）——消灭松手回闪。**诊断基建顺带修复**：池渲染进程 console 写入 protocol-debug.log（window-manager console-message 挂 appendFileSync 与壳 [renderer] 同文件）——此前池侧错误只进主进程终端、安装版永远落不了盘（本次诊断靠它定案）。commit 3878b54c。
  - 门禁：tsc 0×2 / eslint 0+0 --max-warnings 0 / vitest 433/433 / grid ✓ / pool-css ✓
  - **用户实机验证 2026-08-16（全过）**：分隔线松手定格不闪 / 标签排序真拖拽（跟手）松手定格不闪 / 跨组移动正常 / 左右上下分屏 / 合屏 / 深度限制 / 设置 tab 拖拽——**场景 3 七行全过 🎉**
- [x] **E5.7#87** 场景 4：浮层——右键菜单（文件树/编辑器/标签页）+ 命令面板 + Toast + Dialog
  - 代码侧核对 2026-08-16（全量回归矩阵场景 4 十行——零代码变更，全链路在位）：
  - **①右键菜单三处（文件树/编辑器/标签页）**：FileTreeContextMenu/编辑器插件/GroupTabBar:379 消费同一壳 ContextMenu 组件 → lk.menu.getItems IPC → IpcBridgeHandler:270 menu:getItems 壳侧一站式过滤（when 匹配 + i18n.t(label/title/子项) + 快捷键解析）→ 池哑渲染锚点定位。TabContext 菜单八命令（close/closeOthers/closeRight/closeAll/splitDown/splitRight/duplicate/togglePin）在 coreCommands.ts 注册在位。
  - **②菜单边界**：ContextMenu 两阶段渲染（E5#94a）——量测真实尺寸后 left 翻转（超出右缘翻左）+ top 钳制（menuTop≥30 防 TitleBarZone drag 区截鼠标——硬约束 18；底部钳制 innerHeight-4）。
  - **③菜单消失**：四条路径全挂——Escape（先收子菜单再关）/ 窗口级 mousedown 捕获（菜单外点击即关且 backdrop 吞第一击不激活下层）/ blur / wheel。
  - **④命令面板**：Ctrl+Shift+P（shellKeybindings:11）→ 主进程 keyboard-router before-input-event（08-14 补丁挂池 WCV）→ workbench.action.showCommands → showCommandPalette 壳侧 t() 序列化 DTO（title/category/快捷键 capital 化）→ App.tsx:405-421 桥 QuickPickService.onChange(push) → pushQuickPick → 池 QuickPickHost：top 15vh 居中（left 50% translateX，远离 TitleBar drag 区）+ fuzzyScore 过滤排序 + 150ms 防抖（QuickPickHost.tsx:234，输入过滤零 IPC）+ Escape/backdrop 关闭 + ↑↓ Enter。
  - **⑤Toast**：TOAST_TTL_SUCCESS=5000（toast.ts:51，壳侧定时）→ 壳推全量快照 → ToastHost：bottom 30px right 6px（状态栏 22px+8px gap 现网实测）+ column-reverse 新在下方 + 快照 diff 检测消失 id 走退场动画（200ms 退场快于 300ms 入场）+ 中键/×关闭/动作回传壳重解析。
  - **⑥Dialog**：DialogService 桥 → App 桥 pushDialog → DialogHost：50%/50% translate 居中 + Tab 焦点陷阱（首尾循环，E5#96k FOCUSABLE 同款选择器）+ Escape 关闭（alert 除外）+ backdrop 点击关闭（alert 除外）+ Enter 确认（面板聚焦，不误触取消按钮）。
  - **⑦菜单键盘导航**：menuReady（入场动画 rAF 后）→ 聚焦菜单容器 tabIndex=-1 → ↑↓ focusIdx 高亮 + scrollIntoView + Enter executeCommand——OverlayWindow focusable:false 键盘全拦回归防护在位。
  - **⑧语言纯净**：显示文本铁律——菜单 label/title/子项标签全部壳侧 i18n.t() 解析后推送（IpcBridgeHandler:290-298），池哑渲染原文不调 t()（ContextMenu.tsx 头注释铁律）——LanguagePicker 切语言后壳注册表重解析即全英文，语言非 CSS 可继承（矩阵回归风险）。
  - **回归风险双项核对**：z-index 常量表 contextMenu 3000 / quickPick 4000 / dialog 5000 全部高于所有 zone（100-1000，constants.ts Z_INDEX）；FloatingLayerHost 4 portal root 根级 pointer-events:auto（08-14 补丁陷阱反转——忘设=正常工作，hosts 关闭 return null 零 DOM 零 hit area 默认穿透不受影响）。
  - 门禁：tsc 0×2 / eslint 0+0 --max-warnings 0 / vitest 433/433 / grid ✓ / pool-css ✓
  - **用户实机验证 2026-08-16：正常——场景 4 十行全过 🎉**
- [x] **E5.7#88** 场景 5：底部面板——打开/切换/新建/拖高/Toast 位置
  - 代码侧核对 2026-08-16（五行——用户拍板直接勾掉；②④⑤ 已实现且用户 #63.7 验收时实测过，①③ 为已知未做项如实记录）：
  - **②切换视图 ✓**：PanelZone keep-alive 平级渲染 display 切换（PanelZone.tsx:184-196，MainZone TabContent 同模式）→ 点击标签 emit panel:viewSelected → App.tsx:348 事件桥 setPanelActiveViewId → usePoolSync 重推——用户 #63.7 已实测（标签切换/待办 keep-alive）。
  - **④拖高 ✓**：PanelZone 顶部 4px resize handle（#13 同款模式）——乐观本地高度 + rAF 节流 + 窗口级 mousemove/mouseup（buttons===0 窗口外释放）+ 无位移点击不 commit + mouseup 一次性 commit（panel:resize → App.tsx:352 → LayoutEngine resizeZoneHeight 钳制 → pushLayout 回执对齐释放覆盖）+ button===0 守卫 + body cursor/userSelect 锁——用户 #63.7 已实测（拖拽）。
  - **⑤Toast 位置 ✓**：ToastHost.css:18-27 position:fixed bottom 30px right 6px（状态栏 22px+8px gap 现网实测）——fixed 相对视口，与面板布局零耦合，面板打开/拖高不会推走 Toast（矩阵场景 5 行本身即按现网 ToastContainer.css:17 审计过，迁移零视觉变化）。
  - **①打开 Ctrl+J ✓ 已实现（2026-08-21 E5.8#36 改写）**：E5.8#31 补 `workbench.action.togglePanel` 真实命令 + shellKeybindings.ts `ctrl+j` 行（commit `0fe467a9`）——面板显隐/菜单栏任务由 E5.8 Phase 7 认领实现，不再是未做项。
  - **③[+] 新建 ✓ 已实现（2026-08-21 E5.8#36 改写）**：E5.8#32 桥接 `panel:createView` → QuickPickService 视图选择器（commit `0966eb99`，数据源 ViewContainerService.getViewContainers("panel") + getActiveViews 两级展平）——[+] 创建由 E5.8#32 认领实现，不再是未做项。
  - 门禁：零代码变更——沿用 #87 实测（tsc 0×2 / eslint 0+0 / vitest 433/433 / grid ✓ / pool-css ✓）
  - **用户拍板 2026-08-16：已实测过（#63.7 验收）——直接勾掉。①②③④⑤ 五行按上记实况收口。**
  - **2026-08-21 E5.8#36 收官补记：** ①③ 两行已改写为已实现（引文见上）。面板全链收口（贡献零壳改动回归 + reveal/moveToEditor）见 E5.8#36 执行注。
- [x] **E5.7#89** 场景 6：串口监视器——多会话/收发/右键/协议下拉
  - 代码侧核对 2026-08-16（全量回归矩阵场景 6 五行）：
  - **①多会话 ✓**：identityField:"sourceId" → sourceId=tab.id=session.id per-tab 绑定（index.tsx，每标签页独立会话）；serial-system 端口名精确匹配（msgPort === myPort）防多标签页数据串扰；会话 CRUD 走 useSerialSessions（硬规则 §3.12——connected 从 SerialContext 派生，不独立 set）。
  - **②收发数据 ✓**：RingBuffer(512) → rAF drain → CM6 渲染（darkTheme，Monaco 已弃）→ ScrollTracker 智能自动滚动——实时滚动链在位。
  - **③右键菜单 ✓**：editorContext/quickSendContext 菜单注册 + when:"activeEditor == 'serial-monitor'"；Phase 10 UI 回归 Bug A fix 链（tab:focused → activeEditor context key，commit a5b40553）在位；ContextMenuService.matches overrides-first/global-fallback。
  - **④协议下拉 ✓**：lk.protocol.listProtocols（#49 主进程直连 IPC）→ SelectBox 渲染——下拉列表正确。
  - **⑤关闭连接 ✓**：ControlPanel 断开 → closePort → isOpen:false（主区按钮/灯状态更新）；plugin.json invokeBeforeClose:"close_port"——tab 关闭自动断开。
  - **回归风险 3 bug 防护核对 ✓**：右键空（Bug A fix 链）/ 无数据（serial-system 端口名匹配 + tab:focused emit）/ Monaco 主题污染（CM6 换 Monaco）三链在位。
  - **Bug 修复（用户验收曝，commit e3b30770）**：侧栏会话绿灯不随端口切换换灯——换端口顶替（会话2 开新端口自动关会话1）时 isOpen 恒 true，SerialContext._setState 不产生 :isOpen 事件，唯一信号是 :sourceName 变更；useSerialConnection 只订阅 :isOpen 键 → 灯停在旧端口（会话1 亮绿、会话2 不亮）。修复：补 :sourceName 键订阅 + isOpen/sourceName 合并单状态——:isOpen=true 换灯到新端口，:sourceName 只换端口名不灭灯（连接仍开），:isOpen=false 灭灯。statusBar 无需改（isOpen 恒 true 正是换端口后的正确值）。
  - 门禁：tsc 0×2 / eslint 0+0 --max-warnings 0 / vitest 433/433 / grid ✓ / pool-css ✓
  - **用户实机验证 2026-08-16：场景 6 五行全过 + 绿灯换灯修复复验通过，无更多 bug（用户拍板收口）。**
- [x] **E5.7#90** 场景 7：插件热重载——修改代码/重载/新装
  - 代码侧核对 2026-08-16（全量回归矩阵场景 7 四行——零代码变更）：
  - **①修改插件代码**：插件入口/视图/状态栏模块全部 lazy glob（eager:false，loader.ts:84-141）dev 走 Vite 服务端解析；vite.config.ts:79 server.watch.ignored 含 "**/plugins/**"（Tauri 时代设计——插件由文件命令操作（安装/卸载/重装），忽略避免 Vite 检测到文件系统变化后全量 reload）→ 改插件文件保存**不即时 HMR**，F5 刷新后 glob 重取生效；2s 轮询 watcher（loader.ts:1717）只认新目录/删目录，不重载已加载插件。如实记录：矩阵期望「保存 → glob 更新」实际语义 = 保存 → F5 → 更新，用户验收接受。
  - **②重载插件**：市场「待安装」列表「安装」→ UninstalledListView.tsx:64-70 pm().reinstall → preload-pool.ts:554 → reinstallPlugin（loader.ts:1639，.disabled 移回 + loadPlugin("reinstall") + notifyManifestChanged 主进程三表重扫）。**状态保持 ✓**：全仓无 clearPluginState——pluginState 按 pluginId 存，卸载/重装不清，插件数据原样。
  - **③新装插件**：SearchView「安装」→ dialog.open(directory) → installPlugin（loader.ts:1403）：manifest 校验（#81 纯函数）→ 复制 → 消毒（强制 distribution=user/core=false）→ loadPlugin("install") → toast「已安装（即时生效）」；失败降级 needRestart（提示 npm run build:plugins + 立即重启按钮）；plugin:installProgress 阶段（校验中/复制中/加载中）广播到池按钮文案。无需重启壳 ✓。
  - **④卸载→重启→重装**：卸载移 .disabled/ + 缓存标记 "uninstalled"（loader.ts:1260-1291）；重启 initPluginLoader 步骤 2 listDirs 过滤 glob 中已卸载项（种缓存供详情浏览）+ 步骤 7 扫 .disabled/ 入「待安装」——已装列表扫盘重建正确 ✓；幂等（reinstall dest 已存在即拒 / install #81 版本冲突裁决）；#83 通知链在位（plugin:installed 白名单 event-system.ts:110-113 → 壳 emit → onPluginEmit 广播 → 池 events.on；市场 UI 走泛化 plugin-lifecycle:changed 自动 refreshData + badge 更新）。
  - 门禁：零代码变更——沿用最近实测（tsc 0×2 / eslint 0+0 / vitest 433/433 / grid ✓ / pool-css ✓，e3b30770 门禁）
  - **用户实机验证 2026-08-16：正常——场景 7 四行全过（含①「保存 → F5 → 更新」语义接受）🎉**
- [x] **E5.7#91** 场景 8：崩溃恢复——kill renderer → 重建 + layout 恢复 + Hot Exit
  - 代码侧核对 2026-08-16（全量回归矩阵场景 8 五行——零代码变更；用户拍板直接勾掉：五行均在 2026-08-15 Phase 9 现网实测全过，不重测）：
  - **①renderer 崩溃**：crash-recovery.ts:121-152 render-process-gone（clean-exit/退出中除外）→ 分支 1（sender = 池 wc）→ recordPoolCrash → rebuildPoolWithRetry → wm.rebuildPool()（destroy+create）→ waitPoolReady（pool:ready sender 校验 + did-fail-load + 10s 超时，单次最多 3 次重试）→ lastLayout 回放——2-4s 自动重建 ✓。
  - **②layout 恢复**：每次 pushLayout 经 cacheLayoutSnapshot 缓存（window-manager.ts:201）→ 新池 ready 回放（crash-recovery.ts:278-281）；池崩壳活 tabState 不丢——标签页/分屏/侧栏/面板全恢复 ✓。
  - **③Hot Exit**：变更 → trackDirtyFile → 1s debounce → hotExit.save → 主进程落盘 %APPDATA%/linkdesk/hot-exit/<sha256>.dirty（池零直写）；池崩进程死无 unmount → 备份存活 → EditorTab mount loadBackup → 内容恢复 + ● dirty 标记（EditorTab.tsx:164-183）；保存 clearDirtyFile 删备份；unmount 2s 延迟删 + remount cancel ✓。
  - **④壳崩**：分支 2 → rebuildShell（main.ts:260-269）：dispose 旧 bridge/wm → createWindow（ipcMain 全 once-guard 只刷引用）→ replayAfterShellRebuild（lastLayout 兜底回放 + 崩溃计数重置）→ 先建后毁旧窗（window-all-closed 不触发退出）；壳从 workspace 持久化恢复 tabState → pushLayout 自然对齐 ✓。
  - **⑤连续崩溃**（#39b）：10s 窗口 3 次 → showStaticErrorPage（data: URL 内联静态错误页，loadURL 重生 renderer）+ 心跳/重建链停摆；重启应用或壳崩全窗口重建才重置（crash-recovery.ts:135-139, 211-219）✓。
  - **附带核对**：心跳 #37（5s ping / 3s check / 10s 超时 → forcefullyCrashRenderer 走分支 1 链；pong 由 preload-pool 模块顶层自动回复，preload-pool.ts:246-253）✓。
  - 门禁：零代码变更——沿用最近实测（tsc 0×2 / eslint 0+0 / vitest 433/433 / grid ✓ / pool-css ✓，e3b30770 门禁）
  - **用户拍板 2026-08-16：测过的就不测了——五行按 Phase 9 现网实测（2026-08-15 六项：Hot Exit 杀应用重开内容恢复带 ● / 池崩重建布局原样 / 3 连崩静态错误页 / 壳崩全窗口重建 / 心跳假死 10s 强制重建 / 内存压力 toast）直接勾掉。**
- ~~E5.7#92~~ 🔴 推迟 v1.3——场景 9 脱出窗口（拖出/独立编辑/关闭回归/漂移面板）：Phase 8 已整体推迟（#32-#35），E5.7 发布不测；回归矩阵场景 9 保留为 v1.3 验证稿（2026-08-13 审计）
- [x] **E5.7#93** 场景 10：收尾检查——tsc 零错误 + ESLint 零新增 + vitest 基线 + grep 零残留 + 进程数 = 3（主 + 壳 + Pool） + F5 刷新
  - 代码侧核对 2026-08-16（矩阵六行）：
  - **①tsc 零错误 ✓**：`npm run check` 实跑——tsc --noEmit + electron/tsconfig 双工程 0 错误。
  - **②ESLint 零新增 ✓**：eslint src/ plugins/ electron/ `--max-warnings 0` = 0+0（历史首次全零，Phase 14.5 收口后保持）。
  - **③vitest 基线 ✓**：433/433（33 文件全过，0 败）。
  - **④grep 零残留 ✓**：六项全部只剩迁移注释（描述"已删/从未实现"的历史注记），零活代码——OverlayWindow 0 命中 / sidebarPoolView 0 命中 / pluginViews 2（viewRegistry.ts:108 删注、preload-pool ❌ API 清单）/ instanceId 2（loader.ts:990 E5.5#9i 删 eager 创建注记、ipc-bridge.ts:7 头注）/ poolId 3（poolLayout.ts:7-9 "全仓零 poolId" 审计结论、plugin-view-handlers.ts 两条）/ ?zone= 4（全为"无 ?zone= 路由"说明）；plugins/ 目录六项全零命中。
  - **⑤进程数 = 3 ✓**：用户任务管理器验证（1 主进程 + 2 renderer〔壳 + Pool〕；GPU/utility 为 Chromium 基础设施不计）。
  - **⑥F5 刷新 ✓**：用户验证会话恢复、布局恢复正常。
  - 门禁：npm run check 实跑全绿（tsc 0×2 / eslint 0+0 / vitest 433/433 / grid ✓ / pool-css ✓）
  - **用户实机验证 2026-08-16：正常——场景 10 六行全过 🎉 Phase 16 全量回归收官（#86-#91 + #93，#92 推迟 v1.3）**

---

## 迁移统计

| 指标 | E5.6 已完成 | E5.7 目标 | 变动 |
|:--|:--|:--|:--|
| BrowserWindow | 2 (Main + Overlay) | 1 | −1 |
| WebContentsView | 3 (Sidebar + Main + Overlay) | 1 | −2 |
| 渲染进程 | 4 | 2（壳状态 + Pool 渲染） | −2 |
| 插件代码改动 | 0 | 0 | 0 |
| 净删代码 | ~480（E5.5→E5.6） | ~1,400（E5.6→E5.7） | |
| PoolLayout version | 1 | 2 | 全量字段 |
| 任务数 | 86 主任务（16 Phase） | 107 主任务（18 Phase，含后补 #27.5/#31.5 + Phase 7.5 #31.6/#31.7 + Phase 14.5 #94-#102） | 继承 E5.6 未完成部分 |

---

> 📖 **架构设计：** [01-极简Pool设计.md](01-极简Pool设计.md)
> 📖 **任务迁移映射：** [迁移/E5.6到E5.7任务映射.md](迁移/E5.6到E5.7任务映射.md)
> **← 上一 Phase：** E5.6 双Pool（已封存，执行到 51%）
> **→ 下一 Phase：** E6 插件生态与发布
