# E2 执行清单——底层加固 + 侧栏扩展位（40 任务，36/40 完成，4 取消，~1,085 行执行）

> 四批顺序执行，同批内可并行。详细设计见各子文档（E2a-E2d）。
> **零新功能。** 所有任务纯加固——行为不变，架构变。

---

## 第 0 批：E2a ErrorBoundary 增强（6 任务，~140 行）

> ⚠️ **必须最先做。** 多进程前必须的兜底——先装安全气囊。

- [x] **#1** ErrorBoundary 增强——加 `pluginId`/重试按钮/fallback prop/componentDidCatch | `ErrorBoundary.tsx` +50 | 崩溃显示 "「插件名」已崩溃 [重试]"
- [x] **#2** 主区壳视图包 ErrorBoundary——welcome + plugin-detail | `MainContent.tsx` +10 | 欢迎页崩溃 → ErrorBoundary 兜底
- [x] **#3** 插件 WebView 包 ErrorBoundary——每个插件外层包 | `MainContent.tsx` +20 | 插件崩溃 → 其他插件不受影响
- [x] **#4** 侧栏包 ErrorBoundary——sidebar 各面板独立兜底 | `SidePanel.tsx` +10
- [x] **#5** 心跳检测——200ms interval ping + 2s 超时 | `useHeartbeat.ts`（新）+ `main.ts` +40 | JS 死循环 → 2s 后弹窗 "应用无响应 [刷新] [等待]"
- [x] **#6** 内存监控——performance.memory 采样 + 阈值告警 | `useMemoryMonitor.ts`（新） +30 | RSS > 阈值 → toast 建议关闭不活跃插件

---

## 第 1 批：E2b 终端归一化（8 任务，~170/−105 行）

> 终端是 E3 新插件的模板——模板本身必须干净。

- [x] **#7** SerialContext 迁出 core/ → plugins/terminal/ + v3Api 改用 useSourceState | +40/−40 | grep "SerialContext" src/core/ 零结果
- [x] **#8** 术语迁移——portOpen→sourceOpen、portName→sourceName | +30/−30 | grep "portOpen\|portName" src/ 零结果
- [x] **#9** useIpcEvent 归一化——裸 ipcRenderer.on() → useIpcEvent | +20/−15
- [x] **#10** 模块级可变状态消灭——let → const / hook / session | +10/−10
- [x] **#11** 命令路由修复——cmViewMap key = sourceId（保证唯一）+ WeakMap dispose | +20/−10 | 两个终端标签页 → Ctrl+Shift+P 清空 → 只清空活跃标签页
- [x] **#12** 硬编码审计——grep 四类模式 + 🔥 中文硬编码 → t() | 全量 grep | UI文字走 t()、注释通用化
- [x] **#12a** statusBar.tsx——TX/RX 动态渲染标准化 + 🔥 侧栏 onMouseDown | +25 | 侧栏会话列表点击→`onMouseDown` 替代 `onClick`（对标 03-插件制造 UI 规约 §7：快速点击跨元素不丢事件）
- [x] **#12b** 终端状态栏开关 + 配置注册到 ConfigurationRegistry | +30 | Settings Editor "串口监视器"分组→开关→关掉 TX/RX→F5 仍关

---

## 第 2 批：E2c 基础设施缺口（22 任务，~915 行 ✅ 全部完成）

> E2b 拆干净后 core/ 边界更清晰。FileService 必须成为唯一 fs 入口。
> 同批内：#13-#15（FileService/WorkspaceService/DialogService）→ 其余可任意顺序。

- [x] **#13** FileService——listDir / readFile / writeFile / watch | `FileService.ts`（新） +80 | listDir→文件列表、writeFile+readFile→内容一致、watch→外部修改 emit 事件
- [x] **#13a** FileAssociationService——扩展名→插件映射 | `FileAssociationService.ts`（新） +80 | 双击 .dxf → 查找对应插件 → 打开
- [x] **#13b** 插件数据目录约定——E1 env-service + plugin-file-service | +30 | 每个插件有自己的数据目录
- [x] **#14** WorkspaceService——openFolder / onDidChangeFolders | `WorkspaceService.ts`（新） +80 | openFolder→Electron dialog→rootPath 更新→订阅者收到通知
- [x] **#15** DialogService——替代 window.confirm | `DialogService.ts`（新） + `<Dialog>` 组件 +60 | 3 处 window.confirm() 全部替换 + 暗色/亮色主题正确
- [x] **#16** Chord 快捷键——Ctrl+K Ctrl+S 两段式 | `KeybindingRegistry.ts` +40 | Ctrl+K Ctrl+S 触发命令、Ctrl+K 等 2s 无反应
- [x] **#17** keybindings.json——FileService 读写 + watch 热更新 | `KeybindingRegistry.ts` + loader +50 | 改 keybindings.json→即时生效→删文件→回退出厂
- [x] **#17a** 🆕 KeybindingResolver——冲突检测 + 优先级仲裁 + when 条件排序 | `KeybindingRegistry.ts` +30 | 两插件同快捷键→detectConflicts() 返回冲突列表→resolve() 正确仲裁
- [x] **#18** CommandPalette 抬升——mv shared/ + CSS 搬入壳 + 模糊搜索 + 🔥 触发键归一化 | +65 | 卸载终端→Ctrl+Shift+P→浮窗正常（CSS 不受影响）+ fuzzyScore 匹配 "tgl"→"Toggle Terminal" + Ctrl+Shift+P 走 KeybindingRegistry 而非 App.tsx 硬编码
- [x] **#19** CoreEvents 补漏——新增事件类型定义 | `CoreEvents.ts` +20
- [x] **#19a** G14 fix——PluginDetailView 订阅 onDidUnregister | `PluginDetailView.tsx` +10 | 打开插件详情页→卸载该插件→详情页即时显示 "已卸载"
- [x] **#19b** `||` vs `??` 审计——全量 src/ + plugins/ 检查 | ~15 | grep 逐条判断→**0 处需改**——全部 `||` 要么是逻辑 OR、要么故意把 `""`/`0` 当无效、要么是 `e?.message || e` 错误日志模式。代码库干净。
- [x] **#19c** 旧代码 I/O 归一化——ConfigurationService/LayoutService/StorageService 走 FileService 唯一入口 | +31/−49 | `src/core/` 内 `linkdesk().filesystem`/`linkdesk().path` 引用清零
  🐛 验证时发现 2 个旧 bug（已修）：① 自定义强调色切主题后丢失——`applyTheme()` 覆盖 `--accent`；② JSON 按钮 `alert()` 关闭后原生控件无法交互——Electron 原生对话框焦点不归还 DOM
- [x] **#19d** tabIdentity 声明驱动化——消灭 TAB_IDENTITY 硬编码表 | +116/−73 | grep "TAB_IDENTITY" src/ 零结果
- [x] **#19e** factoryRole + FactorySlots.ts——系统插槽机制（settings + marketplace） | `FactorySlots.ts`（新）+ `plugin.schema.json` +40 | FactorySlots.getPluginId("settings") 返回 settings 插件 ID
- [x] **#19f** 壳去终端化——App.tsx 移除 "terminal"/"settings" 引用 | +15/−10 | grep `pluginId === "terminal"` src/core/ src/App.tsx 零结果
- [x] **#19g** statusBar configurable 字段——自动注册配置项 + 注入 visible prop | +20
- [x] **#19h** ThemeEngine 5 缺口修复——清旧变量+冲突检测+unregisterTheme+补内置主题+onDidChangeTheme.fire | +40
- [x] **#19i** statusBar onClick 消费——渲染为 button + executeCommand | `StatusBar.tsx` +10
- [x] **#19j** WelcomeView + TabBar emoji 声明驱动化——删两处硬编码 map | `PluginIcon.tsx`（新） +60 | grep "TYPE_ICON\|getPluginEmoji" src/ 无残留
- [x] **#19j-icon** 图标路径 VS Code 化——`resolvePluginIcon()` 相对插件目录解析 | `iconUtils.ts` + `protocol.ts` + 4 个 `plugin.json` ~30 | `"icon": "icon.svg"` → `plugins/<pluginId>/icon.svg`（对标 VS Code）
- [x] **#19j-structure-a** loader 归一化——glob 支持 `src/` 子目录 | `loader.ts` ~10
- [x] **#19j-structure-b** 文件移入 `src/` + `resources/`——对标 VS Code 目录结构 | 4 个插件目录 + 4 个 `plugin.json` ~30
- [x] **#19k** manifest 11 零消费字段清理——viewRole 消费 / 其余标注 Phase | +44/−18
- [x] **#19l** 🆕 deriveType 排他分类——theme>language>protocol>view | `PluginDetailView.tsx` +7/−8

---

## 第 3 批：E2d 侧栏扩展位（4 任务，~110 行）——❌ 已取消

> ⚠️ **2026-07-25 取消。** VS Code 源码确认侧栏是单槽位切换（`SidebarPart` 只有一个 `activeViewletSettingsKey`），不存在双槽位同时显示。LinkDesk 现有 `lastSidebar` 机制已经是对标 VS Code 的正确行为。文件树就是侧栏中一个普通视图，走现有路径即可，零架构改动。详见 `04-E2d-侧栏扩展位.md`。

- [-] **#20** ~~plugin.json schema 加 `sidebarRole` 字段~~ ❌
- [-] **#21** ~~viewRegistry 加 `getBySidebarRole()`~~ ❌
- [-] **#22** ~~SidePanel 改为双槽位渲染~~ ❌
- [-] **#23** ~~IconBar 区分 persistent vs tagFollower~~ ❌
