# 收口总方案——非样式命名空间「公共花名册」

> **一句话**：本轴的**真问题**不是「哪个名字起坏了」，而是**软件里有一批「公共花名册」——谁都能往上写一个名字，写的时候不核对归属，撞了只小声嘟囔（或连嘟囔都没有）**。样式那一组（四条轴）已由 [样式命名空间归一化](../样式命名空间归一化/22-收口总方案-跨方样式污染九件套.md) 收口；本档把**同一个形状在非样式面上**的复制品量齐、定「收口」的判据、并拆成 **5 件 ＋ 12 轮**。
>
> **本档地位**：**第 0 格（审查＋派工）的唯一权威方案**。它接手 [30-任务-非样式命名空间普查.md](../样式命名空间归一化/30-任务-非样式命名空间普查.md) 的**待验证假设集**——那份档案是「另一个会话抢跑的预查」，本档用**自己复跑的读数**逐条对账（**不符以本档为准**）。
> **立案**：2026-09-16 用户指示「**除了 UI 这部分外，其余部分有没有类似的问题？如数据通信部分？……又或者声明的东西，又或者命令类。**」＋「**不仅仅是我提到的，你还可以自己再找一找。**」＋「**让一个新 AI 去彻查这些，然后像 CSS 这一系列一样给它排任务**」。
> **接力入口**：[交接.md](交接.md) 顶部那段（**一个 AI 一个任务**）。
> **上位约束**：[29-主题系统保护条款](../样式命名空间归一化/29-主题系统保护条款.md)（冲突时以它为准）· 硬约束 23（样式侧同源纪律）。
>
> **非新能力声明（`check-design-flow` §8.4 ③）**：本档是**现状普查 ＋ 归属方案 ＋ 派工表**，文中出现的 `contributes.*` 字段、命令 id、设置键、context key、注册表函数名、门禁脚本名**全部是引用既有实现**；本轴不新增用户可见能力、不改任何机制（只加**归属表达**与**判据**）。⇒ **无需新能力设计前置**。

---

## 〇、收口的对象——这一类问题的定义（先钉死，后面全靠它）

> **问题类**：`一个扁平的全局名字表（Map / JSON 字典 / i18next namespace / 前缀约定），多方都能往里写名字，写时不核对"这名字是谁的"，撞了不报错——最多一句 console.warn；而「被覆盖」这件事，受害方永远收不到通知。`

**「公共花名册」的五条识别特征**（照 30 号档的**同形判据**，四条全中才算同形）：

| # | 判据 | 反例（不同形的样子） |
|:--:|:--|:--|
| 1 | 名字空间是**扁平的**（名字里没有天然归属） | `pluginId:viewId` 复合键（自带归属） |
| 2 | **多方都能写入**（宿主 ＋ 官方插件 ＋ 第三方插件） | 只有宿主写、插件只读 |
| 3 | 写的时候**没有归属校验**（不检查"这名字是谁的"） | 注册时核对 pluginId 并拒绝 |
| 4 | 冲突**静默**（不报错；最多 `console.warn`） | 有用户可见的提示 / UI 高亮 |

🔴 **第 4 条是关键**：`console.warn` **对用户不可见，对「被覆盖的那一方作者」也不可见**——因为他在自己机器上**没装对方插件**，一切正常。**「被覆盖」这件事，受害方永远收不到通知。**

---

## 〇b、🔴 本格定位与边界（**2026-09-16 用户拍板，以此为准**）

| 谁 | 干什么 |
|:--|:--|
| **1.30（原定 · 已并入本格）** | 只做点名轮：立判据 ＋ 快速普查 ＋ 挂账。**CSS 系列不再单独立这一轮** |
| **第 0 格（= 本档，已完成）** | **审查 ＋ 派工**：复量 1.30 的假设集 · 补齐未查项 · 每条追到根 · 立**轮次总表 ＋ 各格任务书** · 建 E6 清单任务格与接力队列。**不做任务本身** |
| **第 1–12 格** | 一格一个 AI，照 CSS 系列的接力机制从 1.31 开始 |

🔴 **两条 2026-09-16 订正（以此为准）**：
1. **审查＋派工这一格不必等 CSS 系列收口**（只读代码 ＋ 只写新档 ⇒ 工作区干净即可开工）；**真正必须排队的是后续各格的执行**（要动 `src/` / `scripts/` / SDK，与 CSS 系列抢文件）。
2. **本轴不是一个轮次做得完的** —— 命令 id · 设置键 · 外观族 id · 上下文键 · i18n 键 **每一个都至少一格（评估轮与落地轮分开）**，外加本格自己再查出来的面。**禁止塞成一格。**

---

## 一、真根因——三层，不是「谁起错了名」

> 这一节是**给后面每一棒的方法论底座**。结论：**名字没起坏，是「谁的名字」这件事被寄托在「取名自觉」上——账本要么没有，要么只用来打印一行字。**

### 层 1（机制）——这些名字空间在一个进程里是**扁平的全局表**

| 名字空间 | 载体 | 出处 |
|:--|:--|:--|
| 命令 id | `Map<string, Command>` | `src/core/registry/commands/CommandRegistry.ts:42` |
| 设置键 | `Map<string, ConfigurationContribution>` ＋ `Map<string, string>` 归属表 | `src/core/registry/ConfigurationRegistry.ts:108-109` |
| 主题 id / 配方 id | 两张 Map（**同一个概念两本账**） | `src/core/registry/appearance/ThemeRegistry.ts:22,29` ＋ `src/core/services/ui/ThemeEngine/registry.ts:27` |
| 图标主题 id / 共享图标 id | `Map<string, …>` ×2 | `src/core/registry/appearance/IconRegistry.ts:36,42` |
| 语言包 id | `Map<string, RegisteredLanguage>` | `src/core/registry/languages/LanguageRegistry.ts:23` |
| 语言定义（扩展名） | `Map<string, LangDefContribution>` | `src/core/registry/languages/LangDefRegistry.ts:15` |
| 协议 id | `Map<string, ProtocolEntry>` | `src/core/registry/ProtocolRegistry.ts:34` |
| 上下文旗子 | `Map<string, unknown>`（＋ preload 同步 store ＋ i18next 之前的全局广播） | `src/core/registry/commands/ContextKeyService.ts:308` · `electron/preload-pool/contextkey.ts:12` |
| i18n 字典键 | i18next `translation` namespace（`addResourceBundle(lng, ns, data, true, true)` 深合并） | `src/pluginLoader/contributions/i18nResources.ts:27` |

⇒ 与 CSS 那一类**完全同源**：平台既有事实（这些表本来就没有作用域），**决定了「只要存在没有命名空间的定义方，碰撞就是迟早的」**。

### 层 2（接口）——契约面按「每个插件自己一份」设计，投进「所有插件共处一个运行时」

`plugin.json` 的 `contributes.*` 与 `window.linkdesk.*` 都是**照着 VS Code 的扩展契约设计的**——在 VS Code 的世界里，每个扩展的贡献**最终都落进同一个扁平表**（这也是 VS Code 的真实形状）。LinkDesk 沿用这套设计，同时继承了它的**未设防部分**；而 LinkDesk 又多了一样 VS Code 没有的东西：**宿主自己的贡献用的是普通命名**（见层 3）。

### 层 3（守门）——**归属只有两种表达方式，而两种都不设防** 🔴

| 表达方式 | 谁在用 | 为什么等于没设防 |
|:--|:--|:--|
| **① 表里记了归属，但只用它打印一行 warn** | 设置键（`_configKeyOwner`，`ConfigurationRegistry.ts:109,123-130`）· 主题配方（`recipeOwners`，`ThemeRegistry.ts:32,209`）· 命令（`_pluginCommands`，`CommandRegistry.ts:43,62-67`） | warn **只在覆盖发生的那台机器**上打印；覆盖照旧发生（"所有权归后注册者"），**没有任何一处降级或拒绝** |
| **② 表里根本没有归属，靠名字前缀「推」出来** | 命令 id 的运行时路径（`commandId.split(".")[0]`，`CommandRegistry.ts:150,186`）· 池命令属主（`commandOwnerId()`，`electron/preload-pool/commands.ts:36-39`）· 插件语言包（`ns = pluginId`，`i18nResources.ts:28`） | **名字既是标识符又是身份声明，而没有任何一处核对两者是否相符** |
| **③ 🔴 宿主自己的名字用的是普通命名** | 宿主伪 pluginId = `"app"`（`src/core/services/plugins/PluginStateService.ts:17`）· `"appearance"`（`src/App/config/appearance.ts:55`）· `"update"`（`src/App/config/update.ts:22`）· 宿主命令前缀 `workbench.*` / `core.*` / `app.*` / `update.*` / `view.*` / `theme.*`（实测 64 条） | 这些 pluginId **全部通过 schema 的 `pattern`**（只禁 `ldk-`）⇒ 第三方插件**可以合法地把自己的 `pluginId` 取成 `app`**，此时与宿主**同一个 owner**，**连 warn 都不会响**，且它的配置会 `Object.assign` **合并进宿主的外观贡献**（`ConfigurationRegistry.ts:133-137`） |

> **③ 是这一轴最值钱的一条**：它与样式那条 `ldk-` 规则**是同一件事的两个面**——**身份层早就设防了（`pluginId` schema `pattern` 禁 `ldk-`），问题是「名字层」没跟上**。宿主保留的是 `ldk-` 前缀，但宿主**自己用的名字**（`app` / `appearance` / `update` / `workbench.*` / `core.*`）**一个都没进保留面**。

### 一句话根因（后面每一棒收尾都要能复述这句）

> **不是名字起坏了，是「谁的名字」被寄托在「取名自觉」上——账本要么没有，要么只用来打印一行字，而宿主自己的名字和第三方名字长着同一张脸。**

**⇒ 由此推出的两条纪律（本轴全部遵守）**：
1. **归属必须结构性**（判据能独立判定，不查表、不靠白名单）——**「宿主保留名账」是唯一被允许的表，且它必须是「生成式 + 双向对账」的**（先例已存在，见 §二.6）；
2. **判据必须证明自己活着**（有 `--self-test` 正负控、自测接线、负控真红）——否则下一把尺子还会漏。

---

## 二、普查表（**2026-09-16 本格实测**，逐行可复跑；⚠️ 30 号档的预查已逐条对账，**不符处以本表为准**）

### 2.0 实测读数总表

| # | 面 | 名字空间载体 | 冲突了会怎样 | 现状设防 | 同形四条 | 严重度（按能否察觉） |
|:--:|:--|:--|:--|:--|:--:|:--:|
| **①** | **命令 id** | `contributes.commands[].id` ＋ 运行时 `commands.registerCommand(id,…)` | **后注册者覆盖 handler，只 `console.warn`**（`CommandRegistry.ts:58-73`） | 🔴 **无归属校验**；🔴 **运行时路径连归属都是"从名字推的"**（`CommandRegistry.ts:150`）⇒ **同名前缀时 warn 不响**（见 §2.1） | 4/4 | 🔴 **最高** |
| **②** | **设置键** | `contributes.configuration.properties` 的键 | **只 warn ＋ 所有权归后注册者**（`ConfigurationRegistry.ts:123-130`） | 🟠 记了归属表但**只用于打印**；🔴 **宿主 `app.*` 与宿主伪 pluginId 无任何保护**；🔴 代码自述「暂不抛硬错误……待 E2c #19b 审计后改 fail-fast」（`:120-121`）= **已有自认欠账** | 4/4 | 🟠 **中**（值串了，用户可能察觉但不归因） |
| **③** | **外观族 id**（主题 · 图标主题 · 配方 · 共享图标 · 语言包） | 五个 Map ＋ **主题有两本账** | 全部 **last-wins ＋ `console.warn`**（`ThemeRegistry.ts:125-130,201-207` · `ThemeEngine/registry.ts:38-46` · `IconRegistry.ts:53-58,126-131` · `LanguageRegistry.ts:34-39`） | 🔴 无归属校验 | 4/4 | 🔴 **高**（**id 被用户配置持久化** ⇒ 卸载/顶替后 `app.theme` 指向的可能已不是原物；用户看得见"外观变了"但**归因必错**） |
| **④** | 🔴 **上下文旗子（context key）** | `ui.contextKey.set(key, value)` —— **key 是任意字符串**（`src/core/api/linkdesk-api/ui.ts:50-52`） | 宿主菜单/快捷键的 `when` 子句读它 ⇒ 插件设一个宿主在用的键 ⇒ **静默改变宿主菜单项与快捷键的可用性**（handler 直接 `ContextKeyService.setValue(key, value)`，**零校验**：`src/core/services/plugins/IpcBridgeHandler/ui.ts:197-201`） | 🔴 **未见任何保护** | 4/4 | 🔴🔴 **最高，且今天已有跨方活体**（见 §2.4） |
| **⑤** | **i18n 字典键** | i18next `translation` namespace（宿主与全部插件共用一本字典） | 后注册语言包**深合并覆盖** ⇒ 界面文案静默换措辞（`i18nResources.ts:27-28`） | ⚠️ **跨仓不可判**（键**可以合法住在应用级字典**——官方 `settings` 插件的 t() 键一个都不在自己仓，住 `lang-defaults`） ⇒ 记忆 `plugin-repo-gate-model` §十定为**黄灯**，**本轴沿用，不重开** | 4/4（但只能黄灯） | 🟡 **低**（文案变了，用户察觉得到但不会归因） |
| **⑥** | 🆕 **协议 id** | `ProtocolRegistry` `Map<string, ProtocolEntry>` | **后注册者覆盖解析器，只 `console.warn`**（`ProtocolRegistry.ts:39-42`） | 🔴 无归属校验 | 4/4 | 🟡 **低**（今天零插件使用，见 §2.6） |
| **⑦** | 🆕 **语言定义扩展名** | `LangDefRegistry` `Map<string, …>`（键 = 扩展名字符串） | **后注册者覆盖，只 `console.warn`**（`LangDefRegistry.ts:29-33`） | 🔴 无归属校验（`.ts` 是**全局概念名**，天生不可能带插件归属） | 4/4 | 🟡 **低**（官方 18 仓共用 2 个扩展名、零碰撞） |
| **⑧** | 🆕 **共享图标 id** | `contributes.icons` → `IconRegistry.registerIcon(iconId,…)` | **后注册者覆盖，只 `console.warn`**（`IconRegistry.ts:122-131`） | 🔴 无归属校验 | 4/4 | 🟡 **最低**（**今天 0 个名字**，纯预设面） |

### 2.1 🔴 ① 命令 id——「归属是**从名字推出来的**」（本格新发现，30 号档未记）

**两条注册路径给出**两种不同的归属**，而它们写进同一本账：

| 路径 | 归属怎么来 | 出处 |
|:--|:--|:--|
| **plugin.json 声明** | **真 `pluginId`** | `src/pluginLoader/contributions/contributions.ts:65` |
| **运行时注册**（`commands.registerCommand`） | 🔴 **`commandId.split(".")[0]`——名字的第一段** | `src/core/registry/commands/CommandRegistry.ts:150`（池元数据）· `:186`（壳侧本地）· `electron/preload-pool/commands.ts:36-39`（属主推导，同一约定） |

**由此产生三个可判定的真害**：

1. **覆盖告警会被整段吞掉**：覆盖检测是 `for (const [pid, ids] of _pluginCommands) if (ids.has(id) && pid !== pluginId)`（`CommandRegistry.ts:62-66`）。两个插件都**运行时注册** `acme.doThing` ⇒ 两者的推导归属都是 `"acme"` ⇒ `pid !== pluginId` **恒 false** ⇒ **静默覆盖，一声不响**。
2. **注销会牵连别人的命令**：`unregisterCommands(pluginId)` 的实现是 `ids.startsWith(pluginId + ".")` 后整片删（`electron/preload-pool/commands.ts:82-87`）⇒ 插件 `beta` 注册的 `acme.foo` 会被**卸载 `alpha`** 时一并摘掉。
3. **`on-command` 激活会去 import 别人**：命令 handler 池内 miss 时，池按 `commandOwnerId(commandId)` **去激活那个插件**（`electron/preload-pool/commands.ts:45-61`）⇒ 名字的第一段被当成**身份**使用。

**存量读数**（官方 18 仓，`plugin.json` 静态声明面）：**35 个独立命令 id**，**跨插件共写 0**；其中**带本插件归属前缀 14 / 不带 21**——不带的 21 个**全部属于 `file-tree`**（`explorer.newFile` / `explorer.copy` / `revealInExplorer` …，是**照抄 VS Code 的功能命名空间**）。

### 2.2 ② 设置键——宿主保留面今天**一个都没设防**

- **插件侧**：**47 个独立键**，跨插件共写 **0**；带本插件归属前缀 **28 / 不带 19**（不带的含 `file-tree` 的 `explorer.*` / `files.exclude` / `terminal.external.*` 与 `editor` 的 `files.autoSave` —— 同样是**照抄 VS Code 的共享命名空间**）。
- **宿主侧**：**28 个 `app.*` 键**（`app.theme` / `app.uiFontScale` / `app.update.mode` …，登录点 `src/App/config/appearance.ts:55` · `src/App/config/update.ts:22` · `src/App/startup.ts:94`）。
- **交集实测 = 0**（今天没撞），但**没有任何机制阻止**插件声明 `app.theme`：
  - 若插件 `pluginId` **不是** `app` ⇒ 走 `console.warn` 分支（`ConfigurationRegistry.ts:123-128`），**然后照旧覆盖归属**（`:130`）；
  - 🔴 若插件 `pluginId` **就是** `app` ⇒ **`owner === pluginId` ⇒ 连 warn 都不响**，且 `Object.assign(existing.properties, …)`（`:133-137`）会把它的属性**合并进宿主的外观贡献**。

### 2.3 ③ 外观族 id——**主题有两本账**，且 id 会被**持久化**

- 五个 id 空间实测独立名：主题 **10** · 图标主题 **1** · 配方 **20** · 共享图标 **0** · 语言包 **4**；**跨插件共写全部 = 0**。
- 🔴 **主题/配方是两本账**：`src/core/registry/appearance/ThemeRegistry.ts`（`themes` ＋ `recipes`，`:22,29`）与 `src/core/services/ui/ThemeEngine/registry.ts`（`pluginThemes`，`:27`）**各存一份**，两边都是 last-wins ＋ warn（后者还**无归属不告警**：`:38-46` 明写「覆盖 fallback 主题不告警——插件主题上位是预期行为」）。
- 🔴 **隐蔽点 = 持久化**：`app.theme` 存的是**配方 id 字符串**（`src/App/config/appearance.ts:58`，applier 读它 `src/App/config/appearanceApplier.ts:77`，切换监听 `src/App/lifecycle.ts:117`）⇒ 插件顶替/卸载后，用户配置里那个名字**指向的可能已不是原物**，而设置页只会显示"空"或"另一个主题"。
- 🔴 **宿主兜底 id 无保留面**：`dark` / `light` / `dark-fallback`（`ThemeEngine/registry.ts:114-130` 的 `registerFallbackThemes`）是宿主注入的，**第三方插件可以用同名 id 顶替它们**。

### 2.4 🔴🔴 ④ 上下文旗子——**今天就有跨方活体，而且方向是「插件驱动宿主」**

**宿主在用的旗子全集（实测）**：

| 旗子 | 谁写 | 谁读（宿主） |
|:--|:--|:--|
| `activeEditor` · `editorHasSelection` · `editorCount` | 宿主（`ContextKeyService.initCoreKeys()`，`ContextKeyService.ts:427-429`）＋ `src/App/lifecycle.ts:84` | 宿主命令 `when` |
| `sidebarPosition` | 宿主（`src/hooks/usePoolSync.ts:130`） | `shellMenus.ts:38,39,161,162` |
| `updateActionable` | 宿主（`src/hooks/usePoolSync.ts:136`） | `updateCommands.ts:122` |
| `inputFocus` | **共享组件**（`src/components/shared/inline-input/InlineInput.tsx:106,117,145`） | 宿主侧消费者 |
| 🔴 `settingKey` · `settingModified` · `settingFollowTheme` · `settingResetsToDefault` | **官方插件 `settings`**（`settings/src/views/SettingsView/SettingRow/gearMenu.ts:25-48`） | 🔴 **宿主命令**（`src/core/commands/shell/coreCommands.ts:264,287,342`） |

⇒ **最后一行就是活体样本**：**一个插件通过设 4 个裸名旗子，决定宿主菜单项（齿轮菜单的「跟随主题」/「重置」/「打开存储位置」）出不出现。** 它今天能工作，只因为**双方约好了名字**——而那个约定**没有任何机器在读**：
- 第三方插件写 `contextKey.set("settingFollowTheme", true)` ⇒ 宿主菜单项**立刻出现**（`IpcBridgeHandler/ui.ts:199` 直通 `setValue`，**零校验、零归属检查**）；
- 反过来，插件写 `activeEditor` ⇒ 命令面板的过滤（`src/core/commands/palette/commandPalette.ts:17`）与快捷键仲裁（`KeybindingRegistry/registry.ts:128-129,143`）**一起被改**——**改的是行为，不是外观，「界面没变」让你根本不会怀疑**。

### 2.5 ⑤ i18n 字典键——**17 处实测共写，天然黄灯**

- 官方 18 仓共 **414 个顶层键**，**共写 17 处**（实测例：`搜索` ← `file-tree` / `marketplace` / `serial-monitor`；`行号显示` ← `editor` / `serial-monitor`；`复制` / `删除` ← `file-tree` / `serial-monitor`）。
- ⚠️ **为什么只能黄灯**：**键可以合法住在应用级字典**——官方 `settings` 插件**一个 t() 键都不在自己仓**（住 `lang-defaults` 的 `en.json` / `zh.json`）。⇒ 在**插件仓里**判「你的键撞了别人」**永远判不出来**（它看不到别人的仓，也看不到宿主字典）。硬判必出假红，**假红会让真红失效**（记忆 `plugin-repo-gate-model` §十）。

### 2.6 ⑥⑦⑧ 三条新花名册 + **对照面**（本格新查出来的）

**新花名册（同形，本格补上；30 号档未覆盖）**：协议 id（`ProtocolRegistry.ts:34-42`，官方 18 仓**零使用**）· 语言定义扩展名（`LangDefRegistry.ts:15,29-33`，官方共用 `2` 个扩展名、零碰撞）· 共享图标 id（`IconRegistry.ts:42,122-131`，**今天 0 个名字**）。

**对照面（**不同形**，是本轴的「样板」——`check-design-flow` 意义上的正面先例）**：

| 面 | 设防形态 | 出处 | 为什么不同形 |
|:--|:--|:--|:--|
| **视图 id** | 🔴 **复合键 `pluginId:viewId`** ＋ 裸 id 解析歧义时 **fail-loud（`console.error` 点名两个 pluginId）** | `src/core/services/layout/ViewContainerService.ts:59,76,182-195,247-262` | 归属**由构造提供**，不靠命名；撞名**合法共存**且**出声**。⇒ 实测 `file-tree` 与 `marketplace` **真的同名 `search` 且今天无害**——这是本轴**最该抄的模板** |
| **数据推流通道** | 唯一路径 `IpcBridge.broadcast`，门禁强制 | `scripts/check-ipc-audit.mjs`（挂 `npm run check`） | 路径纪律已机械 |
| **插件身份 `pluginId`** | schema `pattern` `^(?!ldk-)[A-Za-z0-9][A-Za-z0-9._-]*$` ＋ 硬约束 11 发布后不可变 | `public/schemas/plugin.schema.json` | **身份层早已设防**——「名字层」是本轴要补的那半 |
| **快捷键冲突** | **检测 ＋ 设置页 UI 高亮冲突行**（`KeybindingRegistry/registry.ts:91-115,147-149`）＋ 明确定义仲裁优先级（`user > plugin > builtin`，同优先级后注册者生效 `:118-145`） | 同上 | 冲突**用户可见**（两边都装在同一台机器上）⇒ 不是静默类 |
| **factoryRole 插槽** | **多候选并存 ＋ 用户切换**（记忆 `factory-role-coexistence`） | `src/core/services/bootstrap/FactorySlots.ts` | 冲突**是设计**，不是缺陷 |
| **文件关联** | 同扩展名**允许**多插件注册——"打开方式…"选择器消费全部 | `src/core/services/files/FileAssociationService.ts:36-59` | **加性**设计，无覆盖 |
| **菜单槽 id** | **开放的扩展点**（schema 明写 third parties may declare any new registration point） | `public/schemas/plugin.schema.json` 的 `contributes.menus` ＋ `MenuRegistry.ts:24-53` | **加性**（数组追加），无覆盖 |
| **`window.linkdesk.*` storage 面** | **不存在**——44 个命名空间面实测无 storage（`src/core/api/linkdesk-api/surfaces.ts:26-33`） | 同上 | **没有这本花名册**（插件持久化走 `pluginState`，按 pluginId 分桶） |

> 🔑 **三条最有价值的结论**：
> ① **「复合键 + fail-loud」这个模板在本仓已经存在**（视图 id）⇒ 本轴的修法**不必发明新形状**；
> ② **「宿主保留名账进 SDK」这个模板也已经存在**（`packages/plugin-sdk/schemas/reserved-class-names.json` ＋ 两条腿读同一份 + `check-reserved-names-doc-sync` 双向对账）⇒ 见 §二.7；
> ③ **不是所有冲突都该治**：加性扩展点（菜单槽 / 文件关联 / factoryRole）与用户可见冲突（快捷键）**本来就是设计**，本轴**只登记、不改造**。

### 2.7 🔑 本轴的统一机制——两条，都**已经在仓里有先例**

| 机制 | 适用面 | 现成先例（照抄它的形状） |
|:--|:--|:--|
| **A. 宿主保留名账 —— 进 SDK** | 静态可判的（命令前缀 · 设置键 · 外观族 id · 上下文旗子） | `packages/plugin-sdk/schemas/reserved-class-names.json`：**单一真相源** ＋ **随 npm 包下发** ⇒ **插件仓自己就能判**（不必读文档、不必问宿主）＋ 壳仓门禁 `scripts/check-css-namespace.mjs` 与 SDK 腿**读同一份** ＋ `scripts/check-reserved-names-doc-sync.mjs` 把**登记表 ↔ 作者面两棵树的表 双向钉住** |
| **B. 运行时归属仲裁 —— 首撞者保留 ＋ 后来者降级不覆盖 ＋ fail-loud** | 跨仓不可静态判的、且**存量不能改名**的（真正的跨插件同名、i18n 之外的运行时面） | 视图 id 的 fail-loud（`ViewContainerService.ts:182-195`，用 `console.error` 而不是 warn）＋ 记忆 `plugin-repo-gate-model` §十的「冲突即拒载**太狠**」结论（见下） |

> 🔴 **为什么 B 不能用「抛错」**：`ConfigurationRegistry` 注释自述「**parseContributions 没有 try/catch，抛错会导致整个插件加载失败**」（`:120-121`）⇒ 一个第三方插件的重名会让**整只插件装不上**，**比现在更坏**。⇒ B 的形态只能是 **「首撞者保留 ＋ 后来者降级为不覆盖 ＋ 明确出声」**——**比抛错温和、比 warn 有效**（它真的改变了结果，不只是打印）。

### 2.8「宿主保留名账」该收哪些名字（本轴的账目草案）

> 本表是**派工草案**，各评估轮**自己复量后定稿**——本格只给起点与出处。

| 家族 | 该收进账的名字 | 今天的实测来源 |
|:--|:--|:--|
| **命令 id 前缀** | `workbench.` · `update.` · `core.` · `app.` · `view.` · `theme.` | 宿主 64 条命令的第一段（`grep -rhoP 'command:\s*"[a-zA-Z][^"]*"' src/core/commands` 实测分布：workbench 46 / update 5 / view 4 / theme 3 / core 3 / app 3） |
| **设置键** | `app.*` 全 28 键 | `src/App/config/appearance.ts:55` · `src/App/config/update.ts:22` · `src/App/startup.ts:94` |
| **宿主伪 pluginId** | `app` · `appearance` · `update` | `src/core/services/plugins/PluginStateService.ts:17`（`APP_PLUGIN_ID = "app"`）· `appearance.ts:55` · `update.ts:22` |
| **外观族 id** | 宿主兜底 `dark` · `light` · `dark-fallback`（＋ 实测宿主注入的其余兜底 id） | `src/core/services/ui/ThemeEngine/registry.ts:114-130` |
| **上下文旗子** | `activeEditor` · `editorHasSelection` · `editorCount` · `sidebarPosition` · `updateActionable` · `settingKey` · `settingModified` · `settingFollowTheme` · `settingResetsToDefault` · `inputFocus` | §2.4 表（**逐条带 file:line**） |

---

## 三、逐条定案——严重度 · 修法 · 分级

> **排序纪律**：**按「受害方能不能察觉」排，不许按技术严重性排**（CSS 那一类难查的根源就是受害者看不见）。
> **分级纪律**：照记忆 `plugin-repo-gate-model` §十 —— 第三方侧**新判据默认黄灯**；**判红必须逐条论证「① 本仓可答 ② 有真害（不是审美）」**，缺一即降黄灯。**假红会让真红失效。**

| # | 面 | 严重度 | 受理发（逐条给理由） | 分级（红/黄） | **「本仓可答」吗** |
|:--:|:--|:--:|:--|:--|:--|
| ④ | **上下文旗子** | 🔴🔴 **最高**（改行为 · 零信号 · 今天已有跨方活体） | **(d) 宿主旗子保护区** ＋ **(a) 插件旗子归属前缀**。A 账收「宿主旗子全集」＋ B 仲裁兜运行时 | 🔴 **红**（占用宿主保留旗子）/ 🟡 黄（插件自有旗子未带归属） | ✅ **可答**——宿主旗子名单生成式进 SDK 后，插件仓能自查 |
| ① | **命令 id** | 🔴 **最高**（功能级静默 · **warn 也会被吞**） | **(d) 宿主命令前缀保护区** ＋ **(a) 插件命令 id 必须 `<pluginId>.`** ＋ **修归属来源**（B：`registerCommand` 要拿到真身份，不许从名字推） | 🔴 **红**（占宿主前缀是**真害**；自家命令不带自家前缀**本仓可答**） | ✅ **可答**（前缀账进 SDK ＋ `pluginId` 本仓就有） |
| ③ | **外观族 id** | 🔴 **高**（持久化 id 指向他物 · 归因必错） | **(d) 宿主兜底 id 保护区** ＋ **(a) 新 id 带归属**；🔴 **存量 id 不改名**（改名 = 用户配置失效 ＋ 可见变化）⇒ 存量走 **B 仲裁** | 🔴 **红**（顶替宿主兜底 id）/ 🟡 黄（新 id 未带归属）；🟡 存量不合规**只报不改** | ✅ **可答**（宿主兜底 id 账进 SDK） |
| ② | **设置键** | 🟠 **中**（值串了 · 有 warn 但用户不可见） | **(d) 宿主保留键保护区** ＋ **(a) 插件新键带归属**；存量 VS Code 共享命名空间（`explorer.*` / `files.*` / `terminal.external.*`）**不改名**（改名 = 用户设置失效 ⇒ **与「零 UI 变化」冲突**）⇒ 存量走 **B 仲裁** | 🔴 **红**（占宿主 `app.*` / 伪装宿主伪 pluginId）/ 🟡 黄（新键未带归属） | ✅ **可答**（宿主键账进 SDK） |
| ⑤ | **i18n 字典键** | 🟡 **低** | **(c) 只黄灯 ＋ 文档**（跨仓不可判：键可以合法住应用级字典） | 🟡 **黄，且不许判红** | ❌ **不可答**（插件仓看不到宿主字典与别人的仓）⇒ **天然黄灯** |
| ⑥⑦⑧ | **协议 id · 语言扩展名 · 共享图标 id** | 🟡 **低**（今天零/近零使用） | **(a) 新 id 带归属**（协议 id / 共享图标 id 可带；🔴 **语言扩展名不可以**——`.ts` 是全局概念名，加前缀 = 语义错误） | 🟡 黄（新 id 未带归属）；🔴 红只留「占宿主保留面」 | ⚠️ **部分可答**（协议 id / 共享图标 id ✅；扩展名 ❌ 只登记） |

**禁区（用户与本系列共同定，逐格照抄）**：
1. 🔴 **不许一律把 `console.warn` 改成 `throw`**（§2.7 的顾虑：会让整只插件装不上，**比现在更坏**）。
2. 🔴 **不许动已设防的四处**（它们是样板，不是问题）：数据推流唯一路径 `IpcBridge.broadcast` · `pluginId` schema `^(?!ldk-)…` · **视图 id 复合键 ＋ fail-loud** · 快捷键冲突的 UI 高亮。
3. 🔴 **不许把 i18n 键硬判成红**（跨仓不可判；硬判必出假红）。
4. 🔴 **不许引白名单 / 基线 / 棘轮** —— 优先**结构性判定**（「宿主保留名账」是唯一例外，且它**生成式 ＋ 双向对账**，不是手抄豁免表）。
5. 🔴 **不许改机制**：只加归属与判据，**不重构注册表、不换数据结构**（尤其**不许动 `commitTokens` / `theme:changed` 广播链**——[29 号档](../样式命名空间归一化/29-主题系统保护条款.md) P1）。
6. 🔴 **上位约束同样生效**：**零 UI 变化**（除版本号；含**主题统一调控 / 字体 / 翻译**三条横切机制零回归）· **只做加法且名字一次定对** · 对外宽容（第三方可自画 / 可自定，只黄灯不拦；跟随官方是**奖赏不是枷锁**）。

---

## 四、轮次表与执行序（**本档权威版**）

> **执行序 = 下表自上而下。轮次号 = 执行序**（本轴**没有**「收口轮不是最后」的历史包袱——**收口复核恒为最后一格**）。
> 🔴 **纪律**：**评估轮的下一格任务书，由评估轮收尾时写齐**（落地面数字要靠实测产出）；表里已写「→ 由 X 写齐」的那些格，进场时**必须先把评估轮的任务书读成正式版**。
> 🔴 **每一轮收尾**：跑一遍 §八 的探针并记账（**改前 / 改后读数**）——这是全轴统一进度尺。

| 序 | 轮 | 编号 | 任务 | 详案 | 前置 | 提交轴 |
|:--:|:--|:--|:--|:--:|:--|:--|
| 0 | **1.30** | `#109r` | ▷ **已并入本格**（审查 ＋ 派工，**不做任务**） | [30 号档](../样式命名空间归一化/30-任务-非样式命名空间普查.md) ＋ 本档 | — | 文档 |
| 1 | **1.31** | `#111a` | **件 1·命令 id 与协议 id · 评估**（归属从哪来 ＋ 名字派生归属的修正方案 ＋ 宿主前缀账 ＋ 判据草案 ＋ 代价量化） | [01](01-任务-命令id归属评估.md) | **CSS 系列 1.20 收口后** | 文档（无代码） |
| 2 | **1.32** | `#111b` | **件 1·落地**（命令/协议 id 归属判据 ＋ 归属来源修正 ＋ 双腿 ＋ 存量清账） | → **由 1.31 写齐** | 1.31 | 壳 ＋ SDK |
| 3 | **1.33** | `#111c` | **件 2·设置键 · 评估**（宿主 `app.*` ＋ 宿主伪 pluginId 保护区 ＋ 存量 VS Code 共享名空间的裁决） | [03](03-任务-设置键归属评估.md) | 1.32 | 文档（无代码） |
| 4 | **1.34** | `#111d` | **件 2·落地**（设置键归属判据 ＋ 保护区 ＋ 双腿） | → **由 1.33 写齐** | 1.33 | 壳 ＋ SDK |
| 5 | **1.35** | `#111e` | **件 3·外观族 id · 评估**（五本 id 账 ＋ **双登记本** ＋ 持久化 id 处置 ＋ 宿主兜底 id 保护区） | [05](05-任务-外观族id归属评估.md) | 1.34 | 文档（无代码） |
| 6 | **1.36** | `#111f` | **件 3·落地**（外观族归属判据 ＋ 双腿 ＋ 双本账处置） | → **由 1.35 写齐** | 1.35 | 壳 ＋ SDK |
| 7 | **1.37** | `#111g` | **件 4·上下文旗子 · 评估**（宿主旗子全集生成式账 ＋ 活体样本处置 ＋ 插件旗子归属 ＋ 运行时仲裁） | [07](07-任务-上下文旗子归属评估.md) | 1.36 | 文档（无代码） |
| 8 | **1.38** | `#111h` | **件 4·落地**（旗子保护区判据 ＋ 双腿 ＋ `settings` 活体样本处置 ＋ 存量清账） | → **由 1.37 写齐** | 1.37 | 壳 ＋ SDK ＋ 插件 ×1 |
| 9 | **1.39** | `#111i` | **件 5·i18n 字典键与黄灯族 · 评估与定案**（跨仓不可判的边界定死 ＋ 台账 ＋ 只报不拦的腿形态） | [09](09-任务-i18n与黄灯族评估.md) | 1.38 | 文档（无代码） |
| 10 | **1.40** | `#111j` | **件 5·落地**（黄灯腿 ＋ 作者面两棵树 ＋ i18n 观察台账） | → **由 1.39 写齐** | 1.39 | 壳 ＋ SDK ＋ 文档 |
| 11 | **1.41** | `#111k` | **门禁与作者面收口**（**宿主保留名账**定稿进 SDK ＋ 两棵树作者面 ＋ **五轴发版** ＋ 端到端保真） | [11](11-任务-门禁与作者面收口.md) | 1.40 | SDK(npm) ＋ 文档 ＋ 壳 |
| 12 | **1.42** | `#111l` | 🔴 **本轴收口复核**（终态复核 ＋ 运行时零碰撞终态 ＋ 逐条改前/改后对账 ＋ **残余边界诚实清单** ＋ 给用户的报告） | [12](12-任务-本轴收口复核.md) | **全部** | 文档 |

**系列外（不占上表序，各自可插任意空档；⛔ 不许塞进 1.42）**：

| 轮 | 任务 | 出处 |
|:--|:--|:--|
| — | **E6#110** 作者轴黄灯「观察名单」补齐（原属 CSS 系列外账） | [插件规范化层/07](../../插件规范化层/07-任务-作者轴黄灯观察名单补齐.md) |
| — | 🔴 **键盘 chord 串被 `normalizeKey` 打乱 ⇒ 静默死键**（`ctrl+k ctrl+h` → `ctrl+h+k ctrl`） | 记忆 `keybinding-system-complete` 末节（**已立案、不属本轴**；本轴只登记，见 §六.7） |

---

## 五、收口的定义与判据（**1.42 执行**）

### 5.1 什么叫「收口」（三层同时闭合，缺一层都不算）

| 层 | 闭合条件 | 谁负责 |
|:--|:--|:--|
| **① 归属层**（名字自带归属 / 宿主名字有保留账） | 每条花名册都有**结构性的归属表达**：**宿主保留名账**（生成式 ＋ 双向对账 ＋ 进 SDK）＋ **插件归属前缀**。规则一句话可复述：**宿主保留的名字不许占；插件自己写的名字必须带 `<pluginId>`**。零手抄豁免表 | 1.31–1.40 |
| **② 仲裁层**（存量撞了伤害有限） | 跨仓不可静态判的、**存量不能改名**的（VS Code 共享设置键 · 第三方同 id）走**运行时归属仲裁**：**首撞者保留 ＋ 后来者降级不覆盖 ＋ fail-loud**。⇒ **残余风险如实登记，不许写成「已消除」** | 1.32–1.40 |
| **③ 证明层**（说得出为什么可信） | 每条新判据**带 `--self-test` 正负控**且**接线进 `npm run check`**、**负控真红**；并复核「新判据是否让已有门禁**变空转**」（输入清零后继续跑绿 = 假判据） | 全部各轮 ＋ 1.41 + 1.42 |

### 5.2 1.42 的收口判据（逐条可机械核）

1. **件 1–5 全绿**（每件有终态读数，且读数是**跑出来的**不是抄的）；
2. **静态判据全绿**：宿主保留名账 ↔ 实况**双向**对账绿 · 18 只插件仓 `npm run verify` 绿 · **壳仓全绿**；
3. **每条新判据带 `--self-test` 并接线**（`npm run check` 里能数出它们），且**负控真红过**（逐条留读数）；
4. **运行时仲裁有效性实证**：至少一次「塞入第二条同名注册 ⇒ 日志点名双方 ＋ 先者的值未被覆盖 ＋ 还原后逐字节相同」；
5. **逐条改前 / 改后对账**（本档 §二 的读数 vs 终态读数）；
6. **残余边界诚实清单**（§六）**逐条写进报告**，一条不许省；
7. `npm run check` **EXIT=0** ＋ 残留判据全 0（含 18 只插件仓）。

---

## 六、残余边界（诚实清单——**1.42 报告必带，一条不许省**）

> 这一节是**本方案与「宣布大获全胜」的分界线**。用户 2026-09-16 的原始追问就是「**是不是真收口了，还是还有根因藏着**」——答案写在下面。

1. **机制层未隔离**：本轴收口靠的是**归属约定 ＋ 门禁 ＋ 运行时仲裁**，不是**构造上不可能**（对照：视图 id 用复合键，那是构造性的）。⇒ 表述纪律：说「**事实上不会发生**」，不说「**不可能发生**」。
2. 🔴 **存量名字不改名（明文取舍）**：官方插件里**命令 id 21 个**（全在 `file-tree`，`explorer.*` 族）＋ **设置键 19 个**（`explorer.*` / `files.*` / `terminal.external.*` / `files.autoSave`）**今天不带本插件归属前缀**，而它们**照抄的是 VS Code 的共享命名空间**。**本轴判「不改名」**，理由两条都可判定：① 改名 = 用户 `settings.json` 键失效 + 设置页文案变 ⇒ **与「零 UI 变化」硬冲突**；② 这些键的**语义确实是全局概念**（`files.exclude` 不是「file-tree 的 exclude」）。⇒ 它们由 **B 仲裁**兜（首撞者保留 ＋ 出声），并**逐条登记**在总账里。
3. **静态门禁只看得见静态声明**：运行时用字符串拼出的 id（`` `${prefix}.${action}` ``）理论上可绕。⇒ 本轴**不加运行时拦截**，由 §八 的探针把这一条列为「**探针覆盖不到**」并记录已知形态。
4. **i18n 天然跨仓不可判**：键可以合法住在应用级字典（官方 `settings` 就是）⇒ **只黄灯 ＋ 文档**，**不许判红**。
5. **宿主保留名账需要一次「生成」且必须防漂移**：账是生成式的，但**宿主新增名字时若忘了重跑生成器**，账就陈旧。⇒ 对策 = **双向对账门禁**（照抄 `check-reserved-names-doc-sync.mjs` 的形状：账 ↔ 实况两侧都查，任一侧多出就红）。
6. **`pluginId` 唯一性依赖硬约束 11**（发布后 id 不可变）。⚠️ 本轴**新增一条同族账**：宿主伪 pluginId `app` / `appearance` / `update` **今天不设防**——第三方插件可以合法取名 `app`（它通过 `pattern`），此时与宿主**同 owner、连 warn 都不响**。**处置归 1.33/1.34**（候选：schema `pattern` 加禁 ＋ SDK `validate` 报点）；本条**先如实点名**。
7. **键盘 chord 串的静默死键不属本轴**（`normalizeKey` 把 `ctrl+k ctrl+h` 打乱成 `ctrl+h+k ctrl`，记忆 `keybinding-system-complete` 末节**已立案**）：它是**单方的解析缺陷**（不是「多方共写一本花名册」），**本轴只登记、不动手**。
8. **「已设防」的四处不被本轴改动**：数据推流唯一路径 · `pluginId` schema `ldk-` 禁令 · 视图 id 复合键 ＋ fail-loud · 快捷键冲突 UI 高亮。它们是**样板**，任何一轮若顺手去"统一风格"就是破坏。
9. **不回答「哪块宿主 UI 该变成插件」** —— 那是架构问题（另有主线）；本轴只把**名字边界**做成机械可判。**不许把「命名干净」当成「已经插件化」**。

---

## 七、禁区（逐格照抄）

**通用**：⛔ 一律改 `throw` · ⛔ 动已设防四处 · ⛔ i18n 判红 · ⛔ 白名单/基线/棘轮 · ⛔ 重构注册表或换数据结构 · ⛔ 动 `commitTokens` / `theme:changed` 广播链（[29 号档](../样式命名空间归一化/29-主题系统保护条款.md) P1）。

**新增三条（本档）**：
- ⛔ **不许把「门禁绿了」当成「问题没了」**——绿必须是**证明过的绿**（负控真红过）。
- ⛔ **不许压平残余边界**（§六 九条）——报告里写「已根除」而省略边界 = 造假账。
- ⛔ **不许把「加性扩展点」当缺陷治**（菜单槽 / 文件关联 / factoryRole / 快捷键 UI 高亮）——它们是**设计**。

**上位约束（每格禁区里都要写）**：
- **零 UI 变化**（除版本号；含**主题统一调控 / 字体 / 翻译**三条横切机制零回归）。
- **只做加法、不改已定型的东西** ⇒ **本轴定的名字/契约也会被当「终名」，要一次定对**。
- **[29-主题系统保护条款](../样式命名空间归一化/29-主题系统保护条款.md)**：冲突时以它为准。
- **对外宽容**：第三方可自画 / 可自定，只黄灯不拦；**跟随官方是奖赏不是枷锁**。

---

## 八、复量命令（本档全部读数可复跑）

```bash
# ① 本格总探针（本档 §二 全部读数来源；住 scratch/，.gitignore 内 ⇒ 不进仓）
node scratch/audit-nonnaming.mjs            # 官方 18 仓 + 壳仓 + 交集
LDK_PLUGINS_DIR=<别的容器> node scratch/audit-nonnaming.mjs
node scratch/audit-nonnaming.mjs --json     # 机读

# ② 五处已点名面（30 号档的点名，逐条复跑）
grep -n "被覆盖——原注册者" src/core/registry/commands/CommandRegistry.ts          # ① 命令 id warn
grep -n "已由插件" src/core/registry/ConfigurationRegistry.ts                      # ② 设置键 warn
grep -n "console.warn" src/core/registry/appearance/ThemeRegistry.ts src/core/registry/appearance/IconRegistry.ts src/core/registry/languages/LanguageRegistry.ts
sed -n '49,53p' src/core/api/linkdesk-api/ui.ts                                    # ④ contextKey.set 签名
sed -n '197,201p' src/core/services/plugins/IpcBridgeHandler/ui.ts                 # ④ 直通 setValue（零校验）
grep -n "addResourceBundle" src/pluginLoader/contributions/i18nResources.ts        # ⑤ i18n 深合并

# ③ 归属「从名字推」的三处（本格新发现）
grep -n "commandId.split" src/core/registry/commands/CommandRegistry.ts
grep -n "commandOwnerId" electron/preload-pool/commands.ts

# ④ 宿主保留面实况（§2.8 的账目来源）
grep -rhoP 'command:\s*"[a-zA-Z][^"]*"' src/core/commands --include=*.ts | sed 's/command: *"//;s/"//' | sed 's/\..*//' | sort | uniq -c | sort -rn
grep -rh '"app\.' src/App/config/*.ts | grep -oP '"app\.[a-zA-Z.]+"' | sort -u
grep -n 'APP_PLUGIN_ID = ' src/core/services/plugins/PluginStateService.ts
grep -rn 'registerConfiguration("appearance"\|registerConfiguration("update"' src/App/

# ⑤ 宿主旗子全集（§2.4 表逐条）
grep -rn "ContextKeyService.setValue(" src/ --include=*.ts --include=*.tsx | grep -v "\.test\."
grep -rn "contextKey[?.]*\.set" src/components/shared/
sed -n '426,430p' src/core/registry/commands/ContextKeyService.ts   # initCoreKeys 三键
grep -rn "contextKey?.set" E:/linkdesk-plugins/official/settings/src/views/SettingsView/SettingRow/gearMenu.ts

# ⑥ 样板面（§2.6 的「不同形」先例）
grep -n "viewKey\|撞名" src/core/services/layout/ViewContainerService.ts
grep -n "detectConflicts\|getConflictingBindings" src/core/registry/commands/KeybindingRegistry/registry.ts
grep -n "storage" src/core/api/linkdesk-api/surfaces.ts   # 期望：零命中（无 storage 面）

# ⑦ 归 SDK 下发的账（机制 A 的现成模板）
head -c 800 packages/plugin-sdk/schemas/reserved-class-names.json
grep -rn "RESERVED_FILE_REL" scripts/ packages/plugin-sdk/src/
```

> ⚠️ `scratch/` 在 `.gitignore` 内 ⇒ `audit-nonnaming.mjs` **不进仓**；丢了照本节的注释重写（本轴**不把静态探针提进 `scripts/`**——真门禁是各轮的 `--self-test`）。

---

## 九、纪律（每一棒进场先读这一节）

1. **一个 AI 会话只执行一格**；做完在 [交接.md](交接.md) **顶部**追加一段「给下一个 AI 的话」（**只追加不改旧段**），并把**剩余任务队列表整张复制**进新段、把自己那格改 ✅。
2. **收尾同笔四件**：① 交接段；② [E6-执行清单.md](../../E6-执行清单.md) 第 1 层的**轮次进度行**；③ 勾格要勾全套；④ 队列表整表复制。
3. 🔴 **每轮收尾跑一遍 §八 的探针并记账**（改前 / 改后读数）。
4. 🔴 **面清单只用来对账，不许当输入**——进场自己跑一遍（记忆 `ai-relay-one-round-per-session`；**面数字要写明口径：名数 / 站点数 / 行数**）。
5. 🔴 **不得静默不做**：任何一格若判「本格不做某条」，**必须把结论与理由写进交接段**。
6. 🔴 **发布纪律照旧**：链内不等待（用户 2026-09-15 授权）· 代理先测（`127.0.0.1:7890`）· 推完 `git ls-remote --heads origin <branch>` 对 SHA · npm 走 runbook 五步（**必须显式 `--registry=https://registry.npmjs.org`**）· ⛔ **撤版只能用户本人**。
7. 🔴 **提交只按路径 `git add <本格文件>`，禁止 `git add -A` / `git add .`**（壳仓随时有别的会话在途——本格开工时实测工作区躺着 CSS 系列的 30+ 个改动）。
8. 🔴 **1.42 恒为最后一格**（本轴的收口复核）。

---

## 十、原则对照（🔴 用户给的验收标准，逐条对账）

> 用户原话：「**总的来说，你的规划是要符合归一化、AI 友好化（多层含义，记忆中有）、无硬编码、无死代码、符合软件未来发展、高内聚、低耦合、可拓展、第三方易制造插件，符合我们软件的核心理念：万物皆可插件。**」
> ⇒ **逐条说清本方案怎么满足、哪里还不够**。凡「还不够」的，必须能指向一个**具体轮次**或一条**已登记残余边界**。

### 10.1 AI 友好化四层（记忆 `ai-friendliness-three-layers`）——本轴主攻**层 3**、兼顾**层 4**

| 层 | 内容 | 本轴怎么落 |
|:--:|:--|:--|
| 1 | 运行时操作（CDP / MCP 桥） | **不属本轴** |
| 2 | 结构化文件（`settings.json` 等声明层） | **不属本轴**（但本轴的设置键归属会改善它——键名可预测 = AI 写 `settings.json` 不必猜） |
| **3** | **工程可读**（高内聚低耦合 · 无硬编码 · 无死代码 · 规范化 · 代码结构化） | 🔴 **本轴主战场** —— 见 10.2 |
| **4** | **插件易造**（第三方永不需壳改代码；10 分钟写出首个插件） | **宿主保留名账进 SDK** ⇒ 作者（与其 AI）**读 SDK 的账就知道哪些名字不能占**，不必读宿主源码、不必问人；规则一句话：**「宿主保留的不许占；自己写的带 `<pluginId>`」** |

**核心公式（记忆原文）**：「你要的不是 AI 更聪明。你要的是**代码不给 AI 犯错的机会**。」⇒ 本轴在非样式面上的全部工作就是这句话的落地：**名字的归属不用猜 · 规则不用查表 · 判据自己会红**。

### 10.2 逐条对账（含如实标注「还不够」的）

| 原则 | 本方案怎么满足 | 还不够 / 边界 |
|:--|:--|:--|
| **归一化**（每个概念只有一种写法） | 归属的表达收敛成**两条机制**（A 账 ＋ B 仲裁），全轴复用；宿主保留面从「散在 3 处注册调用里」收敛成**一份生成式账** | 🔴 **主题仍有两本账**（`ThemeRegistry` ↔ `ThemeEngine/registry`）——归 **1.35 评估**（合不合并要过 §七 的「不许改机制」与 29 号档 P1） |
| **无硬编码** | 宿主保留名**生成式派生**（从注册调用实况读出），不手抄 | ⚠️ **生成器本身是一次硬编码风险**（宿主新增名字忘重跑）⇒ 对策 = **双向对账门禁**（§六.5） |
| **无死代码** | 1.31–1.40 各轮顺手盘「**因本轴而空转的判据**」并逐一处置（照 CSS 系列件 8 的做法） | ⚠️ 已知一条候选：`reserved-classes.ts` 的保留名腿在样式侧已空转（**已由 CSS 系列 1.28 处置**）——本轴新增的腿要**一开始就避免**这个形态 |
| **高内聚低耦合** | 归属**由构造表达**（复合键 / 前缀）＝ 高内聚的机械化；判定不靠人记 | ⚠️ **一处耦合成本如实登记**：改宿主保留面名 ⇒ 要同步 SDK 账 ＋ 两棵树 ⇒ 缓解 = 名字一次定对 ＋ 双向对账门禁 |
| **可拓展 · 符合未来发展** | 新插件**不需要登记任何表**；门槛从「记住名字」变成「遵守归属形态」；账是**生成式**的 ⇒ 宿主加新插件/新键**自动进账** | 门从 0 条变成 5 条轴 ⇒ 学习面变大（由 **SDK 腿 ＋ 脚手架模板 ＋ 作者面两棵树**承接，见 1.40 / 1.41） |
| **第三方易制造插件** | **宿主保留名账随 SDK 下发** ⇒ 作者仓**自己就能判**；严守宽容度模型（第三方侧默认黄灯 · 可自画 · 可自定 · **判红只覆盖「本仓可答 ＋ 有真害」**） | ⚠️ **前提**：作者面两棵树**必须同笔跟上**（1.40 已列为必做项） |
| **万物皆可插件（核心理念）** | 🔑 **归属的机械可判是它的前置条件**：只有「哪些名字属于宿主」机械可判之后，**将来把宿主某块 UI 抽成插件才是安全动作**（抽出去的每一块，名字必须与宿主划清）。另：**视图 id 的复合键模板证明了「构造性归属」在本仓可行** | ⚠️ **本轴不回答「哪块宿主 UI 该变成插件」**——那是架构问题（另有主线）。**不许把「命名干净」当成「已经插件化」** |

### 10.3 🔴 宽容度模型（记忆 `plugin-repo-gate-model` §十——本轴一切「判红」必须先过这一节）

**三条硬约束（照 22 号档 §10.3 原文）**：
1. **第三方侧的一切新判据，默认黄灯**；**判红必须逐条论证「① 本仓可答 ② 有真害（不是审美）」**——两条缺一即降黄灯。
2. **「自定颜色 / 自画组件」永远是允许的**；任何规则**不得**要求第三方必须消费 `var(--*)` 或必须用 `@linkdesk/ui`。**官方组件是奖赏，不是枷锁。** （本轴对称：**不得**要求第三方必须用官方命名空间以外的东西——反过来，**宿主保留面必须占**，这是真害不是审美。）
3. **红灯只留两类**：① **占用宿主保留面**（命令前缀 / 设置键 / 外观族 id / 上下文旗子）② **本仓可答的真害**。其余一律**黄灯 ＋ 迁移说明**。

### 10.4 🔴 上位约束一：**零 UI 变化**（用户 2026-09-16）

| 横切机制 | 今天的行为（必须与终态一致） |
|:--|:--|
| **主题统一调控** | 切主题 / 多配色（colorway）· 玻璃（模糊/饱和/不透明度）· 背景（panorama / mask）· 亮暗切换 |
| **字体** | `app.uiFontScale` 85–150 · 字体族（`--font-ui` / `--font-mono`）· `window.zoomLevel`（与字号正交双轴） |
| **翻译** | 中英切换（`t()` ＋ 语言包）· 插件自带 `contributes.i18n`（`lang-defaults` 插件） |

⇒ **这三条被提为 1.42 的显式验收节**，且**每一轮的实机判据都要带这三条**（不是只在收口时查）。
🔴 **本轴与「零 UI 变化」的两处真实张力（已定案，逐条登记）**：① **设置键改名 = 用户设置失效**（⇒ 存量不改名，走 B 仲裁，§六.2）；② **外观族 id 改名 = `app.theme` 指向他物**（⇒ 存量不改名，只保护宿主兜底 id，§2.3）。**这两条是本轴最容易被"顺手改干净"的地方，逐格禁区里点名写死。**

### 10.5 🔴 上位约束二：[主题系统保护条款](../样式命名空间归一化/29-主题系统保护条款.md)（冲突时以它为准）

用户 2026-09-16 点名：「**我费劲心思做的主题系统，各种玻璃，字体……若是不能变了，那简直对软件是毁灭打击。**」
⇒ **本轴只加归属，绝不冻结能力**。🔴 **本轴特有的高危动作（逐格禁止）**：
- **不许**因为「主题 id 归属」去改 `commitTokens` / `theme:changed` 广播链 / `preload-pool` 写池 `:root`（P1）；
- **不许**把 `contributes.themes` 的**配方键**（`colorway.colors`）当「影子契约」要求登记（29 号档 §二 明列：配方键**可以超出** `index.css`）；
- **不许**让归属规则妨碍「新增 token / 新玻璃参数 / 新字体档位 / 新主题能力 / 新配方键」——这些**全是加法，明确欢迎**。

### 10.6 一句话结论

> 本方案在**层 3 / 层 4** 上是「**能机械判的全机械判，不能的如实登记**」。**唯一的机制创新是「宿主保留名账」——而它不是发明，是把样式侧已经跑通的 `reserved-class-names.json` 模式推广到非样式面**。**唯一被你这串原则顶出来的取舍是「存量 VS Code 共享命名空间不改名」** —— 它属于**「改了更坏」**的那一类（用户配置失效 ＋ 可见变化），已在 §六.2 逐条论证并登记为**残余边界**。
