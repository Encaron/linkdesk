# 04 待抉择池 · AI 友好化-全自动操作

> 2026-09-14 立案（用户）。**状态：待拍板**——只登记设计，**未开工、零代码改动**。
> 归属：**04-软件更新 / 软件本体**（壳 API + 契约 + 壳文档 + 安装版与打包；零插件参与）。
> 本项按用户要求**建子夹而不是裸文件**（设计文档 + 增量 mockup ≥2 份 ⇒ 成套收口）。

| 文件 | 是什么 |
|:--|:--|
| [01-设计.md](./01-设计.md) | 设计文档：缘起现象（A1 场景 6 拖拽卡壳）· 四类缺口账（够不着/难驱动/难发现/读取面，带 file:line）· 三条判据 + **边界三条**（OS 层 / AI 是操作员非值守员 / 插件业务动作取决于作者）· 五模块解决方向（M1 读取面/M2 操作面/M3 手册/M4 MCP+CLI/M5 安装版）· 7 条待拍板 · 用户视角验收 |
| [mockups/01-设置页-AI接入分区.html](./mockups/01-设置页-AI接入分区.html) | 增量 mockup：在设置页既有骨架（`02-Electron架构/E5.8_归一化基建/外观主题化/mockups/02-设置页-主题区-重构-mockup.html`）基础上加「AI 接入」分区（MCP 通道 / CLI 通道 / 开放范围 / 连接日志） |

## 立案缘起（一句话账）

A1 实机批场景 6「设置标签页拖到主区右半边」自动化失败（事件全到、dragging 态有、但松手不分屏无玻璃预览），测试 AI 上报「纯自动化问题」，最终用户手动补拖 —— **用户质疑这是否就是 AI 不友好**。2026-09-14 四路调查（拖拽实现 / 全软件交互面 / API 面 / 安装包与安装版）结论 = **操作层整体健康、读取层半成品、知识沉淀空白**：标签页拖拽**有直通车**（`core.splitRight` 命令 / `pool.tabAction splitTab`），不友好在「没有时刻表告诉 AI 直通车在哪」；通知按钮剥掉 command/args、`notifications` 无读取面、`pool.onLayout` 状态快照未文档化、安装版更新重启丢 CDP 参数。用户拍板终局诉求：「**一个能够完全自动化的软件**」——本系列即其工程落点。

## 判据摘录（AI 友好四层定稿 · 层 1「运行时操作」，2026-09-14）

**可读**（界面每一处状态有契约级读取路径）· **可操作**（每个用户动作至少一条非鼠标路径；OS 够不着的列册）· **可查**（操作知识有单一真相源文档）。验收锚点 = 用户 toast 例：AI 应清楚通知「位置 / 因为什么 / 内容 / 几个按钮 / 按钮里有什么 / 该执行哪个 / 执行后做什么」。

## 与调查待拍板 P1–P5 的映射

| 调查待拍板 | 本系列模块 |
|:--|:--|
| P1 更新重启丢 CDP 参数 | **M5** |
| P2 读取面进契约 | **M1** |
| P3 AI 操作手册 | **M3** |
| P4 浮动面板移动/调高无替代 | **M2** |
| P5 方向 B（MCP 桥）重开 | **M4** |

## 依赖与碰撞

- **插件源码外移（E6 第七层，2026-09-14 已执行）**：插件内改动 = **跨仓批次**（壳仓 + N 个插件仓配对发版）；本子夹引用插件内文件一律写作 `<插件 id>:<仓内相对路径>`（真相源 = 各插件独立仓 `Encaron/linkdesk-plugin-<id>`，本机克隆 `E:\linkdesk-plugins\official\<id>\`）。
- **E6 同区**：M1 动的 `src/hooks/usePoolSync/notif.ts` / 通知面与 E6#57.x 更新批次同区——若那批在办，本项排其后、按新 HEAD 改。
- **方向 B 旧账**：M4 = 2026-08-25 暂缓的「AI 操作桥」重开；当时「花费巨大、不如 CLI」讨论未落盘（原话不可考）——**立项前重开这笔账**（记忆 `toolbox-sop §5.2` 已补注记）。
- **安全评估**：M4 涉及对外暴露白名单操作面，立项时与 `security-trust-assessment` 一并过。
- **流程**：按 04 README §一 —— 拍板要做才开 `v1.x-*/` 立项（此时才定版本号）；本子夹在被拍板前不占版本号。
