# 第 1.43 轮 · `#111n-2`：**官方仓清账 · `editor`**

> 🔄 **§〇c 口径修订（2026-09-16 第二次拍板）——先读 [00 档 §〇c 口径修订令](00-整理档案.md)。**
> **被撤回的旧结论**：~~存量不改名 ／ 只报不改~~ ⇒ **改名 ＋ 迁移，规则零例外**。**根治的机械定义 = 官方仓需改处全 0**（`node scripts/audit-plugin-scope.mjs` 全 0）。
> **上位约束的新措辞**：**操作体验零变化**（**名字可变，取值与功能不许变**）。
> **本档因此新增的必做项**：任何改名**同笔带迁移** ＋ **逐项操作实测**。
>
> **一句话**：`editor` 是**清账面最小的一格**（实测只需改 **1 个设置键 `files.autoSave`**），但它有两个**必须复核**的点：① **41 个文件关联扩展名**属「全局概念名」⇒ **不改**（本格要**实证并登记**，不是漏做）；② [1.42](12-任务-清账-file-tree.md) 要**还回**两个自称 `editor.*` 的命令 ⇒ 本格要确认**本仓没有同名声明**。
>
> **编号**：`#111n-2`
> **🔴 执行序与前置**：**前置 = [1.41 迁移基建](11-任务-迁移基建.md) 收官**。**与 [1.42](12-任务-清账-file-tree.md) 互不依赖**（各改各的仓）⇒ 次序可换；但**建议排在 1.42 之后**（1.42 会点名告诉本格 `editor.*` 那条交集的结论）。
> **拍板记录**：无新拍板；继承 §〇c 与 [1.35](05-任务-外观族id归属评估.md) 的三类「全局概念名」豁免口径。

> **非新能力声明（`check-design-flow` §8.4 ③）**：既有名字的归属归一（含**不改**的登记），零视觉变化、零功能变化、零新增能力。⇒ **无需新能力设计前置**。

---

## 〇、本格在轴上的位置

| 项 | 值 |
|:--|:--|
| 射程 | `editor` **一个仓**：设置键 · **文件关联的登记（不改）** · **`editor.*` 前缀的归属确认** |
| 不射程 | `file-tree`（归 1.42）· 别的仓（1.44–1.48）· 壳侧判据与账 |
| 产出 | 改名 ＋ 迁移 ＋ 实证 ＋ 发版 ＋ 目录/种子对账 ＋ **「文件关联不改」的实证登记** |

---

## 一、事实与读数（**2026-09-16 实测**）

| 项 | 读数 | 出处 / 复跑 |
|:--|:--|:--|
| **设置键不合规** | **1**：`files.autoSave` | `node scripts/audit-plugin-scope.mjs`（`── editor` 段） |
| 设置键合规 | **25**（`editor.*` 前缀，已合规） | 同上 |
| 命令 id | **2**，**全部合规**（`editor.*`） | 同上 |
| **文件关联** | **45 个扩展名**（⚠️ **本档原写 41，2026-09-17 复量翻正**，见 §1.2）：`ts` `tsx` `js` `jsx` `mjs` `cjs` `json` `jsonc` `html` `htm` `css` `scss` `less` `md` `mdx` `py` `rs` `c` `h` `cpp` `hpp` `go` `java` `xml` `svg` `yaml` `yml` `toml` `sh` `bash` `sql` `lua` `php` `rb` `swift` `kt` `dart` `diff` `patch` `bat` `cmd` `ini` `cfg` `txt` `log` | 同上 |
| i18n 顶层键 | **47**（跨仓不可判，**只登记**） | 同上 |
| 旗子 | **0**（本仓不设 context key） | 同上 |

### 1.1 🔴 「文件关联」为什么**不改**（本格要给结论并留读数，不是默默跳过）

1. **它是全局概念名**：`.ts` 不可能是「editor 的 ts」，`.md` 不可能是「editor 的 md」——**加前缀 = 语义错误**（[00 §〇c.2](00-整理档案.md) 三类全局概念名之一）。
2. **设计上就是多方共用**：[`FileAssociationService`](../../../../../src/core/services/files/FileAssociationService.ts) 的注释明写「**同一扩展名允许多个插件注册——"打开方式…"选择器消费全部**」（`FileAssociationService.ts:36-40`）⇒ 它**不是覆盖型花名册，是加性扩展点**。
3. 🔴 **实测佐证**：`python` 插件（见 [1.48](18-任务-清账-其余插件.md)）**也声明语言定义扩展名**（`.py` / `.pyi`）⇒ 同一类"全局概念名"在两只仓里都出现，**改名必然两家都要改、且改完更错**。
⇒ **本格结论：不改 ＋ 登记为「明文的全局概念名」**（写进交接段与 1.49 的作者面说明）。

### 1.2 ⚠️ 41 → 45 的**档内订正**（2026-09-17 清账实测，如实记）

- **原读数 41** 是 2026-09-16 抄档时的数；**今日复量 = 45**（`node scripts/audit-plugin-scope.mjs` 的 `── editor` 段直接列出 45 个扩展名，逐条可数）。
- 🔴 **订正只在本档，别处不动**：本轴正典与判据都**不消费这个数字**（文件关联**按设计不改名**，条数不进任何门禁）⇒ 它**纯属事实陈述**，改了不牵连任何结论。
- ⚠️ **教训（本轴第二次）**：**抄来的数字会腐烂**。凡写进档的读数，**写的时候附可复跑命令**（本行已附）；下一棒复量若不符，**以复跑命令的输出为准并当场翻正**——[1.48](18-任务-清账-其余插件.md) §1.3 同一天抓到的是同一类问题的另一半（**探针漏计**，那个是真缺陷）。

---

## 二、要做（**含「同笔面」**）

### 2.1 改名（只有 1 处）

- `files.autoSave` ⇒ **按 [1.31](01-任务-命令id归属评估.md)/[1.33](03-任务-设置键归属评估.md) 定的统一形状**（例：`editor.files.autoSave`）；**形状以那两件的结论为准，本格不许自创**。
- 🔴 **引用点要扫全**：`getConfigurationValue("files.autoSave")` / `useConfiguration` / `inspectConfiguration` / `configurationDefaults` / 本仓 TSX 渲染点 / 本仓 `plugin.json`。

### 2.2 迁移（用 [1.41](11-任务-迁移基建.md) 的，不自己写）

- 用户在 `settings.json` 里若设过 `files.autoSave` ⇒ **值要搬到新名**（走 `registerConfigMigration`）。
- 🔴 **presence 自查**：没设过就不写（零变更零写）。

### 2.3 `editor.*` 前缀的归属确认（**与 [1.42](12-任务-清账-file-tree.md) 的交集**）

- 核 **本仓有没有声明/注册** `selectForCompare` / `compareWithSelected`（1.42 说它由 `file-tree` 注册）。
- 🔴 **两种结论都要写**：① 本仓**没有** ⇒ 那两条是 `file-tree` 的，1.42 还回 `file-tree.*` 后**本仓零动作**；② 本仓**有** ⇒ 与 1.42 **协调**（谁留下、谁改名），**不许两边各改一半**。
- ⚠️ 同时核：本仓是否**引用**过 `file-tree.*` 的名字（反向借用）——照 1.42 §1.2 的同一条纪律。

### 2.4 「不改」的实证登记（§1.1）

- 给出**读数为证**：`contributes.fileAssociations` 41 条**逐条未改** ＋ `python` 仓同类的对照 ＋ `FileAssociationService` 的加性设计出处。
- 🔴 **登记去向**：[1.49](19-任务-门禁与作者面收口.md) 的作者面「三类全局概念名」段（本格只需把材料交上去）。

### 2.5 版本与发布（**同笔**）

`package.json` ＋ `plugin.json` ＋ `CHANGELOG.md` ＋ `AGENTS.md` 版本事实 ⇒ bump ⇒ `verify` / `test` / `build` ⇒ 发版 ⇒ 官方目录回填 ⇒ 出厂种子（**本仓是否 `seed: true` 以 `bundled-plugins.lock.json` 为准，实测后写交接段**）。

---

## 三、判据表（**含必须真跑过的负控**）

| 判据 | 要求 | 负控（**必须真能红**） |
|:--|:--|:--|
| **① 形状证明** | 改的文件剥前缀后与基线 blob **逐字节相等** | 拿 HEAD 冒充 new ⇒ 红；改一处非名字内容 ⇒ 红且只红那一个文件 |
| **② 残留 0** | 本仓 `src/` 面旧名 `files.autoSave` 整词 0（**写明扫描域**；`CHANGELOG.md` 历史记录按**具名例外**报数，不许改写） | 留一处 ⇒ 红；正控：新名计数 > 0 |
| **③ 迁移实证** | 造「旧名有值」的 `settings.json` ⇒ 新名下读得到同值 ＋ 该项仍能切换 ＋ 旧键已清 | 迁移改恒真 ⇒ 断言红 |
| **④ 41 个文件关联未改** | 逐条比对改名前后的扩展名集合**相等** | 断言集合相等（**这一条是"没漏做也没多改"的机械证明**） |
| **⑤ `editor.*` 归属确认有结论** | ① 或 ② 之一 ＋ 出处 | — |
| **⑥ 仓门禁** | `verify` EXIT=0 · `test` 全绿 · `build` 通过 · `audit:plugin-prefix` 零违规 | — |
| **⑦ 壳侧账** | `check:bundled-freshness` 读数（按本仓 `seed` 实况）· 官方目录读回 sha 逐字节对 | — |
| **⑧ 不误伤** | `pluginId` 仍 `editor`；41 个扩展名；`views`/`viewsContainers` 未动 | 断言逐项相等 |

---

## 四、禁区

- 🔴 **不许给 41 个文件关联扩展名加前缀**（全局概念名 ⇒ 加前缀 = 语义错误；[00 §〇c.2](00-整理档案.md)）。
- 🔴 **不许为了"改干净"顺手删/改任何东西**（死设置项、死命令一律**只登记**）。
- 🔴 **不许改写 `CHANGELOG.md` 的历史**（旧名出现在历史里 = 具名例外）。
- 🔴 **不许动 CSS 类名**（已由 CSS 系列 1.14③ 清账）。
- 🔴 **不许引白名单 / 基线 / 棘轮**（映射表是一次性迁移数据）。
- 🔴 **不许改 `pluginId`**（硬约束 11）。
- 🔴 **不许自己造改名工具 / 形状证明**（用 [1.41](11-任务-迁移基建.md) 的作业包——**同源是判据的一部分**）。
- ⚠️ **上位约束**：**操作体验零变化**（名字可变，取值与功能不许变）· 只做加法且名字一次定对 · [29 号档](../样式命名空间归一化/29-主题系统保护条款.md) 优先 · 对外宽容。
- ⚠️ 照旧：不跳 `npm run check` · 推前现测代理 · 推完 `ls-remote` 对 SHA · **提交只按路径 `git add`**。
- ⚠️ 🔴 **发布前置两条**：**工作区干净** ＋ **本地 HEAD 已在远端**（`publish` 先推后发）；发布成功后 API 会往插件仓根提交 `marketplace.json` ⇒ `git pull --ff-only` 跟上。

---

## 五、交棒要求

1. **收尾四件套**（交接段顶部追加 · 清单轮次进度行 · 勾格全套 · **队列表整张复制**）。
2. 🔴 **回报三组读数**：① 改名前/后 `node scripts/audit-plugin-scope.mjs` 的**本仓处数**（期望 1 → **0**）；② §三 的 ①②③④ 判据读数；③ **§2.3 的 `editor.*` 归属结论**（这一条 1.42 与本格都关心）。
3. 🔴 **判「本格不做某条」必须写明理由**（尤其「41 个文件关联不改」的实证）。
4. **下一棒 = [1.44 清账 · `serial-monitor`](14-任务-清账-serial-monitor.md)（`#111n-3`）**。
5. 跑 [00 档 §八](00-整理档案.md) 探针并记账。

---

## 六、🔴 进场第一步（命令）

```bash
# 0) 确认自己是哪一棒
sed -n '1,40p' "docs/02-Electron架构/E6_插件生态与发布/01-插件独立构建/非样式命名空间归一化/交接.md"

# 1) 拿「清账轮作业包」（1.41 交付）→ 交接段顶部第一段；以及 1.42 关于 editor.* 的点名

# 2) 自量本仓
node scripts/audit-plugin-scope.mjs | sed -n '/── editor/,/^──/p'
node scratch/show-plugin-manifest.mjs "E:/linkdesk-plugins/official/editor"

# 3) 那一个键的引用点扫全
grep -rn "files.autoSave" "E:/linkdesk-plugins/official/editor/" --include=*.ts --include=*.tsx --include=*.json | grep -v node_modules

# 4) 文件关联的加性设计出处（「不改」的判据依据）
sed -n '30,45p' src/core/services/files/FileAssociationService.ts

# 5) 基线
git -C "E:/linkdesk-plugins/official/editor" rev-parse HEAD
git -C "E:/linkdesk-plugins/official/editor" status --short   # 必须干净

# 6) 收尾（禁跳）
npm run check
```

---

## 七、与其它格的关系

- **[1.41 迁移基建](11-任务-迁移基建.md)（紧接前序）**：🔴 **依赖它的作业包**。
- **[1.42 清账 · `file-tree`](12-任务-清账-file-tree.md)**：🔴 **交集 = `editor.*` 前缀**（它借了两个自称 `editor.*` 的命令）；本格的 §2.3 就是对这个交集的收口。
- **[1.33/1.34（设置键）](03-任务-设置键归属评估.md)**：本仓那 1 个键的映射与命名形状从它来。
- **[1.48 清账 · 其余插件](18-任务-清账-其余插件.md)**：🔴 **`python` 仓声明了语言定义扩展名（`.py`/`.pyi`）** ⇒ 与本节「全局概念名不改」是**同一类**，两格的结论必须**一致**（1.48 会引本格的登记）。
- **[1.49 门禁与作者面收口](19-任务-门禁与作者面收口.md)**：作者面「三类全局概念名」段要本格交上去的材料。
