# 插件源码外移层——AI 接力交接

> **用途（2026-09-14 用户拍板）：一个 AI 会话只执行一个轮次。** 每个 AI 做完自己那一轮，就在本文件**顶部**追加一段「给下一个 AI 的话」；用户把那段话**整段复制**给下一个 AI 当进场上下文即可。
> **段结构（固定四点）：** ⑴ 第七层是干什么的 → ⑵ 各轮次是干什么的 → ⑶ 重点介绍下一个 AI 即将执行的轮次 → ⑷ 提醒：同笔续写 [E6-执行清单.md 第 7 层头的关键词块](../E6-执行清单.md)。
> **规矩：只追加不改旧段**（最新在顶）；旧段是被执行轮次的历史交接，留着对账。
>
> **非新能力声明（`check-design-flow` §8.4 ③）：** 本档是**跨轮交接说明与执行实录**，不含新能力设计——文中出现的 `contributes.` / `window.linkdesk.` 均为**引用既有插件 manifest 字段与既有公开 API**（其设计前置在各自既定轮次里早已落笔），本档只是把它们**记下来 / 验一遍**。⇒ **无需新能力设计前置**。

---

## → 给执行 7.6 脚手架 git init（E6#103）的 AI（交接人 = 执行 7.5 的 AI，2026-09-14）

**进场第一步**：读本段 → 按第 ⑷ 点核对清单层头关键词块 → 打开 [07-脚手架与工作区.md](07-脚手架与工作区.md) 照格执行（`cargo new` 三语义 + `--no-git` 退路 + 容器目录落位 + 「容器不建仓」红线）。工作纪律照 memory `ai-workflow-canon`（审计→报告→放行→执行）＋ `ai-relay-one-round-per-session`（一会话一轮）＋ 收尾四件套（`npm run check` 全绿 / 清单勾格记账 / 版本判定 / 记忆指针）。

**🧭 现场状态（开工前先知道「什么已提交、什么已推、什么外发过」）**：

| 东西 | 在哪 | 状态 |
|:--|:--|:--|
| **18 个插件仓** | GitHub `Encaron/linkdesk-plugin-<id>` ＋ 本地 `E:\linkdesk-plugins\official\<id>` | 🛑 **本地各多一笔 `ci:` 提交（未推！）**——7.5 给每仓加了 `ci.yml` / `ci-verify.mjs`（6 只有源码的 + `tsconfig.json`，5 只有测试的 + vitest 两件）。**`origin/main` 还停在 7.3 那一版**，18 仓的 CI **一次都没跑过** |
| **壳仓 `e6` 分支** | GitHub `Encaron/linkdesk` | 🛑 **7.5 的提交没推**——本地领先 `origin/e6`（`origin/e6` = `2998bbefe`）。**本轮的提交回到「不推」等待态**（红线②） |
| **npm 作者包** | registry.npmjs.org | ⚠️ **本轮一件都没发**。`create-linkdesk-plugin` 的模板改了 4 件 ⇒ `check-npm-release` **黄灯**（内容漂移、版本没动）——**按原判与 7.6 的脚手架改动合并成一笔版本**（`#103c`），别分成两次发版 |
| **软件版本** | `package.json` = 0.1.63 | ✅ **未变**（本轮零 `src/`/`electron/` 改动 —— 脚手架模板与壳脚本都不在软件侧） |
| **箱内出厂种子 / 官方目录** | `bundled-plugins/`（6 只）+ `Encaron/linkdesk-marketplace`（20 条） | ✅ 未动（7.4 收箱后的状态，本轮零触碰） |
| 7.5 的读数与日志 | `E:\ldk-l7-scratch\` | `102-verify3.log`（18 仓严格门禁逐仓输出）· `102-tests2.log`（5 仓 370 例）· `102-shell-check.log`（壳仓 check 全绿）· `neg1..neg7-*.log`（7 处负控）· `102-npm-install.log` |

> 🔴 **要推插件仓 / 壳仓时**：一律带代理（`git -c http.proxy=http://127.0.0.1:7890 -c https.proxy=http://127.0.0.1:7890 push`）；**先问用户**。插件仓推完**必须回看一次 Actions 结论**——「18 仓 CI 真跑过一次」是 7.5 判据里唯一还没销的那一条。

### ⑴ 第七层是干什么的

**壳与插件源码从此分家**（现状：**已分家、已上架、已收录、出厂种子会自己保鲜、每仓门禁也装上了**）。插件源码住在各自独立的 GitHub 仓，壳仓只留两样：**构建产物**（`bundled-plugins/<id>.linkdesk-plugin`，只装基础插件那 6 只）＋ **文档档案**。真正要解的痛点是**「没上架」**——用户不敢卸载任何插件，根因是卸了没处拿回来；7.2 给了「一插件一仓」、7.3 给了「publish + 收录进官方目录」、7.4 让箱子里的种子自动跟到各插件最新已发布版、**7.5 补上了最后一块「搬出去不等于脱管」：每只插件仓自带自己的 CI 与严格门禁，脚手架造出来的新插件也默认自带**。现状实测与全部决策住 [00-整理档案.md](00-整理档案.md)；执行序 = L5 剩余做完紧接着 L7、L4/L6 跳过——**本层是 E6 当前唯一在跑的层**。

### ⑵ 各轮次是干什么的

| 轮 | 任务 | 一句话 |
|:--|:--|:--|
| 7.0 | #97 判据先立 | ✅ 2026-09-14——五处落点把「源码住在哪 / 身份从哪来 / 版本号几处同源」写成正典 |
| 7.1 | #98 出口打通 | ✅ 2026-09-14——六只插件的依赖/构建自足 ＋ `@src` 测试解耦 ＋ SDK `pack` 通道 ＋ 身份显式化 |
| 7.2 | #99 逐个迁移 | ✅ 2026-09-14——18 只源码外移各自独立仓并推送；壳仓 `plugins/` 只剩两只夹具 |
| 7.3 | #100 上架两步 | ✅ 2026-09-14——18/18 publish 三产物全绿 ＋ 官方目录收录（20 条）＋ 装机验收两阶全绿 |
| 7.4 | #101 出厂种子保鲜 | ✅ 2026-09-14——机制 `sync:bundled` ＋ 账 `bundled-plugins.lock.json` ＋ 门禁 `check-bundled-freshness`；出厂随包收敛为 **6 只** |
| **7.5** | **#102 门禁与 CI** | ✅ **2026-09-14 收官（本地全绿，外发待点头）**——**18 仓各自自带 CI ＋ 严格门禁**（`ci.yml` + `scripts/ci-verify.mjs` 四段）＋ 测试基建随插件走（5 仓 25 文件 / 370 例）＋ **preset 两条缺口核清**＋ 脚手架模板同四件 ＋ 发布清单跨仓四步。📖 [06 §六 §七](06-门禁与CI.md) |
| **7.6** | **#103 脚手架 git init** | **`create-linkdesk-plugin` 照抄 `cargo new` 三语义 ＋ `--no-git` 退路 ＋ 本地容器目录**；🔴 **与 7.5 的模板改动合并成一笔 npm 版本** |
| 7.8 | #105 作者面文档收口 | 🔵 编号是插入序，**执行序在 7.7 之前** |
| 7.7 | #104 全层验收 | 七条判据（[00 §五](00-整理档案.md)）＋ 回归对照（**官方插件 18 只不变 / 随包 6 只**） |

### ⑶ 重点介绍你即将执行的轮次（7.6 脚手架 git init 与本地工作区）

**详案**：[07-脚手架与工作区.md](07-脚手架与工作区.md)。**对应你的两句话**：①「别忘了我在其中提了一点，就是给我们的脚手架加一个 Git 仓库」——现状实测：模板无 `.git`、`index.js` 无 `git init`，作者靠 `publish.ts:226` 报错手敲三条命令；②「不应该全部都平铺，应该造一个文件夹」——容器目录落位 + **「容器本身绝不建仓」**红线。

**🔴 你拿到的最重要一条牌（7.5 交下来的，会直接改你的工作量）**：

1. 🔴 **模板已经改过 4 件，你必须与它们合并成**一笔** `create-linkdesk-plugin` 版本**（`#103c` 原判 + 7.5 实测）：
   - 7.5 新增：`.github/workflows/ci.yml` · `scripts/ci-verify.mjs` · `vitest.config.ts` · `vitest.setup.ts`（＋ `package.json` 的 `verify` / `test` 两条 script 与 4 个 devDeps）；
   - 同时改了**两条生成物契约**（在 `scripts/check-scaffold.mjs` 里）：`scripts` 契约 5 条 → **7 条**（+`verify`/`test`）；占位符残留扫描**对 `.github/**` 开了口子**（GitHub Actions 的 `${{ … }}` 与脚手架占位符同形，不开这个口子，模板里一句合法的 workflow 注释就被判成「未替换的占位符」）。
   - ⇒ **`check-npm-release` 现在是黄灯**（内容漂移、版本没动），它就是你该发版的信号，不是异常。
2. ⚠️ **`check-scaffold.mjs` 的断言 2 与硬约束 11 现在是相反的**（7.5 登记、**刻意没动**，属你的契约）：断言 2 仍**禁止**模板写 `pluginId`（理由写着「E6#94 已删该字段」），而 `#98g` 之后 `pluginId` 已进 schema、官方 20 只全显式声明、缺声明只打黄灯 ⇒ **脚手架生成的插件永远缺 pluginId**。你加 `git init` 断言（`#103b`）时同笔把这条理顺（改断言或改模板，二选一，并写明依据）。
3. **容器的三处现成参考**：`E:\linkdesk-plugins\official\`（容器里有 `README.md` 写明「容器本身绝不建仓」）· `scripts/sync-plugin-ci.mjs`（7.5 的批量同步小工具——详案 §「批量同步小工具」要的东西，可以照它的形做）· memory `dev-artifact-hygiene`（构建/验证产物只落 `E:\linkdesk-build+scratch\`）。

**📌 7.5 留给你的账（都已登记在 [06 §七](06-门禁与CI.md)，别在 7.6 顺手做——各有正主）**：

| 账 | 归谁 | 一句话 |
|:--|:--|:--|
| 🔴 **`no-cross-plugin-import` 与严格腿「补进 SDK preset」** | 下次 SDK 发版 | 本轮是**内联进插件仓**的（npm 侧要发新版才到得了作者手里，而外发要用户点头）。等 SDK 因别的原因发版时同笔收掉，届时各仓 `ci-verify.mjs` 可瘦身 |
| **`vitest.setup.ts` 由 SDK 提供共享版本** | 同上 | 今天 7 份逐字副本（壳仓 1 + 模板 1 + 5 仓），共享版落地后各仓改成一行 re-export |
| **SDK `lintFiles` 在「全无 ts/tsx」时崩栈（`AllFilesIgnoredError`）** | 同上 | 纯数据插件跑 `linkdesk-plugin-sdk lint` 会崩——第三方数据插件作者同样会撞 |
| **壳仓专有规则名随源码搬进插件仓** | 同上 | 实测 1 处（serial-monitor 的 `no-module-level-ipc-listener`），eslint 报 fatal；本轮**报告不拦** |
| **存量 spacing/font-scale 偏离 36 处** | 随插件下次发版 | marketplace 19 / serial-monitor 11 / settings 5（+1 字体）。审美校准档，动它要走「改→bump→build→publish→收录→刷种子」整条链 |
| **`create-linkdesk-plugin` 的版本与发布** | **7.6（你）** | 见上面第 1 条 |
| **`check-scaffold` 断言 2 vs 硬约束 11** | **7.6（你）** | 见上面第 2 条 |
| **壳仓三道「真空绿」门禁**（`check-theme-schema` / `check-lsp-deps` 第一源 / `check-plugin-version-sync`） | 7.7（#104e） | 各仓 `verify` 的 ④ 已覆盖 18 只的声明自洽；壳仓那三处「无对象 ≠ 都合规」的结论行归 7.7 复核 |

**顺手留意（7.5 立的新事实，你一定会撞上）**：

- 🔴 **推送后第一件事：看 Actions**——18 仓 CI 是**这一轮唯一还没验的判据**。若某仓红了，先看是不是 `npm ci` 的 lock 问题（7.5 已用 `--registry=https://registry.npmjs.org` 更过 lock，5750 条 resolved 全是官方源，0 条镜像）。
- **插件仓的 `verify` 判红范围是收窄过的**（lint 规则腿 + css 硬编码腿 + 跨插件 + 字典 + 声明自洽），字号/间距两条腿与「引用了不存在的规则名」那一类**只报告不拦**——理由逐条在 `ci-verify.mjs` 头注与 [06 §六·二](06-门禁与CI.md)。**别把它改成「全判红」**：存量 36 处偏离会让 18 仓当场变红，而修它们要动插件源码（发版链）。
- **别把 `settings` 当侧栏插件改**：它的 `location="auxiliarybar"` 是设计使然（壳侧两处单测 + 本仓 `verify` 的 ④ 都钉着）。
- **`--user-data-dir` 的路径必须加引号**（Git Bash 会吃掉反斜杠）；**NSIS 升级路径会先静默卸载同 appId 旧版**（7.4 实测删掉过用户的 `D:\01link\LinkDesk`）——要装机验收先问用户或先备份。
- **验收要留读数**：写清「哪只、哪条路径、看到什么数字」。读 UI 状态要认**可见实例**（池里 `.side-panel` 有多份，隐藏的也留在 DOM）。

### ⑷ 续写清单层头关键词

收尾时同笔更新 [E6-执行清单.md 第 7 层头的「轮次进度」行](../E6-执行清单.md)（7.6 ✅ → ▶ 7.8），并在本文件顶部追加你的「给执行 7.8 的 AI」一段（四点结构不变）。⚠️ **编号 ≠ 执行序**：7.8（作者面文档收口）在 7.7（全层验收）**之前**跑。

---

## → 给执行 7.5 门禁与 CI（E6#102）的 AI（交接人 = 执行 7.4 的 AI，2026-09-14）

> ✅ **本段对应的轮次已于 2026-09-14 执行完毕（本地全绿；外发动作一件没做，等用户点头）**——原文一字未改留作对账（顶段落里写清了实测结论、7 处负控读数与留给 7.6 的八条账）。以下为当轮交接原文。

**进场第一步**：读本段 → 按第 ⑷ 点核对清单层头关键词块 → 打开 [06-门禁与CI.md](06-门禁与CI.md) 照格执行（掉出去的六类检查 + 每仓 CI 模板 + 25 个测试文件的运行环境）。工作纪律照 memory `ai-workflow-canon`（审计→报告→放行→执行）＋ `ai-relay-one-round-per-session`（一会话一轮）＋ 收尾四件套（`npm run check` 全绿 / 清单勾格记账 / 版本判定 / 记忆指针）。

**🧭 现场状态（开工前先知道「什么已推、什么只在本地」）**：

| 东西 | 在哪 | 状态 |
|:--|:--|:--|
| **18 个插件仓** | GitHub `Encaron/linkdesk-plugin-<id>` | ✅ 已推；**settings 多了一版**：`v1.0.7`（本轮验收版，源码零改动——见下「顺手留意」第 6 条） |
| **官方目录** | `Encaron/linkdesk-marketplace` | ✅ 已收录 18 条 + 既有 2 条 = 20 条；本轮追加一笔 `2e3b8cf`（只把 settings 刷到 1.0.7，其余 19 条逐字节未动） |
| **壳仓 `e6` 分支** | GitHub `Encaron/linkdesk` | ✅ **已推**（用户 2026-09-14 委托发版时放行）——`origin/e6` = 本地 = `2998bbefe`，**未推 0 笔**。🛑 **但本发之后的提交回到「不推」等待态**（惯例：发布之后的提交要再等用户发话） |
| **软件 0.1.63** | GitHub Release `v0.1.63` | ✅ **已发布上线**（2026-09-14）——CI run `34806086080` success、三件资产齐（exe 120,267,633 B / latest.yml / blockmap）、`/releases/latest` 已翻档、正文 = 下载信息块 + CHANGELOG 段。用户已自行装回 `D:\01link\LinkDesk` |
| **箱内出厂种子** | `bundled-plugins/`（6 个 zip）+ 账 `bundled-plugins.lock.json`（18 条） | ✅ 7.4 轮收箱：**箱内 18 → 6**（基础插件）；账仍覆盖官方全集 18 条 |
| 插件源码备份 | `E:\linkdesk-l7-backup.git`（mirror，fsck 过） | 本机 |
| 过渡期脚本与读数 | `E:\ldk-l7-scratch\` | 仓外。**7.4 新增**：`ud-101-e2e/`（一次性空 profile，验收用的那份）· `e2e-boot2.log`（boot 自动装 6 只的原始日志）· `e2e-boot.log`（第一次那份，路径没加引号落错地方，留作教训） |
| 打包产物 | `E:\linkdesk-build\` | `linkdesk-setup-0.1.63.exe` + `win-unpacked/`。⚠️ **旧的 `linkdesk-setup-0.1.61.exe` 已移进 `retained/`**——因为 `assert:installer-name` 要求输出目录顶层**只有一个 exe**（一跑一清是它的规矩，别把它当门禁 bug） |

> 🔴 **要推壳仓时**：`git -c http.proxy=http://127.0.0.1:7890 -c https.proxy=http://127.0.0.1:7890 push origin e6`（**凡搬字节的一律带代理**）；**先问用户**（发布之后尤其——那时提交已回到「不推」等待态）。
>
> ⚠️ **本机做「干净环境装机验收」前必读**（7.4 轮实测教训）：NSIS 安装器走**升级路径**——即便用 `/D` 指定了别的安装目录，它也会**先静默卸载已注册的同一 appId 旧版**。实测后果：装到 E 盘的同时，用户的 `D:\01link\LinkDesk`（0.1.61）**整个被删掉**（连目录都没了），开始菜单快捷方式也被卸载器删除。**用户数据（`%APPDATA%\LinkDesk`）不受影响**（插件数据在 userData，不在安装目录）。⇒ 要么先问用户「旧安装版能不能被移除」，要么先备份，别以为 `/D` 能让新旧并存。

### ⑴ 第七层是干什么的

**壳与插件源码从此分家**（现状：**已分家、已上架、已收录、出厂种子已会自己保鲜**）。插件源码住在各自独立的 GitHub 仓，壳仓只留两样：**构建产物**（`bundled-plugins/<id>.linkdesk-plugin`，只装基础插件那 6 只）＋ **文档档案**。真正要解的痛点是**「没上架」**——用户不敢卸载任何插件，根因是卸了没处拿回来；7.2 给了「一插件一仓」、7.3 给了「publish + 收录进官方目录」，**7.4 补上了最后一块：箱子里的种子会自动跟到各插件的最新已发布版**，而且**出厂只随包基础插件**（卸了随时从市场装回来，所以「一个都不敢卸」这件事从「没有退路」变成了「随时能回来」）。现状实测与全部决策住 [00-整理档案.md](00-整理档案.md)；执行序 = L5 剩余做完紧接着 L7、L4/L6 跳过——**本层是 E6 当前唯一在跑的层**。

### ⑵ 各轮次是干什么的

| 轮 | 任务 | 一句话 |
|:--|:--|:--|
| 7.0 | #97 判据先立 | ✅ 2026-09-14——五处落点把「源码住在哪 / 身份从哪来 / 版本号几处同源」写成正典 |
| 7.1 | #98 出口打通 | ✅ 2026-09-14——六只插件的依赖/构建自足 ＋ `@src` 测试解耦 ＋ SDK `pack` 通道 ＋ 身份显式化 |
| 7.2 | #99 逐个迁移 | ✅ 2026-09-14——18 只源码外移各自独立仓并推送；壳仓 `plugins/` 只剩两只夹具 |
| 7.3 | #100 上架两步 | ✅ 2026-09-14——18/18 publish 三产物全绿 ＋ 官方目录收录（20 条）＋ 装机验收两阶全绿 ＋ 卸载安全/更新通道实证 |
| **7.4** | **#101 出厂种子保鲜** | ✅ **2026-09-14 收官**——**机制（`sync:bundled` 三档）＋ 账（`bundled-plugins.lock.json`）＋ 门禁（`check-bundled-freshness`）三件一起上**；🔴 **出厂随包收敛为 6 只基础插件**；**端到端本命真跑通**（settings 1.0.6→1.0.7 → 收录 → `sync --latest` → 打包 → 干净 profile 读数 = 1.0.7）；`pack-bundled-plugins.mjs` 退休。📖 [05 §七](05-出厂种子保鲜.md) |
| **7.5** | **#102 门禁与 CI** | **每只插件仓自带 CI**（lint/validate/build）＋ **测试基建随插件走**（25 个测试文件的运行环境，`vitest.setup.ts` 是地基）＋ eslint preset 覆盖面核清 ＋ 发布清单跨仓顺序补步；**另有两条本轮交下来的账**（见第 ⑶ 点） |
| 7.6 | #103 脚手架 git init | `create-linkdesk-plugin` 照抄 `cargo new` 三语义 + `--no-git` 退路 |
| 7.8 | #105 作者面文档收口 | 🔵 编号是插入序，**执行序在 7.7 之前** |
| 7.7 | #104 全层验收 | 七条判据（[00 §五](00-整理档案.md)）＋ 回归对照（**官方插件 18 只不变 / 随包 6 只**——口径已在 7.4 轮订正） |

### ⑶ 重点介绍你即将执行的轮次（7.5 门禁与 CI）

**详案**：[06-门禁与CI.md](06-门禁与CI.md)。**一句话**：**搬出去不等于脱管**——18 只插件的源码现在住在各插件仓，`npm run check` 已经够不着它们了（[06 §二](06-门禁与CI.md) 列的六类检查掉出去了）。你要给每只插件仓装上自己的检查，并把「新插件一建出来就自带检查」做进脚手架模板。

**🔴 你拿到的牌（7.4 交出来的实况，逐条都是本轮实测）**：

1. 🔴 **`check-bundled-freshness` 已挂进 `npm run check`，而且它默认联网**（2026-09-14 用户裁：**无网络时红**，`--offline` 是明示降级）。⇒ 你要给插件仓写 CI、或将来动壳仓 CI 时：**shell 的 check 需要一个能出网的 runner**；若某处只能离线，必须**显式**带 `--offline`（别把网络失败当成代码错）。本机口径：`api.github.com` 直连可通，Release asset 与 raw 要代理（Node 的 fetch 要 `NODE_USE_ENV_PROXY=1`，话术在 `scripts/lib/official-catalog.mjs`）。✅ **壳仓 CI 这一步已经接好**：`.github/workflows/build.yml` 的 `Check` 步骤已喂 `GITHUB_TOKEN`（提交 `2998bbefe`）——不加它，共享 runner IP 的匿名配额（60 次/时）会把门禁打成 403 假红。
2. 🔴 **收箱（18 → 6）的连带你都用得上**：`scripts/audit-i18n.mjs` 的字典面现在只聚合**箱内 6 只**的字典（本轮它当场变红 2 条 `暂停`/`继续`——那两条原本靠 serial-monitor 的字典"顺带"覆盖，已按「夹具 UI 串归插件自持」补进 `plugins/panel-demo/i18n/en.json`）；`ThemeEngine/integration.test.ts` 那 7 例改读 **`dev-fixtures/theme-recipes/` 冻结快照**（这正好是 `#102f` 的由来：**插件仓该有「以自己源码为输入」的那条**）。
3. 🔴 **`pack-bundled-plugins.mjs` 已删**（`#101e` 收口）：`package.json` 里 `pack:bundled-json` 没了，多出 `sync:bundled` / `check:bundled-freshness` / `check:bundled-freshness:selftest`。`05-内置插件迁移指南.md` §二**整节按 D3 重写**——写插件仓 CI、写发布文档时**照新的那份走**。
4. 🔴 **7.3 交下来的越层账还挂着**：`IpcBridgeHandler/tabs.ts:24` 的 `tabs.create` 收对象型 `type` 无运行时校验（会落盘成坏标签 `[object Object]-2`）。**7.4 没动它**（不是本轮的活），按 [00 §二](00-整理档案.md) 与 7.3 的登记**归 7.5/后续**。⚠️ 别顺手把 `settings` 改成侧栏视图——它的 `location="auxiliarybar"` 是设计使然，两处单测钉着。
5. 🔴 **本轮如实登记、留给你的三条**（[05 §七.8](05-出厂种子保鲜.md)）：① `packages/plugin-sdk/src/pack.ts` 的注释仍提到壳仓那个已删的旧脚本——**改它要动已发布 npm 包的内容哈希**（`check-npm-release` 会红），所以本轮刻意没动；你若因别的原因要 bump SDK，同笔改掉即可；② `sync-official-catalog.mjs` 读官方目录与各仓 json **仍走 raw**（有 5 分钟 CDN 滞后），本轮只把**新**脚本切到 Contents API，那个工具归它自己的轮次；③ 主题 recipe 的「上游改完立刻能验」归 `#102f`。
6. ⚠️ **`settings` 1.0.7 是「验收版」**：源码与 1.0.6 相同（CHANGELOG 已如实写明），只为让「箱子里的版本」可区分、可验收。**市场上多这一条是真实可见的**——若用户不想留，删掉该 Release 再跑 `sync:bundled --latest` 就回到目录所载那一版（种子侧会自动跟）。
7. 💡 **可复用读数**：本轮验收的原始日志在 `E:\ldk-l7-scratch\e2e-boot2.log`（干净 profile 里 `[bundled-install] ✅ 自动装 6 只`，settings=1.0.7）；`--user-data-dir` 的路径**必须加引号**（Git Bash 会吃掉反斜杠，第一次就踩到，落进了 exe 旁边的目录）。
8. ✅ **软件 0.1.63 已发布上线**（7.4 轮收尾时受用户委托发的）——所以：①「发版链」这一轮真跑通过一次（`npm run publish` → 打 tag → 带代理推 → CI 建 Release），**发布之后的提交回到「不推」等待态**；② 你若改动 `src/`/`electron/`（7.5 大概率会动测试基建），按硬约束 22 判定类别 + bump + 写 CHANGELOG 段；③ 装机验收前先读本档头部那条 ⚠️（NSIS 升级路径会删掉旧安装版）。

### ⑷ 续写清单层头关键词

收尾时同笔更新 [E6-执行清单.md 第 7 层头的「轮次进度」行](../E6-执行清单.md)（7.5 ✅ → ▶ 7.6），并在本文件顶部追加你的「给执行 7.6 的 AI」一段（四点结构不变）。

---

## → 给执行 7.4 出厂种子保鲜（E6#101）的 AI（交接人 = 执行 7.3 的 AI，2026-09-14）

**进场第一步**：读本段 → 按第 ⑷ 点核对清单层头关键词块 → 打开 [05-出厂种子保鲜.md](05-出厂种子保鲜.md) 照格执行（§〇病灶 / §二 三命令档 / §三 lock 字段 / §五 硬约束 10 的红线 都写全了）。工作纪律照 memory `ai-workflow-canon`（审计→报告→放行→执行）＋ `ai-relay-one-round-per-session`（一会话一轮）＋ 收尾四件套（`npm run check` 全绿 / 清单勾格记账 / 版本判定 / 记忆指针）。

**🧭 现场状态（开工前先知道「什么已推、什么只在本地」）**：

| 东西 | 在哪 | 状态 |
|:--|:--|:--|
| **18 个插件仓** | GitHub `Encaron/linkdesk-plugin-<id>` | ✅ **已推**，且**每仓都已发 Release**（7.3 做的：tag `v<ver>` + asset `<id>.linkdesk-plugin` + 仓根 `marketplace.json`，18/18 三产物核验过） ⇒ **`sync:bundled` 的拉取源已就位** |
| **官方目录收录** | `Encaron/linkdesk-marketplace` | ✅ **2026-09-14 已落地**——用户点头当天写入（提交 `66e8a974`，main；2 条 → 20 条；既有 2 条逐字节未动）。**18 只现在对所有默认配置的用户可见** |
| **壳仓本轮改动** | 本地 `e6` 分支 | 🛑 **没推**——`e6` 领先 `origin/e6` **8 笔**（L7 整层：7.0 / 7.1×2 / 7.2×2 / 7.3 + 两笔收尾订正），等用户发话 |
| 插件源码备份 | `E:\linkdesk-l7-backup.git`（mirror，fsck 过） | 本机 |
| 过渡期脚本与全部读数 | `E:\ldk-l7-scratch\` | 仓外。**7.3 新增**：`verify-100b.mjs`（三产物核验）/ `cdp.mjs` `cdp-console.mjs` `cdp-shot.mjs` `cdp-click.mjs` `cdp-watch.mjs`（CDP 驱动+截图+采日志）/ `ud-100d-stage1/`（一次性干净 profile）/ `shot-*.png` 七张 / `official-catalog-before.json`（官方目录**动手前**快照）。⚠️ **收录候选不在这个目录**——它在**壳仓 `scratch/official-catalog.next.json`**（脚本默认输出位，且 `scratch/` 已 gitignore），别删 |

> 🔴 **要推壳仓时**：`git -c http.proxy=http://127.0.0.1:7890 -c https.proxy=http://127.0.0.1:7890 push origin e6`（**凡搬字节的一律带代理**）；**先问用户**。

### ⑴ 第七层是干什么的

**壳与插件源码从此分家**（现状：**已分家、插件已上架到各自仓、且已收录进官方目录**）。插件源码住在各自独立的 GitHub 仓，壳仓只留两样：**构建产物**（`bundled-plugins/<id>.linkdesk-plugin`）＋ **文档档案**。真正要解的痛点是**「没上架」**——用户不敢卸载任何插件，根因是卸了没处拿回来。**这个痛点已经解掉了**：7.2 给了「一插件一仓」，7.3 给了「publish 到各自仓 + 收录进官方目录（默认可见）」，并已实测**「卸掉 → 从市场拿回来的还是新版」**。现状实测与全部决策住 [00-整理档案.md](00-整理档案.md)；执行序 = L5 剩余做完紧接着 L7、L4/L6 跳过——**本层是 E6 当前唯一在跑的层**。

### ⑵ 各轮次是干什么的

| 轮 | 任务 | 一句话 |
|:--|:--|:--|
| 7.0 | #97 判据先立 | ✅ 2026-09-14——五处落点把「源码住在哪 / 身份从哪来 / 版本号几处同源」写成正典 |
| 7.1 | #98 出口打通 | ✅ 2026-09-14——六只插件的依赖/构建自足 ＋ `@src` 测试解耦 ＋ SDK `pack` 通道 ＋ 身份显式化 |
| 7.2 | #99 逐个迁移 | ✅ 2026-09-14——**18 只源码外移各自独立仓（`Encaron/linkdesk-plugin-<id>`，保历史）并推送**；壳仓 `plugins/` 只剩两只夹具；执行实录 → [03 §七 §八](03-逐个迁移.md) |
| **7.3** | **#100 上架两步** | ✅ **2026-09-14 收官**——**18/18 publish 三产物全绿**（asset 下载字节 sha256 与本地逐字节相同）＋ 官方目录工具 ＋ **装机验收两阶全绿**（前阶逐只读数 / 后阶在全新 profile 里「默认可见 20 条 → 装 → 用 → 卸 → 装回拿新版」）＋ **卸载安全三连 + 更新通道实证真跑通** ＋ 🔴 **18 只已收录进官方目录**（用户点头当天落地，提交 `66e8a974`，2 条 → 20 条） |
| **7.4** | **#101 出厂种子保鲜** | **`sync:bundled` + `bundled-plugins.lock.json` + 新鲜度门禁**——新用户下载即得「当前壳版 + 各插件最新已发布版」；**`pack-bundled-plugins.mjs` 的退休在这儿做**（`#101e`） |
| 7.5 | #102 门禁与 CI | 每只插件仓自带 CI + 发布清单补步——**搬出去不等于脱管**（25 个测试文件的运行环境随行）；另有 7.3 交过来的 `tabs.create` 类型守卫缺口（`#100f`） |
| 7.6 | #103 脚手架 git init | `create-linkdesk-plugin` 照抄 `cargo new` 三语义 + `--no-git` 退路 |
| 7.8 | #105 作者面文档收口 | 🔵 编号是插入序，**执行序在 7.7 之前** |
| 7.7 | #104 全层验收 | 七条判据（[00 §五](00-整理档案.md)）＋ 回归对照（~~出厂 18 只恒 18~~ **7.4 轮订正：官方插件 18 只不变 / 随包收敛为 6 只**） |

### ⑶ 重点介绍你即将执行的轮次（7.4 出厂种子保鲜）

**详案**：[05-出厂种子保鲜.md](05-出厂种子保鲜.md)。**要解决的是用户原话**：「软件更新到了第二版，插件没更新无所谓；但有一天我给设置插件更新到第二版，**新用户下载软件就该一起拿到第二版的插件**」。

**🔴 你拿到的牌（7.3 交出来的实况，逐条都是实测）**：

1. 🔴 **`sync:bundled` 的拉取源已经真在网上**——18 只各仓都有 Release + asset（7.3 逐只发完并核验）。每仓根还有 `marketplace.json`（含 `version` / `downloadUrl` / `versions[0].changelog`）。
2. **各仓的实际版本**（7.3 发布后）：editor 1.0.10 / file-tree 1.0.7 / lang-defaults 1.0.17 / lang-test-ja 1.0.3 / marketplace 1.0.29 / python 1.0.5 / serial-monitor 1.0.9 / settings 1.0.6 / theme-aurora-glass 1.0.4 / theme-defaults 1.0.2 / theme-iconset-pastel 1.0.1 / theme-mint-soda 1.0.3 / theme-panorama 1.0.2 / theme-pill 1.0.2 / theme-songti 1.0.2 / theme-terminal 1.0.4 / theme-twilight-forest 1.0.2 / theme-zones 1.0.2。⚠️ **别照抄这串**——**现算**（既有教训：清单里的版本号是写清单那一刻的快照，照抄必错）。
3. **「卸载 → 从网络装回」已实测成立**（你验种子新鲜度时会用到同一套链路）：`lang-defaults` 卸掉种子版 1.0.16 ⇒ 用 GitHub 直链装回 **1.0.17**；`theme-songti` 走更新流 **1.0.1→1.0.2** 成功。⇒ **「箱子里的旧 + 各仓的新」这个落差是真实存在的**，正好是你 `--latest` 要追的东西。
4. 🔴 **`pack-bundled-plugins.mjs` 还活着**（`#101e` 的退休动作是你这一轮的活）：它现在扫 `plugins/` 得 0 个对象、带一行显式「无对象」结论。**退休 = 删脚本 + 删 `package.json` 的 `pack:bundled-json` + 文档回填**（`docs/02-Electron架构/E6_插件生态与发布/01-插件独立构建/05-内置插件迁移指南.md:97` 与 `:134`）。**别提前删**（7.3 刻意留着就是因为它还有「替代品未到」的问题）。
5. 🔴 **三个消费方读种子 zip 的布局契约**（`#101f`）：`scripts/audit-i18n.mjs`（读 `en.json`/`i18n/en.json`）· `src/core/services/ui/ThemeEngine/testFixtures.mock.ts`（读 recipe）· `src/pluginLoader/contributions/floatingPanelDeclarers.test.ts`（读 `plugin.json` + 条目表）。**你若改 `bundled-plugins/` 的命名或包内布局，三处必须同笔改**——它们会**响亮变红**（这是有意的，别把红改没）。
6. 💡 **顺手可复用**：`E:\ldk-l7-scratch\verify-100b.mjs` 里那套「逐只拿 tag/asset/仓根 json + 下载比 sha256」的写法，`sync:bundled` 的校验段可以参考（尤其**下载后比指纹**这一步——只信打印出来的 URL 会骗人）。
7. ⚠️ **硬约束 10 的红线**（05 §五 已详述）：「哪 6 只随包」的载体 = **lock 的 `seed: true` + `bundled-plugins/` 目录本身**，**壳代码零插件 id 字面量**。`sync:bundled` / `check-bundled-freshness` 也**只读 lock**。

**📌 7.3 留给你的账（**收录那条已在当天销掉**；剩下一条越层账，别在 7.4 顺手做）：**

| 账 | 归谁 | 说明 |
|:--|:--|:--|
| ~~🔴 18 只收录进官方目录~~ | — | ✅ **2026-09-14 已销**——用户点头当天落地（提交 `66e8a974`，2 条 → 20 条）。连带 `#100d` **后阶**也已跑完（全新 profile 里默认可见 → 装 → 用 → 卸 → 装回拿新版，全绿）。📖 [04 §九](04-上架两步.md) |
| 🔴 **`tabs.create` 收到对象型 `type` 的防御缺口** | **7.5 或后续（未销）** | 7.3 `#100f` 抓到并定位：`IpcBridgeHandler/tabs.ts:24` 位置解构无类型校验 ⇒ 对象落盘成坏标签 `[object Object]-2`。**修法建议**：加运行时守卫 + 定位发出者 + 补单测。⚠️ **别顺手把 `settings` 改成侧栏视图**——它的 `location="auxiliarybar"` 是设计使然（两处单测钉着） |

**顺手留意（7.3 立的新事实）**：

- 🔴 **raw CDN 是 `Cache-Control: max-age=300`**——官方目录写入成功后，**raw 又服务了约 5 分钟旧内容**才翻档（实测 `Source-Age` 爬到 255）。**凡「市场里怎么还没变」类问题先查这条**（不是壳的问题、也不是写失败）。⚠️ 它会波及你 7.4 的所有「查最新版」动作——**判新鲜度别只信 raw 的一次读**，对不上先等一轮再判。
- **`settings` 是浮动面板插件，不是侧栏插件**：`contributes.viewsContainers.settings.location = "auxiliarybar"`，而 LinkDesk **无该区域渲染**（`floatingPanelDeclarers.test.ts:101` + `loader.test.ts:288` 两处单测钉着）⇒ 打开它的**唯一正确路径**是 `window.linkdesk.panel.revealFloating('settings')`（或壳命令 `core.openSettings`），**点图标栏那个按钮不该切侧栏**。别把它当 bug 改。
- **市场侧栏的「探索插件」段 = 目录浏览**（另外还有「已安装 / 已禁用 / 内置」段）。判「默认可见」要看这一段或**官方目录那个 json**——**别拿「已安装」段的条数当目录**（随包种子让开机就有 18 只，极易误判）。
- **验「某配置项生效」前先从 `contributes`/schema 取真 id，别猜名**：7.3 把主题 id 猜成 `zones`（真 id 是 `paper-zones` / `image-zones`），界面毫无变化，**差点当成插件坏**。
- **壳仓 `scratch/` 已进 `.gitignore`**（7.3 同笔）——它是「候选产物」的暂存地，**入库 = 造第二份真相源**。`sync-official-catalog` 的产物就落那儿。
- **CDP 驱动工具在 `E:\ldk-l7-scratch\`**（`cdp.mjs` eval / `cdp-console.mjs` 采日志 / `cdp-shot.mjs` 截图 / `cdp-click.mjs` **真鼠标事件**）。⚠️ **实测教训**：`element.dispatchEvent(new MouseEvent(...))` 对 React 合成事件**常常不生效**；要点真 UI 用 `cdp-click.mjs`（走 CDP `Input.dispatchMouseEvent`）。
- **验收「留读数」的纪律**（7.3 严格执行、建议沿用）：写清「哪只、哪条路径、看到什么数字」，**不许只写「验过了」**。附带一条实测教训：**读 UI 状态要认「可见实例」**——池里 `.side-panel` 有多份（隐藏的也留在 DOM），只看第一个会恒读到旧的那份（7.3 在这上面白花了几轮）。

### ⑷ 续写清单层头关键词

收尾时同笔更新 [E6-执行清单.md 第 7 层头的「轮次进度」行](../E6-执行清单.md)（7.4 ✅ → ▶ 7.5），并在本文件顶部追加你的「给执行 7.5 的 AI」一段（四点结构不变）。

---

## → 给执行 7.3 上架两步（E6#100）的 AI（交接人 = 执行 7.2 的 AI，2026-09-14）

> ✅ **本段对应的轮次已于 2026-09-14 执行完毕（主体全过、带一条等用户点头的尾巴）**——原文一字未改留作对账（顶段落里写清了实测结论与留给 7.4 的账）。以下为当轮交接原文。

**进场第一步**：读本段 → 按第 ⑷ 点核对清单层头关键词块 → 打开 [00-整理档案.md](00-整理档案.md) 过 §二决策 + §三风险 → 打开 [04-上架两步.md](04-上架两步.md) 照格执行。工作纪律照 memory `ai-workflow-canon`（审计→报告→放行→执行）＋ `ai-relay-one-round-per-session`（一会话一轮）＋ 收尾四件套（`npm run check` 全绿 / 清单勾格记账 / 版本判定 / 记忆指针）。

**🧭 现场状态（开工前先知道「什么已推、什么只在本地」）**：

| 东西 | 在哪 | 推送状态 |
|:--|:--|:--|
| **18 个插件仓** | GitHub `Encaron/linkdesk-plugin-<id>` | ✅ **已推**（用户 2026-09-14 放行；每仓 main 分支，历史 4–88 笔） |
| **壳仓本轮源码改动** | 本地 `e6` 分支 | 🛑 **没推**——`e6` 领先 `origin/e6` **5 笔**（含 **L7 整层**：7.0 / 7.1×2 / 7.2×2），等用户发话（红线②＋ memory `push-wait-for-user`） |
| 插件源码备份 | `E:\linkdesk-l7-backup.git`（mirror，336MB，`git fsck` 过） | 本机，不含在壳仓历史之外的东西 |
| 过渡期脚本与全部读数 | `E:\ldk-l7-scratch\`（`extract-plugins.sh` / `prepare-repos.mjs` / `build-all.sh` / `push-repos.sh` + `check-*.log` 历次读数） | 仓外，**过渡期临时物**——7.7 做「check 前后对照」时这里的日志就是原始读数 |

> 🔴 **要推壳仓时**：`git -c http.proxy=http://127.0.0.1:7890 -c https.proxy=http://127.0.0.1:7890 push origin e6`（**凡搬字节的一律带代理**）；**先问用户**。

### ⑴ 第七层是干什么的

**壳与插件源码从此分家**（现状：**已经分了**）。插件源码住在各自独立的 GitHub 仓，壳仓只留两样：**构建产物**（`bundled-plugins/<id>.linkdesk-plugin`）＋ **文档档案**。出厂套装靠「打包时拉各插件最新已发布版」随包（D3，7.4 落 `sync:bundled`）。真正要解的痛点是**「没上架」**——用户不敢卸载任何插件，根因是卸了没处拿回来；**解药 = 每只插件有自己的仓（7.2 已给）＋ 上架两步（7.3，就是你这一轮）**。现状实测与全部决策住 [00-整理档案.md](00-整理档案.md)；执行序 = L5 剩余做完紧接着 L7、L4/L6 跳过——**本层是 E6 当前唯一在跑的层**。

### ⑵ 各轮次是干什么的

| 轮 | 任务 | 一句话 |
|:--|:--|:--|
| 7.0 | #97 判据先立 | ✅ 2026-09-14——五处落点把「源码住在哪 / 身份从哪来 / 版本号几处同源」写成正典 |
| 7.1 | #98 出口打通 | ✅ 2026-09-14——六只插件的依赖/构建自足 ＋ `@src` 测试解耦 ＋ SDK `pack` 通道 ＋ 身份显式化 |
| 7.2 | #99 逐个迁移 | ✅ 2026-09-14——**18 只源码外移各自独立仓（`Encaron/linkdesk-plugin-<id>`，保历史）并推送**；壳仓 `plugins/` 只剩两只夹具；12 类绑定逐条给结论；执行实录 → [03 §七 §八](03-逐个迁移.md) |
| **7.3** | **#100 上架两步** | **`publish` 到插件自己仓（第一步）＋ 收录进官方目录 `linkdesk-marketplace`（第二步——默认可见的解药）**；🛑 **写别人的仓要用户点头** |
| 7.4 | #101 出厂种子保鲜 | `sync:bundled` + `bundled-plugins.lock.json` + 新鲜度门禁（**`pack-bundled-plugins.mjs` 的退休动作在这儿做**——见第 ⑶ 点第 4 条） |
| 7.5 | #102 门禁与 CI | 每只插件仓自带 CI + 发布清单补步——**搬出去不等于脱管**（25 个测试文件的运行环境随行） |
| 7.6 | #103 脚手架 git init | `create-linkdesk-plugin` 照抄 `cargo new` 三语义 + `--no-git` 退路 |
| 7.8 | #105 作者面文档收口 | 🔵 编号是插入序，**执行序在 7.7 之前** |
| 7.7 | #104 全层验收 | 七条判据 + 回归对照（出厂 18 只恒 18）＋ 作者面终局验收 |

### ⑶ 重点介绍你即将执行的轮次（7.3 上架两步）

**详案**：[04-上架两步.md](04-上架两步.md)（两步模型 + 逐只前置条件 + 官方目录批量工具设计）。

**🔴 你现在拿到的牌（7.2 交出来的实况，逐条都是实测）**：

1. **18 只插件仓已建好并推送**：`Encaron/linkdesk-plugin-<id>`（**N3 已由用户拍板 = `linkdesk-plugin-<id>`**，写进 [09 §五](09-命名规范.md)）——`editor` `file-tree` `marketplace` `settings` `python` `serial-monitor` `lang-defaults` `lang-test-ja` + 10 只 `theme-*`。每仓一笔「独立仓首笔」commit（`chore:` 前缀），**版本已 PATCH +1**（如 settings 1.0.5→1.0.6、marketplace 1.0.28→1.0.29），**CHANGELOG 段也都补好了**（含 `theme-iconset-pastel`——它原来**根本没有 CHANGELOG.md**，本轮替它建了）⇒ **#100a 的两块前置已提前完成，届时核对即可**。
2. **本地落位**（D6 容器）：`E:\linkdesk-plugins\official\<id>\`（容器里有 `README.md` 写明「容器本身绝不建仓」）。**每仓的 `package-lock.json` 已入库且 resolved 指 `registry.npmjs.org`**；`npm ci && npm run build` 在**仓内**实测 18/18 出 zip。
3. 🔴 **本轮没做装机验收**——[03 §五](03-逐个迁移.md) 判据里「仓外构建 → zip → **装安装版** → 功能正常」的第三环，7.2 只做了前两环。**动线已写成清单格 `E6#100d`**（分两阶：**前阶不必等目录收录**——拿插件仓产出的 zip 走「从本地 .linkdesk-plugin 安装」，逐只验 editor 跳转 / python LSP / serial 收发 / settings / 主题 / 语言包；**后阶 = 干净 profile 里市场搜得到 → 装 → 用 → 卸 → 装回**）。别把清单里那格当成已验。
4. 🔴 **`pack-bundled-plugins.mjs` 还活着**（与本层 §4.1 原判「退休」有偏差，理由写在 [03 §7.7](03-逐个迁移.md) 第 10 条）——它现在扫 `plugins/` 得 0 个对象、**加了一行显式「无对象」结论**。**退休动作（删脚本 + 删 `pack:bundled-json`）随 7.4 的 `sync:bundled` 一起做**，别在 7.3 提前删（那时还没有替代品，删了就留下一条指向不存在命令的文档）。
5. **种子已刷新**（你拍的「甲」）：`bundled-plugins/` 里 18 个 zip **已经是插件仓新产的**（含 `pluginId`），门禁 `check-bundled-version-bump` 18/18 `version-bumped` 过。
6. 🔴 **`publish` 的目标仓库 = 工程自己的 `git remote origin`**（`publish.ts:2`）——18 仓的 origin 都已经指对了（`https://github.com/Encaron/linkdesk-plugin-<id>.git`）。**推送一律带代理**（memory `dev-environment`：凡搬字节的都要带）。
7. **官方目录现状**：`Encaron/linkdesk-marketplace` 只有 `README.md` + `marketplace.json`，**现有 2 条**（`hello-linkdesk` / `first-run-setup`），18 只一只都没进去。写它是**写别人的仓 ⇒ 用户点头**（红线②）；`#100c` 的工具设计里写死「**只生成不推**」「**禁止整文件覆盖**」「**不许写死 18 个 id**（硬约束 10）」。

**🔴 三条不可违的红线**（层头已立，再钉一遍）：① **一插件一仓 + 上架两步**（publish ≠ 上架）；② **`git push` 与「写官方目录仓库」都要用户点头**（memory `push-wait-for-user`）；③ **壳代码零插件 ID 字面量**（硬约束 10——「谁随包」由数据声明）。

**📌 7.2 把「搬源码掉出来的尾巴」全部落成了清单格（不留散文欠账，别在 7.3 顺手做——各自有正主）：**

| 尾巴 | 归谁 | 格 |
|:--|:--|:--|
| `pack-bundled-plugins.mjs` 退休（删脚本 + 删 `pack:bundled-json` + 文档回填） | **7.4** | `#101e` |
| 种子布局是三处消费方的契约（`audit-i18n` / `testFixtures.mock` / `floatingPanelDeclarers.test`） | **7.4** | `#101f` |
| 插件仓自己的 i18n 字典完整性（壳侧插件半边随源码搬走了，**现在无人管**） | **7.5** | `#102e` |
| 两处断言在插件仓的对应物（ThemeEngine 真实 recipe / floatingPanel 声明者） | **7.5** | `#102f` |
| doc-links 九条排除 + 六处门禁「无对象」结论行的去向复核 | **7.7** | `#104e` |
| 装机验收动线 | **7.3（你）** | `#100d` |

**顺手留意（7.2 留下、7.3 用得上）**：

- **`10-作者文档族对账.md` 的 C 批已在 7.2 落笔**：`04-插件分发格式`（两代码根 → 三行 + `.disabled/` 坟场结论 + 「非更新通道」与 D3 对齐）· `13-插件开发指南`（第 9 步补「**上架是两步**」）· `06-plugin.json规范`（表头「本文件恒指插件自己仓根」）· `01-插件API契约`（import 白名单复核）· `05-插件UI写法规约` / `10-如何造一个设置插件` / `11-主题制作`（路径改指插件仓）。**最终收口在 7.7**。
- **`check-doc-links` 新增了 9 条单档排除**（`scripts/doc-links-exclude.txt`）：9 份**已收官层的任务档案**里指向 `plugins/<id>/` 源码的 109 处链接按「篡改历史记录」挂账排除。⚠️ **口子只对这 9 份开**——活文档里出现同类链接，门禁照旧红。

### ⑷ 续写清单层头关键词

收尾时同笔更新 [E6-执行清单.md 第 7 层头的「轮次进度」行](../E6-执行清单.md)（7.3 ✅ → ▶ 7.4），并在本文件顶部追加你的「给执行 7.4 的 AI」一段（四点结构不变）。

---

## → 给执行 7.2 逐个迁移（E6#99）的 AI（交接人 = 执行 7.1 的 AI，2026-09-14）

**进场第一步**：读本段 → 按第 ⑷ 点核对清单层头关键词块 → 打开 [00-整理档案.md](00-整理档案.md) 过 §一现状体检 ＋ §二决策 D1-D7 ＋ §三风险六条 → 打开 [03-逐个迁移.md](03-逐个迁移.md) 照格执行。工作纪律照 memory `ai-workflow-canon`（审计→报告→放行→执行）＋ 一次一小步收尾四件套（`npm run check` 全绿 / 清单勾格记账 / 版本判定 / 记忆指针）。

### ⑴ 第七层是干什么的

**壳与插件源码从此分家。** 插件源码搬进各自独立的 GitHub 仓库，壳仓只留两样：**构建产物**（`bundled-plugins/<id>.linkdesk-plugin`）＋ **文档档案**。出厂套装靠「打包时拉各插件最新已发布版」随包（D3 构建期拉取），不再靠源码住在壳里。真正要解的痛点是**「没上架」**——用户不敢卸载任何插件，根因是卸了没处拿回来；解药 = 每只插件有自己的仓 + 上架两步（publish 只是半程，官方目录收录才默认可见）。现状实测与全部决策住 [00-整理档案.md](00-整理档案.md)；执行序（用户 2026-09-14 拍板）= L5 剩余做完紧接着做 L7、L4/L6 跳过——**L4/L5 已双双封站，本层是 E6 当前唯一在跑的层**（L6 安全加固与出厂判定排在本层之后）。

### ⑵ 各轮次是干什么的

| 轮 | 任务 | 一句话 |
|:--|:--|:--|
| 7.0 | #97 判据先立 | ✅ 2026-09-14 完成——五处落点把「源码住在哪 / 身份从哪来 / 版本号几处同源」写成正典 |
| 7.1 | #98 出口打通 | ✅ 2026-09-14 完成——六只插件的依赖/构建自足 ＋ `@src` 测试解耦 ＋ SDK `pack` 通道 ＋ 身份显式化；**仓外构建 7/7 实证**（详见 [02 §九](02-出口打通.md)） |
| **7.2** | **#99 逐个迁移** | **`git filter-repo` 抽子树保历史 → 建仓 → 推送 → 壳仓同笔删源码 + 拆 12 类绑定**；批 A 6 只 React / 批 B 12 只 JSON；**搬一只闭环一只**；🛑 推送等用户点头 |
| 7.3 | #100 上架两步 | `publish` 到插件自己仓（第一步）＋ 收录进官方目录 `linkdesk-marketplace`（第二步——默认可见的解药） |
| 7.4 | #101 出厂种子保鲜 | `sync:bundled` + `bundled-plugins.lock.json` + 新鲜度门禁 |
| 7.5 | #102 门禁与 CI | 每只插件仓自带 CI + 发布清单补步——搬出去不等于脱管（25 个测试文件的运行环境随行） |
| 7.6 | #103 脚手架 git init | `create-linkdesk-plugin` 照抄 `cargo new` 三语义 + `--no-git` 退路 |
| 7.8 | #105 作者面文档收口 | 🔵 编号是插入序，**执行序在 7.7 之前**——03-插件制造 教学动线化 + 主题子文件夹 + 脚手架 `AGENTS.md` + 文档 npm 包 + 两条门禁 |
| 7.7 | #104 全层验收 | 七条判据（00-整理档案 §五）＋ 回归对照（出厂 18 只恒 18）＋ 作者面终局验收 |

### ⑶ 重点介绍你即将执行的轮次（7.2 逐个迁移）

**详案**：[03-逐个迁移.md](03-逐个迁移.md)（三步物理动作 + 批 A/B 切分 + 抽子树四验 + 12 类绑定拆除清单 + 逐只判据）。**本轮的难点不是 `mv`，是第 ③ 步「拆绑定」。**

**🔴 开工前先看这三条（7.1 实测留下）**：

1. ✅ **npm 两个包已经发布了，构建前提就位**（2026-09-14 用户拍板，已发已验）——7.1 给 `@linkdesk/plugin-sdk` 加了 `pack` 子命令（0.1.13 → **0.1.14**）、给 `@linkdesk/contracts` 加了 `PluginManifest.pluginId`（0.1.12 → **0.1.13**）；两包已上架、`npm run release:mark` 已灭灯，并**在真实货架上复验过**（全新目录 `npm install` 装到 0.1.14 + `build`/`pack` 各出一只 zip）。⇒ **你不需要再操心发布**，直接照 §⑶ 的批次干活即可。
   - 💡 **仍值得记的一条手法**（以后壳侧再 bump 这两包时用得上）：包还没发布、但你想先验证插件仓能不能构建时——`cd packages/plugin-sdk && npm pack --pack-destination <dir>`，临时工程 `devDependencies` 指向该 tgz（等价于「货架上有这一版」）→ `npm install && npm run build`。**tarball 路径不许写进任何提交**，插件仓正式形态必须依赖 npm 版本号。
   - ⚠️ **注册表 `latest` 有数分钟传播延迟**（实测约 2 分钟才翻档）——`release:mark` 的货架核对会因此**假失败**（报「货架问不到」或「版本没动」）。发布后**别立刻 mark**，先 `npm view <pkg> version --registry=https://registry.npmjs.org` 确认 latest 真翻过去了再记。
2. **🔴 重打的 zip 一定会与壳仓现存种子指纹不同 ⇒ 会要求 bump 版本**。原因不是搬迁，是 **7.1 给 18 只 `plugin.json` 各加了一行 `pluginId`**（7.1 实测：同一输入下 SDK pack 的产物与既有 zip **指纹逐字相同**；加了 `pluginId` 之后**只有 `plugin.json` 一个条目变了**）。`03 §3.3` 写的「搬迁不是内容变更、不 bump」在**本轮的具体数据下要照实测改判**：**先跑门禁再判**（`check-bundled-version-bump` 说 identical 就不用 bump；说 red-same-version-content-diff 就必须 bump）。反正批 A/B 都要进 7.3 上架发版，**bump 是顺路的**。
3. **`plugins/` 会只剩两只夹具**（`panel-demo` / `floating-panel-demo`）——它们**不发货、留在壳仓、脚本继续走相对路径**（7.1 只同步了它们的版本号，**没动脚本**，这是刻意的）。

**🔴 7.1 发现、留给你顺手修的两处**（都不属 7.2 主线，但越早越省事）：

- **`plugins/editor/plugin.json` 的 `$schema` 多了一级**：写的是 `"../../../public/schemas/plugin.schema.json"`，**解析到仓库外**（实测不存在）；其余 19 只都是 `"../../"`。只影响 IDE 补全/校验，零运行时影响。**搬 editor 那一格时顺手改对**（值 = `"../../public/schemas/plugin.schema.json"`）。
- **`memory/JOURNAL.jsonl` 其实不在 git 里**：根 `.gitignore:41` 的 `memory/` 把它整个忽略了，而 `AGENTS.md` 把它称作「**仓内**流水日志」——两处口径不一致。**处置方向要问用户一句**（「`memory/JOURNAL.jsonl` 要不要进 git？」）：要 → `.gitignore` 加一条 `!memory/JOURNAL.jsonl` 反向放行；不要 → 把 `AGENTS.md` 那行改成「本机流水日志（不进 git）」。**不许自己拍**（这决定记忆要不要随仓走）。

**7.1 已经替 7.2 铺好的路（别重复做）**：

- **身份显式化（`#98g`）已落地**：`pluginId` 进 schema + **20 只全显式声明** + 兜底保留但打黄灯。**7.1 负控实测**：把插件拷成目录名 `renamed-dir-check` 构建 ⇒ 产物仍是 `file-tree.linkdesk-plugin`、包内 `pluginId=file-tree`。⇒ **「不许改目录名」这条迁移红线的原始目的（防身份漂移）已经达成**（[09 §六](09-命名规范.md)）。目录名现在**可以**改，但**仍建议保持 = id**（省得对账时多一层换算）；**不许改的是 `pluginId` 的值**。
- **`#98c` 的 pack 通道已落地且**：SDK 新增 `linkdesk-plugin-sdk pack`（`--out <path>`），**壳仓 `scripts/pack-bundled-plugins.mjs` 已改成只做编排**（打包实现全仓只此一份；SDK 未构建时它会自动 `tsc` 一次）。⇒ `03 §4.1` 第 10 条**已有答案**（取甲案）。批 B 的 12 只插件现在各自有 `package.json`（`build` = `linkdesk-plugin-sdk pack`）。
- **LSP 链已就位且抓到一处真缺陷**：`check-lsp-deps.mjs` 早就基准参数化（`cliBase ?? dirname(plugin.json)` + 向上解析），`check-lsp-args-base.mjs` 是**锚词门禁**不是路径门禁（**两处都不用改**）；`check-lsp-smoke.mjs` 原本缺向上解析 ⇒ pyright 被 hoist 到仓库根后**恒报缺失**（改动前就是红的），已修、现全链路绿。⚠️ **python 搬走后两件事要做**：① `package.json` 的 `lsp:smoke --base plugins/python` 改指新位置；② `check-lsp-deps` 扫 `plugins/` 将**扫不到任何 langDefs ⇒ 真空绿灯**——**逐条给结论，不许默默失效**（`03 §4.1` 第 9 条同款纪律）。
- **版本门禁已进 `check`**：`scripts/check-plugin-version-sync.mjs`（带 `--self-test`）。⚠️ 插件源码搬走后它对发货插件同样会**真空**——7.5 要给每只插件仓自己的那份检查（或共享 SDK 侧实现）。
- **根 `workspaces` 已加 `packages/*`**（`knip.json` 的 `ignoreWorkspaces` 同步）：插件的 `build` 改 npm 包形态后，**仓内靠 workspaces 链接到本地包、仓外靠货架**，同一份 `package.json` 两个环境都对。拆 `plugins/*` 时**别把 `packages/*` 一起摘掉**。

**🔴 三条不可违的红线**（层头已立，此处再钉一遍）：① **搬一只 → 构建验证一只 → 上架一只 → 壳仓那份删净一只**（中间态 = 双真相源，**无门禁可发现分叉**）；② **`git push` 与「写官方目录仓库」都要用户点头**（memory `push-wait-for-user`）；③ **壳代码零插件 ID 字面量**（硬约束 10）。

**顺手留意**：`10-作者文档族对账.md` 的 **B 批已基本落笔**（7.1 同笔改了 `03:623` / `09:94` / `06` 的 pluginId 行与最小示例 / `02` 的裁决行；`00-README` 的 15/16 入口 7.0 已有）；**C 批等你**——`04-插件分发格式.md`（两代码根表 → 三行，**改动最重**）、`13`（上架是两步）、`01`/`05`/`10`/`11` 里的 `plugins/<id>/` 路径引用、`06` 的「本文件恒指插件自己仓根」。**收口在 7.7，但 C 批随 7.2 搬完就该动**（搬一只改一只，别攒到最后）。

### ⑷ 续写清单层头关键词

收尾时同笔更新 [E6-执行清单.md 第 7 层头的「轮次进度」行](../E6-执行清单.md)（7.2 ✅ → ▶ 7.3），并在本文件顶部追加你的「给执行 7.3 的 AI」一段（四点结构不变）。

---

## → 给执行 7.1 出口打通（E6#98）的 AI（交接人 = 执行 7.0 的 AI，2026-09-14）

> ✅ **本段对应的轮次已于 2026-09-14 执行完毕**——原文一字未改留作对账（顶段落里写清了实测结论与留给 7.2 的账）。以下为当轮交接原文。

**进场第一步**：读本段 → 按第 ⑷ 点核对清单层头关键词块 → 打开 [00-整理档案.md](00-整理档案.md) 过 §一现状体检 ＋ §二决策 D1-D7 ＋ §三风险六条 → 打开 [02-出口打通.md](02-出口打通.md) 照格执行。工作纪律照 memory `ai-workflow-canon`（审计→报告→放行→执行）＋ 一次一小步收尾四件套（`npm run check` 全绿 / 清单勾格记账 / 版本判定 / 记忆指针）。

### ⑴ 第七层是干什么的

**壳与插件源码从此分家。** 插件源码搬进各自独立的 GitHub 仓库，壳仓只留两样：**构建产物**（`bundled-plugins/<id>.linkdesk-plugin`）＋ **文档档案**。出厂套装靠「打包时拉各插件最新已发布版」随包（D3 构建期拉取），不再靠源码住在壳里。真正要解的痛点是**「没上架」**——用户不敢卸载任何插件，根因是卸了没处拿回来；解药 = 每只插件有自己的仓 + 上架两步（publish 只是半程，官方目录收录才默认可见）。现状实测与全部决策住 [00-整理档案.md](00-整理档案.md)；执行序（用户 2026-09-14 拍板）= L5 剩余做完紧接着做 L7、L4/L6 跳过——**L4/L5 已双双封站（2026-09-14），本层是 E6 当前唯一在跑的层**（L6 安全加固与出厂判定排在本层之后）。

### ⑵ 各轮次是干什么的

| 轮 | 任务 | 一句话 |
|:--|:--|:--|
| 7.0 | #97 判据先立 | ✅ 2026-09-14 完成——五处落点把「源码住在哪 / 身份从哪来 / 版本号几处同源」写成正典（见 §⑷ 与 01-判据先立.md） |
| **7.1** | **#98 出口打通** | **六只含构建产物的插件脱离壳工程可自足构建**：build 脚本从相对路径改 npm 包、补依赖声明、拆 `@src` 三处测试耦合、**给 12 只纯 JSON 插件造 pack 通道**（本层唯一先造工具项）、LSP 三处基准参数化 |
| 7.2 | #99 逐个迁移 | `git filter-repo` 抽子树保历史 → 建仓 → 推送 → **壳仓同笔删源码 + 拆 17 处绑定**；批 A 6 只 React / 批 B 12 只 JSON；**搬一只闭环一只**；🛑 推送等用户点头 |
| 7.3 | #100 上架两步 | `publish` 到插件自己仓（第一步）＋ 收录进官方目录 `linkdesk-marketplace`（第二步——默认可见的解药） |
| 7.4 | #101 出厂种子保鲜 | `sync:bundled` + `bundled-plugins.lock.json` + 新鲜度门禁——新用户下载即得「当前壳版 + 各插件最新已发布版」 |
| 7.5 | #102 门禁与 CI | 每只插件仓自带 CI + 发布清单补步——搬出去不等于脱管（25 个测试文件的运行环境随行） |
| 7.6 | #103 脚手架 git init | `create-linkdesk-plugin` 照抄 `cargo new` 三语义 + `--no-git` 退路 |
| 7.8 | #105 作者面文档收口 | 🔵 编号是插入序，**执行序在 7.7 之前**——03-插件制造 教学动线化 + 主题子文件夹 + 脚手架 `AGENTS.md` + 文档 npm 包 + 两条门禁 |
| 7.7 | #104 全层验收 | 七条判据（00-整理档案 §五）＋ 回归对照（出厂 18 只恒 18）＋ 作者面终局验收 |

### ⑶ 重点介绍你即将执行的轮次（7.1 出口打通）

- **详案**：[02-出口打通.md](02-出口打通.md)——现状实测表、逐只清单、`@src` 三处精确处置、JSON 插件 pack 缺口、LSP 链、版本号两处不一致。
- **开工先记的实测事实**（00-整理档案 §一，2026-09-14 回源）：**零只插件声明** `@linkdesk/ui` / `@linkdesk/contracts` / `@linkdesk/plugin-sdk`——4 只白吃 ui（file-tree / marketplace / settings / serial-monitor）、2 只白吃 contracts（file-tree / marketplace），全靠壳仓 workspaces 提升；build 脚本一律 `node ../../packages/plugin-sdk/dist/bin.js`（相对路径），**离开壳仓第一分钟就断**；六只插件的 `package.json.version` 全是 `1.0.0` 假版本（真版本在 `plugin.json`）。
- **7.0 已立的判据会约束你**：版本号**唯一真源 = `plugin.json.version`**（`09-插件目录规范.md` §源码位置与分发物的关系 第 5 条）；`package.json.version` 的处置 = N6「四处同源」（保留字段、强制与 plugin.json 相等，门禁在 `#98e`）；身份显式化 = `#98g`（`pluginId` 进 schema ＋ 18 只显式声明——**这是你这一轮最危险的一格**，见下）。
- **🔴 迁移红线（09-命名规范.md §六）：在 `pluginId` 显式化落地之前，一律不许改任何插件的目录名。** 身份今天靠目录名兜底且 `pluginId` 不在 schema，五条漂移后果（双份并存 / 墓碑失效 / catalog 双条 / 更新链静默断 / 数据看似丢失）**全不报错**。
- **顺手留意**：`03-插件contributes规范.md:623` 现写着「pluginId 不用声明——主进程用目录名」，与 N1 冲突——`#98g` 落地同笔改掉（已登记进 [10-作者文档族对账.md](10-作者文档族对账.md) §四）。
- **纪律照旧**：一次一小步收尾四件套；`#98g` 动 schema 必须**三份拷贝同笔同步**（`check-plugin-schema-sync` 字节相等，漏一份 `npm run check` 当场红）；不推（push 等用户发话、推必带代理）。

### ⑷ 续写清单层头关键词

收尾时同笔更新 [E6-执行清单.md 第 7 层头的「轮次进度」行](../E6-执行清单.md)（7.1 ✅ → ▶ 7.2），并在本文件顶部追加你的「给执行 7.2 的 AI」一段（四点结构不变）。
