# 统一换行符：仓库内一律 LF（2026-09-12 立）
#
# 为什么要有这个文件：此前没有它，换行符取决于「文件是怎么到工作区的」——
#   检出得到 CRLF（本机 core.autocrlf=true），而脚本 writeFileSync 写出的是 LF。
#   ⇒ 任何「生成物与盘上比对」的门禁都得**各自记得**归一行尾——靠记性。
#   实测一天内同一个坑犯两次：generate-contract.mjs 记得（2026-09-11 修），
#   generate-api-cheatsheet.mjs 忘了（2026-09-12 CI 首次真跑红，且报错预览里
#   两边「看起来一模一样」）。本文件把这个类从「靠记性」变成「靠规则」。
#
# 加之前已核实（两种独立方法交叉验证，避免用假数据做决策）：
#   仓库内已提交内容 **零个回车符**（`git grep -I -lP '\r' HEAD` 命中 0；
#   逐文件读 blob 数 \r 同样命中 0）⇒ 本规则**不会**让任何文件变成「已修改」，
#   零批量改写。全仓亦无 .bat/.cmd/.ps1（那类文件才需要 CRLF）。
#
# 边界：`text=auto` 由 git 自行判定文本/二进制，二进制文件（png 等）不受影响。
# 判据（这条规则是否真的在起作用）：新写一个「生成物与盘上比对」的脚本，
#   在**干净检出**上跑必须绿——不靠作者记性。
* text=auto eol=lf
