# Serial Monitor V3 开发计划

> 2026-07-18，基于 [P1-设计方案.md](./P1-设计方案.md) 编写。
> **2026-07-18 修订 1：Step 8（顶栏+状态栏）提前并入 Step 2 布局骨架；i18n 基础设施（react-i18next + t()）从 Step 2 起全量使用。**
> **2026-07-18 修订 2：Phase 重排——Phase 2 改为终端完善（Phase 1 后续），原卡片架构后移到 Phase 3，OLED → Phase 4，设置 → Phase 5。**
> **2026-07-18 修订 3：Phase 重排——Phase 3 改为标签页+分屏系统（卡片的前置基础设施），完整设计拆入独立文档 `V3-Phase3-标签页分屏设计.md`。卡片→Phase 4，OLED→Phase 5，设置→Phase 6。**
## 二、Phase 1：项目骨架 + 终端视图 + 串口收发

### 目标

打开 exe → 看到和 V2 一模一样的窗口 → 能打开串口 → 终端视图收发数据 → 蓝色回显、暂停、搜索、导出全通。

工作台和设置视图是空壳——框架在，内容等 Phase 2。

### ⚠️ 修订：Step 8 提前并入 Step 2

原计划 Step 8（顶栏 + 状态栏）在最后才加。读 V2 MainWindow.xaml 后发现：V2 的布局是 **3 行**（顶栏 / 内容 / 状态栏），顶栏的串口控制和右上工具按钮（中/EN、主题、?）是**全局控件**，贯穿所有视图，不是某个 Step 再追加的东西。因此顶栏 + 状态栏现在是 Step 2 布局骨架的一部分，和图标栏、侧栏、主区一起建好。

### ⚠️ 基础设施——从第一天铺好（V2.6 教训）

V2.6 在全部面板写完后再统一手术：93 处主题换色 + 515 条双语替换 + 10 个踩坑。改动量巨大，用户打开 exe 看到的变化只有"切主题不闪了"和"中/EN 按钮"。

V3 从 Phase 1 就内置。每个 Step 标注遵守情况。

**1. 所有颜色走 CSS 变量，禁止硬编码 hex。**

```
✅ var(--text-primary)
❌ #D4D4D4
```

21 色从 V2 ThemeMap 搬入 `:root`。亮色主题变量现在就定义（`[data-theme="light"]`），Phase 1 只激活暗色主题，但切主题的底层已经就绪——未来切主题 = 改 `<html>` 上一个 `data-theme` 属性。V2.6 的 93 处逐个换色 → V3 的 0 行。

**2. 第一行 UI 文字就走 `t()`。**

```
✅ <label>{t("时间戳")}</label>
❌ <label>时间戳</label>
```

翻译表从 V2 的 ~515 条 EnMap 搬入 JSON，Phase 1 先全中文。以后加英文 → 同一个 JSON 加 `"时间戳": "Timestamp"`，不碰组件代码。V2.6 的 515 条逐个替换 → V3 的 0 行。

**3. 组件不从 JS 设颜色，只从 CSS 读。**

切主题 = 改 `data-theme` → 浏览器 GPU 合成层处理全部重绘。不遍历控件、不调方法、不存在"漏掉某个面板"。V2.6 的 5 个面板 UpdateTheme 回调 → V3 的 0 行。

**4. 侧栏设置不走 React state，走 PreferenceService。**

```
用户点开关 → PreferenceService.savePrefs() → React 重新读 prefs → UI 刷新
AI 改 prefs.json → 文件监听 → PreferenceService 通知 → React 刷新
```

人和 AI 在同一个 JSON 文件上交汇。详见 Step 5。

### ⚠️ V2 Bug 模式——V3 如何根除

从 70+ commit 的 bug fix 中提取五类重复模式：

**A. 遗漏式 bug（最高频）——用集中注册表防**

| V2 发生了什么 | 根因 | V3 对策 |
|------|------|------|
| 加新面板 → 忘记在 5 处注册（动画/显隐/切换/退编辑/主题回调） | 注册逻辑散落 | CardRegistry 一处注册 |
| 加新配置项 → 忘记持久化 | 配置散落多个文件 | PreferenceService 唯一入口 |
| 切语言后侧栏标题不跟、菜单不跟 | Locale 调用分散 | react-i18next `t()` 统一，一处改全局生效 |
| CreatePopup 4 处遗漏 PlacementTarget | 重复代码未封装 | React 无此问题（无 WPF Popup） |

**B. 两套系统不同步——用单向数据流防**

| V2 发生了什么 | 根因 | V3 对策 |
|------|------|------|
| QuickSend 暗色菜单白底白字 | 该组件没走 DynamicResource | CSS 变量全量继承，不存在"没走的组件" |
| 拖拽摇杆后文字变黑 | MouseMove 热路径改色 | 组件不从 JS 设颜色，只从 CSS 读 |
| 暗色滚动条对齐偏移 | WPF ScrollBar 模板特判 | CSS `scrollbar-color` 原生支持 |

**C. 静默丢数据——用显式 unknown 处理防**

| V2 发生了什么 | 根因 | V3 对策 |
|------|------|------|
| 未知协议类型消息从接收区静默消失 | switch 无 default 分支 | 未知 ID → 自动建 generic 卡 + toast 通知（方案 §2.4） |

**D. WPF 专有 bug——换技术栈自动避免**

| V2 发生了什么 | 根因 |
|------|------|
| Path 水平线消失/斜线断 | WPF Canvas Shape 渲染缺陷 |
| TypeConverterMarkupExtension 崩溃 | BAML 编译缓存 + ico 路径 |
| Popup.PlacementTarget 遗漏 → 弹窗位置错乱 | WPF Popup 机制 |
| EmbeddedResource 子目录路径错 | MSBuild 资源管理 |
| SnapShapeToPixel + EdgeMode → 直线消失 | WPF 像素对齐 bug |

换 Tauri + React → 这些问题全部不存在。HTML5 Canvas 2D 渲染、Vite 打包、CSS 定位——没有 WPF 的 BAML/Popup/Path 陷阱。

**E. V3 新增风险——需要显式防范**

| 风险 | V2 没有此问题（为什么） | V3 的防范 |
|------|------|------|
| Rust 串口 panic 导致进程退出 | C# try-catch 兜底 | Rust `unwrap_or_else` + Result 传播，读线程 panic 不崩主进程 |
| CM6/Monaco 内存泄漏（长时间运行） | AvalonEdit 成熟稳定 | 行数上限裁剪 2000 行 → 砍 500（和 V2 同逻辑） |
| IPC 事件堆积导致 UI 卡 | WPF 直接同进程调方法 | RingBuffer 512 条上限，满则覆盖最老 |
| Tauri WebView2 版本兼容 | 无 | 最低版本检查，异常时提示安装 WebView2 Runtime |

### 整体框架

```
┌────┬──────────────────┬─────────────────────────────┐
│    │                  │  顶栏：COM3 ▼ 115200 ▼       │
│    │                  │  [● 打开]        中/EN  ☀  ⚙ │
├────┼──────────────────┼─────────────────────────────┤
│    │                  │                             │
│ 📟  │     侧栏         │        主内容区              │
│    │    (220px)       │    视图随图标切换             │
│ 📊  │                  │                             │
│    │  内容随视图切换   │                             │
│ ⚙   │                  │                             │
│    │                  │                             │
├────┴──────────────────┼─────────────────────────────┤
│ ● 已连接 │ TX:1,234  RX:56,789                       │
└───────────────────────┴─────────────────────────────┘
```

三个图标对应三个视图：

| 图标 | 视图 | Phase 1 状态 |
|------|------|:--:|
| 📟 | 终端 | **完整实现**：接收区 + 发送栏 + 侧栏收发设置 |
| 📊 | 工作台 | 空壳：占位文字"暂无卡片"（Phase 3 实现） |
| ⚙ | 设置 | 空壳：占位文字"即将推出"（Phase 7 实现） |

**整体风格和 V2 一致，图标从 V2 借用。**

### 实施顺序

```
Step 1: Tauri + React 脚手架 → tauri dev 能弹出窗口
Step 2: 整体布局 + 顶栏 + 视图切换 + 状态栏 + i18n 基础设施
Step 3: 终端视图 UI（CM6 接收区 + Monaco 发送栏 + 侧栏设置）
Step 4: 工作台 + 设置视图（空壳）
Step 5: 核心服务（DataConverter / ProtocolParser / RingBuffer / PreferenceService）
Step 6: Rust 串口层（枚举 / 打开 / 关闭 / 读写 / emit 事件）
Step 7: 接通数据管道（Rust → RingBuffer → CM6 → 蓝色回显 → 暂停 → 搜索 → 导出）
Step 8: ~~顶栏 + 状态栏~~（已并入 Step 2）
```

**原则**：先建 UI 骨架，再建无依赖的服务（DataConverter、ProtocolParser、RingBuffer），然后攻串口层（唯一不确定性），最后接通全链路。每一步编译通过再走下一步。

---

### Step 1：Tauri + React 项目脚手架

#### 内容

```
1. npm create tauri-app → React + TypeScript 模板
2. cargo init src-tauri
3. 配置 tauri.conf.json（窗口标题/默认尺寸 960×640/最小尺寸 720×560）
4. tauri dev → 确认窗口能弹出
```

#### 产物

`tauri dev` 弹出一个空白窗口，标题"Serial Monitor V3"。

#### 涉及文件

`package.json` / `src-tauri/Cargo.toml` / `src-tauri/tauri.conf.json` / `src/main.tsx` / `src/App.tsx`

---

### Step 2：整体布局 + 视图切换 + 顶栏 + 状态栏

#### 内容

```
1. react-i18next 基础设施：
   - i18next + react-i18next 安装
   - src/i18n/zh.json（空对象——中文是 key 本身，fallback 到 key）
   - src/i18n/en.json（英文翻译映射，后续 Phase 激活）
   - src/i18n/index.ts（初始化配置：lng="zh", fallbackLng="zh"）
   - ⚠️ 所有 UI 文字从第一行起走 t()，遵守 V2.6 教训
2. CSS Grid 骨架——3 行 × 3 列：
   - Row 0 (auto): 顶栏（串口控制 | 工具按钮）
   - Row 1 (1fr): 图标栏 42px | 侧栏 220px | 主区 flex
   - Row 2 (auto): 状态栏
   - 图标素材从 V2 借用
   - 选中态 = 左侧蓝色指示条 + 图标高亮
   - 未选中态 = 灰色
   - 📟 默认选中
3. 侧栏组件（SidePanel.tsx）：220px，内容随 activeView 切换
   - 顶部标题栏："收发设置" + [◀] 折叠按钮
   - 折叠状态：宽度 28px（非 0），只显示 [▶] 展开按钮
   - 展开/折叠动画：CSS `transition: width 200ms ease`
   - 和 V2 的 `btnPanelCollapse` 同行为，折叠后内容隐藏但状态保持
4. 主区组件（MainContent.tsx）：flex:1，内容随 activeView 切换
5. 视图切换逻辑：
   - 点 📟 → activeView = "terminal"
   - 点 📊 → activeView = "workspace"
   - 点 ⚙ → activeView = "settings"
   - 点 ⚙ 不改变上一次的内容视图（和 V2 的 _previousContentTab 同逻辑）
6. 全局 CSS 变量（21 色，从 V2 ThemeMap 搬）：

   ```css
   :root {
     /* Phase 1 默认暗色主题 */
     --bg-window: #1E1E1E;
     --bg-card: #2D2D2D;
     --bg-status: #007ACC;
     --bg-input: #3C3C3C;
     --text-primary: #D4D4D4;
     --text-secondary: #9D9D9D;
     --text-muted: #6A6A6A;
     --accent: #0E639C;
     --accent-hover: #1177BB;
     --border: #3C3C3C;
     --separator: #474747;
     --icon-active: #FFFFFF;
     --icon-inactive: #858585;
     --system-log: #6A6A6A;
     --sent-echo: #0E639C;
     --received: #D4D4D4;
     --status-connected: #4EC9B0;
     --status-disconnected: #6A6A6A;
     /* 剩余色从 V2 ThemeMap 补齐 */
   }

   /* 亮色主题——现在就定义，后续 Phase 激活 */
   [data-theme="light"] {
     --bg-window: #FFFFFF;
     --bg-card: #F5F5F5;
     --bg-status: #007ACC;
     --bg-input: #FFFFFF;
     --text-primary: #2D2D2D;
     --text-secondary: #5A5A5A;
     --text-muted: #999999;
     --accent: #0078D4;
     --accent-hover: #106EBE;
     --border: #D4D4D4;
     --separator: #E8E8E8;
     --icon-active: #2D2D2D;
     --icon-inactive: #999999;
     --system-log: #999999;
     --sent-echo: #0078D4;
     --received: #2D2D2D;
     --status-connected: #388A34;
     --status-disconnected: #999999;
   }
   ```

   ⚠️ **任何组件禁止硬编码 hex 颜色。** 只能 `var(--xxx)`。

7. 反向约束：**组件不从 JS 设颜色，只从 CSS 读取。** 切主题时不存在"遍历控件逐个换色"——改 `<html data-theme>` 一个属性即可。
```

#### 产物

窗口有三栏布局，点击图标栏按钮 → 侧栏和主区切换。主区显示各视图的占位文字（"终端视图" / "工作台视图" / "设置视图"）。

#### 涉及文件

`src/App.tsx` / `src/App.css` / `src/components/IconBar.tsx` / `src/components/SidePanel.tsx` / `src/components/MainContent.tsx`

---

### UI 组件样式规范（Step 2 同步定义，全视图复用）

#### 字体层级

| 层级 | 字号/字重 | 用途 |
|------|:--:|------|
| 标题 | 13px / 600 | 侧栏分组标题（"显示"/"发送"/"编码"） |
| 正文 | 12px / 400 | 侧栏标签、按钮文字 |
| 接收区 | 12px / 等宽 | 数据（Sarasa Mono SC → Consolas → monospace） |
| 辅助 | 11px / 400 | 状态栏、placeholder、"输入要发送的内容…" |
| 微字 | 10px | 流量计数单位 |

#### 全局组件 class（定义在 `App.css` 或 `index.css`，所有视图复用）

**`.setting-group` — 统一分组容器：**

```css
.setting-group {
  background: var(--bg-card);
  border-radius: 6px;
  padding: 12px;
  margin-bottom: 8px;
}
.setting-group-title {
  font-size: 13px;
  font-weight: 600;
  color: var(--text-secondary);
  margin-bottom: 10px;
}
```

用于终端侧栏设置分组、卡片属性编辑器分组、设置页导航分组。**所有视图同一套视觉语言，未来加卡片标签页不会风格分裂。**

**`.toggle` — 滑动开关（替代 CheckBox 外观）：**

```css
.toggle {
  width: 36px; height: 20px; border-radius: 10px;
  background: var(--border);
  transition: background 150ms;
  cursor: pointer;
  position: relative;
}
.toggle.on { background: var(--accent); }
.toggle::after {
  content: '';
  position: absolute; top: 2px; left: 2px;
  width: 16px; height: 16px; border-radius: 50%;
  background: white;
  transition: transform 150ms;
}
.toggle.on::after { transform: translateX(16px); }
```

**`.input` — 文本输入框：**

```css
.input {
  background: var(--bg-input);
  border: 1px solid var(--border);
  border-radius: 4px;
  padding: 4px 8px;
  color: var(--text-primary);
  font-size: 12px;
  transition: border-color 150ms;
}
.input:focus { border-color: var(--accent); outline: none; }
```

**`.divider` — 分隔线：**

```css
.divider {
  height: 1px;
  background: var(--separator);
  margin: 4px 0;
}
```

#### 图标栏选中态

```
未选中：灰色图标，透明底
悬停： 图标变白，底微亮 rgba(255,255,255,0.05)，150ms 过渡
选中： 图标白，左侧 2px 蓝色指示条，底微弱蓝光晕 rgba(0,120,212,0.12)
```

**光晕实现（CSS）：**

```css
.icon-btn.active {
  background: radial-gradient(circle at center, rgba(0,120,212,0.12), transparent 70%);
}
```

---

#### 内容

**接收区（CM6）：**

```
1. CodeMirror 6 安装 + 基础配置
   - @codemirror/view + @codemirror/state + @codemirror/language
2. 只读模式，等宽字体（Sarasa Mono SC → Consolas → monospace）
3. 行号显示（CM6 内置 gutter）
4. 工具栏按钮：[⏸ 暂停] [导出日志] [清空接收区] [🔍 搜索]
   - 搜索 = CM6 内置 search() extension（Ctrl+F 同效）
   - 清空 = 清空 CM6 document + 重置行状态
5. 系统日志独立显示（接收区上方 36-72px 区域，可折叠）
6. "↓ 回到底部" 浮动按钮（用户上滚时出现，点击回到最新）
7. 右键菜单：复制 / 全选 / 清空 / 暂停（CM6 内置已覆盖复制/全选，补充清空/暂停）
```

**发送栏（Monaco 单行模式）：**

```
1. @monaco-editor/react 安装
2. 单行模式，等宽字体
3. `>` 命令前缀（Monaco 左侧，暗示"这是命令输入"）
4. placeholder："输入要发送的内容…"
5. 快捷发送条（接收区和发送栏之间，可折叠）：
   - 药丸形标签：[ AT ] [ AT+CWLAP ] [ + 添加 ]
   - border-radius: 12px, padding: 4px 10px, font-size: 11px, 等宽字体
   - 背景 var(--bg-card)，悬停 var(--accent)
   - 删除：鼠标悬停出现 ×
   - [+] 虚线边框，点击弹出输入框
6. 底栏按钮：[清空发送区] [发送历史 ▾] [发送]
7. Enter 发送 / Shift+Enter 换行
```

**侧栏收发设置（使用 `.setting-group` 分组卡片）：**

```
收发设置          [◀]                    ← 标题 + 折叠按钮

┌─ 显示 ────────────────────┐           ← .setting-group
│ 时间戳       [HH:mm:ss ▾] │
│ ☑ 消息回显                │
│ ☑ 行号显示                │
│ ☑ 系统消息独立显示         │
└───────────────────────────┘
     ↓ 8px 间距
┌─ 发送 ────────────────────┐
│ 换行符       [\r\n ▾]     │
│ ☑ 定时发送                │
│ 间隔(ms)     [1000]       │
│ ☑ 发送后清空              │
└───────────────────────────┘
     ↓ 8px 间距
┌─ 编码 ────────────────────┐
│ 接收模式     [文本 ▾]      │
│ 接收编码     [UTF-8 ▾]    │
│ 发送模式     [文本 ▾]      │
│ 发送编码     [UTF-8 ▾]    │
└───────────────────────────┘
```

每组是一个圆角卡片（`border-radius: 6px`，`background: var(--bg-card)`，`padding: 12px`），组间 8px 间距。比 V2 的分隔线更清晰——用户一眼知道"显示类设置"和"发送类设置"是独立分组。

**⚠️ `.setting-group` 是全局 class——以后工作台卡片编辑器、设置页导航分组都复用同一套，不会风格分裂。**

⚠️ **所有 UI 文字走 `t()`：** `{t("时间戳")}` / `{t("消息回显")}` / `{t("换行符")}` ……翻译表从 V2 EnMap 搬，Phase 1 先全中文。

#### 视觉标准（和 V2 配色相同，层次和动效升级）

详见终端视图视觉标准（memory `terminal-visual-standard.md`）。核心要点：

**层次——三层深度：**

| 区域 | 底色 | 效果 |
|------|------|------|
| 图标栏 | `#18181B`（最深） | 视觉后退 |
| 侧栏 | `#1E1E22`（中等） | 半退 |
| 接收区 | `#252528`（最浅） | 前站，用户眼睛停留最久 |

**微交互：**

| 场景 | 动效 | 时长 |
|------|------|------|
| 打开串口 | 状态圆点灰→绿色相渐变 | 300ms |
| 收到新数据 | 新行滑入 + 极短蓝色闪烁（50ms 半透明蓝底→消失） | 50ms |
| 发送数据 | 发送按钮 `scale(0.96)→scale(1)` | 100ms |
| 暂停 | 接收区顶滑入黄条"⏸ 已暂停 · N 条缓冲" | 200ms |
| 清空接收区 | 内容淡出→清空→淡入 | 200ms |
| 切换视图 | 主区交叉淡入淡出 | 150ms |
| 按钮悬停/按下 | 背景色渐变 + `scale(1.02)` / `scale(0.97)` | 150/100ms |

**发送栏——`>` 命令前缀：** Monaco 单行左侧 `>` 暗示"这是命令输入"，和接收区区分定位。

**滚动条——极细半透明：** 4px 宽，`rgba(255,255,255,0.15)`，悬停变亮，不抢数据视觉重心。

**状态栏——轻量化：** 和顶栏同色底，不用 V2 的全蓝底，靠绿色圆点区分连接状态。

⚠️ **所有动画跑 GPU 合成线程（`transform` + `opacity`），不用 `width`/`height`/`top`/`left`。动效 ≤ 300ms。**

#### 产物

终端视图 UI 全部可见，接收区和发送栏可以手动输入文字（但串口还没通——Step 7 接通）。

#### 涉及文件

`src/components/views/TerminalView.tsx` / `TerminalSidebar.tsx` / `package.json`（新增依赖）

---

### Step 4：工作台 + 设置视图（空壳）

#### 内容

```
1. WorkspaceView.tsx：居中显示"暂无卡片，请先连接串口"（半透明灰色）
2. WorkspaceSidebar.tsx：空壳（Phase 2 显示卡片属性编辑器或概览列表）
3. SettingsView.tsx：居中显示"即将推出"
4. SettingsSidebar.tsx：导航菜单空壳（串口配置 / 主题 / 语言 / 关于）
```

#### 产物

点击 📊 → 工作台空壳。点击 ⚙ → 设置空壳。视图切换流畅，无报错。

#### 涉及文件

`src/components/views/WorkspaceView.tsx` / `WorkspaceSidebar.tsx` / `SettingsView.tsx` / `SettingsSidebar.tsx`

---

### Step 5：核心服务

#### 内容

**DataConverter.ts（V2 照搬，~60 行）：**

```
BytesToHex(bytes: Uint8Array): string
HexToBytes(str: string): Uint8Array
BytesToText(bytes: Uint8Array, encoding: string, byteBuffer: number[]): string
TextToBytes(str: string, encoding: string): Uint8Array
ValidateHexString(str: string): string
```

**ProtocolParser.ts（V2 照搬，~80 行）：**

```ts
interface ParseResult {
  messages: ProtocolMessage[]   // { id: string, fields: string[] }
  plainText: string
}

Parse(rawLine: string): ParseResult
```

解析规则从 V2 照搬：方括号分割 → 逗号切字段 → 引号包裹处理。Phase 1 只写不接通——接收区 Raw 模式直接用纯文本，不做协议路由。

**RingBuffer.ts（~40 行）：**

```
class RingBuffer<T> {
  constructor(capacity: number)  // 默认 512
  write(item: T): void
  drainAll(): T[]               // 取出当前所有数据，清空
}
```

单生产者（Rust emit 回调写）、单消费者（rAF 读）。固定容量，满则覆盖最老。

**PreferenceService.ts（~80 行）：**

```ts
// 唯一配置入口。GUI 操作、文件监听、未来 WebSocket——三条路全走它。
// 遵守 V3 设计方案 §1.5 的硬约束：没有"人用的 API"和"AI 用的 API"两套东西。

class PreferenceService {
  // prefs.json（全局配置）
  static loadPrefs(): Prefs
  static savePrefs(prefs: Prefs): void

  // workspace.json（卡片布局，Phase 1 只建不填）
  static loadWorkspace(name: string): Workspace
  static saveWorkspace(name: string, ws: Workspace): void
  static listWorkspaces(): string[]
}
```

**prefs.json 结构（Phase 1）：**

```json
{
  "window": { "left": 100, "top": 50, "width": 960, "height": 640 },
  "theme": "Dark",
  "lastPort": "COM3",
  "preferences": {
    "timestampFormat": "HH:mm:ss:fff",
    "showEcho": true,
    "showLineNumbers": true,
    "separateSystemLog": true,
    "lineEnding": "\r\n",
    "autoRepeat": false,
    "repeatInterval": 1000,
    "autoClear": false,
    "receiveMode": "text",
    "receiveCoding": "UTF-8",
    "sendMode": "text",
    "sendCoding": "UTF-8"
  },
  "quickSends": { "AT": "AT\r\n" }
}
```

**⚠️ 架构约束：侧栏设置不走 React state——走 PreferenceService。**

```
用户点 ☑消息回显 → onToggle
  → PreferenceService.savePrefs({ ...loadedPrefs, preferences: { ...showEcho: true } })
  → React 从 PreferenceService 读最新 prefs → UI 刷新
```

AI 改配置：
```
直接打开 prefs.json → 改 "showEcho": false → 保存
  → 文件监听（Step 8 后加）→ PreferenceService 检测变化 → React 刷新
```

人和 AI 在同一个 JSON 文件上交汇。不改 React 源码。

#### 验证

每个服务独立可测——DataConverter 和 ProtocolParser 写几个单测用例验证行为与 V2 一致。

PreferenceService 验证：修改 prefs.json → 侧栏设置开关自动同步。GUI 改设置 → prefs.json 文件内容更新。

#### 涉及文件

`src/core/DataConverter.ts` / `src/core/ProtocolParser.ts` / `src/core/RingBuffer.ts` / `src/core/PreferenceService.ts`

---

### Step 6：Rust 串口层

#### 内容

**Cargo 依赖：**

```toml
[dependencies]
tauri = { version = "2", features = [] }
serialport = "4"
tokio = { version = "1", features = ["full"] }
encoding_rs = "0.8"
```

**serial.rs（~150 行）：**

```
结构：
  SerialState { port: Option<SerialPort>, line_buffer: String, ... }

命令（前端 invoke）：
  list_ports() -> Vec<PortInfo>         // 枚举可用 COM 口
  open_port(state, config) -> Result    // 打开串口 + 启动读线程
  close_port(state) -> Result           // 关闭串口 + 冲刷残留
  send_data(state, bytes) -> Result     // 写入字节
  set_dtr(state, enable) / set_rts(state, enable)

读线程（tokio::spawn）：
  loop {
    port.read(&mut buf)
    → 解码（UTF-8 / GBK）→ 拼入 line_buffer
    → \n 拆分 → 完整行 → app_handle.emit("serial-data", { text, mode })
    → 100ms 无新数据 → 强刷残留行
  }

关闭逻辑（参考 V2 SerialPortSession.Close）：
  ① 标记 _is_closing
  ② 停止读线程
  ③ 冲刷 line_buffer 残留
  ④ port.close()
```

#### 验证

`tauri dev` → 前端控制台 `console.log` 能看到 COM 口列表。接上 STM32 板子 → 打开串口 → 前端控制台刷出 `{ text: "...", mode: "text" }`。

#### 降级方案

如果 Rust serialport 在 Windows 上踩坑太多（COM 枚举、USB 热插拔），切 Electron + Node.js `serialport`。React 前端全部保留不动。

#### 涉及文件

`src-tauri/src/serial.rs` / `src-tauri/src/main.rs` / `src-tauri/Cargo.toml`

---

### Step 7：接通数据管道

#### 内容

```
1. 前端 listen("serial-data", e => RingBuffer.write(e.payload))
2. requestAnimationFrame 循环：drainAll → CM6 appendLine
3. 实现三色行着色（CM6 decorations）：
   - 接收数据 → 白色（暗色主题）/ 深灰（亮色主题）
   - 发送回显 → 蓝色（#0E639C 暗色 / #0078D4 亮色）
   - 系统消息 → 灰色（#6A6A6A 暗色 / #999999 亮色）
4. 蓝色回显：用户发送 → LogSent(text) → CM6 追加蓝色行
   - HEX 模式：显示字节数 + hex 预览
   - Text 模式：显示编码 + 内容
5. 智能滚底：检测用户是否在底部 → 不在则显示 ↓ 按钮
6. 暂停 + 缓冲（上限 2000 条）：
   - 暂停 → 数据进缓冲区不写 CM6
   - 恢复 → 缓冲区全部回放
7. 行数上限裁剪（MaxLogLines = 2000，超出砍最早 500 行）
8. 日志导出：SaveFileDialog → File.WriteAllText（UTF-8）
9. 定时发送：setInterval 按间隔调用 send
10. 发送后清空发送区：send 后自动 tbSend.clear()
11. 快捷发送：可添加/编辑/删除预设按钮
```

#### 产物

接上 STM32 板子 → 终端视图收发数据 → 三色行正常 → 暂停/搜索/导出/清空/定时发送/快捷发送全通。行为和 V2 终端视图一致。

#### 涉及文件

`src/components/views/TerminalView.tsx`（更新） / `src/core/RingBuffer.ts`（使用）

---

### Step 8：~~顶栏 + 状态栏~~（已并入 Step 2）

> **2026-07-18 修订：** 顶栏的串口控制和工具按钮（中/EN、☀、?）是全局控件，贯穿所有视图。读 V2 MainWindow.xaml 确认布局是 3 行（顶栏/内容/状态栏），不是 3 列。因此顶栏 + 状态栏提前到 Step 2 作为布局骨架的一部分。

以下为原计划保留以供参考，实际实现见 Step 2。

<details>
<summary>原 Step 8 详情（点击展开）</summary>

```
[COM3 ▾] [115200 ▾] [● 打开]  │  中/EN  ☀  ?  ⚙
  串口控制区                    │    工具区
```

```
1. 视觉分区：中间用半透明分隔竖线隔开
   - 左边 = 串口控制区（跟串口相关的操作）
   - 右边 = 工具区（跟软件本身相关的操作）
   - 分隔线：1px × 20px，rgba(255,255,255,0.08)，左右 12px margin
2. 端口下拉框：Step 6 的 list_ports 填充，定时刷新（热插拔检测）
3. 波特率下拉框：预设列表（115200 默认）
4. 打开/关闭按钮：Toggle 状态
   - 打开时：背景 var(--accent)，文字"● 已连接"
   - 关闭时：背景 var(--bg-card)，文字"● 打开"
   - 状态切换：圆点灰→绿色相渐变 300ms，按钮背景色渐变 150ms
5. 语言切换和主题切换 Phase 1 只放按钮空壳——点击只打印 log
```

**状态栏（轻量化）：**

```
● 已连接 │ TX:1,234 ↑  RX:56,789 ↓
```

```
1. 底色：和顶栏同色（var(--bg-window)），不用 V2 的全蓝底
2. 状态圆点：已连接=绿色 / 已断开=灰色
   - 打开串口：灰→绿色相渐变 300ms
   - 关闭串口：绿→灰色相渐变 300ms
3. 状态文字：已连接 / 未连接 / 端口名，11px 辅助字号
4. TX/RX 计数：从 Rust 端获取，实时更新，10px 微字号
5. 流量分隔编号：TX 和 RX 之间用 `│` 分隔
6. 右键点击流量 → 重置计数
```

`src/components/TopBar.tsx` / `src/components/StatusBar.tsx`

</details>

---

### Phase 1 验证清单

```
☑ tauri dev 弹出窗口，标题"Serial Monitor V3"
☑ 三栏布局：图标栏 42px + 侧栏 220px + 主区 flex
☑ 图标栏：📟/📊/⚙ 三个按钮，点击切换视图
☑ 终端视图：CM6 接收区 + 系统日志 + "↓回到底部"按钮
☑ 终端视图：Monaco 单行发送栏 + Enter 发送 + 快捷发送条
☑ 终端视图：侧栏收发设置（时间戳/回显/行号/系统消息/换行符/编码/定时发送/发送后清空）
☑ 工作台视图：空壳占位文字
☑ 设置视图：空壳占位文字
☑ 顶栏：端口选择/波特率/打开按钮
☑ 状态栏：连接状态 + TX/RX 计数
☑ 串口：枚举 COM 口 → 打开 → 收数据 → 前端 CM6 显示 → 发数据 → STM32 收到
☑ 三色行：接收白/发送蓝/系统灰
☑ 蓝色回显：Text 模式显示编码+内容，HEX 模式显示字节数+预览
☑ 暂停 + 缓冲 + 恢复回放
☑ 智能滚底：上滚显示 ↓ 按钮，到底隐藏
☑ 搜索（Ctrl+F）
☑ 日志导出（UTF-8 txt）
☑ 行数上限裁剪（2000 行 → 砍 500）
☑ 系统消息独立显示（可折叠）
☑ DataConverter / ProtocolParser / RingBuffer 单测通过
```

---
