# E5.8 执行清单——归一化基建

> 2026-08-16。**dsh 借鉴落地——抄工程纪律，不抄产品形态。** 来源 → [00-dsh借鉴档案.md](00-dsh借鉴档案.md)。
> **🔥 进度唯一真相源。** 做完一个勾一个。E5.7 清单已封存，不再追加任务。
> **目标：** 归档基线 → 工程纪律（门禁四件套 + 两条规则）→ 可逆注册升级 → 依赖编排 → 契约生成（linkdesk.d.ts）→ 全量回归。
>
> **当前进度：3/25 已勾（#0a + #0b + #0d）+ 用户补充 #0d.5 已立案（2026-08-16）——Phase 0 文档骨架。**
>
> **编号铁律（E5.5 任务执行铁律）：** 计划任务用整数；用户后续补充用小数后缀（#45.6/#63.8 同款）**线性插入在当前进度之后**，不清洗重排。序号间隔不做预留——补充就是小数。
>
> 📖 **设计总览：** [01-归一化基建设计.md](01-归一化基建设计.md)
> 📖 **借鉴来源：** [00-dsh借鉴档案.md](00-dsh借鉴档案.md)（四路侦察摘要 + 借鉴清单 v2 + 不抄清单）
> 📖 **对手盘源码：** `E:\deepseek-harness`（本地克隆，2026-08-16）

---

## 关键路径

```
Phase 0：归档与基线（串行——文档 + 分支）
Phase 0.5：用户补充修复（随时插入——与四大支柱正交，小数后缀编号）
Phase 1：工程纪律（串行——先行，零架构风险，保护后续重写期）
Phase 2：可逆注册升级（串行——Phase 1 后；最大推翻：loader + 14 Registry）
Phase 3：依赖编排（串行——Phase 2 后，状态机之上）
Phase 4：契约生成（Phase 2 后即可，与 Phase 3 可并行；最大新建：linkdesk.d.ts）
Phase 5：全量回归（最后）
```

---

## 现状速查

| 文件 | 当前状态 | E5.8 动作 |
|:--|:--|:--|
| `package.json` check 链 | tsc×2 + grid + pool-css + eslint `--max-warnings 0` + vitest | + jscpd + knip（Phase 1） |
| `src/core/registry/RegistryBase.ts` | markPlugin/unregisterAll 登记注销对称（E4 Emitter Pattern） | v2：register 返 disposer + 逆序回滚（Phase 2） |
| `src/core/registry/` 14 文件 | register() 返 void，卸载靠 unregisterAll 手动对称 | 逐项返 disposer（Phase 2） |
| `src/pluginLoader/loader.ts` + `lifecycle.ts` | glob 扫描序加载 + PluginLifecycle 事件 | 生命周期状态机（PENDING→ACTIVE→DISPOSED）+ requires 编排（Phase 2/3） |
| `preload-pool.ts` / `preload-shell.ts` | window.linkdesk.* 双面 + ambient 类型 Record<string, any>（E5.7#97 已记） | 生成契约 linkdesk.d.ts（Phase 4） |
| `src/core/api/linkdesk-api.ts` | 文档面（与 preload 实现面两份真相源） | 对接生成契约，文档面收敛（Phase 4） |
| jscpd / knip / lefthook | 不存在 | 接入（Phase 1） |
| `package-lock.json`（npm） | 无 Node 版本带强制 | engines + engine-strict（Phase 1 #4） |

---

# Phase 0：归档与基线

> 不动代码。E5.7 收官封存 + E5.8 边界建立。

- [x] **E5.8#0a** 新建 `e5.8` 分支（从 e5.7 切出）——所有 E5.8 改动在新分支（执行注 2026-08-16：已建，工作树干净，git 用户 Encaron）
- [x] **E5.8#0b** E5.7 执行清单顶部加封存声明 → 指向本清单（执行注 2026-08-16：E5.7 清单标题下加"🔴 2026-08-16 封存——E5.8 已开战"声明行，不追加任务、不动已有任务）
- [ ] **E5.8#0c** 🔴 基线实跑记录——`npm run check` 实测全绿并记录为 E5.8 基线（E5.7#93 收官记录 = tsc 0×2 / eslint 0+0 / vitest 433/433 / grid ✓ / pool-css ✓——以本次实跑为准）
- [x] **E5.8#0d** E5.8 文档骨架——执行清单（本文件）+ 设计总览 + 借鉴档案（执行注 2026-08-16：三文档落地，见 commit）

---

# Phase 0.5：用户补充修复

> 用户后续补充的任务点落此区——小数后缀编号（编号铁律），线性插入在当前进度之后，不清洗重排。与四大支柱正交，随时可做。

- [ ] **E5.8#0d.5** 设置页"以 JSON 打开"应打开 settings.json 而非弹只读弹窗（用户报告 2026-08-16："设置页面点击以json打开,仍旧仅仅只是弹窗"）
  - 现状（已实锤）：`SettingsView.tsx` L258-270 JSON 按钮 onClick 只 `getUserSettings()` → `setJsonDialog(...)` 弹只读 `<pre>` 弹窗（L319-335，含"确定"按钮）——不是打开 JSON 文件
  - 方案（执行时定，对标 VS Code "打开设置 (JSON)"）：在编辑器标签页打开真实 settings.json（可编辑）。既有通道：`explorer.openFile` 命令（file-tree 插件注册，扩展名→FileAssociation→createTab，`FileTreeContextMenu.tsx` L158）或壳侧等价通道；需先拿到 settings.json 真实落盘路径（userData 目录）
  - 验收：点击 JSON 按钮 → 编辑器标签页打开 settings.json 可编辑；只读弹窗移除
  - 门禁：`npm run check` 全绿 + git commit

---

# Phase 1：工程纪律

> 目标：机械哨兵先行——重复/死代码/供应链风险不靠人眼。零架构风险，保护 Phase 2/3 重写期。
> ✅ 完成标准：`npm run check` 全绿且含新门禁；存量问题清零或显式豁免（豁免必须带理由注释，不靠基线放过）。

- [ ] **E5.8#1** jscpd 重复检测入 check
  - `.jscpd.json`（对标 dsh：minTokens 60 / minLines 6 / mode mild / `jscpd:ignore-start~end` 注释块豁免）
  - `npm run duplication` 脚本 + 接入 check 链
  - 存量重复处置：修复或豁免注释——**不设基线放过**
  - 验收：check 含 duplication 且全绿；grep 无未豁免重复
- [ ] **E5.8#2** knip 死代码检测入 check
  - `knip.json` 双工程 entry（src/ + electron/，规模远小于 dsh 的 700 行——预计 30 行内）
  - `npm run knip` 脚本 + 接入 check 链
  - 存量死导出/死依赖/孤儿文件清零或显式 ignore 带理由
  - 验收：check 含 knip 且全绿
- [ ] **E5.8#3** lefthook 自动装 + 快检分层
  - postinstall 自动 install（对标 dsh `install-lefthook.mjs`——自动装不靠文档要求装）
  - pre-commit：staged lint --fix + whitespace（`git diff --cached --check`）；pre-push：typecheck
  - 原则照抄 dsh："local checkpoints fast; CI owns the full matrix"——无 CI，pre-push 是最后防线
  - 验收：hook 生效实测（提交触发 staged lint / 推前 typecheck）
- [ ] **E5.8#4** 供应链纪律 npm 适配版
  - dsh 的 allowBuilds 是 pnpm 10+ 专属——**不切包管理器**，npm 等价物：`engines` + `.npmrc` `engine-strict=true`（锁 Node 版本带，防错版本装机）+ package-lock 钉死
  - 原生模块清单审视：serialport / electron 的 install scripts 逐一记录（干什么、为什么需要）——写进开发管理文档
  - pnpm allowBuilds 记入"若切 pnpm"备注（远期，不现在切）
  - 验收：engine-strict 生效实测（错 Node 版本安装被拒）；原生模块清单落盘
- [ ] **E5.8#5** pushLayout 两条规则入协议注释（第三梯队吸收）
  - whole-value checkpoint：状态事件必须带完整 post-change 值——禁止裸 delta
  - delta 必须带稳定 id + 升序重放确定性——不得依赖 live-only 内存
  - 落点：`poolLayout.ts` 头注释 + `preload-pool.ts` + E5.7 设计文档 §7.2 补注
  - 验收：三处注释在位；现有 pushLayout 面核对无违反（拖拽乐观覆盖的回执对齐 guard 已合规则）
- [ ] **E5.8#6** 决策记录义务（Agent Notes 缩水版）
  - `docs/decisions/` 目录 + 规则："非平凡改动必须落一篇决策记录"（路径编码状态 proposed/implemented/rejected + 被取代即归档）
  - 写入开发管理文档（对标 AI 友好第 3 层：决策轨迹给未来 AI 当上下文）
  - 验收：目录 + 规则成文；本清单后续每个任务的执行注与决策记录挂钩

---

# Phase 2：可逆注册升级

> 目标：一切注册返回 disposer，卸载逆序回滚——插件重装契约（"可卸载 ⇒ 可重装"）的机械保障。最大推翻项。
> 📖 细节设计 → Phase 开工时写 `02-可逆注册设计.md`（E5.7 同款节奏）。
> ✅ 完成标准：14 Registry register() 全返 disposer；loader 状态机可诊断；卸载/重装实测拆净（无残留监听/注册/定时器）。

- [ ] **E5.8#7** 摸底矩阵——全仓注册点清单
  - 14 Registry × register/on/订阅面 + PluginLifecycle 订阅 + useIpcEvent + 定时器/interval 等非 Registry 注册点
  - 现有 dispose 覆盖度逐项标注（RegistryBase 已覆盖 / 手动清理 / 无退路）
  - 产出：注册点矩阵文档（Phase 2 的执行地图）
  - 验收：矩阵覆盖全仓 grep 实证，无遗漏登记
- [ ] **E5.8#8** 设计文档 `02-可逆注册设计.md`
  - RegistryBase v2 路线：register() 返 disposer + effect 登记器（逆序回滚）+ markPlugin 机制归并
  - 双轨兼容期决策：一步到位 vs 过渡期（按 #7 矩阵规模拍板）
  - loader 生命周期状态机设计（PENDING→LOADING→ACTIVE→UNLOADING→DISPOSED + 诊断面）
  - 验收：用户过目拍板
- [ ] **E5.8#9** RegistryBase v2
  - effect 登记器 + 逆序回滚（对标 ctx.effect：execute 立即跑，disposer 逆序跑）
  - markPlugin 机制归并——register 返 disposer 即登记，disposer 跑即注销，两套机制合一
  - 验收：单测覆盖逆序/重复注册/卸载后调用安全
- [ ] **E5.8#10** 14 Registry register() 逐项返 disposer
  - CommandRegistry / MenuRegistry / ThemeRegistry / ProtocolRegistry / KeybindingRegistry / IconRegistry / ConfigurationRegistry / ContextKeyService / LangDefRegistry / LanguageRegistry / StatusBarService / QuickPickService / ClipboardProviderRegistry
  - 消费方适配：插件/壳调用面从"注册了不管"改为"挂 disposer 或交回 RegistryBase"
  - 验收：tsc 零错误；每 Registry 单测钉"注册→dispose→查询为空"
- [ ] **E5.8#11** loader 生命周期状态机
  - 状态迁移 + 失败原因记录（"插件为什么没激活"可查——对标 dsh 诊断面）
  - loader.ts / lifecycle.ts 接入状态机
  - 验收：制造坏插件（坏 schema/缺 entry/加载抛错）→ 状态可查原因；诊断面测试
- [ ] **E5.8#12** 卸载链路机械保障 + 测试
  - 插件卸载 = disposer 全量回滚（含 #7 矩阵中的非 Registry 注册点）
  - 旧手动 unregister 摘除（lifecycle.ts 手动列举清零）
  - 测试钉契约：装载→使用→卸载→重装→再使用 全链通（重装契约）
  - 验收：卸载/重装实测拆净；tsc/eslint/vitest 全绿

---

# Phase 3：依赖编排

> 目标：plugin.json `requires` 声明依赖——顺序框架算，不靠约定。第三方插件互相依赖不惊动壳作者。
> 前置：Phase 2 状态机。
> ✅ 完成标准：requires 声明可用；循环依赖 fail-loud；依赖消失连带卸载、回来自动重载。

- [ ] **E5.8#13** `requires` 字段全链路声明
  - `plugin.schema.json` + `PluginManifest` 类型 + docs/03-插件制造/06-plugin.json规范 同步
  - 语义定义：按名声明依赖（不 import 具体实现——低耦合）
  - 验收：schema 校验生效；文档示例在位
- [ ] **E5.8#14** loader 依赖解析 + PENDING 挂起
  - 依赖未就绪 → PENDING（复用 #11 状态机）；循环依赖检测 fail-loud
  - 加载顺序由依赖图决定，扫描序不再影响激活
  - 验收：依赖插件后加载也能正确编排（测试钉序）
- [ ] **E5.8#15** 依赖消失连带卸载 + 自动重载
  - 被依赖插件卸载 → 消费插件 PENDING（连带卸载，对齐卸载契约）；被依赖插件重装 → 消费插件自动 ACTIVE
  - 验收：卸载/重装链实测（含内置插件适配）
- [ ] **E5.8#16** 适配 + 测试
  - 现有插件（内置三件套 + 用户插件）零改动兼容（无 requires = 无依赖，行为不变）
  - loader 测试补齐：无依赖/有依赖/缺依赖/循环/重载五态
  - 验收：vitest 全绿；现有插件实机装卸无回归

---

# Phase 4：契约生成——linkdesk.d.ts

> 目标：window.linkdesk.* 从"双面手写 + ambient any"收敛为单一生成契约——漂移编译期死。第三方作者拿 d.ts 获得编译期安全（vscode.d.ts 同款）。
> 前置：Phase 2 后即可（与 Phase 3 可并行）。
> 📖 细节设计 → Phase 开工时写 `03-契约生成设计.md`。
> ✅ 完成标准：生成器产出 linkdesk.d.ts；双工程接入；contracts-ready 门禁（契约未生成编译不过）。

- [ ] **E5.8#17** 摸底——linkdesk.* 命名空间全量清单
  - preload-pool.ts / preload-shell.ts 的 exposeInMainWorld 面 + linkdesk-api.ts 文档面 + IPC channel 双端表
  - 漂移点登记（文档面与实现面不一致处——E5.7#97 已知 ambient any 欠账在此盘点）
  - 产出：命名空间×channel×双面矩阵
  - 验收：矩阵覆盖全量命名空间（20 命名空间量级），grep 实证
- [ ] **E5.8#18** 设计文档 `03-契约生成设计.md`
  - 标注语法选型（preload 上下文限制下：标记注释 vs 显式注册表）+ 生成物形态 + 门禁接线
  - 用户过目拍板
- [ ] **E5.8#19** 生成器实现
  - 脚本 + 模板 → 生成 `linkdesk.d.ts` + 双面签名校验件
  - 验收：生成物可独立跑通；改动标注 → 生成物 diff 可见
- [ ] **E5.8#20** 双工程接入
  - preload/IPC handler 两侧签名过生成器（漂移即编译错）
  - 壳/池/electron 三侧 tsc 链路接入
  - 验收：tsc 零错误；故意制造漂移 → 编译失败（实测一次）
- [ ] **E5.8#21** contracts-ready 门禁
  - 生成物未更新 = 编译失败——check 链接线
  - 验收：删生成物 → check 红（实测一次）；恢复 → 绿
- [ ] **E5.8#22** API 文档对接
  - docs/03-插件制造 契约文档改为生成 d.ts 指针（文档面收敛——不再手写第二份真相源）
  - 验收：文档与生成物一致；插件侧示例走 d.ts

---

# Phase 5：全量回归

> 目标：E5.8 全链验证——门禁刷新 + 场景回归 + 收官。

- [ ] **E5.8#23** 门禁收口
  - `npm run check` 全绿（含 Phase 1 新门禁 + Phase 4 contracts-ready）+ 基线刷新记录
  - 验收：check 实跑全绿；基线数字落盘
- [ ] **E5.8#24** 场景回归（对标 E5.7 Phase 16 场景）
  - 启动 / 插件装卸+重装 / 命令+快捷键 / 热更新语义 / 崩溃恢复五件套 / 分屏拖拽（回执对齐 guard 回归）
  - 验收：用户实机验证逐场景通过
- [ ] **E5.8#25** 收官声明 + 下一步指针
  - 执行清单收官声明；下一步候选（E6 蓝图 / E5.9 视用户补充）
  - 验收：用户过目拍板

---

## 门禁（每任务通用）

`npm run check` 全绿（= 双工程 tsc + grid + pool-css + eslint `--max-warnings 0` + vitest + **E5.8 新增 jscpd/knip**）+ git commit。一次一个小任务，做完等用户检查。执行注写入任务条目（E5.7 同款）。
