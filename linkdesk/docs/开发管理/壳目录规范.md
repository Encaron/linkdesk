# 壳目录规范

> 2026-08-12。E5#30 写了插件目录规范，但没写壳目录。E5#42 建 `data/` 没定义语义→今天的 #36k 返工。**此后所有壳代码遵守此规范。**

---

## 1. `src/core/`——圆形大厅（桌子集合）

**准入标准（三条同时满足）：**
1. 多提供方——多个插件可能登记到这张桌子上
2. 多消费方——多个插件可能翻这张桌子
3. 桌子不知道内容——桌子本身不知道登记的信息是什么意思

| 子目录 | 语义 | 放什么 | 不放什么 |
|--------|------|--------|----------|
| `registry/` | Registry 模式——登记/查询/注销 | CommandRegistry, MenuRegistry, ThemeRegistry, CardRegistry, ProtocolRegistry, KeybindingRegistry, IconRegistry, ConfigurationRegistry, ContextKeyService, LangDefRegistry, LanguageRegistry, FileDecorationRegistry, StatusBarService, QuickPickService, MenuRegistry, ClipboardProviderRegistry | 有副作用的 Service（放 services/） |
| `services/` | 有副作用的 Service——读写/网络/IPC | ConfigurationService, FileService, LayoutEngine, WorkspaceService, NotificationService, PluginStateService, StorageService, DialogService, EncodingService, FileSearcher, LogChannel, FactorySlots, ErrorService, ThemeEngine, AppInitializer, ViewContainerService, IpcBridgeHandler, ProfileService, FileAssociationService, LayoutService | 纯 Registry（放 registry/），纯工具函数（放 utils/） |
| `pipeline/` | 数据管道——原始字节→卡片消费 | ProtocolParser, RingBuffer, DataConverter, DataDispatch | Registry/Service/通用工具 |
| `utils/` | 纯函数工具——无状态/无副作用 | CancellationToken, pathUtils, assetPath, splitTree（分屏树纯操作——壳/Pool 双进程共用，从 src/hooks/ 迁入） | 有模块级状态的（放 services/） |
| `react/` | React hooks + context + 事件总线 | useSendData, useConfiguration, usePluginIpcEvent, TabActionsContext, CoreEvents, ShellEvents, SidebarTabSync, useConfigurationIpc | 非 React 代码（放对应目录） |
| `api/` | 类型定义 + API 契约 | linkdesk-api.ts, v3Api.ts, types.ts | 业务逻辑 |
| `types/` | 纯类型——跨模块共享的 type/interface | poolLayout.ts | 带实现代码的（放对应目录） |
| `builtin/` → `commands/` | 壳自举代码——命令/快捷键/菜单注册 | coreCommands, shellKeybindings, shellMenus, CoreCallbacks, settingsCommands, tabCommands, developerCommands, registerBuiltinProtocols | 插件代码（放 plugins/） |

**判据：** 文件放 `core/` 根目录之前——它属于哪个 subsystem？找不到 → 别放 core/。

---

## 2. `src/pool/`——Pool 渲染层（E5.7：唯一 WebContentsView 内）

> 🔥 2026-08-12 E5.7 更新。E5.6 的 `sidebar/`/`main/`（双Pool 专属）已删——SidebarRenderer/MainRenderer 迁入 `zones/`。"加 Pool = 加目录"随双Pool 废弃。

| 子目录 | 语义 | 放什么 |
|--------|------|--------|
| 根目录 | 入口 + 唯一根组件 | `pool-main.tsx`（只创建 PoolZoneShell）、`PoolZoneShell.tsx`（flex 布局所有 zone） |
| `zones/` | 🔥 布局分区——每个 zone 一个文件，**零跨 zone import** | TitleBarZone, IconBarZone, TabBarZone, SidebarZone, EditorZone, PanelZone, StatusBarZone, TopBarZone, RightSidebarZone, FloatingLayerHost |
| `shared/` | 跨 zone 共享 | PluginComponent, GroupTabBar（两个以上 zone 用的组件放这里） |
| `hooks/` | 🔥 池内 UI 交互 hooks | useSidebarResize, useSplitResize, useDragReorder, useTabDropPreview, useDragDetach（从 src/hooks/ 迁入——只被 zone 使用） |
| `views/` | 壳级视图 | ShellViewRenderer, WelcomePoolView, PluginDetailPoolView, OutputPoolView |

**原则：加 Zone = 三步——poolLayout.ts 加字段 → `zones/` 新建文件 → PoolZoneShell 加条件渲染 div。现有 zone 零改动。**

---

## 2.5. `src/overlay/`——🔴 已废弃（E5.6 时代，E5.7 删除）

> E5.6 的 OverlayWindow 是独立透明 BrowserWindow。E5.7 极简Pool 无 OverlayWindow——浮层全部迁入 `src/pool/zones/FloatingLayerHost.tsx`（CSS `position: fixed` + z-index）。整目录删除（清理方案 §3）。**新浮层组件写进 FloatingLayerHost，不要新建 overlay 目录。**

---

## 3. `src/components/`——壳 React 组件（E5.7 大幅迁出）

> 🔥 2026-08-12 E5.7 更新。壳不再渲染 UI DOM（被唯一 WCV 覆盖）——壳只留状态逻辑（tabState/syncLayout/Registry/命令执行）。

| 子目录 | 语义 | 放什么 |
|--------|------|--------|
| 根目录 | 壳布局组件 | 🔴 E5.7 删除——SidePanel/MainContent/StatusBar/TitleBar/IconBar 迁入 `src/pool/zones/` |
| `views/` | 壳级页面 | PluginDetailView（壳版）——随壳 DOM 迁移或删除 |
| `shared/` | 归一化 UI 控件 | E5.7 后壳侧只剩状态无关工具；浮层类（ContextMenu/MenuRenderer/ConfirmDialog 等）迁 Pool FloatingLayerHost；SelectBox/InlineInput/ErrorBoundary 等被 zone/插件使用的迁 `src/pool/shared/` |

**判据：** 两个以上地方用 → `shared/`。只有一个地方用 → 放消费方目录。**E5.7 新组件一律写进 `src/pool/`——壳侧不再新增 UI 组件。**

---

## 4. `src/hooks/`——React hooks

纯 hooks——`use*` 前缀，一个文件一个 hook。不夹带组件、不夹带 service。

> 🔥 E5.7 分工：壳侧状态 hooks（useTabManager/usePoolSync/useHeartbeat/useMemoryMonitor 等壳逻辑）留此；**UI 交互 hooks 迁 `src/pool/hooks/`**——现有 useClickPreview/useDragHandle/useDragReorder/useDebouncedInput + tabDragTypes 迁入，E5.7 新增 useSidebarResize/useSplitResize/useTabDropPreview/useDragDetach 也放此——它们只被 Pool 的 zone 使用。

---

## 5. `src/pluginLoader/`——插件生命周期

加载/卸载/解析 plugin.json / glob 扫描。**不注册 Registry——那是 `core/builtin/` 的事。**

---

## 6. `src/i18n/`——国际化

语言包 JSON + i18n 初始化。**文本走过 `t()`，key = 中文原文。**

---

## 7. `electron/`——主进程

- `main.ts`——入口
- `preload-*.ts`——preload 脚本
- `ipc/`——IPC handler
- 其他——按功能命名

---

## 8. 反模式——加文件前自检

| 反模式 | 正解 |
|--------|------|
| "不知道放哪，先放 `core/` 根目录" | 对照此表——找不到归属 = 不应该进 core |
| "就一个文件，建子目录太早了" | 正确目录宁可只有一个文件——命名就是文档 |
| "这个和那个有点像，放一起吧" | "像"≠"同类"。Registry 和 Service 都管理数据，但一个纯查询一个有副作用——不同目录 |
| "以后再说" | 以后不会再说——E5#42 就是"先分堆"的教训 |

---

## 9. 历史教训

| 问题 | 根因 | 今天 |
|------|------|------|
| `core/data/` 杂货铺（E5#42 → #36k 返工） | 建目录时没定义语义——"放数据的"太模糊 | pipeline/utils/registry/services 四类明确 |
| `core/builtin/` vs `plugins/builtin/` 同名异义 | 壳代码和插件代码共用一个形容词 | `core/commands/` 单独命名 |
| `pool/` 平铺所有 Renderer | 两个 Pool 时没觉得需要分，没想三个以后 | shared/sidebar/main 一 Pool 一目录 |
| 插件目录不规范（E5#33 前） | 每个插件自定结构 | plugins/ 目录规范（E5#30） |

**Why:** E5#42 52 文件迁移只做了"分堆"没做"分类"，因为当时没有这份规范。今天的 #36k #36l 是补上"分类"这一步。
**How to apply:** 新 AI 加文件前先查此表——确认归属子目录再 `touch`。找不到归属 → 问，不要猜。
