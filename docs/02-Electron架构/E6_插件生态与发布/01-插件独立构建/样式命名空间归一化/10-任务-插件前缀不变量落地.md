# 任务 `#109h-b`：插件前缀不变量·**落地**（件 2 下半）——第 1.14 轮

> ⚠️ **本详案是占位稿**——它的形状取决于第 1.13 轮（`#109h-a`）的评估结论与用户拍板。
> **第 1.13 轮的 AI 有义务按结论改写本档**（并在交接.md 说明改了哪几处）。若结论是「不做/缓做」，请把本档改写为「不做」+ 理由 + 复评触发条件，**不要留着一份会误导下一棒的详案**。
>
> **一句话（占位）**：按拍板把插件侧门禁从「保留名黑名单」升级为「前缀不变量」。
> **前置**：第 1.13 轮已 ✅ 且用户已拍板。
> **提交轴**：`@linkdesk/plugin-sdk`（发版）+ 21 个插件仓（若路线需要）。

---

## 一、无论选哪条路线都适用的硬要求（这四条是纪律，不是建议）

1. **单一真相源不许变成两份**：新规则若需要一份清单/前缀表，它必须**长在 SDK 包里、随包下发**（照 `reserved-class-names.json` 的先例），**不许**在壳仓与 SDK 各写一份。
2. **判据必须能从机器事实推出**：能机械校验的才写进门禁（例如「裸定义的类名首段」）；靠人看的写进作者面文档。
3. **负控要真跑红**（照 `#109e` 的做法）：往一只真插件仓塞一条违规 ⇒ 该仓 `npm run verify` **当场红**，报点含文件与类名；还原即绿。**自测通过不算判据**。
4. **fail-closed**：新腿加进 SDK 后，模板 `ci-verify` 的严格档要覆盖到它（L7 已把严格腿改为 fail-closed——除报表档外全部腿自动进严格档，但**请实测确认新腿在里面**）。

---

## 二、按路线的工作面（占位，评估后请按结论裁剪）

### 若选 (a) `plugin.json` 加 `cssPrefix`
- schema 三份拷贝同笔（`check-plugin-schema-sync` 守字节相等）；
- SDK 门禁腿改读该字段（缺失 ⇒ 报什么？缺省语义要先想清楚：**缺字段 = 不校验**（宽容）还是 **= 报错**（严格）？——这是要给用户拍板的问题）；
- 21 仓回填 `cssPrefix`（照各仓**实测的**前缀，不是猜）；
- 脚手架模板带上该字段 + 作者面文档 §12 加一段；
- SDK bump 发版（+ `create-linkdesk-plugin` 同步发——L7 定过规矩：**模板改了必须重发**）。

### 若选 (b) id 派生
- 逐仓把不合规的类名改成 `<pluginId>-*`（**这是破坏面**：要逐仓 verify + 重发 + 目录收录）；
- 其余同上（SDK 腿、脚手架、文档、发版）。

### 若选 (c) 最弱规则
- SDK 腿加一条「裸定义必须含连字符」——改动最小；
- 作者面文档同步一句；
- SDK bump 发版。

---

## 三、判据（无论哪条路线都要有的四类读数）

- [ ] SDK 腿自测（正控 + 负控）全符合预期
- [ ] **至少 1 只真插件仓真跑红**（违规注入 → 红 → 还原 → 绿），报点贴出
- [ ] 21 只仓全量跑一次新判据的读数（违规几只 / 修了哪几只）
- [ ] 受影响轴的版本真发 + `release:mark` + `check:npm-release` 无黄灯
- [ ] 作者面文档（两棵树）同笔更新 + `docs:check` 绿
- [ ] `npm run check` EXIT=0

---

## 四、提交与收尾

- 提交信息按各轴惯例带类别前缀；SDK 轴走 `docs/06-发布管理/作者轴npm发版.md` 五步。
- 收尾两件：交接.md 顶部追加 + 清单轮次进度。
- 若本任务在评估后确实需要拆成多个 AI，请**同笔把清单里的轮次拆开**（这是被鼓励的动作，不是超范围）。

---

## 五、禁区

1. **不许放宽已有门禁来换「全绿」**（L7 立过的规矩：尺子不许为个案放宽；逃生口只有「登记 + 写理由」）。
2. **不许只改壳仓或只改 SDK**——插件侧规则的可信度来自「**插件仓自己的 CI 里能跑红**」。
3. **不许顺手做件 3/件 4**（那是 1.15 / 1.16 / 1.17）。
