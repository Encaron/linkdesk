# 02 — 最终方案

> 2026-07-28 定案。5 项决策全部确认，进入执行。

---

## 已确认决策

### A. Emoji → Codicon ✅

15+ 处 Emoji/文本字符图标 → `codicon` 字体图标。LinkDesk 已全局加载 `@vscode/codicons`。

### B. 移除命令面板按钮 ✅

工具栏 `▸ 命令面板` 按钮删除。入口已有 `Ctrl+Shift+P` + 标题栏 ☰，工具栏按钮是串口监视器 V3 时代历史遗留。

### C. 去除 `transform: scale()` ✅

7 处 hover/active `scale()` → `background` + `color` + `box-shadow`。消除 flex 容器内布局抖动，无 `prefers-reduced-motion` 问题。

### D. `>` → `→` ✅

Monaco 发送区前缀从终端提示符 `>` 改为方向箭头 `→`，语义更准确。

### E. 侧栏设置组分区标题 ✅

12 个设置项分 3 组，每组加 `<div class="setting-section-label">` 小标题：`显示`、`发送行为`、`编码`。

---

## 新增 CSS 变量

在 `src/index.css` 的 `:root` 和 `[data-theme="light"]` 两处各加 4 行：

```css
/* :root（暗色默认） */
--warning-text: #E8AB3E;
--warning-bg: rgba(255, 200, 0, 0.12);
--text-on-accent: #FFFFFF;
--danger: #EF4444;

/* [data-theme="light"]（浅色覆写） */
--warning-text: #BF8800;
--warning-bg: rgba(200, 150, 0, 0.08);
--text-on-accent: #FFFFFF;
--danger: #D32F2F;
```

`--serial-monitor-ok: #22C55E` 已在 `ControlPanel.css` 定义，够用。

---

## 图标对照表

| 功能 | 旧 | 新 codicon | 文件 |
|------|-----|------------|------|
| 暂停接收 | `⏸` 文本 | `codicon-debug-pause` | index.tsx |
| 继续接收 | `▶` 文本 | `codicon-debug-start` | index.tsx |
| 命令面板 | `▸` 文本 | **移除按钮** | index.tsx |
| 筛选 | `📡` emoji | `codicon-filter` | index.tsx |
| 搜索 | `🔍` emoji | `codicon-search` | index.tsx + SearchBar.tsx |
| 上一条 | `▲` 文本 | `codicon-arrow-up` | SearchBar.tsx |
| 下一条 | `▼` 文本 | `codicon-arrow-down` | SearchBar.tsx |
| 关闭搜索 | `✕` 文本 | `codicon-close` | SearchBar.tsx |
| 筛选菜单 | `📡` emoji | `codicon-symbol-bracket` | FilterMenu.tsx |
| 侧栏改名 | `✎` 文本 | `codicon-edit` | sidebar.tsx |
| 侧栏关闭 | `✕` 文本 | `codicon-close` | sidebar.tsx |
| 侧栏确认 | `✓` 文本 | `codicon-check` | sidebar.tsx |
| 侧栏取消 | `✕` 文本 | `codicon-close` | sidebar.tsx |
| 快捷发送编辑 | `✎` 文本 | `codicon-edit` | index.tsx |
| 快捷发送取消 | `✕` 文本 | `codicon-close` | index.tsx |
| 状态栏连接 | `●` 文本 | `codicon-circle-filled` | statusBar.tsx |
| 状态栏分隔 | `│` 文本 | CSS `border-left` | statusBar.tsx |
| 连接按钮 | `●` 文本 | 去掉 ● 字符 | ControlPanel.tsx |

---

## 硬编码颜色 → CSS 变量对照表

| 位置 | 旧值 | 新值 |
|------|------|------|
| `.paused-banner` color | `#FFD700` | `var(--warning-text)` |
| `.paused-banner` background | `rgba(255,200,0,0.15)` | `var(--warning-bg)` |
| `.hex-warning` color | `#E8AB3E` | `var(--warning-text)` |
| `.hex-warning` background | `rgba(255,200,0,0.1)` | `var(--warning-bg)` |
| `.back-to-bottom` color | `white` | `var(--text-on-accent)` |
| `.send-btn` color | `white` | `var(--text-on-accent)` |
| `.history-item:hover` color | `white` | `var(--text-on-accent)` |
| `.settings-json-close` color | `#fff` | `var(--text-on-accent)` |
| `.control-connect-btn` hover border/color | `#EF4444` (fallback) | `var(--danger)` |

---

## 执行清单

按文件组织，共 8 个文件，~75 处改动。

### 1. `src/index.css` — 全局变量

- [ ] `:root` 加 `--warning-text` / `--warning-bg` / `--text-on-accent` / `--danger`（4 行）
- [ ] `[data-theme="light"]` 加同样的 4 行浅色值

### 2. `plugins/serial-monitor/src/index.tsx` — 主视图

- [ ] 工具栏按钮 emoji → codicon（暂停/导出/清空/筛选/搜索，5 处）
- [ ] 移除命令面板按钮（~3 行 JSX 删除）
- [ ] 快捷发送内联表单 emoji → codicon（✎ ✕ → edit/close，2 处）
- [ ] 发送区前缀 `>` → `→`（1 字符）

### 3. `plugins/serial-monitor/src/SerialMonitorView.css` — 主视图样式

- [ ] `.paused-banner` 颜色 → CSS 变量（2 处）
- [ ] `.hex-warning` 颜色 → CSS 变量（2 处）
- [ ] `.back-to-bottom` color → CSS 变量（1 处）
- [ ] `.send-btn` color → CSS 变量（1 处）
- [ ] `.history-item:hover` color → CSS 变量（1 处）
- [ ] 删除 `.toolbar-btn` `transform: scale()`（hover + active，2 处）
- [ ] 删除 `.quick-send-pill` `transform: scale()`（hover + active，2 处）
- [ ] 删除 `.send-btn` `transform: scale()`（hover + active，2 处）
- [ ] 删除 `.back-to-bottom` `transform: scale()`（1 处）
- [ ] `.toolbar-btn` 加 `:focus-visible`（1 处）
- [ ] `.quick-send-pill` 加 `:focus-visible`（1 处）
- [ ] `.quick-send-add` 改 dashed → solid border（1 处）
- [ ] 删除未使用的 `@keyframes dataFlash`（5 行）
- [ ] `.search-input` 加 `:focus-visible` outline（1 处）

### 4. `plugins/serial-monitor/src/ControlPanel.tsx` — 控制面板

- [ ] 连接按钮文字 `● 打开` → `打开`（1 处）

### 5. `plugins/serial-monitor/src/ControlPanel.css` — 控制面板样式

- [ ] `.control-dot.on` 去掉 `animation: dotPulse`（1 行删除）
- [ ] `.control-connect-btn` 加 `:focus-visible`（1 处）
- [ ] `.control-connect-btn.connected:hover` fallback `#EF4444` → `var(--danger)`（2 处）

### 6. `plugins/serial-monitor/src/components/SearchBar.tsx` — 搜索条

- [ ] 搜索图标 emoji → codicon（1 处）
- [ ] 导航按钮 ▲ ▼ → codicon（2 处）
- [ ] 关闭按钮 ✕ → codicon（1 处）

### 7. `plugins/serial-monitor/src/components/FilterMenu.tsx` — 筛选菜单

- [ ] 筛选图标 emoji → codicon（1 处）
- [ ] 关键字输入框内联 style → CSS 类（1 处）

### 8. `plugins/serial-monitor/src/sidebar.tsx` — 侧栏

- [ ] 操作按钮 emoji → codicon（✎ ✕ ✓ ✕，4 处）
- [ ] 设置组加 3 个 `<div class="setting-section-label">`（3 行 JSX）
- [ ] 空状态文字改为两段式（1 处）
- [ ] 内联编辑输入框加 `aria-label`（1 处）

### 9. `plugins/serial-monitor/src/SerialMonitorSidebar.css` — 侧栏样式

- [ ] 新增 `.setting-section-label` 样式（~8 行）
- [ ] `.session-item` 加 `:focus-within .session-item-actions { opacity: 1 }`（1 处）
- [ ] `.session-action-btn` 加 `:focus-visible`（1 处）

### 10. `plugins/serial-monitor/src/statusBar.tsx` — 状态栏

- [ ] 连接指示 `●` → codicon（1 处）
- [ ] 分隔符 `│` → CSS（1 处）

---

## 不做的事

- ❌ CSS 文件拆分（GL-6）— 属结构性重构，E3.5 定位是 UI 打磨，不做文件重组
- ❌ Monaco 高度 `clamp()`（SA-4）— 逻辑等价，不改
- ❌ 快捷发送表单绝对定位（QS-3）— 涉及 JSX 结构调整，风险收益比不划算
- ❌ 搜索加正则/全词匹配（SB-5）— 功能增强，非 UI 打磨
- ❌ CM6 空状态占位文字（CM-4）— 功能增强，非 UI 打磨

---

## 对标 VS Code

| 维度 | VS Code 做法 | LinkDesk 现状 | E3.5 后 |
|------|-------------|--------------|---------|
| 图标系统 | codicon 字体 | Emoji + 文本混用 | codicon 统一 |
| hover 反馈 | background 变化 | scale() 放大缩小 | background + box-shadow |
| 焦点可见 | `:focus-visible` outline | 多处缺失 | 全部补全 |
| 颜色管理 | CSS 变量 + theme JSON | CSS 变量为主，少量硬编码 | 零硬编码 |
| 设置分区 | 设置编辑器有 sections | 侧栏设置无标题 | 3 组标题 |
| 暗/浅双主题 | `body.vs` / `body.vs-dark` | `data-theme` 属性切换 | 保持不变，新变量双主题定义 |
