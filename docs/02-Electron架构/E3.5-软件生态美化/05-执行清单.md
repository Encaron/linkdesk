# E3.5 执行清单——软件生态美化

> 五个专题，~210 行，零架构改动。五组可并行执行。
> 详细审计 + 设计见各组子文档（命令面板/右键菜单/状态栏/Toast通知/TabBar标签栏）。
>
> **👤 AI 进场必读——动手改任何 UI 前，必须先用浏览器打开对应 `preview-整改效果.html` 看完整对比效果。**

---

## 第 1 组：命令面板（~110 行）

> 预览：[preview-整改效果.html](./命令面板美化/preview-整改效果.html)
> 审计：[01-全量审计.md](./命令面板美化/01-全量审计.md)
> 设计：[02-设计方向.md](./命令面板美化/02-设计方向.md)

### P0-1：开合动画（QuickPick 壳 ~20 行）

- [x] **#CP01** `.palette` 加 scale+opacity transition——150ms | `src/index.css` ~10 | 打开时 scale(0.95→1) + opacity(0→1)
- [x] **#CP02** `.ctx-overlay` 加 opacity transition——150ms | `src/index.css` ~3 | 遮罩 fade in/out
- [x] **#CP03** QuickPick 加 `closing` 状态——延时 150ms 卸载 | `QuickPick.tsx` ~10 | open→false 时先去 `.show` class，等 transition 结束再 `return null`
- [x] **#CP03a** 入场：首次渲染后下一帧 `requestAnimationFrame` 加 `.show` class | `QuickPick.tsx` ~5

### P0-2：快捷键显示（CommandPalette ~40 行）

- [x] **#CP04** `renderItem` 第二行改为 flex 行——ID(左) + 快捷键 pill(右) | `CommandPalette.tsx` ~15 | 对标 VS Code 两行布局
- [x] **#CP05** 调用 `findKeybindingForCommand(cmd.id)` 取快捷键 | `CommandPalette.tsx` ~3 | API 已就绪——`src/core/KeybindingRegistry.ts:415`
- [x] **#CP06** 渲染 `<kbd>` keycap pill 组件 | `CommandPalette.tsx` ~10 | padding:1px 5px, mono 字体, 圆角 3px, 半透明背景
- [x] **#CP07** keycap pill CSS——正常态暗色 / hover 态亮色 | `src/index.css` ~12 | `rgba(128,128,128,0.1)` → hover `rgba(255,255,255,0.2)`

### P1-1：空态提示（QuickPick ~8 行）

- [x] **#CP08** `palette-list` 内加空态——`filtered.length === 0` 时显示提示 | `QuickPick.tsx` ~5 | "未找到匹配命令" / "输入命令名称搜索…"
- [x] **#CP08a** 空态样式——居中、12px、`--text-muted` | `src/index.css` ~3

### P2-1：`>` 前缀指示器（QuickPick ~15 行）

- [x] **#CP09** `QuickPickProps` 加 `prefix?: string` | `QuickPick.tsx` ~2
- [x] **#CP10** input 外包裹 `.palette-input-row`——prefix + input + clear | `QuickPick.tsx` ~8 | border-bottom 从 input 移到 row
- [x] **#CP11** prefix CSS——`padding-left:14px`, `color:--text-muted` | `src/index.css` ~5
- [x] **#CP12** CommandPalette 传 `prefix=">"` | `CommandPalette.tsx` ~1

### P3：杂项（~27 行）

- [x] **#CP13** 清除按钮——input 右侧 ✕ | `QuickPick.tsx` ~8 | query 非空时显示
- [x] **#CP13a** 清除按钮 CSS | `src/index.css` ~5 | 绝对定位、hover 态
- [x] **#CP14** 响应式高度——`max-height: min(360px, 60vh)` | `src/index.css` ~2
- [x] **#CP14a** `.palette-list` 改 `flex:1` + `overflow-y:auto` | `src/index.css` ~2
- [x] **#CP15** 滚动条样式——6px 细条 | `src/index.css` ~5 | `-webkit-scrollbar`
- [x] **#CP16** 结果计数——列表底部 "X 个命令" | `QuickPick.tsx` ~5

### P4：QuickPick 布局归一化（~73 行）🆕

> 🔥 **架构修复。** QuickPick `renderItem` 自由度过高——5 个消费者 4 个各写各的布局，视觉不统一。
> 方案：QuickPick 锁死内部两行布局，消费者只填内容槽位（slot），不再手写 HTML 结构。
> 设计文档：[03-QuickPick布局归一化.md](./命令面板美化/03-QuickPick布局归一化.md)

- [x] **#CP17** QuickPick 内部锁死两行布局——加 `renderLabel`/`renderCategory`/`renderDetail`/`renderDetailRight` 四个 slot props | `QuickPick.tsx` ~30 | 消费者不再手写 `.palette-item-*` class
- [x] **#CP18** CommandPalette 切 slot props——删手写 `.palette-item-content` 结构 | `CommandPalette.tsx` ~20 | KeybindingPill 提取为独立组件
- [x] **#CP19** ThemeBrowser 切 slot props——删 `renderItem`，改 slot | `ThemeBrowser.tsx` ~5
- [x] **#CP20** LanguagePicker 切 slot props——删 `renderItem` + inline style，改 slot | `LanguagePicker.tsx` ~8 | 语言代码从第一行移到第二行 detail
- [x] **#CP21** DevTools picker 切 slot props——删 `renderItem`，改 slot | `App.tsx` ~5
- [x] **#CP22** `showQuickPick` 切 slot props——删内联 `renderItem`，改 slot | `QuickPick.tsx` ~5
- [x] **#CP23** 🆕 ThemeBrowser 加 `renderDetail`——通过 `ThemeRegistry.get()` 显示主题类型（暗色/浅色/高对比度） | `ThemeBrowser.tsx` ~5 | items 类型不变，lookup 在回调内
- [x] **#CP24** 🆕 DevTools picker 加 `renderDetail`——显示目标类型"插件 WebView DevTools"/"主窗口控制台" | `App.tsx` ~5
- [x] **#CP25** 🆕 ESLint 防线 `no-quickpick-render-item`——禁止新代码用 `renderItem`，强制走 slot props | `eslint-local-rules.js` + `eslint.config.js` ~40 | error 级别

---

## 第 2 组：右键菜单（~35 行）

> 预览：[preview-整改效果.html](./右键菜单美化/preview-整改效果.html)
> 审计：[01-全量审计.md](./右键菜单美化/01-全量审计.md)
> 设计：[02-设计方向.md](./右键菜单美化/02-设计方向.md)

### P0：快捷键接线（~5 行）

- [x] **#CM01** `resolved` useMemo 内 `shortcut` 接线——`findKeybindingForCommand(item.command)?.key` | `ContextMenu.tsx:79` ~2 | 已有 `shortcut &&` 接线
- [x] **#CM02** `ResolvedItem` 保留 `icon?: string` 字段（含 `cmd.icon ?? item.icon`）—图标方案已废，字段不动 | 零行

> Encaron 决策：右键菜单不加图标。

### P1：文字对比度（~10 行）

- [x] **#CM03** `.ctx-item` 删 text-shadow, font-size 13.5→13px, font-weight 500→400 | `ContextMenu.css` ~3
- [x] **#CM04** `.ctx-item:hover` color `#fff`→`var(--text-on-accent)` | `ContextMenu.css` ~1
- [x] **#CM05** `.ctx-item-shortcut` hover 态 `color: var(--text-on-accent)` | `ContextMenu.css` ~1
- [x] **#CM06** `.ctx-item-danger` color `#E06C75`→`var(--danger)`, hover bg `#E06C75`→`var(--danger)` | `ContextMenu.css` ~3

### P2：开合动画（~20 行）

- [x] **#CM07** `.ctx-menu` 加 scale+opacity transition——150ms | `ContextMenu.css` ~8 | `transform-origin: top left`
- [x] **#CM08** ContextMenu 首次渲染后 `requestAnimationFrame` 加 `.show` | `ContextMenu.tsx` ~8 | useEffect + menuRef
- [x] **#CM09** `.ctx-menu` `border-radius: 6→8px`, `box-shadow` 改双层阴影 | `ContextMenu.css` ~2 | 对齐 preview 效果
- [x] **#CM09a** `.ctx-divider` margin `4px 12px`→`4px 12px`（已是——与 `.ctx-item` padding 对齐） | `ContextMenu.css` ~1

### P3：死代码（−5 行）

- [x] **#CM10** 删 `.ctx-backdrop`（无人引用） | `ContextMenu.css` −5

---

## 第 3 组：状态栏（~30 行）

> 预览：[preview-整改效果.html](./状态栏美化/preview-整改效果.html)
> 审计：[01-全量审计.md](./状态栏美化/01-全量审计.md)
> 设计：[02-设计方向.md](./状态栏美化/02-设计方向.md)

### P0-1：对比度（~5 行）

- [x] **#SB01** `.status-bar` color `--text-muted`→`--text-secondary` | `StatusBar.css` ~1
- [x] **#SB02** `.status-bar-btn` color `--text-muted`→`--text-secondary` | `StatusBar.css` ~1
- [x] **#SB03** `.status-text` 确认用 `--text-secondary`（已是） | 零行

### P0-2：分割符（~5 行）

- [x] **#SB04** 所有 `<span className="status-divider">│</span>` → `<span className="status-divider" />` | `StatusBar.tsx` ~3 | 清空文本内容
- [x] **#SB05** `.status-divider` height 12→14px, 加 `vertical-align:middle` | `StatusBar.css` ~2

### P1：按钮图标（~5 行）

- [x] **#SB06** Theme 按钮 `☀`/`☾` → `codicon codicon-color-mode` | `StatusBar.tsx` ~2
- [x] **#SB07** Lang 按钮 `中`/`EN` → `codicon codicon-globe` | `StatusBar.tsx` ~2
- [x] **#SB08** 两个按钮加 `title` 属性的完整文字（如 `切换主题 (Dark)`） | `StatusBar.tsx` ~1

### P2：间距（~5 行）

- [x] **#SB09** `.status-bar-right` gap `2px`→`6px` | `StatusBar.css` ~1 | 和左侧统一
- [x] **#SB09a** 确认 `.status-bar-left` gap 保持 `6px` | 零行

### P3：错误过渡（~5 行）

- [x] **#SB10** `.status-error` 加 `animation: statusFadeIn 200ms` | `StatusBar.css` ~5

---

## 第 4 组：Toast 通知（~25 行）

> 预览：[preview-整改效果.html](./Toast通知美化/preview-整改效果.html)
> 审计：[01-全量审计.md](./Toast通知美化/01-全量审计.md)

### 退出动画

- [x] **#TO01** ToastContainer 加 `exitingIds` state + `prevToastMap` ref——检测消失的 toast | `ToastContainer.tsx` ~10
- [x] **#TO02** `allToasts` 合并活跃 + 退出中的 toast | `ToastContainer.tsx` ~5
- [x] **#TO03** NotificationItem 加 `exiting` + `onExited` props——exiting 时去 `visible` 触发 reverse 动画 | `ToastContainer.tsx` ~8
- [x] **#TO04** `.toast-item.toast-exiting`——`translateY(-8px)` + `opacity:0` 200ms | `ToastContainer.css` ~5
- [x] **#TO05** `onTransitionEnd` → `onExited()` 从 `exitingIds` 移除 | `ToastContainer.tsx` ~2

---

## 第 5 组：TabBar 标签栏（~12 行）

> 审计：[01-全量审计.md](./TabBar标签栏美化/01-全量审计.md)

### 文本→codicon

- [x] **#TB01** 关闭按钮 `×` → `<span className="codicon codicon-close" />` | `TabBar.tsx` ~2
- [x] **#TB02** dirty 圆点 `●` 文本 → CSS 圆 `width:8px; height:8px; border-radius:50%; background:var(--dirty-dot)` | `TabBar.tsx` + `TabBar.css` ~5
- [x] **#TB03** 滚动箭头 `‹` `›` → `codicon-chevron-left/right` | `TabBar.tsx` ~2
- [x] **#TB04** `.tab-plus-menu` `border-radius: 6→8px`, `box-shadow` 对齐 ContextMenu 双层阴影 | `TabBar.css` ~3

---

## 总结

| 组 | 专题 | 任务数 | 行数 | 可并行 |
|:--:|------|:--:|:--:|:--:|
| 1 | 命令面板 | 15 + 🆕9 = **24** | ~110 + ~83 = **~193** | ✅ |
| 2 | 右键菜单 | 9 | ~35 | ✅ |
| 3 | 状态栏 | 9 | ~30 | ✅ |
| 4 | Toast 通知 | 5 | ~25 | ✅ |
| 5 | TabBar | 4 | ~12 | ✅ |
| **合计** | | **50 任务** | **~293 行** | |

**五组全部可并行执行——互不依赖。**
