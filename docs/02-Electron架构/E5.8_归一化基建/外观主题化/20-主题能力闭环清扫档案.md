# 20-主题能力闭环清扫档案（E5.8#132-#134）

> 2026-08-27。用户问「为了实现主题插件（我想要的那些），还需要做什么」→ 审计主题插件能力面：**核心能力（调色板/玻璃/纹理/背景/字体/混搭来源）全部齐备且有真实插件实例**，缺口收敛到三处收尾（非架构级）。用户拍板立案执行。
> 清单指针：E5.8-执行清单 Phase 11.20 #132-#134。

---

## 一、能力面审计结论（用户要的主题插件能力 vs 现状）

| 用户要的能力 | schema | 引擎消费 | 真实实例 | 结论 |
|---|---|---|---|---|
| 完整调色板 30+（colorways[]） | ✅ | ✅ | aurora-glass / mint-soda | 齐 |
| 五彩斑斓玻璃（tint/specular/morph/radius/shadow） | ✅ | ✅ | aurora-glass（specular 0.6/morph 200/radius 12） | 齐 |
| 背景素材（panorama 全窗 / zones 切片） | ✅ | ✅ | theme-panorama / theme-zones | 齐 |
| 字体组合（font ui/mono） | ✅ | ✅ | theme-songti | 齐 |
| 混搭来源（b 壁纸 + c 字体 + 换域来源） | ✅ | ✅ | mixFont/mixBackground/mixSurface + themeColor(colors) | 齐 |
| 纹理（texture 纸纹） | ✅ | ⚠️ 引擎写 token（tokens.ts:57/mix.ts:146） | 机制 #50.28 收官 | **#134 确认 CSS 消费** |
| 图标主题（a 的图标） | ✅ 独立贡献点 iconThemes | ⚠️ 机制建好无消费闭环 | 零插件实例 | **#133 闭环** |
| per-surface 表面精调（surface 域） | ✅ | ⚠️ 引擎透传 | 零插件实例 + CSS 零消费 | **#132 死键清理** |

**结论：用户要的主题插件能力面 100% 铺好（六域 + colorways + 图标独立贡献点），缺的不是新能力，是三处收尾。**

---

## 二、三处收尾

### #132 surface per-surface 死键 —— ✅ 用户拍板 **A 删**（2026-08-27），拍板锁定待实现

- **现状**：[theme.ts:92-94](theme.ts) `ThemeAppearance.surface?: Record<string, number|string>` 注释自认「预留暂无 CSS 消费者；引擎 flatten 为 surface-<key> token 透传」——schema 有、引擎透传、**CSS 零消费、零插件实例**。
- **冲突**：与 E5.8#119-#121 浮层权威「结构地板统一磨砂」直接矛盾（per-surface 精调 = 又要按表面名枚举，正是浮层权威消灭的东西）。
- **用户拍板（2026-08-27）**：**A 删**——surface 域从 schema/引擎移除，放弃 per-surface 精调，统一结构地板（符合归一化 + 无死代码 + 浮层权威设计一致）。
- **已确认删除足迹（实现时照此执行）**：
  - theme.ts:92-94 `appearance.surface` 映射声明 + 注释（含「预留暂无 CSS 消费者」自认）——删
  - tokens.ts:38-50 `flattenSurfaceDomain`（per-surface 透传唯一写法，recipe/mix 共用）——整函数删（glass 纹理/悬浮 flatten 走 `surfaceVariables`，不受影响）
  - recipe.ts:44 `flattenSurfaceDomain(appearance.surface, tokens)` 调用 + recipe.ts:28 `recipeDomains` 的 `a?.surface → push("surface")`——删
  - mix.ts:153 调用 + mix.ts:146-149 `hasTexture = appearance?.surface?.texture` 读法——**改读 `appearance?.glass?.texture`**（对齐 recipe 路径 surfaceVariables 语义；paper-zones.json 声明在 glass.texture，mix 当前读 surface 映射 = 路径不一致，A 删一并归一）
  - theme.ts:124 `ThemeDomain` 联合类型删 `"surface"` 成员 + constants.ts:128 `MIX_DOMAIN_KEYS`/`MIX_DOMAIN_ORDER` 的 `surface: "app.mixSurface"` + `app.mixSurface` 配置项（表面域来源选择器随域消失）
  - plugin.schema.json `appearance.surface` 字段 + 05 schema 文档 + 01-插件API 相关段落
  - 测试：recipe.test.ts:46-47 `surface-menu-blur`/`surface-menu-radius` per-surface pass-through 断言删；mix.test.ts surface 域测试同步清理
  - **注意**：`app.surfaceRadius`（zone 圆角滑杆）与 `appearance.surface`（per-surface 映射）是**两个不同概念**——前者是系统标尺活键，**不删**。`--surface-radius`/`--surface-inset`/`--surface-shadow`/`--surface-bg-*` token 全部是活消费（CSS ::after + 缝系统），**不碰**。
  - 实现时发现 flattenSurfaceDomain 签名注解 `ThemeSurface` 与调用传参 `Record<string, number|string>` 类型不匹配——A 删把整函数删掉即自消，无残留。

### #133 图标主题闭环（「a 的图标」唯一没着落的一块）—— ✅ 设计前置 8 维度 + 三项拍板（2026-08-27），设计锁定待实现

- **现状**：机制已建——`contributes.iconThemes` 加载（contributions.ts:108）+ IconRegistry 登记本（对标 VS Code Product Icon Theme）+ `IconThemeMappings`（files/extensions/folders → CSS 类名）。**缺**：① `app.iconTheme` 设置项未注册（用户没得选）② 零插件实例（加载→登记→应用的路径从没走通）③ 消费方（文件树图标按当前图标主题映射应用）未验证存在。
- **设计前置（8 维度，2026-08-27 全项目设计流程首次样板）**：
  - ① 能力边界 = 壳能力（多提供方：任意插件贡献 iconThemes / 多消费方：文件树+未来任何文件图标视图 / 桌不知内容：壳只登记+选，不懂图标语义）；**保底 codicon**（现状 icon-mappings.ts 硬编码表，零图标主题插件图标照常）
  - ② API = **零新增 `window.linkdesk.*`**——复用 `fetchPluginDataFile`（加载 mappings JSON）+ `updateConfigurationEnum`（刷枚举）+ `bridge.broadcast("iconTheme:changed", {iconThemeId, mappings})`（对标 theme:changed 先例）。**`iconTheme:changed` 事件契约须入 03-插件制造 文档**（插件作者要知道事件名 + payload 形状，对标 theme:changed 文档先例——本闭环实现时 #133.2 一并落地）
  - ③ 通信 = 插件贡献 contributes.iconThemes（JSON）→ 壳登记 IconRegistry → 用户选 app.iconTheme → 壳广播 → 插件消费 `updateIconResolver(mappings)`；数据流一条链，持久化=壳配置
  - ④ 壳侧 = **零新文件**：`loadIconThemeContributionData` 进 contributions.ts（与 loadThemeContributionData 同文件同模式）+ `app.iconTheme` 进 appearance.ts（与 app.theme 同文件同模式）；单一权威=IconRegistry（已有）；**概念键分离：iconTheme 与 theme 独立键**（配色 a + 图标 b = 用户拍板）；硬约束=枚举动态派生自 IconRegistry 不硬编码插件 ID
  - ⑤ 插件侧 = 贡献点 contributes.iconThemes（schema 已有）；虚构实例 = plugins/user/ 下 feature-folder 插件（硬约束 21 虚构名），贡献 1 份 mappings JSON + 图标字体资产；第三方零壳改动（写 JSON + 图标即够）
  - ⑥ 显示 = 设置页外观分节「图标主题」下拉（uiHint select，对标 app.themeColor）；枚举 = IconRegistry.getAll() 动态刷新（对标 syncAppThemeEnum，装/卸即刷）；立即应用 onApply
  - ⑦ 配置 = `app.iconTheme` 壳给保底：默认 `"default"`（codicon 保底，无插件也成立，UI 恒显该项）；枚举 = 已登记图标主题 + default；**独立选择器**（不跟随配色主题，VS Code 模式）；卸载插件→枚举消失→回退保底
  - ⑧ 验收 = `npm run check` 全绿 + CDP 实机：装虚构实例→枚举出现→选中→文件树图标类名变化（像素采样）→切回 default 恢复 + 卸载回退保底
- **用户拍板 3 项（2026-08-27）**：
  - **独立选择器定案**：`app.iconTheme` 默认 `"default"`，与配色主题完全独立，无「跟随主题」哨兵（VS Code 模式，你拍的「配色 a + 图标 b」自然成立）
  - **mappings 单一权威定案**：core `IconThemeMappings`（含 foldersExpanded）为唯一真相源，文件树插件侧 `IconMappings`（FileIconResolver.ts:22-26 独立接口，无 foldersExpanded）删除改对齐——builtin 插件 monorepo 内可引类型，第三方纯 JSON 无类型依赖，零铁律冲突
  - **IconThemeMappings 当前全仓库零消费**（types.ts:28 定义 + docs，无运行时值）——本次闭环即其首个消费路径
- **Explore 实况关键结论**（2026-08-27 只读调查）：
  - `loadIconThemeContributionData` **不存在**（全仓 grep 零命中）——需新建，镜像 loadThemeContributionData（contributions.ts:408-456）+ fetchPluginDataFile（:373-401 复用基础设施）
  - `app.iconTheme` / `syncIconTheme` src 零命中（唯一提到在 IconRegistry.ts:6 头注释设计意图）
  - 文件树消费：`updateIconResolver`（FileIconResolver.ts:67-70）**零调用**——模块加载时建全局默认 `_resolver`（硬编码表），FileTreeNode.tsx:53-59 走它；插件不订阅任何事件、不读任何图标主题配置
  - 广播先例：`commitTokens`（tokens.ts:133-184）走 `linkdesk.bridge.broadcast("theme:changed", ...)`（插件可见靠 bridge）+ CoreEvents（壳内 UI）——iconTheme 广播照此双轨

### #134 texture 纸纹 CSS 消费确认 —— ✅ 机制完整（2026-08-27 查实，零代码）

- **结论：纸纹分区独立消费 → 机制完整，档案记录即可。** 全链四件套实证：
  - **schema**：`ThemeSurface.texture`/`textureOpacity`（theme.ts:39-42）——与 glass 正交（⑬ 纸纹分区，不带玻璃也能 per-surface 纹理）。
  - **引擎**：`surfaceVariables(glass)`（tokens.ts:53-62）读 ThemeSurface.texture → 写 `--surface-bg-image`/`--surface-bg-opacity` token；mix.ts:143-146 纹理归 surface.texture 显式声明。
  - **CSS 消费**：index.css:578-593 `.panel-zone::after` 等 5 zone 表面 + `.status-bar::after` 消费 `var(--surface-bg-image)` / `--surface-bg-repeat` / `--surface-bg-size`；#125 玻璃激活读 `--glass-surface-bg-opacity`（原值 × alpha）合成淡出，未激活回退原值零变化。
  - **真实实例**：`plugins/user/theme-zones/paper-zones.json` `appearance.glass.texture` = paper-texture.svg 纸纹 + textureOpacity 0.45。
- **注**：texture 归属 `appearance.glass`（ThemeSurface 形态）——`appearance.surface` 是另一回事（#132 死 per-surface 映射），两者无混淆，纹理不在死键清理范围。
- 可选加分项：CDP 实机挂 paper-zones 主题像素采样纸纹，非必需。

---

## 三、验收总则

1. ✅ **#134 已结**（2026-08-27 零代码查实机制完整，commit `41475884`）。
2. 🔒 **#132 拍板 A 删 + #133 设计前置 8 维度 + 三项拍板已锁定**（2026-08-27），实现另行择时执行（用户指示：当前仅档案 + 清单阶段，非代码执行时间）。
3. #132 实现 = 按 §二足迹删除（独立 commit + `npm run check` 全绿 + 确认 `--surface-*` 活键零影响）。
4. #133 实现 = 虚构实例走通全链 + CDP 切换图标主题零回归 + 文件树图标像素采样变化。
5. 每 commit 前 `git status` + `git diff --stat` 查共享文档污染。
