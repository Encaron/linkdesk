# 第 1.20 轮 · `#109k-c`：🔴 **系列收口报告**（十件套终态 ＋ 四轴对账 ＋ 残余边界诚实清单）

> **这一格是「报完成」轮，不是新工作。** 它把**十件套**（件 1–9 样式四轴 ＋ 件 10 非样式普查）的终态**逐件给读数**，把系列状态收成一句话，并**如实交出残余边界**。
> **权威判据 = [22 号档 §五](22-收口总方案-跨方样式污染九件套.md)**（本档是执行记录）。
> 🔴 **本格与「宣布大获全胜」的分界线 = §五 的六条残余边界，一条不许省。**
> **本格性质**：**零代码改动**（只读 ＋ 只写文档）。实机实例为读数服务，采完即还原。

---

## 一、逐件终态读数（**每件给「跑出来的读数」，不是「做过了」**）

| 件 | 轮次 | 终态读数（2026-09-16 本格实跑） |
|:--:|:--|:--|
| **件 1 裸名归一** | 1.5–1.12 ✅ | `check-css-namespace`：**共享组件裸定义 0** · 宿主全局工具类 **0** · 关键帧 8（全部带前缀）· 跨组件借用 0。`@linkdesk/ui` 终态 **0.3.0**（货架版）· 出厂种子含新名 |
| **件 2 插件前缀不变量** | 1.13 ＋ 1.14①–⑧ ✅ | 🔴 **18 仓全量审计：零不合规 18/18**（不合规类名 **0** ／ 关键帧 **0** ／ token 红 **0** 黄 **0** ／ 无锚 S2 **0** ／ 跨方 S3 **0**）；SDK 终态 **0.1.28**（前缀腿随包下发，插件仓 CI 严格腿判红）；schema 第二道防线 `pluginId.pattern = ^(?!ldk-)…` 在位；作者面 §12 写的是 `<pluginId>-` 规则（含关键帧与 `ldk-` 禁忌） |
| **件 3 保留名清单单一真相源** | 1.15 ✅ | `check-reserved-names-doc-sync` ✅：§12 两张表与登记表**双向一致**（保留名 **11** 个、**全部为 `ldk-` 名**；关键帧 **8** 个）；**中英两棵树名字集合相等**；作者面 §12 无手抄表（只剩机器守的那份） |
| **件 4 宿主与共享组件类名定格** | 1.16 ＋ 1.17 ＋ 1.21 ＋ 1.21b ✅ | 🔴 **两个定义域独立定义全部 `ldk-`（判据①③ 结构性、零登记表）**：宿主 **258** 个 ＋ 共享组件 **89** 个，**非 `ldk-` 独立定义 = 0**；**跨域同名 0**（判据⑥）；关键帧 **8** 个与实况双向一致、引用不悬空（判据⑧）；`classes` 登记表**整块已删**；两棵树 §12 同步；4 只消费方产物解包复核旧名零命中 |
| **件 5 运行时全表断言探针** | 1.22 ✅ | 纯分析层自测 **42 例**（已接线）· 采集层**本格真跑通**（活实例）：**零未归属** · ✅ **探针结论成立：零 red 跨方碰撞、方名册够真** · 覆盖不到清单已成文（§六.3） |
| **件 6 CSS 自定义属性（token）轴** | 1.23 ＋ 1.24 ✅ | 定案成文（零名单／零白名单）· 门禁判据⑨：**152 个定义点零越界** · 探针镜像：**红 0 / 黄 0** · **document 级违规 = 0**（改前插件侧 4 处）· `serial-monitor` 4 处已改挂自有根类并重发（终态 **1.0.17**）· 主题配方/schema 未动 |
| **件 7 选择器形态轴** | 1.25 ＋ 1.26 ✅ | 四条规则 R0–R3 成文 · **宿主基线 17 条**（登记表 `scripts/css-selector-baseline.json`，**文件 ＋ 条数**、零名字清单，门禁每次逐条打印）· **插件侧无锚 0 / 跨方 S3 0** · 判据⑦⑧⑩⑪ 在位 ＋ SDK S2/S3 随包 · 🔴 **静态 17 ＝ 运行时 17**（pool 与 shell 各 17，逐字相等）· **域边界不一致 0 个名字** |
| **件 8 门禁健康度** | 1.27 ＋ 1.28a ＋ 1.28b ✅ | 🔴 **`check-gate-health` 真跑：`35 道 check-*.mjs —— 有自测＋已接线 34 道 ／ 豁免 1 道`**（豁免 = `check-lsp-smoke.mjs`，环境依赖；34 道自测**逐道真跑、退出码全 0**）· **已知空转登记 3 条**（`reserved-classes.ts` 判据①【已处置／退役】· 市场拒装腿【已处置／生产端已补】· **判据⑧ 插件域另一半【未处置 → 归一个新 SDK 轮】**）· 链 **81 步**／自测步 **38** |
| **件 9 隔离决策落盘** | 1.29 ✅ | 决策记录 [docs/decisions/rejected/plugin-view-style-isolation.md](../../../../decisions/rejected/plugin-view-style-isolation.md)（**状态目录与结论一致**：不做 ⇒ `rejected/`，该目录首篇）· `decisions/README.md` 索引已加 · **`CLAUDE.md` 一行已补** · **触发条件 5 条且逐条可判定**（T1–T5，各带判定方式与复核时机） |
| **件 10 非样式命名空间普查** | 1.30 ✅ | [34 号档](34-件10-非样式命名空间普查报告.md)：**普查表 17 行**——🔴 **同形 6 条** ／ 🟠 半同形 5 条 ／ ✅ 已设防 4 条；两条并列第一的新发现（**④ 上下文键** · **⑥ `pluginState`**）；**建议轮次表 7 格** ＋ 新系列第 0 格「必须先做的三条」。⚠️ **件 10 只普查、未落地**——与件 1–9 的「已收口」**不是同一状态**（见 §五 末条） |

---

## 二、🔴 运行时探针终态（**本类问题「真收口」的实机证据**）

**采集方式（本轮是真跑，不是离线复算）**：按 `scripts/runtime-style-audit.mjs` 文件头的启动配方起隔离 profile ＋ CDP：`APPDATA='E:\linkdesk-rmt-appdata'` 起 Vite(1420) → `tsc -p electron/tsconfig.json` → `electron . --remote-debugging-port=9222 --user-data-dir='E:\linkdesk-rmt-appdata\linkdesk'`。**池文档已挂载 4 只插件视图**（file-tree ／ marketplace ／ serial-monitor ／ panel-demo）——与 1.26 基线**同一套方名册**，可直接对账。

```bash
node scripts/runtime-style-audit.mjs --json scratch/rt-120-final.json --raw scratch/rt-120-final.raw.json --compare-static --prefix-audit
```

| 判据（[19 号档 §二](19-任务-收口批-遗留账与系列收口.md)） | 读数 | 结论 |
|:--|:--|:--:|
| 全部样式表已 dump | **池文档 27 张 / 4280 条规则** · 壳文档 3 张 / 708 条规则 | ✅ |
| **未归属样式表 = 0** | `unattributed = 0` | ✅ |
| **四条轴上「被两方以上独立定义的名字」= 0** | **跨方碰撞 88 处 = red 0 ／ yellow 2 ／ info 86**（86 处 info = `@linkdesk/ui` 的 CSS 被**内联进每只插件 bundle** 的**已知结构性事实**，不判红） | ✅ |
| 探针总结论 | ✅ **零 red 跨方碰撞、零未归属、方名册够真** | ✅ |
| token 作用域（判据⑨ 镜像） | **红 0 处 / 黄 0 处** | ✅ |

**两条 yellow 的逐条交代**（⛔ 不许当「零」混过去）：**`other:.codicon[class*="codicon-"]` ← `codicon` × `host`**（pool 与 shell 各一处，同一形态在两个文档各报一次）。判级依据（探针自带）：**形态带类名锚** ⇒ 命中范围被那个类限定，归属问题落在**轴 ①（类名命名空间）**；且它正是**残余边界第 4 条**（三方 `codicon` 外部契约）在轴 ④ 的投影 ⇒ **黄灯只报事实**，不升红（升红 = 用一条没定案的规则制造假红）。

**每方「非 `ldk-` 独立定义」计数（改前/改后对账用的那把尺子）**：

| 方 | 非 `ldk-` 类名 | 非 `ldk-` 关键帧 | 轴 ④ 无锚站点 |
|:--|:--:|:--:|:--:|
| **host**（宿主域） | **0** ✅ | **0** ✅ | **17**（= 已登记基线） |
| **shared**（共享组件域） | **0** ✅ | **0** ✅ | **0** ✅ |
| codicon（三方） | 651（**外部契约**，只消费不定义） | 1 | 0 |
| plugin:file-tree | 63（**全部 `file-tree-` 前缀**） | 0 | 0 |
| plugin:marketplace | 146（**全部 `marketplace-` 前缀**） | 1（`marketplace-ms-icon-spin`） | 0 |
| plugin:panel-demo | 19（**全部 `panel-demo-` 前缀**） | 0 | 0 |
| plugin:serial-monitor | 65（**全部 `serial-monitor-` 前缀**） | 2（`serial-monitor-fadeIn` / `-slideDown`） | 0 |

> 🔑 **读法**：**「非 `ldk-` 类名」在插件方不为 0 是正确的**——插件侧的正确形态是「**非 `ldk-` 且带自有前缀**」。**宿主与共享组件两域的 0 才是本系列的收口目标**，两处都是 **0** ✅。

**静态源 ↔ 运行时对账**：

| 项 | 读数 |
|:--|:--|
| **域一致性（三处逐字一致）** | ✅ 探针 host 归属域 ＝ 门禁 `HOST_DOMAIN` ＝ 登记表 `domain.files` ＝ `["src/*.css","src/pool/**"]` |
| host 域 | 文件 19 ｜ 静态独立定义 **259** ＝ 运行时 **259** ｜ **运行时独有 0** ✅ ｜ 关键帧 静态 7 ＝ 运行时 7 |
| shared 域 | 文件 16 ｜ 静态 89 ｜ 运行时 26 ｜ **运行时独有 0** ✅ ｜ 静态独有 63（本次未挂载的文件，正常） |
| 🔴 **轴 ④ 无锚站点** | **pool 静态 17 ＝ 运行时 17** ✅ · **shell 静态 17 ＝ 运行时 17** ✅（逐字相等） |
| 🔴 **域边界不一致** | **0 个名字** ✅ |

---

## 三、逐轴「改前 → 改后」对账（**四行，全部有读数**）

| 轴 | 改前 | 终态 | 读数来源 |
|:--|:--|:--:|:--|
| **类名** | 宿主 **252**（非 `ldk-` 独立定义，1.21 实测重算）＋ 共享组件 **49**（1.21b，[11 档 §三](../11-样式命名空间审计.md)） | **0 ＋ 0** | 探针前缀审计表（host 0 ／ shared 0）＋ `check-css-namespace` |
| **`@keyframes`** | **8**（宿主 7 ＋ 共享 1） | **0 ＋ 0** | 同上（host 0 ／ shared 0；8 个名字全部带前缀） |
| **自定义属性（document 级违规）** | 插件侧 **4 处**（`serial-monitor` 三个文件） | **0** | 探针 token 判级（**红 0 / 黄 0**）＋ 门禁判据⑨（152 定义点零越界） |
| **顶层非类名选择器** | 宿主 **16** → **21**（1.25 复量订正：A 段 17 ＋ D 段 4） | **＝ 基线条目 17**（逐条带理由，见下）；插件侧 **0** | 门禁判据⑩ 逐条打印 ＋ 探针轴 ④ 对账 |

**🔴 基线 17 条的逐条理由**（`src/index.css`，[32 号档 §二.1](32-任务-选择器形态轴门禁与落地.md) 有逐条实证）：

| 形态 | 条数 | 理由 |
|:--|:--:|:--|
| `:root` / `[data-theme="light"]` | 2 | **宿主契约块**（token 定义的家，契约本身） |
| `*` / `*::before` / `*::after` | 3 | **box-model / 布局 reset**——插件依赖它（`border-box` 等） |
| `html` / `body` | 2 | 文档根基线 |
| `::-webkit-scrollbar` 一族 | 4 | 滚动条外观基线（插件元素共用） |
| `*:focus-visible` | 1 | 无障碍焦点环基线 |
| `input[type="number"]` / `select` | 2 | 表单元素基线 |
| 计 | **14 个形态 / 17 个站点** | （`*::before`／`*::after` 在 CSSOM 里落成 3 个站点等，站点数 > 形态数） |

> ⛔ **不许把基线当 bug 删**（那是无障碍/布局地基）；🚫 **也不许在基线文件里静默加**——**条数守卫会红**（17→16 已负控实证）。

---

## 三b、🔴 横切机制零回归（**用户 2026-09-16 硬约束：除版本号外不得有任何 UI 变化**）

**方法**：活实例（池 ＋ 壳两个文档）通过公开 config API 逐项切值 → 读回 **computed 样式** → 每项还原。**判据不是「看起来没变」，是「机制仍然响应」＋「系列零触碰」**。

| 机制 | 行使方式 | 实机读数 | 结论 |
|:--|:--|:--|:--:|
| **主题切换 / 亮暗** | `app.theme` dark ⇄ light | `--bg-window` `#1E1E1E` ⇄ `#FFFFFF`；`data-theme` dark ⇄ light；元素 `background` `rgb(30,30,30)` ⇄ `rgb(255,255,255)`；`color` `rgb(212,212,212)` ⇄ `rgb(45,45,45)`；**池与壳两文档同步** | ✅ |
| **字号（85 / 100 / 150 四档）** | `app.uiFontScale` | 85 → `--ui-scale: 0.85` / `--font-size-sm: 11.05px`；150 → `1.5` / `19.5px`；元素 `font-size` 同步 | ✅ |
| **圆角（滑杆 0 / 6 / 16）** | `app.surfaceRadius` | 0 → `--radius-md: 0px`；6 → `6px`；16 → `16px` | ✅ |
| **玻璃三参** | 读 scoped 根（`.ldk-icon-bar` / `.ldk-side-panel` / `.ldk-panel-zone`） | `--surface-glass-blur-scale` **0.44 / 0.44 / 1.11**（与 1.24 记录的 `.icon-bar, .side-panel { …: 0.44 }` / `.panel-zone { …: 1.11 }` **逐字相符**）⇒ **类限定 token 仍按 1.21 改名后的 `.ldk-*` 根解析** | ✅ |
| **字体族** | 读 token | `--font-ui` `-apple-system, BlinkMacSystemFont, "Segoe UI", …`；`--font-mono` `"JetBrains Mono", Consolas, monospace` | ✅ |
| **翻译（中英切换）** | `app.language` zh ⇄ en | 中文：`置顶 / 最小化 / 文件 / 查看 / 帮助 / 串口监视器`；英文：`Always on Top / Minimize / File / View / Help / Serial Monitor`；⇄ 往返正常 | ✅ |
| **背景（panorama / mask 通道）** | `app.appearanceMode=custom`（**`dependsOn` 前置**）＋ `app.backgroundImage`（data: URI）＋ `backgroundOpacity` / `backgroundMask` | `--bg-image: none` → `url("data:image/png;base64,…")`；`--bg-opacity: 1 → 0.5`；`--bg-mask: 0 → 0.3`；还原全部归位 | ✅ |
| **多配色 / 混搭** | ⚠️ **本 profile 未充分行使** | 隔离 profile 只装了 `theme-defaults`（内置 dark/light 两条配方）；**9 只纯数据主题插件不在 profile 里** | 🔷 **结构性论证代替**（见下） |
| **monaco 内容孤岛** | ⚠️ **本格未重跑** | 隔离 profile **无工作区、无打开的文件** ⇒ 编辑器视图未挂载 | 🔷 **引 1.21 读数 ＋ 结构性论证**（见下） |

**🔷 两处未行使项的论证（诚实交代，不用「看起来没变」糊过去）**：

1. **多配色 / 混搭** —— **10 只主题插件实测 `0` 个 CSS 文件**（本格逐仓核：`theme-aurora-glass` / `theme-defaults` / `theme-iconset-pastel` / `theme-mint-soda` / `theme-panorama` / `theme-pill` / `theme-songti` / `theme-terminal` / `theme-twilight-forest` / `theme-zones` **全部 0 css**）⇒ 它们是**纯数据配方**（`themes/*.json`），而**本系列的每一次改动都是 CSS 标识符**（类名 / 关键帧 / token 作用域 / 选择器形态）⇒ **构造上够不到它们**。旁证：本轮主题切换链（`app.theme` ⇄ light/dark 经配方解析 → token 落 `documentElement`）实机读数全部正常。
2. **monaco 内容孤岛** —— ① **结构性**：全系列 **零 monaco 文件改动**（逐提交核 `git show --stat` 覆盖 1.16 起的改名轮，`monaco` 命中 **0**）；② **读数**：1.21 那一轮**实机测过并留了读数**——「**字号 85/150 下宿主标签 11.9/21px、monaco `.view-line` 恒 14px**」（= 字号不随全局 = 孤岛成立），且同轮记录「旧选择器命中 = [] ＋ 宿主面缺失 = []」（= 改名零命中）。⚠️ **本格未重新采一次**（隔离 profile 无工作区）⇒ **如实标注为「引前轮读数 ＋ 结构性论证」，不冒充本格实测**。

---

## 四、发版清单（**十件套一路发了什么**）

**软件（壳）**：`0.1.65 → 0.1.66`（1.12）… → **`0.2.0`**（件 7 落地域对齐同批）→ **`0.2.1`**（1.28a，PATCH：命令面板 `>` 前缀 14→16 的向后兼容修复）。**终态 `0.2.1`。**

**作者轴四包（终态版本）**：

| 包 | 终态 | 本系列最后一跳 |
|:--|:--:|:--|
| `@linkdesk/plugin-sdk` | **0.1.28** | 1.28b（带 `minAppVersion` 生产端） |
| `@linkdesk/ui` | **0.3.0** | 1.21b（共享组件域 58 名归一） |
| `@linkdesk/plugin-docs` | **0.1.10** | 1.26（§12.6 进文档包） |
| `@linkdesk/contracts` | **0.1.17** | 本系列未动（不在四轴射程） |
| `@linkdesk/create-linkdesk-plugin` | **0.1.10** | 本系列未动（1.14② 骨架轮之后） |

**插件仓（终态版本）**：`file-tree 1.0.10` · `settings 1.0.14` · `serial-monitor 1.0.17` · `marketplace 1.0.34` · `editor 1.0.11` · 其余 13 仓见各仓 `plugin.json`。
**作者轴黄灯**：`check:npm-release` ✔️ **基线一致（灭）**。

---

## 五、🔴 残余边界（诚实清单——**逐条写进报告，一条不许省**）

> **本节是本报告与「宣布大获全胜」的分界线。** 权威源 = [22 号档 §六](22-收口总方案-跨方样式污染九件套.md)（**只互指、不复制**，防第二真相源）；下面是**本系列终态下的对应事实**。

| # | 边界 | 本系列终态下的事实 |
|:--:|:--|:--|
| **1** | **机制层未隔离**（明文折中） | 决策 = [docs/decisions/rejected/plugin-view-style-isolation.md](../../../../decisions/rejected/plugin-view-style-isolation.md)（四路评估 ＋ 5 条触发条件）。⇒ 表述纪律：**说「事实上不会发生」，不说「不可能发生」** |
| **2** | **宿主基线选择器是有意的跨方命中** | 记为**已登记例外**：`src/index.css` **17 处**（文件级 ＋ 条数登记、零名字清单），逐条理由见 §三。**不假装它不存在**，也不当 bug 删 |
| **3** | **门禁是静态的** | 运行时动态拼出的名字理论上可绕过（已知形态：`theme-${variant}` 这类由数据拼出的类名/属性名）。⇒ **本系列不加运行时拦截**；探针的「覆盖不到」清单已列此项 |
| **4** | **第三方依赖注入的 CSS 不被命名空间化** | `@vscode/codicons` 的 **651 个 `codicon*`** 类名（60+ 图标靠它）是**外部契约**，改了全软件图标变方块。机械依据：本仓**只消费、从不独立定义**它（探针实测：codicon 方 651 名，宿主/共享域与它**交集 0**）。⚠️ 本轮那 **2 处 yellow** 正是它在轴 ④ 的投影 |
| **5** | **`<pluginId>-` 唯一性依赖硬约束 11** | 前缀唯一性建立在「**插件 id 发布后不可变**」上；若将来出现 id 冲突，前缀唯一性失效（今天由 loader 的存在性检查兜） |
| **6** | **scoped 调优是特性不是缺陷** | 插件**按设计**可以命中宿主/共享组件的元素（文档明写合法）。件 7 给它加了**形态**门禁（必须自带自有锚），但**门禁管形态、不管意图**——一次有意的、写在门禁允许形态里的覆盖，仍然是允许的 |

**🔴 另加两条本系列自己长出来的（不在 22 档 §六 那六条里，但必须同格交代）**：

| # | 边界 | 事实 |
|:--:|:--|:--|
| **7** | **件 10（非样式轴）只普查、未落地** | 件 1–9 是**样式四轴**（已收口）；件 10 是**非样式轴**（[34 号档](34-件10-非样式命名空间普查报告.md)：**6 条 🔴 ＋ 5 条 🟠**，**未落地**）。⇒ ⛔ **不许把本报告读成「样式收口了 ⇒ 全收口了」**。两条并列第一的风险（**④ 上下文键** 静默改宿主菜单/快捷键可用性 · **⑥ `pluginState`** 跨 IPC 的 `pluginId` 零校验 ⇒ 越权读写）**与样式无关**，`plugin.schema.json` 全 schema **仅 2 处 `pattern`**（②③④⑦ 无 schema 层防线）。<br>🔴 **后续已立项（并发会话完成）**：非样式轴已立 **E6#111「非样式命名空间归一化」**专项（[00-整理档案](../非样式命名空间归一化/00-整理档案.md) ＋ [交接.md](../非样式命名空间归一化/交接.md)），**第 0 格（审查＋派工）已完成**，射程 **20 轮（1.31–1.50）**：5 件的评估＋落地 10 格 ＋ **迁移基建 1 格** ＋ **官方仓清账 7 格** ＋ 收紧与作者面收口 1 格 ＋ 收口复核 1 格。**下一棒 = 1.31（命令 id 与协议 id 归属 · 评估）** |
| **8** | **一座未被 `FILES` 守护的 schema 拷贝**（1.28b 抓到，本批补记为边界） | `packages/plugin-docs/docs/zh/plugin.schema.json` —— **git 跟踪文件、与 live 同 blob**（今天字节相同），但**不在** `check-plugin-schema-sync.mjs` 的 `FILES` 里。它的同步由「真源同字节 ＋ 生成器 `--check`」**间接**关闭（它是 `scripts/generate-plugin-docs.mjs` 从 `docs/03-插件制造/`（**已在 `FILES` 内**）生成的产物）。⇒ 1.28b **有意不动判据**（动它会把读数从「共 7 份」变成 8 份，并要重新裁决「生成的产物算不算拷贝面」），**只在 `FILES` 旁留注释**（`scripts/check-plugin-schema-sync.mjs:35`）。🔴 **它不是样式四轴的问题** ⇒ **本系列不再跟**，归属 = 「公共面决策」专项（**无编号，尚未立**）；**当它腐烂时的症状** = 该拷贝与 live 悄悄漂移而门禁不响。⚠️ 登记来源见 §九.9.0 第 4 行 |

---

## 六、探针覆盖不到什么（**照 22 号档 §八 的硬要求：1.20 报告必须照抄这一段**）

1. **插件 CSS 只在视图挂载时注入** ⇒ **没有活视图 = 采不到该插件的样式**（本格的方名册 = 4 只插件，与本系列历轮的基线一致）；
2. **只覆盖样式四轴**（类名 / 关键帧 / token / 选择器形态）——**本格件 10 普查出的 6 条 🔴 一条都看不见**（那些是命令 id、设置键、contextKey…）⇒ **非样式轴今天没有任何运行时量尺**；
3. **按文档算碰撞**——池文档与渲染进程文档各有自己的 CSSOM，跨文档同名**不是**碰撞（同一份 `src/index.css` 在两个文档各注入一次，跨文档求和会得 2 倍，那是**重复计数不是差异**）；
4. **看不见运行时动态拼出的名字**（`theme-${variant}`）；
5. **`vendored-shared-css` 只列名不判红**（`@linkdesk/ui` 不是 external ⇒ 每只插件 bundle 内联一份 UI 库 CSS 的**已知结构性事实**）——插件**源码**里的 `ldk-` 借用由 SDK 静态腿判红，**两把尺子合起来才无死角**；
6. **采集层不进 `npm run check`**（check 必须能在没有软件实例的机器上跑）⇒ 探针结论靠**人工/轮次纪律**触达；
7. **域外文件不采**（探针只认仓内 host / shared 两个域）；
8. **浅层形态判定**（轴 ④ 判「形态」不判「意图」）。

---

## 七、验收

- ✅ **十件全绿**（§一 逐件读数）
- ✅ **运行时探针终态零跨方碰撞**（§二：零 red、零未归属、方名册够真）
- ✅ **四轴改前/改后对账**（§三 四行全有读数）
- ✅ **横切机制零回归**（§三b：9 项中 7 项实机行使 ✅，2 项结构性论证 ＋ 如实标注未重跑）
- ✅ **残余边界六条 ＋ 自长两条成文**（§五）
- ✅ **系列外账五行终审**（§九，**2026-09-17 补**：已立项 2 ／ 自然作废 1 ／ 已裁决·已落盘·已闭环 4 ／ 真挂账 1 ／ 口径说明 1 ／ 新收 1）
- ✅ **三处状态同笔**（[00-整理档案 §九](00-整理档案.md) · [E6-执行清单.md](../../E6-执行清单.md) 层头 · `CLAUDE.md` 硬约束 23）
- ✅ **`npm run check` EXIT=0** ＋ 提交（类别前缀）＋ **未推送**
- ✅ **零 `src/`／零 `electron/`／零 `scripts/`／零 `packages/`／零插件改动**（本格纯收口文档轮）

---

## 八、给用户的大白话（**查出什么 / 哪些真危险 / 怎么治 / 还剩什么不治**）

**这一整套在治什么。** 一句话：**「不是问题在长大，是尺子一直在漏。」** 插件的样式和宿主的样式住在**同一个文档、同一张扁平名字表**里，谁都能往里写，撞了不报错——**只是长得不对**（`.badge` 案：徽标文字被自己的背景吞掉，看着是一块纯色）。而**受害方永远收不到通知**：你在自己机器上没装对方插件，一切正常。

**治的结果。** 四条轴（类名 / 关键帧 / 自定义属性 / 选择器形态）**全部结构性收口**：宿主与共享组件**自己定义的类名 100% 是 `ldk-`**（258 ＋ 89 个独立定义零例外），插件必须带**自己的 id 前缀**（18 仓实测零违规）。**「登记表」这个逃生口已经拆掉——只有改名一条路。**

**凭什么信它。** 三层：**约定**（作者面 §12 写死规则）＋ **静态门禁**（35 道，其中 34 道带自测且**逐道真跑**）＋ **运行时探针**（实机 dump 全部样式表，机械断言零跨方碰撞——本轮真跑：**零 red**）。**「门禁绿了」这句话现在是有分量的**：件 8 之后，绿必须是**证明过的绿**（每条判据都得真红过一次才算数）。

**还剩下什么没治（如实说）。**
1. **机制层没隔离**——靠的是「约定 ＋ 门禁 ＋ 探针」，**不是构造上不可能**。所以措辞是「**事实上不会发生**」，不是「不可能发生」。要不要上真隔离**想过并否决了**，决策和** 5 条触发条件**（什么情况下回头重估）都写进了 `docs/decisions/`。
2. **宿主那 17 处基线选择器是「有意的跨方命中」**（`*` reset、滚动条、`select` 这些），**插件依赖它们** ⇒ 登记成例外，不是违规，也**不许删**。
3. **门禁是静态的** ⇒ 运行时动态拼出来的名字理论上能绕过（本系列选择不加运行时拦截）。
4. **三方的 `codicon` 图标字体（651 个类名）不归我们管**，那是外部契约，动了全软件图标变方块。
5. **插件「有意覆盖共享组件外观」是合法特性**，门禁只约束**形态**、不管你**意图**。
6. 🔴 **最大的一条：样式之外的同类问题，这一轮只「查清」了，还没「治」。** 普查发现**同一个形状在这个软件里还有 6 处**：命令 id、设置键、主题/图标/语言 id、**上下文键**、`pluginState`、快捷键。其中两条**比样式的任何一条都危险**，因为它们改的是**行为/权限**而不是外观——**插件一条 `contextKey.set` 能静默改宿主菜单和快捷键的可用性（连一行警告都没有），`pluginState` 的接口还能让一个插件读写另一个插件、甚至宿主自己的数据**。这两条**与样式无关**。🔴 **后续已经立项**：**E6#111「非样式命名空间归一化」专项**（[00-整理档案](../非样式命名空间归一化/00-整理档案.md)），**第 0 格（审查＋派工）已完成**，把普查结论变成了 **20 轮**的排期（1.31–1.50，含迁移基建与 7 个官方仓清账轮），**下一棒 = 1.31**。

**一句话结论**：**样式这一类，收口了**（四条轴结构性覆盖 ＋ 三层证明 ＋ 零 red 实测）；**样式之外，查清了、排好队了、还没治**——那是下一个系列的事。

---

## 九、补记账（**2026-09-17 补**——CSS 系列「系列外账」五行的终审 ＋ 一条新收）

> ⚠️ **这一节是补的，不是 1.20 当时写的** —— 如实说明：**1.20 收口轮漏吃了这五行**。它们是 [交接.md](交接.md) 各轮尾部「⇒ 交 1.20 / 写进 1.20」的挂账行（`:125`／`:126`／`:127`／`:128`／`:129`），而本报告 §一–§八 全程没有它们（复量：`grep -n "记账\|取整\|拷贝\|LIVE_ONLY\|taskBase" 35-系列收口报告.md` ⇒ 补记前零命中）。本批逐行核实补上，**一条不省**。

### 9.0 五行终审（**一行一读数，带 file:line**）

| # | 交接原文（挂账） | 今天的事实 | 终态 |
|:--:|:--|:--|:--:|
| 1 | 🔴 **插件侧死类**（原文只写 `serial-monitor` 一处；「同类可能还有」）⇒ 归插件仓自查／规范化层 | 系统性重数：**10 处 / 3 仓**（file-tree 5 · marketplace 4 · serial-monitor 1），**零跨方消费**（壳仓代码命中 0）。见 [09 号档](../../插件规范化层/09-任务-插件侧死类清账.md) §1.1／§1.3 | ✅ **已立项 `E6#113`**（任务书 [09](../../插件规范化层/09-任务-插件侧死类清账.md)） |
| 2 | ① **判据⑧ 插件域另一半**（插件侧 `animation:` 引用的关键帧是否存在）⇒ 归一个新 SDK 轮<br>② **CLAUDE.md 7.9 轮「153 文件 / 2,070 例」与本机实测不符** ⇒ 交 1.20 | ① 实测 SDK 腿这一半为空（`plugin-prefix.ts:249` 只有文案）；缺口有真实质量（18 仓 **3 对**引用／定义）。见 [08 号档](../../插件规范化层/08-任务-插件域关键帧引用判据.md) §1.2／§1.3<br>② 见 §9.2 | ✅ ① **已立项 `E6#112`**（任务书 [08](../../插件规范化层/08-任务-插件域关键帧引用判据.md)）<br>✅ ② **自然作废** |
| 3 | ① **1.26 的 token V6 黄灯诚实边界**<br>② **1.27 的两条读数账**（探针报 host 259 vs 文档 258）⇒ 交 1.20 | ① V6 = 「插件把**有主**的名字写在文档级」= 🟡 **只报不拦**（[31 号档 §2.2](31-任务-token轴门禁与清账.md) 判级表 `:170`）；存量已清零（本报告 §二：探针 token 判级 **红 0 / 黄 0**），作者面已写进 [05-插件UI写法规约 §12.5](../../../../03-插件制造/05-插件UI写法规约.md)<br>② 见 §9.3 | ✅ ① **已裁决 ＋ 已落盘**（非挂账）<br>✅ ② **已闭环** |
| 4 | ⚠️ **1.28 三条**：① `LIVE_ONLY` 豁免 1 条 ② 未被 `FILES` 守护的拷贝 1 座 ③ `check-design-flow` 的 `taskBase()` 不取整 | ① **已落地**（`workspace.schema.json` 的 `LIVE_ONLY` 豁免 ＋ 条带机械核验）② **仍在**：`packages/plugin-docs/docs/zh/plugin.schema.json`（git 跟踪、与 live 同 blob、**不在 `FILES` 里**），1.28b 只在 `FILES` 旁留注释（`scripts/check-plugin-schema-sync.mjs:35`）③ **已钉成事实**（未改代码，自测里钉住 `parseFloat("146.2")` → 146.2 的真实行为） | ✅ ① 已闭<br>🔴 ② **真挂账 → 转 §五 残余边界第 8 条**<br>✅ ③ 已闭 |
| 5 | ⚠️ **1.29 ＋ 1.30 两条**：`check-doc-links` 基线 **593 → 594 → 595** 个 md | 见 §9.4 | ✅ **不是挂账，是口径说明** |

### 9.1–9.4 四条实质结论

**9.2｜CLAUDE.md 那组读数：已自然作废（零动作）。**
复量：`grep -n "150 文件\|153\|2,0\|2070" CLAUDE.md` ⇒ **零命中**。CLAUDE.md **今天不持任何「文件数／例数」读数**（并行压缩轮把带读数的那段压掉了；现在「提交前自检」只描述门禁**构成**、不记读数）。⇒ 该行**无须复核即作废**。⚠️ `E6-执行清单.md` 各轮行里的历史读数（153 文件 / 2063–2075 例）**保持原样**——历史行的读数就该是当时的值，**改它才是造假**。

**9.3｜259 vs 258：差的那 1 个名字是 `.app-shell`——两行本是同一件事（本批新查实）。**

| | 读数 | 域 | 来源 |
|:--|:--:|:--|:--|
| **旧** | **258** | `src/index.css` ＋ `src/pool/**` | 件 4 定格（1.16／1.17／1.21b） |
| **今** | **259** | `src/*.css` ＋ `src/pool/**` | 1.26 域对齐后（[27 号档 §十一](27-任务-门禁健康度体检.md) 旁证行） |

差的那 **1 个** = `src/App.css:1` 的**唯一**独立定义 `.app-shell`（复量：`grep -c "^\." src/App.css` ⇒ **1**）。而 [27 号档 §十一](27-任务-门禁健康度体检.md) **同一张表的上一行**就是「① 域边界不一致：**0 个名字** ✅ 闭合（1.25 改前 = **1 个：`app-shell`**）」——**同一张表里上下两行说的本是同一个名字**，当时没人把它俩连起来。机械证据 = `scripts/check-css-namespace.mjs:260-265` 的注释原文（「原先只有 `index.css` ⇒ `src/App.css` 的 `.app-shell` **落在域外** ⇒『宿主自己定义的类名 100% 是 `ldk-`』这句话**按门禁口径为假**（1.22 实机抓到、1.25 裁决 (a) 对齐域）」）。1.26 同笔把它改名 `.ldk-app-shell` ⇒ **今天域内非 `ldk-` 独立定义 = 0**。

🔴 **处置 = 一个历史读数都不改**：`258` 是**件 4 定格口径**下的历史数，出现在 CLAUDE.md 硬约束 23 ／ [11 档 §三](../11-样式命名空间审计.md) ／ 本报告 ／ [00-整理档案](00-整理档案.md) ／ [交接.md](交接.md)——改它 = **动「258 ／ 89」两串全系列读数**，而 [32 号档 §四](32-任务-选择器形态轴门禁与落地.md) 已写死「⛔ 不许为 F3 改既有语义——那会动 258/89 两串读数」。⇒ **规矩**：**今天的实时读数报 259**（域 = `src/*.css`）；**看到 `258` 时按「旧域定格」读，不算错**。两个数都对，**差的是域不是名字**。

**9.4｜doc-links 那三个数：不是账，是「巡检面的大小」。**
`scripts/check-doc-links.mjs:324` 每次运行打印一行 `已扫 N 个 md（源 docs/）`——**它不是基线**，是**被扫的 md 文件数**，每写一个 md 就 **+1**（1.27 = 592 → 1.28 = 593 → 1.29 = 594 → 1.30 = 595）。今天读数 = **598**（含本批新增 [08](../../插件规范化层/08-任务-插件域关键帧引用判据.md)／[09](../../插件规范化层/09-任务-插件侧死类清账.md) 两档；**另有 1 个差额本批不追** —— 追它就是把巡检面当基线，**那正是本系列裁过「空转 ≠ 零存量」的同一个病**）。⇒ **判据**：任何轮读数只要 = 上一轮 ＋ 本轮新增 md 数，即为正常，**不需要记账**。

### 9.5 🔴 本批新收一条：`scratch/audit-plugin-scope.mjs` **不在 git 里**

- **事实**：`git ls-files scratch/` ⇒ **空**；`.gitignore:52` 忽略整个 `scratch/`。而该脚本被**约 10 份文档**引作「**根治的机械定义**」（含 **E6#111 的 1.50 验收判据**）。
- **后果**：**引用它的文档全部指向一个不在仓里的文件** —— 换台机器、或别人 clone 本仓后，那份「机械定义」**根本不存在**。⚠️ 现有门禁**查不出来**：`check-doc-links` 只扫 `docs/` 内 md 之间的互链，**不校验「文档 → 脚本」**。
- **两个合法形态（🔴 需拍板 —— 它动的是「`scratch/` 是本机临时」这条既有约定）**：

| 形态 | 做法 | 代价 |
|:--|:--|:--|
| **(a) 搬进 `scripts/`**（照审计族 `scripts/audit-*.mjs`） | 移动 ＋ **同笔改那 ~10 处引用** | 10 处文档改动；它从此是仓的**长期资产**（⚠️ 审计族**不需要** `--self-test` —— `check-gate-health` 只管 `check-*.mjs`） |
| (b) `git add -f scratch/audit-plugin-scope.mjs` | 给 ignore 目录开单文件例外 | **与 `scratch/` 的「本机临时」定位冲突**，不推荐 |

- ⇒ 本报告**只记账 ＋ 给出两形态，不动手**（跨系列约定，且 E6#111 正引用它）。**附带立一条判据**：**写文档时不许引 gitignore 里的文件** —— 引了就等于写了一条永远断不了、也永远点不开的链（`check-doc-links` 看不见它）。

### 9.6 补记的验收

- ✅ **五行逐行有终态**（§9.0）：**已立项 2**（`E6#112`／`E6#113`）· **自然作废 1**（7.9 读数）· **已裁决/已落盘/已闭环 4** · **真挂账 1**（转 §五 第 8 条）· **口径说明 1**（doc-links）· **新收 1**（§9.5）
- ✅ **两个新号已开格**：[08](../../插件规范化层/08-任务-插件域关键帧引用判据.md)（`E6#112`）· [09](../../插件规范化层/09-任务-插件侧死类清账.md)（`E6#113`）
- ✅ **报告自证完整**：`grep` 复量显示补记前 §一–§八 对这五条**零命中** ⇒ §九 是**净增**，不是重复
- ✅ **一条历史读数都没改**（§9.3 的规矩）
