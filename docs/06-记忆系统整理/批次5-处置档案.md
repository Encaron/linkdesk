# 批次 5 处置档案 · E6 巨型文件瘦身

> **执行日期：** 2026-09-12
> **记忆仓提交：** `bd2a595`（消息含「批次 5」）· **本批前回滚点：** `5a8ff16`（批次 4）
> **范围：** `01-分批执行路线.md` 批次 5 —— **输入 8 份**（6 压缩 + 1 折叠 + 1 删除）
> **结果：** 压缩 **6** · 折叠 **1** · 删 **2** · 修引用 **9 文件 / 9 行** · 入库文件总数 **199 → 197**（净 −2）· 工作区 `memory/*.md` **200**（含并行 E6 agent 3 份未跟踪新文件）
> **本批不合并，只压缩。**

---

## 一、审议门结论

- 2026-09-11 前置会话已呈现批次 5 报告（8 份读数 + 逐份压缩方案 + 保留清单 + 2 份删除项折入方案），**用户「同意」放行**（无推翻项，2 份删除按建议执行）。
- 2026-09-12 本会话用户追加**「全自动」授权**——执行机制（写文件 / 跑脚本 / git add / commit）不再逐步确认；**审议门本身不废**（判定权仍在用户，只是本次判定已放行）。
- 「待用户裁」区见 §七（三份未达标体积）。

---

## 二、压缩结果（6 份）

| 文件 | 前 | 后 | 路线书目标 | 达标 | 一字未动 / 强保真项 |
|:--|--:|--:|:--:|:--:|:--|
| `e6-progress.md` | 219.9K | **34.4K** | < 40K | ✅ | **全部轮次明细 + 83 个 commit 号**（逐条核验 0 伪造）；硬约束出处可检索 |
| `notification-surface-single-panel.md` | 45.2K | **8.0K** | < 8K | ✅（+0.05K） | Why / How to apply / 已否决项 |
| `hall-architecture-model.md` | 32.4K | **15.3K** | < 15K | 🟡（+0.3K） | **顿悟原文 + 大厅 ASCII 图一字不动**；3 张桌子表 / 3 条管理规则 / 4 种角色 / 2 种通信模式 / 全部 bug 反例 |
| `snapshot-shadows-truth-bug-class.md` | 23.7K | **15.4K** | < 12K | ❌（+3.4K） | 六个 bug 类**全保**（形 / 扫法 / 修法 / 第二实例 / 收尾）+ **第四句问法** + 回归测试硬要求 |
| `e6-review-rhythm-execution-batch.md` | 19.6K | **9.7K** | < 8K | ❌（+1.7K） | 审视单位＝轮 + **三教训（含全部 git 实锤 hash）** + Why + 十条 + E6 语境校准 + 五判据 + 挪位置判据；**「已审完的轮」长枚举 → 压成一行** |
| `design-skills-inventory.md` | 14.4K | **10.7K** | < 8K | ❌（+2.7K） | 18 项一览 + **自然语言路由表（触发词功能性数据，未压）** + 兜底顺序 + 玻璃态歧义裁决 + 壳 UI skill 选择 + 竞标裁决「皮肤由主题系统定」+ 接线结构 |

**判据自检（T5.12）：** 逐份重读压缩稿，自问「原文里有没有哪句是**结论 / 拍板 / 数字**被压没了」——
- `hall` 的 **G9 `update.ts:213` 三次复发**、`ContextKeyService.setValue` 5 key、`FactorySlots.initialize` → `Ctrl+,` 死 ID、`loadThemePlugin`/`revertThemeIfCurrent` 裸 CSS、`~40/+20/+15 行`：**全在**。
- `snapshot` 的 **9 个 commit 号**（`b96667e29`/`60c2863b8`/`f9ca3cdd0`/`8e196f9f6`/`903fa02e5`/`32fb8f149`/`43a0fbeac`/`ba07f2e7c`/`dc3a4c66f`）与 **① 三问② 成对改③ 判定自现算④ 一次机会⑤ 隐形记账⑥ 守卫在包里** 六类结论：**全在**。
- `e6-review-rhythm` 的 **`b4f7f01a` / `e243089c` / `e9b0813e` / `f94b09f1` / `a7d6e54b`** 五次事故实锤与 **13-14 个轮**：**全在**。
- `design-skills` 的**触发词**（路由功能数据）：**全在**（不予压缩——压掉即降级可用性）。

---

## 三、折叠与删除（3 份）

### ③ 折叠 1：`factory-vs-marketplace-plugins.md`（14.1K）→ `factory-role-coexistence.md`

> 该文 **2026-09-05 用户重定义后两分框架作废**（插件只有一类，见 `e6-plugin-folder-flatten`）。**只折存活部分**，作废的四层全景 / 三层防御表 / 判断四问 / 用户视角 / JSON 模式**不折**。

折入 `factory-role-coexistence.md` 新增节 **「factoryRole 槽位机制（原 `factory-vs-marketplace-plugins` 存活部分）」**，四项：

| 存活项 | 内容 |
|:--|:--|
| **极客替换——工厂插槽** | 壳不写死 `pluginId === "settings"`，走 `FactorySlots.get("settings")`；**替换四步**（写插件声明 factoryRole → 丢进 `plugins/` → 改 `factory-slots.json` → 重启） |
| **FactoryRole 开放枚举**（`#36e2d`，2026-07-26） | closed enum → open string：`FactorySlots.ts` 两行 + `plugin.schema.json` 删 `enum` 保 `type:string` |
| **Marketplace 两条实现路径** | 路 A 内部模式切换（`mode: "sidebar" \| "store"`）vs 路 B 换插件（新插件声明 `factoryRole:"marketplace"`）；**两路径可共存** |
| **factoryRole 硬编码指针** | 核心仅两个：`Ctrl+,` → `"settings"` · 侧栏市场按钮 → `"marketplace"`；**文件树不需 factoryRole**（走 `contributes.views`/`contributes.commands`） |

`factory-role-coexistence.md` 体积 10.6K → **13.4K**（+2.8K，折叠所致，非膨胀）。

### ④ 删除 2 份

| 文件 | 前 | 依据 |
|:--|--:|:--|
| `new-ai-prompt.md` | 24.4K | **2026-09-11 已折入** `linkdesk-is-universal-container.md`「AI 自检——你会忍不住说的话」整表（该文 §AI 自检，含溯源注记）⇒ 本次直接删 |
| `factory-vs-marketplace-plugins.md` | 14.1K | 存活部分已折入 `factory-role-coexistence.md`（见 §③）⇒ 删 |

---

## 四、删 / 改产生的引用修复（9 文件 / 9 行）

| 文件 | 原文 | 改为 |
|:--|:--|:--|
| `card-workspace-universal-engine.md:54` | `[[factory-vs-marketplace-plugins]] — 工厂插件 vs 市场插件` | `[[factory-role-coexistence]] — factoryRole 槽位机制（工厂/市场两分框架已作废）` |
| `file-tree-foundational-plugin.md:69` | `[[factory-vs-marketplace-plugins]] — 文件树在第一层（基本生存）` | `[[factory-role-coexistence]] — factoryRole 槽位机制` |
| `keybinding-system-complete.md:133` | `[[factory-vs-marketplace-plugins]] — 快捷键管理器远期应是工厂插件` | `[[factory-role-coexistence]] — 快捷键管理器远期应可替换（factoryRole 槽位）` |
| `plugin-independence-iron-law.md:91` | `[[factory-vs-marketplace-plugins]] — 出厂插件 = 市场插件 + 出厂保护` | `[[factory-role-coexistence]] — factoryRole 槽位与极客替换` |
| `settings-plugin-replaceable-ui.md:60` | `[[factory-vs-marketplace-plugins]] — 工厂插件 vs 市场插件定义（core:true 语义 = 跟发行版打包 + GUI 无卸载按钮）` | `[[factory-role-coexistence]] — factoryRole 槽位机制（core:true = 纯 UI 防误删旗标）` |
| `pre-v1-bundled-plugin-curation.md:19` | `[[factory-vs-marketplace-plugins]]（出厂 vs 市场分发哲学）` | `[[factory-role-coexistence]]（factoryRole 槽位）` |
| `e6-plugin-folder-flatten.md:45` | `[[factory-vs-marketplace-plugins]]（历史两分框架，已盖取代 banner）` | `[[factory-role-coexistence]]（工厂 vs 市场两分框架已作废，仅存 factoryRole 槽位机制）` |
| `ai-communication-protocol.md:66` | `[[new-ai-prompt]] [[ai-workflow-discipline]]` | `[[ai-workflow-discipline]]` |
| `why-code-exceeds-expectations.md:126` | `- [[new-ai-prompt]] — 新 AI 进场必读` | `- [[linkdesk-is-universal-container]] — 新 AI 进场必读` |

> **刻意保留的非链接提及（非断链）：** `linkdesk-is-universal-container.md` 溯源注记「由 `new-ai-prompt.md` 折入」（反引号，非 wiki 链接）· `progress-tracking-rules.md:14` 叙述性提及「散落在 …`new-ai-prompt.md`…」（历史陈述，保留）· `factory-role-coexistence.md §factoryRole 槽位机制` 节首同样以反引号提及已删文件名。

---

## 五、执行与校验

| 项 | 结果 |
|:--|:--|
| 6 份压缩文件 | 全部产出并落库（体积见 §二） |
| 折叠件 | `factory-role-coexistence.md` 新增节含全部 4 项存活内容 |
| 2 份删除 | `git rm` 已执行，全文残留引用 **0** |
| 全库 `[[wiki]]` 断链 | **HEAD 40 → 批3 38 → 批4 34 → 本批 34**（**本批未新增**；净变化 0） |
| 全库 `](x.md)` 断链 | 45 条（**均指向工程 `../docs/…` 外部文件或已删文档，非 memory 内部**；非本批引入，留批次 6 判定口径） |
| 孤儿文件（无链接且不在 MEMORY.md） | **20 份**（本批未触碰；批次 6 T6h 归零项） |
| 白名单 16 份 | 完好，未触碰 |
| 体积 | 工作区 `memory/*.md` 合计 **1010K（0.99MB）**；本批 9 份净 **−297K** |
| `git diff --cached --stat` | `19 files changed, 532 insertions(+), 1917 deletions(-)` |

**机械核对：**

```
git ls-files memory | grep -c '\.md$'                    # 197
git show bd2a595 --stat | tail -1                        # 19 files changed, 532 insertions(+), 1917 deletions(-)
```

---

## 六、MEMORY.md 处理（blob 重建 + 工作树双向重放）

- 索引行 `MEMORY.md`：「`- [Design Decisions](design-decisions.md) / [Factory vs Market](factory-vs-marketplace-plugins.md) / [Monaco]…`」→ **移除 `[Factory vs Market](…)` 片段**（该文件已删）。
- **手法（沿用批次 4）：** `git show HEAD:memory/MEMORY.md` → Node 字符串替换 → `git hash-object -w` → `git update-index --cacheinfo 100644 <hash> memory/MEMORY.md`。**只纳入我方 hunk，不触碰并行 E6 agent 对顶部的未提交改动。**
- **工作树双向重放：** 对并行 agent 工作树版本**重放同款删行**——保证工作树同时含「我方删行 + agent 顶部插入」，避免 agent 后续 `git add MEMORY.md` 时回退我方改动。

---

## 七、遗留 / 待用户裁

1. **⚠️ 三份未达路线书体积目标**（**未强压，等用户裁**）——按「**压缩比删除更容易误伤**（压没了读者不会发现）」铁律，**未删任何 bug 结论 / 拍板 / 触发词去凑数字**：
   - `snapshot-shadows-truth-bug-class` 15.4K（目标 <12K，+3.4K）：六个 bug 类每类都有独立的**形 / 扫法 / 修法 + 第二实例 + 收尾**，全部为拍板级结论；实测已 5 轮压缩（21.2→17.1→16.7→16.4→15.4K）。
   - `e6-review-rhythm-execution-batch` 9.7K（目标 <8K，+1.7K）：路线书要求保留的「审视单位 / 三教训含 hash / Why / 十条 / 语境校准 / 五判据 / 挪位置判据」已占满；唯一可压的「已审完的轮」长枚举**已压成一行**。
   - `design-skills-inventory` 10.7K（目标 <8K，+2.7K）：**路由触发词是功能性数据**（删词即路由失效），不属可压散文。
   - **裁量建议：** 接受三份超出（合计 +7.8K，相对终检 550KB 目标为 1.4%）；或用户点名指定可删项。
2. **20 份孤儿 + 34 条 `[[wiki]]` 断链** → 移交批次 6（T6h 校验目标：孤儿 0 / 断链 0）。断链多为「点 vs 横线」命名漂移（如 `[[e5-8-contracts-npm-publish-ready]]` 实为 `e5.8-contracts-npm-publish-ready.md`），**均非本批引入**。
3. **并行 E6 agent 未提交改动原样保留**（`MEMORY.md` 顶部、`cdp-ui-automation`、`dev-environment`、`e6-l4-deferred`、`e6-l5-handoff`、`flaky-full-suite-tests`、`linkdesk-is-universal-container`，及 3 份未跟踪新文件）——本批提交只纳入我方改动；MEMORY.md 已做**双向重放**防回退。
4. **临时脚本已清理**（`.b5*.cjs` / `.b5*.txt` / `.b5out/`）。
