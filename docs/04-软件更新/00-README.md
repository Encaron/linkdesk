# 04 — 软件更新（软件本体）

> 2026-09-13 重组定案：原「04-出厂制造」+「05-版本更新/壳版本」合并至此，改名 **软件更新**。
> **出厂制造概念退役**——E6（插件生态与发布）完成即出厂，不再有独立出厂工序；**壳版本/官方插件/市场插件三分法退役**——plugins 目录已铺平，只有「普通插件 + core:true（仅 UI 层防误删，不挡命令行/工程手段）」。
> ⚠️ 本目录管**软件本体**（壳 + 出厂自带的那批插件的宿主体验）；**插件各自的更新**归 [`../05-插件更新/`](../05-插件更新/)。

## 一、版本号规则：CHANGELOG 是唯一真相源

软件更新点**不按本目录顺序走，也不按预设版本号走**——用着用着发现哪里不好，那个点就该更新，可能插队到任何规划点之前。流程：

```
发现更新点 → 进「待抉择池」（不占版本号）
          ↓ 拍板要做
开 v1.x-*/ 目录立项（此时才定版本号，颗粒自定）
          ↓ 做完
CHANGELOG.md 落版本段（版本号唯一真相源）+ 本 README 已立项表划销
```

MINOR/PATCH 颗粒、MAJOR 触发条件沿用 [CHANGELOG.md](../../CHANGELOG.md) 既有口径；本目录只管「点什么、做没做」，不管「号排到几」。

## 二、待抉择池（想到先记，不占版本号）

| 更新点 | 状态 | 档案 |
|:--|:--|:--|
| **底部面板显隐重启复现**——关面板→关软件→重开，面板又回来了；持久化机制存在（`layout.json panel.visible`），疑启动后有路径重新展开 | 🔍 待调查 | [待抉择池/底部面板-关闭后重启复现.md](待抉择池/底部面板-关闭后重启复现.md) |
| **菜单系统补全**——壳顶部菜单栏补全（原 v1.1.0 的壳侧一半；Git 一半已拆为插件，见 05） | 📋 待拍板 | [待抉择池/菜单补全.md](待抉择池/菜单补全.md) |
| **项目管理器**——对标 `.code-workspace` 的项目概念（`.linkdesk-project/`：项目名/工作台布局/项目级设置，欢迎页「最近项目」）；原 v1.4.0 | 📋 待拍板 | [待抉择池/项目管理器.md](待抉择池/项目管理器.md) |
| **发行说明刷新按钮**——发行说明数据是 24h 落盘缓存且全链路无手动刷新入口（`retry` 只管"拉失败重试当前版"），刚发的新版本当天看不到；补一个绕过缓存的「刷新」按钮 | 📋 待拍板（**子夹成套**：设计文档 + 增量 mockup） | [待抉择池/发行说明刷新按钮/00-README.md](待抉择池/发行说明刷新按钮/00-README.md) |
| **设置页「通用」归类**——把壳声明的「更新」2 条并进「通用」（同为壳声明），8 条用现成 `prop.group` 二级机制分节：界面 / 系统集成 / 更新 | 📋 待拍板 | [待抉择池/设置页通用分组归类-并入更新组.md](待抉择池/设置页通用分组归类-并入更新组.md) |
| **工作区导入导出——布局恢复断线**——文件菜单「导入工作区」点了没用：设置生效、布局静默丢弃（`lifecycle.ts:133` 空分支 + 承诺接管的 `workspace:restore` 事件从未存在）；根因已定位，断点 = E5#5e-ii-f 重构搬家 | 📋 待拍板（根因已定位，类别 fix） | [待抉择池/工作区导入导出-布局恢复断线.md](待抉择池/工作区导入导出-布局恢复断线.md) |
| **标签栏内容自适应与横向滚动**——标签宽度改成按内容撑开（不再挤压/省略），溢出走横向滚动（滚轮已实现，缺「自动滚入视野」配套）；对标 VS Code 但宽度策略取自适应 | 📋 待拍板 | [待抉择池/标签栏内容自适应与横向滚动.md](待抉择池/标签栏内容自适应与横向滚动.md) |
| **程序图标偏小 + 品牌标不统一**——桌面/任务栏/开始菜单图标比别的软件小一圈（实测 256px 帧内容仅占画布 63%×73%，对照 App 92–100%），且软件内标题栏/关于页用的是「LD」文字标而非程序图标（六边形+三节点）；根因 = 品牌资产无单一源 | 📋 待拍板 | [待抉择池/程序图标偏小与软件内品牌标不一致.md](待抉择池/程序图标偏小与软件内品牌标不一致.md) |
| **发行后-帮助菜单待补项**——打开帮助（需整合 `03-插件制造/`）+ 隐私政策 | 📋 发行后 | [待抉择池/发行后-帮助菜单待补项.md](待抉择池/发行后-帮助菜单待补项.md) |
| **API 注释与速查表英文化**——作者在 IDE 里读到的签名说明、以及 npm 速查表的 Notes 列仍是中文（零行为改动，动 `src/**` 注释面；交付落点是**作者轴 npm**） | 📋 待抉择 | [待抉择池/API注释与速查表英文化.md](待抉择池/API注释与速查表英文化.md) |
| **许可证方案**——🔴 实测仓库无 `LICENSE`；推荐归 AI、选择归用户 | 📋 发行后 | [待抉择池/许可证方案待选.md](待抉择池/许可证方案待选.md) |
| **更新检查遇匿名额度限流**——toast 说「稍后会自动重试，无需处理」，但检查腿失败的自动重试**未开工**（#57.9g；`manual` 档彻底不成立）；403 归因不读 `x-ratelimit-remaining`/`-reset` ⇒ 拿不到恢复时刻、且一切 403 都说成限流；根因是开代理时出口 = **共享 IP** 的 60/时匿名册 | 📋 待拍板 | [待抉择池/更新检查遇匿名限流.md](待抉择池/更新检查遇匿名限流.md) |

## 三、已立项

（暂无——立项后在此挂行：`v1.x-xxx/` 目录 + 一句话 + 状态。做完在 CHANGELOG 落版本段后此处划销。）

## 四、已销账（结论保留一行，档案已删）

| 原条目 | 结论 |
|:--|:--|
| 03-FloatingPanel-通用悬浮面板 | 已实现为壳内底部面板（E5.8 panel 体系），档案删 |
| 04-ColorPicker-取色器 | 已做成插件，档案删 |
| 08-dependsOn-Accent-Mode-强调色联动 | 已实现（主题强调色联动），档案删 |
| E4后-插件SDK（原 05/壳版本） | 已完成——`packages/plugin-sdk` + `@linkdesk/ui` 已发 npm，档案删 |
| v1.3.0-可拖出标签页（原 05/壳版本） | 已完成（E5.7 脱出窗口体系），档案删；设计沿革见 `02-Electron架构/E5.7_极简Pool/脱出窗口/` |
| v1.5.0-自动更新与分发安全（原 05/壳版本） | 由 **E6 承接**（进度真相源：`02-Electron架构/E6_插件生态与发布/E6-执行清单.md`），档案删 |
| v1.6.0-WebView淘汰策略（原 05/壳版本） | 废案——架构已演进，档案删 |

> 原「05-版本更新/00-README」顶部两条发行后提醒（打开帮助/许可证）已迁待抉择池；其「壳版本路线图」职能由本文件 §一/§三 接管。

---

> **→ 插件更新：** [`../05-插件更新/`](../05-插件更新/)
> **← 架构基础：** [`../02-Electron架构/`](../02-Electron架构/)
> **← 发布管理：** [`../06-发布管理/`](../06-发布管理/)
