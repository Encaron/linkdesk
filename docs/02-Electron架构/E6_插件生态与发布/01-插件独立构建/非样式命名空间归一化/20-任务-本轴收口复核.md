# 第 1.50 轮 · `#111l`：**本轴收口复核**（🔴 恒为最后一格）

> **一句话**：本轴的**收口复核轮**——把件 1–5 的终态**逐条跑出读数**（不是抄）、把**运行时零碰撞**做实、把每一件的 **改前 / 改后对账**摆平、把**残余边界诚实清单逐条写进报告**，并给用户一份**大白话**说明：**查出什么 · 哪些是真危险 · 怎么治的 · 还剩什么不打算治**。
>
> **编号**：`#111l` · **收口复核（本轴最末一格）**
> **🔴 执行序与前置**：**前置 = [1.49](19-任务-门禁与作者面收口.md) 收官（件 1–5 ＋ **迁移基建** ＋ **官方仓 7 格清账** ＋ 门禁收紧 ＋ 作者面 ＋ 发版 全部完成）**。**本格之后本轴结束。**
> 🔴 **本格进场第一件事**：读 1.41 的交接段（**它带 §2.1 的核对表**，那是本格的复核底稿）。
> **拍板记录**：无。**本格不改代码**——只跑读数、写报告、点欠账（**发现必须改的，立新格，不许顺手改**）。

> **非新能力声明（`check-design-flow` §8.4 ③）**：本格是**复核与报告**，零代码改动。⇒ **无需新能力设计前置**。

---

## 〇、本格是不是「最后一格」——**是的，而且没有历史包袱**

> ⚠️ **一处容易误记的地方**：CSS 系列有「**1.29 是最后一个干活的格、1.20 是收口报告格，但 1.20 恒在最后**」这条特殊安排（用户原话「最后一个任务就是 1.20，不管怎么样反正这个任务必须有」）。
> 🔴 **本轴没有这个包袱**：**1.50 就是队列最后一格，也是执行序最后一格**——它前面没有"排在它之后的干活格"。
> ⇒ 但**「收口报告必须有」这条精神照搬**：**本格的报告是派工时的显式交付物**，不是可选项。

---

## 一、收口判据（**逐条可机械核，照 [00 档 §5.2](00-整理档案.md)**）

| # | 判据 | 怎么核 |
|:--:|:--|:--|
| 1 | **件 1–5 全绿** | 每件有终态读数，且**是跑出来的不是抄的**（每条读数要带可复跑命令） |
| 2 | **静态判据全绿** | 宿主保留名账 ↔ 实况**双向**对账绿 · 18 只插件仓 `npm run verify` 绿 · 壳仓 `npm run check` **EXIT=0** |
| 3 | **每条新判据带 `--self-test` 并接线** | `npm run check` 里**能数出**每把尺子的自测 ＋ **负控真红过**（逐条留读数） |
| 4 | **运行时仲裁有效性实证** | 至少一次「塞入第二条同名注册 ⇒ **日志点名双方** ＋ **先者的条目未被顶替** ＋ 还原后 **sha256 逐字节相同**」——**四件各跑一次**（命令 / 设置键 / 外观族 / 旗子） |
| 5 | **逐条改前 / 改后对账** | 用 [00 档 §二](00-整理档案.md) 的读数 vs 终态读数，**逐行摆平**（含"没变的"也要写，**变了要说清怎么变的**） |
| 6 | **残余边界诚实清单**（§三）**逐条写进报告**，一条不许省 | — |
| 7 | **横切机制零回归**（§二） | 实机逐项跑并记读数 |
| 8 | **残留判据全 0** | 含 18 只插件仓 |

---

## 二、🔴 横切机制零回归（**用户 2026-09-16 点名，必跑**）

| 机制 | 逐项（一项不许省） |
|:--|:--|
| **主题统一调控** | 切主题（含**多配色** colorway 与**混搭**）· 玻璃三参（模糊 / 饱和 / 不透明度，**含 s=0 端点**）· **浮层磨砂地板**（玻璃关仍可读）· 圆角六档 ＋ 滑杆 0/16/32 · 背景（panorama / mask）· 亮暗切换 |
| **字体** | 字号 `app.uiFontScale` **85 / 100 / 125 / 150** 四档 ＋ `window.zoomLevel` **正交**（`devicePixelRatio` 读数）· 字体族（`--font-ui` / `--font-mono`）· **monaco 内容孤岛**（字号不随全局、颜色跟随） |
| **翻译** | 中英切换（`t()` ＋ 语言包）· 插件自带 `contributes.i18n`（`lang-defaults` 插件）· **插件语言包注册后文案正确**（本轴件 5 动过注册路径） |

⚠️ **任一项与今天不一致 ⇒ 本轴不算过**（照 [29 号档 §四.4](../样式命名空间归一化/29-主题系统保护条款.md) 的口径）。

---

## 三、残余边界诚实清单（**报告必带，一条不许省——本格逐条核实并补全**）

> [00 档 §六](00-整理档案.md) 已列 **11 条**骨架（§〇c 修订后的数目——旧文写的"九条"是修订前，**以 11 条为准**）；**本格的责任是**：① 逐条核实是否仍然成立；② 把各件在 1.31–1.40 判「不做」的条目**全部收进来**；③ **补上本轴自己新发现的**。

**必须逐条给状态的骨架**：

| # | 边界 | 本格要补什么 |
|:--:|:--|:--|
| 1 | **机制层未隔离**（靠约定 ＋ 门禁 ＋ 运行时仲裁，非构造上不可能） | 写清与视图 id（**构造性**）的对照 |
| 2 | 🔴 **存量名字不改名**（命令 21 ＋ 设置键 19 ＋ 插件旗子 9 ＋ 主题/配方 id …） | **逐类补上终态读数与"为什么改名更坏"**（用户设置失效 / 可见变化 / `app.theme` 指向他物） |
| 3 | **静态门禁只看得见静态声明**（模板字符串拼名可绕） | 补上**已知形态清单**（各件的实测例子） |
| 4 | **i18n 天然跨仓不可判** | 补上**真共写**读数（1.39 订正后的） |
| 5 | **账需要一次生成且必须防漂移** | 补上**双向对账门禁的实测读数** |
| 6 | **`pluginId` 唯一性依赖硬约束 11** ＋ 宿主伪 pluginId 保护情况 | 补上 1.33/1.34 的**终态处置** |
| 7 | **键盘 chord 静默死键不属本轴**（已立案） | 补上「**仍然挂着，去向**」 |
| 8 | **已设防的四处不被本轴改动** | 补上「**逐条核实本轴零改动**」（git 层面） |
| 9 | **不回答「哪块宿主 UI 该变成插件」** | 补上「**不许把命名干净当成已插件化**」 |

**＋ 本格要新增的（若各件跑出新的）**：
- 各件落地时**实测发现的新边界**（例：某条判据的射程盲区、某个无法机械化的形态）；
- **本轴未覆盖的面**（若有：本轴没查到的花名册或没定的裁决）⇒ **如实列出 ＋ 建议去向**（不许写成"应该没有"）。

---

## 四、要做

### 4.1 逐件跑终态读数（**跑，不是抄**）

照 [1.49 §2.1 的核对表](19-任务-门禁与作者面收口.md) 逐条跑；**每条读数写清：命令 ＋ 输出 ＋ 日期**。

### 4.2 逐条改前 / 改后对账

用 [00 档 §二](00-整理档案.md) 的总表逐行对；**新增行/删除行也要说明**（读数会与派工时的起点不同——**如实记，不许"对不上就改起点"**）。

### 4.3 运行时仲裁四连实证（判据 4）

**四件各一次**，每次都要：塞入 → 读数 → 还原 → **sha256 逐字节相同**。

### 4.4 残余边界收口（§三）

逐条核实 ＋ 补全。

### 4.5 写报告（**本格的主要交付物**）

报告结构（照 [22 号档 §五](../样式命名空间归一化/22-收口总方案-跨方样式污染九件套.md) 的形态）：
1. **一句话结论**（真问题是什么 ＋ 收口到什么程度）
2. **根因复述**（[00 档 §一](00-整理档案.md) 那句话——**每一棒收尾都要能复述**）
3. **件 1–5 终态读数**（逐条带命令）
4. **改前 / 改后总对账表**
5. **横切机制零回归读数**（§二 三项）
6. **残余边界诚实清单**（§三，一条不许省）
7. **原则对照复核**（[00 档 §十](00-整理档案.md) 逐条 —— 含"哪里还不够"）
8. **给用户的大白话**（见 4.6）

### 4.6 🔴 给用户的大白话（**硬交付物**）

按记忆 `plain-language-for-client` 的口径（**用户 = 普通甲方**；**禁「黑话＋(推荐)标签＋事后归咎」**），讲清四件事：

| 要讲的 | 例 |
|:--|:--|
| **查出什么** | 「软件里有 X 本『公共花名册』——谁都能往上写名字，撞了只在小字日志里嘟囔一句，用户看不见、被覆盖的插件作者也看不见」 |
| **哪些是真危险** | 「最危险的是**上下文旗子**：插件能悄悄让宿主菜单项消失/出现，界面一点没变，你根本不会怀疑」 |
| **打算怎么治** | 「两条：① **给每本花名册立一份"宿主保留名单"**（随 SDK 发给插件作者，他们自己就能查）；② **撞了要出声，而且先写的不许被顶掉**」 |
| **还剩什么不治** | 「有几类**故意不改**：**照抄 VS Code 的共享设置键**（如 `files.exclude`）——改了会让你已有设置失效；**i18n 文案键**——技术上看不出谁撞了谁，只能给建议」 |

### 4.7 本格**不做**的（写清，防越界）

- ❌ **不改代码**（发现必须改的 ⇒ **立新格 ＋ 在报告里点名**，**不许顺手改**）。
- ❌ **不重开已裁决的事**（各件的结论照用）。
- ❌ **不动 [29 号档](../样式命名空间归一化/29-主题系统保护条款.md) 的保护对象**。

---

## 五、判据（本格自己的验收）

| 判据 | 要求 |
|:--|:--|
| **读数全跑** | §一 八条判据逐条留读数（**命令 ＋ 输出 ＋ 日期**） |
| **对账摆平** | 改前 / 改后逐行；**对不上要说明，不许改起点** |
| **仲裁四连** | 四件各一次 ＋ 还原 sha256 相同 |
| **零回归读数** | §二 三项逐项实机 |
| **残余边界完整** | 骨架九条 ＋ 各件判"不做"的全部 ＋ 本格新发现 |
| **报告八节齐全** | §4.5 |
| **大白话四问齐全** | §4.6 |
| **零代码改动** | 本格 `git diff` 只含文档 |
| **`npm run check` EXIT=0** | ＋ 残留判据全 0 |
| **E6 清单** | 勾格 ＋ 轮次进度行 ＋ 交接段（**本轴收口标志**） |

---

## 六、禁区

- 🔴 **不许顺手改代码**（发现必须改的 ⇒ **立新格 ＋ 点名**）。
- 🔴 **不许压平残余边界**（报告里写「已根除」而省略边界 = **造假账**）。
- 🔴 **不许把「门禁绿了」当成「问题没了」**（绿必须是**负控真红过**的绿）。
- 🔴 **不许把「黄灯」在报告里写成「已解决」**（i18n 那条尤其）。
- 🔴 **不许重开已裁决的事**；**不许动已设防的四处**。
- 🔴 **不许改起点让对账好看**。
- ⚠️ **上位约束**：零 UI 变化 · 只做加法 · [29 号档](../样式命名空间归一化/29-主题系统保护条款.md) 优先 · 对外宽容。
- ⚠️ 照旧：不跳 `npm run check` · 推前现测代理 · 推完 `ls-remote` 对 SHA · **提交只按路径 `git add <本格文件>`**。

---

## 七、交棒要求

1. **收尾四件套**（交接段顶部追加 · 清单轮次进度行 · 勾格全套 · 队列表整张复制——**本格之后队列为空**）。
2. 🔴 **本轴的「收口复核」标志**：在 [交接.md](交接.md) 顶部段明确写「**本轴全部完成 ＋ 报告已出 ＋ 剩余 = 无（或列出新立的格）**」。
3. 🔴 **判「这条不做」必须写明理由**（不许静默不做）。
4. **下一棒 = 无**（本轴结束）——但**报告里点名的欠账**要各自有归宿（**新格 / 明确不做 ＋ 理由**）。

---

## 八、🔴 进场第一步（命令）

```bash
# 0) 确认自己是哪一棒（本格是本轴最后一棒 —— 复核者，不是执行者）
sed -n '1,40p' "docs/02-Electron架构/E6_插件生态与发布/01-插件独立构建/非样式命名空间归一化/交接.md"

# 1) 拿 1.41 的核对表当底稿（它带每件判据落在哪条腿）
# → 交接段顶部第一段

# 2) 逐条跑终态读数（每条命令都要留输出）——完整清单见本文件 §10.1
npm run audit:nonnaming          # 普查探针（曾在 scratch/，2026-09-17 已搬进 scripts/）
node scripts/audit-plugin-scope.mjs      # 18 仓清账面（期待：全 0）
node scripts/audit-plugin-ownership.mjs  # 🔴 四条腿的容器读数（1.49 新立；期待：红 0 黄 0）
node scripts/gen-host-reserved.mjs --check        # 账四向对账
node scripts/check-reserved-names-doc-sync.mjs    # 账 ↔ 作者面两棵树（两段射程）
npm run check
node -e "const p=require('./package.json');console.log(p.scripts.check)" | tr '&' '\n' | grep -c self-test
for f in scripts/check-*.mjs; do grep -q "self-test\|selfTest" "$f" || echo "无自测: $f"; done
for d in E:/linkdesk-plugins/official/*/; do (cd "$d" && npm run verify >/dev/null 2>&1 && echo "OK   $(basename $d)" || echo "FAIL $(basename $d)"); done

# 3) 横切机制零回归（§二）——实机（隔离 profile ＋ CDP，别动用户已装实例）
#    参照 CSS 系列 1.21 的实机脚本姿势与 29 号档 §四.4 的清单

# 4) 确认本格零代码改动
git status --short
```

---

## 九、与其它格的关系

- **[1.49 门禁与作者面收口](19-任务-门禁与作者面收口.md)（紧接前序）**：本格复核它的产出 ＋ 用它给的核对表。
- **[00-整理档案.md](00-整理档案.md)**：本格的**判据来源**（§5.2 收口判据 · §六 残余边界骨架 · §十 原则对照）。
- **[29-主题系统保护条款](../样式命名空间归一化/29-主题系统保护条款.md)**：§四.4 的回归清单 = 本格 §二 的必跑项。
- **CSS 系列 [19 号档 §第 1.20 轮](../样式命名空间归一化/19-任务-收口批-遗留账与系列收口.md)**：**报告结构的先例**（它管样式轴，本格管非样式轴；两轴各自收口，**互不隶属**）。
- **记忆 `plain-language-for-client`**（大白话口径）· **`ai-relay-one-round-per-session`**（收尾四点交接）。

---

## 十、1.49 交棒补全（[19 档 §2.5](19-任务-门禁与作者面收口.md) 四项 · 2026-09-18 由 1.49 写齐）

> [19 §2.5](19-任务-门禁与作者面收口.md) 点名要 1.49 把本任务书写成正式版：**终态读数清单（逐件）· 残余边界待填项 · 报告结构 · 收口判据逐条的可复跑命令**。四项照做如下。
> 🔴 **本格的复核底稿 = [交接.md](交接.md) 最上一段的 §2.1 核对表**（1.49 已逐格填满，含**四条诚实发现**——尤其「命令腿 1.32 起就是红的，注释写错了」与「插件侧 `set` 点其实有腿看」）。

### 10.1 逐件终态读数清单（**跑，不是抄**——命令 ＋ 1.49 实测的期望读数）

| 件 | 读数项 | 可复跑命令 | 1.49 实测（2026-09-18） |
|:--|:--|:--|:--|
| 件 1 | 命令/协议 id 归属 | `node scripts/audit-plugin-scope.mjs` | 18 仓**需改 0 处**（含命名空间引用面） |
| 件 1 | 同上（四条腿·容器读数） | `node scripts/audit-plugin-ownership.mjs` | **红 0 / 黄 0**（18 仓 × 命令/配置/外观/旗子） |
| 件 1 | 负控（三面各一条） | `node scratch/_149-four-legs-negative.mjs` | 声明 / 运行时 / 协议 ⇒ 各 **EXIT=1 ＋ 报点带引导语** |
| 件 2 | 设置键归属 ＋ 宿主键保护 | 同 `audit-plugin-scope` / `audit-plugin-ownership` | 全 0；弱默认值面写**别人的键**合法（正控绿）、写**宿主键**红 |
| 件 3 | 外观族 id（三空间按栏比） | `node scripts/audit-plugin-scope.mjs` ＋ `npm run audit:nonnaming` §⑩b | 主题族 0 处；配方 ∩ 账 0（`light` **持证照**）· 图标主题 ∩ 账 0 |
| 件 4 | 上下文旗子（含**插件侧 `set` 点**） | `npm run audit:nonnaming` §④/§⑩b ＋ `scratch/_149-four-legs-negative.mjs` | 宿主专用旗子 ∩ 账 **0**；**约定面 4 名 ∩ 账 = 4（合法，1.46 裁决）**；负控 `contextKey.set("myFlag")` ⇒ 红 |
| 件 5 | i18n（**天然黄灯**） | `npm run audit:nonnaming` §⑦ ＋ `npm run check` 里的 `audit-i18n --strict` | 台账读数照旧；⛔ **不许在报告里写成"已解决"** |
| 账 | 生成式账四向对账 | `node scripts/gen-host-reserved.mjs --check`（`--self-test` 同批） | ✅ 四向一致（实况→账 / 账→实况 / SDK 副本逐字节 / 运行时副本逐元素）；家族 10 · 62 名 |
| 账 | 账 ↔ 作者面两棵树（两段射程） | `node scripts/check-reserved-names-doc-sync.mjs`（＋ `--self-test`） | ✅ ①（样式）§12 两表 ②（非样式）§七总表 20 行 / 62 名 / 规则句在；**自测 25 条** |
| 腿接线 | 自测接线计数 | `node -e "const p=require('./package.json');console.log(p.scripts.check)" | tr '&' '\n' | grep -c self-test` | **39** 条 `--self-test` 在 `npm run check` 里 |
| 腿接线 | 谁还没自测 | `for f in scripts/check-*.mjs; do grep -q "self-test\|selfTest" "$f" || echo "无自测: $f"; done` | 只剩 `scripts/check-lsp-smoke.mjs`（**与本轴无关**，非命名空间尺） |
| 插件仓 | 18 仓自检 | `for d in E:/linkdesk-plugins/official/*/; do (cd "$d" && npm run verify >/dev/null 2>&1 && echo "OK $(basename $d)" || echo "FAIL $(basename $d)"); done` | **18/18 OK**（lock 已刷到 SDK **0.1.34**——`npm install` 对旧 lock 是空操作，必须 `npm update`） |
| 壳仓 | 总门禁 | `npm run check` | **EXIT=0 · 173 文件 / 2432 测试**（⚠️ **别只看 vitest 那一段**：本格收尾时 knip 抓了一个 vitest 全绿却让整条门禁红的死导出 ⇒ 报告里的"绿"必须是**整条命令的退出码**，见 交接段 ⑶⑤） |

### 10.2 残余边界待填项

- 🔴 **骨架不是九条，是十一条**：[00 §六](00-整理档案.md) 在 §〇c 修订后有 **11 条**（第 2 / 10 / 11 条带 🔄 修订标记 ⇒ 本格的表按 **11 条**逐条给状态，别照 §三 表头的"九条"数）。
- **1.49 已填好、本格直接引用**：第 5 条（账防漂移 ⇒ **四向对账门禁 ＋ `version` 字段**已接线）· 第 6 条（宿主伪 pluginId ⇒ 1.34 已处置）· 第 11 条（三类全局概念名 ⇒ 作者面 §7.2 已写清"不是欠账"）。
- **本格必须自己补的**：第 1 条（与视图 id 的**构造性**对照）· 第 3 条（**已知形态清单**：模板字符串拼名的实测例子）· 第 4 条（i18n 真共写读数）· 第 8 条（**git 层**核实"已设防四处"零改动）· 第 10 条（"改名收益面向未来"的措辞纪律）。
- **各件判「不做」的全部条目**：收集点 = ① [交接.md](交接.md) 各格段落的「不做」节；② [00 §四](00-整理档案.md)「系列外」三条（E6#110 观察名单 / **E6#112 插件域关键帧引用** / **E6#113 插件侧死类**）；③ **1.49 登记两条**：市场详情页「大小 8.3 MB vs 装后 36 MB」· 键盘 chord 被 `normalizeKey` 打乱 ⇒ 静默死键。

### 10.3 报告结构（照 §4.5 八节，逐节给数据来源）

| 节 | 数据来源（**现成**，别现编） |
|:--:|:--|
| 1 一句话结论 | [00 §一](00-整理档案.md)「一句话根因」＋ §5.1 四层闭合表 |
| 2 根因复述 | [00 §一.层 1-3](00-整理档案.md)（扁平全局表 / 契约面按"每插件一份"设计 / 归属只有两种表达且都不设防） |
| 3 件 1–5 终态读数 | **本文件 §10.1 表**（逐条带命令） |
| 4 改前/改后总对账 | [00 §二.0](00-整理档案.md)（改前）↔ 本文件 §10.1（改后）＋ [00 §〇c.4](00-整理档案.md) 清账面 |
| 5 横切零回归 | §二 三项实机读数（隔离 profile ＋ CDP，**别动用户已装实例**） |
| 6 残余边界 | §三 ＋ §10.2（**11 条 ＋ 各格"不做" ＋ 本格新发现**） |
| 7 原则对照复核 | [00 §十](00-整理档案.md) 逐条 ＋ §七 禁区逐条 |
| 8 大白话 | §4.6 四问（口径 = 记忆 `plain-language-for-client`） |

### 10.4 收口判据 ↔ 可复跑命令（[00 §5.2](00-整理档案.md) 九条）

| # | 判据 | 命令 |
|:--:|:--|:--|
| 1 | 件 1–5 全绿 | §10.1 表逐行 |
| 2 | 🔴 零例外：18 仓需改 0 | `node scripts/audit-plugin-scope.mjs`（全 0）＋ `node scripts/audit-plugin-ownership.mjs`（红 0 黄 0） |
| 3 | 账 ↔ 实况双向 ＋ 18 仓 verify ＋ `npm run check` EXIT=0 | `node scripts/gen-host-reserved.mjs --check` ＋ §10.1 的 18 仓循环 ＋ `npm run check` |
| 4 | 每把尺子 `--self-test` 接线 ＋ **负控真红过** | 计数命令（§10.1）＋ 逐条负控留读数（**1.49 的 `scratch/_149-four-legs-negative.mjs` 与 `scratch/prove-149-host-doc-sync.mjs` 可直接复用**） |
| 5 | 运行时仲裁有效性（四件各一次） | 见 §4.3（塞入 → 点名双方 ＋ 先者未被顶替 → 还原 sha256 相同） |
| 6 | 迁移实证（逐项操作） | 见 §4.2/§二 —— 设置项仍能切换且值沿用 · 改 id 的命令快捷键仍触发 · 改 id 的主题仍跟随 |
| 7 | 逐条改前/改后对账 | [00 §二](00-整理档案.md) ＋ §〇c.4 ↔ §10.1 |
| 8 | 残余边界诚实清单 | §三 ＋ §10.2（**11 条一条不许省**） |
| 9 | `npm run check` EXIT=0 ＋ 残留判据全 0 | §10.1 末两行 |

### 10.5 🔴 交棒给 1.50 的一件事（**唯一未收尾项**）

**壳 `v0.2.11` 的 tag ＋ GitHub Release 没推**——红线 `push-wait-for-user`（推送等用户当次点头 ＋ **带代理**；撤版只能用户本人）。
拿到点头后的形状：`git tag v0.2.11 && git push origin electron --follow-tags`（走代理）⇒ 推完 `ls-remote` 对 SHA ⇒ CI 建 Release。
⚠️ **18 只插件仓的 lock 提交（各一条 `chore:`）也全部未推**——同理等用户点头。
