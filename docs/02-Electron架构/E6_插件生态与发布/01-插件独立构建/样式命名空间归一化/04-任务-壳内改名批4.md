# 任务 `#109g-4`：壳内改名·批 4（markdown-view）——第 1.8 轮

> **一句话**：`.mdv` → `.ldk-mdv`（单文件 38 处，全是 `.mdv` 及其后代选择器），并从保留名清单摘掉 `mdv`。
> **前置**：第 1.7 轮（批 3）已 ✅。
> **规模**：约 41 处 · 3 个文件（**本系列单文件最集中**的一批）。
> **提交轴**：壳（**不 bump 版本**——见 [01 §四](01-任务-壳内改名批1.md)）。
> **通用禁区 / 提交与收尾**：照 [01 §四、§五](01-任务-壳内改名批1.md)。

---

## 一、面清单（逐条实测）

| 文件 | 处数 | 现状 → 改成 |
|:--|--:|:--|
| `src/components/shared/markdown-view/MarkdownView.css` | **38 处选择器** | 全部以 `.mdv` 开头（含后代选择器：`.mdv h1` / `.mdv h2` … `.mdv th` / `.mdv ul` 等）→ 主体改 `.ldk-mdv`，**后代标签选择器不动**（`.ldk-mdv h1`） |
| `src/components/shared/markdown-view/MarkdownView.tsx:173` | 1（三元拼接） | `` className={className ? `mdv ${className}` : "mdv"} `` → `ldk-mdv` / `` `ldk-mdv ${className}` `` |
| `src/components/shared/markdown-view/MarkdownView.test.tsx:37,39` | 2 | it 标题「容器类名 = mdv + className 合并」→ `ldk-mdv`；`toBe("mdv extra")` → `toBe("ldk-mdv extra")` |

**保留名清单**：`packages/plugin-sdk/schemas/reserved-class-names.json` 的 `classes.shared` 组里**摘掉 `mdv` 一项**。

> 🔴 **本批做完，`classes.shared` 组应当为空**（8 项全摘完）。**空组是正确的终态**——不要为了「表不为空」而保留任何条目；判据⑤ 反向核对的语义是「登记项必须与实况一致」，实况里已经没有共享组件裸定义了。

---

## 二、步骤

1. 改 `MarkdownView.css`（38 处，建议 `perl -pi -e` 或脚本一把过，然后**逐段 diff 复核**——单文件集中改最容易手滑）。
2. 改 TSX + 测试。
3. 摘 JSON 的 `mdv` 项 → `npm run check` EXIT=0。

**改名规则**：`\.mdv(?![a-zA-Z0-9_])` → `.ldk-mdv`。

---

## 三、判据（做完逐条给读数）

- [ ] `npm run check` EXIT=0（贴末尾三行）
- [ ] `grep -rnE "\.mdv([^a-zA-Z0-9_-]|$)" src --include=*.css --include=*.tsx --include=*.ts | grep -v "\.ldk-mdv"` 只剩注释（贴输出）
- [ ] `npx vitest run src/components/shared/markdown-view` 全绿
- [ ] **`classes.shared` 为空数组**（贴读取命令输出）——这是本系列「件 1 壳侧改名」收尾的标志
- [ ] `git diff --stat`

---

## 四、禁区（本批专属）

1. **不许动插件仓**——`marketplace/src/styles/detail/detail-changelog.css:84` 的 `.mpd-chg .mdv` 属于**第 1.11 轮**；本批改完之后插件侧**暂时失配是预期的**（§1.3 机制：失配只在插件升级 ui 时显形，且它自带旧 ui CSS）。
2. **不许动 MarkdownView 的渲染逻辑**（react-markdown / rehype-sanitize 相关一行不碰）。
3. **不许顺手改视觉**（硬约束 16）。
