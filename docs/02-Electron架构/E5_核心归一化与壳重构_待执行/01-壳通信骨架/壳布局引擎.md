# 壳布局引擎——dock 与 float

> 2026-08-02。**E5 第 1 层第 3 轮。** 壳内四个区域低耦合之后，布局层独立——加新区域只改布局配置。
> **E5 实现 docked 模式。v1.4 实现 floating 模式——数据结构已留口子，LayoutEngine 零改动。**
> 执行清单任务：E5#9

---

## 一、前因——为什么需要这个

### 1.1 现在的布局——CSS flex 写死

```tsx
// App.tsx（当前）
<div className="app-layout">
  <IconBar />           ← 固定 42px，永远在最左
  <Sidebar />           ← 固定 280px，永远是左边第二个
  <MainContent />       ← flex: 1，永远是中间
  <StatusBar />         ← 固定 24px，永远是底部
</div>
```

**换布局（侧栏换右边）= 改 CSS flex + 改 IconBar isActive + 改 SidePanel 动画方向 + 改 MainContent 分屏基准 + 改 StatusBar 对齐。** 改 6 个文件——因为 CSS 定义了谁在谁左边，而组件代码依赖"我在左边"的假设。

### 1.2 目标

```typescript
// 换布局 = 改一行配置。四个组件代码一行不改。
layoutEngine.setLayout([
  { zone: "iconbar",    mode: "docked", dock: { edge: "left",   width: 42 } },
  { zone: "main",       mode: "docked", dock: { edge: "center", flex: 1 } },
  { zone: "sidebar",    mode: "docked", dock: { edge: "right",  width: 280 } },
  { zone: "statusbar",  mode: "docked", dock: { edge: "bottom", height: 24 } },
]);
```

**布局引擎是一层纯计算——输入 zone 配置，输出每个 zone 的 bounds（x/y/width/height）。组件只接收 bounds 渲染——不关心自己在哪里、是 dock 还是 float。**

### 1.3 对标——Visual Studio 工具窗口系统

VS 的工具窗口是可 dock 可 float 的经典实现：

```
拖拽到边缘 → 菱形引导图标 → 5 个 dock 目标：
  左/右/顶/底 → 贴边 dock
  中间 → tab-dock（叠成标签页）

拖到窗口外 → floating

右键 title → Floating / Dock / Dock as Tabbed Document / Auto Hide / Hide
```

**LinkDesk 的 IconBar / SidePanel / MainContent / StatusBar 对标 VS 的工具窗口。** 布局引擎是它们共享的 dock 系统。

| Visual Studio | LinkDesk |
|---------------|----------|
| Tool Window (`IVsWindowFrame`) | Zone（`ZoneConfig`） |
| Dock target（左/右/顶/底/中间） | `dock.edge`（left/right/center/bottom） |
| Floating window | `mode: "floating"` + `float` 坐标 |
| Diamond guide UI | 吸附动画 + ShellEvents（v1.4） |
| Auto Hide | `visible: false` + hover 呼出（v1.6） |

---

## 二、设计方案

### 2.1 核心——ZoneConfig（口子开在数据结构里）

```typescript
// src/core/LayoutEngine.ts

/**
 * 壳区域配置。
 * E5 只实现 mode === "docked"。
 * v1.4 实现 mode === "floating"——不改这个接口。
 */
export interface ZoneConfig {
  /** 区域唯一标识 */
  zone: string;

  /** 当前模式——E5 总是 "docked"，v1.4 加 "floating" */
  mode: "docked" | "floating";

  // ── Docked 属性（mode === "docked" 时生效，E5 实现）──

  dock?: {
    /** 贴哪条边 */
    edge: "left" | "right" | "center" | "bottom";
    /** 固定宽度（left/right zone） */
    width?: number;
    /** 固定高度（bottom zone） */
    height?: number;
    /** 弹性比例（center zone，默认 1） */
    flex?: number;
    /** 最小/最大尺寸 */
    minWidth?: number;
    maxWidth?: number;
    minHeight?: number;
    maxHeight?: number;
    /** 是否可拖拽 resize */
    resizable?: boolean;
    /** 同 edge 多个 zone 时的排列顺序——越小越靠近窗口边缘 */
    order?: number;
  };

  // ── Floating 属性（mode === "floating" 时生效，v1.4 实现）──

  float?: {
    x: number;
    y: number;
    width: number;
    height: number;
    /** z-index——floating zone 之间可层叠 */
    zIndex?: number;
    /** 最小尺寸——float 时重置尺寸的底线 */
    minWidth?: number;
    minHeight?: number;
  };

  // ── 控制（E5 声明，v1.4 消费）──

  /** 是否可以拖出 dock——侧栏=true，图标栏=false，主区=false */
  undockable?: boolean;
  /** 显示/隐藏 */
  visible?: boolean;
}

/** 区域边界——布局引擎输出给组件。docked 和 floating 都用同一个。 */
export interface ZoneBounds {
  x: number;
  y: number;
  width: number;
  height: number;
}
```

### 2.2 LayoutEngine（E5 实现——docked 模式）

```typescript
export class LayoutEngine {
  private _zones: ZoneConfig[] = [];
  private _bounds = new Map<string, ZoneBounds>();
  private _containerWidth = 0;
  private _containerHeight = 0;
  private _onDidChangeLayout = new Emitter<void>();
  readonly onDidChangeLayout: Event<void> = this._onDidChangeLayout.event;

  constructor() {
    // E5 默认布局——4 个 docked zone
    this._zones = [
      { zone: "iconbar",   mode: "docked",  dock: { edge: "left",   width: 42,  minWidth: 42,  maxWidth: 42,  undockable: false }},
      { zone: "sidebar",   mode: "docked",  dock: { edge: "left",   width: 280, minWidth: 170, maxWidth: 600, resizable: true, undockable: true }},
      { zone: "main",      mode: "docked",  dock: { edge: "center", flex: 1,  undockable: false }},
      { zone: "statusbar", mode: "docked",  dock: { edge: "bottom", height: 24, minHeight: 24, maxHeight: 24, undockable: false }},
    ];
  }

  /** 设置容器尺寸（窗口 resize 时调用） */
  setContainerSize(width: number, height: number): void {
    this._containerWidth = width;
    this._containerHeight = height;
    this._recalculate();
  }

  /** 设置布局 */
  setLayout(zones: ZoneConfig[]): void {
    this._zones = zones;
    this._recalculate();
  }

  /** 添加一个 zone（v1.4 浮窗 dock 回来时用） */
  addZone(zone: ZoneConfig): void {
    this._zones.push(zone);
    this._recalculate();
  }

  /** 移除一个 zone（v1.4 undock 时用） */
  removeZone(zoneId: string): void {
    this._zones = this._zones.filter(z => z.zone !== zoneId);
    this._bounds.delete(zoneId);
    this._onDidChangeLayout.fire();
  }

  /** 移动 zone 的 dock 边（侧栏从左换到右） */
  dockTo(zoneId: string, edge: ZoneConfig["dock"]["edge"]): void {
    const z = this._zones.find(z => z.zone === zoneId);
    if (!z || !z.dock) return;
    z.dock.edge = edge;
    this._recalculate();
  }

  /** 调整 zone 尺寸 */
  resizeZone(zoneId: string, newWidth: number): void {
    const z = this._zones.find(z => z.zone === zoneId);
    if (!z || !z.dock) return;
    z.dock.width = Math.max(z.dock.minWidth ?? 0, Math.min(z.dock.maxWidth ?? Infinity, Math.round(newWidth)));
    this._recalculate();
  }

  /** 获取某个 zone 的当前 bounds——不管 docked 还是 floating */
  getBounds(zoneId: string): ZoneBounds | undefined {
    // 先查浮动坐标
    const zone = this._zones.find(z => z.zone === zoneId);
    if (zone?.mode === "floating" && zone.float) {
      return { x: zone.float.x, y: zone.float.y, width: zone.float.width, height: zone.float.height };
    }
    // 查 dock 坐标
    return this._bounds.get(zoneId);
  }

  /** 获取全部 zone 配置 */
  getAllZones(): readonly ZoneConfig[] { return this._zones; }

  /** 获取某个 zone 配置 */
  getZone(zoneId: string): ZoneConfig | undefined { return this._zones.find(z => z.zone === zoneId); }

  /** E5 核心计算——只有 docked zone */
  private _recalculate(): void {
    const W = this._containerWidth;
    const H = this._containerHeight;
    if (W === 0 || H === 0) return;

    // 🔥 E5 只处理 mode === "docked" 的 zone
    const docked = this._zones.filter(z => z.mode === "docked" && z.dock);
    // floating zone 的坐标由 float 字段直接返回——不经过 _recalculate

    const bottomZones = docked.filter(z => z.dock!.edge === "bottom").sort((a, b) => (a.dock!.order ?? 0) - (b.dock!.order ?? 0));
    const leftZones   = docked.filter(z => z.dock!.edge === "left").sort((a, b) => (a.dock!.order ?? 0) - (b.dock!.order ?? 0));
    const rightZones  = docked.filter(z => z.dock!.edge === "right").sort((a, b) => (a.dock!.order ?? 0) - (b.dock!.order ?? 0));
    const centerZones = docked.filter(z => z.dock!.edge === "center");

    const bottomHeight = bottomZones.reduce((sum, z) => sum + (z.dock!.height ?? 0), 0);
    const contentHeight = H - bottomHeight;

    const leftWidth = leftZones.reduce((sum, z) => sum + (z.dock!.width ?? 0), 0);
    const rightWidth = rightZones.reduce((sum, z) => sum + (z.dock!.width ?? 0), 0);
    const centerWidth = Math.max(0, W - leftWidth - rightWidth);

    // Left zones——从左向右堆叠，order 小的靠左
    let leftX = 0;
    for (const z of leftZones) {
      const w = z.dock!.width!;
      this._bounds.set(z.zone, { x: Math.round(leftX), y: 0, width: w, height: contentHeight });
      leftX += w;
    }

    // Right zones——从右向左堆叠，order 小的靠右
    let rightX = W;
    for (const z of rightZones.reverse()) {
      const w = z.dock!.width!;
      rightX -= w;
      this._bounds.set(z.zone, { x: Math.round(rightX), y: 0, width: w, height: contentHeight });
    }

    // Center zones——填满剩余空间
    for (const z of centerZones) {
      this._bounds.set(z.zone, { x: Math.round(leftWidth), y: 0, width: centerWidth, height: contentHeight });
    }

    // Bottom zones——底部从左向右
    let bottomX = 0;
    for (const z of bottomZones) {
      const h = z.dock!.height!;
      this._bounds.set(z.zone, { x: Math.round(bottomX), y: contentHeight, width: W, height: h });
      bottomX += (z.dock!.width ?? W);
    }

    this._onDidChangeLayout.fire();
  }
}

import { Emitter, Event } from "./CoreEvents";
export const layoutEngine = new LayoutEngine();
```

### 2.3 v1.4 做浮窗时——LayoutEngine 零改动

```
E5 数据结构里:
  ZoneConfig.mode = "docked" | "floating"  ← 字段已在
  ZoneConfig.float = { x, y, width, height, zIndex }  ← 字段已在
  ZoneConfig.undockable = boolean           ← 字段已在

v1.4 FloatingManager（新建 ~100 行）:
  undock("sidebar"):   → zone.mode = "floating" → zone.float = 当前鼠标坐标
  dock("sidebar", "left"): → zone.mode = "docked" → zone.dock.edge = "left"
  
LayoutEngine 行为:
  - _recalculate() 已经跳过 mode === "floating" 的 zone——不改任何代码
  - getBounds() 已经优先取 float 坐标——不改任何代码
  - addZone() / removeZone() 已经是 public 方法——v1.4 直接用

Zone 组件行为:
  - 仍然只接收 bounds——不管是 dock 坐标还是 float 坐标，bounds 就是 bounds
```

**和 HAL_Delay 一样：** 今天 SysTick，明天 DWT——API shape 不变，调用方无感。

---

## 三、实现步骤（E5）

### E5#9a LayoutEngine 类（~100 行）

**文件：** 新建 `src/core/LayoutEngine.ts`

**内容：** `ZoneConfig` / `ZoneBounds` / `LayoutEngine` ——含 `mode` / `dock` / `float` 全部字段，但 E5 只消费 `mode: "docked"`。

### E5#9b 坐标计算（~40 行）

**内容：** `_recalculate()` 完整实现——left/right/center/bottom 四个 edge 的堆叠计算。`order` 控制同 edge 多 zone 的排列顺序。所有坐标 `Math.round()`。

**边界条件：**
- 容器尺寸为 0 → 不计算
- center width 为负 → clamp 到 0
- floating zone → `_recalculate` 跳过

### E5#9c 默认布局（~10 行）

```typescript
constructor() {
  this._zones = [
    { zone: "iconbar",   mode: "docked", dock: { edge: "left",   width: 42,  minWidth: 42,  maxWidth: 42,  undockable: false }},
    { zone: "sidebar",   mode: "docked", dock: { edge: "left",   width: 280, minWidth: 170, maxWidth: 600, resizable: true,  undockable: true }},
    { zone: "main",      mode: "docked", dock: { edge: "center", flex: 1,                                      undockable: false }},
    { zone: "statusbar", mode: "docked", dock: { edge: "bottom", height: 24, minHeight: 24, maxHeight: 24,      undockable: false }},
  ];
}
```

### E5#9d App.tsx 接入（~15 行）

**内容：** 用 `layoutEngine.getBounds(zoneId)` 设置四个区域的位置——替代硬编码 CSS flex。

```tsx
const bounds = layoutEngine.getBounds("sidebar");
// SidePanel 收到 { x: 42, y: 0, width: 280, height: 1056 }
// 不关心这是 docked 的坐标还是 floating 的坐标——bounds 就是 bounds
```

---

## 🔴 预测 Bug

### Bug E5-9a 🔴🔴 布局坐标错位——区域重叠或空白

**历史：** E3a #29——bounds 计算错 → WebView 和 React 重叠。

**🔥 防线——`_recalculate` 末尾验证总宽度：**
```typescript
const docked = this._zones.filter(z => z.mode === "docked" && z.dock);
const totalW = docked.reduce((s, z) => s + (z.dock?.width ?? 0), 0) + centerWidth;
if (Math.round(totalW) !== W) console.warn(`[LayoutEngine] 宽度不匹配: ${totalW} vs ${W}`);
```

### Bug E5-9b 🔴 窗口 resize → 不重算 → 区域溢出

**🔥 防线——App.tsx 订阅 resize + useEffect cleanup：**
```typescript
useEffect(() => {
  const handleResize = () => layoutEngine.setContainerSize(window.innerWidth, window.innerHeight);
  window.addEventListener("resize", handleResize);
  handleResize();
  return () => window.removeEventListener("resize", handleResize);
}, []);
```

### Bug E5-9c 🔴 position:fixed + WebContentsView 冲突

**E3a 教训：** WebContentsView 由 Electron 进程管理——不受 CSS `position:fixed` 控制。

**🔥 防线：** LayoutEngine 只管 React 组件的 `style` prop。MainContent 内部的 WebContentsView 仍通过 `WindowManager.setBounds()` 手动设坐标——这部分逻辑在 MainContent 的 WebView sync effect 中，不受 LayoutEngine 影响。

---

## 四、涉及文件

| 文件 | 改动 | 行数 |
|------|------|:--:|
| 新 `src/core/LayoutEngine.ts` | ZoneConfig / ZoneBounds / LayoutEngine 类 + 单例 | ~150 |
| `src/App.tsx` | 用 layoutEngine.getBounds() 替代 CSS flex | ~15 |
| `src/App.css` | 删 `.app-layout` flex 规则 | −20 |
| 四个 zone 组件 | 接收 bounds style prop | 0 |

---

## 五、完工标准

- [ ] `LayoutEngine` 类完整——`setLayout` / `addZone` / `removeZone` / `dockTo` / `resizeZone` / `getBounds`
- [ ] `ZoneConfig` 含 `mode` / `dock` / `float` / `undockable`——数据结构 v1.4 ready
- [ ] 默认布局和 E5 执行前视觉完全一致
- [ ] `dockTo("sidebar", "right")` → 侧栏移到右边——四个组件代码零改动
- [ ] 窗口 resize → bounds 自动更新
- [ ] `_recalculate` 跳过 floating zone——v1.4 不需要改 LayoutEngine
- [ ] `npm run check` 零错误
- [ ] vitest `LayoutEngine.test.ts`

---

> **← E5 索引：** `../00-README.md`
> **← 执行清单：** `../05-执行清单.md` E5#9
> **← 前置：** `ShellEvents类型系统.md`（E5#1–#2）
