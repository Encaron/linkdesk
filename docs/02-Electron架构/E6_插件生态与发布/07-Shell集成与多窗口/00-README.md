# 07 — Shell 集成与多窗口（第 5.3 轮设计档案）

> 本目录 = 执行清单**第 5.3 轮**（E6#45-#47）的设计档案。清单是指针集，方案细节全在这里。
> 设计定稿：2026-09-13（用户逐项拍板，见 §二）。**设计定稿 ≠ 实现授权**——开工写生产代码前按 §四执行序逐格推进，每格做完 `npm run check` 全绿 + 提交再下一格。

## 一、文档地图

| 文档 | 覆盖任务 | 内容 |
|------|---------|------|
| [01-多窗口架构.md](01-多窗口架构.md) | E6#47a-f | 单进程多 BrowserWindow、每窗独立 workspace、冷启动恢复最后活跃窗、关窗清理 |
| [02-命令行与文件intake.md](02-命令行与文件intake.md) | E6#46a-c | argv / open-file / second-instance 三条 intake 源 → 文件/文件夹路由 |
| [03-安装器勾选页与注册表.md](03-安装器勾选页与注册表.md) | E6#45a-f | NSIS 附加任务勾选页、右键三键、文件关联手写注册、PATH、卸载清理、软件内开关 |

## 二、拍板记录（2026-09-13，用户逐项确认）

| # | 决策 | 内容 |
|:--:|------|------|
| D1 | 多窗口技术路线 | **一个 Electron 进程 + 多 BrowserWindow**（非多 app 实例）——用户拍板在先（清单 #47 原文） |
| D2 | 安装器勾选页 | **照抄 VS Code**「选择附加任务」五项，默认勾选状态同 VS Code：桌面快捷方式(勾) / 文件右键菜单(不勾) / 文件夹右键菜单(不勾) / 文件类型编辑器注册(勾) / 添加到 PATH(勾) |
| D3 | 文件关联注册路线 | **弃用 electron-builder 自带 `fileAssociations`**（它无条件注册、接不上勾选框），改在 `installer.nsh` 手写注册表、受勾选框控制 |
| D4 | PATH 勾选项 | **要**，默认勾选——终端里可直接敲 `linkdesk 文件.txt` / `linkdesk D:\工程`，与 #46 命令行 intake 成套 |
| D5 | 软件内开关 | **做**——设置里可勾选/取消右键菜单与文件关联，主进程直写 HKCU 注册表，立即生效不用重装（比 VS Code 强的点，它只能重跑安装器） |
| D6 | Win11 新式右键菜单（第一层） | **本轮不做，立案后补**——需稀疏包 shell 扩展（IExplorerCommand），VS Code 2025 年中才做上去且接连出回归；本轮只做「显示更多选项」第二层（传统注册表路线，带图标，VS Code 同层） |
| D7 | 冷启动恢复策略 | 无参数启动（双击桌面图标）→ **只恢复最后活跃窗口的工程**（对标 VS Code `window.restoreWindows: "one"` 档）；`all` 档设置以后再说 |

## 三、与既有账的联动（做完即销）

- **A8**（实机验收总表，#57.9f）：多窗口读数——物理上无法测，等本轮。→ 批 C 现场。
- **A10 多窗半**（#57.11）：漂移窗/脱出窗也要带 TitleBar 更新按钮。→ 批 C 现场。
- **#57.16c**（多窗口一格）：更新机制多窗口验收。→ 批 C 现场。
- 🔴 **销账 ≠ 销格**（#57.16f 纪律）：只销实机那半读数，别顺手动主格复选框。

## 四、执行序（批 A → 批 B → 批 C）

```
批 A 纯代码（electron:dev 可验）     批 B 安装器脚本（写完不打包）      批 C 重打包 + 实机（先问用户）
─────────────────────────        ─────────────────────────        ─────────────────────────
A1 #46a intake 三源骨架            B1 #45a 文件关联手写注册           C1 重打包（先问用户）+ NSIS 真安装
A2 #46b 文件/文件夹路由            B2 #45d 勾选页五项 + 静默口径      C2 实机三格 #45c / #46c / #47d
A3 #47a second-instance 开新窗     B3 #45b 右键三键 + 图标            C3 顺手销 A8 / A10 多窗半 / #57.16c
A4 #47e workspace 每窗一份         B4 #45e 卸载反向清理
A5 #47b 多窗创建与清理             B5 #45f 软件内设置开关（UI 先调设计 skill）
A6 #47c 每窗隔离验证
A7 #47f 冷启动恢复最后活跃窗
```

衔接关系：A1/A3 同动 `main.ts` 启动序（intake 源先于路由消费方）；A4 是 A5 的地基（先有每窗状态才有每窗工作区）；B 系全部落 `build/installer.nsh` 单文件（+ `electron-builder.yml` 仅注释说明弃用 `fileAssociations`）；批 C 一次性验证批 A+B 全部实机判据。

## 五、立案（本轮不动工）

- **Win11 新式右键菜单第一层**（D6）：稀疏 MSIX 包 + IExplorerCommand shell 扩展。VS Code 1.102（2025-06）才落地，且 1.103 起接连出「菜单项消失 / 点击无反应」回归（microsoft/vscode#260419、#334716）。等第二层路线（注册表）在本轮跑稳后再评估是否立项。

## 六、开工前置（清单段头红字的展开）

1. 读 [05-文档与发布/02-发布流水线.md](../05-文档与发布/02-发布流水线.md) §1.4（版本轴纪律）。
2. `build/installer.nsh` 已存在（#42d 降级守卫载体）——批 B 所有改动**追加**在同一文件，不推倒重写；`customInit` 宏保持不动。
3. **每次动已装版（D:\01link\LinkDesk 的 0.1.60）或重打包前先问用户**。
4. 实机三格（#45c/#46c/#47d）必须真 NSIS 安装版，不许拿 `electron:dev` 冒充（清单执行序表红字）。
