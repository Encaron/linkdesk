# 任务 `#109g-3`：壳内改名·批 3（combobox 家族 + string-list-editor）——第 1.7 轮

> **一句话**：combobox 与 string-list-editor 两组类名加 `ldk-` 前缀（combobox 借用的 selectbox 族名同笔跟上，**只加前缀不拆借用**），并从保留名清单摘掉 `combobox` 与 `sle`。
> **前置**：第 1.6 轮（批 2）已 ✅（借用的 `ldk-selectbox-*` 必须已在）。
> **规模**：约 53 处 · 6 个文件（本系列最大的一批，仍逐文件可控）。
> **提交轴**：壳（**不 bump 版本**——见 [01 §四](01-任务-壳内改名批1.md)）。
> **通用禁区 / 提交与收尾**：照 [01 §四、§五](01-任务-壳内改名批1.md)。

---

## 一、combobox（约 22 处）

| 文件 | 面上位置 | 现状 → 改成 |
|:--|:--|:--|
| `src/components/shared/combobox/Combobox.css` | 11 处选择器：`.combobox` · `.combobox-field` · `.combobox-input` · **借用的** `.selectbox-disabled` · `.selectbox-dropdown` · `.selectbox-open` | 自有 3 个 → `.ldk-combobox*`；**借用的 3 处 → `.ldk-selectbox-*`**（跟上批 2 已改的名字） |
| `src/components/shared/combobox/Combobox.tsx` | 21（注释里的族名列表）· 125（根：`combobox` + `selectbox-open` + `selectbox-disabled`）· 128（`combobox-field`）· 132（`combobox-input`）· 143（`selectbox-arrow` + `selectbox-arrow-up`）· 154（`selectbox-empty`）· 159（`selectbox-item` + `-item-focus` + `-item-selected`） | 自有 → `ldk-combobox*`；借用 → `ldk-selectbox-*` |
| `src/components/shared/combobox/Combobox.test.tsx` | 120（it 标题）· 125（断言：`querySelector(".combobox")` + `toContain("selectbox-disabled")`） | 同步 |

> 🔴 **借用关系保持**：combobox 的根元素**仍然**同时带 `ldk-combobox` 与 `ldk-selectbox-open` / `ldk-selectbox-disabled`；下拉面板仍渲染 `ldk-selectbox-*` 骨架。这是 `Combobox.css` 文件头写明的既有决策（「下拉面板视觉复用 SelectBox……一个视觉语言一处写（jscpd 0 克隆门禁）」）。**本批只加前缀。**

## 二、string-list-editor（约 31 处）

| 文件 | 面上位置 | 现状 → 改成 |
|:--|:--|:--|
| `src/components/shared/string-list-editor/StringListEditor.css` | 17 处选择器：`sle` · `sle-add-btn` · `sle-add-row` · `sle-badge` · `sle-del` · `sle-glyph` · `sle-input` · `sle-lock` · `sle-msg` · `sle-msg--error` · `sle-msg--hint` · `sle-row` · `sle-url`（含 BEM 变体 `--` 形式） | 全部 → `.ldk-sle*`（**变体照加**：`.sle-msg--error` → `.ldk-sle-msg--error`） |
| `src/components/shared/string-list-editor/StringListEditor.tsx` | 99（根 `sle`）· 102 · 103 · 104 · 105 · 106 · 112 · 113 · 114 · 116 · 127 · 129 · 142 · 147 · 148 | 全部 → `ldk-sle*` |

## 三、保留名清单（**同笔必做**）

`packages/plugin-sdk/schemas/reserved-class-names.json` 的 `classes.shared` 组里**摘掉 `combobox` 与 `sle` 两项**。

---

## 四、步骤

1. combobox 三个文件 → `npx vitest run src/components/shared/combobox` 绿。
2. string-list-editor 两个文件 → 相关测试绿（先 grep 一下有没有别的测试引用 `sle*`）。
3. 摘 JSON 两项 → `npm run check` EXIT=0。

**改名规则**：`\.combobox(?![a-zA-Z0-9_])` → `.ldk-combobox`；`\.sle(?![a-zA-Z0-9_])` → `.ldk-sle`。
⚠️ `\.sle` 的正则**会不会误伤**？实测语料里没有「以 sle 开头但不是本族」的类名——但仍请改完 grep 一遍（见 §五判据）。

---

## 五、判据（做完逐条给读数）

- [ ] `npm run check` EXIT=0（贴末尾三行）
- [ ] `grep -rnE "\.(combobox|sle)" src --include=*.css --include=*.tsx --include=*.ts | grep -v "\.ldk-"` 只剩注释（贴输出）
- [ ] `grep -rn "selectbox-" src/components/shared/combobox/ | grep -v "ldk-selectbox"` 只剩注释（**证明借用跟上批 2 了**）
- [ ] `npx vitest run src/components/shared/combobox src/components/shared/string-list-editor` 全绿
- [ ] JSON 里 `classes.shared` 不含 `combobox` / `sle`
- [ ] `git diff --stat`

---

## 六、禁区（本批专属）

1. **不许拆 combobox ↔ selectbox 的族名借用**（[00 §六·3](00-整理档案.md)）——只加前缀。
2. **不许动 `mdv`**（批 4）。
3. **不许动插件仓**（第 1.11 轮）。
4. **不许顺手改视觉**（硬约束 16）。
