# E4 执行清单——文件树与 Monaco 编辑器（39 任务，~2,060 行）

> 机械执行。做完一步打勾一步。六批顺序执行，同批内可并行。
> 详细设计见 E4a-E4f 各子文档。
> 🔥 **UI 布局规格：** `00.5-UI布局规格.md`——写 CSS 前必读。token 表 + 状态矩阵 + 动画规格。
> 🎨 **交互预览：** [filetree-preview.html](filetree-preview.html)——浏览器打开查看文件树 + 装饰器 badge + twistie 动画效果。
> **零核心架构改动。** 全部任务 = `plugin.json` + React 组件。⚠️ **少量核心新增**：`FileService.copy()`（弥补缺口）、`EncodingService`（多消费方准入）、`FileDecorationRegistry`（E3f #59b 前置）。
> **进度唯一真相源。** 所有 checkbox 集中于此——不改 00-README、不改设计文档、不改 CLAUDE.md。
>
> ## 第 0 批：E4 前置依赖（5 任务，~170 行）——E4 起步前必须就绪
>
> - [x] **#83** `FileService.copy(src, dest)` —— 目前未导出。E4b 拖放/剪贴板依赖。~5 行
> - [x] **#84** `FileDecorationRegistry` —— E3f #59b 目前未建。E4c #106 依赖。~40 行
> - [x] **#85** `EncodingService` —— 放入 `src/core/encoding/`。file-tree 搜索 + editor 共享。~70 行
> - [x] **#86** 🔥 `plugins/builtin/` + `plugins/user/` 目录分离（7 子任务，~87 行）——设计见 `00.6`，执行手册见 `00.7`
>   - [x] **#86a** 物理目录移动——`git mv` 9 个插件（6→builtin, 3→user）。前提：关闭 electron:dev
>   - [x] **#86b1** 6 个工厂插件的 `plugin.json` 各加 `"distribution": "builtin"`
>   - [x] **#86b2** `plugin.schema.json` 双份同步（`public/schemas/` + `docs/03-插件制造/`）
>   - [x] **#86c** `plugin-file-service.ts`——`_findPluginDir()` + 7 方法改路径 + 安装消毒
>   - [x] **#86d** `protocol.ts`——handler `path.join()` 先试 `builtin/` 再试 `user/`
>   - [x] **#86e1** `loader.ts` + `plugin-shell-main.tsx`——16 个 glob 模式（8×2）适配 `builtin/` + `user/`
>   - [x] **#86e2** `loader.ts`（extractPluginId + fetchPluginDataFile）+ `plugin-shell-main.tsx`（modulePath 查找）
> > **Why E4 前置：** E4 所有任务路径都假设 `plugins/builtin/file-tree/` 和 `plugins/builtin/editor/` 存在。
> - [x] **#87** `closedTabStack` —— useTabManager 加 ~10 行。Ctrl+Shift+T 恢复关闭标签页依赖。`src/hooks/useTabManager.ts`

---

## 第 1 批：E4a 文件树核心渲染（8 任务，~470 行）

> ⚠️ **必须最先做。** 没有树模型就没有交互/集成/搜索。
> 详细设计见 `01-E4a-文件树核心.md`。

- [x] **#88** FileTreeModel——数据模型 + 懒加载 + 排序 + findClosest + 符号链接标记
  文件：`plugins/builtin/file-tree/src/FileTreeModel.ts`（新） | ~90 | 验证：`listDir("/")`→`ExplorerItem[]`→`children=null`未加载→`getChildren()`懒加载→`sort()`文件夹在前→`findClosest(uri)`正确查找

- [x] **#89** FileTree + FileTreeNode——虚拟滚动 + `onMouseDown` 选中 + 缩进 `(depth-1)*16` + twistie + preview tab
  文件：`plugins/builtin/file-tree/src/FileTree.tsx` + `FileTreeNode.tsx`（新） | ~160 | 验证：1000+文件目录不卡→单击选中+预览打开（斜体）→双击固定打开（正体）→twistie 展开/折叠

- [x] **#90** sidebar.tsx——侧栏包装 + 路径面包屑 + 工具栏按钮
  文件：`plugins/builtin/file-tree/src/sidebar.tsx`（新） | ~60 | 验证：路径面包屑显示当前根→工具栏：新建文件/新建文件夹/刷新/收起全部→按钮功能正常

- [x] **#91** plugin.json + 图标栏注册 + 命令/快捷键/配置声明
  文件：`plugins/builtin/file-tree/plugin.json` + `src/index.tsx`（新） | ~20 | 验证：点 📁→侧栏文件树、点别的图标→切走、`lastSidebar` 保持状态

- [x] **#92** WelcomeView——空工作区欢迎视图 + 打开文件夹按钮 + 最近文件夹列表
  文件：`plugins/builtin/file-tree/src/WelcomeView.tsx`（新） | ~40 | 验证：无工作区→"你没有打开文件夹"→[打开文件夹]按钮→最近文件夹列表（从 FileService 读）

- [x] **#93** FileIconResolver——数据驱动图标映射（映射表 `icon-mappings.ts` + 解析器分离）
  文件：`plugins/builtin/file-tree/src/FileIconResolver.ts` + `icon-mappings.ts`（新） | ~50 | 验证：`.tsx`→React 图标、`package.json`→npm 图标、`.git`→Git 图标、普通文件→默认图标。🔥 加新图标只改映射表不改解析器

- [x] **#94** CompactFolder——紧凑单子文件夹路径压缩
  文件：`plugins/builtin/file-tree/src/CompactFolder.ts`（新） | ~30 | 验证：`explorer.compactFolders:true`→`src/components/Button.tsx` 显示为单行面包屑→`src / components / Button.tsx`

- [x] **#95** FileExcludeFilter——glob 模式排除过滤器
  文件：`plugins/builtin/file-tree/src/FileExcludeFilter.ts`（新） | ~20 | 验证：`files.exclude:{"**/node_modules":true}`→node_modules 不出现在树中

### 第 1 批补充——品质加固（5 任务，~110 行）

> 🔥 第 1 批初版完成后的品质修复——归一化、消除硬编码、接入闲置模块。
> 设计 token 见 `00.5-UI布局规格.md` §二 + §十三。

- [x] **#95a** `file-tree.css`——提取全组件内联 `style={{}}` 到独立 CSS 文件（对标 marketplace 的 `MarketplaceSidebar.css`）
  文件：`plugins/builtin/file-tree/src/file-tree.css`（新） | ~80 | 验证：视觉效果与改前一致→无 style={{}} 残留→ESLint 零错

- [x] **#95b** 设计 token CSS 变量——用 `00.5-UI布局规格.md` §十三定义的变量替代硬编码 px
  文件：`file-tree.css` | ~20 | 验证：`--tree-item-height`/`--tree-indent`/`--tree-twistie-size` 等在 CSS 中生效→改 token 值全局联动

- [x] **#95c** CompactFolder 接入——`FileTree.flattenTree()` 消费 `getCompactedPath()`，压缩链单行渲染
  文件：`plugins/builtin/file-tree/src/FileTree.tsx` | ~15 | 验证：`src/components/Button.tsx` 单目录链→"src / components / Button.tsx" 单行分隔符灰色

- [x] **#95d** FileExcludeFilter 接入——`FileTreeModel.getChildren()` 消费 `FileExcludeFilter.matches()`
  文件：`plugins/builtin/file-tree/src/FileTreeModel.ts` | ~10 | 验证：`files.exclude:{"**/node_modules":true}`→node_modules 不出现在树中

- [x] **#95e** React.memo 生效——消除 FileTree→FileTreeNode 内联箭头函数，改用 `useCallback` 稳定引用
  文件：`plugins/builtin/file-tree/src/FileTree.tsx` | ~10 | 验证：React DevTools profiler→切换选中节点时仅目标节点重渲染

---

## 第 2 批：E4b 文件树交互操作（7 任务，~410 行）

> 依赖第 1 批——有树才能交互。
> 详细设计见 `02-E4b-文件树交互.md`。

- [x] **#96** 右键菜单——🔥 **MenuRegistry 注册 15 项**→`MenuId.FileContext` + ContextKeyService when 条件 + Git 可注入
  文件：`plugins/builtin/file-tree/src/FileTreeContextMenu.tsx` + sidebar.tsx 接线 | ~136 | 验证：右键文件→15 项菜单→分组正确→when 条件动态求值（`explorerItemIsFile`/`explorerItemIsDir`）→Git 插件可注入菜单项到 FileContext

- [x] **#97** 键盘导航——↑↓←→/Home/End/Enter/Space + type-ahead 字母跳转
  文件：`plugins/builtin/file-tree/src/FileTreeKeyboard.ts`（新） | ~80 | 验证：↑↓ 切换焦点→←折叠/→展开→Home/End 首尾→输入"pac"1s内→跳转到 package.json

- [x] **#98** 快捷键注册——F2/Delete/Ctrl+XCV/Ctrl+N/Ctrl+Shift+N/Enter + when 条件
  文件：`plugins/builtin/file-tree/plugin.json` | ~60 | 验证：F2→行内重命名、Delete→删除确认→Ctrl+C→复制路径到剪贴板→when 条件只在 explorerFocus 触发

- [x] **#99** 拖放——OS 拖入 + 树内拖拽 + 拖到编辑区 + 插入线/目录高亮 + 确认
  文件：`plugins/builtin/file-tree/src/FileTreeDnD.ts`（新） | ~50 | 验证：拖 .json 到树内→复制到目录→树内拖目录到另一目录→移动→不能拖祖先到后代
  > 🔥 **前置依赖：`FileService.copy(src, dest)` 目前未导出。E3 完工前必须加。**
  > 🐛 **已知 bug（3 个，已修复 2 个，#99a-#99c 根治第三个）：** FileService.copy 不支持文件 / 拖后源目录折叠 / 路径 `/` vs `\` 不一致

- [x] **#99a** 🔥 根除路径分隔符——`FileTreeModel._toExplorerItem` + `setRoots` 入口归一化 `uri` → `/`
  文件：`plugins/builtin/file-tree/src/FileTreeModel.ts` | ~2 行 | 对标 VS Code `URI` 对象内部永远 `/`
  > 一行 `replace(/\\/g, "/")`——所有下游（`_expanded`/`findClosest`/`flattenTree`/`dirname`/`isExpanded`）自动统一。

- [x] **#99b** 🧹 撤回 hack——`FileTreeDnD.refreshDir` 去双格式 `isExpanded` 尝试
  文件：`plugins/builtin/file-tree/src/FileTreeDnD.ts` | ~2 行 | #99a 归一化后不再需要

- [x] **#99c** 🔍 `file-tree/` 插件全量审计——grep `replace(/\\/g, "/")` + `replace(/\//g, "\\")` 确认无残留不一致
  文件：`plugins/builtin/file-tree/src/**` | ~5 处 grep | 审计后零 `\` 硬编码或只留必要的 OS 边界转换

- [x] **#99d** 🔧 `pathUtils.ts` 收口——迁入 `dirname`/`joinPath`/`FlatItem`，消除 `FileTreeDnD.ts` + `FileTreeKeyboard.ts` 重复定义
  文件：`pathUtils.ts` + `FileTreeDnD.ts` + `FileTreeKeyboard.ts` + `FileTree.tsx` | ~25

- [x] **#99e** 🧹 删防御性 `replace`——只保留 `FileTreeModel.getChildren` 里对 `FileEntry.path`（listDir 边界）的转换。其余 no-op 全删
  文件：`FileTreeDnD.ts` + `FileTreeModel.ts` | ~8

- [x] **#99f** 🔍 第二轮审计——grep 确认 `file-tree/` 下零 `replace(/\\/g, "/")` 残留（仅 `normalizePath` + listDir 边界除外）
  文件：`plugins/builtin/file-tree/src/**` | 只读

- [x] **#99g** 🛡️ P1 工具补齐——`pathUtils.ts` 加 `extension(name)` + `extensionWithDot(name)`，后续 #101/#103/#118 共用
  文件：`plugins/builtin/file-tree/src/pathUtils.ts` | ~8

- [x] **#99h** 📝 P2 标注——`FileTreeModel.findClosest` 加 JSDoc 限制警告 + `/** TODO #104 findAndExpandToBypassExclude */` 占位
  文件：`plugins/builtin/file-tree/src/FileTreeModel.ts` | ~8

- [x] **#99i** 📋 P3 文档加固——设计文档路径拼接示例改 `joinPath()` + 搜索历史补 PluginStateService 强调 + `src/core/types.ts` 不存在告警
  文件：`03-E4c-文件树集成.md` + `04-E4d-文件搜索.md` | ~10

- [x] **#99j** 🎨 UI 对齐——选中色从 `var(--bg-card)` → `var(--tree-bg-selected)` 动态适配主题
  文件：`plugins/builtin/file-tree/src/file-tree.css` | ~6 | 对标 `filetree-preview.html` 蓝色调选中

- [x] **#99k** 🎨 UI 对齐——补 CSS 细节：滚动条 4px 暗色 + hover/选中过渡 80ms + 缩进引导线 + excluded 文件灰显
  文件：`plugins/builtin/file-tree/src/file-tree.css` | ~25 | 对标 `filetree-preview.html` §文件树区域样式

- [ ] **#100** 多选——Ctrl+Click / Shift+Click / Ctrl+A + 批量删除
  文件：`plugins/builtin/file-tree/src/FileTree.tsx` | ~40 | 验证：Ctrl+Click 追加选中、Shift+Click 范围选中、全选后批量删除→confirm→逐个删除

- [ ] **#101** 行内重命名——F2 输入框替换文件名 + 冲突提示 + 粘贴重名 "Copy" 后缀
  文件：`plugins/builtin/file-tree/src/FileTree.tsx` | ~50 | 验证：F2→输入新名称→Enter 确认→重名提示"是否替换？"→Esc 取消→粘贴到同目录→自动加" - Copy"
  - [ ] **#101a** 获取扩展名用 `import { extension, joinPath, dirname } from "./pathUtils"`——禁止手写 `substring(lastIndexOf)`
  - [ ] **#101b** 重命名后新路径走 `joinPath(dirname(uri), newName)`——禁止手写 `+ '/' +`

- [ ] **#102** 剪贴板服务——复制/剪切/粘贴 + "Copy" 后缀递增 + 剪切一次性
  文件：`plugins/builtin/file-tree/src/FileTreeClipboard.ts`（新） | ~40 | 验证：Ctrl+C→Ctrl+V→"file - Copy.txt"、再粘贴→"file - Copy 2.txt"、Ctrl+X→Ctrl+V→源文件消失→再粘贴无效
  - [ ] **#102a** 用 `import { joinPath, dirname } from "./pathUtils"`——禁止手写路径拼接
  - [ ] **#102b** 复用 `isAncestorOf` 从 `FileTreeDnD.ts`（已归一化）——不重写

---

## 第 3 批：E4c 文件树集成接口（5 任务，~230 行）

> ⚠️ 依赖第 1-2 批。🔥 **公共 API 契约——设计错了影响所有调用插件。**
> 详细设计见 `03-E4c-文件树集成.md`。

- [ ] **#103** FileAssociation 消费——双击打开编辑器 + "打开方式…"选择器
  文件：`plugins/builtin/file-tree/src/FileTree.tsx` | ~40 | 验证：双击 .json→`FileAssociationService.getPluginFor(".json")`→Monaco 打开→双击无关联文件→"打开方式…"菜单
  - [ ] **#103a** 获取扩展名用 `import { extensionWithDot } from "./pathUtils"`——禁止手写 `substring(lastIndexOf)`

- [ ] **#104** revealInExplorer——定位文件 + 展开目录链(绕过排除) + 高亮(2s) + 滚动 + 返回 RevealResult
  文件：`plugins/builtin/file-tree/src/commands.ts`（新） | ~60 | 验证：`executeCommand("revealInExplorer",{filePath:"a/b/c.json"})`→`{success:true}`→逐层展开→高亮→路径不存在→`{success:false,error:'not_found'}`→排除目录也可定位
  - [ ] **#104a** handler 入口 `normalizePath(args.filePath)`——外部调用方可能传 `\`
  - [ ] **#104b** 实现 `FileTreeModel.findAndExpandToBypassExclude(uri)`——目前只有 TODO 占位。逐层 listDir + 绕过 FileExcludeFilter + 展开。参考 `FileTreeModel.ts` L130 的 JSDoc
  - [ ] **#104c** `RevealInExplorerArgs` 类型定义在 `commands.ts`——**不新建 `src/core/types.ts`**。圆形大厅天然隔离，调用方通过 `executeCommand` args 传值，无需共享类型文件

- [ ] **#105** 跨插件命令完整注册——13 个 explorer.* 命令全部注册
  文件：`plugins/builtin/file-tree/src/commands.ts` | ~40 | 验证：`executeCommand("explorer.newFile")`→创建+自动进入重命名、`explorer.copyPath`→剪贴板含绝对路径
  - [ ] **#105a** `explorer.newFile` handler 入口 `normalizePath(args.dirPath)` + 路径拼接用 `joinPath(dir, name)`——禁止 `+ '/' +`
  - [ ] **#105b** 创建后调 `findClosest(path)` → 如父目录未展开返回 null（见 `findClosest` JSDoc）。若需自动展开，调 `findAndExpandToBypassExclude`（#104 实现后）

- [ ] **#106** FileDecorationProvider 消费——**import 核心类型、不重复定义** + 事件订阅（增量/全量刷新）
  文件：`plugins/builtin/file-tree/src/FileDecoration.ts`（新） | ~50 | 验证：Git 插件注册 provider→修改文件→树条目出现 M 标记→卸载 Git 插件→标记消失→ctrl+z→事件空数组触发全量刷新
  > 🔥 **前置依赖：E3f #59b `FileDecorationRegistry` 必须完工。**
  - [ ] **#106a** `import type { FileDecorationProvider, FileDecoration } from "@src/core/FileDecorationRegistry"`——**只 import，不重定义**。Copilot 补全中出现内联类型定义 → 删掉，改用 import

- [ ] **#107** tabActions 集成——autoReveal + preview→pin 自动转换 + 活跃标签页切换→文件树定位
  文件：`plugins/builtin/file-tree/src/commands.ts` | ~40 | 验证：切编辑器标签页→文件树自动定位→preview 标签页编辑后自动 pin→`explorer.autoReveal:false`→不自动定位
  - [ ] **#107a** `tab.payload.filePath` 传入 `revealInExplorer` 前调 `normalizePath()`——编辑器侧路径格式未知
  - [ ] **#107b** `createTab` 传参 key 统一用 `filePath`——保证跨插件一致性

---

## 第 4 批：E4d 文件搜索（5 任务，~310 行）

> 依赖第 1-3 批——需要 FileTreeModel + FileExcludeFilter + EncodingService。
> 详细设计见 `04-E4d-文件搜索.md`。

- [ ] **#108** SearchView 搜索面板 UI——搜索框 + 替换框 + include/exclude + 结果树 + 防抖
  文件：`plugins/builtin/file-tree/src/SearchView.tsx`（新） | ~100 | 验证：Ctrl+Shift+F→搜索面板→输入关键词→300ms 防抖→搜索结果按文件分组→显示匹配数和行预览
  - [ ] **#108a** `FileEntry.path` 来自 listDir（`\`）→ 显示/存储前调 `import { normalizePath } from "./pathUtils"`

- [ ] **#109** FileSearcher——递归搜索 + 编码检测 + 取消 + 结果缓冲(2000条上限)
  文件：`plugins/builtin/file-tree/src/FileSearcher.ts`（新） | ~80 | 验证：搜索 "pluginId"→遍历所有 workspace root→跳过 node_modules→GBK 文件也能匹配→结果达 2000 截断→取消按钮中止
  - [ ] **#109a** 搜索结果 `filePath` 统一存 `/` 格式——`normalizePath(entry.path)`

- [ ] **#110** SearchReplace——替换全部 + 单文件替换 + 确认(Cannot undo) + 替换后刷新
  文件：`plugins/builtin/file-tree/src/SearchReplace.ts`（新） | ~50 | 验证：填入替换文本→"将在 N 个文件中替换 M 处"→确认→`FileService.writeFile()`→文件树自动刷新→结果更新

- [ ] **#111** 编码检测集成——搜索前 detect encoding + 结果显示编码
  文件：`plugins/builtin/file-tree/src/FileSearcher.ts` | ~30 | 验证：搜索含中文的 GBK 文件→正确匹配→结果旁显示 "GBK"

- [ ] **#112** 搜索结果导航——F4/Shift+F4 跳转 + 搜索历史(20条) + Esc 关闭
  文件：`plugins/builtin/file-tree/plugin.json` + `SearchView.tsx` | ~50 | 验证：F4→下一个匹配→Shift+F4→上一个→Esc→关闭搜索→🔥 **搜索历史走 PluginStateService**→F5 不丢
  - [ ] **#112a** 🔥 持久化走 `PluginStateService`——`setPluginStateValue("filetree", "searchHistory", [...])`。**禁止 `localStorage.setItem()` 或 `FileService.writeFile(新文件)`**——多路径持久化 = B66/B67/B68 的种子

---

## 第 5 批：E4e 工作区与持久化（5 任务，~250 行）

> 依赖第 1-3 批。**F5 刷新后恢复状态——不做就是半成品。**
> 详细设计见 `05-E4e-工作区与持久化.md`。

- [ ] **#113** Multi-root 工作区——多根文件夹 + .linkdesk-workspace 文件格式 + 根节点右键菜单
  文件：`plugins/builtin/file-tree/src/FileTreeModel.ts` | ~80 | 验证：`WorkspaceService.addFolder("/other")`→文件树出现两个根→各自独立展开→拖放可跨根移动

- [ ] **#114** 工作区状态持久化——🔥 **PluginStateService 统一入口** + F5 恢复 + debounce 保存
  文件：`plugins/builtin/file-tree/src/workspace-state.ts`（新） | ~70 | 验证：展开 5 层目录→F5 刷新→展开状态恢复→选中恢复→滚动位置恢复→关闭窗口→下一次打开状态一致
  > 🔥 **走 `setPluginStateValue("filetree", "explorerState", state)`——不另建文件、不直写 localStorage。**
  - [ ] **#114a** `restoreExplorerState` 反序列化后对 `workspaceFolders` 和 `expandedFolders` 逐一调 `normalizePath()`——防止旧数据含 `\`

- [ ] **#115** 配置项清单——explorer.*/files.* 归属明确（`files.encoding`/`files.autoSave` 归 editor 声明）
  文件：`plugins/builtin/file-tree/plugin.json` | ~40 | 验证：设置页 "资源管理器"分组→explorer.* 配置项→修改 `explorer.sortOrder`→文件树实时更新排序→🔥 `files.encoding` 不在文件树 plugin.json 中

- [ ] **#116** 最近文件夹——🔥 **PluginStateService** + WelcomeView 快速打开 + 手动清除
  文件：`plugins/builtin/file-tree/src/WelcomeView.tsx` | ~30 | 验证：打开 3 个文件夹→关闭软件→重新打开→WelcomeView 显示 3 个最近文件夹→单击→打开
  - [ ] **#116a** 同 #114——走 PluginStateService，不另建文件。存储路径统一 `normalizePath()`

- [ ] **#117** 在文件管理器中显示——跨平台 Electron shell API
  文件：`plugins/builtin/file-tree/src/commands.ts` | ~30 | 验证：右键→"在文件管理器中显示"→Windows 资源管理器打开并选中文件→拒绝工作区外路径
  - [ ] **#117a** `findClosestRoot(filePath)` 前调 `normalizePath(filePath)`——外部路径可能含 `\`

---

## 第 6 批：E4f Monaco 编辑器（4 任务，~215 行）

> 依赖第 1-2 批（需要文件树浏览文件）和第 5 批（需要 files.encoding / files.autoSave 配置）。
> 详细设计见 `06-E4f-Monaco编辑器.md`。

- [ ] **#118** Monaco React 包装——创建/复用编辑器 + Ctrl+S + **language-map 数据驱动** + preview→pin
  文件：`plugins/builtin/editor/src/EditorView.tsx` + `language-map.ts`（新） | ~90 | 验证：文件树双击 .json→Monaco 打开→语法高亮→修改→Ctrl+S→`FileService.writeFile()`→标签页 ● 消失→编辑内容→标签页自动 pin。🔥 `language-map.ts` 加新语言换数据不改 EditorView
  - [ ] **#118a** 扩展名→语言映射用 `import { extension } from "file-tree/src/pathUtils"`——跨插件引用同一归一化工具

- [ ] **#119** EncodingService——🔥 **在核心 `src/core/encoding/EncodingService.ts`**（不在 editor 插件）BOM/UTF-8/GBK 检测 + iconv-lite
  文件：`src/core/encoding/EncodingService.ts`（新） | ~70 | 验证：打开 GBK 文件→自动检测→正确显示中文→状态栏显示 "GBK"→点击切换到 UTF-8→重新读文件。🔥 file-tree 搜索也 import 同一份——不重复定义

- [ ] **#120** JSON schema 自动补全——settings.json / keybindings.json schema + 悬停提示
  文件：`plugins/builtin/editor/src/MonacoSchemaConfig.ts`（新） | ~30 | 验证：打开 settings.json→输入 `"app."`→自动补全 `accentColor`/`theme`/`language`→悬停显示 schema 描述→错误值红色波浪线

- [ ] **#121** 多标签页 + 🔥 **setDirty(tabId, bool) 现有 API** + `files.encoding`/`files.autoSave` 配置 + Ctrl+Shift+T
  文件：`plugins/builtin/editor/plugin.json` + `EditorView.tsx` | ~25 | 验证：打开 3 个文件→3 个标签页→修改一个→标题显示 ●→Ctrl+W 关闭→"是否保存？"→Ctrl+Shift+T→恢复关闭的标签页。🔥 不创建新命令——壳已有 `setDirty()`/`updateTabLabelBySourceId()`
  - [ ] **#121a** `files.encoding`/`files.autoSave` 声明在 `editor/plugin.json` 的 `contributes.configuration`——不放在 file-tree 的 plugin.json（见 `05-E4e` 归属表）

---

## 验证路线

### 第 1 批完成后
打开 LinkDesk→点 📁→文件树出现（空工作区→欢迎视图）→打开文件夹→展开目录→1000+文件不卡→紧凑文件夹正确压缩→node_modules 不显示

### 第 2 批完成后
右键文件→菜单完整→F2 重命名→Ctrl+C/X/V→拖放移动→多选删除→type-ahead "pac"→跳到 package.json

### 第 3 批完成后
双击 .json→Monaco（如果有）→设置页→"在文件树中显示 settings.json"→文件树展开定位高亮→Git 插件注册装饰器→文件出现标记

### 第 4 批完成后
Ctrl+Shift+F→搜索 "pluginId"→结果列表→F4 逐个跳转→替换为 "pluginID"→确认→文件中被替换→文件树自动刷新

### 第 5 批完成后
添加第二个文件夹→文件树显示两个根→展开 5 层→F5 刷新→展开/选中/滚动全部恢复→最近文件夹列表正确

### 第 6 批完成后
双击 .json→Monaco 语法高亮→编辑→Ctrl+S→GBK 文件正确显示→settings.json 自动补全→Ctrl+Shift+T 恢复关闭

---

> **← 上一 Phase 执行清单：** `../E3_多WebView与壳收尾_暂定/08-执行清单.md`
> **详细设计：** `01-E4a-文件树核心.md` ~ `06-E4f-Monaco编辑器.md`
> **索引：** `00-README.md`
> **🏁 E 编号到此为止。** #83-#121 是最后 39 个有编号的任务。此后全是 `plugin.json` + `index.tsx`，不占编号。
