# E4 执行清单——重构版

> 2026-07-29。**对标 VS Code Explorer 6 文件、5,500 行——按架构层拆分。**
> 旧 39 任务清单（`07-执行清单.md`）已归档——过于扁平、未分层。
>
> **编号从 1 开始。已完成打 ✅。🔴 = 致命级 bug。45 任务（含 ViewContainer #46 长期）。**

---

## 第 1 层：数据模型（对标 `explorerModel.ts` 526 行）

> 前置：无。第 1 层是所有上层的基石——模型不定，视图/命令/配置全依赖它。

- [x] **#1** `FileTreeModel`——数据模型 + 懒加载 + 排序 + findClosest
  文件：`FileTreeModel.ts` | ~170 行 | commit `18c7287` 附近
  > 对标 VS Code `ExplorerModel`。核心设计：`children=null` = 未加载，`[]` = 空目录。

- [x] **#2** `ExplorerItem` 类型——uri / name / isDirectory / children / parent / decoration
  文件：`FileTreeModel.ts` 接口导出 | 包含在 #1 中

- [x] **#3** `setRoots` + `getChildren` + `expand`/`collapse` + `refresh`
  文件：`FileTreeModel.ts` | 包含在 #1 中
  > 🔴 **已知风险：** `refresh(path)` 设 `children=null` 但不重载。`_expanded` 状态和 `children` 不同步→ twistie 箭头与内容不一致。拖放修复（#14）加了 `refreshDir` 临时补丁。**建议在模型层加 `refreshAndReload` 方法。**

- [x] **#4** URI 归一化——`_toExplorerItem` + `setRoots` 入口 `normalizePath()`
  文件：`pathUtils.ts` `normalizePath` + `FileTreeModel.ts` | ~12 行 | commit `a4a8aa6`
  > 对标 VS Code `URI` 对象——内部永远是 `/`。根除了 `\` vs `/` 混用。

- [x] **#5** `pathUtils.ts`——`basename` / `splitPath` / `normalizePath` / `dirname` / `joinPath` / `extension` / `extensionWithDot`
  文件：`pathUtils.ts` | ~70 行 | commit `2d1b5e4`
  > 🔴 **AI 进场须知：** 所有路径拼接走此文件。禁止 `substring(lastIndexOf)` 取扩展名，禁止 `+ '/' +` 拼接。`FlatItem` 类型也在此文件。

- [x] **#6** `findClosest` JSDoc 警告 + `/** TODO findAndExpandToBypassExclude */` 占位
  文件：`FileTreeModel.ts` | ~8 行 | commit `48e1495`
  > `findClosest` 沿 children 链深度遍历——路径链未展开时返回 null。#32 需要新方法。

- [ ] **#7** 🔴 R1 压缩控制器——从 `flattenTree` 视图层提取到模型层
  文件：`FileTreeModel.ts` + 新 `CompactController.ts` | ~80 行
  > **这是 Bug A/B/C 的根因。** VS Code 对标：`CompressedNavigationController` + `ITreeCompressionDelegate`。压缩态/展开态切换应在模型层——不在 `flattenTree` 里 `return` 跳过。
  > 🔴 当前三个 bug：
  > - A: 同名目录压缩（`工具软件 / 工具软件`）→ 点两下才展开
  > - B: 压缩链末端是文件 → twistie 消失回不去
  > - C: 叶子展开后子节点仍不可见（已部分修复 `18c7287`）
  > 新设计文档：`02-视图与渲染/02-压缩控制器.md`（待写）

- [ ] **#8** FileSorter 扩展——对标 VS Code 6 种排序
  文件：`FileTreeModel.ts` | ~50 行
  > 当前只有 `foldersFirst`。VS Code 有：`default` / `mixed` / `filesFirst` / `type` / `modified` / `foldersNestsFiles`。

- [ ] **#9** FileExcludeFilter 扩展——对标 VS Code `FilesFilter`
  文件：`FileExcludeFilter.ts` | ~50 行
  > 当前只支持 glob。VS Code 还有：`.gitignore` 集成（`explorer.excludeGitIgnore`）、文件嵌套（`fileNesting`）、只读/锁定检查。

---

## 第 2 层：视图与渲染（对标 `explorerViewer.ts` 2,117 行）

> 依赖第 1 层。模型就绪后才能渲染。

- [x] **#10** `FileTree.tsx`——虚拟滚动 + `flattenTree` + twistie 展开/折叠
  文件：`FileTree.tsx` | ~280 行 | commit `18c7287` 附近
  > 虚拟滚动：`TREE_ITEM_HEIGHT=22` + `OVERSCAN=10`。`flattenTree` 扁平化模型→虚拟列表。

- [x] **#11** `FileTreeNode.tsx`——节点渲染 + twistie + 图标 + 文件名 + decoration badge
  文件：`FileTreeNode.tsx` | ~140 行 | 包含在 #10 中
  > `React.memo` 生效——回调传参数不传闭包。

- [x] **#12** `FileIconResolver`——数据驱动图标映射
  文件：`FileIconResolver.ts` + `icon-mappings.ts` | ~125 行

- [x] **#13** `CompactFolder`——`getCompactedPath` + `findLeaf`
  文件：`CompactFolder.ts` | ~42 行 | commit `18c7287`
  > ⚠️ 此模块将在 #7（压缩控制器）后重构——逻辑从 `flattenTree` 提取到模型层。

- [x] **#14** 拖放——OS 拖入 + 树内拖拽 + 约束
  文件：`FileTreeDnD.ts` | ~190 行 | commit `62e45de`
  > `useFileTreeDnD` hook。OS 拖入走 `e.dataTransfer.files` → `FileService.copy`。树内拖拽走 `dataTransfer text/plain` → `copy` + `deleteEntry`。
  > 🔴 **已知修复（已提交）：** `FileService.copy` 后端只支持目录→加 `copy(src,dest)` 判断文件/目录（`5f0b9c7`）。拖放后目录折叠→`refreshDir` 重载子节点（`1e90f13`）。OS 拖入无祖先检查→递归嵌套膨胀 1.85GB（`afc9bb6`）。
  > 🔍 **UI 验证：** 见 `05-拖放系统.md` §三——6 步测试清单（含 🔴🔴🔴 步骤 3：拖根目录到子目录必须被拦截）
  - [ ] **#14a** 🔴🔴🔴 OS 拖入补漏——`srcPath === targetDir` 同目录拦截（`afc9bb6` 只覆盖了祖先→后代，未覆盖自己→自己）
    文件：`FileTreeDnD.ts` | ~3 行 | 场景：Explorer 拖 `sub/` 到树上 `sub` 节点 → `joinPath(sub, "sub")` = `sub/sub/` → 递归嵌套
    > 修复：`if (normalizedSrc === normalizedTarget || normalizedTarget.startsWith(normalizedSrc + "/")) continue;`

- [x] **#15** 键盘导航——↑↓←→/Home/End/Enter/Space + type-ahead
  文件：`FileTreeKeyboard.ts` | ~230 行 | commit `19a4119`
  > `useFileTreeKeyboard` hook。type-ahead 1s 窗口累积字符匹配。ArrowLeft 折叠/跳父节点、ArrowRight 展开/跳首子节点。
  > ⚠️ **已知限制：** type-ahead 是简单 startswith——对标 VS Code `ExplorerFindProvider`（200 行，支持模糊匹配+高亮）。当前够用，未来可升级。

- [x] **#16** 🔴 `file-tree.css`——选中色 + 悬浮色 + 滚动条 + 过渡动画 + 缩进引导线 + dimmed
  文件：`file-tree.css` | ~330 行 | commit `5170cfd`
  > 对标 `filetree-preview.html`。`color-mix(accent 15%, transparent)` 动态适配主题。
  > 🔴 **已知遗漏（未对齐 HTML 预览）：** 缩进引导线 CSS 类已定义但未在 `flattenTree` 中接线（`file-tree-node--guide`）。点击水波纹未实现。excluded 文件 dimmed 未接线（`FileExcludeFilter` 直接过滤掉——需改为保留但灰显）。

---

## 第 3 层：视图容器（对标 `explorerView.ts` 1,145 行）

> 依赖第 1+2 层。侧栏包装 + 面包屑 + 工具栏 + 欢迎视图 + Context Key。

- [x] **#17** `sidebar.tsx`——侧栏包装 + 面包屑 + 工具栏
  文件：`sidebar.tsx` | ~140 行 | commit `18c7287` 附近
  > ⚠️ **UI 差异（对标 VS Code）：** 工具栏应和根名同行（`📁 工具软件 [+][🔄][⊟]`），面包屑多余（根节点名即路径）。见 `00.5-UI布局规格.md` §一。
  > 🔴 **已知问题：** 打开第二个文件夹时树不立即刷新——`syncRoots` async 竞态。需验证 `WorkspaceService.addFolder` 路径格式是否一致。
  > ⬜ **#17a** 工具栏移到 header 行——和根名同行
  > ⬜ **#17b** 去面包屑——根节点名替代（`depth=0` 加粗样式 `file-tree-node--root`）
  > ⬜ **#17c** `flattenTree` 根 depth 从 1 改为 0——根节点缩进 0，第一层子节点缩进 1
  > 🔴 **硬约束：** sidebar 不能写死 file-tree——将来要改为 ViewContainer 渲染循环。新增代码不加剧硬编码。见 `00.9` §层 6。

- [ ] **#46** 🔵 P2 ViewContainer 桌子——核心提供 API 让插件贡献视图到侧栏容器（大纲/时间线/Git/AI）
  文件：新 `src/core/ViewContainerService.ts` | ~80 行
  > 🔴 **不在 E4 实现——壳级能力。** 文档先写清楚（`00.9` §层 6）。对标 VS Code `ViewContainer` + `ViewDescriptor`。plugin.json 加 `contributes.views` + `contributes.viewsContainers` 贡献点。
  > 时机：第一个非 file-tree 的视图插件（大纲/时间线）需要接入侧栏时。

- [x] **#18** `WelcomeView`——空工作区 + 打开文件夹 + 最近列表
  文件：`WelcomeView.tsx` | ~130 行

- [x] **#19** `plugin.json`——图标栏注册 + 命令/快捷键/配置声明
  文件：`plugin.json` | ~60 行

- [ ] **#20** 🔴 R2 Context Key 矩阵——对标 VS Code 14+ keys
  文件：`FileTree.tsx` + `sidebar.tsx` | ~20 行
  > 当前只有 4 个 key（`explorerFocus` / `explorerItemIsFile` / `explorerItemIsDir` / `explorerItemIsRoot`）。
  > **缺失（阻塞后续任务）：**
  > - `explorerResourceCut`——#27 剪贴板粘贴的 when 条件
  > - `explorerResourceReadonly`——#25 重命名的 when 条件（只读文件禁止重命名）
  > - `explorerResourceMoveableToTrash`——#25 删除时区分回收站 vs 永久删除
  > - `explorerViewletCompressedFocus`——#7 压缩节点键盘导航
  > - `explorerViewletCompressedFirstFocus` / `CompressedLastFocus`——压缩段内导航
  > - `viewHasSomeCollapsibleItem`——工具栏"全部折叠"按钮显隐
  > - `FoldersViewVisibleContext`——视图容器可见性（大纲/时间线切换）
  > 新设计文档：`03-视图容器/02-ContextKey矩阵.md`（待写）

---

## 第 4 层：命令系统（对标 `fileActions.ts` 1,416 行 + `fileConstants.ts` 48 行）

> 依赖第 1+2+3 层。文件树的所有操作命令——右键菜单 + 快捷键触发。

- [x] **#21** 右键菜单——15 命令注册 + `MenuId.FileContext` 菜单项 + `ContextMenu` 消费
  文件：`FileTreeContextMenu.tsx` | ~130 行 | commit `d24d5ca`
  > 5 组：navigation / editing / creation / modify / search。全部 handler 为占位 `console.warn`。

- [x] **#22** 快捷键注册——10 条 keybindings + `explorerFocus` context key
  文件：`plugin.json` | +9 条 | commit `fcff549`
  > F2/Delete/Ctrl+XCV/Ctrl+N/Shift+N/Enter/Ctrl+Enter。全部 when 条件依赖 #20 Context Key。

- [ ] **🔴 #23** CompactFolder Bug A/B/C——三合一修复
  文件：`FileTree.tsx` + `CompactFolder.ts` | ~15 行
  > **这必须在 #24-#29 之前修。** 根因：`flattenTree` 压缩后 `return` 跳过子节点 + `findLeaf` 返回文件（无 twistie）。
  > 修复方案见 `00.9-Explorer架构重审.md` §二 层 2。

- [ ] **#24** 多选——Ctrl+Click / Shift+Click / Ctrl+A + 批量删除
  文件：`FileTree.tsx` | ~40 行

- [ ] **#25** 行内重命名——F2 输入框 + 冲突提示 + "Copy" 后缀
  文件：`FileTree.tsx` | ~50 行
  > 🛡️ `#25a` 扩展名用 `extension` from pathUtils。`#25b` 新路径用 `joinPath(dirname, newName)`。

- [ ] **#26** 剪贴板服务——复制/剪切/粘贴 + "Copy" 后缀递增 + 剪切一次性
  文件：`FileTreeClipboard.ts`（新） | ~40 行
  > 🛡️ `#26a` 路径拼接用 `joinPath`/`dirname`。`#26b` 复用 `isAncestorOf` from FileTreeDnD。

- [ ] **#27** 命令 handler 实现——替换占位
  文件：`FileTreeContextMenu.tsx` + 新 `commands.ts` | ~60 行
  > #21 的 handler 全为 `console.warn`。#24-#26 实现后在此接入真实 handler。
  > `explorer.openFocused` 需要在 `commands.ts` 中注册。

- [ ] **#28** FileAssociation 消费——双击打开编辑器
  文件：`FileTree.tsx` | ~40 行
  > 🛡️ `#28a` 扩展名用 `extensionWithDot` from pathUtils。

- [ ] **#29** revealInExplorer——定位文件 + 展开目录链 + 高亮 + 滚动
  文件：`commands.ts`（新） | ~60 行
  > 🛡️ `#29a` 入口 `normalizePath(args.filePath)`。`#29b` 实现 `findAndExpandToBypassExclude`（#6 的 TODO）。`#29c` 类型放 `commands.ts`——不建 `src/core/types.ts`。

- [ ] **#30** FileDecorationProvider 消费——import 核心类型 + 事件订阅
  文件：`FileDecoration.ts`（新） | ~50 行
  > 🛡️ `#30a` `import type` from `@src/core/FileDecorationRegistry`——只 import，不重定义。

- [ ] **#31** tabActions 集成——autoReveal + preview→pin
  文件：`commands.ts` | ~40 行
  > 🛡️ `#31a` `tab.payload.filePath` 前调 `normalizePath()`。`#31b` `createTab` 传参 key 统一用 `filePath`。

- [ ] **#32** 🔥 MenuBar 菜单栏——`registerMenuItems(MenuId.MenuBar, "file-tree", [...])`
  文件：`FileTreeContextMenu.tsx` | ~30 行
  > 对标 VS Code 文件/编辑/选择/查看菜单。核心已就绪——`MenuId.MenuBar` + `TitleBar.tsx` L74 已消费。只需注册，零核心改动。
  > `05-菜单栏贡献.md` 完整菜单项对标表。
  - [ ] **#32a** 文件菜单——打开文件夹/新建文件（`group: "file_open"` / `"file_new"`）
  - [ ] **#32b** 编辑菜单——剪切/复制/粘贴（`group: "edit_clipboard"`）
  - [ ] **#32c** 查看菜单——资源管理器/全屏/缩放（`group: "view_panels"`）

---

## 第 5 层：文件搜索（对标 `searchView.ts`）

> 依赖第 1+2+3 层。搜索面板独立于文件树——VS Code 也是独立 Viewlet。
> ⚠️ 搜索可能独立成插件而非内嵌在 file-tree。

- [ ] **#33** SearchView——搜索框 + 替换框 + include/exclude + 结果树
  文件：`SearchView.tsx`（新） | ~100 行
  > 🛡️ `#33a` `FileEntry.path` 显示前调 `normalizePath()`。

- [ ] **#34** FileSearcher——递归搜索 + 编码检测 + 取消 + 结果缓冲
  文件：`FileSearcher.ts`（新） | ~80 行
  > 🛡️ `#34a` 存储 `filePath` 时调 `normalizePath()`。

- [ ] **#35** SearchReplace——替换全部 + 确认不可撤销
  文件：`SearchReplace.ts`（新） | ~50 行

- [ ] **#36** 搜索结果导航——F4/Shift+F4 + 搜索历史(PluginStateService)
  文件：`SearchView.tsx` | ~50 行
  > 🔴 `#36a` 持久化走 `PluginStateService`——`setPluginStateValue("filetree", "searchHistory", [...])`。禁止 `localStorage` 或新文件。B66/B67/B68 教训。

---

## 第 6 层：工作区与持久化（对标 VS Code Multi-root + storageService）

> 依赖第 1+2+3 层。

- [ ] **#37** Multi-root 工作区——多根文件夹 + .linkdesk-workspace 格式
  文件：`FileTreeModel.ts` | ~80 行

- [ ] **#38** 工作区状态持久化——`PluginStateService` + F5 恢复 + debounce
  文件：`workspace-state.ts`（新） | ~70 行
  > 🔴 `#38a` 反序列化后逐项 `normalizePath()`——防止旧数据含 `\`。

- [ ] **#39** 配置项完整清单——explorer.* + files.*
  文件：`plugin.json` | ~40 行
  > `files.encoding`/`files.autoSave` 在 editor 插件声明——不在 file-tree plugin.json。

- [ ] **#40** 最近文件夹——`PluginStateService` + WelcomeView
  文件：`WelcomeView.tsx` | ~30 行
  > 🛡️ `#40a` 同 #38——走 PluginStateService。路径 `normalizePath()`。

- [ ] **#41** 在文件管理器中显示——跨平台 shell API
  文件：`commands.ts` | ~30 行
  > 🛡️ `#41a` `findClosestRoot` 前调 `normalizePath()`。

---

## 第 7 层：Monaco 编辑器（对标 VS Code editor）

> 依赖第 1+2 层。独立插件——`plugins/builtin/editor/`。

- [ ] **#42** Monaco React 包装——Ctrl+S + language-map
  文件：`EditorView.tsx` + `language-map.ts`（新） | ~90 行
  > 🛡️ `#42a` 扩展名映射用 `extension` from pathUtils。

- [ ] **#43** EncodingService——核心 `src/core/encoding/`
  文件：`EncodingService.ts`（新） | ~70 行

- [ ] **#44** JSON schema 自动补全——settings.json / keybindings.json
  文件：`MonacoSchemaConfig.ts`（新） | ~30 行

- [ ] **#45** 多标签页——setDirty + files.encoding/files.autoSave + Ctrl+Shift+T
  文件：`plugin.json` + `EditorView.tsx` | ~25 行
  > 🛡️ `#45a` encoding/autoSave 在 editor 的 `contributes.configuration`，不在 file-tree plugin.json。

---

## 🔴 致命级 Bug 速查（新 AI 进场先看）

| Bug | 症状 | 根因 | 修复状态 |
|:--|:--|:--|:--:|
| CompactFolder A | 同名目录压缩，点两下才展开 | `flattenTree` 无条件 `return` | ⬜ #23 |
| CompactFolder B | 压缩链末端文件→twistie 消失 | `findLeaf` 返回文件无 twistie | ⬜ #23 |
| CompactFolder C | 叶子展开后子节点不可见 | `shouldUnfold` 判断不完整 | ⬜ #23 |
| 打开第二个文件夹不刷新 | 树不更新，要点已有目录才"刷"出来 | `syncRoots` async 竞态 | ⬜ #17 已知 |
| `FileService.copy` 不支持文件 | 拖 exe 报 ENOTDIR | Electron 端只调 copyDir | ✅ `5f0b9c7` |
| 拖放后目录折叠 | twistie↓但空 | `refresh` 后未重载 | ✅ `1e90f13` |
| 🔴🔴🔴 OS 拖入无祖先检查（**数据损毁**） | 拖根目录到子目录→递归嵌套膨胀→1.85GB | 树内拖拽有 `isAncestorOf` 但 OS 拖入没有 | ✅ `afc9bb6` |
| 🔴🔴🔴 OS 拖入同目录补漏（**数据损毁**） | 拖 `sub/` 到树上 `sub` 节点 → `sub/sub/` 递归 | `afc9bb6` 只拦祖先→后代，未拦自己→自己 | ⬜ #14a |
| 路径 `\` vs `/` 失配 | `isExpanded` 查不到 | `dirname` 用 `/`，`_expanded` 存 `\` | ✅ `a4a8aa6` |
| `TREE_TREE_ITEM_HEIGHT` typo | 运行时空白 | 多写 TREE_ 前缀 | ✅ `d24d5ca` + 防线加固 |

---

> **← 旧清单：** `📦 已归档/07-执行清单.md`
> **索引：** `00-README.md`
> **架构重审：** `00.9-Explorer架构重审.md`
