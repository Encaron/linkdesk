# 批次 1 处置档案 · 旧时代施工脚手架

> **执行日期：** 2026-09-11
> **记忆仓提交：** `0b60afe`（消息含「批次 1」）· **回滚点：** 提交前 `e5644e6` · **全量快照：** `b6e5048`（250/250）
> **范围：** `01-分批执行路线.md` 批次 1 的 1a（24 份「直接删除」）+ 1b（7 份「折进后删」）= 31 份候选
> **结果：** 删 **18**（1a 12 + 1b 6）· 延后/保留 **13** · 其中 **7 份折入既有文件** · 文件总数 **250 → 233**（含本批新增 1 份 `report-file-paths-not-bare-names.md`，净减 17）

---

## 一、审议门结论（第零条铁律）

- 2026-09-11 在对话中**逐份报告处置表**（判定 / 理由 / 折进去向 / 存疑点）→ 用户**明确「同意」放行** → 进入执行。
- 用户补充：「**不用等我审批，我同意后你直接做就行**」——放行后连续执行到提交，不再逐项停顿。
- **用户未推翻任何条目**；本批**无「用户拍板改判」项**。
- 1a 表内三条 ⚠️「须确认」经**查工程现状**全部消解或落地（证据见 §三）→ 全部删除，**「待用户裁」区为空**。
- 1a 中 13 份未在本批删除，**非用户否决**，而是折入目标尚不存在或属后续批次题材——详见 §四。

---

## 二、折入既有文件（1b 6 份 + 1a 折入 1 份 = 7 份，先写后删）

| 源文件 | 折入目标 | 折入内容 | 核对 |
|:--|:--|:--|:--:|
| `e5-120-audit-plugin-core-imports.md` | `plugin-import-exceptions.md` | 新增「规则的由来与强度（E5#120 → E5.5#0a 审计）」节：零 `@src/core` 导入规则、`import type` 唯一例外、命中数 28→52、ESLint warn→error、52 项账本为死快照（多 WebView 时代产物，已归档于 `b6e5048`）、测试不豁免 | ✅ |
| `e5.5-0c-ipc-listener-audit.md` | `hard-constraints.md` | 新增「Electron / preload 运行时约束」节：约束 19（IPC 监听器模块顶层注册 + 缓冲回放）、约束 20（IPC 桥用 `_refCount` 非 `_initialized`）+ E5.5#0c 审计来历 | ✅ |
| `e5.5-task-execution-discipline.md` | `ai-workflow-discipline.md` | 新增「任务线性插入——只加在当前进度后面」小节 | ✅ |
| `work-cadence-one-task-at-a-time.md` | `ai-workflow-discipline.md` | 新增「节奏：不心急、一次一个小任务、每任务存 git、禁止提前打勾」小节 | ✅ |
| `execution-checklist-parallel-slimmer.md` | `ai-workflow-discipline.md` | 新增「commit 前查共享文档是否混入他处未提交改动」小节（含 `e902bc0f` 实证） | ✅ |
| `e4-emitter-pattern-plugin-model.md` | `design-decisions.md` | 新增决策 26「插件自有数据模型必须用 Emitter 响应式模式——command handler 只改 model」+ 三次同类时序 bug + E4V#55 定案 | ✅ |
| `lsp-packages-in-extraresources-temporary.md` | `lsp-self-contained-gaps.md` | 新增「历史补记——E5#114d 的 `extraResources` 临时 hack 已删」段（含「忘了清则壳膨胀」教训） | ✅ |

**说明：** `execution-checklist-parallel-slimmer.md` 原列于 1a（「一次性并行 agent 事件的记录」），执行时判定其教训（commit 前查共享文档混入）有长期价值 → **改判为折入 `ai-workflow-discipline.md`**，而非纯删。

**核对方法（T1.9）：** 每份折入写出后，回查原文件逐条对照，确认无遗漏；折入段均带「合并自 `xxx.md`（2026-09-11）」溯源行，可在目标文件内 grep 定位。

---

## 三、纯删（11 份）+ 工程现状核对

> 判据：时代已废（进度快照 / session 交接 / 一次性审计）或「待办」已被后续任务闭环。后 5 项做了**代码核对**。

| # | 文件 | 删除理由 | 代码核对 |
|:-:|:--|:--|:--|
| 1 | `e5-execution-progress.md` | E5 进度快照（78 任务），真相源在工程 E6-执行清单 | — |
| 2 | `e5-handover-2026-08-06-session.md` | E5 收尾 session 交接，「分析已完成、编码未开始」——过程件 | — |
| 3 | `e5-handover-2026-08-06-session-2.md` | 同上，第 2 次 session 交接 | — |
| 4 | `e5.5-0d-e3a-lesson-recheck.md` | 一次性审计（`pluginViews.create()` 全链路 6 节点验证通过）——多 WebView 时代链路由 E5.7 单 Pool 重建后失效 | 单 Pool 重建，旧链无对应物 |
| 5 | `e5.5-0e-workspace-tab-persistence-audit.md` | 审计发现 `_folders` 不持久化 | **已修**：`src/core/services/layout/WorkspaceService.ts:194` `_persistFolders()` 写 StorageService；同文件 `:19` 留 `E5.5#0e` 标记 |
| 6 | `e5.6-progress-2026-08-12.md` | 双 Pool 进度快照（51%），E5.6 已封存 | — |
| 7 | `e5.6-preload-shell-pool-backfills.md` | `preload-shell.ts` 临时垫片记录 | **已消解**：`electron/preload-shell.ts` 已无 `encoding`/`langDef` 等临时暴露 |
| 8 | `ringbuffer-location-design-gap.md` | 双 Pool 时代开放问：RingBuffer 在主进程还是 Pool 进程 | **已定**：`src/core/pipeline/RingBuffer.ts`（core/renderer 侧），问题随单 Pool 消失 |
| 9 | `e4-openinterminal-hardcoded-powershell.md` | 待办：加 `terminal.external.windowsExec` 配置项 | **已落地**：`src/core/api/linkdesk-api/shell.ts:99` `openInTerminal(dirPath, terminalExe?, customCommand?)` 可配 |
| 10 | `events-api-polish-items.md` | events API 打磨清单（字符串频道 + `any` 载荷） | **已落地**：`src/core/api/linkdesk-api/data.ts:47` `on<T>/emit` 泛型分型；channel 自由字符串为**有意设计** |
| 11 | `fileservice-api-naming-normalization.md` | FileService 命名归一待办 | **已落地**：见 `api-naming-mandatory-read.md`（`mkdir`→`createDir` 等已改名） |

---

## 四、延后 / 保留（13 份，本批**未动**）

> 不在本批删除 ≠ 用户否决。原因分三类：**折入目标尚未建立**、**属后续批次题材**、**仍是活跃文件**。

| 文件 | 去向 | 原因 |
|:--|:--|:--|
| `market-install-button-dev-dir-zip-gap.md` | ⚠️ 待定 | 1a 表「须确认：71c/71d 后缺口是否已闭」——**本批未做完整工程核对即先留**，交后续批次 |
| `e5-migration-audit.md` | 批次 6a `bug-atlas.md` | 折入目标（bug 图谱）尚未建立——**先建目标再删源** |
| `e4v44-emptycontent-deadlock.md` | 批次 6a `bug-atlas.md` | 同上 |
| `e4-v46-viewcontainer-persistence.md` | 批次 6a `bug-atlas.md` | 同上 |
| `sidebar-tab-bidirectional-sync.md` | 批次 6a `bug-atlas.md` | 同上 |
| `title-empty-string-no-remount.md` | 批次 6a `bug-atlas.md` | 同上 |
| `e4-sticky-abandoned.md` | 批次 2 `evolution-chronicle.md` | 放弃功能，「重做前提」一句折入编年史 |
| `e5.5-blueprint.md` | 批次 3 `multi-webview-era.md` | 多 WebView 蓝图，归时代志 |
| `e5.5-last-webview-epoch.md` | 批次 3 | 时代宣告，归时代志（现仍被 `quickpick-normalization.md` 引用） |
| `webview-ready-whitelist-lifecycle.md` | 批次 3 | 多 WebView 白名单生命周期，归时代志 |
| `e5.6-16.7-glass-drop-zone.md` | 批次 4 外观档案 | 1b 表内已注明「留到批次 4」 |
| `e5-blueprint-complete.md` | 🟢 保留 | E5 蓝图完工记录，仍作架构索引（`e4-complete.md` 引用） |
| `e5.5-0b-module-state-audit.md` | 🟢 保留 | 模块级状态审计，仍被 E5.5 系文件引用 |

---

## 五、执行与校验记录

### 动作
- **删 18**（`git rm`）：§二 7 份折入源 + §三 11 份纯删。
- **改 15 + 增 1**：折入写入 5 个目标文件（`plugin-import-exceptions` / `hard-constraints` / `ai-workflow-discipline` / `design-decisions` / `lsp-self-contained-gaps`）+ 10 个存活文件修正对已删文件的 wiki 断链 + 新增 `report-file-paths-not-bare-names.md`。
- **MEMORY.md 索引同步**：删除 `[E4 Emitter]`、`[E4 openInTerminal]`、`[清单并行精简]` 三条；新增 `[报告文件给全路径]`。索引断链归零。

### 校验（脚本见 `02-索引重写规范.md` §四）
| 项 | 结果 |
|:--|:--|
| 18 份残留 | **0**（全部移除） |
| `MEMORY.md` 索引断链（`](x.md)` 形式） | **0** |
| 全库 `[[wiki]]` 断链 | **34**——**全部为既有**，无一条指向本批删除文件（基线遗留，见 §六） |
| 孤儿（MEMORY.md 未引用） | **53**（整理前 68）——批次 6g 重写索引时归零 |
| 白名单 16 份（`00-整理总纲.md` §三） | 完好，未触碰 |

### 提交
- 记忆仓 `git commit` → **`0b60afe`**，消息含「批次 1」，独立回滚点。

---

## 六、遗留问题（交后续批次 / 工程）

1. **全库 34 条 `[[wiki]]` 断链**（既有，非本批产生）：如 `recurring-themes`（多份引用）、`e5-8-theme-audit-bugs`、`e6-execution-plan`、`window-namespace` 等——与工程 E6#96（`check-doc-links.mjs` 门禁）同域，宜在批次 6 索引重写时一并清扫。
2. **孤儿 53 份**：索引重写（批次 6g）后应归零。
3. **`market-install-button-dev-dir-zip-gap.md`**（⚠️）：1a 表要求「查 71c/71d 后缺口是否已闭」——本批未完成该项工程核对，暂留，待后续批次补查。
4. **并行 agent 干扰（重要）**：记忆仓存在**另一个 E6 agent 的未提交改动**——`e6-l5-handoff.md`、`e6-l4-deferred.md`、新文件 `doc-code-citation-shape-trap.md`，及 `MEMORY.md` 顶部时段行。
   - 本批提交**只纳入我方 35 项**：`MEMORY.md` 采用「HEAD + 我方 3 处索引编辑」重建 blob 后 `git update-index --cacheinfo` 单独入索引，**未吞并**上述他处改动；其余文件按显式路径 `git add`。
   - 提交后工作区剩余未提交项 = 该 agent 的产物，**原样保留**，等待其自行提交。
