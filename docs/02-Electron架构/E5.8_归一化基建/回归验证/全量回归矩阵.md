# E5.8 全量回归矩阵

> 对应清单任务 **E5.8#24 场景回归**（[../E5.8-执行清单.md](../E5.8-执行清单.md) Phase 5）——Phase 5 执行本矩阵，每个场景实测后勾选。
> E5.8 是基建工程——回归的唯一目的是证明 **UI/行为零变化**，同时新机制（disposer 卸载 / requires 编排 / 契约生成）本身可诊断、可验证。
> 回归风险列标注各场景与 E5.8 改动的关联点——Phase 5 实测时优先盯这些。

---

## 场景 1：启动与装载

| 验证点 | 操作 | 期望 |
|:--|:--|:--|
| 冷启动 | 全新启动软件 | 正常进入，欢迎页/文件树就位，无启动报错 |
| 插件全载 | 启动后查看插件列表 | 内置三件套 + 用户插件全部 ACTIVE |
| 坏插件诊断面 | 制造坏插件（坏 schema / 缺 entry / 加载抛错）→ 启动 | 不崩溃；状态机可查"为什么没激活"（#11 诊断面） |
| 依赖编排加载序 | 有 requires 的插件 + 依赖插件乱序放置 | 依赖图决定激活序，扫描序不影响（#14） |
| 无依赖兼容 | 现有插件（无 requires） | 行为不变（#16 零改动兼容） |

**回归风险：** Phase 2/3 重写 loader——装载时序、扫描序语义不得变；`import.meta.glob` 加载路径不得回归（E5.7 冻结语义）。

**✅ 实测（2026-08-20，synthetic 夹具 6 件）：**
- 冷启动正常；内置 + 用户插件全 ACTIVE ✅
- 依赖编排加载序：dep-a requires dep-b → 两者都 ACTIVE，b 先激活，扫描序不影响 ✅
- 无依赖兼容：现有插件零改动正常 ✅
- **⚠️ 坏插件诊断面预期纠正**：坏 entry（指向不存在文件）**不触发** #11 诊断面——`loadPluginComponent` 失败被 `runtime.ts:283` catch 静默吞掉，插件照样 ACTIVE（仅无视图组件）。详情页只显示 plugin.json description，无「为什么没激活」。诊断面仅 plugin.json 解析失败/读盘抛错/依赖环/版本不满足时触发。→ 坏 entry 静默降级 = 静默吞错，呼应 #24.6「失败必出声」治理方向，已立案。

## 场景 2：插件装卸 + 重装（E5.8 核心验收）

| 验证点 | 操作 | 期望 |
|:--|:--|:--|
| 卸载拆净 | 卸载一个活跃插件 | 无残留监听/注册/定时器（#12 实测拆净——disposer 全量回滚） |
| 重装可用 | 卸载后立即重装同插件 | 装载→使用→卸载→重装→再使用全链通（重装契约） |
| 连带卸载 | 卸载被依赖插件 | 消费插件自动 PENDING，不报错（#15）+ toast 提示可见（#15.5） |
| 自动重载 | 重装被依赖插件 | 消费插件自动 ACTIVE（#15） |
| 循环依赖 | 制造 requires 环 | fail-loud 报错，不静默（#14） |
| PENDING 可见 | 制造缺依赖插件（requires 不存在的 ID）→ 启动 | marketplace 显示 PENDING + "等待依赖: xxx" 原因可读（#15.5） |

**回归风险：** Phase 2 是最大推翻项——所有 Registry 从"手动对称"改 disposer 逆序回滚，漏一处 = 卸载残留。卸载/重装本场景必测三轮。

**✅ 实测（2026-08-20，synthetic 夹具 6 件）：**
- 连带卸载：卸载 dep-b → dep-a 自动变黄字「等待依赖: "synthetic-dep-b"」（#15.5 提示可见）✅
- 自动重载：装回 dep-b → dep-a 自动回 ACTIVE，提示消失 ✅
- **三轮重复（#12 拆净验证）**：卸载→装回 ×3，行为一致，无残留堆积 ✅
- 循环依赖：cycle-a/b 互 requires → **fail-loud 报错恰好 1 次/插件，不刷屏**（#24 回归修复 commit 282ec790 生效）✅
- PENDING 可见：missing-dep → marketplace 显示 PENDING + "等待依赖: no-such-plugin-id" ✅
- 卸载拆净 + 重装可用：串口监视器卸载→装回，命令/快捷键/收发全通 ✅
- 循环插件卸载：先卸 cycle-a → b 不报连带警告（b 已 failed 不在活跃集——符合设计），卸 b 正常 ✅

## 场景 3：命令 + 快捷键

| 验证点 | 操作 | 期望 |
|:--|:--|:--|
| 命令面板 | Ctrl+Shift+P | 居中显示，输入过滤，防抖 150ms 不回归 |
| 命令执行 | 面板执行任意命令 | 正常执行 |
| 快捷键 | Ctrl+B / Ctrl+J / F2 等常用快捷键 | 正常触发 |
| 卸载后无残留 | 卸载注册了命令/快捷键的插件 → 触发旧命令/旧快捷键 | 无效——死 handler 清零（#12 后无残留） |
| 齿轮菜单 | 左下齿轮 → 菜单各条目 | 执行正确，无静默 no-op（E5.7#56 教训防回归） |

**回归风险：** Registry register() 改返 disposer——消费方适配漏一处 = 注册泄漏或重复注册。命令/菜单/快捷键三 Registry 是重灾区。

**✅ 实测（2026-08-20）：** 命令面板正常；齿轮菜单各条目执行正确，无静默 no-op；卸载串口监视器后快捷键无残留（死 handler 清零）。

## 场景 4：热更新语义

| 验证点 | 操作 | 期望 |
|:--|:--|:--|
| 修改插件代码 | 改插件文件后保存 | `import.meta.glob` 更新生效（既有决策：vite watch 有意排除 plugins/ 保持不变） |
| 重载插件 | 市场里 Reload | 状态保持 |
| 新装插件 | 装新插件 | 无需重启壳 |

**回归风险：** Phase 4 契约生成改动 preload 面——热更新走的 `/@fs/` 加载通道不得被契约接线影响。

## 场景 5：崩溃恢复五件套（对标 E5.7 场景 8）

| 验证点 | 操作 | 期望 |
|:--|:--|:--|
| renderer 崩溃 | 任务管理器 kill Pool renderer 进程（**注：池是 preload 沙箱，DevTools 无 `process` 全局——`process.crash()` 不可用，用任务管理器 kill） | 2-4s 自动重建 |
| layout 恢复 | 崩溃后 | 标签页/分屏/侧栏/面板全恢复；插件正常 ACTIVE，无 PENDING 假死 |
| Hot Exit | 编辑未保存内容 → 崩溃 | 重建后内容恢复 + dirty 标记 |
| 壳崩 | kill 壳 renderer | 全窗口重建 + tabState 持久化恢复 + lastLayout 兜底 |
| 连续崩溃 | 10s 内 3 次 | 停止重建，显示错误页 |

**回归风险：** E5.8 不碰崩溃恢复链——本场景防连带回归即可（重建后插件正常 ACTIVE，无 PENDING 假死）。

**✅ 实测（2026-08-20）：** 任务管理器 kill 最大 renderer 进程 → 自动重建，插件全 ACTIVE；编辑内容在重建后保留，Ctrl+S 可保存（Hot Exit 通过）。连续崩溃 10s×3 停止重建（此前已测）。

## 场景 6：分屏拖拽

| 验证点 | 操作 | 期望 |
|:--|:--|:--|
| 左右分屏 | 拖 tab 到编辑器右半区 | Glassmorphism 预览，drop 后分屏 |
| 合屏 | 拖 tab 回原 group | 分屏合并 |
| 深度限制 | 4 层分屏后继续拖 | 拒绝第 5 层 |
| 回执对齐 guard | 快速连续拖拽 | 拖拽乐观覆盖的回执对齐 guard 不回归（#5 pushLayout 两规则核对面） |
| 设置 tab 拖拽 | 拖"设置"标签页 | 正常分屏（titlebar drag region 冲突防回归） |

**✅ 实测（2026-08-20）：** 拖 tab 到右半区分屏 + 拖回合屏正常。

## 场景 7：收尾检查（#23 门禁收口 + 本矩阵）

- [ ] #0d.7 + #0d.8 目录归一化防回归全过（8 子步 + 2 子步每步门禁 + 旧路径 grep 清零 + 冒烟映射——执行详情见 [`../目录归一化/01-目标目录树.md`](../目录归一化/01-目标目录树.md) §9 + §10）
- [ ] `npm run check` 全绿（含 Phase 1 新门禁 jscpd/knip + Phase 1.5 ipc-audit + Phase 4 contracts-ready + #6.6 新哨兵 no-hardcoded-hex / no-hardcoded-chinese→error）
- [ ] 基线数字落盘刷新（#23，对照 #0c 基线）
- [x] 进程数 = **5 个正常**（主进程 + 壳 renderer + Pool renderer + GPU + utility）——任务管理器验证；E5.7 消灭的是 O(N) 标签 WebView，剩 2 渲染进程（壳 + Pool）是设计目标（「=3」为旧表述漏算 GPU/utility，已纠正）
- [ ] 收尾 grep 清零：旧手动 unregister 摘除（#12）/ register() 返 void 清零（#10）/ #20 插件侧 @src/core 类型 import 清零 / #6.5 getAllPluginIds 零命中 / 无调试日志残留
- [ ] #6.6 归一化全景审计账本清零（5 唯一方式 grep 实证全零或全豁免）
- [x] #22.5 运行期校验件 dev 冒烟——故意制造版本错配（旧插件 vs 新壳）→ dev 报错可诊断，生产静默不崩——**实测（2026-08-20）**：panel-demo 加 `minAppVersion: "99.0.0"`（> 壳 3.0.0）→ 重启 → toast「需要应用版本 ≥99.0.0（当前 3.0.0），已跳过」+ 该插件不 ACTIVE + 其余照常；删除字段恢复 ACTIVE ✅
- [x] 数据推流回归（#6.5）——串口实机冒烟 serial data/stats/system 三通道不断流 + 文件变更推送文件树刷新 + LSP 推送编辑器无报错——**实测（2026-08-20）**：三通道细验不断流 ✅ / 文件变更推送（外部新建+改文件→文件树刷新）✅ / LSP 推送（.py/.ts F12 跳转正常，pyright 修复不反弹）✅
- [x] F5 刷新——会话恢复、布局恢复——**实测（2026-08-20）**：含分屏 + 未保存编辑 → F5 → 会话/布局恢复 ✅
- [x] 设置页"以 JSON 打开"回归（#0d.5 修复后不反弹——打开编辑器标签页而非弹窗）——实测：设置 JSON 打开、改主题正常（打开的是编辑器标签页）

## 场景 8：底部面板全链（E5.8#36 收口——2026-08-21）

> 对应清单任务 **E5.8#36**（[../E5.8-执行清单.md](../E5.8-执行清单.md) Phase 7）。E5.7 矩阵场景 5 ①③（Ctrl+J 打开 / [+] 新建）为已知未做项——E5.8 Phase 7 已补（#31/#32），本场景 = 面板全链回归收口。**实机验证 = 用户验收（五行待实测）**，本表列代码侧回归结论 + 用户实测路径。

| 验证点 | 操作 | 期望 | 状态 |
|:--|:--|:--|:--|
| 面板贡献链零壳改动回归（#63.7 实证流程复跑） | panel-demo 声明 `contributes.views location:"panel"` → 启动 | 面板出现其标签，池代码零改动——链路：parseContributions → ViewContainerService.registerView（_pluginId/_renderPath 附挂）→ usePoolSync buildPanelViewMetas（getViewContainers("panel") + getViews 展平 → PanelViewMeta）→ pushLayout panel.views → 池 PanelZone PluginComponent 按 renderPath O(1) 动态加载 | ✅ 链路代码侧复核全在位（sidebar-panel.ts:46 buildPanelViewMetas 同 #63.7 原样）；机制零改动（#31-#35.5 全走同一注册面） |
| 显隐命令 ① | Ctrl+J | 面板开/关切换（#31 `workbench.action.togglePanel`） | ✅ 实现（commit `0fe467a9`）；✅ 用户实测通过 2026-08-21 |
| 新建视图 ③ | 面板 [ + ] | QuickPick 视图选择器，选中即新建（#32） | ✅ 实现（commit `0966eb99`）；✅ 用户实测通过 2026-08-21 |
| 容器切换器 | 面板标签行首 switcher | 分组下拉切换容器 + 勾选显隐落盘（#34） | ✅ 实现（commit `e22cd43a`）；✅ 用户实测通过 2026-08-21 |
| 菜单栏「面板」 | 菜单栏 → 面板 | 显隐切换 + 插件声明的 panel 菜单归并（#33） | ✅ 实现（commit `35570599`）；✅ 用户实测通过 2026-08-21 |
| reveal 实测 | 插件调 `linkdesk.panel.reveal(viewId)`（终端视图） | 面板隐藏→展开切视图 / 已显示→切换聚焦；无贡献 no-op（#34.5） | ✅ 单测 4 例全绿（panelReveal.test.ts）；✅ 用户实测通过 2026-08-21 |
| moveToEditor 实测 | 插件调 `linkdesk.panel.moveToEditor(viewId)` 或面板视图 tab 右键「移至主区标签页」 | 视图变标签页（活动 group 尾部）+ 面板内移除（#35.5） | ✅ 单测 4 例全绿（panelMoveToEditor.test.ts）；✅ 用户实测通过 2026-08-21 |

**✅ 代码侧回归结论（2026-08-21）：** ①③ 两行由 E5.7 已知未做项改写为已实现（E5.7 矩阵场景 5 + E5.7#88 记录同步更新）；reveal/moveToEditor 8 单测全绿；面板贡献链零壳改动（机制全走 #63.7 注册面，Phase 7 零新注册面）。**实机验证五行（显隐/切换/新建/拖高/Toast 位置 + reveal/moveToEditor 两条 API 实测）= 用户验收项——2026-08-21 用户实测通过，验收闭环。**

## 场景 9：壳内悬浮面板全链（E5.8#41 收口——2026-08-21）

> 对应清单任务 **E5.8#41**（[../E5.8-执行清单.md](../E5.8-执行清单.md) Phase 8）。类型 B 悬浮面板 = #37 三件套 + #39.5 声明制（contributes.floatingPanel + `panel.revealFloating`）+ #38 settings 声明接入 + #40 变量化/语言纯净。**实机验证 = 用户验收项（本表状态待实测）**，代码侧结论标 ✅。

| 验证点 | 操作 | 期望 | 状态 |
|:--|:--|:--|:--|
| Ctrl+, 弹面板 | 菜单栏 齿轮 → 设置（或 Ctrl+,） | 设置视图以壳内悬浮面板弹出——**#41.6 默认居中大卡 85vw×80vh（淡入 + 微缩放 scale(0.96)→1，250ms）**；settings 声明 `floatingPanel.viewId=settings`，`core.openSettings` 声明寻址弹面板 | ✅ 实现链全在位（#38 `eae55a67` + #41.6 `97a72fe9`）；⚠ 待用户实测 |
| 面板身份开关键替换语义（I8-2） | 面板开着时再调 `revealFloating`：同视图 / 他视图 | 同视图 → 关（toggle）；他视图 → 替换内容；无面板 → 开 | ✅ 决策纯函数 `decideFloatingPanelReveal` 单测 3 例全绿（floatingPanelReveal.test.ts）；⚠ 替换语义待实测 |
| 面板↔标签页互转（I8-4） | 面板动作「在主窗口中打开」 | 面板关闭 + 视图落当前活动 group 尾部为标签页（复用文件树打开落点规则） | ✅ open-in 门控（appearsIn.tabBar + entry）+ settle 'open-in' → openTab 实现（#38）；⚠ 落点待实测 |
| 右键回程按声明出现（I8-3） | 标签页右键 | 声明 `contributes.floatingPanel` 的插件（settings / floating-panel-demo）出现「在悬浮面板中打开」；未声明不出现 | ✅ viewRegistry `getFloatingPanelViewId` 4 例 + IpcBridgeHandler 动态注入（#39.5 子项 C）；⚠ 实机目测 |
| 拖拽 resize（I8-5/I8-7） | **#41.6 整条标题栏**（动作按钮区排除不误触）/ 底部 resize 条 | 移动拖拽投影抬升（**无半透明**——用户 2026-08-22 否决 opacity 0.85）；调高底部 8px 手柄 | ✅ 池 CSS/host 实现（#37 + #41.6 标题栏拖拽 + actions 排除单测）；⚠ 拖拽手感待实测 |
| 最大化还原 toggle（I8-9） | 点面板动作 □ / ⤡ | 铺满（钳制 inset）+ 图标/文案两态 toggle（纯池本地视觉态） | ✅ 实现（#37）+ 单测断言两态字段（floatingPanelReveal.test.ts）；⚠ 实机目测 |
| 拖拽防误关（I8-6） | 拖拽/调高松手落在遮罩 | 松手一拍内（setTimeout 0）不触发遮罩点击关闭 | ✅ suppressRef 实现（FloatingPanelHost.tsx 起手势 + 遮罩 onClick）；⚠ 实机目测 |
| 主题跟随 | 切换暗色/亮色主题，面板开着 | 面板背景/边框/标题栏/遮罩/投影跟随主题（#40 token：`--floating-panel-backdrop/shadow/shadow-dragging` 暗亮双定义） | ✅ #40 commit `eaeac992`；⚠ 亮色观感待实测 |
| 语言切换文案重推 | 面板开着时 Ctrl+K Ctrl+L 切语言 | 面板标题栏 title + 动作文案跟随切换（内容区本来就切——插件视图 useTranslation 自渲染）；壳 `refreshPanelText` 重推 DTO（`refresh` 标记 → 池不抢焦点） | ✅ 2026-08-22 点修③：FloatingPanelService.refreshPanelText + 契约 `refresh` 字段 + 池 skip-focus + 单测 6 例；⚠ 实机目测 |
| open-in hover 展开全文（去硬编码） | 英文态 hover「在主窗口中打开」动作 | 全文完整展开——`width: max-content` 内容自适应，不硬编码 128px（英文「Open in Main Window」不再截断） | ✅ 2026-08-22 点修②：FloatingPanel.css open-in max-content / label max-width:none；⚠ 实机目测 |

**✅ 插件 API 面复核（#41——contributes.floatingPanel + revealFloating 两建设面）：**
- `contributes.floatingPanel`：schema（plugin.schema.json:618）+ contributions.ts 消费 + 文档（03 §3.15 完整 + 06:119）✅
- `panel.revealFloating`：PanelAPI 类型（linkdesk-api/panel.ts:20）+ preload-pool + channel `panel:reveal-floating` + ipc-bridge + IpcBridgeHandler/panel + ShellEvents + App useFloatingPanelReveal 全链在位 ✅；**本次补齐文档缺口**——01-插件API契约.md panel 行原只记 `panel.reveal`（#34.5），补 `revealFloating` 契约（声明寻址/身份开关键/默认动作）
- 卸载声明插件 → 右键条目消失 + revealFloating no-op 不崩（#39.5 验收）✅ 声明随生命周期卸载（registrationTracker LIFO）

**⚠️ 实机验证八行（Ctrl+, 弹面板 / 开关键替换 / 互转落点 / 右键回程 / 拖拽 resize / 最大化 toggle / 拖拽防误关 / 主题跟随）= 用户验收项——待用户实测后勾选。**

**① 复测「最大化只剩左半」（E5.8#41.7——#41.5 内容修复后复测，发现并修复真实几何根因）：** 两层根因——① 错误内容（viewId "settings" 碰撞 #41.5 正主，Ctrl+, 渲染串口收发设置）；② **真实几何 bug：`.floating-panel-body` 是 flex-row，插件视图为单 flex 子项（`flex:0 1 auto` = 内容宽）不横向伸展 → 即使内容正确也只占面板左半**（最大化几何本身正确——`{top/left/right/bottom:6px, width:"auto"}` position:fixed 铺满窗口减 12px；FloatingLayerHost 根 `position:fixed; inset:0` 无 transform，containing block = 视口）。**修复（#41.7）**：`.floating-panel-body` 改 `flex-column`（侧栏 SidebarZone 同款模式）——cross-axis stretch 让视图铺满面板宽。**代码侧已闭环（#41.5 内容 + #41.7 几何），实机复测待用户勾选。**

---

> 📖 执行清单 → [../E5.8-执行清单.md](../E5.8-执行清单.md) Phase 5
> 📖 E5.7 对标（原十场景原文）→ [`../../E5.7_极简Pool/回归验证/全量回归矩阵.md`](../../E5.7_极简Pool/回归验证/全量回归矩阵.md)
