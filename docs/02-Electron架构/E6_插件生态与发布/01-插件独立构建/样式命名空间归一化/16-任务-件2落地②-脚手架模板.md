# 任务 `#109h-b②`：件 2 落地·**脚手架模板**——第 1.14② 轮

> **一句话**：把脚手架模板的示例类名改成 `{{pluginName}}-starter*`，**让新插件一建出来就合规**——否则作者拿到手的第一个工程就会红在自己的 CI 上。
> **前置**：[① SDK 判据腿](15-任务-件2落地①-SDK判据腿.md) ✅（模板必须先于任何仓改名——新产物不能一边出生一边违规）。
> **规模**：模板 2 文件 ＋ `check-scaffold` 断言 1 条 ＋ `create-linkdesk-plugin` **发版**。
> **提交轴**：`packages/create-linkdesk-plugin`（发版）＋ 壳仓 `scripts/check-scaffold.mjs`。
>
> **非新能力声明（`check-design-flow` §8.4 ③）：** 本档是**示例代码与门禁断言的同步改动**，不新增作者面能力。⇒ **无需新能力设计前置**。

---

## 一、改什么（**实测**：模板里只有 2 个文件提到示例类名）

| 文件 | 处 | 现状 |
|:--|--:|:--|
| `packages/create-linkdesk-plugin/template/src/index.css` | **5** | `.starter`（:7）· `.starter__title`（:12）· `.starter__text`（:17）· `.starter__hint`（:22）· `.starter__hint code`（:28，**后代选择器**） |
| `packages/create-linkdesk-plugin/template/src/index.tsx` | **6** | `className="starter"`（:23）· `starter__title`（:24）· `starter__text`（:25）· `starter__hint` ×3（:26 / :29 / :32） |

**改成**：`{{pluginName}}-starter` / `{{pluginName}}-starter__title` / `{{pluginName}}-starter__text` / `{{pluginName}}-starter__hint`（＋后代选择器那句同步）。

> ✅ **零新机制**：占位符 `{{pluginName}}` 已存在，且 CLI 的 `replacePlaceholders()` 是**递归替换模板目录下所有文件**（`index.js:165–178`，`pluginName` = 用户的插件名 = `pluginId`）。⇒ 不需要给 CLI 加任何逻辑。

🔴 **同时改的那一条**：`template/plugin.json` 里给 `pluginId` 补一句「**不得以 `ldk-` 开头**」的说明（拍板 Q4）——它是**实例注释**（JSONC），改了不影响 schema。

⚠️ **检查一下模板里还有没有别的裸类名/关键帧**：跑 `node scripts/plugin-css-prefix-audit.mjs <生成物目录>`（工具在 [①](15-任务-件2落地①-SDK判据腿.md) 交付）——**以工具读数为准**，上表是 2026-09-15 的实测快照。

---

## 二、`check-scaffold.mjs` 补一条断言

**新增「断言 11：生成物零裸类名/关键帧违规」**——**不许在断言里重写判据**：调用 [①](15-任务-件2落地①-SDK判据腿.md) 的审计工具（或直接 import SDK 的同一条实现），断言「违规数 == 0」。理由与断言 10 同款：

> 🔴 **判据必须与门禁同源**（文件头那句「两处各写一份正则 = 必然漂移」）。这里再抄一份 = 给下一次漂移埋钉子。

**断言必须能被负控推翻**：`--self-test` 里加一例——把 `template/src/index.css` 的 `.starter` 临时改回无前缀（桩模板）⇒ 断言**红**；还原 ⇒ 绿。**恒真的断言等于没有断言**（照断言 10 的先例：**真变异实测**，不靠推理）。

---

## 三、发版（`create-linkdesk-plugin` PATCH）

- bump `packages/create-linkdesk-plugin/package.json` ＋ `CHANGELOG` 同笔（硬约束 22）。
  - ⚠️ **实测补正（2026-09-15，本格执行时）**：**这半条不适用**——`packages/create-linkdesk-plugin/` **没有 `CHANGELOG.md`**，而 `check-changelog-section.mjs` 只读**仓根** `package.json` ↔ 仓根 `CHANGELOG.md`。硬约束 22 的 CHANGELOG 规矩是**软件轴**的；作者轴照 [作者轴npm发版.md](../../../../06-发布管理/作者轴npm发版.md) 五步（bump → publish → `release:mark` → 提交 version ＋ state.json），**没有 CHANGELOG 这一步**。⇒ 本格只 bump **0.1.9 → 0.1.10**（PATCH）。
- 刷 lock：`npm install --package-lock-only`（否则 `check:lockfile-sync` 红）。
- `git add` 后、`npm publish` **前**真跑一次 `npx lefthook run pre-commit`（runbook 坑④：尾空行）。
- `cd packages/create-linkdesk-plugin && npm publish --registry=https://registry.npmjs.org`（**不要** `npm publish <folder>`，坑②）。
- ⚠️ **`npm publish` 要用户点头**；货架约 3 分钟复制延迟后 `npm run release:mark`。
  - ✅ **已被 2026-09-15 用户授权覆盖（1.14① 轮同笔标注）**：CLI 与 SDK 同属作者轴 `npm publish`，**在授权内**（原话「同意发版，无论是软件层面还是 npm 那里，亦或是插件层面」）⇒ **不要因这一句停下来问**。仍要守的是纪律：推前现测代理 / runbook 五步 / 货架核对 / `release:mark`；⛔ **撤版不授权**。权威表述见 [交接.md 顶部 §〇 授权表](交接.md)。
  - 🔴 **实测补正（本格执行时）**：`release:mark` **本格记不进去**——它是**整批原子**的，被 `@linkdesk/plugin-sdk` / `@linkdesk/plugin-docs`（「内容变了、版本没动」）双双拒（真跑 EXIT=1、`state.json` 零改动）。**刻意不用 `--allow-drift`**（那会清掉 ⑧ 要用的两盏提醒）⇒ 把记基线**整体留给 ⑧**（那时三根轴都是「版本动过」，一次记齐）。实测货架翻面约 **100s**（不是 3 分钟）。
- **货架保真**（不是看版本号就算）：从真货架真跑一次生成，**解包核生成物里零裸类名**：
  `npm create linkdesk-plugin@latest <tmp> -- --no-git` ⇒ 跑审计工具 ⇒ 违规 0。
  - ✅ **实测**：16 文件 / 审计 **0 违规** / `plugin.json` 的 `ldk-` 注释在——**16 文件**这条顺带证「不是 npx 旧缓存」（旧缓存那份只有 7 个文件）。

---

## 四、判据（逐条给读数）——**2026-09-15 实测勾注（第 1.14② 轮，提交 `bee163dcc`）**

- [x] `node scripts/check-scaffold.mjs` 绿（含新断言 11）；`--self-test` 绿（含新负控）
      ⇒ ✅ 门禁绿（**16 文件 / 7 命令 / 零裸类名·关键帧**）；`--self-test` **6/6**（新 3 例：真生成物 0 违规 · 真变异 ⇒ **红 4 条**且报点带「应以 `scaffold-probe-` 开头」· 还原 sha256 **逐字节相同**）
- [x] 真生成一次（仓外临时目录）⇒ 新类名（`<id>-starter*`）在产物 CSS/TSX 里齐、`starter` 裸名 **0**
      ⇒ ✅ `E:\linkdesk-build+scratch\109hb2-probe`：CSS 5 处 ＋ TSX 6 处全是 `.{{pluginName}}-starter*`，裸 `starter` 0 命中
- [x] `npm create linkdesk-plugin@latest` **从真货架**跑出来的产物同样 0 违规（§三）
      ⇒ ✅ `create-linkdesk-plugin` **0.1.10** 已发（约 100s 货架翻面）；真货架生成物 = **16 文件**（= 非 npx 旧缓存）/ 审计工具 **0 违规** / `plugin.json` 的 `ldk-` 注释在
- [x] `check:lockfile-sync` 绿 · `check:npm-release` **黄灯灭**（本格**要**发版，与 ① 不同）
      ⇒ ✅ lockfile-sync 绿（7 manifest 同源）；⚠️ **「黄灯灭」这条未达成**——**`release:mark` 本格记不进基线**：整批原子，被 `@linkdesk/plugin-sdk` / `@linkdesk/plugin-docs`（内容变了、版本没动）双双拒（真跑 EXIT=1、`state.json` 零改动）。**刻意不用 `--allow-drift`**（那会清掉 ⑧ 要用的两盏提醒）⇒ **交 ⑧ 一次记齐**。收尾时 CLI 那盏灯从 A「内容漂移」变成 B「bump 了但基线没记」= **设计内的排队，不是漏项**
- [x] bump ＋ `CHANGELOG` 段非空（`check-changelog-section` 绿）
      ⇒ ✅ bump **0.1.9 → 0.1.10**（PATCH，作者轴）＋ lock 同笔；⚠️ **`CHANGELOG` 这半条对本轴不适用**——`packages/create-linkdesk-plugin/` **没有 `CHANGELOG.md`**，而 `check-changelog-section.mjs` 只读**仓根** `package.json` ↔ 仓根 `CHANGELOG.md`（硬约束 22 的 CHANGELOG 规矩是**软件轴**的；作者轴照 [作者轴npm发版.md](../../../../06-发布管理/作者轴npm发版.md) 五步，没有 CHANGELOG 这一步）。仓根 `check-changelog-section` 绿（壳未 bump）
- [x] `npm run check` **EXIT=0**（贴末尾三行）
      ⇒ ✅ **EXIT=0**（**156 文件 / 2,115 例** / 24.40s；`Test Files 156 passed` · `Tests 2115 passed` · `Duration 24.40s`）

## 五、负控（**2026-09-15 实测**）

| # | 注入 | 期望 | 实测 |
|:--:|:--|:--|:--|
| NC1 | 模板 CSS 里把 `{{pluginName}}-starter` 改回 `.starter` | 断言 11 🔴 红（报点含文件 ＋ 类名 ＋「应以 `<id>-` 开头」） | ✅ **真跑**：红 1 条 / 报点 4 行（`src/index.css:10,15,20,25`），每行 = 文件＋行＋现名＋建议名＋「应以 `scaffold-probe-` 开头」 |
| NC2 | 同上，还原 | 绿（且逐字节还原） | ✅ 还原后门禁绿，sha256 与变异前**相同**（自测里另有一条常驻的逐字节还原判据） |
| NC3 | 模板 TSX 只改 CSS 不改渲染点（反向） | 断言 11 **不红**（它只管 CSS）+ **token 判据红** ⇒ 证「渲染点判据」是另一条腿（本格不引入，但要知道边界） | ✅ **真跑**：只把 TSX 的 `className` 改回裸名 ⇒ 门禁 **EXIT=0**（不红）——边界确认，已写进 `check-scaffold.mjs` 文件头与断言注释 |

> ⚠️ **一处口径更正（实测）**：NC1 期望的条数写的是「`starter` 那 5 处」，实测判据只报 **4** 条——模板 CSS 里类名出现 5 处，但 `…-starter__hint code` 是 **scoped 后代选择器（不占名）**，不在「裸定义」射程内。⇒ 自测把期望**显式写死成 4**，并在注释里写明这个数的来历（与 ① 的「两把尺子」是同一条口径）。
> ⚠️ **另一处实测补正**：断言 11 依赖 SDK 的 `dist`（判据不重写 ⇒ `require` 腿的同一个导出）。dist 缺失或比 `packages/plugin-sdk/src` 旧时**先构建**（≈2s，仅在缺失/过旧时付），对齐 `scripts/build-linkdesk-ui.mjs` 的「CI 与本地走同一条路」；**实测**：把 `packages/plugin-sdk/dist` 整个移走 ⇒ 自动构建后仍绿。⇒ CI（fresh clone）无需新增构建步骤。

## 六、提交与收尾

- 提交信息：`feat:E6#109h-b② 脚手架模板示例类名带 pluginId 前缀 + check-scaffold 断言 11（与判据同源）`。
- 收尾两件：交接.md 顶部追加 ＋ 清单轮次进度与格子勾选（**含 §四 全部框**）。
- 🔴 下一格是 **③ `file-tree` 改名**（[17 号通用作业书](17-任务-件2落地③-⑦-改名轮通用作业书.md)）。

> ✅ **本格执行实录（2026-09-15，第 1.14② 轮，提交 `bee163dcc`）**：面 **7 文件 / 236 增 36 删** = 模板 3（`index.css` 5 处类名 ＋ 头注补**第 4 条纪律**「类名一律以 `{{pluginName}}-` 开头」——那句「lint 会照着下面 N 条查」的清单必须与实际相符 · `index.tsx` 6 处 `className` · `plugin.json` 给 `pluginId` 补「不得以 `ldk-` 开头」）＋ `scripts/check-scaffold.mjs`（断言 11 ＋ 3 例真变异自测 ＋ dist 懒构建）＋ `packages/create-linkdesk-plugin/package.json`（**0.1.10**）＋ `package-lock.json` ＋ 工具链档 [01-create-linkdesk-plugin脚手架.md](../../02-插件开发工具链/01-create-linkdesk-plugin脚手架.md)（**它逐条抄着模板的示例与那段头注** ⇒ 不同笔改就是「文档教旧名」）。**禁区全守**：视觉/语义一字未动（只改标识符 ＋ 注释）、CLI 零新占位符（连 `index.js` 一行都没改）、判据没重写（`require` SDK 腿的同一个导出）、货架保真真跑了。完整读数与新事实（`release:mark` 整批原子 ⇒ 本格记不进基线、CLI 轴没有 CHANGELOG、5 处 vs 4 站点、断言 11 的射程边界、并发写手）见 [交接.md 顶部本节](交接.md)。

## 七、禁区

1. **不许改模板的视觉/语义**（只改类名标识符；颜色、间距、字号一字不动——硬约束 16 不触发，若发现必须改视觉 ⇒ 停下先调 design skill）。
2. **不许给 CLI 加新占位符**（`{{pluginName}}` 已够）。
3. **不许把判据复制进 `check-scaffold.mjs`**（同源）。
4. **不许跳过货架保真**（「本地生成绿」不等于「作者拿到的是新版」——7.9 那次 npx 缓存暗礁就是这么踩的）。
