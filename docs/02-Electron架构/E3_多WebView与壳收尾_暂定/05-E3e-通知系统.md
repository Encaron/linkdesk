# E3e — 通知系统全功能

> 2026-07-24。从旧 P7d 拆分——通知系统是壳级能力，独立于 Profile。
> **性质：** 纯 TS/React，框架无关。Phase 4 建了 Toast 队列，Phase 5 建了 LogChannel——现在是完整版。
> **依赖：** E3a（多 WebView——插件通过 IPC 发通知，NotificationService 在壳 WebView 运行）

---

## 一、通知进度条

```typescript
showProgress(title: string, options?: { cancellable?: boolean; total?: number }): ProgressHandle
interface ProgressHandle {
  report(increment: number, message?: string): void;
  finish(message?: string): void;
  cancel(): void;
}
```

状态栏下方渲染进度条组件。对标 VS Code `vscode.window.withProgress`。

---

## 二、通知来源过滤 / Do Not Disturb

```typescript
setDoNotDisturb(enabled: boolean): void;
setSourceFilter(pluginId: string, enabled: boolean): void;
```

plugin.json 可声明通知来源：
```json
{ "contributes": { "notificationSources": [{ "id": "terminal.portErrors", "label": "串口错误", "defaultEnabled": true }] } }
```

---

## 三、"Don't show again" 持久化

通知按钮 `isCloseAffordance: true` → localStorage 持久化 → 下次不弹。

---

## 四、完整 Notification Center 面板

铃铛图标 → 通知列表（未读/已读、按时间排序、按 source 分组）。

---

## 五、通知 source 归类

`getNotificationsBySource(): Map<string, Notification[]>` → 面板按插件分组渲染。

**多 WebView 注意事项：**
- NotificationService 在壳 WebView 中运行
- 插件通过 IPC 发通知：`ipc.notify({ title, message, source: pluginId })`
- Notification Center 面板在壳 WebView 中渲染

---

## 六、任务清单

| # | 任务 | 行数 | 独立验证 |
|:--:|------|:--:|------|
| 46 | 通知进度条——showProgress + ProgressHandle | ~50 | 长操作 → 状态栏进度条 → finish 消失 |
| 47 | 通知来源过滤 / Do Not Disturb | ~40 | 关掉 terminal 通知 → terminal 的 toast 不弹 |
| 48 | "Don't show again" 持久化 | ~10 | 关掉某通知 → 重启 → 仍然不弹 |
| 49 | 完整 Notification Center 面板 | ~100 | 铃铛图标 → 通知列表 → 按时间/已读过滤 |
| 50 | 通知 source 归类（按插件分组） | ~30 | 面板里 terminal 的通知和 marketplace 的分开显示 |
| **合计** | | **~230 行** | |

---

## 七、验证标准

```
进度条 → 长操作有进度条 → 可取消 → finish 消失
来源过滤 → 关掉某插件的通知 → 该插件的 toast 不再弹
DND → 全局静默 → 所有 toast 不弹
持久化 → "不再提示" → 重启后仍然不弹
Center → 铃铛 → 历史通知可查看
```

---

> **← 上一份：** `04-E3d-Profile与激活.md`
> **→ 下一份：** `06-E3f-壳UI收尾.md`
> **E3 索引：** `00-README.md`
