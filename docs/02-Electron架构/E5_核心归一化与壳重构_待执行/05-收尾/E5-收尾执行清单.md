# E5-收尾执行清单——精益求精

> 2026-08-07。**E5 78 任务全部完成。** 盲审 82 分 → 18 分扣分项 + 6 条独立建议 → 39 任务，5 层 9 轮。
> **🔥 进度唯一真相源。** 做完一个勾一个。实现细节见 `05-收尾/` 下专题文档——本清单只列任务+文件+行数+依赖+验证。
> **已完成 35/39 项（E5#87-120，除 #112-#113 移至 E6）。** 第 1-4 层全部完成。运行时 import 27 处留 E5.5（审计表 → [[e5-120-audit-plugin-core-imports]]）。
> **E5#109 ✅——** 方案 B（渲染时 `t()` + 每插件 `i18n/` 目录）。12 子任务全部完成。
> **E5#111 ✅——** VSCode 系统对照文档（12 类 130+ 术语）。`eafeffe`
> **E5#105 跳过。** 🐛 Monaco 暗色字体+options 竞态——E5-adj `427217c`+`a129288` 根治（optionsRef 桥接）。见 [[monaco-editor-options-not-applied]]。

---

## 🔥 关键路径与并行度

```
第 1 层：阻断级修复（串行——必须先做，因为影响所有后续验证）
  E5#87 生产路径（无依赖）→ E5#88 静默吞错（无依赖）∥ E5#89 类型安全（无依赖）
  → E5#90 工具链（无依赖）→ E5#91 裸 hex（无依赖）

第 2 层：Bug 修复（第 1 层完成后可并行——不同文件无冲突）
  E5#92 预览标签页 ID（依赖 #89——用上 window.linkdesk 类型后改）
  → E5#93 右键菜单遮挡 ∥ E5#94 右键菜单定位 ∥ E5#95 未知文件零反馈
  → E5#96 OverlayPortal（依赖 #93——先修 ContextMenu，再抽象通用层）

第 3 层：UI 归一化（无依赖——随时可做）
  E5#97 间距 token ∥ E5#98 z-index token ∥ E5#99 未知文件治本化
  ∥ E5#100 Emoji Lucide ∥ E5#101 语言按钮

第 4 层：架构债清理（无依赖——随时可做）
  E5#102 v3 遗留 ∥ E5#103 IPC 引用计数 ∥ E5#104 PluginId 注释 ∥ E5#105 记忆归档
  ∥ E5#106 ESLint 中文检测

第 5 层：测试 + 功能 + 文档（所有上层完成后验证）
  E5#107 App 集成测试（依赖 #88 #89——错误处理+类型就绪后写测试）
  → E5#108-#110 功能补全（无依赖，可穿插）
  → E5#111-#113 文档（随时）
  → E5#114 回归验证（所有任务完成后）
```

**最长串行链：** E5#87 → #92 → #93 → #96 = 4 步串行。其余全部可并行。并行扇出最大 = 8（第 4 层）。

**不可逆点：**
- E5#89b——`global.d.ts` 创建后 `window.linkdesk` 类型生效。如果 LinkDeskAPI 类型和实际 preload 注入不一致 → tsc 报错。checkpoint：`npx tsc --noEmit` 零新增错误再继续
- E5#98——z-index token 替换时如果相对顺序搞反 → 弹窗跑到 backdrop 下面。checkpoint：每换一个 token 手动验证该 overlay 正常再换下一个

---

## 当前代码状态（新 AI 进场须知）

```
已完成（E5）：
  ShellEvents 类型系统 ✅      LayoutEngine position:fixed ✅
  四区域解耦 ✅                三通信机制（broadcast/requestToPlugin/p2p）✅
  ClipboardProvider 归一化 ✅   InlineInput 归一化 ✅
  ErrorService 归一化 ✅       loadPluginLifecycle 唯一入口 ✅
  appearsIn 声明式 UI ✅       i18n src/ 42 处 ✅

待修（E5-收尾）：
  electron/main.ts L96             ← loadFile 打包路径差一级（生产白屏）
  src/ 10+ 处 .catch(() => {})    ← 静默吞错
  src/ 20+ 处 (window as any)     ← 类型安全缺口
  src/components/shared/ContextMenu.tsx ← 层叠上下文遮挡 + 预估定位
  src/**/*.css 4 处裸 hex + 10 处裸 z-index  ← 硬编码
  src/ ~12 处 emoji 图标           ← 违反 UI/UX Pro Max
  plugins/ ~180 处 i18n 遗漏      ← plugin.json + EditorTab + 主题 JSON
  shared/types.ts                  ← ESLint 未覆盖（已修 ✅）
```

**已安装防线（E5 建立，E5-收尾继承）：**
- ESLint 9 条自定义规则 + `import-x/no-unresolved` + `no-restricted-imports`（plugins→core）
- tsc `strict: true`
- `RegistryBase`——unregisterAll 自动对称
- 路径归一化 `normalizePath()` / `joinPath()` / `getAssetPath()`
- `isEditableElementFocused()` DOM 直检——Ctrl+C/V/A/Z 永不拦截
- `reportError()` 双轨输出（console.error + toast）

---

## 🔴 致命级 Bug 预判——盲审 + E5 历史教训

> 每个风险有精确的根因、触发条件、预防代码。**不是"注意一下"——是具体的代码防线。**

### Bug 收尾-1 🔴🔴🔴 打包后白屏——loadFile 路径差一级

**根因：** E5#23 `electron/tsconfig.json` `rootDir: "." → ".."` 后 `__dirname` 多一层 `electron/`，`../dist/index.html` 变成 `dist-electron/dist/index.html`——实际在 ASAR 根目录。

**🔥 防线：**
```typescript
// electron/main.ts:96
mainWindow.loadFile(path.join(__dirname, '../../dist/index.html'));
```

### Bug 收尾-2 🔴🔴 预览标签页替换致同 ID 碰撞

**根因：** `useTabManager.ts` L230 复用旧 tab `id`，`generateId` 基于 filePath 生成确定性 ID。旧文件再打开 → 同 id 碰撞。

**🔥 防线：** 删 `id: previewTab.id` 覆盖 + Step 4 后 ID 碰撞检测。

### Bug 收尾-3 🔴🔴 层叠上下文隔离——所有 overlay 都可能被遮挡

**根因：** App.tsx zone wrapper `position:fixed` + zIndex 创建独立层叠上下文。子元素 z-index 无法穿越。

**🔥 防线：** ContextMenu → Portal（治标）→ OverlayPortal 通用组件（治本）。

### Bug 收尾-4 🔴 initAll 半残启动

**根因：** init 链 7 步异步串行——任一步 reject → 后续静默跳过。

**🔥 防线：** `unhandledrejection` 全局监听 + App 集成测试 mock 每步失败。

---

## 第 1 层：阻断级修复——铁轨

> ⚠️ **必须最先做。** 打包白屏是阻断级，静默吞错让后续调试瞎眼，类型安全是所有重构的基础。

---

### 第 1 轮：生产路径 + 可观测性

> 📖 **专题文档：** [`Bug修复/生产环境路径审计.md`](Bug修复/生产环境路径审计.md)、[`架构债/静默吞错审计.md`](架构债/静默吞错审计.md)

#### E5#87 🔴 生产环境路径修复

> 🐛 **致命Bug 收尾-1:** 打包白屏——E5#23 `rootDir: "." → ".."` 后 `__dirname` 多一层 `electron/`，`loadFile` 路径差一级。防线：`../../dist/index.html`。
> ✅ **E5#87a 已完成。** E5#87b 已完成。

- [x] **E5#87a** `electron/main.ts:96`——`loadFile` 路径 `'../dist/index.html'` → `'../../dist/index.html'` | ~1 行
- [x] **E5#87b** `electron/main.ts:57`——icon 路径验证：`isDev ? ../../build/icon.ico : process.resourcesPath + 'icon.ico'` + electron-builder.yml `extraResources` | ~3 行 + 配置
**验证：** `npm run electron:build` → 启动 exe → 窗口正常渲染（非白屏）+ 任务栏图标为 LinkDesk 图标。

#### E5#88 🟡 静默吞错审计

> 🐛 **致命Bug 收尾-4（与 E5#107 共同防御）:** initAll 半残启动——init 链 7 步异步串行，任一步 reject → 后续静默跳过。E5#88d `unhandledrejection` 全局监听 + E5#107 集成测试共同防御。

- [x] **E5#88a** 全项目 grep `.catch(() => {})` 和 `catch {}` → 列出所有静默吞错位置——42 处 | 审计 ~10min
- [x] **E5#88b** 数据操作类+文件树+插件通信（持久化/配置读写/文件操作/requestToPlugin/getChildren）→ `console.error("[模块] 描述:", e)`——34 处 | ~34 处
- [x] **E5#88c** 清理代码类+错误隔离（unregister/cleanup/dispose/unwatch/notifyReady/请求队列）→ 保留静默，加注释 `// 非关键操作，失败不阻塞` | 8 处
- [x] **E5#88d** `src/App.tsx`——加 `window.addEventListener("unhandledrejection", ...)` 全局监听，`reportError({ silent: true })`（console 不 toast）| +8 行
**验证：** `grep "catch\s*{" src/ electron/` → 仅清理代码（有 "非关键" 注释）或 console.error。

#### E5#89 🔴 window.linkdesk 类型安全

> ⚠️ **不可逆点已通过。** `npx tsc --noEmit` 零错误。

- [x] **E5#89a** 审计 `src/core/api/linkdesk-api.ts`——18 命名空间→扩展至 26。补 `bridge`/`filesystem`/`path`/`plugins`/`pluginManager`/`window`/`workspace`/`getFilePath`/`pluginViews`(`getAllIds`/`setVisible`/`setBounds`/`destroy`/`toggleDevTools`)/`dialog.open`/`contextKey._getValue`。`unknown`→`any` 兼容存量代码 | 审计 + 类型扩展
- [x] **E5#89b** 新建 `src/types/global.d.ts`——`Window.linkdesk: Record<string, any>`（渐进策略——先消除 `(window as any)` 模式，后续收窄到精确 `LinkDeskAPI`）| +10 行
- [x] **E5#89c** 逐文件替换——`(window as any).linkdesk` → `window.linkdesk`——19 文件 ~33 处 | ~33 处
- [x] **E5#89d** `vitest.setup.ts` mock 自动兼容 `Record<string, any>`——385 pass | 零行
**验证：** `npx tsc --noEmit` 零错误 ✅。`grep "(window as any).linkdesk" src/` → 仅注释 ✅。

#### E5#90 🟡 工程化工具链补全

- [x] **E5#90a** `package.json`——加 `"fix": "eslint src/ plugins/ electron/ shared/ --fix"` | +1 行
- [x] **E5#90b** `package.json`——加 `simple-git-hooks` pre-commit 跑 `npm run check` | +4 行配置
- [x] **E5#90c** `npm install -D simple-git-hooks` + `npx simple-git-hooks`——`.git/hooks/pre-commit` 已创建 | 命令
**验证：** `npm run fix` → ESLint fixable warnings 自动消。`git commit` → pre-commit hook 触发。

---

### 第 2 轮：硬编码速赢

> 📖 **专题文档：** [`UI合规/裸hex颜色.md`](UI合规/裸hex颜色.md)

#### E5#91 🟡 裸 hex 颜色

- [x] **E5#91a** `PluginDetailView.css:172`——`#4caf50` → `var(--status-connected)` | 1 行
- [x] **E5#91b** `OutputPanel.css:114`——`#cca700` → `var(--warning)` | 1 行
- [x] **E5#91c** `OutputPanel.css:118`——`#f14c4c` → `var(--error)` | 1 行
- [x] **E5#91d** `IconBar.css:80`——`#CCA700` → `var(--warning)` | 1 行
**验证：** `grep -rE '#[0-9a-fA-F]{3,6}' src/**/*.css | grep -v index.css` → 零结果。

---

## 第 2 层：Bug 修复——用户可感知

> ⚠️ 第 1 层完成后可并行。

---

### 第 3 轮：标签页 + 右键菜单

> 📖 **专题文档：** [`Bug修复/预览标签页替换致同ID双标签页.md`](Bug修复/预览标签页替换致同ID双标签页.md)、[`Bug修复/右键菜单遮挡与定位.md`](Bug修复/右键菜单遮挡与定位.md)

#### E5#92 🔴 预览标签页 ID 碰撞

> 🐛 **致命Bug 收尾-2:** 预览标签页替换致同 ID 双标签页——`useTabManager.ts:230` 复用旧 tab `id` + 旧文件再打开 → 同 id 碰撞。
> ⚠️ **倒退风险 🔴:** 标签 ID 是身份主键——React key、keep-alive 匹配、分屏映射全依赖它。删 id 覆盖 + 碰撞检测如果逻辑有 bug → 标签切不过去 / React duplicate key / 分屏乱了。防线：严格按三步复现验证——单击 A → Ctrl+Click B → 双击 A → 只有一个 A。

- [x] **E5#92a** `src/hooks/useTabManager.ts:230`——删 `id: previewTab.id` 覆盖 | −1 行
- [x] **E5#92b** `src/hooks/useTabManager.ts` Step 4 后——加 ID 碰撞检测：`flatMap` 跨组查重 + `Date.now()` 后缀 | +4 行
- [x] **E5#92c** 验证——三步复现通过。`npm run check` 零新增
**验证：** 单击 A → Ctrl+Click 跳 B → 双击 A。只有一个 A 标签页。React DevTools 无 duplicate key。

#### E5#93 🔴 右键菜单遮挡——Portal

> 🐛 **致命Bug 收尾-3（与 E5#96 共同防御）:** 层叠上下文隔离——App.tsx zone wrapper `position:fixed` + zIndex 创建独立层叠上下文，子元素 z-index 无法穿越。ContextMenu → Portal（治标）→ OverlayPortal 通用组件（治本）。

- [x] **E5#93a** `src/components/shared/ContextMenu.tsx`——`return` 包裹 `createPortal(..., document.body)` | +3 行
- [x] **E5#93b** 子面板（E5#44d `subData`）同样 portal——同在 Fragment 内，自动跟随 | +0 行
**验证：** 侧栏底部右键（经过分割线）→ 菜单不被遮挡。所有 `<ContextMenu>` 调用点正常。

#### E5#94 🟡 右键菜单定位——量测

- [x] **E5#94a** `ContextMenu.tsx`——`adjustedAnchor` 改两阶段渲染：`useLayoutEffect` 量测真实 DOM → 修正溢出 → `visibility` 显隐 | ~20 行
- [x] **E5#94b** 子菜单方向跟随可用空间——右边放不下自动翻左边 | +3 行
**验证：** 窗口右下角右键 → 菜单不超出边界。宽菜单项不被截断。

#### E5#95 🟡 未知文件零反馈

- [x] **E5#95a** `plugins/builtin/file-tree/src/views/FoldersView.tsx` L351-356——两处 `console.warn` 后加 `window.linkdesk?.dialog?.alert?.("...")` | +4 行
**验证：** 双击 `.key` → toast "没有为 .key 文件注册编辑器"。

---

### 第 4 轮：治本归一化——改一处消灭一类

> 📖 **专题文档：** [`架构债/OverlayPortal通用悬浮层.md`](架构债/OverlayPortal通用悬浮层.md)

#### E5#96 🔴 OverlayPortal 通用悬浮层

> 🐛 **致命Bug 收尾-3（与 E5#93 共同防御）:** 同上——层叠上下文隔离。OverlayPortal 是治本，一个组件替代所有 overlay portal。
> ⚠️ **倒退风险 🔴🔴:** Portal 到 body 后 DOM 父节点变了——CSS 继承链 + 事件冒泡路径全变。ColorPicker/SelectBox 是调用方组件内渲染的，portal 出去后 `useRef`/`onClickOutside` 逻辑可能断裂。防线：E5#93 ContextMenu 单独 portal 先做 → 验证通过再抽象 OverlayPortal → **不一次性全切**。

- [x] **E5#96a** 新建 `src/components/shared/OverlayPortal.tsx`——`createPortal` 包装 | +10 行
- [x] **E5#96b** `ContextMenu.tsx`——`createPortal` → `<OverlayPortal>` | −2/+2 行
- [x] **E5#96c** `ToastContainer.tsx`——`return` → `<OverlayPortal>` | +3 行
- [x] **E5#96d** `ConfirmDialog.tsx`——`return` → `<OverlayPortal>` | +3 行
- [x] **E5#96e** `ColorPicker.tsx`——面板+遮罩 → `<OverlayPortal>` | +3 行
- [x] **E5#96f** `SelectBox.tsx`——下拉部分 → `<OverlayPortal>` + `position:fixed` + `getBoundingClientRect` | +9 行
  **🐛 E5#96f 引入 bug:** SelectBox portal 后 `containerRef.contains()` 失效——下拉项点击被外部检测先拦截。已修复：加 `dropdownRef` + 双重 `contains` 检查。
- [ ] **E5#96g** 验证——逐一验证 5 个 overlay 组件在不同 zone 位置弹出均不被遮挡

---

#### 🔥 E5#96+ OverlayPortal 行为归一化

> 📖 **新增专题文档：** [`架构债/E5-96-OverlayPortal行为归一化.md`](架构债/E5-96-OverlayPortal行为归一化.md)——问题→方案→Bug预判+防线→涉及文件

**阶段 1：升级 OverlayPortal——纯增量**

- [x] **E5#96h** `OverlayPortal.tsx`——加 prop 类型（`onClose`/`triggerRef`/`trapFocus`/`zIndex`）+ `contentRef` | +8 行
- [x] **E5#96i** mousedown 外部点击检测（捕获阶段）——排除 `contentRef` + `triggerRef` | +12 行
- [x] **E5#96j** keydown Escape 监听 | +8 行
- [x] **E5#96k** `trapFocus`——Tab/Shift+Tab 焦点循环 + 首次自动 focus | +15 行
- [x] **E5#96l** `zIndex` prop → wrapper inline style | +3 行

**阶段 2：迁移消费方——删各自手写检测**

- [x] **E5#96m** `SelectBox.tsx`——删双重 `contains` + keydown Escape → 传 `onClose`/`triggerRef` | −14/+1
- [x] **E5#96n** `ColorPicker.tsx`——删 keydown Escape + overlay onClick → 传 `onClose` | −10/+1
- [x] **E5#96o** `ConfirmDialog.tsx`——Escape 分支 → 传 `onClose`（Alert 模式不传，保留 backdrop+Enter）| −2/+1
- [x] **E5#96p** `ContextMenu.tsx`——**跳过**（两步 ESC 先子后主，OverlayPortal 不支持）| —

**🐛 发现：漏网之鱼——HamburgerMenu + TitleBar 自定义 dropdown**

> HamburgerMenu 在 IconBar zone 内渲染 `position:fixed` 自定义 `<div>`，z-index:2550 困在 zone 的 zIndex:10 内 → 被 Resize handle(zIndex:15)/Sidebar 遮挡。TitleBar 不挡纯属位置巧合（根级渲染），不是设计。

- [x] **E5#96r** `HamburgerMenu.tsx`——自定义 dropdown → `<OverlayPortal onClose triggerRef={btnRef}>`。删手写 mousedown（−11/+3）
- [x] **E5#96s** `TitleBar.tsx`——自定义 dropdown → `<OverlayPortal onClose triggerRef={triggerRef}>`。删手写 mousedown + dropdownRef → triggerRef 动态同步（−15/+5）

**回归验证**

- [x] **E5#96q** 逐一验证——7 个 overlay 全部使用 OverlayPortal ✅
**验证：** 全部 overlay 外部点击+Esc 统一走 OverlayPortal。净 +25 行 / −27 行 + 96r/s ~−22/+6 = 净 −18 行重复代码。

#### E5#97 🟡 间距治本化——`--space-*` token

- [x] **E5#97a** `src/index.css`——加 `--space-*` token（xs:4 / sm:8 / md:12 / lg:16 / xl:20 / 2xl:24 / 3xl:32）| +7 行
- [x] **E5#97b** grep 扫描不在 4px 刻度的 `padding`/`margin`/`gap` → 逐处替换 | ~10-20 处
**验证：** `node scripts/check-spacing-grid.mjs` ✅。`grep -rE '\b(10|14|18|22|26|30)px\b' src/**/*.css`（排除 font-size、border-radius）→ 零结果。

#### E5#98 🟡 z-index 分层

> 🐛 **致命Bug 收尾-5:** z-index 顺序翻转——弹窗跑到 backdrop 下面。裸数字是无数次调试确定的，token 替换搞反相对顺序 → 新遮挡 bug。
> ⚠️ **倒退风险 🔴🔴🔴（最高）:** 全局排序——改 10 处裸数字，可能引入 N 个当前不存在的遮挡 bug。防线：**改一个验一个，不批量。** Token 值照抄当前裸数字，不重新排序。每改一个手动验证该 overlay 正常再改下一个。

- [x] **E5#98a** `src/index.css`——`--z-dropdown: 100→650` + 补 `--z-drag-preview: 1000` | +2 行
- [x] **E5#98b** 逐组件替换裸 z-index → `var(--z-*)`——9 文件 11 处，逐个验证通过 | ~11 处
- [x] **E5#98c** ⚠️ 逐个验证——不可批量。每改一个手动验证该 overlay 正常再改下一个 | 手动 ✅
**验证：** `grep -rE 'z-index:\s*\d+' src/**/*.css | grep -v 'var(--z-'` → 仅组件内部微堆叠（0-2, 10），无全局 overlay 裸数字。

#### E5#99 🔴 未知文件治本化——壳 `tabs.create` handler

- [x] **E5#99a** `src/core/services/IpcBridgeHandler.ts`——`case "tabs:create"` 未知 type → 路由到 `"editor"`（对标 VS Code 纯文本 fallback）| −6 行
- [x] **E5#99b** `FoldersView.tsx`——撤两个拦截块（无扩展名/无 pluginId），全交壳路由 | −10 行
**验证：** 文件树 + SearchView + 任何调 `tabs.create` 的代码——未知类型都自动 toast，不需各调用方自己写。

---

### 🐛 新发现：配置初始化竞态——启动时配置不生效

> 📖 **新增专题文档：** [`Bug修复/配置初始化竞态.md`](Bug修复/配置初始化竞态.md)
> 影响范围：`menuStyle`（汉堡/标题栏）、`editor.ctrlScroll`（Ctrl+滚轮）、任何 mount 时读配置的组件。
> 症状：启动后须手动切换一次设置才生效。

#### E5#115 🔴 配置初始化竞态修复

> 📖 **新增专题文档：** [`Bug修复/配置初始化竞态.md`](Bug修复/配置初始化竞态.md)

- [x] **E5#115a** 审计——grep `getConfigurationValue\|useConfigurationValue` 列出所有 mount 时读配置的组件 → 确认受影响范围 | 审计 ~10min
- [x] **E5#115b** `src/main.tsx:37-38`——`initStorageService()` + `initConfigurationService()` 在 `createRoot` 前 | 已就位
- [x] **E5#115c** 验证——启动后 hamburger 菜单/标题栏/Ctrl+滚轮 无需手动切换即生效
**验证：** 重启应用 → 所有配置项直接生效，不需要切来切去。

---

### 🐛 新发现：串口监视器右键菜单空白

> E5 迁移到共享 ContextMenu 时，写了 `<ContextMenu menuId={MenuId.EditorContext}>` 但 `registerMenuItems` 从未调用——菜单项从未注册。收到区右键只显示空分组横杠。

#### E5#116 🟡 串口监视器 EditorContext 注册菜单项

- [x] **E5#116a** `serial-monitor/src/index.tsx`——`window.linkdesk.menu.registerItems(MenuId.EditorContext, ...)`：复制/全选/清除（clipboard/selection/edit 分组）| +4 行
- [x] **E5#116b** 同样注册 `MenuId.QuickSendContext`——编辑/删除 | +3 行
**验证：** 串口接收区右键 → 复制/全选/清除（带分组横杠）。快捷发送 pill 右键 → 编辑/删除。

---

### 🐛 发现：ContextMenu isPluginWebView 判断错误——壳进程误走 IPC

> `isPluginWebView` 用 `pluginViews.notifyReady` 判断——此 API 壳 preload 和插件 preload 都有 → 壳进程被误判为"插件 WebView" → ContextMenu 多绕一圈 IPC 读自己的 `_menus` → 引入不必要的时序竞态。

#### E5#117 🔴 ContextMenu isPluginWebView 修正——壳进程直读本地

- [x] **E5#117a** `ContextMenu.tsx`——`isPluginWebView`：`!!pluginViews.notifyReady` → `!!contextKey._getValue` | −1/+1
  - `_getValue` 只在插件 preload 注入——壳 preload 没有 → 正确区分"壳"还是"插件进程"
  - 壳：`isPluginWebView=false` → `getLocalMenuItems` 同步读本地 `_menus`（零 IPC）
  - 插件：`isPluginWebView=true` → `getItems` IPC 读壳的 `_menus`（正确隔离）
- [x] **E5#117b** `serial-monitor`——`window.linkdesk.menu.registerItems` 模块级 IPC。单/多 WebView 统一 | 已就位
**验证：** 单 WebView 第一次打开串口 → 右键立即可用。F12 无 `menu:getItems` IPC。多 WebView 恢复后同样可用。

---

## 第 3 层：UI 升级——声明式 + 矢量

> ⚠️ 无依赖，随时可做。

---

### 第 5 轮：图标

> 📖 **专题文档：** [`UI合规/Emoji图标Lucide化.md`](UI合规/Emoji图标Lucide化.md)、[`UI合规/间距与动画.md`](UI合规/间距与动画.md)

#### E5#100 🟡 Emoji → Lucide

- [x] **E5#100a** `npm install lucide-react@1.29.0` | 命令
- [x] **E5#100b** `src/components/shared/PluginIcon.tsx`——渲染逻辑改造：Lucide 优先 → codicon → SVG/PNG → emoji（`@deprecated` 回退）+ `iconSource: "lucide"` 类型 | ~50 行重构
- [x] **E5#100c** 逐文件替换——`PluginDetailView.tsx`（📦💡🔒→Package/Lightbulb/Lock）/ `WelcomeView.tsx`（📂📁📖→FolderOpen/Folder/BookOpen）/ `WorkspaceView.tsx`（📊→BarChart3）| 7 处
- [x] **E5#100d** CSS 补 `.plugin-icon--lucide`（IconBar+TabBar），emoji fallback 标 `@deprecated` | +18 行
**验证：** `grep -rE '[\x{1F300}-\x{1F9FF}]' src/ plugins/` → 仅注释/字符串（非功能性 UI）。

#### E5#101 🟡 语言按钮设计

- [x] **E5#101a** `src/components/StatusBar.tsx`——语言按钮 `中`/`EN` → `icon: "globe"`（codicon-globe）| −5 行
- [x] **E5#101b** 确认——点击仍弹出 LanguagePicker SelectBox，行为不变 | 验证
**验证：** 状态栏语言按钮显示地球图标，不随当前语言变化。

---

## 第 4 层：架构债清理——渐近

> ⚠️ 无依赖，随时可做。优先级低——不影响功能。

---

### 第 6 轮：v3 残余 + IPC + PluginId + 记忆 + ESLint

> 📖 **专题文档：** [`架构债/v3遗留与硬编码残余.md`](架构债/v3遗留与硬编码残余.md)、[`架构债/IPC监听器引用计数模式.md`](架构债/IPC监听器引用计数模式.md)、[`架构债/壳硬编码PluginId清理.md`](架构债/壳硬编码PluginId清理.md)、[`架构债/记忆文件归档.md`](架构债/记忆文件归档.md)、[`架构债/ESLint覆盖审计.md`](架构债/ESLint覆盖审计.md)

#### E5#102 🟢 v3 遗留与硬编码残余

- [x] **E5#102a** `StorageService.ts`——`v3_` localStorage key 去前缀 + `_tryV3Fallback` 自动迁移 | +34 行
  > 迁移 guard：旧 key 不删——`read()`/`readSync()` 回退读 `v3_*` → 自动写新 key。
- [x] **E5#102b** `shared/constants.ts`——`DEV_SERVER_PORT`+`DEV_SERVER_URL`。vite/electron/plugin-view-handlers 引用 | 4 文件 +12 行
- [x] **E5#102c** 魔数 → 模块级常量——KeybindingRegistry/App/FileSearcher/ProfileService | 4 文件 4 处
- [x] **E5#102d** `electron/tsconfig.json`——`moduleResolution: "node"` → `"node10"`（TS 7.0 安全，行为完全相同）| 1 行
**验证：** `grep -r "v3_" src/ --include="*.ts"` → 仅迁移 guard 注释。`npx tsc --noEmit` 无 `moduleResolution=node10` 弃用警告。

#### E5#103 🟢 IPC 监听器引用计数

- [x] **E5#103a** `src/core/services/IpcBridgeHandler.ts`——`_initialized` → `_refCount` + `unregisterIpcBridgeHandler()` | +8 行
- [x] **E5#103b** `App.tsx`——useEffect cleanup 调 `unregisterIpcBridgeHandler()` | +2 行
**验证：** React StrictMode 下无双重注册 warning。

#### E5#104 🟢 PluginId 硬编码注释

- [x] **E5#104a** `src/components/MainContent.tsx`——2 处 `"editor"` / `"serial-monitor"` 加 `@deprecated E5#26d` 注释 + `// TODO: 多 WebView 恢复时改为 plugin.json 声明式 IPC 通道` | +4 行
**验证：** 不改行为。注释清晰。

#### E5#105 🟢 记忆文件归档 ✅

- [x] **E5#105a** `memory/archive/` 目录 + 搬 E3/E4/Phase3 历史记忆 ~100 个 | 整理 ~30min
- [x] **E5#105b** `memory/MEMORY.md`——重写为仅 active 索引（~20 个核心记忆）| 重写
**验证：** MEMORY.md 从 ~200 行 → ~50 行。

#### E5#106 🟡 ESLint `no-hardcoded-chinese` 规则

- [x] **E5#106a** `eslint-local-rules.js`——新规则 `no-hardcoded-chinese`（warn）：JSXText/Literal/TemplateLiteral 中文检测，排除 t() 包裹 | +100 行
- [x] **E5#106b** `eslint.config.js`——注册 `linkdesk/no-hardcoded-chinese: "warn"` | +2 行
**验证：** `npx eslint plugins/` → 6 处命中，全部真违规，无误报。`t("中文")` → 不报。

---

## 第 5 层：测试 + 功能 + 文档

> ⚠️ 所有上层完成后验证。功能补全可穿插在任意阶段。

---

### 第 7 轮：测试 + 功能补全

> 📖 **专题文档：** [`功能补全/App启动集成测试.md`](功能补全/App启动集成测试.md)、[`功能补全/文件树原生剪贴板与拖出.md`](功能补全/文件树原生剪贴板与拖出.md)、[`功能补全/i18n第二轮-插件层与主题.md`](功能补全/i18n第二轮-插件层与主题.md)、[`功能补全/插件开发测试环境.md`](功能补全/插件开发测试环境.md)

#### E5#107 🔴 App 启动集成测试

> 🐛 **致命Bug 收尾-4（与 E5#88d 共同防御）:** initAll 半残启动——init 链 7 步异步串行，任一步 reject → 后续静默跳过。E5#107 mock 每步失败验证退化逻辑不崩溃。

- [x] **E5#107a** 提取 `src/App.tsx` init 链为可测试函数——`src/core/services/AppInitializer.ts` `initAll(deps)` | ~200 行
- [x] **E5#107b** `src/App.tsx` useEffect → 调 `initAll(deps)` | −97/+57 行
- [x] **E5#107c-g** 新建 `src/__tests__/AppInit.test.ts`——8 条测试：正常 + initServices/pluginLoader/applyConfig/serial/layout 各自失败 + 全失败降级 + cleanup 返回验证 | +161 行
- [x] 🐛 Monaco 暗色字体——E5#107 第一轮 `loadThemes:true` 降概率 + E5-adj 第二轮 `optionsRef` 根治（React 竞态——`buildMonacoOptions()` `initMonacoEnv()` 时间差）。见 `427217c` `a129288`
**验证：** `npx vitest run src/__tests__/AppInit.test.ts` → 8/8 通过。

#### E5#108 🟢 文件树原生剪贴板与拖出

- [x] **E5#108a** `electron/ipc/clipboard-handlers.ts`——`clipboard:writeFileList` handler + `buildCFHDROP()` Windows DROPFILES 结构 | +48 行
- [x] **E5#108b** `electron/main.ts`——`shell:startDrag` handler：`event.sender.startDrag()` + 版本守卫 + icon fallback | +11 行
- [x] **E5#108c** `electron/preload-shell.ts` 暴露 `clipboard.writeFileList` + `shell.startDrag`；`FileTreeContextMenu.tsx` `writeSystemClipboard` 原生 CF_HDROP + 纯文本双写 | +8 行
- [x] **E5#108d** `FileTreeDnD.ts` `handleDragStart`——对标 VS Code `fillEditorsDragData`：`DownloadURL` + `text/uri-list` + `text/plain`（H5 dataTransfer，不用 Electron startDrag——闪退）| +5 行
- [x] **E5#108e** 平台兼容——`process.platform === "win32"` → `CF_HDROP`，`"darwin"` → `NSFilenamesPboardType`，`"linux"` → `text/uri-list` | 已含在 108a
**验证：** 文件树 Ctrl+C → 桌面 Ctrl+V → 粘贴成功。文件树拖出 → 对标 VS Code H5 方式，Chromium 自动识别 native file drag。

#### E5#109 🟢 i18n 第二轮——每插件自带翻译（渲染时 t()）

> 🔥 **2026-08-07 重构。** 原方案对标 VS Code `%key%` 包语法 → **改为方案 B：渲染时 `t()`，每插件 `i18n/` 目录。**
> plugin.json 保留中文原文不动。翻译文件由每个插件自己提供，`contributes.i18n` 声明。壳单向读取，切语言即时生效，无需重载。

- [x] **E5#109a** `types.ts` + `plugin.schema.json`——`PluginContributes` 加 `i18n?: Record<string, string>`（lang → 相对路径）。迁移旧顶层 `i18n`（`@deprecated`）→ `contributes.i18n` | `f3e4a89`
- [x] **E5#109b** `loader.ts`——`loadPluginI18nData()` 检查 `contributes.i18n`，`fetchPluginDataFile` 加载各语言 JSON，调现有 `registerLanguageBundle(langCode, data, pluginId)` | `910d1ee`
- [x] **E5#109c** `file-tree/`——新建 `i18n/en.json`（~38 key——commands + configurations + viewsContainers + views）+ `plugin.json` 加 `contributes.i18n` | `aae1a43`
- [x] **E5#109d** `editor/`——新建 `i18n/en.json`（~28 key——commands + configuration descriptions）+ `plugin.json` 加 `contributes.i18n` | `aae1a43`
- [x] **E5#109e** `serial-monitor/`——新建 `i18n/en.json`（~85 key——commands/config/viewsContainers/views + 从 lang-defaults 迁出的 UI 字符串）+ `plugin.json` 加 `contributes.i18n` | `aae1a43`
- [x] **E5#109f** `marketplace/`——新建 `i18n/en.json`（~6 key——viewsContainers/views）+ `plugin.json` 加 `contributes.i18n` | `aae1a43`
- [x] **E5#109g** `lang-defaults/en.json`——去重：移除 ~80 条串口专有字符串（已迁入 serial-monitor），保留壳级通用 key | `36f517d`
- [x] **E5#109h** `EditorTab.tsx`——`useTranslation` + "加载中…""无法打开文件""保存失败:" → `t()` | `f65420e`
- [x] **E5#109i** 🔥 **归一化——`ContextMenu.tsx` `{item.label}` → `{t(item.label)}` + `CommandPalette.tsx` `cmd.title` → `t(cmd.title)`**——不逐个改 22 处 FileTreeContextMenu 注册代码，改 2 个渲染点全局生效 | `f65420e`
- [x] **E5#109j** 主题 `plugin.json` label——已是中英双语（"暮光 Sunset"），无需改动 | —
- [x] **E5#109k** `StatusBar.tsx`——`useTranslation` + chord 提示 ~2 处 → `t()` 模板插值 + lang-defaults 模板 key | `f65420e`
- [x] **E5#109l** 目录规范 + plugin.json 规范——`09-插件目录规范.md` 补 `i18n/` 目录段 + VS Code 对照、`06-plugin.json规范.md` 更新 `contributes.i18n` | `ac4befc`
**验证：** 切英文 → 命令面板/右键菜单/设置/标签页/状态栏全部英文，无中文 key 裸奔。`grep -rE '[\x{4e00}-\x{9fff}]' plugins/*/plugin.json` → 仅 `i18n` 字段路径值（非 title/description）。

#### E5#110 🟢 插件开发测试环境

- [x] **E5#110a** `package.json`——加 `"dev:plugin": "vite --mode plugin-dev --open /plugin-shell.html"` | +1 行
- [x] **E5#110b** `src/plugin-shell-main.tsx`——加 `?pluginId=` query param 过滤：只加载指定插件 | ~15 行
- [x] **E5#110c** （可选）新建 `src/dev/mockApi.ts`——mock `linkdesk.*` IPC 降级返回 | ~30 行
**验证：** `npm run dev:plugin file-tree` → 最小壳只渲染 file-tree 插件。HMR 可用。

---

### 第 8 轮：文档 + 回归验证

#### E5#111 🟢 VSCode 系统对照文档

- [x] **E5#111a** 新建 `05-收尾/VSCode对照表.md`——LinkDesk 架构术语 → VS Code 对应模块映射表（12类 130+术语）| `eafeffe`
**验证：** 新 AI 看到 `tabs.create` 知道对标 `IEditorService.openEditor`。

#### E5#112 ➡️ 移至 E6

> **整个任务迁至 E6#9-#11。** 含设计文档 + 脚手架代码 + npm 发布。E5 不再碰。

#### E5#113 ➡️ 移至 E6

> **整个任务迁至 E6#28-#30。** 含审计清单 + CI 实现 + 官网 + 发布清单。E5 不再碰。

#### E5#114 🔴 E5-收尾 回归验证

- [x] **E5#114a** E5#25a-g 七场景回归——三图标切换/文件树/编辑器/串口/市场/设置/F5 | 已确认正常 ✅
- [x] **E5#114b** E5#26a-f 防线验证——`npm run check` tsc✅ spacing✅ vitest 393pass✅ ESLint 1预存 | ✅
- [x] **E5#114c** E5#28a-f AI 友好度——VSCode对照表 + 执行清单 + 记忆索引 | ✅
- [x] **E5#114d** `npm run electron:build` 打包 → 安装 → 启动——已验证 ✅。pyright 放 extraResources + resolveLspArg resourcesPath 查找。Python Ctrl+Click 生产环境正常。
**验证：** 全部勾选通过。

---

### 第 9 轮：散落 UI 归一化 + 插件通信违规清理

> 📖 **审计来源：** 2026-08-06 全量扫描——E5#96+ 归一化后剩余遗漏。

#### E5#118 🟡 NotificationCenter → OverlayPortal

> 手写 `window.addEventListener("mousedown")` + `panelRef.contains` + `bellRef.contains`——和 SelectBox/HamburgerMenu 迁移前同模式。

- [x] **E5#118a** `NotificationCenter.tsx`——通知面板 → `<OverlayPortal onClose triggerRef={bellRef}>`。删手写 mousedown（−11/+3）| ✅ `279ad74`

#### E5#119 🟡 QuickPick (命令面板) → OverlayPortal

> 手写 `createPortal`——和 ContextMenu 迁移前同模式。

- [x] **E5#119a** `QuickPick.tsx`——`createPortal` → `<OverlayPortal>`（−2/+2）| ✅ `279ad74`

#### E5#120 🟡 插件通信违规清理——分批复核 37 处 ESLint warn

> ESLint `no-core-import-in-plugin` 当前 37 处 warn。逐文件评估：切 linkdesk API / 记录例外 / 白名单放行。

- [x] **E5#120a** 审计分类——37 处分四组：(A) `import type` 纯类型 5 处 (B) 测试文件 4 处 (C) 缺 API 留 E5.5 27 处 (D) 可立即切 1 处 | ✅ 审计表 → [[e5-120-audit-plugin-core-imports]]
- [ ] **E5#120b** `WorkspaceService` (7 处) + `FileService` (5 处) → 已有 `linkdesk.workspace` / `linkdesk.filesystem` API，但关键方法缺失（openFolder/removeFolder/onDidChangeFolders 等）→ 留 E5.5
- [ ] **E5#120c** `CommandRegistry` (4 处) + `ViewContainerService` (3 处) + 其余 (14 处) → 留 E5.5 补 API
**验证：** 审计表完成。ESLint 规则白名单 → 消 9 处。1 处 `executeCommand` 可切。剩余 27 处 = E5.5 逐 API 消灭。

---

## 执行顺序与依赖

```
第 1 层（阻断——先修）
  第 1 轮：E5#87–#90  生产路径 + 静默吞错 + 类型安全 + 工具链  ← 全部无依赖
  第 2 轮：E5#91      裸 hex                                       ← 无依赖

第 2 层（Bug——第 1 层后）
  第 3 轮：E5#92–#95  标签页 ID + 右键菜单 + 未知文件              ← #92 依赖 #89
  第 4 轮：E5#96–#99  治本归一化——OverlayPortal(含r/s) + 间距+z-index+壳handler  ← #96 依赖 #93

新增（第 2 层发现）：
  E5#96r/s  HamburgerMenu + TitleBar → OverlayPortal                ← 依赖 #96+
  E5#115    配置初始化竞态——启动时配置不生效                        ← 无依赖
  E5#116    串口监视器 EditorContext registerMenuItems 缺失         ← 无依赖

第 3 层（UI——随时）
  第 5 轮：E5#100–#101 Emoji Lucide + 语言按钮                     ← 无依赖

第 4 层（架构债——随时）
  第 6 轮：E5#102–#106 v3 残余 + IPC + PluginId + 记忆 + ESLint    ← 全部无依赖

第 5 层（测试 + 功能 + 文档——最后）
  第 7 轮：E5#107–#110 测试 + 剪贴板 + i18n + 插件开发             ← #107 依赖 #88 #89
  第 8 轮：E5#111–#114 文档 + 回归                                 ← 所有轮次
  第 9 轮：E5#118–#120 散落 UI 归一化 + 插件通信违规清理            ← 无依赖，随时可做
```

---

## 总结

| 层 | 内容 | 任务 | 行数 |
|:--|------|:--:|:--|
| L1 | 阻断级修复 | 5 (E5#87-#91) | ~40 + ~30 处替换 |
| L2 | Bug 修复 + 治本归一化 | 12 (E5#92-#96+#96r/s + E5#97-#99) | ~130 + ~30 处替换 |
| 新增 | L2 发现——HamburgerMenu/TitleBar portal + 配置竞态 + 串口右键 | 4 (E5#96r/s + E5#115 + E5#116) | ~45 行 |
| L3 | UI 升级 | 2 (E5#100-#101) | ~60 + ~12 处替换 |
| L4 | 架构债清理 | 5 (E5#102-#106) | ~90 + ~35 处替换 |
| L5 | 测试 + 功能 + 文档 + 散落清理 | 11 (E5#107-#114 + #118-#120) | ~460 + ~180 处替换 |
| **合计** | | **39** | **~805 + ~300 处替换** |

---

## 🔴 致命 Bug 速查（任务中的高风险点）

| Bug | 症状 | 任务 | 防线 | 状态 |
|:--|:--|:--:|:--|:--|
| 打包白屏 | 生产环境窗口加载失败 | E5#87 | `../../dist/index.html` | ✅ |
| 同 ID 双标签页 | React key 冲突 → 两个都高亮 | E5#92 | 删 id 复用 + 碰撞检测 | ⬜ |
| Overlay 被遮挡 | 菜单/Toast 被分割线盖住 | E5#93 + #96 | Portal → body | ⬜ |
| 半残启动 | init 链任一步失败 → 后续跳过 | E5#88d + #107 | unhandledrejection + 测试 | 🟡 #88d已做 |
| z-index 顺序翻转 | 弹窗跑到 backdrop 下面 | E5#98 | 逐个验证——不批量 | ⬜ |

---

## 🟡 代码倒退风险——修 bug 可能引入新 bug

> 2026-08-06。不是所有 28 任务都零风险。以下 5 个任务可能引入当前不存在的 bug——执行时需额外谨慎。

| 风险等级 | 任务 | 倒退症状 | 根因 | 防倒退对策 |
|:--|:--:|:--|:--|:--|
| 🔴🔴🔴 | E5#98 z-index 分层 | 弹窗跑到 backdrop 下 / 菜单被遮挡 | 全局排序——裸数字是无数次调试确定的。token 替换搞反相对顺序 → 新遮挡 bug | **改一个验一个，不批量。** Token 值照抄当前裸数字，不重新排序 |
| 🔴🔴 | E5#96 OverlayPortal | 定位偏移 / onClickOutside 断裂 / 关闭逻辑失效 | Portal 到 body 后 DOM 父节点变了——CSS 继承链 + 事件冒泡路径全变。ColorPicker/SelectBox 是调用方组件内渲染的 | E5#93 ContextMenu 单独 portal 先做 → 验证通过再抽象 OverlayPortal → **不一次性全切** |
| 🔴 | E5#92 标签页 ID | 标签切不过去 / React duplicate key / 分屏乱了 | 标签 ID 是身份主键——React key、keep-alive 匹配、分屏映射全依赖它 | 严格按三步复现步骤验证：单击 A → Ctrl+Click B → 双击 A → 只有一个 A |
| 🟡 | E5#102a v3_ → linkdesk- | 用户配置全部丢失 | 改 key 名 = 存量数据迁移。guard 写错 → 读不到旧数据 → 回默认值 | 迁移 guard = 读旧 key → 写新 key → **保留旧 key 不删**——验证通过后再清理 |
| 🟡 | E5#102d moduleResolution | tsc 新增几十个编译错误 / import 解析到错文件 | `node16` 更严格——要求扩展名 + `exports` 字段 | 改前记下 `npx tsc --noEmit` 当前错误数 → 改后只允许零**新增** |

### 安全任务（不会倒退）

以下 23 个任务要么纯增量（加日志/类型/注释/token），要么机械替换（token 值 = 原值），**不改运行时行为**：E5#87 #88 #89 #90 #91 #93 #94 #95 #97 #99 #100 #101 #103 #104 #105 #106 #107 #108 #109 #110 #111 #112 #113 #114。

---

> **← 索引：** `README.md`
> **← E5 执行清单：** `../05-执行清单.md`
