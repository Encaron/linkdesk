# 任务 `#109j-a`：宿主 5 个内部工具类加前缀（件 4 上半）——第 1.16 轮

> **一句话**：`.titlebar` `.divider` `.about` `.rn` `.vta` 这 5 个**宿主内部**全局工具类加 `ldk-` 前缀——实测**零插件借用** ⇒ 零破坏的壳内改动。
> **前置**：第 1.8 轮已 ✅（件 1 四批改名完成）。可与件 2/3 的任务并行。
> **规模**：5 个名字 · 约 20 处（**定义处**为主，渲染点只有 7 处）。
> **提交轴**：壳（**不 bump**——壳版本在件 1 的收口轮已落 0.1.66；本批若单独提交，请在提交信息第二段写版本判定结论）。

---

## 一、面清单（实测读数——**先复核，再动手**）

| 类名 | 定义处 | 渲染点 | 备注 |
|:--|--:|--:|:--|
| `.titlebar` | 9（`src/index.css` ×5 + `src/pool/floating/floating-panel/FloatingPanel.css` + …） | 1（`src/pool/zones/title-bar/TitleBarZone.tsx:122`） | 也是硬约束 18 的拖拽区那个顶部条 |
| `.divider` | 1（`src/index.css:289`） | **0** | 🔴 **0 渲染点是可疑的**——见 §二·1 |
| `.about` | 1（`src/pool/views/about/AboutView.css:35`） | 2（`AboutView.tsx:62,116`） | |
| `.rn` | 1（`src/pool/views/release-notes/ReleaseNotesPoolView.css:34`） | 3（`ReleaseNotesPoolView.tsx:81,130,184`） | 「release notes」缩写 |
| `.vta` | 1（`src/pool/shared/view-title-actions/ViewTitleActions.css:5`） | 1（`ViewTitleActions.tsx:118`） | 「view title actions」缩写 |

**保留名清单**：`packages/plugin-sdk/schemas/reserved-class-names.json` 的 `classes.host` 组里**摘掉这 5 项**——**`.input` 保留**（它是件 4 下半 `#109j-b` 的裁决对象，本任务不动）。

---

## 二、动手前必须做的两件核查

1. 🔴 **`.divider` 到底怎么被渲染的**：实测「定义 1 处、渲染 0 处」——可能它由某处 `classList` / 字符串拼接 / 动态类名渲染，也可能是**死代码**。
   - 先查清：`grep -rn "divider" src --include=*.tsx --include=*.ts | grep -v "\.ldk-"`，以及 `index.css:289` 那条规则的注释与历史（本仓有「废案压成一句」的纪律，但**没有证据前不许删**）。
   - 结论二选一：**（i）找到渲染点 → 照改名**；**（ii）确认是死规则 → 记进交接.md，本轮只加前缀不做删除**（删除是另一件事，会让 diff 语义变得不纯）。
2. **复查插件侧零借用**（本任务「零破坏」的前提）：`grep -rnE "\.(titlebar|divider|about|rn|vta)([^a-zA-Z0-9_-]|$)" E:\linkdesk-plugins --include=*.css --include=*.tsx | grep -v node_modules`
   - 预期 0 命中（L7/#109e 的读数：插件只借 `.input`）。**若有命中 ⇒ 停下，在交接.md 记一行并请示**（那说明本任务不是零破坏）。

---

## 三、步骤

1. 做 §二 的两项核查（**先给读数再动手**）。
2. 改定义处 + 渲染点：`.titlebar` → `.ldk-titlebar` 等（**只需加前缀，词干不动**）。
   - ⚠️ 注意别误伤：`.rn` / `.vta` 是两个字母的缩写，grep 时要带边界（`\.rn(?![a-zA-Z0-9_])`）；`.about` 会命中 `AboutView` 之类**非类名**位置，逐条确认。
3. 摘 JSON 的 `classes.host` 5 项（**留 `.input`**）。
4. `npm run check` EXIT=0。

---

## 四、判据（做完逐条给读数）

> ✅ **2026-09-15 补勾（第 1.16 轮收尾清账——用户点出「前面 AI 做完却留着空方框」）**：以下各框是该轮判据，对应轮次 = 第 1.16 轮（`#109j-a`，本轮），**已在 [E6-执行清单](../../E6-执行清单.md) 宣告完成**（逐条读数见该文件「第 1 层头」的轮次进度行、以及 [交接.md](交接.md) 的该轮次段）。**本次只改勾选状态，未改任何判据条文与结论**；若日后发现某条实际未过 ⇒ 改回 `[ ]` 并写明原因。

- [x] §二 两项核查的**实测输出**（`.divider` 结论 + 插件侧 0 命中）
- [x] `npm run check` EXIT=0（贴末尾三行）
- [x] `grep -rnE "\.(titlebar|divider|about|rn|vta)([^a-zA-Z0-9_-]|$)" src --include=*.css --include=*.tsx --include=*.ts | grep -v "\.ldk-"` 只剩注释（贴输出）
- [x] JSON `classes.host` = **只剩 `input`**（贴片段）
- [x] 实机/截图抽查：**标题栏**（`.ldk-titlebar` 家族，含右上角按钮）与**关于页**、**发行说明页**三处外观与改前一致（改名不许有视觉变化）
- [x] `git diff --stat`

---

## 五、提交与收尾

- 提交信息：`fix:E6#109j-a 宿主 5 个内部工具类加 ldk- 前缀（零插件借用，壳内改动）`；第二段写版本判定结论（**本轮不 bump** 或按你判定）。
- 收尾两件：交接.md 顶部追加 + 清单轮次进度（1.16 ✅ → ▶ 1.17）。

---

## 六、禁区

1. **不许动 `.input`**（`#109j-b` 的裁决对象）——它是**运行时同表**的即时破坏面（[00 §1.3 的 ⚠️](00-整理档案.md)）。
2. **不许删疑似死规则**（发现 `.divider` 真没人用 ⇒ 记进交接.md，不顺手删）。
3. **不许改视觉**（硬约束 16；本任务是纯标识符改名）。
4. **不许顺手做「把宿主 CSS 全量前缀化」**——边界就是这 5 个名字。
