# Phase 14.5 静态卫生清零方案

> 2026-08-15。**背景：** 用户指示——E6 上架之前给软件一个整洁的环境；Phase 10 起有大量清理死代码/删除硬编码任务，先扫 tsc + ESLint + vitest，对齐颗粒度后移入任务。审计完成、用户拍板"全做"后成立本 Phase。
>
> **原则对齐（用户十原则）：** 精益求精 / 归一化（一个 bug 不多个地方）/ 插件独立 / 无死代码 / 无硬编码 / AI 友好度（API 表面类型化 = 自文档）/ 健壮性（门禁机械零容忍，整洁是稳态不是一次大扫除）。

## §1 审计快照（2026-08-15 → 2026-08-16 #94 重跑）

| 项 | 08-15 快照 | 08-16 #94 重跑 | 说明 |
|:--|:--|:--|:--|
| tsc（根 + electron 两工程） | 0 错误 | **0 错误** | 反复验证 |
| @ts-ignore / @ts-expect-error | 0 处 | **0 处** | 全仓（src/electron/plugins/builtin） |
| ESLint | 3 errors + 927 warnings（门禁全量口径 `src/ plugins/ electron/`——含 plugins/user 7 插件）；#40 后（08-15）2 errors + 922 warnings | **0 errors + 821 warnings** | errors 历史首次清零（#94：monaco-init 假阳性改名 + loader.ts normalizePath；ShellPluginComponent error 已随 #40 删除）。warnings 922→821 来自 Phase 10-14 死代码清理 + #80 豁免 7 条 + preload-plugin.ts 删除等 |
| vitest | 393 通过 / 1 失败 | **408 通过 / 1 失败** | DataConverter.test.ts:59，根因 §3.4（#102 修） |
| 网格检查 / pool-css 检查 | ✓ / ✓ | ✓ / ✓ | check-spacing-grid / check-pool-css-imports |

🔴 **快照 08-15 数字已由 #94 重跑覆盖**（上表右列）：
- #40 `git rm ShellPluginComponent.tsx` = **error 2 所在文件**——先删后修，否则白修；
- #45.5 与 #97 的 shared/ 落点冲突（§5 #97 协调）；
- #45 会给 no-restricted-syntax 加 selector——#95 收窄在其后做，结构协同；
- 10 处 core-import 警告已有主人（Phase 12 目标 + Phase 14 #80 升 error），本 Phase 不重复。

## §2 3 errors 定性（#94 已全部清零 ✅）

| # | 位置 | 性质 | 修法 |
|:--|:--|:--|:--|
| 1 | `plugins/builtin/editor/src/services/monaco-init.ts:51` `react-hooks/rules-of-hooks` | **假阳性，非 bug**。`useWorkerFactory` 来自 monaco-languageclient，已读 node_modules 源码确认：它是往 MonacoEnvironment 挂 getWorkerUrl/getWorkerOptions 的普通函数，命名带 `use` 前缀才触发 React 规则 | import 改名：`import { useWorkerFactory as configureWorkerFactory }`——一行，零 disable 注释 |
| 2 | `src/components/ShellPluginComponent.tsx:59` `react-hooks/rules-of-hooks` | **真违规**。useMemo 在 `if (!loader) return` 早退之后——hooks 顺序不稳定，潜在真 bug | 文件随 Phase 10 #40 删除自动消失（本 Phase 只验证，不修） |
| 3 | `src/pluginLoader/loader.ts:1283` `linkdesk/no-raw-path-replace` | **真违规 + 硬约束**。手写 `replace(/\\/g, "/")`——Windows 路径反复出 bug 的类 | 改 `normalizePath(sourcePath)`，一行 |

> 顺带修正一个不一致：CLAUDE.md 提交自检写"npm run check 零错误"，实际提交门禁把 3 errors 当基线接受（历次提交"eslint 3+xxx 基线"）。E6 上架前 errors 必须清零，门禁名副其实（#94 + #102）。

## §3 927 warnings 分档（门禁全量口径；#40 后 922）

> 口径：门禁 = `eslint src/ plugins/ electron/`——plugins/ 含 user/ 7 个树内插件（lang-defaults / lang-test-ja / python / serial-monitor / theme-* ×3），共贡献 ~100 处警告。本方案按全量口径计数。

### §3.1 550 处 `no-explicit-any`（builtin 口径 490）

| 文件 | 处数 | 定性 |
|:--|--:|:--|
| `src/core/api/linkdesk-api.ts` | 47 | **API 表面**——插件作者直接读，类型化 = AI 友好度第一层 |
| `electron/preload-shell.ts` | 39 | 边界——IPC 线缆丢类型 |
| `electron/preload-pool.ts` | 36 | 边界——同上 |
| `electron/preload-plugin.ts` | 34 | 边界——**但此文件随 #44 git rm 删除**，快照自动降 |
| `plugins/builtin/file-tree/.../FileTreeContextMenu.tsx` | 31 | 纯债 |
| `plugins/builtin/file-tree/.../FoldersView.tsx` | 23 | 纯债 |
| `plugins/builtin/file-tree/.../FileTree.tsx` | 17 | 纯债 |
| `src/pluginLoader/loader.ts` | 15 | 纯债（部分动态 import 场景可落 unknown） |
| `plugins/builtin/marketplace/.../ExtensionItem.tsx` | 14 | 纯债 |
| 其余散件（App.tsx 9 / EditorView 9 / 测试 8+8 等） | ~186 | 逐修 |
| plugins/user 7 插件（serial-monitor 等树内 dev 插件） | ~60 | 参与门禁——修复归属见 §7 决策点 4 |

**归口设计：** 边界 ~156 处（preload 三兄弟 + linkdesk-api.ts）不做孤立 patch——先定义 **IPC wire DTO 契约类型**（一处定义，壳/池/插件作者三处消费），函数签名接上后 body 内 any 大半自动消除或收窄。真正无法定型的（Monaco 内部、动态模块加载）落 `unknown` + 守卫，不落 any。插件侧 + 壳 src 属纯债，同原则逐修。

### §3.2 328 处"疑似 pluginId 硬编码比较"（no-restricted-syntax；builtin 口径 294）

现 selector `BinaryExpression[operator=/^[!=]==?$/] > Literal[value=/^[a-z]/]` 匹配**任何**与小写字符串字面量的比较——误伤率极高（例：`label === "typescript"`、`distribution !== "user"` 都是合法 tag 判别，与 pluginId 无关）。真 pluginId 硬编码只占一小撮。

**收窄设计（#95）：** selector 限定左操作数为名字含 `pluginId`/`plugin` 的 MemberExpression/Identifier——294 → 预计 20-40 处真违规。顺手补盲点：现规则拦不住 `switch (pluginId) { case "literal" }` 和 `arr.includes("literal")`——同一危害类三种写法只拦一种，归一化不完整。

**剩余比较分类归口（#96）——三类，每类有归一化终点，不做 294 个孤立 patch：**

1. **跨堆协议值**（layout 的 location/mode、distribution、panel location……壳写池读的 wire 字符串）：两边各写一遍裸字面量 = v2.6"一个 bug 多个地方"的缝 → 归口 wire 契约类型模块的 union literal type（与 #97 同模块），壳池双端 import type；
2. **进程内 closed union 判别**（`item.location === "top"` 等，类型本身已是 union）：字面量就是类型安全值，**不提取常量**（形式主义、常量爆炸反而脏）；同一字面量重复 3+ 处才提；
3. **可疑处**（对 `string` 类型比字面量）：逐个修。

### §3.3 25 处 `react-hooks/exhaustive-deps`（builtin 口径 13）

App.tsx ×6（291/689/703/717/926/1004）、editor ×3（EditorTab 152/223、EditorView 275）、FileTree 143、src/core/react/useSendData 112、useDragReorder 268、MainZone 411；**+12 处全在 plugins/user/serial-monitor**（statusBar.tsx:64 / index.tsx:430/589/696/795 / SerialContext.tsx:246/256/262/274/286/294 / useSendData.ts:112）——归属见 §7 决策点 4。

🔴 **禁止机械加 deps**——加 deps 可能引发死循环/闭包过期（React 老坑）。每处先判断"依赖是真实需要还是误报"，真实依赖补上后验证不循环。

### §3.4 其余架构级小项

| 规则 | 处数 | 位置 | 定性 |
|:--|--:|:--|:--|
| `no-core-import-in-plugin` | 10（运行时 7 + 测试 3） | file-tree ×6 / marketplace ×3 | 插件独立铁律违规——**已有主人**：Phase 12 消灭 import + Phase 14 #80 升 error（含 import type 豁免 + 测试豁免），本 Phase 不重复 |
| `no-effect-callback-without-active-guard` | 5 | ContextMenu.tsx:140/166、OverlayPortal.tsx:36/49、serial-monitor SearchBar.tsx:25 | 硬约束 14（#59c Bug 2）——加 `if (!open) return` 守卫，潜在真 bug |
| `no-hardcoded-chinese` | 4 | editor index.tsx:13 / DiffEditor.tsx:94 / ExtensionItem.tsx:96 / UninstalledListView.tsx:69 | t() 包裹，5 分钟 |
| v3- 遗骨（no-restricted-syntax） | 6 | 全在 `src/core/api/v3Api.ts`（33/59/60×2/64×2） | v3 = 旧应用名遗骨——消费方排查：零消费 → 整删；有消费 → 改名 |

### §3.5 vitest 1 失败

`src/core/__tests__/DataConverter.test.ts:59` `expect(bytes).toEqual(new Uint8Array([0x41]))`——`toEqual` 对 Uint8Array 比引用不比内容（Uint8Array 无可枚举自有属性，vitest 打印"内容相同但判 false"）。实现 TextToBytes 正确，**测试自身的断言 bug**。修法：`expect(Array.from(bytes)).toEqual([0x41])`。

## §4 与既有 Phase 的边界与顺序

| 既有任务 | 关系 |
|:--|:--|
| Phase 10 #40 | 删 ShellPluginComponent.tsx → error 2 自动消失。**本 Phase 必须排在 Phase 10 之后** |
| Phase 10 #44 | 删 preload-plugin.ts → any 快照 −34 |
| Phase 10 #45 | 给 no-restricted-syntax 加 selector——#95 收窄在其后做 |
| Phase 10 #45.5 | shared/ 并入 electron/constants.ts 后删目录——与 #97 契约落点冲突，执行时协调 |
| Phase 11 #46-#53 | Registry 主进程化——#97 契约类型与 IPC 通道收敛（#63.6）同向，先后无冲突 |
| Phase 12 #54-#63.7 | 消灭插件 import @src/core → 10 处 core-import 警告的主力消解 |
| Phase 13 #64-#77 | 硬编码消灭（定点）——#96 分类归口承接其未覆盖的泛字符串比较 |
| Phase 14 #80 | no-core-import-in-plugin 升 error + 豁免——与 #95 规则工作姊妹，顺序上 #80 在前 |

**排序铁律：** 先删死文件（Phase 10）再修警告——审计数字是快照，修了要删的文件 = 白修。

## §5 任务表（9 任务，#94-#102）

| # | 任务 | 依赖 | 验收 |
|:--|:--|:--|:--|
| #94 | 重跑审计快照 + 3 errors 清零 | Phase 10（#40/#44 删除后） | eslint errors = 0；快照表更新 |
| #95 | no-restricted-syntax 规则收窄 + 补 switch/includes 盲点 + 真 pluginId 违规清零 + 升 error | #45、#94 | 规则零误伤；pluginId selector 为 error；CI 插违例自证变红 |
| #96 | 剩余字符串比较分类归口（§3.2 三类） | #95 | 跨堆协议值走契约类型；closed union 保持；可疑清零 |
| #97 | wire DTO 契约类型 + preload/API 边界 any 清零（§3.1） | Phase 12 | linkdesk-api.ts/preload 三兄弟签名全 typed；残余 any 仅 unknown 无法覆盖处且有注释 |
| #98 | 插件侧 + 壳 src 剩余 any 清零 | #97（契约类型先立） | src/ + plugins/ 下 no-explicit-any = 0 |
| #99 | exhaustive-deps 13 处逐个修（§3.3） | — | 13 处归零且无死循环/闭包回归 |
| #100 | active-guard 4 处 + 硬编码中文 4 处 | — | 两条规则归零 |
| #101 | v3Api 消费方排查（死→删 / 活→改名） | Phase 10 后 | v3- selector 零命中 |
| #102 | vitest 断言修复 + 门禁收口 | 全部 | npm run check 严格门禁通过（§6） |

## §6 门禁收口设计（#102）

修完 821 处警告 ≠ 整洁环境——警告会再长回来。收官 = 门禁机械零容忍：

```
npm run check =
  tsc --noEmit 两工程 0 错误
  + eslint --max-warnings 0（任何 warning 退出非零）
  + vitest 全绿（基线失败清零）
  + check-spacing-grid / check-pool-css-imports ✓
```

- `--max-warnings 0` 与 #95 升 error 互补：error = 局部即时反馈，max-warnings = 全量兜底；
- CLAUDE.md 提交自检同步改写（去掉"基线接受"口径）；
- 自证：故意插一条 warning 验证门禁红，再移除。

## §7 落点决策点（执行时定死）

1. **wire DTO 契约模块位置**：#45.5 计划删 shared/，但契约类型三方消费（electron preload / 壳 src / 池 src/pool）——推荐 shared/ 保留为纯类型/协议常量目录（electron 侧 import 已通，.js 后缀）；或另立 `src/core/types/ipc/`（池侧 import type 不受 Path B 拦——allowTypeImports:true）。二选一执行时定死，与 #45.5 协调。
2. **#95 收窄后的 selector 精确形态**：MemberExpression property 名 `/pluginId|plugin/` + Identifier 名 `/^pluginIds?$/`，以实测命中为准。
3. **v3Api 生死**：消费方 grep 结果定。
4. **plugins/user 树内插件的修复归属**：7 个 user 插件（serial-monitor 等）在树内、参与门禁（~100 处警告 + exhaustive-deps 12 + active-guard 1）。修复本身合法（树内 dev 插件，不是第三方作者代码），铁律"第三方作者不用改壳"不适用；但工作量要计入 #98/#99/#100。执行时定：全覆盖 or 门禁 scope 排除 user/（eslint ignore）。
