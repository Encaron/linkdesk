# 第三方作者实测反馈落地 · 交接（倒序追加，顶部 = 最新）

> **接力规矩**（照 L8 惯例）：每一棒收尾在**顶部**追加一段接力记录（做了什么 / 读数 / 发了什么版 / 留给下一棒什么），再把下方**队列整表**里自己那几格划掉（✅），整表传给下一棒。判「这条不做」必须把结论与理由写进自己的接力段。
> **任务真相源**：各格正文与状态以 [E6-执行清单.md](../E6-执行清单.md) §第 10 层 为准；**本档只管接力与队列**，不复制任务正文。
> **详案**：[00-整理档案](00-整理档案.md)（总纲·版本账·边界）→ [01](01-任务-SDK修补与dev宿主保真.md)（会话一）· [02](02-任务-SDK新腿两件.md)（会话三）· [03](03-任务-作者面文档与脚手架.md)（会话二）· [04](04-待拍板方向题.md)（待拍板三格）。

---

## 会话二（AI-C · `#138`-`#140`）＋ 会话三（AI-B · `#136`-`#137`）合并棒 —— 🔴 **本层收口报告**（2026-09-20）

> **本棒由来**：会话二（#138-#140）先做；收工前用户追加一句「**那你顺便把会话三的内容共两格：E6#136-#137。顺便做了。做完后该发版的直接发版**」——于是会话三并入本棒，且**发版授权一次性拿到**（docs 0.1.29/0.1.30、scaffold 0.1.13、SDK 0.1.44→0.1.46、官方 4 仓 PATCH、目录收录——全部真发上架）。本棒 = 本层**最后一棒**，故含 9 格读数摘要与收口。

**做了什么**

- `#138` **17 号点击行为矩阵 ＋ 00 号动线指针**：17 号新增「声明组合 → 图标栏点击行为」矩阵 **5 行**（entry+iconBar+tabBar⇒开标签页／entry+iconBar 无 tabBar 无视图容器⇒**点了没反应**／无 entry 侧栏⇒切容器再点收起／entry+tabBar+侧栏容器⇒**始终走侧栏切换**／statusBar⇒永不经图标栏）——每格以壳侧源码实测定（`IconBarZone.tsx` → `tabActions.ts`／`sidebarHost.ts` 两条消费链查证，⛔ 无一格凭记忆）＋ 17 号 §④ 三图模型指针块 ＋ 00 号动线两根指针（图标身份→06／README 媒体→12）。英文树真源 ＋ 中文维护者面同笔 ＋ `docs:build` 产物对账绿。
- `#139` **脚手架四件套**：icon 注释升级三图模型＋预置注释掉的 `marketIcon` 行；resources/ 预置带注释 `icon-bar.svg`（Type-1 单色剪影）＋`cover.svg`（README 封面）；contributes 每键一行速查（⚠️ **statusBar 是顶层字段非 contributes** 坑写明）；README「测试工具需自装」过时句修正 ＋ **会话一交接的定向任务**：「dev 宿主 `#ld-root` 有确定高度、直接写 `height: 100%`」约定句落地。`check-scaffold.mjs` 断言同步（EXPECTED_FILES＋npm pack 保真）。
- `#140` **发布篇 CI 小节**：作者面 13 号新增「Publishing from CI / non-interactive shells (AI agents included)」——token 取值链 ① env＞② 配置档＞③ 掩码（非 TTY 直接失败不挂起）＋ bash/GitHub Actions 示例 ＋ 经典 PAT 401=到期提醒。⛔ 未做 token 存储机制、未改 publish.ts。
- `#136` **TSX 悬空类名腿**：新腿 `dangling-own-classes`（自有前缀 className 字面量引用必须在自有 CSS 有定义；定义集 = 提及集、孤儿 `*/` 段剔除与运行时「规则被吞」如实对齐、动态拼接跳过计数、fail-closed）＋单测 9 例；**18 仓普查＝11 个死类名／17 处站点**（editor 4 名 6 处／marketplace 2 名 6 处／serial-monitor 4 名 4 处／settings 1 名 1 处），逐处裁决全为「仓内 CSS 零定义＋零消费」⇒ 删净。
- `#137` **window keydown 腿**：新腿 `no-global-key-listener`（字面量红；文案教 focus 分区正解＝容器 `onKeyDown`＋`tabIndex`；豁免走 disable＋理由、无白名单）＋单测 6 例；普查 5 处逐处裁决——官方 3 处（serial-monitor SearchBar 的 Ctrl+F/Esc/Enter、settings 录制器 capture=true）**全为正当形态**⇒ 豁免注释写明理由、行为零变化；🔴 第三方 `geme-tihu-bicycle` 2 处（index.tsx:272/273）**不代改**（转达作者下版自修）。**顺笔两件修复**（都由新腿豁免注释首次真实踩中）：preset 登记脚本伪 id 为 no-op 哑规则（豁免注释不再触发「Definition for rule was not found」假红）＋ 豁免解析器跨行块注释换行**双重计数** bug（settings CI 首次真实踩中——其后 disable 行号整体偏大、豁免错位失效）＋回归钉子单测。

**发了什么版**（全部真发上架，2026-09-20；`release:mark` 五基线 = contracts 0.1.20／plugin-sdk 0.1.46／create-linkdesk-plugin 0.1.13／ui 0.2.15／plugin-docs 0.1.30）

| 包/仓 | 版本 | 内容 |
|:--|:--|:--|
| `@linkdesk/plugin-docs` | 0.1.29 → **0.1.30** | 会话二批（17/00/13 号）→ 补发（中文 05 号两腿段落随包重生成） |
| `create-linkdesk-plugin` | **0.1.13** | 脚手架四件套 ＋ README 两处句 |
| `@linkdesk/plugin-sdk` | 0.1.43 → **0.1.46** | 会话一 0.1.43；会话三 0.1.44→0.1.45→0.1.46 同日三连（0.1.44/0.1.45 短命无消费者） |
| editor / marketplace / serial-monitor / settings | **1.0.14 / 1.0.43 / 1.0.22 / 1.0.18** | `#136`/`#137` 存量整改 PATCH，逐仓 Release ＋ 目录 upsert 收录（E:/linkdesk-marketplace `4dc1f7f`） |

**现场证**（memory `sdk-published-not-equal-enforced` 口径：读货架实装 ＋ 塞违规看红）：货架 tmp 工程实装 0.1.46 ⇒ 塞 `demo-root-card`（CSS 有定义但注释被 `*/` 提前闭合吞规则）＋`demo-root-ghost`（拼错）⇒ own 腿红 2、`orphanSegments=1`、文案带「提前闭合」；塞 `window.addEventListener("keydown")` ⇒ key 腿红 1、文案教正解。**4 仓 CI 全绿**（settings 首轮红 = 解析器 bug 首次真实踩中 ⇒ 修复后转绿）。

**本层 9 格终态**：全部 ✅（逐格读数已回写 [E6-执行清单](../E6-执行清单.md) §第 10 层，该层头含收口行）。**壳 0.2.15 未动**——9 格零改 `src/`/`electron/`，兑现立案承诺（第三方反馈的落点几乎全在作者轴四包，这本身就是本层价值的证明）。

**残留边界（留给未来，不是待办）**

1. 🔴 **转达作者一件**：`geme-tihu-bicycle` 的 2 处 `window.addEventListener("keydown")`（index.tsx:272/273）未代改——**下次给该作者发消息时带一句**：新腿 `no-global-key-listener`（SDK 0.1.46 起）会判红，正解 = 容器 `onKeyDown`＋`tabIndex`（见作者面 05-ui-conventions §4.2），确需全局抓键走 disable＋理由。
2. 官方 4 仓 CHANGELOG 里写「SDK 0.1.44/0.1.45」而最终 lock 统一刷到 **0.1.46**（lock-only 笔已注明，插件内容与 Release 资产零变化）——不值得为一句版本号再 PATCH 四仓，此处报备。
3. `#141`-`#143` 三待拍板格（game 模板＋canvas mock／contentCanvas 一等声明／catalog:submit）**未动**，照 [04](04-待拍板方向题.md) 等拍板。
4. 壳仓（linkdesk 本仓）**全程零 push**（用户红线）——本层所有壳仓提交只落本地；插件仓与目录仓已推（唯二例外，用户授权）。
5. SDK 工作树与已发 0.1.46 有**一行尾空行差异**（`src/eslint/rules.ts` 伪 id 段末尾——pre-commit 风格门禁要求清除，0.1.46 tarball 里带着它）：空白级、零行为影响 ⇒ **未为此重发 npm**，`release:mark --allow-drift` 显式记基线（黄灯灭，基线文件内注记「本次绕过货架核对」）；下一次 SDK 真发版自然抹平。

**下一棒**：本层已收官，队列无待派格。续做 = `#141`-`#143` 拍板后照 [04] 开格；或按 [00 总纲](00-整理档案.md) §〇e 不做清单收边。

---

## 队列整表（本批 9 执行格 + 3 待拍板格；状态以本表 + 清单格双写，冲突以清单为准）

| 会话 | 格 | 一句话 | 发版 | 状态 |
|:--:|:--:|:--|:--|:--|
| 一 | `#132` | css-hardcode 提示语对齐 token 库（＋防再脱节单测） | SDK 0.1.43 | ✅ 2026-09-20 |
| 一 | `#133` | publish 收尾「远端已更新，建议 git pull」提示 | SDK 0.1.43 | ✅ 2026-09-20 |
| 一 | `#134` | dev 宿主保真两件（`#ld-root` 定高 ＋ 兜底 token 对齐 ui 包） | SDK 0.1.43 | ✅ 2026-09-20 |
| 一 | `#135` | marketplace.schema.json 随包 | SDK 0.1.43 | ✅ 2026-09-20 |
| 二 | `#138` | 17 号组合行为矩阵 ＋ 00 号动线指针（三图模型） | docs 0.1.29 | ✅ 2026-09-20 |
| 二 | `#139` | 脚手架四件套（icon 三图注释/资源模板/contributes 速查/README 句） | scaffold 0.1.13 | ✅ 2026-09-20 |
| 二 | `#140` | 作者面发布文档补「非交互 token」一节 | docs 0.1.29 | ✅ 2026-09-20 |
| 三 | `#136` | TSX 悬空类名腿（窄口子＋口径锚定＋18 仓普查） | SDK 0.1.46（计划 0.1.44） | ✅ 2026-09-20 |
| 三 | `#137` | window keydown 腿（含官方 2 仓逐处裁决整改） | SDK 0.1.46（＋官方 4 仓 PATCH） | ✅ 2026-09-20 |
| — | `#141` | game 模板 ＋ canvas mock | — | 🟡 待拍板（[04](04-待拍板方向题.md)） |
| — | `#142` | contentCanvas 一等声明 | — | 🟡 待拍板（[04](04-待拍板方向题.md)） |
| — | `#143` | catalog:submit | — | 🟡 待拍板（[04](04-待拍板方向题.md)） |

## 编排（用户拍板：「几个任务合为一个 AI，不要一个 AI 全做，也不要一个 AI 一个任务」）

```
并行启动：
  会话一（AI-A）＝ #132–#135  → SDK 0.1.43 一批发   【只动 packages/plugin-sdk】
  会话二（AI-C）＝ #138–#140  → docs 0.1.29 + scaffold 0.1.13
                                【只动作者面文档树 + plugin-docs + create-linkdesk-plugin】
会话一收尾后：
  会话三（AI-B）＝ #136–#137  → SDK 0.1.44 + 官方 2 仓按裁决 PATCH   【本层最后一棒，出收口段】
随时（你一句话即可插队）：
  #141–#143 拍板后照 [04] 开格（与三会话无文件域冲突，但 🆕 动 plugin-docs/SDK 前看当时版本账）
```

- **并行依据**：会话一与二文件域**零重叠**（SDK ↔ 文档/脚手架）；会话三对 SDK 的发版依赖会话一（版本串行），对两官方仓的裁决不依赖任何人。
- **⛔ 冲突窗口**：会话三若与 #113（插件侧死类清账）撞期——两者都可能动 `serial-monitor`/`settings` 仓，**别同时派**（照 [派单序 §一档 #113 的独占要求](../E6-剩余任务分档-派单序.md)）。

## 版本账快照（立案时点）

| 包 | 立案时版本 | 本批终点 |
|:--|:--|:--|
| `@linkdesk/plugin-sdk` | 0.1.42 | 0.1.44（两批：0.1.43 会话一 → 0.1.44 会话三） |
| `@linkdesk/plugin-docs` | 0.1.28 | 0.1.29（会话二） |
| `create-linkdesk-plugin` | 0.1.12 | 0.1.13（会话二） |
| **壳（软件）** | **0.2.15** | **不变**（九格无一改 `src/`/`electron/`） |
| `@linkdesk/ui` / contracts | 0.2.15 / 0.1.20 | 不变 |
| `serial-monitor` / `settings` | 1.0.20 / （读 plugin.json） | 按裁决 PATCH 或不动（会话三） |

## 每棒必跑的连带面（收尾自检，逐条打勾写进接力段）

1. `npm run check` 全绿（含新增自测**已接线**）。
2. 动过模板 ⇒ `node scripts/check-scaffold.mjs`；动过作者面英文树 ⇒ `npm run docs:build` ＋ `docs:check` ＋ 双语对齐门禁 ＋ 中文维护者面同笔；动过维护者面写法规约 ⇒ `npm run sync:plugin-agents --check`。
3. 发版后现场证（memory `sdk-published-not-equal-enforced`：读消费者 node_modules 版本 ＋ 塞违规看它红）。
4. 官方目录收录：凡插件仓 bump 重发 ⇒ 两步走补收录（memory `plugin-source-external-repos`）。
5. 发版 / 推送 / 写别人的仓——**都等用户点头**（红线）。

## ⚠️ 会话三留给用户的转达事项（现成清单）

- `geme-tihu-bicycle`（第三方仓）`src/index.tsx:272–273` 两处 window keydown 在 #137 腿落地后 CI 会红——正解（容器 `onKeyDown` + `tabIndex`，见作者面 05-ui-conventions §4.2）已在腿的报错文案里，等作者下版自修；本工程不代改。

---

## 接力记录

### 2026-09-20 · 会话一（AI-A）：#132–#135 SDK 修补与 dev 宿主保真 —— ✅ 全清，🔴 **SDK 0.1.43 已发**

- **四格全落地**（一笔代码 `dcb22fd51` ＋ 一笔发版 `3be4ac6bc`，全程未 push）：#132 报错文案收敛单一常量 `CSS_TOKEN_HINT`（css/tsx 两处消费）＋补 `--shadow-*` 等七族＋**删真源不存在的 `--surface-*`**＋对账单测；#133 publish 收尾 `PUBLISH_REMOTE_UPDATED_HINT`（建议 git pull）＋unpushed 分支补 pull 指引（原「tag 挪正」保留）；#134 `#ld-root` flex 链定高＋`:root` 兜底 94 token 全集照抄＋对账单测三断言＋[02-本地预览环境.md §3.2](../02-插件开发工具链/02-本地预览环境.md) 两条保真约定；#135 `marketplace.schema.json` 随包（publish.ts 类型为真源，全封闭）＋**真实产出过 schema** 同源对账。
- **发版**：`@linkdesk/plugin-sdk` **0.1.42 → 0.1.43**（npm publish 2026-09-20 用户点头；`release:mark` 基线已记——**顺带补记了上棒欠记的 `@linkdesk/ui@0.2.15`**；CHANGELOG v0.2.15 节补作者轴行；root/plugin-sdk 双 lock 同步，check:lockfile-sync 绿）。
- **读数**：全量 check 真绿 186 文件 / 2557 测试（净增 17 例单测）；实机 CDP（file-tree 真仓 dev 宿主）：`#ld-root` computed **580px**、子元素 height:100% 撑满、`--bg-card` `#2D2D2D`／`--text-primary`／`--shadow-float` 有值；现场证两条照 memory `sdk-published-not-equal-enforced`：货架 tmp 实装读数 **0.1.43** ＋ 塞 box-shadow 违规看红且文案即新 `CSS_TOKEN_HINT`（`--shadow-*` 在列）。
- **⚠️ 对账真源口径（给后续所有棒）**：SDK 内两处对账单测（css-hardcode / dev-host）锚**壳 `src/index.css` 顶层 `:root` 块**（进 git 的真源，94 token）——实测 `@linkdesk/ui` dist css 是构建产物、token 定义仅 `--danger`/`--ghost` 两个自有；**别改锚 dist**（干净 checkout 会挂 + 口径漂移）。
- **留给会话三（AI-B）**：🔴 **开工闸门已解除（0.1.43 已发）**——#136/#137 照 [02 号档](02-任务-SDK新腿两件.md) 执行，发版目标 0.1.44；⚠️ 与 #113 撞期纪律照旧（`serial-monitor`/`settings` 别同时派）；官方 2 仓 keydown 摸底开工前**重跑**（读数可能已漂）。
- **留给会话二（AI-C）**：#134 的「插件可放心假设容器有确定高度、直接写 `height:100%`」约定句，作者面英文树归 **#139 脚手架 README** 落地（本棒未动英文树——红线禁区；01 号档 §三的交接声明兑现于此）。
- **上棒遗留（不在我文件域，留用户处置）**：仓根未跟踪的 `E6-执行清单.html`（来历不明，本棒未动）。

### 2026-09-20 · 立案棒（本会话，零代码改动）

- 完成：16 条核对（结论见 [00 §〇b](00-整理档案.md)）→ 立 9 执行格 + 3 待拍板格；五份档案落笔（00–04 ＋ 本表）；E6 清单 §第 10 层 立段；[派单序](../E6-剩余任务分档-派单序.md) 增补；CLAUDE.md 头部进度行与关键文件表同笔。
- 基线读数已钉死：SDK 0.1.42 / docs 0.1.28 / scaffold 0.1.12 / 壳 0.2.14；18 仓 keydown 摸底 3 处（geme-tihu-bicycle:272–273 · serial-monitor SearchBar:35/53 · settings useKeybindingEditor:96 capture=true）——会话三开工前**重跑摸底**（读数可能已漂）。
- 留给下一棒：三格详案即任务书；⚠️ 会话一详案 §三里「作者面约定句」若并入 #139，交接段要写清归属，避免两棒都动作者面树。
