# 06-任务——作者面文档 + 脚手架同步（E6#126）

> **第 9.5 轮 · 前置：#124 ✅（重锚号已定案）· 发版轴：plugin-docs PATCH（随 #127 真发）**
> **一句话**：两棵作者面树 + 脚手架 + 07 号设计档，把「组件编译进插件 bundle」的旧语义全网订正为新语义。**🔴 判据：完成后全仓 grep 不到旧语义残句。**

## 一、改动点

1. **作者面两棵树**（`docs/03-插件制造/` 中文维护者面 + `docs/03-plugin-authoring/` 英文作者面，双语对齐门禁守）：
   - `@linkdesk/ui` 的说明段（含 05 §12 CSS 命名空间相关、19 速查表相关行）改写新语义：**运行时由壳池统一供给（import-map），本包 = 类型契约 + dev 解析体；样式由池 vendor css 供给，⛔ 插件源码不 import css**。
   - 新增「版本一条线」小节：类型包版本 = 壳版本（锁步）；旧类型 + 新壳 = 旧组件自动吃壳更新；用新组件才需升类型 + rebuild；`minAppVersion` 写法。
   - 行为承诺段（给作者的契约话术）：只加不删 / 同版本内行为不变 / 样式随壳更新——对齐 L8 兼容机械化五词口径，⛔ 过度承诺（不写「永远不坏」，写门禁与退役流程保什么）。
2. **`packages/plugin-docs/`** 重生成 + PATCH bump（照 #120 先例：只走 `docs:build`）。
3. **`create-linkdesk-plugin` 脚手架**：模板 `devDependencies."@linkdesk/ui"` 改为装配时解析当前重锚号（模板占位 → `npm i @linkdesk/ui@<当前壳版>` 语义；照 template v2 机制定实现）；模板内旧语义文案同步。
4. **壳仓设计档回填**：`01-插件独立构建/07-共享组件独立分发设计.md` 顶部加「状态更新」块——分发式 → 集中供给的翻转指针指向本层档案；⑤「其它皆内联」句订正。⛔ 不重写整档（历史档案留证据，加块不涂改）。
5. **CLAUDE.md / AGENTS.md 指针**：若关键文件表 L9 行尚缺细节，同笔补（立案时已加行——本格核对描述不过期）。

## 二、验收

- `npm run check` 全绿（含 `check-author-docs-*` 三件套 + 双语门禁）。
- 全仓 grep「编译进各插件 bundle / 其它皆内联」旧语义残句**零命中**（历史封存档案内引用除外——以是否误导新读者为准，逐条判）。
- 脚手架实跑一次（照 #108 F5 的 npx 缓存教训：验证**裸 specifier**拉到的是新模板）。

## 三、回勾要求

- 本档执行记录 + 清单回勾 + 交接本一段。

---

## 执行记录（2026-09-19 · 会话 C）

### 一、改动清单（逐条对 §一 五条）

**① 作者面两棵树（中英逐篇对齐，双语门禁绿）**

| 文件（中 / 英） | 改了什么 |
|:--|:--|
| `03-插件制造/19-组件速查.md` / `03-plugin-authoring/19-component-cheatsheet.md` | §二 改写新语义（组件代码与样式由壳池运行时单实例供给、包 = 类型契约 + dev 解析体；示例加 `⛔ 不要 import "@linkdesk/ui/index.css"`）；**新增 §2.1「版本一条线（没有对照表）」**（ui 版本 = 壳版本；该装哪个 / 不用追着升 / 什么时候必须动）；**新增 §2.2「壳对作者的三条承诺」**（只加不删 / 行为改走新名字 / 样式跟着壳走 —— 每条注机械兜底：导出面快照门禁）；§五 常见错误加一行「壳改了样式我的插件没跟随 ⇒ 源码 import 了 css」 |
| `05-插件UI写法规约.md` / `05-ui-conventions.md` | §12.4「宿主以后改共享名，你这边的动作永远是两件：① 升依赖 ② 重新打包。**依赖是内联进你的 bundle 的**」整段删除，换为「不必改代码/改 CSS（壳池供给）」＋ 新增 css-import 判红条（`linkdesk/no-ui-css-import`，**无豁免出口**）；`@linkdesk/ui 0.3.0 起` / `升到 0.3.0 时` 两处改「2026-09-16 那轮起」＋「壳升到含该改名的版本时」（0.3.x 线已弃用，⛔ 不再指向它） |
| `01-插件API契约.md` / `01-plugin-api-contract.md` | 允许 import 表：`（"@linkdesk/ui": "^0.1.4"）` 旧例 → 「版本与你要支持的壳同号（壳 0.2.13 ⇒ `"^0.2.13"`）」＋ ⛔ 不 import css 一句 |
| `00-README.md` / `00-readme.md` | 工具链表 ui 行：加「运行时由壳池供给同一份实例（装它为类型与本地 dev）」「版本与壳同号（一条线）」 |
| `10-如何造一个设置插件.md` / `10-building-a-settings-plugin.md` | §共享控件：加同号版本线 + ⛔ 不 import css |
| `13-插件开发指南.md` / `13-development-guide.md` | 第 6 步「用共享组件」：加壳池供给 + 同号线 + ⛔ 不 import css |

`@linkdesk/ui` 版本一条线的**唯一真相源**仍 = [00-整理档案 §三](00-整理档案.md) + version-bump skill §〇；作者面只给判据（同号 / 无对照表），⛔ 不抄版本表。

**② `packages/linkdesk-ui/README.md`（维护段）**：删「has its own version axis — publishing it does not bump the application…」→ 改为**锁步例外**（L9 重锚：ui 版本 = 壳版本，壳每次发版必带同号 bump，撞号壳让位；机械断言 = `check-publish-gate.mjs` 判据⑤ `judgeUiVersionLockstep`）；表面定义改「改组件 = 壳树与该包同动 ⇒ 重建 + 与壳同号发布」。

**③ plugin-docs 重生成 + PATCH**：`npm run docs:build` → 47 文件；版本 0.1.23 → **0.1.24**。

**④ 脚手架「装配时解析当前重锚号」**：模板 `package.json` 首次带 `dependencies: { "@linkdesk/ui": "latest" }` —— 模板里**不写死号**，作者 `npm install` 那一刻货架 `latest` 指向哪条线就装哪条（= 当前重锚号，装配时解析）；要保底自己钉（模板 README 已写）。CLI 零依赖零网络、**不代跑 npm install** ⇒ 这是唯一能表达「装配时解析」的形态（`check-scaffold.mjs` 的 8 条断言不含依赖集，未受影响）。模板 README 同笔说明；脚手架档 [02-插件开发工具链/01](../02-插件开发工具链/01-create-linkdesk-plugin脚手架.md) 加 **E6#126 条**（模板列表行 + 新增段落）。

**⑤ 07 号设计档**：[01-插件独立构建/07-共享组件独立分发设计.md](../01-插件独立构建/07-共享组件独立分发设计.md) 顶部加「状态更新」块（分发式 → 集中供给 / 零改动 / 版本语义翻转 / 新语义真相源指针）；§⑤ 第 28 行与 §二 表 A 行的旧句**划线 + 一行订正**，⛔ 不重写整档。

**附带（用户点名）**：`packages/plugin-sdk/src/eslint/checks/ui-css-import.ts` 的 `UI_CSS_IMPORT_WHY` 与头注「报文指向」由占位（指向 L9 档案）改指**作者面文档**（19 速查 §二 / 05 §12.4，中英双树名都写出）。

### 二、判据：旧语义残句全仓 grep（逐条判）

| 处 | 位置 | 判定 |
|:--|:--|:--|
| 「编译进各插件 bundle」 | `E6-执行清单.md:2061` / `:2260`、`UI集中供给/00-整理档案.md:4` | ✅ **保留** —— 是 L9 新语义的「从 X **翻转为** Y」描述（描述翻动作本身，不误导） |
| 同上 | `01-插件独立构建/07-共享组件独立分发设计.md:28`、§二 表 A 行 | ✅ **划线 + 状态块** —— E6#54 立案证据，留历史不涂改 |
| 「其它皆内联」 | 同上两处 | ✅ 同上 |
| L8 历史快照 | `插件兼容机械化/07-图解-版本线与兼容场景.html`（证据盒「设计文档原话」） | ⚠️ **加 2026-09-19 补注**（原文是当日实测快照，标注 L9 已翻转 + 指针） |
| L8 历史快照 | `插件兼容机械化/08-任务-通用悬停提示卡.md:25`（「ui 不在 external 名单 ⇒ 编译进各插件 bundle ⇒ 三步」） | ⚠️ **加 2026-09-19 补注**（结论「要调用新件仍需自己加 import + 重发」仍成立；被推翻的是「零件编译进 bundle」这个前提，补注写明壳升级即得实现） |
| L8 补注块 | `E6-执行清单.md:2278`（#120 的「不涉 SDK」理由后半句） | ⚠️ **加 2026-09-19 补注**（结论仍成立于当日，理由以 L9 档案为准） |

**读法**：本判据的「零命中」= **旧语义不再有误导性陈述**，历史封存档案内引用按 §二 明示「逐条判」——三条补注均为**加注不涂改**。

### 三、版本与机械读数

- bump：`@linkdesk/plugin-sdk` 0.1.40 → **0.1.41**（报文/头注动了 = 发货面动了）；`@linkdesk/plugin-docs` 0.1.23 → **0.1.24**；`create-linkdesk-plugin` 0.1.10 → **0.1.11**；`@linkdesk/ui` 不动（0.2.13，归 #124）。
- `npm install` → lock 四号回读：`create-linkdesk-plugin 0.1.11` / `@linkdesk/ui 0.2.13` / `@linkdesk/plugin-docs 0.1.24` / `@linkdesk/plugin-sdk 0.1.41` ✓（`check-lockfile-sync` 随 check 绿）。
- `npm run check` = **全绿**（180 文件 / 2517 例；含 `docs:check` 逐字节比对、`check-scaffold` 生成物契约、双语/符号/链接三件套）。
- `npm run check:npm-release` = 四盏黄灯（ui / sdk / docs / scaffold「版本动过未 mark」）——**预期**：真发在紧随的发布批（#125 之前），`release:mark` 归 #127。

### 四、执行序（偏离已定编排的显式记账）

任务书原计划「plugin-docs PATCH **随 #127 真发**」。实际改为：**#126 内容 + bump 一次做完并提交；四包真实发布压缩成一批，排在 #125 之前**。理由两条：① #125 的四仓必须 `npm install --prefer-online` 装到**货架上的真版本**（ui 0.2.13 / sdk 0.1.41）；② 本格改了 SDK 发货面 —— SDK 若晚于 #125 才发，四仓重 build 用的就不是含 `linkdesk/no-ui-css-import` 判据的那一版（发出去 ≠ 生效，memory `sdk-published-not-equal-enforced`）。`release:mark` 仍归 #127（批次原子，四包 + ui 一起记基线）。
