# 交接.md ——「插件兼容机械化」批次接力本

> **读法**：**倒序，顶部 = 最新**。**只追加，不改旧段**（旧段是历史，改了就丢证据）。
> **一次可做多格**（🔴 **2026-09-18 用户取消原「一 AI 一格」**；建议编排 = **3 会话 × 2 格**，见顶部「编排变更」段）：一个会话**可**做一格或多格，**但每格各自收尾**——做完**每一格**都在**顶部追加**一段「给下一个 AI 的话」（**两格 = 两段**，⛔ 不许揉成一笔），并把**剩余任务队列表整张复制**进新段、把自己那格改 ✅；**做完一格就提交一格**。
> **批次总纲**（权威方案 / 6 格全貌 / 禁区 / 复量命令）：[00-整理档案.md](00-整理档案.md)
> **上位清单**：[E6-执行清单.md](../E6-执行清单.md) 第 8 层（勾格与轮次进度行落在那里）。

---

## ← 第 6 格（格 6 · `E6#119` **判据铺开**）· ✅ 已完成（2026-09-19）：**给下一个 AI ＝ 格 7 · `E6#120` 通用悬停提示卡（本批最后一棒）**

### ⑩ 🔴 先说你是谁（⛔ 别猜）

你是**这一棒的执行者**，任务是 [08-任务-通用悬停提示卡.md](08-任务-通用悬停提示卡.md)（`E6#120`）——**本批最后一棒**：除自己的档外要出**全批收口报告**（素材 = [06 §8.7](06-任务-判据铺开.md)，**⛔ 不许重跑**）。
- **以本段为准**：本段与你读到的任何别的说法冲突，**用本段**。拿不到本段 ⇒ 先问用户「我是不是格 7」。
- **你不是复核者**（本批没有「复核轮」这种格），**也不是派工者**。
- ⚠️ 本批一个会话可做多格，但**每格各自收尾、各自提交**：格 6 已单独提交。

### ⑴ 上一棒（格 6）做完了什么（一句话 ＋ 读数）

- **一句话**：把格 1 的悬空名判据**搬进 SDK 随包下发**——作者在他自己的仓里跑 `linkdesk-plugin-sdk lint` / `ci-verify` 就能跑出红黄；宿主定义集走**新生成物** `packages/plugin-sdk/schemas/host-css-names.json`（与壳运行时清单同一生成器）；退役名提示同步落地（**只提示、永不拒绝**）。
- **读数**：SDK **0.1.39 → 0.1.40** ＋ plugin-docs **0.1.20 → 0.1.21**（均 **bump 未发**——🔴 用户 2026-09-19 拍板：发版等 6、7 做完一起发）· SDK 单测 **17 例** ＋ 尺子 selfTest **14/14**（含 `锚⑩`）· 18 仓本地真跑**悬空 0 / 退役提示 0**（与格 1 基线对上）· **真仓负控真红**（serial-monitor 复制品塞 `ldk-ghost-negative-control` ⇒ 报 `NegativeControl.tsx:1`）· 壳仓**零代码改动**（恒 0.2.12）· `npm run check` **EXIT=0**（178 文件 / 2502 例）。
- **执行记录全文**（落地表 ／ `锚⑩` 6 句 ／ 18 仓读数与 lock ／ 发版实录 ／ 残余边界 6 条 ／ **§8.7 收口素材**）：[06 §八](06-任务-判据铺开.md)。**别重写它**。
- 🔴 **与格 7 直接相关的两句**：① 格 6 若把类名定义集加进了 `schemas/host-reserved.json` 就要 regen——**没有发生**：定义集走了**独立的新文件** `host-css-names.json`（账与面不混装），`host-reserved.json` 本格零改动；② 你新增 `ldk-hint-card` 类名后**不需要**动这份定义集生成链的判定——但**要**跑 `npm run host-css:regen`（新类名进宿主定义集三份投影）＋ `audit:plugin-scope:regen` 不涉及（`ldk-` 是通配家族，零保留名登记）。

### ⑵ 剩余任务队列（**自己那格已改 ✅ → 你做完改你那格，再整张复制进新段**）

| 序 | 轮次 | 编号 | 任务 | 详案 | 前置 | 状态 |
|:--:|:--|:--|:--|:--|:--|:--:|
| 1 | 第 8.1 轮 | `E6#114` | 悬空名核验（产物层只读审计） | [01](01-任务-悬空名核验.md) | 无 | ✅ |
| 2 | 第 8.2 轮 | `E6#115` | 只加不删门禁（面快照 ＋ diff ＋ 判红） | [02](02-任务-只加不删门禁.md) | 无（与格 1 互不依赖） | ✅ |
| 3 | 第 8.3 轮 | `E6#116` | 退役登记（账 ＋ 流程 ＋ 机械对账） | [03](03-任务-退役登记.md) | 格 2 ✅ | ✅ |
| 4 | 第 8.4 轮 | `E6#117` | 兼容读数 | [04](04-任务-兼容读数.md) | 格 1 ✅ | ✅ |
| 5 | 第 8.5 轮 | `E6#118` | 市场显示状态（**含 HTML 设计图**） | [05](05-任务-市场显示状态.md) | 格 4 ＋ **用户点头 ✅ 2026-09-18** | ✅ |
| 6 | 第 8.6 轮 | `E6#119` | 判据铺开（SDK 随包 ＋ 18 仓接腿 ＋ 发版） | [06](06-任务-判据铺开.md) | 格 1 ✅ ＋ 格 2 ✅ | ✅ |
| 7 | 第 8.7 轮 | `E6#120` | 通用悬停提示卡（**壳的共享件**）＝ **本批最后一棒**（＋ 全批收口报告） | [08](08-任务-通用悬停提示卡.md) | 格 5 ✅（⚠️ **可换序、⛔ 不并行**） | ⬜ |

> **执行序 = 自上而下**。🔴 **格数是上限不是配额**（**7 格**一格不能省；判「不做」要写理由，写进本交接段的「不做」节）。
> ⚠️ **别与 `E6#113`（插件侧死类清账）并行**——那格要动 3 个插件仓，格 7 也动 `marketplace`。

### ⑶ 你这一棒（格 7）具体做什么——**以 [08 号档](08-任务-通用悬停提示卡.md) 为准**，这里只点三条

1. **动 CSS 前必走硬约束 16**：先 `Skill(ui-ux-pro-max)` 拿设计系统再写 `HintCard.css`（08 §一.8，纪律不是可选项）；样式只走 token，过 `check-spacing-grid` ＋ `check-font-scale-audit`。
2. **玻璃结构铁律**（08 §一.3）：卡体表面必须是 `OverlayPortal` 卡根下 `[data-overlay-wrapper]` 的**直接子**——多包一层就没玻璃；`OverlayPortal` 一行不改（不传 `trapFocus`、`closeOnOutsideClick={false}`、卡体 `pointer-events:none`）。
3. **文案与触发逐字保留**（格 5 段 ① 预交的替换清单）：`marketplace` 的 `CompatStatus.tsx` 内 `marketplace-mpd-compat-card` 卡体 → 共享件 `<HintCard>`（**容器/触发/文案不动**——容器与触发逻辑留在组件里，只换卡体渲染）；替换后 grep `marketplace-mpd-compat-card` 应**零命中**。⚠️ 插件仓有未推送的 `c5e8b8f`（1.0.36），在它之上继续改。

### ⑷ 本格实测踩到的坑（**别重踩**）

| 坑 | 实情与正解 |
|:--|:--|
| 注释里写 `packages/**/*.test.ts` | `*/` **提前终止块注释**（esbuild 当场 parse 红——格 4 坑单同款再撞）——注释里别写 `*/` 序列 |
| vitest 里 `import.meta.url` 不是 file 协议 | 锚词测试读壳尺子路径用 `process.cwd()`（壳根）找 ＋ URL 解析兜底；别直接 `fileURLToPath(new URL(...))` |
| Git Bash 的 `/tmp` ≠ Node 的 `/tmp` | 临时夹具放真实 Windows 路径（如 `scratch/negctl-fixture/`），否则 Node 空扫给你假绿；跑完即删 |
| ESM 导入 Windows 绝对路径 | `ERR_UNSUPPORTED_ESM_URL_SCHEME`——用相对导入（scratch 驱动 `../packages/...`） |
| tsx 跑 SDK 源码 | `npx tsx scratch/dangling-18repo.mjs`（.ts 的 `.js` 后缀导入 tsx 能解）——18 仓真跑照此复跑 |
| `check-doc-links` 会抓文档链接 | 中文树里链英文文件名（`16-naming-conventions.md`）当场红——两棵树链接各指各的 |

### ⑸ 不做（**边界，明写理由**）

| 不做 | 理由 |
|:--|:--|
| `npm publish`（SDK 0.1.40 / docs 0.1.21）/ 18 仓 lock 升级 / 推任何仓 | 🔴 用户红线：**2026-09-19 拍板发版等 6、7 做完一起发**；lock 未发版时升级会让 `npm ci` 挂 |
| 18 仓任何文件改动 | SDK 未发布 ⇒ CI 无法验证；铺开边界如实写进 [06 §8.3](06-任务-判据铺开.md)（⛔ 不许写成「已铺开」） |
| 壳仓 `src/` 代码改动 | 格 6 零壳侧需求（判据全在 SDK 随包轴；壳只动 `scripts/` 生成器与尺子锚词） |
| 把宿主定义集并进 `host-reserved.json` | **账与面不混装**（格 4 生成器头注先例）：那是保留名账（退役账门禁看管），定义集是纯投影——走了独立文件 `host-css-names.json` |
| 退役提示升级成拒绝 | 本格禁区④＋00 总纲：`retired[]` 不是黑名单，永远只提示 |
| 不推 `git push` | 🔴 用户红线：等用户本人点头（壳仓未推送项已积 883f75b1d / 7cd2af652 / 4f23a675f ＋ 本格） |

### ⑹ 提交清单（格 6）

- **新增**：`packages/plugin-sdk/src/eslint/checks/dangling-names.ts` · `…/retired-names.ts` · `…/dangling-names.test.ts` · `packages/plugin-sdk/schemas/host-css-names.json`（生成物）。
- **改动**：`packages/plugin-sdk/src/eslint/lint.ts` ＋ `index.ts` · `scripts/gen-host-css-manifest.mjs` · `scripts/plugin-dangling-name-audit.mjs` · `packages/plugin-sdk/package.json`（0.1.40）· `packages/plugin-docs/**`（重生成）· `packages/plugin-docs/package.json`（0.1.21）· `package-lock.json` · `docs/03-插件制造/16-命名规范.md` ＋ `docs/03-plugin-authoring/16-naming-conventions.md` ＋ 两树 05 号档 · `docs/…/src/core/compat/host-css.generated.ts` 未动（投影未变）。
- ⚠️ 提交信息带类别前缀（`feat:` / `docs:`）；`git add` 按**显式路径**（⛔ 禁 `git add -A`）。**本格提交 sha = `3c4fb68b1`**（24 个文件 · **未推送**）；上一棒格 5 = 插件仓 `c5e8b8f` ＋ 壳仓 `7cd2af652`/`4f23a675f`。

### ⑺ 给格 7 的输入（**可直接抄的东西**）

- **收口素材（全批报告的本体输入）**：[06 §8.7](06-任务-判据铺开.md)——六条判据逐条读数 ＋ 残余边界 ＋ 大白话四问草稿，**直接汇总，⛔ 不重跑**。
- **发版链现状账**：[05 §9.4](05-任务-市场显示状态.md) ＋ [06 §8.4](06-任务-判据铺开.md) ＋ 本段 §⑸——黄灯合计：SDK 0.1.40 · plugin-docs 0.1.19/0.1.20/0.1.21 · marketplace 1.0.36；发版链 = publish → 目录收录 → `sync:bundled --latest`（**要代理**）→ 壳 tag。
- **读数形状与唯一入口**（08 号档 §一.3 玻璃结构、§一.5 消费方三步）：[04 §8.3](04-任务-兼容读数.md)（读数）＋ [05 §9.1/9.3](05-任务-市场显示状态.md)（显示口径与卡文案）。
- **复跑命令**：`npm run audit:plugin-dangling:selftest`（尺子 14 例含 `锚⑩`）· `npx vitest run packages/plugin-sdk/src/eslint/checks/dangling-names.test.ts`（SDK 17 例）· `npx tsx scratch/dangling-18repo.mjs`（18 仓真跑，scratch 驱动**不在 git**，丢了照 [06 §8.3](06-任务-判据铺开.md) 重写）· `npm run host-css:regen -- --check`（两份投影对账）· 壳仓 `npm run check`。

### ⑻ 发布实录

- **插件仓**：零改动（未推送的 1.0.36 照旧攒着）。
- **壳仓**：零代码改动 ⇒ 版本恒 **0.2.12**、无 CHANGELOG 变更、零发版。
- **黄灯合计（全不拦 check，可攒着一次发）**：`@linkdesk/plugin-sdk` **0.1.40**（本格 bump）· `@linkdesk/plugin-docs` **0.1.21**（本格 bump；0.1.19/0.1.20 格 1/3 留）· `marketplace` 1.0.36（格 5）。发版链走完一步补齐 lock/种子；`check:npm-release` 基线发版后重记。

---

## ← 第 5 格（格 5 · `E6#118` **市场显示状态**）· ✅ 已完成（2026-09-18）：**给下一个 AI ＝ 格 6 · `E6#119` 判据铺开**

### ⑩ 🔴 先说你是谁（⛔ 别猜）

你是**这一棒的执行者**，任务是 [06-任务-判据铺开.md](06-任务-判据铺开.md)（`E6#119`）。
- **以本段为准**：本段与你读到的任何别的说法冲突，**用本段**。拿不到本段 ⇒ 先问用户「我是不是格 6」。
- **你不是复核者**（本批没有「复核轮」这种格），**也不是派工者**。
- ⚠️ 本批**一个会话可做多格**，但**每格各自收尾、各自提交**：格 5 已单独提交（**插件仓 `marketplace` 一笔 ＋ 壳仓一笔**）。

### ⑴ 上一棒（格 5）做完了什么（一句话 ＋ 读数）

- **一句话**：把兼容读数**标在市场插件详情页**——信息栏「兼容性」组一行状态标（**全页唯一**，用户面五词逐字照抄 00 §〇d）＋ 悬停帮助卡（`OverlayPortal` 容器、400ms 悬停意图、键盘/触屏可达、卡体 `pointer-events:none`），文案住插件 `i18n/en.json`（15 条同笔）。
- **读数**：`marketplace` **1.0.35 → 1.0.36**（PATCH，插件仓提交 **`c5e8b8f`**，**未推送**）· 插件门禁 build 8/8 ＋ lint **零偏离** ＋ test **233/233** · 壳仓 `npm run check` **EXIT=0**（177 文件 / 2485 例）· bundled-freshness **6/6 绿**（种子未动——见 §⑸）。
- **执行记录全文**（落地表 ／ 设计差异 5 条 ／ 版本账 ／ 残余边界 5 条）：[05 §九](05-任务-市场显示状态.md)。**别重写它**。
- 🟢 **与格 6 直接相关的一句**：05 §9.2 的用户点头范围**含格 7 要抽的那张卡**——卡文案与触发行为已按点头定稿，格 7 替换成共享件 `HintCard` 时**文案与触发逐字保留**。

### ⑵ 剩余任务队列（**自己那格已改 ✅ → 你做完改你那格，再整张复制进新段**）

| 序 | 轮次 | 编号 | 任务 | 详案 | 前置 | 状态 |
|:--:|:--|:--|:--|:--|:--|:--:|
| 1 | 第 8.1 轮 | `E6#114` | 悬空名核验（产物层只读审计） | [01](01-任务-悬空名核验.md) | 无 | ✅ |
| 2 | 第 8.2 轮 | `E6#115` | 只加不删门禁（面快照 ＋ diff ＋ 判红） | [02](02-任务-只加不删门禁.md) | 无（与格 1 互不依赖） | ✅ |
| 3 | 第 8.3 轮 | `E6#116` | 退役登记（账 ＋ 流程 ＋ 机械对账） | [03](03-任务-退役登记.md) | 格 2 ✅ | ✅ |
| 4 | 第 8.4 轮 | `E6#117` | 兼容读数 | [04](04-任务-兼容读数.md) | 格 1 ✅ | ✅ |
| 5 | 第 8.5 轮 | `E6#118` | 市场显示状态（**含 HTML 设计图**） | [05](05-任务-市场显示状态.md) | 格 4 ＋ **用户点头 ✅ 2026-09-18** | ✅ |
| 6 | 第 8.6 轮 | `E6#119` | 判据铺开（SDK 随包 ＋ 18 仓接腿 ＋ 发版） | [06](06-任务-判据铺开.md) | 格 1 ✅ ＋ 格 2 ✅ | ⬜ |
| 7 | 第 8.7 轮 | `E6#120` | 通用悬停提示卡（**壳的共享件**）＝ **本批最后一棒**（＋ 全批收口报告） | [08](08-任务-通用悬停提示卡.md) | 格 5 ✅（⚠️ **可换序、⛔ 不并行**） | ⬜ |

> **执行序 = 自上而下**。🔴 **格数是上限不是配额**（**7 格**一格不能省；判「不做」要写理由，写进本交接段的「不做」节）。
> ⚠️ **别与 `E6#113`（插件侧死类清账）并行**——那格要动 3 个插件仓，格 7 也动 `marketplace`。

### ⑶ 你这一棒（格 6）具体做什么——**以 [06 号档](06-任务-判据铺开.md) 为准**，这里只点三条

1. 🔴 **SDK 新腿与壳侧两个扫描实现是三份同口径**：壳 `scripts/plugin-dangling-name-audit.mjs`（格 1 尺子）＋ 壳运行时腿 `src/core/compat/dangling-scan.ts`（格 4，**已与格 1 在随包 6 zip 上对账逐只相等**）——你的 SDK 腿照 [06](06-任务-判据铺开.md) 用「锚词」钉口径，⛔ 不许只靠注释说「一致」。
2. 🔴 **发版链是攒着的**：`@linkdesk/plugin-sdk` **0.1.39**（格 4 已 bump 未发布：README 速查表＋dev-host mock 263 桩含新面）· `@linkdesk/plugin-docs` **0.1.19/0.1.20**（格 1/3 留）· `marketplace` **1.0.36**（格 5，`c5e8b8f` 未推送）——你的 SDK PATCH 在 0.1.39 之上抬，发版仍等用户点头。
3. ⚠️ **作者面 [16-命名规范.md §7.5](../../../03-插件制造/16-命名规范.md) 有一句「SDK 侧退役名读数今天还没有」**——你若落地退役名提示**必须同笔改这句**，否则作者面成假话。

### ⑷ 本格实测踩到的坑（**别重踩**）

| 坑 | 实情与正解 |
|:--|:--|
| `@linkdesk/contracts` npm 0.1.15 无新面类型 | 插件侧调新面要**本地声明 wire 形状**（`useCompatReading.ts` 受控收窄）——契约包发版后替换 `import type`（已记 05 §9.6） |
| `sync:bundled --latest` 会**挂起** | 它取 GitHub Release asset（**要代理**）；产物未发布时永远取不到——种子同步是**发版后**动作，别在发版前硬跑 |
| src/ 诊断字符串撞 `audit-i18n` | 全 src 扫描——中文诊断串用**机器码**（格 4 实测）；插件侧文案进 i18n 走 key=中文原文 |
| 用户面禁词 grep 与设计图原文打架 | 「这不是评分」句 = 点头范围内的逐字原文 ⇒ **豁免要登记在 §9.3**（格 6 的作者面说明要与显示口径对齐时注意同款处理） |
| 实机验证的闸门 | dev 插件根只有夹具；新插件版本进实机 = 动用户已装集 ⇒ **AI 不擅动**——实机读数留给发版后/收口（05 §9.5 先例） |

### ⑸ 不做（**边界，明写理由**）

| 不做 | 理由 |
|:--|:--|
| `npm publish` / 官方目录收录两轮 / GitHub Release | 🔴 用户红线：各自**单独等点头**（通知段明列） |
| 随包种子同步（`bundled-plugins/` 仍 1.0.35） | **串行闸门**：`sync:bundled --latest` 取已发布 asset（1.0.36 未发）——发版链走完一步补齐；`check-bundled-freshness` 6/6 绿证明非回归 |
| 实机四态截图 | 同因（实机无 1.0.36）＋ 用户已装集不该擅改——替代读数见 [05 §9.5](05-任务-市场显示状态.md) |
| 壳仓代码改动 | 格 5 零壳侧代码需求（读数面在格 4；显示全在插件） |
| 不推 `git push` | 🔴 用户红线：等用户本人点头（两仓均未推送） |

### ⑹ 提交清单（格 5）

- **插件仓 `marketplace`**：`c5e8b8f`（8 文件：新增 `CompatStatus.tsx`/`useCompatReading.ts`，改 `DetailView.tsx`/`useInfoGroups.tsx`/`detail-body.css`/`i18n/en.json`/`package.json`/`CHANGELOG.md`；**未推送**）。
- **壳仓**：docs 一笔（05 §九 ＋ 上位清单勾格＋进度行 ＋ 本段）——**零壳代码改动**。
- ⚠️ 提交信息带类别前缀；`git add` 按**显式路径**（⛔ 禁 `git add -A`）。**壳仓提交 sha = `7cd2af652`**（3 文件 · **未推送**）。

### ⑺ 给格 6 的输入（**可直接抄的东西**）

- **读数形状与唯一入口**（作者面说明要与它对齐）：[04 §8.3](04-任务-兼容读数.md)；市场显示口径（五词＋卡文案）= [05 §9.1/9.3](05-任务-市场显示状态.md)——⛔ 别写出两套说法。
- **发版链现状账**：[05 §9.4](05-任务-市场显示状态.md) ＋ 本段 §⑶.2。
- **格 7 的替换清单（代格 5 预交）**：卡体替换 = `CompatStatus.tsx` 内 `marketplace-mpd-compat-card` → 共享件 `<HintCard>`（容器/触发/文案不动——容器与触发逻辑留在本组件，只换卡体渲染）；替换后 grep `marketplace-mpd-compat-card` 应零命中。
- **复跑命令**：`cd E:/linkdesk-plugins/official/marketplace && npm run build && npm run lint && npm test`；壳仓 `npm run check`。

### ⑻ 发布实录

- **插件仓**：`marketplace` **1.0.36** 已 bump 未发（CHANGELOG 同笔）；🔴 未 publish、未收录、未推。
- **壳仓**：零代码改动 ⇒ 版本恒 **0.2.12**、无 CHANGELOG 变更、零发版。
- **黄灯合计（全不拦 check，可攒着一次发）**：`@linkdesk/plugin-sdk` 0.1.39 · `@linkdesk/plugin-docs` 0.1.19/0.1.20 · `marketplace` 1.0.36（发版链 = publish → 收录 → `sync:bundled --latest`（要代理）→ 壳 PATCH）。

---

## ← 第 4 格（格 4 · `E6#117` **兼容读数**）· ✅ 已完成（2026-09-18）：**给下一个 AI ＝ 格 5 · `E6#118` 市场显示状态**

### ⑩ 🔴 先说你是谁（⛔ 别猜）

你是**这一棒的执行者**，任务是 [05-任务-市场显示状态.md](05-任务-市场显示状态.md)（`E6#118`）。
- **以本段为准**：本段与你读到的任何别的说法冲突，**用本段**。拿不到本段 ⇒ 先问用户「我是不是格 5」。
- **你不是复核者**（本批没有「复核轮」这种格），**也不是派工者**。
- ⚠️ 本批**一个会话可做多格**，但**每格各自收尾、各自提交**：格 4 已单独提交。你若连做格 5 ⇒ 收尾时**再追加一段**（两格 = 两段，⛔ 不许揉成一段、也不许揉成一笔提交）。
- 🟢 **格 5 段 1 闸门已过**（用户 2026-09-18「注意，我同意按设计图走」）⇒ 直接进段 2 动插件代码，⛔ 不必再要一次点头；点头范围六条见下面「通知段」与 [05 §9.2](05-任务-市场显示状态.md)。

### ⑴ 上一棒（格 4）做完了什么（一句话 ＋ 读数）

- **一句话**：把「这份插件跟当前版本搭不搭」算成**机器读数**——壳新增**唯一只读面** `window.linkdesk.plugins.getCompatibility`（判据单点在壳 `src/core/compat/compatibility.ts`，市场插件零复算），状态 = 悬空读数 ＋ `minAppVersion` ＋ **两个真日期比大小**（无阈值）⇒ 五态（`incompatible`/`drifted`/`current`/`compatible`/`unknown`）自动得出。
- **读数**：状态算法单点 1 处 · 运行时扫描腿与格 1 尺子**随包 6 zip 对账逐只相等**（桩 8 例 ＋ 真 6 例）· 真样本：`drifted` = `settings`（悬空 1 = `ldk-sle-row--locked`）、`compatible` = 其余 5 只；`incompatible`/`current` = 桩（标注）· 面快照 **1057 条面**（+2，additive 🟡 未判红）· contracts 三产物再生成（mock 263 桩）· `npm run check` **EXIT=0**（177 文件 / 2485 例，**新增对账测试常驻 check**）。
- **执行记录全文**（8 维度裁 ／ 读数形状 ／ 真样本 ／ 残余边界 7 条）：[04 §八](04-任务-兼容读数.md)。**别重写它**，要补就补自己那格的档。

### ⑵ 剩余任务队列（**自己那格已改 ✅ → 你做完改你那格，再整张复制进新段**）

| 序 | 轮次 | 编号 | 任务 | 详案 | 前置 | 状态 |
|:--:|:--|:--|:--|:--|:--|:--:|
| 1 | 第 8.1 轮 | `E6#114` | 悬空名核验（产物层只读审计） | [01](01-任务-悬空名核验.md) | 无 | ✅ |
| 2 | 第 8.2 轮 | `E6#115` | 只加不删门禁（面快照 ＋ diff ＋ 判红） | [02](02-任务-只加不删门禁.md) | 无（与格 1 互不依赖） | ✅ |
| 3 | 第 8.3 轮 | `E6#116` | 退役登记（账 ＋ 流程 ＋ 机械对账） | [03](03-任务-退役登记.md) | 格 2 ✅ | ✅ |
| 4 | 第 8.4 轮 | `E6#117` | 兼容读数 | [04](04-任务-兼容读数.md) | 格 1 ✅ | ✅ |
| 5 | 第 8.5 轮 | `E6#118` | 市场显示状态（**含 HTML 设计图**） | [05](05-任务-市场显示状态.md) | 格 4 ＋ **用户点头 ✅ 2026-09-18** | ⬜ |
| 6 | 第 8.6 轮 | `E6#119` | 判据铺开（SDK 随包 ＋ 18 仓接腿 ＋ 发版） | [06](06-任务-判据铺开.md) | 格 1 ✅ ＋ 格 2 ✅ | ⬜ |
| 7 | 第 8.7 轮 | `E6#120` | 通用悬停提示卡（**壳的共享件**）＝ **本批最后一棒**（＋ 全批收口报告） | [08](08-任务-通用悬停提示卡.md) | 格 5（⚠️ **可换序、⛔ 不并行**） | ⬜ |

> **执行序 = 自上而下**。🔴 **格数是上限不是配额**（**7 格**一格不能省；判「不做」要写理由，写进本交接段的「不做」节）。
> ⚠️ **别与 `E6#113`（插件侧死类清账）并行**——那格要动 3 个插件仓，格 5 与格 7 也动 `marketplace`。

### ⑶ 你这一棒（格 5）具体做什么——**以 [05 号档](05-任务-市场显示状态.md) 为准**，这里只点三条最容易做错的

1. **改真插件 CSS 前必走硬约束 16**：先 `Skill(ui-ux-pro-max)` 拿设计系统再动 `marketplace` 的样式（05 §三第二步，纪律不是可选项）。
2. **只显示、不复算**：状态来自 `plugins.getCompatibility` **唯一入口**（见 §⑺），⛔ 插件里写第二套状态算法；**用户面只许五个词**（正常/兼容/部分不适配/不适配/—，逐字照抄 [00 §〇d](00-整理档案.md)），**文案住插件 `i18n/en.json`（key=中文原文）**，⛔ 壳不下发句子、⛔ 读数里的 `unknown[]` 机器码是诊断面不许上屏。
3. **状态标全页唯一**（用户点头范围）：只挂详情页信息栏「兼容性」组那一行；⛔ 标题旁不挂、⛔ 侧栏不标、⛔ 信息栏不写「鼠标停上来看」、⛔ 状态标不挂 `title`；悬停卡容器 = `@linkdesk/ui` 的 `OverlayPortal`（**不传 `trapFocus`、`closeOnOutsideClick={false}`**、卡体 `pointer-events:none`），照 `SearchView/AddSourcePopup.tsx` 形状。

### ⑷ 本格实测踩到的坑（**别重踩**）

| 坑 | 实情与正解 |
|:--|:--|
| 生成物注释里写 `src/**/*.css` | `*/` 会**提前终止块注释**（tsc 当场 1005 一串）——生成器里注释别含 `*/` 序列 |
| 生成物行数 | 348+ 类名一行一个 = **1115 行**，`check-file-size` 800 上限当场红——按行压缩（每行 8 个）⇒ 167 行 |
| src/ 里的**诊断字符串** | `audit-i18n --strict` 扫全 src，中文诊断串（`dangling（未安装）`）当场红 ⇒ 诊断面用**机器码**（`dangling:uninstalled`），用户面词归插件 i18n |
| 手写 `replace(/\\/g, "/")` | ESLint `linkdesk/no-raw-path-replace` 当场红 ⇒ 走 `normalizePath`（`src/core/utils/path/pathUtils`） |
| knip 对 `src/core/compat/` | 内部函数**别加 export**（shoutSites/scanArtifact/judgeDangling 三个函数＋三个 interface 被判 unused ⇒ 红）；只导出真被跨文件消费的（`scanInstalledPluginDir`/`computeCompatibilityReading`/`normalizeDay`） |
| 双 preload 镜像行 | `getCompatibility` 在壳/池两 preload 各一行 ⇒ jscpd 判克隆 ⇒ 照 `buildPluginManager` 先例 `jscpd:ignore-start/end` ＋ `E5.8#1d EXEMPT` 注记 |
| 运行时 import 不到 `scripts/*.mjs` | 打包进 asar 的只有 dist-electron ⇒ 运行时扫描必须 **TS 移植腿**（`src/core/compat/dangling-scan.ts`）；「不是第二把尺子」靠对账测试钉死——**格 1 口径若改，对账当场红，同笔改两处** |

### ⑸ 不做（**边界，明写理由**）

| 不做 | 理由 |
|:--|:--|
| 不做显示 / 不碰 UI | 格 5 专属（本格只出机器态读数） |
| 不设时间阈值、不产出「死插件」判定 | 本层红线③＋用户口径：时间只做事实展示；「已死但量大」需要使用量读数，本批不产出 |
| 不把状态算法下发给 SDK/目录 | 判据必须单点（00 §〇d 与 04 §8.2②）；作者侧铺开是格 6 的事 |
| 不动机制（加载器/注册表/主题引擎/`minAppVersion` 拒载） | 本层红线①：`git diff src/pluginLoader/resolution/runtime.ts` 为空是机械证据 |
| 不缓存读数、不做使用量统计 | 读数随请求算（现算一只几百 KB 级，无压力）；使用量本批不产出 |
| 不推 `git push` / 不发 npm / 不推 18 仓 | 🔴 用户红线：**等用户本人点头**（memory `push-wait-for-user`）。黄灯见 §⑻ |

### ⑹ 提交清单（格 4）

- **新增**：`src/core/compat/compatibility.ts`（100 行）· `src/core/compat/dangling-scan.ts`（417 行）· `src/core/compat/host-css.generated.ts`（167 行生成物）· 单测 2 文件（24 例）· `scripts/gen-host-css-manifest.mjs`。
- **改动**：`electron/ipc/channels.ts` ＋ `plugin-handlers.ts` ＋ `preload-shell.ts` ＋ `preload-pool/namespaces-plugin.ts` · `src/core/api/linkdesk-api/plugins.ts` ＋ `types.ts`（契约面）· `contracts/linkdesk.d.ts` ＋ `runtime-shapes.ts` · `packages/plugin-sdk/`（version 0.1.39 ＋ README ＋ dev-host mock）· `package.json`（version 0.2.12 ＋ check 链两环 ＋ 2 npm scripts）· `package-lock.json` · `scripts/host-api-surface.json` · `docs/…/命名空间矩阵.md`（回填 170 通道）· 本批文档（04 §八 ／ 上位清单勾格 ＋ 进度行 ＋ 本段）· `CHANGELOG.md`（v0.2.12）。
- **软件轴 PATCH 0.2.11 → 0.2.12**（新增对外面 ⇒ `feat:` ＋ CHANGELOG 同笔）· `@linkdesk/plugin-sdk` **0.1.39**（内容变 ⇒ PATCH，**未发布**）。
- ⚠️ 提交信息带类别前缀（`feat:` ／ `docs:`）；`git add` 按**显式路径**（⛔ 禁 `git add -A`）。**本格提交 sha = `883f75b1d`**（24 个文件 · **未推送**；上一棒格 3 = `757df41e4`）。

### ⑺ 给格 5 的输入（**可直接抄的东西**——别翻代码）

- **唯一入口**：`window.linkdesk.plugins.getCompatibility({ pluginId, minAppVersion?, publishedAt? })` → `Promise<PluginCompatibilityReading>`（**optional 面，先判存在**；类型住 `src/core/api/linkdesk-api/types.ts`）。已装插件磁盘 manifest 覆盖传入的 `minAppVersion`；`publishedAt` 由你从 catalog 条目传（`packages/plugin-sdk/src/publish.ts:425` 那个字段）。
- **state ↔ 用户面词**（逐字）：`incompatible`→不适配 · `drifted`→部分不适配 · `current`→正常 · `compatible`→兼容 · `unknown`→—。**`null` 语义与机器码 `unknown[]` 清单 = [04 §8.3](04-任务-兼容读数.md)**。
- **真样本（截图用）**：`settings` ⇒ 部分不适配（悬空 1）；其余随包 5 只 ⇒ 兼容；dev 壳下 `current` 产不出（壳日期 `'—'`）——**「正常」那帧要么桩、要么实机安装版补**，进档时如实标注。`incompatible` 可用高 minApp 的目录条目桩。
- **对照设计图核过一遍读数够用**（[04 §五.5](04-任务-兼容读数.md) 要求的返回核）：五态 ✓ · 日期展示（`lastUpdate`/`shellBuiltAt` 均在读数里）✓ · minApp（`minAppVersion`）✓——设计图信息栏那几行**画得出来**，无缺口。
- **文案注意**：读数里的 `dangling.names`/`count` 是**作者面/维护者面**的词——⛔ **不许上用户面**（禁词 grep 见 05 §四⑥）；用户面只说「个别界面可能显示不正常」这类能感知的话。

### ⑻ 发布实录

- **软件轴 PATCH：0.2.11 → 0.2.12**（新增对外面；CHANGELOG v0.2.12 同笔 · 🔴 **未 tag 未发 Release**——发版等用户点头）。
- 🔴 **三盏 npm 黄灯（都不拦 check）**：`@linkdesk/plugin-sdk` **0.1.39 未发布**（本格 bump）· `@linkdesk/plugin-docs` **0.1.19/0.1.20 未发布**（格 1/3 留）· `@linkdesk/contracts` 无本地包（d.ts 内容已变，npm 轴等发版链）。**可攒着与格 6 发版链一次发**，发完 `npm run release:mark` 关灯。
- 本格 `npm run check` **EXIT=0**（177 文件 / 2485 例）——判据 ①–⑧ 逐条读数见 [04 §八](04-任务-兼容读数.md)。

---

## ← 第 3 格（格 3 · `E6#116` **退役登记**）· ✅ 已完成（2026-09-18）：**给下一个 AI ＝ 格 4 · `E6#117` 兼容读数**

### ⑩ 🔴 先说你是谁（⛔ 别猜）

你是**这一棒的执行者**，任务是 [04-任务-兼容读数.md](04-任务-兼容读数.md)（`E6#117`）。
- **以本段为准**：本段与你读到的任何别的说法冲突，**用本段**。拿不到本段 ⇒ 先问用户「我是不是格 4」。
- **你不是复核者**（本批没有「复核轮」这种格），**也不是派工者**。
- ⚠️ 本段**只取代**下面「格 2 段」的 §⑩「你是谁」（那节说你是格 3）；**格 1 段与格 2 段其余各节照旧生效**。
- ⚠️ 本批**一个会话可做多格**，但**每格各自收尾、各自提交**：格 3 已单独提交。你若连做两格 ⇒ 收尾时**再追加一段**（两格 = 两段，⛔ 不许把两段揉成一段、也不许揉成一笔提交）。

### ⑴ 上一棒（格 3）做完了什么（一句话 ＋ 读数）

- **一句话**：账加 `retired[]` **退役登记栏**（`version` **1 → 2**）——「宿主把某个名字拿走了」从此有了**唯一合法出口**：每条写清**谁批的 ／ 为什么退 ／ 替身是谁 ／ 旧名今天还在哪儿被读**；同时把格 2 的那条红出口改成「**有登记即放行**」。
- **读数**：`retired[]` **1 条**（首条 `app.themeColorMode` · configKey · since `2026-08-25` · 替身 `app.theme` · 落点 `src/App/startup.ts:246` · 签名 `用户 · 2026-09-18`）· 对账门禁自测 **7 例**（正控 1 ／ 负控 5 ／ 计数 1）· 格 2 门禁自测 **9 → 12 例** · **真造红 3 次**（判据②③④，逐字报文见 [03 §8.5](03-任务-退役登记.md)）· 面快照仍 **1055 条面**（**未动**）· `npm run check` **EXIT=0**（175 文件 / 2461 例）。
- **执行记录全文**（形状七字段 ／ 唯一归属的裁 ／ 残余边界 9 条 ／ 造红与还原读数）：[03 §八](03-任务-退役登记.md)。**别重写它**，要补就补自己那格的档。

### ⑵ 剩余任务队列（**自己那格已改 ✅ → 你做完改你那格，再整张复制进新段**）

| 序 | 轮次 | 编号 | 任务 | 详案 | 前置 | 状态 |
|:--:|:--|:--|:--|:--|:--|:--:|
| 1 | 第 8.1 轮 | `E6#114` | 悬空名核验（产物层只读审计） | [01](01-任务-悬空名核验.md) | 无 | ✅ |
| 2 | 第 8.2 轮 | `E6#115` | 只加不删门禁（面快照 ＋ diff ＋ 判红） | [02](02-任务-只加不删门禁.md) | 无（与格 1 互不依赖） | ✅ |
| 3 | 第 8.3 轮 | `E6#116` | 退役登记（账 ＋ 流程 ＋ 机械对账） | [03](03-任务-退役登记.md) | 格 2 ✅ | ✅ |
| 4 | 第 8.4 轮 | `E6#117` | 兼容读数 | [04](04-任务-兼容读数.md) | 格 1 ✅ | ⬜ |
| 5 | 第 8.5 轮 | `E6#118` | 市场显示状态（**含 HTML 设计图**） | [05](05-任务-市场显示状态.md) | 格 4 ＋ **用户点头 ✅ 2026-09-18** | ⬜ |
| 6 | 第 8.6 轮 | `E6#119` | 判据铺开（SDK 随包 ＋ 18 仓接腿 ＋ 发版） | [06](06-任务-判据铺开.md) | 格 1 ✅ ＋ 格 2 ✅ | ⬜ |
| 7 | 第 8.7 轮 | `E6#120` | 通用悬停提示卡（**壳的共享件**）＝ **本批最后一棒**（＋ 全批收口报告） | [08](08-任务-通用悬停提示卡.md) | 格 5（⚠️ **可换序、⛔ 不并行**） | ⬜ |

> **执行序 = 自上而下**。🔴 **格数是上限不是配额**（**7 格**一格不能省；判「不做」要写理由，写进本交接段的「不做」节）。
> ⚠️ **别与 `E6#113`（插件侧死类清账）并行**——那格要动 3 个插件仓，格 5 与格 7 也动 `marketplace`。

### ⑶ 你这一棒（格 4）具体做什么——**以 [04 号档](04-任务-兼容读数.md) 为准**，这里只点三条最容易做错的

1. **「状态」的词先定受众，再定词表**：本层红线（上位清单第 8 层）写着**用户面词表 = [00 §〇d](00-整理档案.md) 的五个词**，作者面 ／ 维护面是**另一套** ⇒ 你出的「兼容性状态」给**谁**看（市场卡片？详情页？作者自查？）先定，⛔ 别一套词打天下。
2. **时间只做事实展示、不设阈值**：拿**两个真日期**比大小（例如插件最近提交日 vs 宿主发布日）——⛔ **版本号绝不可当时间轴**（本层红线③的原文）；⛔ 也别自己发明「超过 N 天算过期」这类阈值（要设也得用户点头）。
3. **动 `src/` 就有三条尾巴**：① 动了**对外**的面 ⇒ `contracts:check` 同笔更新（d.ts）；② 新增**对外**的面 ⇒ `npm run api-surface:regen` 同笔提交快照，否则 `gen-api-surface.mjs --check` 判漂；③ 若改**随包插件**（`plugins/`）⇒ 走插件侧那条版本同步链（⛔ 别只改壳）。

（⚠️ 第 1、2 条是上位清单第 8.4 行的**红线原文**、不是本格发明的；第 3 条是格 2 ／ 本格实测出来的。）

### ⑷ 本格实测踩到的坑（**别重踩**）

| 坑 | 实情与正解 |
|:--|:--|
| 工作区是 **CRLF** | 改既有文件前先看 EOL、改完**保原样**（本格改 `gen-host-reserved.mjs` 时它是 CRLF）；**还原用 `cp` 字节备份**，⛔ 别拿 `git checkout --` 当逐字节还原手段（本机 `core.autocrlf` 下它不保证） |
| 「找写点」的词首要卡 **`\b`** | `resetConfigurationValue` **包含** `setConfigurationValue` 子串 ⇒ 不卡词首会把「清扫旧键」误判成「还在写」，判据① 当场自相矛盾 |
| `scripts/lib/*.mjs` **在 knip 射程内** | 新抽的共享库**别「先全 export」**——knip 对过度导出的符号判红；本格只导出 **9 个真被别处用**的面 |
| 改作者面文档 = **四连** | `docs/03-插件制造/**` 或 `docs/03-plugin-authoring/**` 一改 ⇒ ① 中英**同笔** ② `npm run docs:build` 重生成 ③ `@linkdesk/plugin-docs` **PATCH** ④ `package-lock.json` 同步（`check-lockfile-sync` 判这个） |
| **生成式文件不许手改** | `scripts/host-reserved.json` 的**家族栏**是扫描产物，手改 ⇒ `gen-host-reserved.mjs --check` 判漂；**只有 `retired[]` 是人工栏**（生成器原样携带，regen 冲不掉） |

### ⑸ 不做（**边界，明写理由**）

| 不做 | 理由 |
|:--|:--|
| 不把 `retired[]` 做成**黑名单** | 本层红线②：**要豁免就修机制**，不是养名单。任何门禁拿它**拦插件**都是错的（那是拒绝墙）——对账门禁末行固定打印这句当路标 |
| 不追认**历史遗留** | 本格之前就被拿走的名字、以及「从未定义过」的死钩子（格 1 交来的 `ctx-item-chevron`）不进账；它们归已划走的那部分（格 1 射程） |
| 不替**格 6** 决定「要不要给作者提示退役名」 | 数据已就位（SDK 副本**逐字节**带着 `retired`）⇒ 格 6 的取舍点是**要不要做这件事**，不是数据在不在包里 |
| 不改机制（加载器 ／ 注册表 ／ 主题引擎 ／ `minAppVersion` 拒载） | 本层红线①：本批只加判据、账、读数、显示 |
| 不推 `git push` ／ 不发 npm ／ 不推 18 仓 | 🔴 用户红线：**等用户本人点头**（memory `push-wait-for-user`）。黄灯见 §⑻ |

### ⑹ 提交清单（格 3）

- **新增**：`scripts/lib/retired-ledger.mjs`（358 行，形状与匹配规则的唯一真相源）· `scripts/check-retired-ledger.mjs`（203 行，对账门禁 ＋ `--self-test`）。
- **改动**：`scripts/host-reserved.json`（`version 2` ＋ `retired[]` 首条）· `packages/plugin-sdk/schemas/host-reserved.json`（逐字节同步）· `scripts/gen-host-reserved.mjs`（抬版 ＋ `retired` 段 ＋ 原样携带）· `scripts/check-api-surface-additive.mjs`（**唯一放行出口** ＋ 自测 9→12 例）· `scripts/check-reserved-names-doc-sync.mjs`（`retired` 划出家族）· `scripts/lib/api-surface.mjs`（`collectLedger` 跳过 `retired` ⇒ 快照不变）· `package.json`（check 链插两条）· `docs/03-插件制造/16-命名规范.md` ＋ `docs/03-plugin-authoring/16-naming-conventions.md`（新 §7.5，中英同笔）· `packages/plugin-docs/package.json` ＋ `package-lock.json`（0.1.19 → **0.1.20**，`docs:build` 47 文件）· 本批三份文档（02 ／ 03 ／ 本段 ＋ 上位清单勾格）。
- **零 `src/` 行为改动 ⇒ 软件版本不 bump**（恒 **0.2.11**）· 无 CHANGELOG 变更 · **零 npm 发版** · 🔴 **未推送**。
- ⚠️ 提交信息带类别前缀（`feat:` ／ `docs:`）；`git add` 按**显式路径**（⛔ 禁 `git add -A`）。**本格提交 sha = `757df41e4`**（19 个文件 · **未推送**；上一棒格 2 = `09cbe1470`）。

### ⑺ 给下一棒的输入（**可直接抄的东西**）

- **判「这个名字算不算已登记」**：`import { exemptionFor, readRegistry } from "./lib/retired-ledger.mjs"`（`exemptionFor:151`）——⛔ **不要**再写第三套匹配规则。
- **账自洽门禁**：`node scripts/check-retired-ledger.mjs`（另 `--self-test` ／ `--json`）——它**只管账自己**，⛔ 不判「面被拿走」（那是格 2 门禁的事，一处判红）。
- **账的七个字段与九档 `kind`**：见 [03 §8.2](03-任务-退役登记.md)（含「`since` 写日期不写版本号」「`landing` 写 `file:line` 或 `无落点`」）。
- 📌 **一条口径澄清（格 2 的预告被本格推翻一半）**：格 2 段里写「动了 `scripts/host-reserved.json` 就必须同笔重跑 `api-surface:regen` 并提交快照」——**`retired` 栏不再适用**（它是**记录**不是**面**，快照刻意跳过它）；**家族栏仍然适用**。别照格 2 那句话一刀切。
- ⚠️ **作者面 §7.5 里有一句「SDK 侧的退役名读数今天还没有」**——**格 6 一旦落地读数，必须回来改这句**（否则作者面就成了假话）。

### ⑻ 发布实录

- **软件（壳）零发版**：无 `src/` 改动 ⇒ 版本恒 **0.2.11**（`src/core/registry/host-reserved.generated.ts` **未动**就是这条的机械证据）。
- ⚠️ **一盏 npm 黄灯（不拦 check）**：`@linkdesk/plugin-docs` 格 1 的 0.1.19 ＋ 本格的 **0.1.20 均未发布** ⇒ 第 1 ／ 3 ／ 7 格的作者面改动**可攒着一次发**（发完 `npm run release:mark` 关灯）。`check:npm-release` 黄灯。
- 本格 `npm run check` **EXIT=0**——末尾三行与判据 ①–⑦ 逐条读数见 [03 §8.5](03-任务-退役登记.md)。

---

## ← 第 2 格（格 2 · `E6#115` **只加不删门禁**）· ✅ 已完成（2026-09-18）：**给下一个 AI ＝ 格 3 · `E6#116` 退役登记**

### ⑩ 🔴 先说你是谁（⛔ 别猜）

你是**这一棒的执行者**，任务是 [03-任务-退役登记.md](03-任务-退役登记.md)（`E6#116`）。
- **以本段为准**：本段与你读到的任何别的说法冲突，**用本段**。拿不到本段 ⇒ 先问用户「我是不是格 3」。
- **你不是复核者**（本批没有「复核轮」这种格），**也不是派工者**。
- ⚠️ 若你是**同一个会话的第二步**（会话 A：格 2 →格 3）：格 2 已收尾并单独提交，**现在开的是另一格**——收尾时**再追加一段**（两格 = 两段，⛔ 不许把两段揉成一段）。

### ⑴ 上一棒（格 2）做完了什么（一句话 ＋ 读数）

- **一句话**：把「插件能用的面」冻成**快照** `scripts/host-api-surface.json`（六栏 / **1055 条面**），并与**上一个已发布 tag** 的那份比 —— **缺项/改名 ⇒ 🔴 红**、新增 ⇒ 🟡 提示；**已进 `npm run check`**（门禁 ＋ 自测 ＋ 生成器 `--check` 三条）。
- **读数**：`apiNamespaces` 45 命名空间 / 261 成员 · `poolExposed` 44 / 237 · 根级成员各 1 · `manifestFields` 122 路径（9 带枚举）· `hostClassNames` 348 · `reservedKeyframes` 8 · `ledger` 11 栏。自测 **9/9**（正控 3 ／ 负控 5 ／ 计数 1）；**真造过一次红**（EXIT=1，报出三个点，还原后 sha256 逐字节相同）。
- **执行记录全文**（口径／造红逐字报文／残余边界 11 条／check 读数）：[02 §八](02-任务-只加不删门禁.md)。**别重写它**，要补就补自己那格的档。

### ⑵ 剩余任务队列（**自己那格已改 ✅ → 你做完改你那格，再整张复制进新段**）

| 序 | 轮次 | 编号 | 任务 | 详案 | 前置 | 状态 |
|:--:|:--|:--|:--|:--|:--|:--:|
| 1 | 第 8.1 轮 | `E6#114` | 悬空名核验（产物层只读审计） | [01](01-任务-悬空名核验.md) | 无 | ✅ |
| 2 | 第 8.2 轮 | `E6#115` | 只加不删门禁（面快照 ＋ diff ＋ 判红） | [02](02-任务-只加不删门禁.md) | 无（与格 1 互不依赖） | ✅ |
| 3 | 第 8.3 轮 | `E6#116` | 退役登记（账 ＋ 流程 ＋ 机械对账） | [03](03-任务-退役登记.md) | 格 2 ✅ | ⬜ |
| 4 | 第 8.4 轮 | `E6#117` | 兼容读数 | [04](04-任务-兼容读数.md) | 格 1 ✅ | ⬜ |
| 5 | 第 8.5 轮 | `E6#118` | 市场显示状态（**含 HTML 设计图**） | [05](05-任务-市场显示状态.md) | 格 4 ＋ **用户点头 ✅ 2026-09-18** | ⬜ |
| 6 | 第 8.6 轮 | `E6#119` | 判据铺开（SDK 随包 ＋ 18 仓接腿 ＋ 发版） | [06](06-任务-判据铺开.md) | 格 1 ✅ ＋ 格 2 ✅ | ⬜ |
| 7 | 第 8.7 轮 | `E6#120` | 通用悬停提示卡（**壳的共享件**）＝ **本批最后一棒**（＋ 全批收口报告） | [08](08-任务-通用悬停提示卡.md) | 格 5（⚠️ **可换序、⛔ 不并行**） | ⬜ |

> **执行序 = 自上而下**。🔴 **格数是上限不是配额**（**7 格**一格不能省；判「不做」要写理由，写进本交接段的「不做」节）。
> ⚠️ **别与 `E6#113`（插件侧死类清账）并行**——那格要动 3 个插件仓，格 5 与格 7 也动 `marketplace`。

### ⑶ 你这一棒（格 3）具体做什么——**以 [03 号档](03-任务-退役登记.md) 为准**，这里只点三条最容易做错的

1. 🔴 **红报文里那句「② 若是有意的退役 ⇒ 走退役登记 `E6#116`」就是你的登记入口**：格 2 **只判红、没有任何放行逻辑**（它落地时格 3 还不存在）。⇒ 现在轮到你把「拿走」变成**唯一合法出口**：账加 `retired[]` ＋ 三条断言 ＋ **「有登记即放行」只能归一个文件**（二选一：改格 2 的门禁去读 `retired[]`，或在你自己的门禁里认领）——**选哪个都要写进两边的档**，⛔ 不许两条口径并存。
2. 🔴 **动了 `scripts/host-reserved.json` 就必须同笔重跑 `npm run api-surface:regen` 并提交快照**（格 2 的面快照第 ④ 栏就是这本账的形状；账加栏 ⇒ 快照那一栏跟着变）。忘了跑 ⇒ 下一次 `npm run check` 里 `gen-api-surface.mjs --check` **当场红**，且报的是「快照与实况不一致」——文案会直接告诉你跑哪条命令。
3. 🔴 **`retired[]` 不是黑名单**（03 号档禁区①）：它供人看/提示，⛔ **任何门禁不许拿它拦插件**；**退役 ≠ 删除**（名字**不腾位**——老 `settings.json` 里可能还有值，插件此刻占它 = 顶掉宿主历史数据）；`approvedBy` 必须是**用户本人**（`"用户 · 日期"`，⛔ AI 不许自己签）。

### ⑷ 格 2 踩出来的（**别重踩**）

1. 🔴 **`git checkout --` 还原不出「逐字节相同」**：本仓 `.gitattributes` 是 `* text=auto eol=lf`，若工作区那份是 CRLF，`git checkout --` 会写回 **LF** ⇒ sha 变了但**内容没变**（`git hash-object --path=…` == HEAD blob）。**要「逐字节」就用 `cp` 字节备份**；用 checkout 就得如实写「EOL 归化、内容一致」。详证见 [02 §8.3](02-任务-只加不删门禁.md)。
2. ⚠️ **报文别分两个流**：门禁的 header/footer 走 `stderr`、明细走 `stdout`，管道抓取时明细会跑到「下一步」之后（**肉眼看不见，抓日志才露**）。全走 stdout。
3. ⚠️ **TS 面要解到「属性名」而不是「文本」**：`Pick`/`Omit`/交叉/命名接口在文本解析下一律塌成「零成员」⇒ 纯重构假红（格 2 走 `ts.createProgram` ＋ `checker.getPropertiesOfType`）。
4. ⚠️ **可选性（`?`）会骗人**：`bridge?: X` 在检查器眼里是 `X | undefined` **联合**，取属性得**空** ⇒ 会被误判成「根级成员」（格 2 实测踩到：`apiRootMembers` 一度多收两三个）。`unwrapOptional()` 必须剥掉 `undefined | null`。
5. ⚠️ **别把「新增」判红**（禁区②）——「只加」是被允许的方向；判红 = 把正常演进逼成绕过。
6. ⚠️ **两处口径必须同函数**：格 1 的尺子与格 2 的快照共用 `scripts/lib/host-surface.mjs` 的 `collectHostDefs()`——你也读宿主定义集时**照用**，⛔ 别自己再写一遍（「快照说缺、尺子说没悬空」就是本系列还过两轮的债）。
7. ⚠️ **文档门禁会抓自己**：`check-doc-links`（断链 = 红）＋ `check-design-flow` §8.4③（含 `window.linkdesk.` / `contributes.` 的 md 必须带「设计前置」或「非新能力」声明）；改 `docs/03-插件制造/**` 或 `docs/03-plugin-authoring/**` 就得跑 `npm run docs:build` ＋ 动 `@linkdesk/plugin-docs` 版本 ＋ 同步 `package-lock.json`（`check-lockfile-sync` 会抓）。

### ⑸ 不做（**边界，明写理由**）

| 不做 | 理由 |
|:--|:--|
| 不推 `git push` / 不发 npm / 不推 18 仓 | 🔴 用户红线：**等用户本人点头**（memory `push-wait-for-user`）。两盏黄灯挂着：`@linkdesk/plugin-docs` 0.1.19 未发布 ＋ **快照的「比 tag」主路径要等下一次发版才真跑过** |
| 不改机制（加载器/注册表/主题引擎/`minAppVersion` 拒载） | 本层红线①：只加判据、账、读数、显示 |
| 格 2 不引白名单 / 棘轮 / 豁免名单 | 本层红线②：要豁免就**改机制** |
| 格 2 不改 `contracts:check` / `check-api-contracts.mjs` | 各管一件事，本格只**补第三条** |
| 格 2 不改 `src/` 行为 | 造红时临时改过两处，**已还原并验过 sha256**（§8.3） |
| 格 2 不动 SDK / 插件仓 | 插件仓那条腿是**格 6**；SDK 副本漂移归既有 `check-plugin-schema-sync.mjs` |

### ⑹ 提交清单（格 2）

- **新增**：`scripts/lib/host-surface.mjs` · `scripts/lib/api-surface.mjs` · `scripts/gen-api-surface.mjs` · `scripts/check-api-surface-additive.mjs` · `scripts/host-api-surface.json`（**快照随版本提交**）。
- **改动**：`scripts/plugin-dangling-name-audit.mjs`（改 import 走共享生成器，读数不变）· `package.json`（5 条 scripts ＋ check 链插三条）· `docs/02-Electron架构/E6_插件生态与发布/E6-执行清单.md`（第 8 层 `E6#115` 勾格全套 ＋ 轮次进度行 8.2 ✅）· `docs/02-Electron架构/E6_插件生态与发布/插件兼容机械化/02-任务-只加不删门禁.md`（§三 判据逐条读数 ＋ §八 执行记录）· `docs/02-Electron架构/E6_插件生态与发布/插件兼容机械化/交接.md`（本段）。
- **零 `src/` 行为改动 ⇒ 软件版本不 bump**（恒 **0.2.11**）、无 CHANGELOG 变更、**零 npm 发版**。
- ⚠️ 提交信息带类别前缀（`feat:`／`docs:`）；`git add` 按**显式路径**（⛔ 禁 `git add -A`）。

### ⑺ 给下一棒的输入（**可直接抄的东西**）

- **红报文形状**（你登记完要让它闭口的那个出口）＝ `scripts/check-api-surface-additive.mjs` `run():122`——逐字报文见 [02 §8.3](02-任务-只加不删门禁.md)。
- **tag 取法**（三档 ＋ 拒绝判定）＝ 同文件 `resolveBaseline():61` ＋ `NoBaselineError:44`。
- **快照形状**（六栏 ＋ `generatedFrom` 口径散文）＝ `scripts/lib/api-surface.mjs` `COLUMN_CALIBERS:47`／`collectApiSurface():207`；**格 6 要在作者侧复用同一形状**。
- **`--self-test` 的写法**（正控/负控/计数 ＋ 桩快照/桩 git）＝ `selfTest():189`。
- **复跑命令**：`npm run api-surface:regen`（重写快照）／`api-surface:check`（磁盘 == 重算）／`check:api-surface`（门禁真跑）／`check:api-surface:selftest`。

### ⑻ 发布实录

- **软件（壳）零发版**：无 `src/` 改动 ⇒ 版本恒 **0.2.11**。
- 🔴 **两盏黄灯（都自愈或等用户点头，都不拦 check）**：
  1. `@linkdesk/plugin-docs` **0.1.19 未发布**（格 1 留下的；格 3 与格 7 也要动作者面文档 ⇒ **可攒着一次发**，发完 `npm run release:mark` 关灯）。
  2. **快照的「与已发布 tag 比」主路径尚未被真数据走过**——今天 tag `v0.2.11` 里**没有**快照文件 ⇒ 门禁走**降级档**（HEAD → 工作区）。它**自愈**：本次提交后比 HEAD，**下一条 tag 后才是真「比 tag」**。（桩测负控 5 已证那条腿存在。）
- 本格 `npm run check` **EXIT=0**——末尾三行见 [02 §8.5](02-任务-只加不删门禁.md)。

---


> **本段 = 当前派工**（倒序，最新在最上）。它**只取代**下面「格 1 段」的 §⑩「你是谁」（那节写的是「只做一格」），**格 1 段其余内容（§⑶ 三条易错 / §⑷ 坑单 / §⑸ 不做 / §⑹ 提交 / §⑺ 输入 / §⑻ 发布实录）全部照旧生效**。再下面的「通知段」（用户对设计图点头 ⇒ 格 5 段 2 解锁）是**格 5 的闸门，与本会话无关**。
> **▼ 以下 = 可整段复制给下一个 AI 的内容**

---

你是 LinkDesk 工程「插件兼容机械化」批次（E6 第 8 层）的**会话 A**——**本会话做两格**：先做**格 2 `E6#115` 只加不删门禁**，做完**收尾并提交**后，再做**格 3 `E6#116` 退役登记**。工作目录 `e:\linkdesk`，当前分支 `electron`。

**🔴 进场先读（顺序固定，读完再动手）**

1. `AGENTS.md`（指针）→ `CLAUDE.md` 全文。
2. `docs/02-Electron架构/E6_插件生态与发布/插件兼容机械化/交接.md` **顶部四段**：最新的是**你这段** → 下面「编排变更」段（你说「可以不做一格」这条规矩从哪来 ＋ 四条护栏 ＋ 成本账）→ 再下面**格 1 段**（上一棒交接，**除 §⑩ 外的全部内容照旧生效**）→ 最后「通知段」（格 5 的事，与你无关）。
3. 同夹 `00-整理档案.md`（批次总纲：7 格全貌 ＋ **§〇d 三套词表** ＋ §九 纪律）。
4. **你自己的两份任务书**：`02-任务-只加不删门禁.md`（格 2）＋ `03-任务-退役登记.md`（格 3）——**做什么以这两份为准**；格 2 的红报文就是格 3 的登记入口（同一会话内，你做完格 2 的产物就是格 3 的输入）。

拿不到这几份 ⇒ 先问用户「我是不是会话 A（格 2 ＋ 格 3）」。

**🔴 两格的收尾规矩（本会话唯一的新东西）**

- **做完格 2 就先收尾并提交，再开工格 3**。每格各自做全套：① `交接.md` **顶部追加一段**「给下一个 AI 的话」② **剩余任务队列表整张复制**进新段、把自己那格改 ✅ ③ `E6-执行清单.md` 第 8 层**勾格全套**（标题行 ＋ 所有子项，不许只勾一半）④ 同段**轮次进度行**；然后 `git add` **显式路径**提交（⛔ 禁 `git add -A`）。
- 格 3 做完**同样再来一套**。**两格 = 两段、两套勾格、两笔提交——⛔ 不许合表、⛔ 不许揉成一笔**。
- **同一时刻只推进一格**；跨仓 / 发版 / 用户点头处**串行**。
- 若做到约 **250 次调用**、或上下文**压缩 ≥3 次**，**做完手头这格就收尾换会话**（把剩下的格写进交接段交给下一棒），别硬撑。

**🔴 硬约束（照旧）**

`npm run check` **整条命令退出码 = 0** 才算绿；提交信息带类别前缀（`feat:` / `fix:` / `docs:`）、版本号同步 CHANGELOG、CHANGELOG 历史不许回改；**⛔ 不许 `git push`**（推送等用户本人点头，推必带代理）；报结论不复述工具输出全文；**别扇出子代理**；**格数仍是上限不是配额：7 格一格不能省**（判「不做」必须写明理由）。

**📌 格 2 最容易做错的三条（格 1 实测交接）**

1. **面快照第 ③ 栏「宿主 `ldk-*` 类名 ＋ 保留关键帧」必须与尺子同源**：扫 `src/**/*.css`（含共享组件单一源码），走 `scripts/lib/css-selectors.mjs` 的 `bareClassDefinitions` 取**裸定义**（今天 **348** 个，全部 `ldk-*`）＋ `packages/plugin-sdk/schemas/reserved-class-names.json` 的 **8 条**关键帧。两处不同源 ⇒ 就会出现「快照说缺、尺子说没悬空」。口径全文 = `01-任务-悬空名核验.md` §8.2 ①③⑤。
2. **拿不到基线 ⇒ 拒绝判定并红**（02 号档硬边界②）：⛔ 不许静默放行、⛔ 不许改成比工作区。
3. 📌 **格 1 交给你的一条真读数**：`ctx-item-chevron`（宿主 `ContextMenu.tsx:404` ＋ `:438` 渲染，宿主 CSS 该族名**任何拼写都不存在**，`git log -S` 证明从未定义过）——格 1 按口径**只计数不判**，它正落在你面快照第 ③ 栏的边界上；**怎么处置请写进你的 §八 与交接段**。同类还有 `ldk-sle-row--locked`（带 `ldk-`，格 1 报出的真悬空）。

**📌 别重踩（格 1 踩过的）**

改 `docs/03-插件制造/**` 或 `docs/03-plugin-authoring/**` 就必须跑 `npm run docs:build` ＋ 动 `@linkdesk/plugin-docs` 版本 ＋ 同步 `package-lock.json`（`check-lockfile-sync` 会抓）；产物是 zip，要 `unzip -p <zip> <entry>` 读条目，**grep zip 得全 0 不是读数**；静态尺子必须做 **JS 词法**（不做就会被字符串里的 `className=` 假红淹掉）；`packages/linkdesk-ui/dist/index.css` 是 gitignored 的**派生物**，别当定义源；两道文档门禁会抓自己（`check-doc-links` 断链 = 红、`check-design-flow` §8.4③ 要求含 `window.linkdesk.` / `contributes.` 的 md 带「设计前置」或「非新能力」声明）。

**🔴 别做的**

不改插件仓任何文件（发现只**报**）；不新造第二把 CSS 抽取尺子；**不引白名单 / 基线棘轮 / 豁免名单**（本层红线②：要豁免就修机制）；不改机制（加载器 / 注册表 / 主题引擎 / `minAppVersion` 拒载）；不推 `git push` / 不发 npm / 不推 18 仓。

---

## ← 编排变更（**非格子 · 交棒纪律变更**）· 2026-09-18：用户取消「一 AI 一格」⇒ **建议 3 会话 × 2 格**

> 本段不是某一棒的交接口，是**纪律变更**的告知（按「只追加不改旧段」补记）——免得下一棒拿着旧读法**以为只能做一格**，或反过来把两格**揉成一笔**。
> 🔴 **射程只在本批（L8 插件兼容机械化）**：`样式命名空间归一化` / `非样式命名空间归一化` / `插件源码外移层` 各自的接力纪律**一字未动**（那些系列的「一格一账」照旧）。

### ⓪ 用户改了什么、什么没改

- **用户原话（2026-09-18）**：「我也取消必须一次一格的要求了，所以让你看看怎么让ai做，几个任务一个ai这样？」
- **作废**：「一个 AI 会话只执行一格」。
- **保留**（这四条不是仪式，是**账目可核对的前提**）：
  1. **一格一次收尾**：每格做完**各自** ① 顶部追加一段 ② 剩余队列表整张复制 ③ 勾格全套 ④ 轮次进度行——**两格 = 两段、两套勾格**，⛔ 不许揉成一笔（先例：样式归一化系列 `1.17` ＋ `1.19b` 交给同一个 AI，那次的写死要求就是「**不要揉成一笔**」）。
  2. **做完一格就提交一格**（⛔ 不攒着提交）——中途换会话 / 出错时损失最小，账也随时可核。
  3. **同一时刻只推进一格**；**跨仓 / 发版 / 用户点头闸门处串行**，⛔ 不并行（格 5 与格 7 都动 `marketplace`；格 4 的读数就是格 5 的输入；格 5 段 2 的用户点头见下面「通知段」）。
  4. **格数仍是上限不是配额**：**7 格一格不能省**——取消的是「几格一个会话」，**不是**「格能不能省」。

### ⑴ 建议分组：**3 会话 × 2 格**（顺着依赖链切，不是按均匀切）

| 会话 | 格 | 为什么这两格一起 | 预计调用 | 非高峰估算 |
|:--:|:--|:--|:--|:--|
| **A** | 格 2 `E6#115`（先）→ 格 3 `E6#116` | **格 3 的前置就是格 2**，且格 2 的**红报文就是格 3 的登记入口**；同一族文件（面快照 / 退役账 / `host-reserved` / SDK schemas）**摸一遍就够** | 240–310 | ¥1.7–2.4 |
| **B** | 格 4 `E6#117`（先）→ 格 5 `E6#118` | **同一条数据链的两半**：格 4 算出状态、格 5 把它显示出来（格 5 前置 = 格 4 ＋ 用户点头 ✅ 已解锁）；两格都动壳 ＋ `marketplace` | 300–430 | ¥2.3–3.3 |
| **C** | 格 6 `E6#119`（先）→ 格 7 `E6#120` | **格 6 交 §8.7 素材、格 7 出全批收口报告**（第 0 格写死的分工）⇒ 连做天然衔接；两格都在壳 ＋ SDK / `@linkdesk/ui` 发版轴上 | 330–470 | ¥2.5–3.6 |

- **依赖（定顺序，别背成口诀）**：**A 与 B 互不依赖**（谁先都行）；**C 必须最后**——格 6 等格 2（在 A），格 7 要等全部读数（A ＋ B）。
- ⚠️ **这是建议、不是死规**：任一会话**做到 ~250 调用、或压缩到 ≥3 次**就该收尾换会话（账本里「三格一会话」那段 = **269 调用 / 4 次压缩 ＝ 已知成功上限**；格 1 = **140 调用 / 1 次压缩**）。**A/B/C 之间的边界随时可切**——每格本来就是天然断点。
- **变体（要一句话质量最稳时）**：格 5 是全批**唯一用户可见 ＋ 有禁词口径**的格，可把它**单独一棒**（4 会话 ≈ ＋1 次进场 ¥0.1）。默认仍建议 3 会话。

### ⑵ 诚实成本账：分批省的**不是大头**

- 实测单价 ≈ **¥0.0076 / 次调用（非高峰）**，且**与会话长短几乎无关**：格 1（单格）= 140 调用 ¥1.06（均 ctx 104k）；账本里「三格一会话」那段 = 269 调用 ¥1.96（均 ctx 102k）⇒ 单次均 **0.0076 vs 0.0073**，基本持平（长会话靠压缩反复把上下文压回 ~100k）。
- ⇒ **分批真正省的**：① 少 3 次进场重读（≈30–45 次调用）；② **同一工作面不重复探索**（格 2/3 同一族文件、格 5/7 同一 `marketplace`——第二次做时地图已在上下文里）。合计约 **10–15%**。
- ⇒ **分批的代价**：单会话更长 ⇒ 压缩更多（每次 ¥0.04–0.365，且**压缩会重写账本里的旧记录**）＋ 判据串味风险 ＋ 中途出事损失更大——所以 ⓪ 里那四条**不是可选项**。
- 🔴 **省钱大头不在这**：**非高峰（工作日 18:00 后 / 周末）跑 ⇒ 单价直接减半**，比任何编排都划算；其次**不返工**（进场一次读齐、照各格 §⑷/§⑸ 的坑单走）。

### ⑶ 用户派工话术（照抄，只改 N/X/Y）

「你是本批第 N 棒。**本会话做两格：格 X（先）＋ 格 Y（后）**。① 格 X 做完**先收尾并提交**（勾格全套 ＋ 轮次进度行 ＋ 交接段追加 **X 段**），**再**开工格 Y；② 两格读数各写各的表，**⛔ 不许合表、不许揉成一笔提交**；③ 同一时刻只推进一格，跨仓 / 发版处串行；④ 🔴 不许 `git push`，推送等用户点头。」

---

## ← 第 1 格（格 1 · `E6#114` **悬空名核验**）· ✅ 已完成（2026-09-18）：**给下一个 AI ＝ 格 2 · `E6#115` 只加不删门禁**

### ⑩ 🔴 先说你是谁（⛔ 别猜）

你是**第 2 棒**，任务是 [02-任务-只加不删门禁.md](02-任务-只加不删门禁.md)（`E6#115`）。
- **以本段为准**：本段与你读到的任何别的说法冲突，**用本段**。拿不到本段 ⇒ 先问用户「我是不是格 2」。
- **你不是复核者**（本批没有「复核轮」这种格），**也不是派工者**（派工在第 0 格就做完了）。
- ⚠️ **上一棒的「通知段」（用户对设计图点头 ⇒ 格 5 段 2 解锁）仍在下面，照旧生效**——它管的是格 5，与你无关但别当成你的任务。

### ⑴ 上一棒（格 1）做完了什么（一句话 ＋ 读数）

- **一句话**：造出**只读尺子** `scripts/plugin-dangling-name-audit.mjs`——解开插件产物（zip 或目录）取「它喊的名字」，判**在不在宿主定义集里**；**只报不拦、退出码恒 0、⛔ 不进 `npm run check`**。
- **读数（产物 24 个：随包 6 ＋ 官方 18 · 未构建 0）**：**合计悬空 = 2 处 / 1 个名字**。
  - ✅ **判据①（误报控）达标**：`editor` 单独量 **悬空 0**（19 关键帧引全部被兜住／9 模板跳过／137 动态跳过／1128 CSS 提及），**74 个 Monaco·VS Code 裸名一条没报**。
  - 🔴 **唯一那条悬空 ＝ 宿主自己的死引用**：`ldk-sle-row--locked`（渲染点 `StringListEditor.tsx:102`，宿主 CSS **从未定义过**）⇒ 按「只报不修」**挂给宿主维护者**，本格**未改任何插件仓**。
  - 📌 **附带一条真读数（与你有关，见 §⑶ 末条）**：`ctx-item-chevron`（宿主 `ContextMenu.tsx:404,438` 渲染、宿主 CSS **从未定义过该族名**）——按本格口径（非 `ldk-` 只计数）**未判**，**交给你判「算不算面」**。
- **执行记录全文**（口径／逐仓表／残余边界 9 条／check 读数）：[01 §八](01-任务-悬空名核验.md)。**别重写它**，要补就补 §八.4 或自己那格的档。

### ⑵ 剩余任务队列（**自己那格已改 ✅ → 你做完改你那格，再整张复制进新段**）

| 序 | 轮次 | 编号 | 任务 | 详案 | 前置 | 状态 |
|:--:|:--|:--|:--|:--|:--|:--:|
| 1 | 第 8.1 轮 | `E6#114` | 悬空名核验（产物层只读审计） | [01](01-任务-悬空名核验.md) | 无 | ✅ |
| 2 | 第 8.2 轮 | `E6#115` | 只加不删门禁（面快照 ＋ diff ＋ 判红） | [02](02-任务-只加不删门禁.md) | 无（与格 1 互不依赖） | ⬜ |
| 3 | 第 8.3 轮 | `E6#116` | 退役登记（账 ＋ 流程 ＋ 机械对账） | [03](03-任务-退役登记.md) | 格 2 | ⬜ |
| 4 | 第 8.4 轮 | `E6#117` | 兼容读数 | [04](04-任务-兼容读数.md) | 格 1 ✅ | ⬜ |
| 5 | 第 8.5 轮 | `E6#118` | 市场显示状态（**含 HTML 设计图**） | [05](05-任务-市场显示状态.md) | 格 4 ＋ **用户点头 ✅ 2026-09-18** | ⬜ |
| 6 | 第 8.6 轮 | `E6#119` | 判据铺开（SDK 随包 ＋ 18 仓接腿 ＋ 发版） | [06](06-任务-判据铺开.md) | 格 1 ✅ ＋ 格 2 | ⬜ |
| 7 | 第 8.7 轮 | `E6#120` | 通用悬停提示卡（**壳的共享件**）＝ **本批最后一棒**（＋ 全批收口报告） | [08](08-任务-通用悬停提示卡.md) | 格 5（⚠️ **可换序、⛔ 不并行**） | ⬜ |

> **执行序 = 自上而下**。🔴 **格数是上限不是配额**（**7 格**一格不能省；判「不做」要写理由，写进本交接段的「不做」节）。
> ⚠️ **别与 `E6#113`（插件侧死类清账）并行**——那格要动 3 个插件仓，格 5 与格 7 也动 `marketplace`。
> ✅ **格 1 与格 2 互不依赖**（第 0 格已写死）⇒ 格 2 可立即开工，**不必等任何前置**。

### ⑶ 你这一棒（格 2）具体做什么——**以 [02 号档](02-任务-只加不删门禁.md) 为准**，这里只点三条最容易做错的

1. **面快照第 ③ 栏「宿主 `ldk-*` 类名 ＋ 保留关键帧」的生成口径，直接抄格 1**：[01 §8.2](01-任务-悬空名核验.md)①③⑤——扫 `src/**/*.css`（含共享组件单一源码）用 `scripts/lib/css-selectors.mjs` 的 `bareClassDefinitions` 取**裸定义**（今天 **348** 个，全部 `ldk-*`）＋ `reserved-class-names.json` 的 **8 条**关键帧；🔴 **两处必须是同一个生成器**（否则「快照说缺、尺子说没悬空」，就是本系列还了两轮的那种债）。
2. **拿不到基线 ⇒ 拒绝判定并红**（02 号档硬边界②）——⛔ 不许静默放行、⛔ 不许比工作区。
3. 📌 **格 1 交给你的一条真读数**：`ctx-item-chevron`（宿主 `ContextMenu.tsx:404` ＋ `:438` 渲染，宿主 CSS 该族名**任何拼写都不存在**，`git log -S` 证明**从未定义过**）。格 1 按口径（非 `ldk-` 不是宿主契约）**只计数不判**——**它落在你的面快照第 ③ 栏边界上**：它是「渲染方」不是「定义」，**本格如何处置（计不计面 / 记不记账）请写进你的 §八 与交接段**。⚠️ 顺带的同类：`ldk-sle-row--locked`（这条带 `ldk-`，是格 1 报出来的真悬空）。

### ⑷ 格 1 踩出来的（**别重踩**）

1. 🔴 **改作者面文档 ⇒ 必须重生成随包文档**：改了 `docs/03-插件制造/**` 或 `docs/03-plugin-authoring/**` 就跑 `npm run docs:build`，否则 `generate-plugin-docs --check` **当场红**（本格实撞）；内容变 ⇒ `@linkdesk/plugin-docs` **版本要动**（0.1.18 → **0.1.19**）＋ `package-lock.json` 同步（`check-lockfile-sync` 会指出「manifest = 0.1.19，lock = 0.1.18」）。
2. ⚠️ **产物是 zip**：必须 `unzip -p <zip> <entry>` 读条目；**grep zip 得全 0，那不是读数**。
3. ⚠️ **静态尺子必须做 JS 词法**：不做就会命中**字符串里**的 `className=`（生成式代码 `'… className="' + x`）⇒ 本格 v1 一版误报 **2145** 处（editor 一只 ~1029 处 Monaco 名）。做法 = 跳过字符串/模板/注释/正则，只认**代码上下文**的站点。
4. ⚠️ **跨产物聚合别在「条目数组」上误用 `.keys()`**：`r.notJudged` 是 `[[名字, 出处], …]` 数组，`.keys()` 给的是**下标** ⇒ 把「去重名字数」算成「最长那份的条数」（本格实测踩到：74 被当成并集，真并集 99）。**已提成 `uniqueNotJudged()` ＋ 自测「负控F-并集去重」钉住**。
5. ⚠️ **读数相等 ≠ 字节相等**：随包 6 只与官方同名 6 仓读数逐格相等，但 `settings` 一对 **zip 字节不同**（逐条目 sha256 相同 ⇒ 差在 zip 容器元数据）——对账时别用 sha256 下结论。
6. ⚠️ **`packages/linkdesk-ui/dist/index.css` 是派生物**（gitignored）：本格**实测**不读它**不丢定义**（它里面 85 个 `ldk-*` 名逐个都能在壳 `src/**/*.css` 找到，未覆盖 = 0）——你要用「宿主面」时照此口径，别把派生物和真源重复计。
7. ⚠️ **文档门禁会抓自己**：`check-doc-links`（断链 = 红）＋ `check-design-flow` §8.4③（含 `window.linkdesk.` / `contributes.` 的 md 必须带「设计前置」或「非新能力」声明）——续写时**保持这两个词在**。

### ⑸ 不做（**边界，明写理由**）

| 不做 | 理由 |
|:--|:--|
| 不推 `git push` / 不发 npm / 不推 18 仓 | 🔴 用户红线：**等用户本人点头**（memory `push-wait-for-user`）。格 1 因此**留了一盏 npm 黄灯**（见 §⑻） |
| 不并格（**格 1 当时**的纪律） | 该纪律 2026-09-18 已被用户取消 ⇒ 下一棒起按顶部「**编排变更**」段的分组做（**格数仍是上限不是配额：7 格一格不能省**） |
| 不改机制（加载器/注册表/主题引擎/`minAppVersion` 拒载） | 本批只加判据、账、读数、显示 |
| 格 1 不改任何插件源码 / 不改 SDK | 格 1 是**只读审计**（发现悬空只**报**，挂给作者 / 宿主维护者） |
| 格 1 不引黑名单 / 不引白名单 | 禁区③（退役名黑名单 ＝ 要人养名单 ＋ Monaco 当场假红）；红线段② |
| 格 1 不做门禁 | 静态尺子读不出动态拼接（今天 500 处跳过）⇒ 拦就假红；照 `runtime-style-audit.mjs` 的定位 |

### ⑹ 提交清单（格 1）

- **新增**：`scripts/plugin-dangling-name-audit.mjs`。
- **改动**：`package.json`（3 条 `audit:plugin-dangling*`）· `docs/02-Electron架构/E6_插件生态与发布/E6-执行清单.md`（第 8 层 `E6#114` 勾格全套 ＋ 轮次进度行 8.1 ✅）· `docs/02-Electron架构/E6_插件生态与发布/插件兼容机械化/01-任务-悬空名核验.md`（§八 执行记录）· `docs/03-插件制造/05-插件UI写法规约.md` ＋ `docs/03-plugin-authoring/05-ui-conventions.md`（§12.4 作者面一句）· `packages/plugin-docs/docs/**`（重生成 2 文件）· `packages/plugin-docs/package.json`（0.1.18 → 0.1.19）· `package-lock.json`（1 行同步）。
- **零 `src/` 改动 ⇒ 软件版本不 bump**（恒 **0.2.11**）。
- ⚠️ 提交信息带类别前缀（本格用 `feat:`／`docs:`）；`git add` 按**显式路径**（⛔ 禁 `git add -A`）。**提交 sha = `219672986`**（11 个文件 · **未推送**）。

### ⑺ 给下一棒的输入（**可直接抄的东西**）

- **宿主定义集口径**（哪些算 / 哪些不算 / 为什么 ＋ **清单怎么生成随包下发**）：[01 §8.2](01-任务-悬空名核验.md)——**格 6 要把它搬进 SDK，口径必须与壳体一致**。
- **`--self-test` 的形态**（正控 / 负控 / 计数的写法）：`scripts/plugin-dangling-name-audit.mjs` 的 `selfTest()`——**格 2 照这个形状写自测**（含「跨产物并集去重」这种**只会在真数据上咬人**的负控）。
- **今天这条基线读数**：[01 §8.3](01-任务-悬空名核验.md)（24 产物 · 悬空 2 处 / 1 名 · 未构建 0）。
- **复跑命令**：`npm run audit:plugin-dangling`（人读）／`:json`（机读）／`:selftest`（13 例）。

### ⑻ 发布实录

- **软件（壳）零发版**：无 `src/` 改动 ⇒ 版本恒 **0.2.11**。
- 🔴 **作者面 `@linkdesk/plugin-docs` 0.1.18 → 0.1.19（内容变 ⇒ 版本动了）· 尚未 `npm publish`、未 `release:mark`** ——红线「`npm publish` 等用户点头」⇒ `npm run check:npm-release` **亮黄灯**（该闸永不拦，check 照绿 EXIT=0）。
  - **收尾动作（等用户点头）**：`npm publish`（`packages/plugin-docs`）＋ `npm run release:mark`。
  - ⚠️ **格 3 与格 7 也要动作者面文档**（各自要 plugin-docs PATCH）⇒ 三格的作者面改动**可以攒着一次发**；发完记得 `release:mark` 把灯关掉。
- 本格 `npm run check` **EXIT=0**（175 文件 / 2461 例）——末尾三行见 [01 §8.5](01-任务-悬空名核验.md)。

---

## ← 通知（**非格子 · 闸门状态变更**）· 2026-09-18：用户对设计图点头 ⇒ **格 5 段 2 解锁**

> 本段不是某一棒的交接口，是**闸门状态变更**的告知（按「只追加不改旧段」补记）——避免下一棒**误以为还要再等一次点头**而空转。

- 🟢 **用户原话（逐字）**：「**注意，我同意按设计图走**」。
- **效力**：格 5（`E6#118`）**段 1 的出口判据① 已达成**（[05 §四①](05-任务-市场显示状态.md) 已勾 ✅）⇒ **段 2（动插件代码）直接开工**，⛔ **不要再向用户要一次点头**。
- **点头对象 = 现行** [mockups/01-市场兼容状态-细节详解.html](mockups/01-市场兼容状态-细节详解.html)——**当天已按用户要求改过的那一版**（插件详情页大标题旁的「部分不适配」标砍掉，状态词**全页只留信息栏那一处**）。
- **覆盖范围（6 条，逐条照办、偏离写 §9.3）**：① 五词（正常/兼容/部分不适配/不适配/—）② **状态标全页唯一**（信息栏那一行；标题旁不挂、侧栏不标）③ 悬停帮助卡（停标出卡、移开消失）④ 信息栏**不写**「鼠标停上来看」提示行 ⑤ 状态标**不挂 `title`** ⑥ **不进侧栏列表**。全文见 [05 §9.2](05-任务-市场显示状态.md)。
- ⚠️ **不覆盖**（各自**单独**等用户点头）：`git push` 壳仓 / 推插件仓 `marketplace` / `npm publish` / 官方目录收录。**本批红线未变**。
- 📌 队列与前置**其余不变**（下面这张表照旧；格 5 与格 7 仍是「**可换序、⛔ 不并行**」）。

| 序 | 轮次 | 编号 | 任务 | 详案 | 前置 | 状态 |
|:--:|:--|:--|:--|:--|:--|:--:|
| 1 | 第 8.1 轮 | `E6#114` | 悬空名核验（产物层只读审计） | [01](01-任务-悬空名核验.md) | 无 | ⬜ |
| 2 | 第 8.2 轮 | `E6#115` | 只加不删门禁（面快照 ＋ diff ＋ 判红） | [02](02-任务-只加不删门禁.md) | 无（与格 1 互不依赖） | ⬜ |
| 3 | 第 8.3 轮 | `E6#116` | 退役登记（账 ＋ 流程 ＋ 机械对账） | [03](03-任务-退役登记.md) | 格 2 | ⬜ |
| 4 | 第 8.4 轮 | `E6#117` | 兼容读数 | [04](04-任务-兼容读数.md) | 格 1 | ⬜ |
| 5 | 第 8.5 轮 | `E6#118` | 市场显示状态（**含 HTML 设计图**） | [05](05-任务-市场显示状态.md) | 格 4 ＋ **用户点头 ✅ 2026-09-18** | ⬜ |
| 6 | 第 8.6 轮 | `E6#119` | 判据铺开（SDK 随包 ＋ 18 仓接腿 ＋ 发版） | [06](06-任务-判据铺开.md) | 格 1 ＋ 格 2 | ⬜ |
| 7 | 第 8.7 轮 | `E6#120` | 通用悬停提示卡（**壳的共享件**）＝ **本批最后一棒**（＋ 全批收口报告） | [08](08-任务-通用悬停提示卡.md) | 格 5（⚠️ **可换序、⛔ 不并行**） | ⬜ |

---

## ← 第 0 格（派工）· 已完成（2026-09-18）：**给下一个 AI ＝ 格 1 · `E6#114`**

### ⑩ 🔴 先说你是谁（⛔ 别猜）

你是**第 1 棒**，任务是 [01-任务-悬空名核验.md](01-任务-悬空名核验.md)（`E6#114`）。
- **产物 = 一把只读尺子 ＋ 自测 ＋ 逐仓读数 ＋ 作者面一句 ＋ 本交接段**。
- **你不是复核者**（本批没有「复核轮」这种格），**也不是派工者**（派工在本段就做完了：第 0 格写了 6 份任务书）。
- **以本段为准**：本段说的与你读到的任何别的说法冲突，**用本段**。拿不到本段 ⇒ 先问用户「我是不是格 1」。

### ⑴ 本批是干什么的（一句话 ＋ 判据 ＋ 根因 ＋ 收口口径）

- **一句话**：平台承诺「旧插件在新版本上仍然能跑」（memory `plugin-authoring-manual.md:179-180`），但**没有门禁拦「把插件能用的名字拿走」**，也**没有尺子量「插件喊的名字宿主还有没有」** —— 本批 **6 格**把这两件事变成机械件；**第 7 格**（`E6#120`，[08](08-任务-通用悬停提示卡.md)）把格 5 那张悬停提示卡抽成**壳的共享件**。
- **判据**：产出的每一件都必须是**机械化、自动化、常驻**的（不靠记性）——这是用户对前面几轮反复说的「我讨厌『下次小心』」的正面答案。
- **根因**（每棒收尾都要能复述）：**宿主手里有一堆插件正在引用的名字，拿走它们没有任何机械阻力，也没有任何尺子量得出拿走之后谁的产物悬空了。**
- **收口口径**：本批结束时该读到的**判据六条 ＋ 共享件一条**（见总纲 §五）；残余边界八条**一条不许省**。🔴 **报告由格 7 出**（格 6 只交 §8.7 素材）。

### ⑵ 剩余任务队列（**每棒收尾：自己那格改 ✅，然后整张复制进新段**）

| 序 | 轮次 | 编号 | 任务 | 详案 | 前置 | 状态 |
|:--:|:--|:--|:--|:--|:--|:--:|
| 1 | 第 8.1 轮 | `E6#114` | 悬空名核验（产物层只读审计） | [01](01-任务-悬空名核验.md) | 无 | ⬜ |
| 2 | 第 8.2 轮 | `E6#115` | 只加不删门禁（面快照 ＋ diff ＋ 判红） | [02](02-任务-只加不删门禁.md) | 无（与格 1 互不依赖） | ⬜ |
| 3 | 第 8.3 轮 | `E6#116` | 退役登记（账 ＋ 流程 ＋ 机械对账） | [03](03-任务-退役登记.md) | 格 2 | ⬜ |
| 4 | 第 8.4 轮 | `E6#117` | 兼容读数 | [04](04-任务-兼容读数.md) | 格 1 | ⬜ |
| 5 | 第 8.5 轮 | `E6#118` | 市场显示状态（**含 HTML 设计图**） | [05](05-任务-市场显示状态.md) | 格 4 ＋ **用户点头** | ⬜ |
| 6 | 第 8.6 轮 | `E6#119` | 判据铺开（SDK 随包 ＋ 18 仓接腿 ＋ 发版） | [06](06-任务-判据铺开.md) | 格 1 ＋ 格 2 | ⬜ |
| 7 | 第 8.7 轮 | `E6#120` | 通用悬停提示卡（**壳的共享件**）＝ **本批最后一棒**（＋ 全批收口报告） | [08](08-任务-通用悬停提示卡.md) | 格 5（⚠️ **可换序、⛔ 不并行**） | ⬜ |

> **执行序 = 自上而下**。🔴 **格数是上限不是配额**：**7 格**一格不能省（⚠️ 2026-09-18 立格 7 后由「6 格」改为 7 格；判「不做」要写理由，写进本交接段的「不做」节）。
> ⚠️ **别与 `E6#113`（插件侧死类清账）并行**——那格要动 3 个插件仓，格 5 与格 7 也动 `marketplace`。
> 🔴 **格 6 原写的「恒最后」已作废**（2026-09-18 立格 7）⇒ **全批收口报告归格 7**；格 6 只交 §8.7 的收口素材。

### ⑵b 🔴 文案口径（用户 2026-09-18 两次纠偏后定稿——**格 5 那一棒必须照办**）

用户原话（逐字）：①「我感觉你很多东西写的都很口语化，比如"**老但兼容**"，直接写个**兼容**不好吗？」②「安装版软件更多是给用户看的，你写个 **n 处待修**，这个"待修"是给谁看的？给用户看的吗？指望用户修完后放到源码中吗？**你要清楚这是给谁的软件**。」③「**我从没见过手机厂给用户发的版本更新里写他们改的什么源码**。」④「在用户视角看，有个**正常，兼容，不适配**等提示才是合理的」。

- **三套词表按受众分家**（**唯一真相源 = [00 总纲 §〇d](00-整理档案.md)**）：**用户**面只许 **正常 / 兼容 / 部分不适配 / 不适配 / —** ＋ 用户自己能做的动作；**插件作者**面才写「N 处引用已失效的名字 ＋ `文件:行` ＋ 升依赖重打包」；**维护者**面用「悬空名 / 面快照 / 只加不删 / 退役账」。
- ⛔ **已作废的说法**（谁都不许再用）：「老但兼容」「N 处待修」「部分异常」「契合度/契合」。**禁词机械核** = [05 §四⑥](05-任务-市场显示状态.md)。
- ⚠️ 本批文档里凡是给用户看的字，写的时候先问一句：**这句话是给用户看，还是给作者看？**

### ⑶ 你这一棒（格 1）具体做什么

- **要做的四步**：① 脚本 `scripts/plugin-dangling-name-audit.mjs`（只读、只报不拦、**不进 `npm run check`**）② npm script ＋ `--self-test`（正控 ＋ 三条负控）③ 逐仓读数（随包 6 只 ＋ 官方 18 仓产物，合计预期 **0**）④ 作者面一句 ＋ 清单勾格 ＋ 交接段。详案见 [01](01-任务-悬空名核验.md) §二。
- 🔴 **最要紧的一条判据 = 误报控**：`editor` 产物里 **60 处裸名是 Monaco / VS Code 自带的**（`monaco-workbench` 978 处等）⇒ **一条都不许报**。假红比漏报更坏。
- ⛔ **别把尺子做成门禁**（静态尺子读不出动态拼接，拦就假红）；⛔ **别放 `scratch/`**（那里不进 git）。

### ⑷ 第 0 格踩出来的（**别重踩**）

1. 🔴 **「版本印记」方案已否**：插件 zip 里**没有 `package.json`**（`packages/plugin-sdk/src/pack.ts:56-62` 的排除表）、bundle 文本里**没有任何版本字符串**（grep `0.2.x` 命中 0）⇒ 判据**物理上取不到输入**。
2. 🔴 **「扫退役名黑名单」已否**：要人养名单（禁区），而且 Monaco 的 60 处裸名当场假红。
3. ⚠️ **产物是 zip，不是文本**：必须 `unzip -p <zip> <entry>` 读条目；**grep zip 得到全 0，那不是读数**（第 0 格踩过）。
4. ⚠️ **中文长文本别用 shell heredoc**（反斜杠会被吃掉）；写文档用编辑器/工具。
5. ⚠️ **两道文档门禁会抓自己**：`check-doc-links`（断链 = 红）＋ `check-design-flow` §8.4③（含 `window.linkdesk.`/`contributes.` 的 md 必须带「设计前置」或「非新能力」声明）——本批 7 份档已按此写好，你续写时**保持这两个词在**。

### ⑸ 不做（**边界，明写理由**）

| 不做 | 理由 |
|:--|:--|
| 不推 `git push` / 不发 npm / 不推 18 仓 | 🔴 用户红线：**等用户本人点头**（memory `push-wait-for-user`） |
| 不并格（一格做完就交棒） | 纪律：一 AI 一格，格数是上限不是配额 |
| 不改机制（加载器/注册表/主题引擎） | 本批只加判据、账、读数、显示 |
| 本格不改任何插件源码 / 不改 SDK | 格 1 是**只读审计**（发现悬空只**报**，挂给作者） |
| 不设「已 bump 未 tag」哨兵 | 用户 2026-09-18 明确不要（接受跳版本、攒着发） |

### ⑹ 提交清单（第 0 格 ＝ 本批文档，**未提交**）

- 新增：`docs/02-Electron架构/E6_插件生态与发布/插件兼容机械化/`（`00-整理档案.md` · `01`–`06` 六份任务书 · `交接.md` · `mockups/01-市场兼容状态-细节详解.html`）。
- 改动：`docs/02-Electron架构/E6_插件生态与发布/E6-执行清单.md`（新增第 8 层块 ＋ 执行顺序段一行 ＋ 总结表 L8 行与合计 ＋ 📌 追加说明）。
- **提交 sha 事后追记**（本段写于派工时，尚未提交）。
- ⚠️ 提交信息带类别前缀（`docs:`）；`git add` 按**显式路径**（⛔ 禁 `git add -A`）。

### ⑺ 给下一棒的输入（**可直接抄的东西**）

- **口径**：宿主定义集 = 壳 `src/**/*.css` 的类名定义 ＋ `packages/plugin-sdk/schemas/reserved-class-names.json` 的 8 条保留关键帧；自身定义集 = 产物包内 CSS。
- **形状先例**：`scripts/plugin-css-prefix-audit.mjs`（只读审计）／`scripts/runtime-style-audit.mjs`（不进 check 的定位）／`scripts/lib/css-selectors.mjs`（抽取库，⛔ 别新造）。
- **今天的基线**：随包 6 只 ＋ 四只 ui-dependent 产物**悬空 = 0**（开跑前自己复量一遍对账）。

### ⑻ 发布实录

- 本批**零发版**（第 0 格只动文档）；后续每格的发版记在自己那段里。🔴 **凡更新就发**（SDK/ui/docs 内容变 ⇒ 必须发）＋ **插件轴先发、壳最后**。

---

<!-- ↑ 新的交接段追加在这条线的上方（越靠上越新） -->
