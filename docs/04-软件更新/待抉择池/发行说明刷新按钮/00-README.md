# 04 待抉择池 · 发行说明刷新按钮

> 2026-09-13 立案（用户）。**状态：待拍板**——只登记设计，**未开工、零代码改动**。
> 归属：**04-软件更新 / 软件本体**（壳内视图 + 主进程取数腿 + 壳命令，零插件参与）。
> 本项按用户要求**建子夹而不是两个裸文件**（设计文档 + HTML 图 ≥2 份 ⇒ 成套收口）。

| 文件 | 是什么 |
|:--|:--|
| [01-设计.md](./01-设计.md) | 设计文档：语义边界（刷新 vs 重试）· 现状四层链路（带证据）· 8 维度 · 4 条落点 · 3 条待拍板 · 用户视角验收 |
| [mockups/01-发行说明刷新按钮.html](./mockups/01-发行说明刷新按钮.html) | 增量 mockup：在 `06-主软件更新/mockups/03-发行说明标签页.html` 基础上加「刷新」按钮与三态（默认 / 刷新中 / 完成无新版 / 失败） |

## 立案缘起（一句话账）

发行说明的数据是主进程 **24h 落盘缓存**（`update-release-notes.ts` 的 `CACHE_MAX_AGE_MS`，失效判据两条：24h 过期 ＋「所请求那版不在缓存里」），且**全链路没有任何手动刷新入口**——`retry` 只在「拉失败」的空态出现、语义是"重试当前这一版"。于是刚发布的新版本，用户在当天可能一直看不到。补一个「刷新」按钮即可（绕过缓存的取数腿现成，只差一个 `force` 形参与一条池→壳命令）。

## 依赖与碰撞

- **契约同步**：`PoolReleaseNotesData` 若加「刷新中」位 ⇒ `06-主软件更新/07-数据流通格式.md` 同笔更新。
- **同区在办**：E6#57.x 更新批次也动 `useReleaseNotes` / 主进程取数腿 / 通知面——本项排其后，按新 HEAD 改。
