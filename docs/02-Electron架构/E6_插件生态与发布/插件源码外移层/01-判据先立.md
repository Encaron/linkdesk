# 7.0 判据先立——纯文档零代码

> **任务号：** E6#97 · **前置：** 无 · **产物：** 规范条文 + 作者面文档 + 指针（**零源码改动 ⇒ 不 bump**）
> **体例同源：** [文件整理层 §3.6.1](../文件整理层/00-整理档案.md) 的「规范文档对齐」、[插件规范化层 §3.7.1](../插件规范化层/00-整理档案.md) 的同名轮——**先把尺子立正，后面七轮才有判据**。那两轮的实际教训是：三份规范互相矛盾且都过期，不先立正，后面每轮都会各按自己的理解做。

---

## 〇、为什么必须先做这一轮

本层要动的是**「源码住在哪」这条从没被写下来的规矩**。全仓现状：

- `docs/03-插件制造/09-插件目录规范.md` 规定了插件源码目录长什么样，但**一个字都没说「源码该住在哪个仓库」**；
- `docs/03-插件制造/04-插件分发格式.md` 写了「安装目标 `{userData}/plugins/<id>/`」「源码树 vs 已安装是两个代码根」——**但「源码树」在哪，它默认就是壳仓**；
- `docs/05-插件更新/00-README.md:10` 有一句「单个插件的新版本**在仓外独立工程目录开发**」——**这句是未来的设想，不是现行规范**，而且没写「源码本身搬出去、壳仓那份删掉」。

⇒ 不先立这条，"搬出去"在执行层就永远能被理解成「复制一份到外面」（双真相源，无门禁可发现分叉）。

---

## 一、落点一：插件目录规范补「源码位置与分发物关系」

**文件：** `docs/03-插件制造/09-插件目录规范.md`

**要做：** 新增一节（建议放在现有「分发物 = SDK 构建的单文件 zip」那条之后，紧挨着，因为它们是同一条链的两端），写死五条：

1. **源码真相源 = 插件自己的仓库**（`github.com/<owner>/<repo>`），**不是壳仓**。壳仓内的 `plugins/<id>/` 只允许存在两类东西：**夹具**（不发货的 demo）与**构建产物**（`bundled-plugins/<id>.linkdesk-plugin`）。
2. **壳仓 `bundled-plugins/<id>.linkdesk-plugin` 的语义** = **出厂发货用的种子快照**，其版本由 7.4 轮的 `bundled-plugins.lock.json` 记账。它**不是**源码的第二份拷贝——源码改了必须回插件仓改，改完走发布链路刷种子。
3. **插件源码目录与分发物的关系** = 源码仓 → `npm run build` → `<id>.linkdesk-plugin`（zip）→ GitHub Release asset → catalog 条目。**从源码到用户桌面没有一步经过壳仓。**
4. **插件身份（`plugin.json` 的 id）永不可变**；显示名（`name`）可改；仓库名可改。
5. **插件内有且只有一处版本号真源** = `plugin.json.version`；`package.json.version` 不是真源（7.1 轮 §六 处置它）。

**判据：** 该节里每一条都能在 7.1–7.4 轮里找到消费它的任务；**没有一条描述与 `publish.ts` 的实际行为相反**（逐条对源：[publish.ts](../../../../packages/plugin-sdk/src/publish.ts) 的「目标仓库 = origin」「写仓库根 marketplace.json」「readmeUrl/changelog 自动写」三处）。

---

## 二、落点二：新建作者面文档「多仓开发与本地工作区」

**文件：** 新建 `docs/03-插件制造/15-多仓开发与本地工作区.md`

> 📌 编号 15 是建议——**落笔时先 `ls docs/03-插件制造/` 取实际下一个空号**（当前最大是 14）。

**要做：** 覆盖四件事，**每件都要有「为什么」**（这份文档的读者包含未来的 AI，只写「怎么做」它会在下一个会话里按自己的理解改掉）：

1. **本地目录约定（D6）**
   - 推荐容器 `E:\linkdesk-plugins\`，内部按**归属**分 `official\` / `third-party\`，**不要按功能分类**（归属稳定，功能分类会随插件增加不断重划；且 catalog 里的 `category` 已经在做功能分类，本地再分一遍是重复）。
   - 🔴 **容器目录本身绝不建仓。** 理由写清楚：一旦容器被 `git init`（IDE 的「初始化仓库」提示、在错层级敲 `git init`、从父目录 `git add .` 都可能触发），每只插件的 `.git` 变成嵌套仓、父仓把它们记成 gitlink——**静默坏掉且极难看出来**；更要命的是它会**把脚手架的「已在 git 仓内 → 不 init」规则反转成灾难**（每只新插件都不建自己的仓，全变成容器子目录 = 你要避开的 monorepo）。
   - **容器里放一张说明**（`README.md` 或 `不要在这里建仓.txt`）——防的不只是作者本人，还有以后进场读目录的 AI。
2. **插件仓命名（D5）**：仓库名**完全自由**，不强制任何前缀。给出**建议但非强制**的惯例，并写明**改仓库名的代价**：已发布 catalog 条目里的 `downloadUrl` / `readmeUrl` 内嵌 `owner/repo`，改名会断链（源码不受影响）。
3. **身份 vs 显示名 vs 仓库名（D5）**——照 VS Code 实测口径写（§五 引用块）。⚠️ **这一条不止两行**：它牵出「插件身份今天靠目录名兜底」这个结构问题 ⇒ **完整展开与落地在 [09-命名规范.md](09-命名规范.md)**，本落点只负责把结论落进作者面文档（**引用而非复制**）。
4. **本地目录位置与插件身份无关**（这是本层最重要的一条性质，也是 `hello-linkdesk` / `first-run-setup` 平铺在外的正当性）：插件仓是自足的，`git clone` 到哪都能构建；壳只认 Release URL 与 catalog，**不认本地路径**。⇒ 目录结构随时可重排，零风险。
5. **发布链路上的四个名字不许手改**（`<id>.linkdesk-plugin` / `v<version>` tag / 版本号格式 / `pluginId`）——它们参与安装裁决与更新对账，**手改即身份漂移或更新链断**（[09-命名规范.md §一/§五](09-命名规范.md) 的规则表）。

**判据：** 一个没参与过本轮讨论的人只读这份文档，能回答「我的插件工程该放哪、仓库该叫什么、怎么改显示名」。**自测方式：让 AI 在无上下文的情况下读它并回答这三问。**

---

## 三、落点三：开发指南补「建仓」这一步

**文件：** `docs/03-插件制造/13-插件开发指南.md`（九步动线）

**要做：** 在第 1 步（脚手架）之后补一句「**脚手架已代建 git 仓**」（7.6 轮落地后为真），并把第 10 步「发布」的前置从「手敲 `git init` / `remote add` / `push -u` 三条命令」改成「确认 origin 指向你的 GitHub 仓」。

> ⚠️ **顺序陷阱：** 这一步的措辞依赖 7.6 轮的脚手架改动。**若 7.6 轮尚未做完，此处写成「脚手架将代建（见 7.6 轮）」并标注状态**——不许写成已实现的承诺（`publish.ts:226` 现在的报错文案就是让作者手敲三条命令，那是现状真相）。

**判据：** 指南里**没有一句**与 `publish.ts` 的实际前置检查冲突。

---

## 四、落点四：发布清单补「出厂种子」占位

**文件：** `docs/06-发布管理/发布清单.md`

**要做：** 在发布前清单里加一行（**内容先写占位，具体命令由 7.4/7.5 轮回填**）：

```
- [ ] 出厂种子已是各 bootstrap 插件最新已发布版（npm run sync:bundled → 门禁 check-bundled-freshness 绿灯）
```

⚠️ 这份清单的既有硬规矩是**不许写死版本号、不许写死文件名**（指针集，见该档 §底），所以这一行**不许出现任何版本号**。

**判据：** 该行存在，且不含版本号/文件名。

---

## 五、落点五：总纲与记忆指针

| 文件 | 改什么 |
|:--|:--|
| `CLAUDE.md` | ① Phase 表「之后」行的表述里补「出厂插件源码在仓外、随包靠构建期同步」；② 「关键文件」表加一行指向 [00-整理档案.md](00-整理档案.md)；③ 头部进度行按 L7 实际进度更新 |
| `docs/05-插件更新/00-README.md:10` | 那句「未来开发工作流……届时为该插件单独建工程目录」**已经实现了一半** ⇒ 改成与 7.0 落点一一致的现行表述（去掉「未来」口吻，补「壳仓只留产物」） |
| `docs/开发管理/工程管理与Git策略.md` | 🔴 **该档已腐烂**（写着分支 `e4`、写着 `E:\linkdesk\linkdesk\` 套娃目录——2026-09-04 已上移合一）。本层是 Git 策略的实质性变更 ⇒ **同笔重写该档的「目录结构」「分支策略」两节**。🔴 **并且**：该档同时是 **`docs/decisions/` 决策记录义务的规则出处**（E5.8#6 立的）⇒ **重写时要同笔写死「memory ↔ decisions 的分工判据」**（见下一行） |
| **`docs/decisions/README.md`** 🔴 **新加（2026-09-14 用户点头）** | **两件事**：① **补漏登**——「目录」一节原本只列 `lefthook-monorepo` 一篇，另两篇既有决策（`normalization-sentinels` / `reversible-registration-decision`）**从来没登记**；② **把收录口径从"非平凡就写"改成按判据收**（下表）。**背景实测**：该目录 2026-08-19 立，一个月里只 3 篇（全在立项当天）、**零门禁、总纲不指它**——而同期 E6 的架构决策全都写进了各层档案与 memory ⇒ **"重要但没人用"的机制就是第二份真相源的候选**，必须划清边界 |

> 🔴 **memory ↔ decisions 分工判据（要写进上面两处，本层新立）**
>
> **一句判据：这个决策，需要「仓库外的人」知道吗？**
>
> | 需要（→ `docs/decisions/`） | 不需要（→ memory） |
> |:--|:--|
> | 第三方插件作者 / 别的 AI / **换机器的人 / clone 仓库的人**能看见的都是 git | 只有我和我的 AI 用；工作法、教训、执行节奏、进度记账 |
> | 例：一插件一仓（`plugin-source-out-of-shell-repo`）· 作者文档边界（`author-docs-reader-based-boundary`）· lefthook monorepo 方案 | 例：AI 工作法正典 · 提交前自检 · 检查清单节奏 |
>
> **根本理由（实测）：memory 不进 git**（在 `C:\Users\fengy\.claude\...`，换机器就没了、别人 clone 仓库看不到），而 **`docs/decisions/` 进 git**。**⇒ L7 之后插件作者与他们的 AI 在别的仓库工作、读不到 memory，"仓库外的读者"正是这个目录存在的真正理由。**
>
> **边界三条（防它继续膨胀成第二真相源）**：
> 1. **层的执行决策仍留层档案**（如 L7 的 D1-D7 / Y1-Y9 / N1-N6 在 `00-整理档案.md`）——它们是**过程**，不进 decisions/；只有**对外契约级**的才单独成篇。
> 2. **memory 侧不镜像** decisions/ 的内容（现在 `design-decisions.md` 54 行 + 7 个决策类条目与它部分重叠 ⇒ 只互指、不复制）。
> 3. **不写"关于记录的记录"**——本判据的正文就住在这两处（本档 + `工程管理与Git策略.md`），**不再单独立一篇决策记录**（否则机制自我增殖）。
>
> **建议的可机械形态（可选，落笔时定）**：decisions/ 每篇必须提到「**插件作者/AI 可感知的面**」（如 `plugin.json` 字段、Release 资产、目录约定），否则提示"降级到 memory"。⚠️ 判据本身带人的判断成分，**先做成黄灯提示、不做硬拦**（硬判会逼出凑数记录——"非平凡"机械判不住，这也是甲案被否的理由）。
| **`docs/03-插件制造/` 全族（十五篇 + schema）** | **本档不逐篇列**——见 [10-作者文档族对账.md](10-作者文档族对账.md) 的十五篇矩阵（`E6#97g` 文档更新加成任务）。**三批依依赖拆**：A 批随本轮落笔，B 批等 `#98g`，C 批等 7.2/7.3，**最终收口在 7.7** |

> 📌 参考：VS Code 实测口径（本轮审计实测，本机 56 个已装扩展 + 97 个内置扩展）
>
> ```
> 用户安装目录名  <publisher>.<name>-<version>[-<平台>]     ← 如 ms-vscode.cpptools-1.35.0-win32-x64
>                 带版本号只为「磁盘上多版本并存」，与工程仓库名无关
> 内置扩展目录名  裸 id                                     ← 如 git/ cpp/ docker/（app bundle 内 97 个）
> 身份            publisher.name —— 发布后永不可变
> 显示名          displayName —— 随便改（如 "C/C++"）
> 版本            独立于壳（内置 git 扩展 = 10.0.0，壳 = 1.105）
> ```

---

## 六、判据（本轮过不过）

- [ ] 五处落点全部落笔，**且互不矛盾**（逐条交叉读一遍——这是本类轮次最容易翻车的地方）
- [ ] 落点一的每一条都能被 7.1–7.4 轮消费（无悬空条文）
- [ ] 落点三的措辞与 7.6 轮的实际状态一致（未做就不许写成已做）
- [ ] `npm run check` 全绿（纯文档改动，**新增/改动的内链必须在 `check-doc-links` 视野内**——链接写错当场红）
- [ ] **不 bump**（零源码改动；判据见 [.claude/skills/version-bump/SKILL.md](../../../../.claude/skills/version-bump/SKILL.md)）

---

> **← 总档案：** [00-整理档案.md](00-整理档案.md) · **下一轮 →** [02-出口打通.md](02-出口打通.md)
