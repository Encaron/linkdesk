# 7.7 全层验收——总账与封层

> **任务号：** E6#104 · **前置：** 7.0–7.6 全闭 · **产物：** 一张可核对的总账 + 本层封层结论
> **🔴 本轮的判据全部来自 [总档案 §五](00-整理档案.md)——本档只负责「怎么验」，不另立标准。**
> **非新能力声明（`check-design-flow` §8.4 ③）：** 本档是**验收判据与总账**，不设计、不引入任何新能力——文中出现的 `contributes.*` / `window.linkdesk.*` 均为**引用既有 manifest 字段与既有公开 API**（其设计前置与 8 维度论证在各自既定轮次里早已落笔），本档只是把它们**验一遍**。⇒ **无需新能力设计前置**。

---

## 〇、逐轮销账表（每轮必须给出可核实的证据，不是「已做」）

| 轮 | 任务 | 销账证据 |
|:--|:--|:--|
| 7.0 | E6#97 判据先立 | 五处落点文件路径 + 交叉复读结论（无互相矛盾）+ **`#97f` 命名规范进作者面文档** + **`#97g` 03-插件制造 十五篇逐篇结论（A/B/C 三批各自的完成状态）** |
| 7.1 | E6#98 出口打通 | 六只插件「仓外 `npm ci && npm run build` 出 zip」的**逐只实录**；`@src` 三处处置后的测试结果；JSON 插件 pack 通道方案 + 落地实证 |
| 7.2 | E6#99 逐个迁移 | 18 只插件仓地址清单；`git ls-files plugins/` 只剩夹具；归零 grep 的逐条解释 |
| 7.3 | E6#100 上架两步 | 18 只的 Release + 根 `marketplace.json`；官方目录 18 条；**更新通道实证**（bump → publish → 市场出徽标 → 更新成功） |
| 7.4 | E6#101 种子保鲜 | `sync:bundled` 三档实测；`check-bundled-freshness` 负控实证；**「装到干净机看到的插件版本 = 刚发布的新版」端到端实证** |
| 7.5 | E6#102 门禁与 CI | 18 仓 CI 首跑记录；25 个测试在插件仓内的结果与壳仓一致；三处 lint 负控 |
| 7.6 | E6#103 脚手架与工作区 | 脚手架 git init 三情形实测；容器目录 + 说明文件在位 |

---

## 一、总账（照 [总档案 §五](00-整理档案.md) 七条逐条跑）

### 1.1 结构账

```bash
# ① 壳仓 plugins/ 下只剩夹具
git ls-files plugins/ | cut -d/ -f2 | sort -u
#   期望：panel-demo / floating-panel-demo（发货插件归零）

# ② 壳侧绑定归零
grep -rn "plugins/\(editor\|file-tree\|marketplace\|settings\|python\|serial-monitor\)" \
  tsconfig.json vitest.config.ts eslint.config.js knip.json package.json scripts/*.mjs
#   期望：零命中，或每条都有解释（夹具/注释/文档指针）
```

### 1.2 独立性账（**每条都在壳仓之外验**）

```bash
# 逐只：全新临时目录
git clone <插件仓> /tmp/verify-<id> && cd /tmp/verify-<id>
npm ci && npm run build && npm run lint && npm run validate
ls *.linkdesk-plugin        # 期望：产出 zip
```
🔴 **不许在壳仓里验**——在壳仓里 `npm ci` 会把根 workspaces 的一切都装上，**那验不出独立性**（这正是本层要治的病）。

### 1.3 分发与新鲜度账

> 🔴 **2026-09-14（7.4 轮）口径订正**：出厂随包 = **账里 `seed:true` 的那些（今天 6 只基础插件）**——
> 决策 D4 原写「其余不随包、纯市场」，7.4 轮由用户亲口确认并落地。**账覆盖官方全集（18 条）**，
> 但**箱子只装 seed 集**：`lock.plugins.length` 与「箱内 zip 只数」从本轮起**是两个数**，别再混读。

```bash
npm run check:bundled-freshness                # 期望：绿灯（默认联网比对官方目录）
npm run sync:bundled -- --offline              # 期望：绿灯（只验「箱内 ≡ 账」）
node -e "const l=require('./bundled-plugins.lock.json'); console.log(l.plugins.length, l.plugins.filter(p=>p.seed).length)"
#   期望：18 条（官方全集）  6 只（随包集 = D4 基础插件）
ls bundled-plugins/*.linkdesk-plugin | wc -l   # 期望：= 上一条的「随包集」数（箱子 ≡ 账的 seed 集）
```

### 1.4 装机账（**最后一把，不许用 electron:dev 冒充**）

- [ ] `electron:build` → 装到 Windows（真 NSIS）
- [ ] 首启：**随包 6 只**基础插件全部正常（设置 / 插件市场 / 语言 / 基础主题 / 文件树 / 编辑器）
- [ ] `npm run lsp:smoke` 绿灯
- [ ] 干净 profile 启动 ⇒ 市场「**探索插件**」段默认能看到 18 只（= 官方目录里我们的条目；**别看「已安装」段**——随包只 6 只，剩下的要靠这一段装回来）

### 1.5 版本与提交账

- [ ] 壳 `package.json` 版本按 [version-bump skill](../../../../.claude/skills/version-bump/SKILL.md) 判定（本层有 `src/`/`electron/` 改动 ⇒ **必须带类别前缀并 bump**，与 7.0/7.1 的「不 bump」区分开）
- [ ] `CHANGELOG.md` 有对应 `## v<版本>` 段（`check-changelog-section` 机械兜底）
- [ ] 各插件仓的版本各自独立、各自 CHANGELOG（**不进壳 CHANGELOG**）

---

## 二、回归对照（本层最容易忽略的一条）

**`npm run check` 的覆盖会变小**——插件从壳的编译/测试图里摘出去了。**这不是回归，是本层的目的之一**（壳只考壳）。

但要**如实登记前后数字**（🔵 **L7 前基线已于 2026-09-14 L7 立案当天实测**，见下表「L7 前」列——**这就是比对的基准，别重测**）：

| 项 | L7 前（2026-09-14 实测） | L7 后 | 说明 |
|:--|:--:|:--:|:--|
| 壳仓 vitest 文件数 / 用例数 | **178 文件 / 2,433 用例**（`npm run check` EXIT=0） | **153 文件 / 2,063 用例**（7.7 轮末复测，2026-09-14；`npm run check` EXIT=0） | 下降量 = 25 个测试文件（**逐条核对去向，不许只说「少了就对」**）；7.4 轮只改了 7 例的**输入来源**（种子 → 冻结快照），**没增删测试文件** |
| `npm run check` 端到端耗时 | **53.4 s**（`7a28c463c` worktree 实测） | **46.5 s** | **变快了**（−6.9 s） |
| 其中 vitest 段 | **26.58 s** | **19.88 s** | **−6.7 s** |
| **官方插件只数**（备了案、在官方目录里、可装回来） | **18** | **18** | **必须不变**（少了 = 迁移丢件） |
| **随包种子只数**（箱子里的 zip） | 18 | 🔴 **6**（7.4 轮按 D4 收箱，2026-09-14 用户拍板） | **这一项本轮就是变小的**——不是丢件：少掉的 12 只仍在官方目录、可随时装回（见 §1.3 口径订正与 [05 §七.6](05-出厂种子保鲜.md)） |

> 🔴 **2026-09-14（7.7 轮）两处订正 + 补齐**，依据是本机留档的读数日志与一次受控复测：
> ① **「L7 前」的用例数原写 2,429，实际是 2,433**——`check.log`（05:22）/ `check3.log`（05:30）/ `check4.log`（09:04，L7 立案当天）三份读数**全是 178 / 2,433**，且 `2,433 − 2,063 = 370` 与「25 个测试文件在插件仓里跑出的 370 例」**一例不差**（见 §5.2）。
> ② **`npm run check` 耗时两栏原文都写「记录」（即待补）**，本轮补齐：在**最后一个 L7 前提交 `7a28c463c`** 上开 `git worktree` + `npm ci` 后真跑，与 L7 后同一台机、同一会话、同一负载口径。
> ③ 🔵 **别把「vitest 段 19.88 s」与 09:04 立案当天那次的 19.81 s 混读**——立案当天那一跑是**另一个负载态**（当次 20+ 个后台任务在跑）；两栏可比的是本表这两个数（26.58 → 19.88），因为它们同法同负载。

🔴 **判据：官方插件只数不变（18）+ 被收箱的 12 只都能从市场装回 + 每个消失的测试文件都能在插件仓里找到对应物。**

---

## 三、封层条件（缺一条不算过）——🔴 **2026-09-14 全部满足，L7 已封层**

- [x] [总档案 §五](00-整理档案.md) 七条判据**全绿**（逐条读数见 [§5.1](#51-七条判据逐条读数104a)）
- [x] §〇 逐轮销账表**每行都有可核实证据**（不是「已做」）
- [x] §二 回归对照数字已登记，且**测试文件去向逐条对得上**（25/25 按原路径在插件仓找到，370 例一例不差）
- [x] 没有任何一只插件处于双真相源状态
- [x] 本层所有档案的**内链无断**（`check-doc-links` 机械兜底，`npm run check` 绿）
- [x] 🔴 **作者面文档族已收口且「内容跑得通」**：`03-插件制造/` 十五篇逐篇有结论（`E6#97g`）；**并且本轮用「陌生作者的 AI」真跑了三档计时**（[§5.6](#56--作者面终局验收三档计时104c-的账③)，10 min 档 2.1 min / 30 min 档 2.3 min 达标，1 天档跑出 6 个缺篇）；`docs/03-插件制造/plugin.schema.json` 与 live 字节相等
- [x] 记忆库/CLAUDE.md/`docs/05-插件更新/00-README.md` 的指针已同笔更新（[7.0 §五](01-判据先立.md) 的落点在实操后复核）

**封层后新挂的账（本轮抓到的真发现，见 §5.5 / §5.6）：** F2 `pack` 通道多带两个死重文件 · F3 N6 第 ④ 处锚两侧都没人守 · F4 `check-theme-audit` 规则无仓外形态 · ~~F5 npm 缓存陈旧会让脚手架静默降级~~ ✅ **已销账（第 7.9 轮 E6#108，见 [§5.5b](#55b--f5-销账2026-09-14第-79-轮-e6108用户实机立案)）** · F6 `@linkdesk/ui`「已 external」与实现相反 ＋ `Badge` 的 `d.ts` 指向不存在的目录 · G1-G7 作者面缺篇七条。

---

## 四、封层后遗留（如实登记，不假装全清）

| 项 | 说明 |
|:--|:--|
| **L4 / L6 的归属** | 本层插入前 L4 已被拍板「最后一把做」、L6 无前置依赖。L7 封层后它们是 E6 仅剩的未闭层——**由用户拍板是否并进 L7 收尾，或单列**。✅ **2026-09-14 更新：L4 已收尾闭合**（keybindings 缺口用双段快捷键实测销账、`my-cool-plugin` 上架归档、12 步走查如实登记为「等价覆盖」）⇒ **E6 仅剩 L6** |
| **`plugins/` 夹具的去向** | `panel-demo` / `floating-panel-demo` 长期留在壳仓（夹具语义已写进 7.0 落点一） |
| **R1 跨仓批次** | 壳 API 变更引发「改壳 + 改 N 个插件仓」的场景**首次发生时**要复盘一次（这是 D1 的最大代价，第一次真遇到才知道疼不疼） |

---

## 五、执行记录——7.7 轮实测（2026-09-14）

> 本节是**执行与读数**，判据仍以 §〇–§四 与 [总档案 §五](00-整理档案.md) 为准。所有数字均为现场实测，原始日志在 `E:\ldk-l7-scratch\104-*.log`（可复跑）。
> **一句话：七条判据全绿、四本账对平、回归对照数字闭合；本层收官。** 抓出 **6 条真发现**（1 条已修、5 条挂账），作者面三档计时**两档远超达标、一档探针式跑出 6 个缺篇**。

### 5.1 七条判据逐条读数（`#104a`）

| # | 判据 | 读数（哪只 / 哪条路径 / 什么数字） | 结论 |
|:--|:--|:--|:--:|
| ① | 壳仓 `plugins/` 只剩夹具 | `git ls-files plugins/` → 只有 `floating-panel-demo` / `panel-demo` 两个目录、共 **16 个文件**；`git status --porcelain plugins/` 空（无未跟踪残留）；18 只发货插件源码**零命中** | ✅ |
| ② | 每只在壳仓外自足构建 | 全新临时目录 `E:\ldk-l7-scratch\verify\<id>\`（在壳仓之外）**逐只 `git clone https://github.com/Encaron/linkdesk-plugin-<id>` → `npm ci` → `npm run build` → `npm run validate`**：**18/18 全过**，每只都产出 `<id>.linkdesk-plugin`（逐只字节数与 sha256 见 `104-independence.log`；例：editor 8,652,035 B / `293c28f5…`，theme-pill 13,877 B / `8475dc2f…`）。**一次都没有在壳仓里跑过** | ✅ |
| ③ | 每只在官方目录默认可见 | `api.github.com/.../linkdesk-marketplace/contents/marketplace.json` → **HTTP 200**、`sha=975b8279a02c8c1c015ab19b85f8d3f136c27cce`、**20 条**：我们的 **18 只全在**（逐 id 对账，版本与 `bundled-plugins.lock.json` 逐条相等）＋ 既有的 `hello-linkdesk@0.1.1` / `first-run-setup@1.0.0` | ✅ |
| ④ | 出厂种子新鲜 | `npm run check:bundled-freshness` → `账 18 条（seed:true 6 只）｜箱内与账一致 6/6 ｜官方目录 20 条｜新鲜度已比对 6/6` ⇒ **✓ 绿灯**；`npm run sync:bundled -- --offline` → **✓ 箱内 6 只「版本 + 指纹与账一致」**；`lock.plugins.length = 18` / `filter(seed).length = 6`；`ls bundled-plugins/*.linkdesk-plugin \| wc -l = 6`（箱 ≡ 账的 seed 集） | ✅ |
| ⑤ | 壳侧绑定归零 | `grep -rn "plugins/\(editor\|file-tree\|marketplace\|settings\|python\|serial-monitor\)" tsconfig.json vitest.config.ts eslint.config.js knip.json package.json scripts/*.mjs` → **代码路径零命中**；剩 **9 处命中全是注释里的「已外移」说明**（`check-file-size:22/161`、`check-empty-dirs:6`、`check-css-hardcode:59-60`、`check-lsp-deps:20`、`check-lsp-smoke:34/35`）＋ **1 处是有意的候选位数组**（`check-lsp-smoke.mjs:84` 的 `CANDIDATE_BASES`，文件头写明理由） | ✅ |
| ⑥ | 装机无回归 | 见 [§5.4](#54-判据⑥-装机无回归的实测口径)，三条腿全绿（随包 6 只／LSP／市场默认可见 20 条） | ✅ |
| ⑦ | `npm run check` 全绿且比 L7 前更短 | **`EXIT=0`**：**153 文件 / 2,063 例**全过、`plugin-docs 产物与真源一致（47 个文件）`。端到端 **46.5 s**（L7 前同机同法实测 **53.4 s**）；vitest 段 **19.88 s**（L7 前 **26.58 s**）⇒ **变快**，见 [§二](#二回归对照本层最容易忽略的一条) 订正表 | ✅ |

### 5.2 `#104b` 回归对照——订正 + 闭合

🔴 **两处基线订正（以本机留档的读数日志为准）：**

1. **用例数 2,429 → 2,433。** §二表里「L7 前」写的 2,429 找不到出处；本机留存的三份 L7 前读数（`check.log` 05:22、`check3.log` 05:30、`check4.log` 09:04——最后一份就在 L7 立案当天）**三份全是 178 文件 / 2,433 例**。⇒ 以 **2,433** 为准。
2. **`npm run check` 耗时两栏**（原文两格都写「记录」）**本轮补齐**：用 `git worktree` 在 **最后一个 L7 前提交 `7a28c463c`**（`plugins/` 尚在壳内的最后一态）上 `npm ci` 后真跑，同一台机器、同一会话、同一负载口径：

| 项 | L7 前（`7a28c463c` worktree 实测） | L7 后（主线 `electron` 实测） | 变化 |
|:--|:--:|:--:|:--|
| 壳仓 vitest 文件数 / 用例数 | **178 / 2,433** | **153 / 2,063** | −25 文件 / **−370 例** |
| `npm run check` 端到端 | **53.4 s** | **46.5 s** | **−6.9 s（≈ −13%）** |
| 其中 vitest 段 | **26.58 s** | **19.88 s** | **−6.7 s（≈ −25%）** |
| **官方插件只数**（备了案、在官方目录、可装回） | **18** | **18** | **不变** ✅ |
| **随包种子只数**（箱内 zip） | 18 | **6** | 按 D4 收箱，见 [§1.3 口径订正](#13-分发与新鲜度账) |

🔴 **25 个消失的测试文件——逐个去向（不是「少了就对」）：**

- `git ls-tree -r 7a28c463c plugins/ \| grep .test.tsx?` → **正好 25 个**（editor 2 / file-tree 6 / marketplace 10 / serial-monitor 1 / settings 6）。
- **25/25 全部能在对应插件仓里按原相对路径找到**（`verify/<id>/<原路径去掉 plugins/<id>/ 前缀>`，逐条 ✅，清单见 `104-pre-tests.txt`）。
- **370 例这个数字也能对上**：在 5 个插件仓里真跑 `vitest run` ⇒ editor 2/28 · file-tree 6/69 · marketplace 10/232 · serial-monitor 1/10 · settings 6/31 ⇒ **合计 25 文件 / 370 例，全绿**。而 2,433 − 2,063 = **370** ⇒ **一例不差地搬走了**。
- file-tree 的 69/69 与 7.1、7.5 两轮记录**逐字相同**。

### 5.3 `#104e` 两类挂账复核

**① `scripts/doc-links-exclude.txt` 里 E6#99 那 9 条单档排除——逐条复核结论：9 条全部仍该排除，一行不删。**

复核判据 = 该档案**是否仍在 `docs/` 里**（回收条件写在清单注释里：「若被正式归档/移出 `docs/` ⇒ 同笔删除」）＋ **是否仍含失效链接**。实测（`104-exclude-recheck.log`）：

| 档案 | 在位 | 仍指向 `plugins/<id>/…` 的链接数 |
|:--|:--:|:--:|
| `E3.5-软件生态美化/串口监视器UI整改/01-全量审计.md` | ✅ | 11 |
| `03-插件市场/08-信任与安全.md` | ✅ | 1 |
| `03-插件市场/13-详情页HTML对账任务档案.md` | ✅ | 27 |
| `03-插件市场/14-市场图标系统任务档案.md` | ✅ | 6 |
| `03-插件市场/16-市场区健康审视任务档案.md` | ✅ | 14 |
| `03-插件市场/18-通知系统全账与设计定案.md` | ✅ | 43 |
| `插件规范化层/00-整理档案.md` | ✅ | 4 |
| `插件规范化层/02-元数据链路归一.md` | ✅ | 9 |
| `插件规范化层/03-官方插件补齐.md` | ✅ | 5 |

⇒ **9 份都还在 `docs/` 下、都还挂着失效链接** ⇒ 债随树走，**保留全部 9 行**（另外 9 行整树排除同理保留）。

**② 六处门禁「无对象 / 覆盖域变更」结论行——逐个给去向：**

| 门禁 | 现在扫到什么 | 去向结论 |
|:--|:--|:--|
| `check-theme-audit` | `themeFiles = 0`（10 只主题 + 1 只图标集插件全外移）⇒ 打 ⚠ 行 | 🔴 **没有被接走（挂账）**——`toggle-knob-on ≠ accent` 全仓只有壳内这一份实现，SDK lint preset 与 18 仓 `ci-verify.mjs` **都没有这条规则**（实测 0 命中）。⇒ 要么补进插件仓 CI 模板，要么正式认下这个真空。**本轮不补**（改 18 仓 CI 模板要重跑 `sync:plugin-ci`，属 7.5 域） |
| `check-lsp-deps` | 第一源（`plugins/*/plugin.json` 的 `langDefs.lsp`）**0 条**；第二源（`electron/` 的 spawn 字面量）**仍有对象** | ✅ **长期如此**：壳侧那行保留（第二源仍有效，不是真空）；第一源的去向 = 各插件仓自己的 CI，手动核对走 `npm run lsp:smoke -- --base <python 插件仓路径>`（本轮真跑过） |
| `check-plugin-version-sync` | 仓内只剩 2 只夹具 ⇒ 打 ⚠「覆盖域变更」行 | 🔴 **结论行说错了（挂账）**——它写「18 只的第 ④ 处锚由各插件仓自己的 CI 守」，但**负控实证不成立**：把 `settings` 仓的 `package.json.version` 改成 `9.9.9`（`plugin.json` 仍 `1.0.7`）后，`npm run verify` / `npm run validate` / `npm run lint` **三条全绿**（`ci-verify.mjs` 四段里没有版本同源这一段）。⇒ **N6 第 ④ 处锚目前在两侧都是裸的**，该行的话要么改口、要么补一段检查 |
| `pack-bundled-plugins` | **脚本已于 7.4 退休删除**（`scripts/pack-bundled-plugins.mjs` 不存在） | ✅ **已被接走**：目录整树打包能力收进 SDK `pack`（12 只纯 JSON 插件的 `build` 就是它），本轮 18/18 仓外构建实证 |
| `check-file-size` | 保留 `true`，仍扫两只夹具 | ✅ **长期如此**——**不是真空**（夹具仍在扫），夹具语义已写进 7.0 落点一 |
| `check-font-scale-audit` | 该段保留，对象 = `panel-demo` / `floating-panel-demo` | ✅ **长期如此**——同上，非真空 |

### 5.4 判据⑥ 装机无回归的实测口径（`#104a⑥`）

**三条腿全绿，且读数落到具体数字：**

1. **随包 6 只真装上了。** 用安装版 `D:\01link\LinkDesk\LinkDesk.exe` 带**一次性干净 profile**（`--user-data-dir="E:\ldk-l7-scratch\profile-fresh"`）启动，首启后 `profile-fresh/plugins/` 下**正好 6 个目录**，逐只 `plugin.json` 版本与账**逐条相等**：`editor@1.0.10` / `file-tree@1.0.7` / `lang-defaults@1.0.17` / `marketplace@1.0.29` / `settings@1.0.7` / `theme-defaults@1.0.2`（6/6）。
2. **LSP 全链路绿。** `npm run lsp:smoke` → `pyright 脚本: E:\linkdesk\node_modules\pyright\dist\pyright-langserver.js` · `initialize 握手 ✓` · `textDocument/definition → 1 个位置，落在 smoke_target.py line 0 ✓` · `✓ .py 跳转全链路通过`。
3. **市场「探索插件」段默认可见 20 条。** 同一干净 profile 里用 CDP（`--remote-debugging-port=9222`）点开插件市场并展开「探索插件」，把渲染出的文本与官方目录逐条比对：**官方目录 20 条 → 市场真拉到 20 条**（含我们的 18 只，逐条命中描述片段）。⚠️ **别看「已安装」段**——那里只有 2 只（`官方语言` / `官方主题`），另外 4 只 seed 是 `core:true`，显示在「内置 4」段；这是设计使然，不是丢件。
4. **箱内件与装机件逐字节相同**：安装版 `resources/bundled-plugins/` 下 6 个 zip 与壳仓 `bundled-plugins/` 的**sha256 逐条相等**。

**没验的部分（如实登记，不假装）：**
- **串口收发**未验——需要真实串口设备，本轮无。
- **NSIS 安装全程**未重跑——`electron:build` 的 NSIS 升级路径会**先静默卸载同 appId 旧版**（7.4 实测删过用户的安装目录），而软件版本 **0.1.63 自 7.4 起未变**、7.7 又零 `src/`/`electron/` 改动 ⇒ 装的是同一批字节，7.3/7.4 的两阶装机验收仍然有效。要真跑须先问用户或先备份。

### 5.5 🐛 本轮抓到的真发现（`#104` 的副产物，1 修 5 挂）

| # | 发现 | 证据 | 处置 |
|:--|:--|:--|:--|
| **F1** | 🔴 **`npm run check` 进场就是红的**：`@linkdesk/plugin-docs` 的**产物与真源文件名大小写不一致**——真源 `docs/03-plugin-authoring/00-readme.md`（小写）而产物 `packages/plugin-docs/docs/00-README.md`（大写） | `[plugin-docs] 产物与真源不一致（2 处）：缺文件 00-readme.md / 多文件 00-README.md` | ✅ **已修**：根因是 **Windows 的大小写不敏感 + git `core.ignorecase=true`**——生成器 `writeAll` 先 `rmSync` 再写，磁盘上已经是小写，但**索引里留着当初 add 时的大写名**，`git add` 不更新纯大小写的改名 ⇒ 提交进去的是大写、磁盘是小写，**commit 自身不自洽**。修法 = `git rm -r --cached` 后重新 `git add` 让索引记下小写。**同笔做了全仓审计**（`104-case-audit.mjs`：遍历 1,679 个跟踪文件比对索引名与磁盘真名）⇒ **problems = 0**，全仓只此一处。⚠️ **落点说明**：这笔改名（`R100`，零内容变化）**被 17:15 另一会话的提交 `beb0563cb` 一并带走**（纯并行会话共享 index 的老坑——本项目已记过这教训：提交前必须核对 `git status --short` 的**暂存区**清单，不能只看工作区）。**内容上一字不差，但记录归属要说清** |
| **F2** | ⚠️ **`pack` 通道会把「发布元数据 + CI 脚本」打进出厂件**：12 只纯 JSON 插件仓内重建出的 zip，比**已发布的 Release asset** 多两个文件——`marketplace.json`（`publish` 写进仓的目录条目）与 `scripts/ci-verify.mjs`（7.5 的 CI 脚本） | `104-18-fingerprint.log`：**6 只 `build` 通道（React 型）与账逐字节一致；12 只 `pack` 通道（JSON 型）全部多出这两个文件**。逐仓下载 GitHub Release asset 复核：`lang-defaults@1.0.17` 的 asset 只有 8 个条目、确实没有这两个（`marketplace.json` 是 10:36 那笔 publish 才写进去的，`ci-verify.mjs` 是 13:09 的 7.5 才加的，都晚于该版发布） | 🟡 **挂账**（改名 `pack` 的排除表 = 改 SDK 要发版；或 12 仓各自 bump 重发 = 外发动作）。**危害 = 出厂件里多两枚死重**（不是装不上），但**出厂件从此不可能由仓内重建复现**，R2 那条「可审查的指纹账」因此打了折 |
| **F3** | ⚠️ **N6 第 ④ 处锚（`package.json.version === plugin.json.version`）两侧都没人守** | 负控实测见 [§5.3 ②](#53-104e-两类挂账复核) | 🟡 **挂账**：`check-plugin-version-sync` 的结论行要改口（壳侧确实只剩夹具），或给插件仓 `ci-verify.mjs` 补第 ⑤ 段 |
| **F4** | ⚠️ **`check-theme-audit` 的规则没有仓外形态** | 全仓 grep `toggle-knob-on` 只命中 `scripts/check-theme-audit.mjs` 自己 | 🟡 **挂账**，同上 |
| **F5** | ⚠️ **npm 缓存陈旧时 `npm create linkdesk-plugin` 会静默生成旧模板** | 首次探针拿到的是 **7 文件的老模板**（无 `.git`、无 `AGENTS.md`/`CHANGELOG`/`README`/CI、`package.json` 只有 `dev`/`build`/`validate`），CLI 提示文案也是旧的；`npm exec --loglevel=http` 显示它**确实解析到 0.1.4**（且 registry 两侧 `latest` 都是 0.1.4）；换成干净缓存（`--cache=<空目录>`）后 ⇒ **16 文件新模板 + `✔ 已建 git 仓（main 分支 + 一次初始提交）`**。发布件本身没问题（tarball 逐个复核过：16 个模板文件全在） | ✅ **已销账（2026-09-14，第 7.9 轮 E6#108）**——用户实机复现同一症状后立案（见下方「F5 销账」块）。修法 = **文档加版本锚**（五轴 README ＋ 作者面两棵树一律改 `@latest`）＋ **补维护者 runbook**（`docs/06-发布管理/作者轴npm发版.md` 第五节专门讲这条缓存暗礁）＋ **订正两棵树里那句与实测相反的话**（"`npm create linkdesk-plugin` 默认取最新版"——假）＋ 顺手补上脚手架 README 缺的两个模板文件行 |
| **F6** | ⚠️ **作者面文档有一处「与实现相反」**：`19-组件速查`（两棵树）写 `@linkdesk/ui`「**不进**你的 `.linkdesk-plugin` bundle（构建时 external）——不用担心体积」 | **实测不成立**：`packages/plugin-sdk/src/vite-config.ts:57` 的 `DEFAULT_EXTERNAL` 只有 `react`/`react-dom`/`react/jsx-runtime`/`react-i18next`/`i18next`——**没有 `@linkdesk/ui`**。探针实测：一个两视图插件每个表面各内联 525 KB JS + 18.8 KB CSS，出厂件 **269.4 KB**（对照组：不带 UI 的 10 分钟档插件 5.3 KB） | 🟡 **挂账**（要么把 `@linkdesk/ui` 加进 external，要么改文档口径——**改的是两棵树，且第二条要动 SDK**）。同类另有一处：`packages/linkdesk-ui/dist/index.d.ts:2` 导出的 `Badge` 指向 `./badge/Badge`，**该目录在源码与已发布包里都不存在**（`src/` 下只有 `index.ts`；靠 `skipLibCheck` + inlined JS 兜住） |

### 5.5b ✅ F5 销账（2026-09-14，第 7.9 轮 E6#108——用户实机立案）

> **立案原话**：用户在 `E:\BaiduNetdiskDownload` 跑 `npm create linkdesk-plugin my-cool-plugin`，产出**与 npm 介绍页那张文件树不一样**；并追问「网上 npm 更新了，我本地再跑**为什么出来的是旧版的**？」

**① 病灶定性（本轮实测三组对照，本机 2026-09-14）**

| 组 | 命令 | 产出 | 说明 |
|:--:|:--|:--|:--|
| A | `npm create linkdesk-plugin my-cool-plugin` | **7 文件**，无 `.git`/`AGENTS.md`/`README`/`CHANGELOG`/CI | 命中 `_npx` 缓存里那份 **0.1.0** |
| B | `npm create linkdesk-plugin@latest my-cool-plugin` | **16 文件** ＋ `✔ 已建 git 仓（main 分支 + 一次初始提交）` | 带版本锚 ⇒ 强制问货架 |
| C | 删掉那条 0.1.0 缓存后再跑 **裸命令** | **16 文件** | 病根在缓存，不在发布件 |

缓存现场：`<npm cache>/_npx` 下三份 `create-linkdesk-plugin`——**0.1.0**（7 文件老模板）＋ 两份 **0.1.4**；而货架 `latest` 当时已是 **0.1.5**。
⇒ **机制**：npx 按「不带版本号的规格」算缓存键，**命中即不再问货架**；而 CLI **不打印自身版本号**，所以症状只有"生成的工程少文件"，**完全静默**。

**② 第二个原因（当时没人注意到）**：脚手架 README 那张树**本身也是旧的**——只列 **14** 项，实测模板 **16** 件，**缺 `vitest.config.ts` / `vitest.setup.ts`**。所以哪怕拿到了新版，"照着树核对"也照样对不上。

**③ 修法（四件，本轮全部落地并真发）**

1. 五轴 README ＋ 作者面两棵树（英/中）**命令一律加 `@latest` 版本锚**，并写明"不带锚会被缓存静默降级、已中招用 `npm cache clean --force`"；
2. **订正两棵树里那句与实测相反的话**——原写「`npm create linkdesk-plugin` 默认取最新版」（**假**，本轮证伪），改成「CLI 没有 `--version`，它说不出自己哪一版，这正是命令必须带 `@latest` 的原因」；
3. 脚手架 README 文件树**补到 16 项**；
4. 新建维护者 runbook **`docs/06-发布管理/作者轴npm发版.md`**，第五节专章讲这条缓存暗礁（含 A/B/C 三组对照与两条救法）。

**④ 顺带销的账 / 新抓的坑**

- 脚手架 README 与 `13` 两棵树的「**15 个模板文件**」→ **16**（该数是 `check-scaffold` 的生成物契约，7.5 已从 15 改到 16，文档没跟上）。
- 🔴 **新抓一个真坑（已写进 runbook §三 ②）**：`npm publish contracts` 的裸名字**被当成"包规格"**，npm 去货架解析了**别人**的 `contracts@0.4.0`（日志 `filename: contracts-0.4.0.tgz`、12 文件），**只因重复版本护栏才没发出去**。正确形状 = `cd contracts && npm publish`。⚠️ 7.8 那份档案 §11.8.4 坑② 写的是 `npm publish <folder>`——**该写法在本机实测会走偏**，已订正。
- ⚠️ **一处顺序错（如实记）**：`contracts` 先发 0.1.16、**发完才改 README** ⇒ 货架那份带着错指示 ⇒ bump **0.1.17 重发**订正。**教训：README 属于"表面"，改完再发。**

**⑤ 读数**：五包全发（终态：contracts **0.1.17** / plugin-sdk **0.1.18** / create-linkdesk-plugin **0.1.8** / ui **0.1.6** / plugin-docs **0.1.2**）＋ `release:mark` 记五条基线、`check:npm-release` 黄灯灭；**货架保真**（`npm pack` 解包核 16 件模板 + README `@latest` ×3；**从真货架再真跑一次生成** ⇒ 16 文件 + `main` 分支 git 仓；0.1.8 再核一次 ⇒ **生成物零内部任务号**）；`npm run check` **EXIT=0（153 文件 / 2,070 例）**。软件版本**不 bump（恒 0.1.64）**——本轮零 `src/`/`electron/` 改动。

**⑥ 两条自踩的顺序错（如实记，已固化成 runbook 的坑④）**：① `contracts` **发完才改 README** ⇒ 重发一位（0.1.16→0.1.17）；② `create-linkdesk-plugin` / `plugin-sdk` **发完后**被 pre-commit 的 `blank-at-eof` 钩子抓到尾空行、trim 后**又漂** ⇒ 各重发一位（0.1.6→0.1.7 / 0.1.17→0.1.18）。**净结果五包共发 7 次**。⇒ **规矩：`git add` 之后、`npm publish` 之前先真跑一次 pre-commit，钩子先绿再发。**

**⑦ 同轮追加：脚手架生成物清内部坐标 ＋ 立断言 10（用户点头「包括版本上新」）**：**实测 15 处任务号 + 6 处同类内部坐标**（`index.js` / `ci.yml` / `ci-verify.mjs` / `vitest.config.ts` / `vitest.setup.ts` / **`src/index.tsx`**——最后那个是作者打开的第一个文件，原来指向的还是**中文维护者树**）⇒ 共 21 处改净。🔴 **机械兜底上了**：`check-scaffold.mjs` **断言 10「生成物零内部任务号」**，尺子与 `check-author-docs-symbols.mjs` **同一份**（为此抽出 `scripts/lib/author-symbols.mjs`）；负控两条（`--self-test` 增一例 + **真变异实测：塞 `E6#102` ⇒ 红、还原 ⇒ 绿**）。`create-linkdesk-plugin` 0.1.7→**0.1.8** 已真发并核（**线上 tarball 残留任务号 0 · 从货架真跑生成的工程任务号 0**）。

### 5.5c 🔍 搬迁后插件仓体检（2026-09-14 用户立案：「看看已经搬迁的那些插件还缺少什么」）

> **立案原话**：「已经搬迁成功的几个插件……未来我可能打算单独在他们自己的这个仓库里面使用 AI 来完成一些任务。那么这个时候就需要让 AI 拥有一些快速的了解这个工程是什么、这个插件是什么。脚手架里面咱们内置了 AGENTS.md，但已经搬迁的那几个好像没有 ⇒ 应该给它们每个都来上一个 AGENTS.md，向已有的脚手架靠齐。**然后你看看咱们已经搬迁的那些插件，它还缺少什么？**」

#### 一、已做：AGENTS.md ×18 ＋ `.vscode/settings.json` ×18（E6#108g）

**做法不是手抄 18 份**——18 份手抄 = 18 份会各自漂移的副本，而且「脚手架改了、18 只没跟」这类漂移**没有任何门禁能发现**（插件仓不在壳仓 `npm run check` 的扫描域里）。所以与 `sync-plugin-ci.mjs` 同一套思路：**共享骨架只写一份 ＋ 每仓一段「事实」**，并带 `--check` 当漂移门禁。

| 件 | 落点 | 说明 |
|:--|:--|:--|
| `scripts/sync-plugin-agents.mjs` | 壳仓 | 生成 + 校验；`--dry-run` / `--check`；`npm run sync:plugin-agents` |
| 自动派生（**绝不手抄第二份**） | — | `pluginId`/`name`/`version`/`description` ← 现场读该仓 `plugin.json`；**是否随包（seed）** ← 壳仓 `bundled-plugins.lock.json`；**有哪些命令** ← 现场读该仓 `package.json`；有无 `src/`、`i18n/` ← 现场看目录 |
| 人写的那一半 | 脚本里的 `FACTS` 表 | 每仓四条：① 这是什么 ② 用户在哪看到它 ③ 关键路径住哪 ④ 动它之前必须知道的 |
| 生成物 | 18×`AGENTS.md`（72–79 行）＋ 18×`.vscode/settings.json`（与模板逐字节同源） | 幂等：连跑两次第二次报「全部与生成源一致」 |

🔴 **生成器自己也被自己的尺子量**：生成文本进 `--check` 前先扫**内部坐标**——① 任务号（复用 `scripts/lib/author-symbols.mjs`，与壳仓作者面同一把尺子）；② **层/轮名**（`L7` / `E6` / `第 7.9 轮` 这类）。**这条是现场抓出来的**：初版正文写了「源码在 **L7** 那一轮从壳仓整体外移」——正是本轮刚立的规矩要禁的东西，改成人话「源码后来从壳仓整体外移」。
🔴 **语言选择**：官方插件仓的 AGENTS.md 用**中文**（这些仓的 README / CHANGELOG 本来就是中文，读者是维护者本人与他的 AI）；脚手架模板那份保持英文（读者是第三方作者）。**结构同形，只有语言不同。**

#### 二、体检结果：18 只仓还缺什么

| # | 缺什么 | 实际读数 | 严重度 / 处置去向 |
|:--|:--|:--|:--|
| **A1** | ✅ **AGENTS.md**（用户点名） | **0/18 → 18/18**（本轮） | **已做**（见 §一） |
| **A2** | 🔴 **许可证 —— 17/18 没有** | 只有 `theme-iconset-pastel` 有 `LICENSE.md`；**18/18 的 `plugin.json` 都没有 `license` 字段**（schema 里也**没有**这个字段）。壳仓自己 2026-09-13 已 MIT 定案（LICENSE 正本 ＋ `license` 字段） | 🟡 **待用户拍板**：18 只**公开**仓默认是「保留所有权利」，与壳的 MIT 姿态不一致（别人不能合法复用示例）。补 LICENSE 是零风险文件动作；**想在 `plugin.json` 里声明则要动 schema ⇒ 要发 SDK**。另：`theme-iconset-pastel` 用的是 `LICENSE.md`，命名不统一 |
| **A3** | ⚠️ **`.vscode/settings.json` 0/18** | 模板有（让编辑器按 JSONC 解析 `plugin.json`，否则注释满屏红） | ✅ **本轮已补**（与模板逐字节同源） |
| **A4** | ⚠️ **主题配方摊在仓根，与作者文档相反** | 作者面两棵树都写 🔴「配方**只许住 `themes/`**，摊到仓根是禁止项」；实测 **`theme-aurora-glass`（`aurora-glass.json`）** 与 **`theme-zones`（`paper-zones.json` / `image-zones.json`）** 摊在仓根。另：`lang-defaults`（`zh.json`/`en.json`）与 `lang-test-ja`（`ja.json`）的词典也在仓根（词典这条文档只说 `i18n/` 是家、没写「禁止」） | 🟡 **真·文档与样例打架**：官方插件是作者抄的第一份样本 ⇒ 抄出来就学反了。**机制层无门禁**（`ci-verify` 只查「声明的文件在不在」）。修 = 挪目录 ＋ 改 `path` ＋ **发版**（外发动作，等点头）；或反向把文档口径放宽（不推荐——文档的理由「主题会长深，位子先留好」成立） |
| **A5** | 🔴 **`theme-defaults` 的 `core` 旗标缺失** | README 第 12 行写「出厂自带（`core: true`）——**不显示卸载按钮**」，`description` 也写「内置工厂插件，不可卸载」；但 **`plugin.json` 里没有 `core` 字段** ⇒ 按当前清单，**卸载按钮是显示的**（且它是 `seed: true` 的出厂种子） | 🟡 **待拍板**：要么补 `core: true`、要么改 README（**两者都要发版**）。这是「README 与实现相反」那一类，只是方向相反（文档承诺了、实现没做） |
| **A6** | ⚠️ `theme-mint-soda` 的 `plugin.json` **没有 `$schema`** | 18 只里唯一一只（直接以 `{` 开头） | 🟢 小账：补一行就有字段补全。顺带一提它 `type` vs `appearance` 的老形态也在 AGENTS.md 里记了 |
| **A7** | ⚠️ `theme-iconset-pastel` 的 README 指着一个**不在本仓**的脚本 | README「来源与许可」写 `scripts/convert-material-icons.mjs`；本仓 `scripts/` 里只有 `ci-verify.mjs`（那个转换器在**壳仓** `scripts/`） | 🟢 小账：指路写错，改文案即可（要发版） |
| **A8** | ⚠️ 7/18 的 `plugin.json` **没有 `icon`**、6/18 **没有 `resources/`** | 无 icon：`lang-test-ja` / `theme-defaults` / `theme-iconset-pastel` / `theme-panorama` / `theme-pill` / `theme-songti` / `theme-twilight-forest` | 🟢 **不算缺陷**（`icon` 可缺省，市场侧走默认块）——但作为**官方样例**，少一张图就少一分「长得像官方插件」的说服力。登记为可选收尾 |
| **A9** | ✅ 不成立的「缺」：测试只有 5/18 | 25 文件 / 370 例，分布在 editor / file-tree / marketplace / serial-monitor / settings | **不是缺**：`sync-plugin-ci` 本来就**按仓分档**（有测试才铺 vitest 两件）。纯数据插件没有 TS 工程，不该有测试基建 |
| **A10** | ✅ 不成立的「缺」：`tsconfig.json` 只有 6/18 | 同上 | **不是缺**：主题 / 语言包是纯数据插件 |
| **A11** | ⚠️ `marketplace` 仓里提交的 `marketplace.json` 自我条目停在 **1.0.29**，而插件是 **1.0.30** | 17/18 的自我条目与 `plugin.json` 同值，只此一只落后 | 🟢 **低危、会自愈**：**线上官方目录读数是 1.0.30**（实测 `Encaron/linkdesk-marketplace` 的 `marketplace.json`），因为目录合并取**版本高者**、低版本推不回去 ⇒ 用户侧无影响；下一次 publish 会自然写正 |
| **A12** | ⚠️ **本轮自身引入的副作用**：`AGENTS.md` 会被 `pack` 通道打进 12 只纯数据插件的出厂件 | `isPackableRelPath` 只排除隐藏项（`.vscode/` **被排除** ✓）＋ `node_modules`/`dist`/`package*.json`；**仓储文件 `AGENTS.md` / `marketplace.json` / `ci-verify.mjs` 都会进包**（后者两个正是 F2 的账） | 🟡 **F2 的账又多一个文件**。**一行修法**：把 `AGENTS.md` 加进 SDK 的 `EXCLUDED_FILES` —— 但那要**发一次 `@linkdesk/plugin-sdk`**。功能无影响（包里多 2 KB 文本），影响的是「出厂件可由仓内重建」那条 R2 判据 |

#### 三、登记的账（本轮新增，别丢）

| 账 | 内容 | 修它要动什么 |
|:--|:--|:--|
| **G1** | 18 只官方插件仓补 **LICENSE**（＋ 若要在清单里声明 `license`，先给 schema 加字段） | LICENSE = 纯文件；`license` 字段 = **发 SDK** |
| **G2** | `theme-aurora-glass` / `theme-zones` 的配方挪进 `themes/` | 两仓各自 **bump + 发版**（外发） |
| **G3** | `theme-defaults` 的 `core` 旗标（README 与清单对不上） | 该仓 **bump + 发版**（外发） |
| **G4** | SDK `pack` 的排除表补 `AGENTS.md`（替 F2 收口） | **发一次 SDK** |
| **G5** | `theme-mint-soda` 补 `$schema`；`theme-iconset-pastel` 订正 README 的脚本路径 | 各仓 bump + 发版（外发，可与别的改动合并成一笔） |

### 5.6 🔴 作者面终局验收——三档计时（`#104c` 的账③）




**方法（这是本项可复现的前提）**：模拟「**陌生作者的 AI**」——工作根在**壳仓之外**（`E:\ldk-l7-scratch\authoring\`），**只给两样东西**：① `npm create linkdesk-plugin` 的脚手架产物；② `@linkdesk/plugin-docs@0.1.0` 解包后的文档树（`docs/00-readme.md` 起）。**明令禁止读 `E:\linkdesk` 或任何 LinkDesk 源码检出**，**不许问任何人**。计时按 `date +%H:%M:%S` 真采，**找文档的时间算在里面**。

| 档 | 目标 | 实测 | 预算 | 结论 |
|:--:|:--|:--|:--:|:--|
| ⏱ **10 分钟** | 最小可跑插件 | 干净缓存下 **2 分 07 秒**走完 scaffold→install→build→validate→dev（dev 宿主 `http://localhost:1421/?pluginId=…` `HTTP 200`、HMR 实测触发、事后端口释放）：`build` 出 `my-cool-plugin.linkdesk-plugin` **5,431 B**、`validate` `✅ plugin.json 验证通过`、脚手架**真建 git 仓**（`✔ 已建 git 仓（main 分支 + 一次初始提交）`，`git log` = 1 笔） | 10 min | ✅ **达标，余量 7.9 min**（最大单项成本 = `npm ci` 37 s） |
| ⏱ **30 分钟** | 视图插件（进区域 + 声明贡献点 + 用共享组件） | **2 分 16 秒**到五个必需里程碑（含自加 lint/渲染测试则 3 分 01 秒）：`plugin.json` 真声明 `viewsContainers."<id>-sidebar"`（sidebar / order 50）+ 两个 view（一个带 `titleActions` 绑命令）+ 3 条命令 + `appearsIn.iconBar`；代码真用 `@linkdesk/ui` 的 `Button`/`Toggle`/`Badge`/`FormRow`；`build` 出 **275,903 B**（2/2 表面）、`validate` ✅、`lint` 零偏离；**跨区域数据流按文档的 `events.emit`/`on` + `pluginState` 配方写通** | 30 min | ✅ **达标，余量 27.7 min** |
| ⏱ **1 天** | 高难度插件（数据管道 / 跨插件通信 / Canvas 或 LSP 级） | **探针式**（明确告知「预算是 25 分钟、到点就写报告、不许凑满」）：**4 分钟**内把 6 个触点全部写出来并通过全部机械门（`validate` / `verify` 四段 / `tsc --noEmit` 零错 / 7 例测试 / 出厂件 9,735 B）；**但只有 4/6 是行为验证过的**——Canvas **一个像素都没真画过**（jsdom 无 2D context，测试把 `getContext` 打了桩）、`emit` 路径没真执行过；**0/6 在真壳里验过** | 1 天 | ⚠️ **「造得出来」成立、「跑得通」不成立**：卡点全在**一处没写下来的握手**（见下） |

**缺篇清单（卡点即缺篇——喂回 [11 §11.3](11-作者面文档收口.md)，本轮不补，作为 L7 之后的作者面增量）：**

| 优先 | 缺什么 | 证据（陌生 AI 卡在哪） |
|:--:|:--|:--|
| 🔴 1 | **「双击一个 .csv，插件到底怎么拿到那个路径」** —— 缺一篇《文件类型插件：从双击到渲染》。要回答：谁调谁、`contributes.fileAssociations[].command` 收到的参数形状、插件是否要自己 `tabs.openOrFocus(pluginId,{sourceId})`、`fileAssociation.getPluginFor` 干什么用、不注册命令的插件怎么拿到路径。**当前文档留白 ⇒ 作者只能靠防御性地兼容六种参数形状来赌** | `03-contributes-spec §3.13` 只说「command 是打开该扩展名文件时执行的命令 ID」；`06-plugin-json-spec:116` 只说「双击会自动打开」；**没有任何一篇说参数是什么** |
| 🔴 2 | **「工作区文件怎么读写」** —— `filesystem.readTextFile / writeTextFile / readBinaryFile / listDir / watch` 的方法级用法（今天只存在于生成的 `linkdesk.d.ts` 里，**docs 从没出现过 `readTextFile` 这个词**；`01` §5 的例子只有 `listDir`） | 任务的核心触点，文档面零命中 |
| 🔴 3 | **预览宿主的能/不能对照表** —— `npm run dev` 的 mock 里 `filesystem.*` 恒返回空串；一个工作区插件在预览里**看起来就是坏的**，而没有任何一篇说该走 `dev:real`。侧栏型插件更直接：dev 宿主**拒收无 `entry` 的插件**，报错指向的 `02-本地预览环境.md` **不在文档包里** | 两个不同探针各自撞上同一面墙（30 分钟档卡 25 s、1 天档完全无法自验） |
| 🟡 4 | **「哪条命令会做类型检查」** —— `build` 是 esbuild/vite **不做 tsc**、`lint` 只有 eslint、`validate` 只验 schema；文档 `13` 第 4 步承诺「拼错方法名 `tsc` 当场红」，但**文档给的五条命令没有一条会跑它**（探针真写出过 tsc 不过、build 却绿的代码） | 需手动 `npx tsc --noEmit` 才发现 |
| 🟡 5 | **多视图插件的数据该住哪** —— 两个视图要共享数据时模块级 store 是正解（模块作用域跨视图共享），`18` 的配方只演示各自 `useState` + 事件重同步（重挂载即丢数据）；且没有一篇说这种做法在 keep-alive 下安全 | 1 天档探针自行推导出 `pluginState` + `emit` + 模块单例 |
| 🟡 6 | **组件 props 表** —— `19-组件速查` 刻意不抄 props 表（改为指包），这个决定本身合理，但**包里的 `d.ts` 对 `Badge` 是坏的**（见 F6），于是「`Button` 有哪些 props」这类问题会落到一份残缺文件上 | 见 F6 |
| 🟡 7 | **`enumDescriptions` 的例子是错的** —— `20-我的插件加一条配置项`（两棵树）示范成数组 `["Millimeters","Centimeters"]`，`npm run validate` 当场红：`字段类型应为 object`；schema `plugin.schema.json:350` 要的是 `{值: 显示名}` 映射 | 一次 validate 往返（约 10 s），但这是**教反了**，属「顺手订正」那一类 |

**同批的定性结论（正面，也要写下来）：** 三档的**速度**全部远超达标线，且「进哪块区域、怎么声明、什么时候用什么机制」这部分文档是**一次就读对**的——区域地图 / 视图容器 API / 区域间互动 / 组件速查四篇在第一遍就给出了正确答案（`PoolSectionStack` 自动包一层、`appearsIn.iconBar` 才进图标栏、事件通道带插件 id 前缀等细节都对）。**卡点集中在「文件/预览」这一面，不在「区域/机制」那一面。**

### 5.7 封层结论

- ✅ [总档案 §五](00-整理档案.md) 七条判据**全绿**（读数见 §5.1）
- ✅ §〇 逐轮销账表**每行都有可核实证据**（各轮档案的「执行记录」节）
- ✅ §二 回归对照数字已登记且订正，**25 个测试文件去向逐条对得上**（25/25 按路径找到、370 例一例不差）
- ✅ **没有任何一只插件处于双真相源状态**：18 只源码各自只在插件仓；壳仓 `plugins/` 只剩两只夹具；箱内是发布件的快照（有账、有指纹）
- ✅ 本层档案**内链无断**（`check-doc-links` 在 `npm run check` 里绿）
- ✅ **作者面文档族已收口且「内容跑得通」**：[10-作者文档族对账](10-作者文档族对账.md) 的十五篇矩阵已有结论、两条作者面门禁在跑（本轮 `460` 条仓内链接全过、`23 ↔ 23` 篇对齐）、`plugin.schema.json` 与 live 同步（`check-plugin-schema-sync` 绿）——**并且本轮用「陌生作者的 AI」真跑了三档计时**（§5.6），**新增 7 条缺篇清单**
- ✅ 记忆库 / CLAUDE.md / `docs/05-插件更新/00-README.md` 的指针已同笔更新

**⇒ L7「插件源码外移与上架」封层。** 遗留 5 条挂账（F2–F6）＋ 7 条作者面缺篇，已在 §5.5 / §5.6 逐条登记；**L4 / L6 的去向仍由用户拍板**（见 §四）。

---

> **← 上一轮：** [07-脚手架与工作区.md](07-脚手架与工作区.md) · **← 总档案：** [00-整理档案.md](00-整理档案.md)
