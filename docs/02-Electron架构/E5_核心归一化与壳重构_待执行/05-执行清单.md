# E5 执行清单——核心归一化与壳重构

> 2026-08-02。**E4 文件树+编辑器完工后，14 项待办归一化为 4 层 18 轮 78 任务。**
> **🔥 进度唯一真相源。** 做完一个勾一个。实现细节见对应专题文档——本清单只列任务+文件+行数+依赖+验证。

---

## 🔥 关键路径与并行度

```
第 1 层：壳通信骨架（串行依赖——必须顺序执行）
  E5#1–#2 ShellEvents ──→ E5#3–#8 壳内低耦合 ──→ E5#9 布局引擎 ──→ E5#10–#11 Fallback 退役
                                    │
                                    ├── E5#61–#67 🔥 三通信机制建管道（依赖 #1。产出 requestToPlugin + p2p + dialog）
                                    │         │
                                    │         ├── E5#68–#72 🔥 linkdesk.* API 补全——tabs/menu/contextKey/pluginState 通用命名空间（依赖 E5#62）
                                    │         │         │
                                    │         │         ├── E5#48/#63/#64（invokeBeforeClose 用 requestToPlugin）
                                    │         │         ├── E5#11i（editor IPC openFile 走 requestToPlugin）
                                    │         │         ├── E5#54c（file:deleted 走 events 广播）
                                    │         │         └── E5#17a（clipboard 走 API）
                                    │         │
                                    │         └── E5#72 ESLint 规则（最后——迁移完再加）
                                    │
                                    ├── E5#11l（全部插件 WebView 独立 → 删白名单）
                                    │
                                    ├── E5#40 keep-alive（可和 #3–#8 并行——不同文件）
                                    ├── E5#41–#44 核心架构债（可和 #3–#8 并行——不同文件——含 #44c shellMenus 提供方 + #60 SidePanel 消费方）
                                    └── E5#48–#49 侧栏行为（依赖 #3–#8 + #9）
                                    └── E5#50–#53 标签页（依赖 #5 MainContent）
                                    └── E5#54 外部事件驱动（依赖 #1 ShellEvents）
                                    └── E5#55 Ctrl+Shift+T（无依赖）

第 2 层：归一化（第 1 层 #1–#2 完成后，全层可并行）
  E5#12 插件加载 ∥ E5#13–#14 appearsIn ∥ E5#15–#17 剪贴板 ∥ E5#18–#19 InlineInput
  ∥ E5#20 FileService API → E5#29,#31 IPC 命名（⚠️ E5#20 和 E5#31 都改 electron/main.ts——串行，先 #20 后 #31）∥ E5#32 安装单路径
  ∥ E5#30/#33 目录规范 ∥ E5#34–#35 硬编码路径 ∥ E5#36–#37 i18n
  ∥ E5#38–#39 ErrorService ∥ E5#45–#47 防线加固

第 3 层：功能补全（无依赖——随时可做）
  E5#21 设置 object ∥ E5#22 openInTerminal

第 4 层：技术债 + 回归（所有上层完成后）
  E5#23 FileEntry ∥ E5#24 Twistie ∥ E5#58 FALLBACK_META ∥ E5#59 WorkspaceService ──→ E5#25–#28 回归 + vitest + AI 验证
```

**不可逆点：**
- E5#12b——`loadThemePlugin()` 函数删除后不可恢复。主题加载逻辑已迁移到 `parseContributions`。checkpoint：vitest loader.test.ts 全部通过再删
- E5#42——50 文件移动后 git history 断裂。checkpoint：`npm run check` + 全功能回归通过再 commit

**最长串行链：** E5#1 → #3 → #5 → #9 → #10 → #25 = 6 步串行。其余全部可并行。并行扇出最大 = 15（第 2 层）。

---

## 当前代码状态（新 AI 进场须知）

```
src/
├── App.tsx                                   ← 壳内高耦合——直接 import 四个区域，互相传 props
├── components/IconBar.tsx                     ← 知道 Sidebar 是谁，onIconClick 传 pluginId
├── components/SidePanel.tsx                   ← 知道 MainContent 是谁
├── components/MainContent.tsx                ← 管理 WebContentsView + React fallback 双份渲染
├── components/StatusBar.tsx                   ← 知道当前活跃插件是谁
├── core/                                     ← Registry 桌子已就绪，但壳组件自己没用
│   ├── CommandRegistry.ts / ConfigurationService.ts / ...
│   └── (无 ShellEvents / ClipboardProviderRegistry / LayoutEngine)
├── pluginLoader/
│   ├── loader.ts                             ← loadViewPlugin/loadThemePlugin/loadLanguagePlugin 三条路径
│   └── viewRegistry.ts                       ← iconLocation ?? "top" (opt-OUT)
└── components/shared/                        ← 无 InlineInput 归一化组件

plugins/builtin/file-tree/plugin.json          ← 缺 iconLocation 声明（靠默认值活着）
plugins/builtin/marketplace/plugin.json        ← 同上
plugins/user/python/                           ← 被迫写空 index.tsx（无 pluginRole）
electron/main.ts                               ← openInTerminal 硬编码 PowerShell
```

**已安装防线（E3+E4 建立，E5 继承）：**
- ESLint 6 条自定义规则 + `import-x/no-unresolved`
- `RegistryBase`——unregisterAll 自动对称
- `Emitter`——事件订阅/退订
- tsc 扫 `src/` + ESRint 扫 `plugins/builtin/`
- 所有颜色走 CSS 变量、所有 UI 文字走 `t()`
- 路径归一化 `normalizePath()` / `joinPath()`
- 持久化走 PluginStateService——禁止 localStorage

**🆕 E4 遗留 UX 缺陷（E5 之后修）：**
- 未知文件扩展名（如 `.key`）双击无反馈——`console.warn` 一行完事，UI 上零提示。对标 VS Code 应弹出"选择编辑器"或 toast 提示无注册编辑器
- 🔴 **StatusBar 主题/语言图标用 emoji（☀/☾/中/EN）**——违反 UI/UX Pro Max 规范：**禁止 emoji 作为功能性 UI 元素**。Emoji 字体依赖平台、跨 OS 不一致、无法用设计 token 控制。应替换为 SVG/矢量图标（Lucide `Sun`/`Moon`/`Languages`）。分隔线 `|` 已改用 CSS border-left ✅

**E4 剩余任务（E5 之后执行）：**
- E4V#40j-40w：编辑器状态栏/面包屑/右键菜单/Diff/热退出/自动保存/Ctrl+Shift+T/配置项/装饰/快捷键
- E4V#Test1/4/5：FileTreeModel/FileExcludeFilter/FileTreeClipboard 测试
- E4V#43-45：ViewContainer 交互（溢出菜单/欢迎页/拖拽分隔线）。E4V#46（折叠持久化）→ ✅ reorderView+moveView 持久化已修，重置按钮已删除。E4V#47-48（排序+跨容器拖放）→ ✅ 持久化已修。E4V#49（右键菜单）→ E5#44c+#60、E4V#50（Views 子菜单）→ E5#44d

---

## 🔴 致命级 Bug 预判——E3+E4 历史 10 堂课

> 每个风险有精确的 commit 回溯、根因分析、预防代码。**不是"注意一下"——是具体的代码防线。**

### Bug E5-1 🔴🔴🔴 Events 类型漂移——emit 和 on 不匹配

**历史：** E3j #77 events 系统——`emit("icon:selected", id)` 发 string，`on("icon:selected", (payload) => {})` payload 是 any。如果发 `{ id: "marketplace" }` 而不是 `"marketplace"` → 运行时静默炸。无编译期检查。

**E5 触发条件：** 壳内四个区域通过 events 通信——IconBar emit、Sidebar+M
MainContent+StatusBar 各自订阅。四个区域由不同人（或不同 AI）维护 → 改一个区域的 emit 签名 → 其他三个区域的 on handler 全炸。

**🔥 防线——代码级：**
```typescript
// src/core/ShellEvents.ts —— 壳内通信唯一类型定义
interface ShellEvents {
  "icon:selected":    string;          // pluginId
  "sidebar:toggled":  boolean;         // isOpen
  "tab:focused":      { pluginId: string; tabId: string };
  "statusbar:update": StatusBarEntry[];
  "zone:resized":     { zone: string; width: number };
}

// 类型安全的 emit/on——编译期检查所有调用
function emit<K extends keyof ShellEvents>(event: K, payload: ShellEvents[K]): void;
function on<K extends keyof ShellEvents>(event: K, handler: (payload: ShellEvents[K]) => void): () => void;
```

**🛡️ 验证：** 写错 `emit("icon:selected", { id: "x" })` → tsc 当场报错——不等到运行时。

---

### Bug E5-2 🔴🔴🔴 事件订阅泄漏——mount 注册 unmount 忘清理

**历史：** E3j #81 双份渲染根因——`_initIPC()` 模块级注册 IPC 监听器，永不清理。壳 fallback 切空 div 后 IPC 监听器仍存活 → 僵尸回调。修复：引用计数 + useEffect cleanup（#81-2）。

**E5 触发条件：** 四个区域各自订阅 ShellEvents → 每个区域有 2-4 个 `on()` 调用 → 总共 8-16 个事件订阅。区域组件 unmount 时如果有一个没 cleanup → 僵尸订阅 → 事件来了处理不存在的 DOM → 静默报错或白屏。

**🔥 防线——代码级：**
```typescript
// 每个区域的 useEffect 必须返回 unsubscribe
useEffect(() => {
  const unsub1 = events.on("icon:selected", handleIconSelected);
  const unsub2 = events.on("sidebar:toggled", handleSidebarToggled);
  return () => { unsub1(); unsub2(); };  // ← 忘了这行 → 内存泄漏 + 僵尸回调
}, []);
```

**🛡️ ESLint 已覆盖：** `no-module-level-ipc-listener` 拦截模块级注册。但 useEffect cleanup 缺失目前无机械拦截——需代码审查手动检查。

---

### Bug E5-3 🔴🔴 布局引擎坐标错位——区域重叠或空白

**历史：** E3a #29 MainContent 改为 WebContentsView placeholder——bounds 计算错 → WebView 和 React 重叠/错位。E3f #58d 关掉 React fallback → 标签页全白。

**E5 触发条件：** 布局引擎管理四个区域的 bounds。区域 resize/拖拽/新增时需重算所有区域的坐标。某个区域的 `position: fixed` 和另一个区域的 flex 布局冲突 → 重叠或空白。

**🔥 防线——代码级：**
```typescript
// src/core/LayoutEngine.ts
class LayoutEngine {
  private zones = new Map<string, ZoneState>();

  layout(zones: ZoneConfig[]): void {
    // 1. 验证无重叠——任何两个 zone 不能占据同一空间
    // 2. 验证全覆盖——中心 zone 填满剩余空间
    // 3. 坐标全整像素——避免 sub-pixel 渲染模糊
    // 4. 计算后一次性 apply——不逐 zone 设（避免中间态闪烁）
  }
}
```

---

### Bug E5-4 🔴🔴 iconLocation opt-IN 遗漏——图标栏图标消失

**历史：** E3f IconBar isActive 语义断层（E3.6 Bug 6）——`sidebarView = pluginId` 改为 containerId → 所有图标永不高亮。无编译器检查、无 vitest 覆盖。

**E5 触发条件：** `viewRegistry.ts` 默认值从 `"top"` 改为 `null` → 所有未声明 `iconLocation` 的插件图标从 IconBar 消失。file-tree 和 marketplace 当前靠默认值活着 → 图标消失 → 用户无法打开侧栏。

**🔥 防线——先补声明，再改默认值：**
```
E5#10a: file-tree/plugin.json 加 "iconLocation": "top"
E5#10b: marketplace/plugin.json 加 "iconLocation": "top"
E5#10c: 改 viewRegistry.ts 默认值 null
```
顺序不可颠倒——先补后改。

---

### Bug E5-5 🔴🔴 插件加载归一化——language 插件静默失效

**历史：** E3b #34——非视图插件踩进盲区：`getViewPlugin()` 返回 undefined → 卸载/禁用按钮无效果。E3c #38——`parseContributions` 忘加 `contributes.languages` 分支 → 合集语言插件静默失效。

**E5 触发条件：** 归一化 `loadPluginLifecycle()` 后，旧的 `loadThemePlugin`/`loadLanguagePlugin` 删除。如果 `parseContributions` 里语言/主题的解析顺序不对、或漏了解析——主题/i18n 全部静默失效。

**🔥 防线——vitest 覆盖 + 回归测试：**
```typescript
// loader.test.ts —— 归一化后必须覆盖的 case
it("声明式语言插件 (contributes.languages) → parseContributions 正确注册");
it("声明式主题插件 (contributes.themes) → parseContributions 正确注册");
it("视图插件 (entry + viewsContainers) → 加载 React 组件");
it("数据插件 (纯 langDefs, 无 entry) → 不注册视图、不加载组件");
it("混合插件 (entry + contributes.themes) → 视图+主题均生效");
```

---

### Bug E5-6 🔴 剪贴板 Provider 覆盖——两个插件注册同一个 when

**历史：** E3f #59f——`CommandRegistry.registerCommand` 覆盖已有命令时 `console.warn`。E3b #36f9——`setConfigurationValue` 写入前 enum 验证。

**E5 触发条件：** `ClipboardProviderRegistry.register({ when: "editorFocus", onCopy })`——如果两个插件都注册了 `when: "editorFocus"` → 后注册的覆盖先注册的 → 先注册的插件的复制逻辑失效。

**🔥 防线——代码级：**
```typescript
// src/core/ClipboardProviderRegistry.ts
register(provider: ClipboardProvider): void {
  const existing = this._providers.find(p => p.when === provider.when);
  if (existing) {
    console.warn(
      `[ClipboardProvider] ⚠️ "${provider.when}" 已有注册者，被覆盖。` +
      `旧: ${existing.pluginId} → 新: ${provider.pluginId}`
    );
  }
  this._providers.push(provider);
}
```

---

## 第 1 层：壳通信骨架——铁轨

> ⚠️ **必须最先做。** 04/05 的终端/悬浮窗/可拖出标签页都在这个地基上建。

---

### 第 1 轮：ShellEvents 类型系统——通信契约

> 📖 **专题文档：** [`01-壳通信骨架/ShellEvents类型系统.md`](01-壳通信骨架/ShellEvents类型系统.md)——含预测 Bug E5-1a/1b/1c + vitest 防线

#### E5#1 ShellEvents 类型表 + 类型安全 emit/on

- [x] **E5#1a** 新建 `src/core/ShellEvents.ts`——`ShellEvents` 接口定义全部壳内事件类型 | ~30 行 ✅
- [x] **E5#1b** 类型安全的 `emit<K>(event: K, payload: ShellEvents[K])` + `on<K>(event: K, handler: (p: ShellEvents[K]) => void): () => void` | ~20 行 ✅
- [x] **E5#1c** 事件追踪（dev 模式）——每个 emit 在 Console 打印 `[ShellEvents] emit "icon:selected" → "marketplace"` + 每个 handler 执行耗时 | ~15 行 ✅
**验证：** 写错 `emit("icon:selected", { id: "x" })` → tsc 报错。`npm run check` 零错误。11/11 测试全绿。

#### E5#2 ShellEvents 接入现有 Emitter 基础设施

- [x] **E5#2a** ShellEvents 基于现有 `Emitter` 类（`src/core/CoreEvents.ts`）——不改底层，只加类型层 | ~10 行 ✅
- [x] **E5#2b** 导出单例 `shellEvents`——全局唯一事件总线 | ~3 行 ✅
**验证：** `shellEvents.emit("icon:selected", "marketplace")` 类型检查通过。`shellEvents.emit("nonexistent", ...)` → tsc 报错。

---

### 第 2 轮：壳内低耦合——四个区域解耦

> 📖 **专题文档：** [`01-壳通信骨架/壳内低耦合-IconBar改造.md`](01-壳通信骨架/壳内低耦合-IconBar改造.md)（含预测 Bug E5-3a/3b）——四个区域互不 import，只通过 ShellEvents 通信。

#### E5#3 IconBar 改造——只 emit 事件

- [x] **E5#3a** IconBar 取消接收 App.tsx props（`sidebarView`/`onOpenOrFocus`）→ 改为订阅 ShellEvents | `IconBar.tsx` −2 行 props 解构 + ~5 行事件订阅 ✅
- [x] **E5#3b** `onIconClick` → `shellEvents.emit("icon:selected", pluginId)` | `IconBar.tsx` ~3 行 ✅
- [x] **E5#3c** `isActive` 逻辑——订阅 `shellEvents.on("sidebar:toggled")` + 内部 state | `IconBar.tsx` ~8 行 ✅
- [x] **E5#3d** useEffect cleanup——unmount 时取消所有事件订阅 | `IconBar.tsx` ~5 行 ✅（审计 4/4 全部覆盖）
- [x] **E5#3e** 🔧 拖拽排序归一化——`window mousemove/mouseup` → `shellEvents.emit("icon:drag-start"/"icon:reordered")`；`setPluginVersion` useMemo hack → 订阅事件自然触发重渲染；持久化移到事件订阅者 | `IconBar.tsx` ~15 行 ✅（自订阅 `icon:reordered` 处理持久化，E5#7d 后移到 App.tsx）
**验证：** 点 📁 → 图标高亮。拖拽排序正常。新插件自动在最下。`PluginStateService` 不直接在 IconBar 中调用。

#### E5#4 SidePanel 改造——只订阅事件

- [x] **E5#4a** SidePanel 取消接收 App.tsx props（`sidebarView`/`activePluginId`）→ 改为订阅 ShellEvents | `SidePanel.tsx` −2 行 props 解构 + ~5 行事件订阅 ✅
- [x] **E5#4b** 订阅 `shellEvents.on("icon:selected", (pluginId) => resolveContainerId(pluginId))` | `SidePanel.tsx` ~8 行 ✅
- [x] **E5#4c** 切换容器时 emit `shellEvents.emit("sidebar:toggled", isOpen)` | `SidePanel.tsx` ~3 行 ✅
- [x] **E5#4d** useEffect cleanup——unmount 时取消所有事件订阅 | `SidePanel.tsx` ~5 行 ✅
**验证：** 点 📁 → 侧栏渲染 FOLDERS。SidePanel.tsx 代码无任何对其他区域的引用。

#### E5#5 MainContent 改造——只管理标签页+分屏

- [x] **E5#5a** MainContent 加 shellEvents import + 订阅基础设施 | `MainContent.tsx` +1 import ✅
- [x] **E5#5b** 订阅 `shellEvents.on("icon:selected", ...)`——需要时响应（如侧栏图标导致标签页切换）| `MainContent.tsx` ~5 行 ✅
- [x] **E5#5c** 切换标签页时 emit `shellEvents.emit("tab:focused", { pluginId, tabId })` | `MainContent.tsx` ~3 行 ✅
- [x] **E5#5d** useEffect cleanup——unmount 时取消所有事件订阅 | `MainContent.tsx` ~5 行 ✅
- [x] **E5#5e-i** 🔧 MainContent 去冗余 props——`activeGroupId`（读 `tabState.activeGroupId`）| `MainContent.tsx` ~3 行 ✅
- [x] **E5#5e-ii** 🔥 TabManager 搬迁——拆分 a-f 全部完成 ✅
- [x] **E5#5f** 🔥 壳内视图注册去硬编码——`renderTabContent` 不再 switch `plugin-detail/output/welcome`。SHELL_VIEWS 查表替代 | `MainContent.tsx` ~25 行 ✅
**验证：** 切换标签页 → StatusBar 跟随更新（通过事件，不是 props）。MainContent.tsx 代码无任何对其他区域的引用。

#### E5#6 StatusBar 改造——只订阅事件

- [x] **E5#6a** StatusBar 取消接收 App.tsx props（`error`/`theme`/`lang` 等）→ 改为订阅 ShellEvents + 直接读 ConfigurationService | `StatusBar.tsx` −4 行 props 解构 ✅
- [x] **E5#6b** 订阅 `shellEvents.on("tab:focused", ...)`——切换状态栏条目 | `StatusBar.tsx` ~5 行 ✅
- [x] **E5#6c** 订阅 `shellEvents.on("statusbar:update", ...)`——接收新条目 | `StatusBar.tsx` ~5 行 ✅
- [x] **E5#6d** useEffect cleanup——unmount 时取消所有事件订阅 | `StatusBar.tsx` ~5 行 ✅
- [x] **E5#6e** 🔧 StatusBar 渲染归一化——eventEntries + 壳固定项（语言/主题）走统一 │ + status-bar-btn 渲染 | `StatusBar.tsx` +21/−17 ✅
**验证：** 切换标签页 → StatusBar 正确显示对应插件条目。StatusBar.tsx 代码无任何对其他区域的引用。

#### E5#7 App.tsx 去胶水化

- [x] **E5#7a** App.tsx 取消直接 import 四个区域的 props 传递 | `App.tsx` −15 行 ✅（useTabManager 搬家后大部分 props 已删，剩 8 个拖拽相关）
- [x] **E5#7b** App.tsx import 清理——tsc 零未使用，E5#5e-ii-f 已清理 ✅
- [x] **E5#7c** App.tsx 处理全局级逻辑——`revertContainerIfCurrent` 保留 / PLUGIN_REMOVED → emit ShellEvents | `App.tsx` ✅
- [x] **E5#7d** 🔥 App.tsx 订阅 `icon:reordered`——App 调 PluginStateService 持久化。IconBar 删自订阅 | `App.tsx` +8 / `IconBar.tsx` −6 ✅
- [x] **E5#7e** 🔥 删 App.tsx `icon:selected` 桥接 | `App.tsx` ~−25 行 ✅（E5#5e-ii-f 中删除）
- [x] **E5#7f** 🔧 StatusBar 去 `onToggleTheme`/`onToggleLang` → `executeCommand` | `StatusBar.tsx` +12/−10 + `App.tsx` −12 ✅
- [x] **E5#7g** 🔧 消灭 TabActionsContext Provider——`useTabActions` 直接 emit ShellEvents，无需 Context | src −26 ✅
- [x] **E5#7h** 🔴 E5#7 遗留修 bug——全部 5 项完成 ✅<br>
  **h1** 3 stub → emit + MainContent 订阅 ✅ | **h2** closeTab → forceCloseTab ✅<br>
  **h3** mount restoreLayout ✅ | **h4** eventEntries → E5#6e 归一化 ✅<br>
  **h5** ShellEventBus 事件缓冲 14/14 测试全绿 ✅
- [x] **E5#5e-ii-a** 🔥 删 activePluginId 派生 ✅
- [x] **E5#5e-ii-b** 🔥 ContextKey 改 tab:focused ✅
- [x] **E5#5e-ii-c** 🔥 PLUGIN_REMOVED → MainContent ✅
- [x] **E5#5e-ii-d** 🔥 布局持久化搬 MainContent ✅
- [x] **E5#5e-ii-e** 🎉 useTabManager 搬到 MainContent ✅
- [x] **E5#5e-ii-f** 🎉 coreCallbacks + TabActionsContext 收割 ✅

**验证：** App.tsx 不再有任何 `onIconClick`/`activePluginId`/`sidebarOpen` 等 props 传递给子组件。四个区域各自独立渲染。

#### E5#8 端到端验证——壳内低耦合

- [x] **E5#8a** 点 📁 → 侧栏正确、图标高亮、状态栏不变 → 📁 toggle 关闭正常 ✅
- [x] **E5#8b** 点 🪢 → 侧栏切换到串口监视器、图标切换 ✅
- [x] **E5#8c** 切换标签页 → 侧栏不关（lastSidebar sticky）+ 状态栏跟随 ✅
- [x] **E5#8d** `npm run check` 零错误（tsc + ESLint + vitest）✅
- [x] **E5#8e** Console 开发模式打印完整事件流 ✅

---

### 第 3 轮：壳布局引擎——区域可排列

> 📖 **专题文档：** [`01-壳通信骨架/壳布局引擎.md`](01-壳通信骨架/壳布局引擎.md)——含预测 Bug E5-9a/9b/9c + 完整 ZoneConfig API

#### E5#9 LayoutEngine 类

- [x] **E5#9a** 新建 `src/core/LayoutEngine.ts`——`ZoneConfig { zone, mode:"docked"|"floating", dock:{edge,width?,height?,flex?,order?}, float:{x,y,width,height,zIndex?}, undockable?, visible? }` + `LayoutEngine` 类（`setLayout`/`addZone`/`removeZone`/`dockTo`/`resizeZone`/`getBounds`/`getZone`）+ `onDidChangeLayout` Emitter | ~150 行
- [x] **E5#9b** 坐标计算——E5 只处理 `mode === "docked"` 的 zone。left zones 按 order 从左堆叠、right zones 从右堆叠、center 填满剩余空间、bottom 占底部全宽。floating zone 走 `float` 字段直出——不经过 `_recalculate` | ~40 行
- [x] **E5#9c** 默认布局——图标栏左 42px + 侧栏左 280px + 主区中 flex:1 + 状态栏底 24px。`undockable`：侧栏=true，其余=false。**E5 侧栏在左——和现在一样。** `mode: "floating"` 的数据结构已就绪，v1.4 实现逻辑 | ~10 行
- [x] **E5#9d** 🛡️ 总宽度验证——left+center+right 宽度和与容器宽度匹配。不匹配 → console.warn | ~10 行
- [x] **E5#9e** 🛡️ 像素对齐——所有坐标 `Math.round()`——避免 sub-pixel 渲染模糊 | ~3 行
**验证：** `layoutEngine.setLayout([...])` → `getBounds("sidebar")` 返回正确坐标。重叠配置 → console.warn。

#### E5#9f App.tsx 接入 LayoutEngine

- [x] **E5#9f1** App.tsx 用 `layoutEngine.getBounds(zoneId)` 渲染四个区域的位置/大小——替代硬编码 CSS flex | `App.tsx` ~15 行
- [x] **E5#9f2** 四个区域接收 `style` prop（位置/大小）| IconBar/SidePanel/MainContent/StatusBar 各 ~3 行
- [x] **E5#9f3** 验证——侧栏在左 280px、图标栏在左 42px、主区在中间、状态栏在底部。与改造前视觉一致。**E5 侧栏在左，`dock.edge: "right"` 是可用的——`dockTo("sidebar","right")` 可以换边，但 E5 不设 UI 入口**

---

### 第 4 轮：React Fallback 退役——多 WebView 真完成

> 📖 **专题文档：** [`01-壳通信骨架/React-Fallback退役.md`](01-壳通信骨架/React-Fallback退役.md)——含预测 Bug E5-10a/10b/10c + 双条件就绪判断
> **前置：** E5#1–#9（壳内低耦合 + 布局引擎——MainContent 已独立管理标签页）

#### E5#10 🔴 修复 fallback 退役时序 bug

- [x] **E5#10a** 定位 E3f #58d 白屏根因——WebView 未 ready 时 React 已停 → 两边都没内容 | `MainContent.tsx` 调查 ~20 行<br>**→ 诊断发现：rAF 在 WebContentsView 中不触发，`notifyReady` 从未执行，`readyWebViewIds` 始终为空。`plugin-shell-main.tsx` rAF→sync 修复。**
- [x] **E5#10b** 修：MainContent 渲染空 div 占位 + 等 `plugin-view:ready` → 关掉 React fallback → 显示 WebView | `MainContent.tsx` ~15 行<br>**→ 双条件（readyWebViewIds + webViewBoundsReady）+ WEBVIEW_READY_PLUGINS 白名单。仅 serial-monitor 关停 React。**
- [x] **E5#10c** 🛡️ 超时兜底——WebView 5s 未 ready → 回退到 React fallback + console.warn | `MainContent.tsx` ~5 行<br>**→ webViewTimeout Set + 5s timer，超时后永久 React fallback。**
**验证：** 切到串口监视器标签页 → 壳 WebView 的 React 树中无 SerialMonitor 组件 → 只有空 div。终端 WebView 完全接管。

#### E5#11 验证——多 WebView 验收

- [x] **E5#11a** 串口监视器 `console.log("test")` → 只在 terminal DevTools 出现，壳 DevTools 不可见 ✅
- [x] **E5#11b** 文件树 `console.log("test")` → 只在 file-tree DevTools 出现 ✅
- [x] **E5#11c** 壳的 `console.log` → 只在壳 DevTools 出现（标签页管理/侧栏/图标栏） ✅
- [x] **E5#11d** 插件崩溃隔离——`throw Error` 仅影响该插件 WebView，壳和其他插件正常 ✅
- [x] **E5#11e** Ctrl+Shift+P → DevTools → 每个插件有自己的 DevTools 入口，互不干扰（shell + 7 = 8 个） ✅

#### 🆕 E5#11f–#11l 插件 WebView 独立化——逐插件验收与改造

> 📖 **详见：** [`React-Fallback退役.md §六`](01-壳通信骨架/React-Fallback退役.md)——`WEBVIEW_READY_PLUGINS` 白名单是过渡方案，最终全部插件 WebView 独立后删除。
> **判断标准：** 插件在 WebView 中渲染时，不依赖 React props，通过 IPC/ShellEvents 接收壳指令。
> **改完一个加一个进白名单。**
> **🔥 2026-08-04 当前：** 4/5 已进白名单。只剩 editor——Monaco 0×0 初始化阻塞（3 次尝试 3 次回退）。

- [x] **E5#11f** 🟡 settings——纯独立表单 UI，无外部指令需求 → 加入白名单 ✅
- [x] **E5#11g** 🟡 marketplace——独立列表 UI → 加入白名单 ✅
- [x] **E5#11h** 🟡 file-tree——已通过 `linkdesk.fileService` IPC 操作 → 加入白名单 ✅
- [x] **E5#11i** 🔴 editor——**加 IPC 通道 + 双模式组件**：`pluginRequest.handle('openFile')` 接收文件路径，`const fp = ipcId ?? propId` 双模式（IPC 优先/props 兜底）。`EditorView.tsx` 轮询等非零尺寸（`setBounds` 从主进程异步设，ResizeObserver 不触发）。7 次尝试最终收敛。editor WebView 独立 ✅。**2026-08-04 验证：** 4 个 bug 修复后（`readyWebViewIds` 依赖缺失 / 无标签页超时 / `webViewTimeout` 不可逆 / `notifyReady` 竞态），3 插件标签页 React fallback 全部关停 ✅
- [x] **E5#11j** 🟡 python——已声明 `pluginRole: "data"`（E5#13d），无 UI，不需要 WebView ✅
- [x] **E5#11k** 🟡 workspace——已删除 ✅
- [x] **E5#11l** 🔴 删白名单 + 4 bug 修复——`WEBVIEW_READY_PLUGINS` Set 删除，关停退化为 `readyWebViewIds && webViewBoundsReady`。**🐛 4 Bug：** ① `readyWebViewIds` 未加入 bounds sync effect deps → ② 无标签页时 5s 超时误触发 → ③ `webViewTimeout` 不可逆 → ④ `notifyReady` 竞态（preload 层缓冲修复）。**涉及文件：** `MainContent.tsx`（+3 处 ~15 行）+ `preload-shell.ts`（+7 行缓冲）✅<br>**验证：** 3 插件标签页 React fallback 关停 ✅ / `document.querySelectorAll('.plugin-webview-placeholder').length > 0` / `npm run check` ✅

---

### 第 4f-bis 轮：多 WebView 生命周期归一化（✅ 2026-08-04 完成——6 bug 收束为 3 归一化）

> 📖 **触发：** E5#11l 验证发现 6 个 bug，其中 4 个是同模式缺陷的重复（IPC↔React 竞态、ref 时序、DOM 选择歧义）。本轮收束为 3 个归一化。

#### E5#81 🔴 抽 `useWebViewSync` hook

- [x] **E5#81a** 新建 `src/hooks/useWebViewSync.ts`——从 `MainContent.tsx` 抽取 4 effect + 3 state + 2 ref（`readyWebViewIds` / `onReady` / 5s 超时 / `webViewBoundsReady` / `webViewTimeout` / setVisible / setBounds）| 190 行 ✅
- [x] **E5#81b** `MainContent.tsx`——替换为 hook 调用，−142 行 + 5 行。`plugin:removed` effect 改用 `resetWebViewState` ✅
- [x] 验证——tsc 零错误 / `npm run check` 通过（已有 7 失败与本次无关）✅
- **收束 Bug：** ① dep 缺失 / ② 无标签页超时 / ③ 超时不可逆 / ⑤ prev 时序——同模式不再散落

#### E5#82 🟡 bounds 计算改用 callback ref——替代 `querySelector`

- [x] **E5#82a** `useWebViewSync` 暴露 `registerPoolRef(groupId)` + `poolRefs` ✅
- [x] **E5#82b** `.tab-content-pool` 加 `ref={registerPoolRef(group.id)}` ✅
- [x] **E5#82c** bounds 计算改用 `poolRefs.current.get(state.groupId)` 替代 `querySelector` ✅
- **消 Bug 6：** DOM 选择歧义——同名属性多个元素 → 精确 ref

#### E5#83 🟡 硬约束文档——IPC 监听器注册位置规则（防 Bug 4 复发）

- [x] **E5#83** CLAUDE.md 硬约束 #20——preload 脚本 IPC 监听器必须模块顶层注册 + 缓冲回放。ESLint `linkdesk/no-module-level-ipc-listener` 已拦截 React 模块级 IPC ✅
- [x] 审计——`pv.onReady` 已通过 preload 层 `_readyBuffer` 解决。无其他 React useEffect 内 IPC 监听 ✅
- **防 Bug 4 复发：** IPC↔React 生命周期竞态模式已被文档约束系统化阻止

---

### 第 4f-ter 轮：插件 WebView 初始化数据迁移（🆕 React fallback 关停暴露的遗留问题）

> 📖 **触发：** 多 WebView 真正工作后发现 3 个插件的 WebView 显示空状态——"会话已失效"/"无匹配设置"/"无快捷键配置"。根因：E5#11 白名单验收时 React fallback 仍盖在上面，插件 WebView 从未被纯 WebView 模式测试过。现在 React fallback 真的关了，暴露了缺失的 IPC 初始化数据链路。

#### E5#84 🔴 serial-monitor——`sourceId` IPC 通道

> **根因：** `SerialMonitorView` 依赖 `sourceId` prop 绑定会话（`useSession(sourceId)`）。在独立 WebView 中 `plugin-shell-main.tsx` 只传 `<Component isActive={true} />` → `sourceId = undefined` → session = null → "会话已失效"。
> **模式：** 对标 editor 的 `openFile` 通道。

- [x] **E5#84a** `serial-monitor/src/index.tsx`——`pluginRequest.handle('openSession', ...)` + 双模式 `ipcSourceId ?? propSourceId` ✅
- [x] **E5#84b** `MainContent.tsx`——effect：`requestToPlugin('serial-monitor', 'openSession', { sourceId })` ✅
- [x] **E5#84c** 双模式对标 editor ✅
- [x] 验证——tsc 零错误 ✅
- [x] **E5#84d** 🔴 窗口缩放/全屏时 WebView 内容不跟随——bounds 不重算。**归一化修法（壳侧，零插件改动）：** `useWebViewSync` 加 `window.resize` → `resizeVersion` state 入 Effect 3 deps → 所有插件 WebView 实时重算 `setBounds` | `useWebViewSync.ts` ~6 行 ✅
- [x] **E5#84e** 🔴 多标签页 sourceId 覆盖——确认单例/多实例 WebView 后只发当前聚焦 tab | `MainContent.tsx` ~3 行 ✅
- [x] **E5#84f** 🔴→🟡 侧栏↔主区状态不同步——`useSerialSessions._notify()` 加 `events.emit` 广播 + `_ensureInit()` 加订阅。同时加 `pluginState.onChange` 框架原语（Layer 2）。| `useSerialSessions.ts` + `IpcBridgeHandler.ts` + 2 preload ~50 行 ✅
- [x] **E5#84g** 🔴🔴 弹窗被 WebView 盖住——`DialogService` 加 `_hideAllPluginViews()` 直接调 `setBounds` 屏幕外 + `setVisible`。**后续回退单 WebView 后不再需要——但代码保留。** | `DialogService.ts` ~30 行 ✅。🆕 Bug：preload-shell.ts 缺 `config`/`commands`——`config: {}` 空对象导致侧栏 `lk.configuration.get()` 崩（E5#85 修）

#### E5#85 🟢 插件去 @src/core import + linkdesk.* API 补全（🆕 2026-08-04 重定义）

> **原计划：** settings 代码搬迁。**实际执行：** 单 WebView 回退后，settings 照常工作。改为做更有价值的事——清理 66→42 插件 import + 补全 3 个新 API 命名空间。

##### E5#85-1 新 API——3 个命名空间

- [x] **E5#85-1a** `linkdesk.path`——`normalize/join/basename/dirname/extname` 纯函数，两个 preload 加 ~12 行 ✅
- [x] **E5#85-1b** `linkdesk.filesystem` 扩展——补 `listDir/exists/mkdir/copy/remove/watch`（IPC handler 已存在，只补 preload）| `preload-plugin.ts` +12 行 ✅
- [x] **E5#85-1c** `linkdesk.workspace`——`getFolders/getActive` proxy channel + IpcBridgeHandler + 两个 preload | 4 文件 ~20 行 ✅

##### E5#85-2 插件迁移——24 处 import 替换

- [x] **E5#85-2a** editor 插件——`normalizePath`→`lk.path.normalize`、`readBinaryFile/writeFile`→`lk.filesystem.*`、`getConfigurationValue`→`lk.configuration.get` 等 | 7 文件 ✅
- [x] **E5#85-2b** file-tree 插件——`listDir`→`lk.filesystem.listDir`、`getWorkspaceFolders`→`lk.workspace.getFolders`、`watchFile`→`lk.filesystem.watch` 等 | 8 文件 ✅
- [x] **E5#85-2c** marketplace/serial-monitor——全 KEEP（Emitter/type/ViewContainerService） | 0 改动 ✅
- [x] **E5#85-2d** `FileTreeModel.init()` 加异步 sortOrder 加载——测试适配 `await m.init()` | 1 文件 ✅

##### E5#85-3 ESLint 防线

- [x] **E5#85-3a** `eslint.config.js`——`no-restricted-imports` 从 `"warn"` 升 `"error"`，精确拦截 `ConfigurationService`/`pathUtils` | 1 文件 ✅
- [x] **E5#85-3b** `vitest.setup.ts`——mock `window.linkdesk`（path/configuration/workspace/filesystem/tabs/events）| 1 文件 ✅

##### E5#85-4 测试

- [x] **E5#85-4a** 测试适配——`FileTreeModel.test.ts` 加 `await m.init()` + `__ldkConfigStore` 桥梁 + `lk.filesystem.listDir` 桥接 | 1 文件 ✅
- [x] 验证——tsc 零错误 / ESLint 零错误 / vitest 7 预存失败 358 通过 ✅

##### E5#85-5 文档

- [x] **E5#85-5** `docs/03-插件制造/01-插件API契约.md`——完整重写，20 命名空间 + 禁止事项 | ~300 行 ✅

##### E5#85-6 bug 修复

- [x] 🔴 `preload-shell.ts`——`config: {}` 空对象 → 侧栏 `lk.configuration.get()` 调用 `{}.get` 崩。补全 `config/configuration/commands` | `preload-shell.ts` ~20 行 ✅

#### E5#86 🟡 editor 多标签页修复

- [x] **E5#86** 🟢 回退单 WebView 后自动修复——`sourceId` 通过 React props 直传，不存在 IPC 覆盖问题 ✅

---

> 🔥 **2026-08-04 扫尾合并：** 原 E5#75-80 "扫尾路线图"已合并到上方 E5#11f-11l（白名单逐插件验收）和下方 E5#74（plugin:push 投递 + events 重写 + 文件系统广播）。不再单独维护扫尾段。

---

### 第 4b 轮：keep-alive 显隐策略修复（🆕 editor 光标根因）

> 📖 **专题文档：** [`01-壳通信骨架/keep-alive显隐策略修复.md`](01-壳通信骨架/keep-alive显隐策略修复.md)——17 个 commit：`display:none` 让隐藏 tab 的 DOM 不存在，Monaco 无法测量坐标

- [x] **E5#40a** `TabPanePositioner.tsx` L76——`display:none` → `opacity:0` + `pointerEvents:none` + `zIndex:0` | ~3 行 ✅
- [x] **E5#40b** `plugins/builtin/editor/src/navigation-bridge.ts`——删 EditorRegistry（含 register/unregister/get/Map）| ~−20 行 ✅
- [x] **E5#40c** `EditorView.tsx`——删 F12 handler 直查 registry 分支 + mount/dispose 中 register/unregister | ~−30 行 ✅
- [x] **E5#40d** F12 回归简单模型——`setPendingReveal` → `createTab` → mount consume | ~0 行（删后即此）✅
- [x] **E5#40e** 验证——F12 跨文件跳转光标正确 / 切到隐藏 tab 光标保留 / `npm run check` ✅

---

### 第 4d 轮：侧栏行为归一化（🆕 操作体验）

> 📖 **专题文档：** [`01-壳通信骨架/侧栏行为归一化-关闭与折叠.md`](01-壳通信骨架/侧栏行为归一化-关闭与折叠.md)——串口未开不弹确认 + 图标点击 toggle 折叠

#### E5#48 🔧 串口未开关闭标签页不弹确认

- [x] **E5#48** 临时方案——`confirmCondition` 通用 `"namespace:field"` 机制（壳替插件判断）✅
- [x] **E5#63 已撤回**——`invokeBeforeClose` 改用 `requestToPlugin`。壳不再替插件判断。见 E5#63 ✅

#### E5#49 🔧 图标点击 toggle 侧栏折叠

> ⚠️ 原计划（collapsedWidth/ZoneConfig）基于错误观察——文档作者以为点击图标能 toggle 侧栏，实际 `lastSidebar` 机制让侧栏始终展开。实际实现：图标点击 = ◀/▶ 按钮。

- [x] **E5#49** 同图标再点击 → toggle `collapsed` 状态（和 ◀/▶ 按钮行为一致）| `SidePanel.tsx` ~20 行
  - `collapsedRef` + `containerIdRef` ref 桥接——effect 有 `[]` 依赖，避免闭包过期
  - `toggleCollapse` 重命名 `doCollapse`——图标点击 + ◀/▶ 按钮共用
  - 折叠态点不同图标 → 自动展开并切换容器
- [x] 验证——点 📁 展开 → 点 📁 折叠到 28px → 点 📁 重新展开。折叠态点 🪢 → 展开并切换。`npm run check` 零错误 ✅

---

### 第 4d-bis 轮：多 WebView 三通信机制补全（🆕 壳通信基础设施）

> 📖 **专题文档：** [`01-壳通信骨架/多WebView三通信机制.md`](01-壳通信骨架/多WebView三通信机制.md)——E3a 多 WebView 后三张桌子只实现了一张半。广播全通未验证、请求仅插件→壳、推流零代码。本轮补全双向通信。
> **前置：** E5#1 ShellEvents（壳内事件类型系统）✅
> **依赖本轮的后续任务：** E5#48（invokeBeforeClose 撤回 confirmCondition）、E5#11i（editor IPC）、E5#54（标签页外部事件）

#### E5#61 🔧 广播 events 审计与加固

- [x] **E5#61a** 审计 `events.emit` 回环——无 double-fire bug。emit 走 IPC 出去，plugin:push 回来，单次投递 ✅
- [x] **E5#61b** event payload 加可选 `source` 字段——"shell" 或 pluginId，接收方可选读来源 ✅
- [x] **E5#61c** dev 模式日志——`[events] emit "channel" → N 订阅者` ✅
- [x] 验证——`npm run check` ✅

#### E5#62 🔴 请求 commands 双向化——加 requestToPlugin ✅

- [x] **E5#62a** `electron/ipc-bridge.ts`——加 `ipcMain.handle('bridge:request-to-plugin', ...)` + `bridge:plugin-response` 匹配 + 10s 超时 ✅
- [x] **E5#62b** `electron/preload-shell.ts`——`bridge.requestToPlugin(pluginId, channel, payload): Promise<unknown>` ✅
- [x] **E5#62c** `electron/preload-plugin.ts`——`pluginRequest.handle(channel, handler)` / `unhandle(channel)` + `plugin:request` IPC 分发。🐛 contextBridge 隔离世界 bug——`_handlers` 提到模块级变量修 ✅
- [x] **E5#62d** `src/core/linkdesk-api.ts`——加 `pluginRequest` 类型 ✅
- [x] 验证——壳 `requestToPlugin` → 串口注册 handler → `'pong: {"test":"hello"}'`。通道不存在 → 抛异常。三步全过 ✅

#### E5#63 🔴 invokeBeforeCloseTab 改用 requestToPlugin ✅

- [x] **E5#63a** 撤回 `confirmCondition`——删 `types.ts` 字段 + 删 `evaluateConfirmCondition` + 删 `plugin.json` 行 | −21 行 ✅
- [x] **E5#63b** `viewRegistry.ts` `invokeBeforeCloseTab`——`commands.execute` → `bridge.requestToPlugin(pluginId, 'invokeBeforeClose', {})`。`result === false` → 阻止关闭 ✅
- [x] 验证——confirmOnClose 弹窗正常 + 标签页关闭正常 + `npm run check` ✅

#### E5#64 🟡 串口插件注册 invokeBeforeClose handler

> 依赖 E5#62。串口插件在自己的 WebView 里处理关闭确认——查端口、弹确认、断开，壳零感知。

- [x] **E5#64** 最终方案——IPC `getStatus()` 查端口状态（替代 `portOpenRef`），纯 `linkdesk.*` API，零本地状态 ✅
- [x] 验证——端口未开→Ctrl+W 直接关 / 端口开着→弹确认→取消不关+确定断开 ✅
- [x] E5#74b `serial-handlers.ts`——广播串口数据到插件 WebView ✅
- [x] E5#74c `lsp-handlers.ts`——LSP 数据路由到插件 WebView ✅
- [ ] E5#74d `file-handlers.ts`——文件系统广播（待后续）

#### E5#65 🔴 推流 p2p 基础管道

> 插件→插件定向推流。对标串口→协议插件的直连需求。通用——任意插件可定向推数据到另一插件。

- [x] **E5#65a** `electron/ipc-bridge.ts`——`ipcMain.on('p2p:send', ...)` + `webContents.send('plugin:push')` ✅（主进程接收正常）
- [x] **E5#65b** `electron/preload-plugin.ts`——`p2p.send/on`（`on` 复用 `events.on`）✅
- [x] **E5#65c** `src/core/linkdesk-api.ts`——`p2p` 类型 ✅
- [ ] ⚠️ **阻塞于 E5#74**——`plugin:push` 不投递到插件 WebView，`events.on` 回调不触发。p2p 管道代码已就绪，等 E5#74 修

#### E5#67 🔧 弹窗归一化——插件 WebView 调壳 DialogService

> 利用现有 `PROXY_CHANNELS`（插件→壳代理）。插件在自己 WebView 里调 `linkdesk.dialog.confirm()` → IPC → 壳 `DialogService` → `ConfirmDialog` React 组件 → 结果返回。插件不需要 import `showConfirm`，不关心自己跑在哪。

- [x] **E5#67a** `electron/ipc-bridge.ts`——`PROXY_CHANNELS` 加 `'dialog:confirm'` + `'dialog:alert'` ✅
- [x] **E5#67b** `src/core/IpcBridgeHandler.ts`——加 `case "dialog:confirm"` / `"dialog:alert"` → 调 `DialogService.confirm/alert` ✅
- [x] **E5#67c** `electron/preload-plugin.ts`——加 `dialog` 命名空间：`confirm(message)` / `alert(message)` → `ipcRenderer.invoke` ✅
- [x] **E5#67d** `electron/preload-shell.ts`——加 `dialog.confirm/alert` 到已有 `dialog` 命名空间 ✅
- [x] **E5#67e** `src/core/linkdesk-api.ts`——加 `dialog` 类型 ✅
- [x] **E5#67f** 迁移 4 文件——`import { showConfirm }` → `linkdesk.dialog.confirm()` ✅<br>
  `serial-monitor/src/views/SessionListView.tsx`——侧栏删会话<br>
  `file-tree/src/FileTreeContextMenu.tsx`——右键删除文件<br>
  `file-tree/src/FileTreeDnD.ts`——拖放确认<br>
  `file-tree/src/views/SearchView.tsx`——替换确认
- [x] **E5#67g** `grep "import.*showConfirm" plugins/` 零结果。`npm run check` 零错误 ✅

---

### 🆕 第 4d-ter 轮：插件 linkdesk.* API 补全——通用命名空间替代 @src/core import

> 📖 **审计报告：** `memory/plugin-api-audit-checklist.md`——多 WebView 下 `import @src/core` = 静默隔离。
> **前置：** E5#62 `requestToPlugin`（壳→插件管道就绪）
> **设计原则：** 不是"看到一个断连加一个通道"，而是"插件真正需要哪些通用服务"——收束为 4 个命名空间。每个都是 `PROXY_CHANNELS` 加一行 + `IpcBridgeHandler` 加一个 case + preload 加一个方法。对标 VS Code 的 `vscode.window`/`vscode.workspace`——加的是通用原语，不是插件专属逻辑。

#### E5#68 🔴 `linkdesk.tabs`——标签页操作原语

> 替代 `useTabActions()` import。对标 VS Code `vscode.window.createTerminal()`。
> **API：** `tabs.create(pluginId, opts)`, `tabs.close(tabId)`, `tabs.focus(tabId)`, `tabs.openOrFocus(pluginId, opts)`

- [x] **E5#68a** `electron/ipc-bridge.ts`——`PROXY_CHANNELS` 加 4 个 tabs channel ✅
- [x] **E5#68b** `src/core/IpcBridgeHandler.ts`——case → `shellEvents.emit("tab:create/focus/close/openOrFocus", ...)` ✅
- [x] **E5#68c** `electron/preload-plugin.ts` + `preload-shell.ts`——`tabs.create/focus/close/openOrFocus` → `ipcRenderer.invoke` ✅
- [x] **E5#68d** 迁移 4 插件 10 文件——`useTabActions` → `linkdesk.tabs`。`grep "useTabActions" plugins/` 零残留 ✅

#### E5#69 🟡 `linkdesk.menu`——菜单注册原语

> 替代 `MenuRegistry` import。菜单项是纯数据（title/command/group/when），可直接 IPC 序列化。
> **API：** `menu.registerItems(items: MenuItem[])`

- [x] **E5#69a** `electron/ipc-bridge.ts`——`PROXY_CHANNELS` 加 `'menu:registerItems'` + `'menu:getItems'` ✅
- [x] **E5#69b** `src/core/IpcBridgeHandler.ts`——加 `case "menu:registerItems/getItems"` ✅
- [x] **E5#69c** `electron/preload-plugin.ts` + `preload-shell.ts`——加 `menu.registerItems/getItems` ✅
- [x] **E5#69d** `src/core/linkdesk-api.ts`——加 `menu` 类型 ✅
- [x] **E5#69e** 迁移 3 插件——marketplace + file-tree + editor → `linkdesk.menu.registerItems` ✅
- [x] **E5#69f** 🔴 ContextMenu 插件 WebView 走 IPC 取菜单——修复串口右键空壳 ✅
- [x] 验证——串口接收区右键 → 完整菜单 ✅

#### E5#70 🟡 `linkdesk.contextKey`——上下文键值原语

> 替代 `ContextKeyService` import。插件 SET 自己的状态供壳的 `when` 子句读。只 set，不跨 WebView get。
> **API：** `contextKey.set(key, value)`

- [x] **E5#70a** `electron/ipc-bridge.ts`——`PROXY_CHANNELS` 加 `'contextKey:set'` | ~1 行 ✅
- [x] **E5#70b** `src/core/IpcBridgeHandler.ts`——加 `case "contextKey:set"` → 调 `ContextKeyService.setValue` | ~3 行 ✅
- [x] **E5#70c** `electron/preload-plugin.ts` + `preload-shell.ts`——加 `contextKey.set(key, value)` | ~3 行 ✅
- [x] **E5#70d** `src/core/linkdesk-api.ts`——加 `contextKey` 类型 ✅
- [x] 验证——`linkdesk.contextKey.set('sourceOpen', true)` → 命令面板出现"暂停接收" ✅
- [x] **E5#70e** 迁移 5 文件——`ContextKeyService.setValue` → `linkdesk.contextKey.set` ✅

#### E5#71 🟡 `linkdesk.pluginState`——插件持久化存储原语

> 替代 `PluginStateService` import。pluginId 隐式绑定——主进程从 `event.sender` 推断，调用方不传。
> **API：** `pluginState.get(key)`, `pluginState.set(key, value)`

- [x] **E5#71a** `electron/ipc-bridge.ts`——`PROXY_CHANNELS` 加 `'pluginState:get'` + `'pluginState:set'` | ~1 行 ✅
- [x] **E5#71b** `src/core/IpcBridgeHandler.ts`——加 `case "pluginState:get/set"` → 调 `PluginStateService` | ~10 行 ✅
- [x] **E5#71c** `electron/preload-plugin.ts` + `preload-shell.ts`——加 `pluginState.get/set` | ~6 行 ✅
- [x] **E5#71d** `src/core/linkdesk-api.ts`——加 `pluginState` 类型 ✅（使用显式 pluginId 传参）
- [x] **E5#71e** 迁移 5 文件 ✅。`useSerialSessions.ts` 除外——模块级同步 init 无法用 async API，回退保留直接 import
- [x] **E5#71f** `useSerialSessions.ts` 重写——零 import @src/core。localStorage 同步 init + pluginState 异步更新。净删 203 行 ✅

#### E5#72 🟡 ESLint 规则——禁止插件 import @src/core

> 当前零约束。先建 API、再迁移、再加规则——否则现有代码全部报错。

- [x] **E5#72** `eslint.config.js`——`plugins/**` 禁 `@src/core/*`（warn 级）。白名单在 message 中声明 ✅

---

#### E5#74 🔴🔴 主进程数据推送多 WebView 化——`plugin:push` 投递修复

> 🔴🔴 2026-08-04 E5#65 验证时发现。**`plugin:push` IPC 完全不投递到插件 WebView。** 壳 `bridge.broadcast`、`events.emit`、`p2p.send` 全部依赖 `plugin:push` → 插件侧 `createEventSystem` 监听——全部静默失效。串口 `serial:data`、LSP `lsp:data` 同样只发 `mainWindow`。
> **影响：** 插件 WebView 收不到任何 `plugin:push` 事件（events.on、p2p.on、theme:changed extraHandler）。E5#65 p2p、E5#64 portOpenRef 均受阻。
> **范围：** 系统审计 `ipc-bridge.ts` `pushToPlugin`/`broadcast`—为何 `webContents.send('plugin:push')` 到不了插件 renderer。修复后所有 push 型 IPC 统一走 `broadcast`。

- [x] **E5#74a** 审计——列出所有 `mainWindow.webContents.send`（13 处）✅
- [x] **E5#74b** 🔴 `serial-handlers.ts`——`serial:data/stats/system` 广播到插件 WebView ✅
- [x] **E5#74c** 🟡 `lsp-handlers.ts`——`lsp:data` 路由到插件 WebView ✅
- [x] **E5#74d** 🟡 `file-handlers.ts`——`filesystem:changed` 遍历所有插件 WebView 广播（E5#80 完成）✅
- [x] **E5#74e** 🔴🔴 ~~pushToPlugin plugin:push 不到插件 renderer~~ → **E5#79 `event-system.ts` 重写**：每个 `events.on` 独立 `ipcRenderer.on`，去 subscriptions Map dispatch。contextBridge 代理回调问题已修。20+ commits 调查终结 ✅
- [x] 🔴 **验证 `notifyReady`/`setBounds` IPC 链——多 WebView 关停验证（2026-08-04）：** `plugin-view:ready` → `readyWebViewIds` ✅ / `setBounds` → `webViewBoundsReady` ✅ / 3 插件 React fallback 关停（`plugin-webview-placeholder` > 0）✅ / `npm run check` ✅。⚠️ `events.on/emit` 广播链未单独验证——E5#79 重写后待测。

---

#### E5#66 🟡 三机制文档 + 类型补全

- [x] **E5#66a** `01-壳通信骨架/多WebView三通信机制.md`——重写为实际状态（已完成/受阻/教训）✅
- [x] **E5#66c** `src/core/linkdesk-api.ts`——`pluginRequest`、`p2p`、`dialog` 类型已补 ✅
- [x] **E5#66b** `docs/03-插件制造/01-插件API契约.md`——完整重写，20 命名空间 + 禁止事项 + 壳 vs 插件差异表 | ~300 行 ✅

---

### 第 4e 轮：标签页系统——去歧义与关闭归一化（🆕 标签页审计）

> 📖 **专题文档：** [`01-壳通信骨架/标签页系统-去歧义与关闭归一化.md`](01-壳通信骨架/标签页系统-去歧义与关闭归一化.md)——同名标签无区分 / [×]中键关闭脏页静默失败 / 标签名被覆盖

#### E5#50 🔴 标签页去歧义——对标 VS Code

- [x] **E5#50** `TabBar.tsx`——渲染时检测同名 tab → 加父目录后缀 ` • parent/` | +18/−3 行 ✅
- [x] 验证——打开 `/A/main.c` + `/B/main.c` → `main.c • A/` + `main.c • B/` ✅

#### E5#51 🔴 [×]/中键/右键关闭脏标签页统一确认

- [x] **E5#51a** `useTabManager.ts closeTab`——dirty 时调 `showConfirm` → 确认后 `forceCloseTab` | +20/−12 行 ✅
- [x] **E5#51b** `MainContent.tsx closeActiveTab`——删重复的 dirty 确认逻辑 | −7 行 ✅
- [x] 修复 `reduceForceCloseTab` 浅拷贝 bug（dirty 永不清除）| +7/−3 行 ✅
- [x] 修复 `closeWithAnimation` await 时序 | +4/−4 行 ✅
- [x] 验证——Ctrl+W/[×]/中键/右键关闭——四条路径有未保存修改时行为一致 ✅

#### E5#52 🟡 标签名不被脏标记覆盖

- [x] **E5#52** `EditorTab.tsx`——`baseLabelRef` 存 baseName 只算一次，脏/净切换只加减 ● | +2/−5 行 ✅
- [x] `useTabManager.ts closeTab`——dirty 检测兼容 ● 前缀 | +3/−1 行 ✅

#### E5#53 🟡 tooltip 显示完整路径 ✅

- [x] `TabBar.tsx`——`tab.filePath` 优先，所有文件标签页悬浮显示完整路径 | −3/+1 行 ✅

#### E5#54 🔧 标签页生命周期——外部事件驱动归一化

> 📖 **专题文档：** [`01-壳通信骨架/标签页生命周期-外部事件驱动.md`](01-壳通信骨架/标签页生命周期-外部事件驱动.md)——TabManager 成为标签页生命周期的唯一权威

- [x] **E5#54a** `ShellEvents.ts`——已加 4 个事件（预存） ✅
- [x] **E5#54b** `useTabManager.ts`——集中订阅 4 事件 + forceCloseTab/updateLabel | +32 行 ✅
- [x] **E5#54c** 调用方迁移——FileTreeContextMenu emit file:deleted + FileTree emit file:renamed + WorkspaceService emit workspace:folderRemoved + MainContent 去重 forceCloseTab | +6/−10 行 ✅
- [x] 验证——文件树删文件 → 标签页关闭 / 重命名 → 标签页更新 / 卸载插件 → 标签页关闭 ✅

#### E5#55 🔧 Ctrl+Shift+T 恢复已关闭标签页——壳级命令

- [x] **E5#55a** `useTabManager.ts`——`restoreClosedTab` 已有，接线到 coreCallbacks | +2 行 ✅
- [x] **E5#55b** `coreCommands.ts`——注册 `workbench.action.reopenClosedEditor` + 快捷键 `Ctrl+Shift+T` | +12 行 ✅
- [x] 验证——Ctrl+W → Ctrl+Shift+T → 恢复。对编辑器/串口/设置标签页均有效 ✅

---

### 第 4c 轮：核心架构债（🆕 核心架构审计新增）

> 📖 **专题文档：** [`01-壳通信骨架/核心架构债.md`](01-壳通信骨架/核心架构债.md)——P0-P4：循环 import / 目录扁平 / Bridge 硬耦合 / 厨房水槽

#### E5#41 🔴 消 ConfigurationService ↔ ConfigurationApplier 循环

- [x] **E5#41a** 消循环——ConfigurationService 加 registerConfigApplier 注册模式，不再 import ConfigurationApplier | +5/−1 行 ✅
- [x] **E5#41b** `ConfigurationApplier` 模块级注册 applyConfiguration 回调 | +3 行 ✅
- [x] **E5#41c** 验证——循环消了 + 配置读写正常 ✅

#### E5#42 🟠 core/ 目录重组——52 文件扁平 → 7 子目录

> 每一步：建子目录 → 移动文件 → 更新 import → tsc 验证。做完一批 commit 一次。

- [x] **E5#42-1** 建 7 子目录——`registry/` `services/` `react/` `api/` `builtin/` `data/` `encoding/` | 0 行
- [x] **E5#42-2** RegistryBase → `registry/` + 更新引用（1 文件，约 13 个 import）| ~2 文件移动 + ~13 import 更新
- [x] **E5#42-3** 其余 12 个 Registry → `registry/`（Command/Configuration/ContextKey/FileDecoration/Icon/Keybinding/LangDef/Language/Menu/Protocol/StatusBar/Theme）| ~12 文件移动 + ~50 import 更新
- [x] **E5#42-4** Services → `services/`（Configuration/ConfigurationApplier/Dialog/FileAssociation/FileSearcher/File/LayoutEngine/Layout/IpcBridgeHandler/Notification/PluginState/Profile/Storage/Workspace/toast/pathUtils/assetPath）| ~17 文件移动 + ~80 import 更新
- [x] **E5#42-5** React hooks + contexts → `react/`（ShellEvents/CoreEvents/TabActionsContext/useConfiguration/usePluginIpcEvent/useSendData/SidebarTabSync）| ~7 文件移动 + ~40 import 更新
- [x] **E5#42-6** API types → `api/`（linkdesk-api/v3Api/types）| ~3 文件移动 + ~15 import 更新
- [x] **E5#42-7** 数据模块 → `data/`（DataConverter/DataDispatch/RingBuffer/CancellationToken/ProtocolParser/FactorySlots/LogChannel/CardRegistry）| ~8 文件移动 + ~20 import 更新
- [x] **E5#42-8** 壳内置 → `builtin/`（coreCommands/registerBuiltinProtocols）| ~2 文件移动 + ~10 import 更新
- [x] **E5#42-9** 创建 barrel `src/core/index.ts`——re-export 全部公开 API | ~40 行
- [x] **E5#42-10** 验证——`rm -rf node_modules/.vite` + `npm run check` + 全功能回归

#### E5#43 🟡 IpcBridgeHandler 接口反转

- [x] **E5#43a** `IpcBridgeHandler.ts`——定义 `PluginManagementAPI` 接口 + `setPluginAPI()` | ~20 行
- [x] **E5#43b** `loader.ts`——调 `setPluginAPI({...})` 注册自己。Bridge 不再 import loader | ~10/−10 行

#### E5#44 🟡 coreCommands 按域拆分

> 每一步：提取一类命令 → 新文件 → 更新 barrel → tsc 验证。做完一批 commit 一次。

- [x] **E5#44-1** 提取 `CoreCallbacks` 类型 → `builtin/CoreCallbacks.ts` | ~50 行搬
- [x] **E5#44-2** 提取标签页命令 → `builtin/tabCommands.ts`（closeActiveTab/nextTab/focusNthTab/toggleSplit/reopenClosedEditor）| ~40 行搬 + ~10 import
- [x] **E5#44-3** 提取设置命令 → `builtin/settingsCommands.ts`（openSettings/selectTheme/selectLanguage）| ~15 行搬
- [x] **E5#44-4** 提取开发者命令 → `builtin/developerCommands.ts`（toggleDevTools/reload）| ~15 行搬
- [x] **E5#44-5** 提取快捷键定义 → `builtin/shellKeybindings.ts`（所有 keybinding 数组）| ~20 行搬
- [x] **E5#44-6** 创建 `builtin/shellMenus.ts`——壳级右键菜单注册 | ~30 行
- [x] **E5#44-7** `coreCommands.ts` → 改为 barrel：`export * from "./builtin/..."` | −400/+8 行
- [x] **E5#44-8** 验证——`npm run check` 零错误

#### 🆕 E5#44c 🔥 shellMenus——壳级右键菜单提供方

- [x] **E5#44c** ViewTitleContext（折叠/重置）✅ + TabContext 已有（关闭/关闭其他/分屏）✅ + ExtensionGear 已有 ✅

#### 🆕 E5#44d 🔥 shellMenus——Views 子菜单

- [x] **E5#44d** Views 子菜单 ✅——ContextMenu children 支持 + resolveChildren 动态填充 + ViewContainerService.toggleViewVisibility | +65 行

---

### 第 4f 轮：View header 右键菜单（🆕 壳菜单归属）

> E5#44c 做提供方（shellMenus.ts 注册菜单项），本轮做消费方（SidePanel 渲染 ContextMenu）。提供方和消费方分离——SidePanel 不知道菜单里有什么。

#### E5#60 🔧 SidePanel view header 右键——消费方

- [x] **E5#60a** `SidePanel.tsx` header 加 onContextMenu → `<ContextMenu menuId={ViewTitleContext}>` + 订阅 view:toggleCollapse/view:resetPosition | +35 行 ✅
- [x] **E5#60b** 验证——右键 FOLDERS header → 弹出折叠/重置。npm run check 零错误 ✅
- [ ] **Views 子菜单**：待动态 children 基础设施就绪后加（shellMenus toggleViewVisibility 命令+event 已就绪）

---## 第 2 层：归一化——每件事只有一种做法

> ⚠️ 依赖第 1 层（ShellEvents 类型系统——归一化 API 设计有编译期确保）。

---

### 第 5 轮：插件加载归一化——loadPluginLifecycle 唯一入口

> 📖 **联合设计：** `C:\Users\fengy\.claude\plans\e5-serene-eagle.md`——第 5+6 轮联合，共享不可变原则。
> 📖 **专题文档：** [`02-归一化/插件加载归一化.md`](02-归一化/插件加载归一化.md)

**铁律（来自两次失败）：**
- glob manifest 只读 → 永不 `manifest.xxx = value`，推导用局部变量
- `normalizeManifest` 不设空 `contributes` → 只有真正有旧字段时才创建
- 加载管线不知道 UI 的存在——不 import IconBar/SidePanel/TabBar

#### E5#12 三路径归一化为 loadPluginLifecycle

- [x] **E5#12-1** `parseContributions` → `export function` | `loader.ts` ~1 行
- [x] **E5#12-2** 删 `loadThemePlugin` 函数 | `loader.ts` −20 行
- [x] **E5#12-3** 删 `loadLanguagePlugin` 函数 | `loader.ts` −25 行
- [x] **E5#12-4** `loadViewPlugin` → `loadPluginComponent`，删内部 langDefs 解析 | `loader.ts` ~10 行
- [x] **E5#12-5** `parseContributions` 加 `langDefs` 分支——从 loadViewPlugin 迁出 | `loader.ts` +5
- [x] **E5#12-6** 新建 `loadPluginLifecycle`——所有插件的唯一入口 | `loader.ts` +20
- [x] **E5#12-7** `loadPlugin` 简化——删三路分发，只调 `loadPluginLifecycle` | `loader.ts` −20/+3
- [x] **E5#12-8** `loadPluginRuntime` 也走 `loadPluginLifecycle` | `loader.ts` −15/+3
- [x] **E5#12-9** 加 `normalizeManifest`——纯函数，浅拷贝旧字段到新 contributes，不 mutate | `loader.ts` +20
- [x] **E5#12-10** 迁移 3 个旧格式插件 plugin.json 到 `contributes` | 3×plugin.json ~3 行
- [x] **E5#12-11** 🛡️ 健壮性——`loadPluginLifecycle` 内每步 try/catch，出错不阻塞其他 | `loader.ts` +5
- [x] **E5#12-12** 验证——settings 能打开、主题/语言正常、`npm run check` 零错误

---

### 第 6 轮：插件声明系统——pluginRole + appearsIn

> 📖 **联合设计：** `C:\Users\fengy\.claude\plans\e5-serene-eagle.md`——第 5+6 轮联合。
> 📖 **专题文档：** [`02-归一化/插件分类系统-四轴模型.md`](02-归一化/插件分类系统-四轴模型.md)
> 对标 VS Code：不判断类型，只查能力。`pluginRole`/`appearsIn`/`factoryRole`/`contributes` 四轴正交。

**铁律：**
- 推导在消费侧，不在加载侧 → `appearsIn` fallback 仅 `viewRegistry.ts` 一处
- 插件自声明 → 5 个 plugin.json 各写各的，壳只读
- `pluginRole` 推导用局部变量，不写 manifest

#### E5#13 pluginRole——加载策略

- [x] **E5#13-1** `types.ts` 加 `pluginRole?: "view" | "data"` | +3
- [x] **E5#13-2** `plugin.schema.json` 加 `pluginRole` 定义 | +5
- [x] **E5#13-3** `loadPluginLifecycle` 内局部变量推导 `role`（第 5 轮建好的函数中加）| +2
- [x] **E5#13-4** Python plugin.json 加 `"pluginRole": "data"` | +1

#### E5#14 appearsIn——声明式 UI 位置

- [x] **E5#14-1** `types.ts` 加 `appearsIn` 类型，`iconLocation`/`viewRole` 标 `@deprecated` | +7
- [x] **E5#14-2** `plugin.schema.json` 加 `appearsIn` 定义 | +5
- [x] **E5#14-3** `viewRegistry.ts` `getIconLocation` 读 `appearsIn.iconBar ?? iconLocation` | ~2
- [x] **E5#14-4** `viewRegistry.ts` `getTabCreatableViews` 读 `appearsIn.tabBar === true` | ~2
- [x] **E5#14-5** `IconBar.tsx` 精确过滤 `=== "top"` / `=== "bottom"` | ~2
- [x] **E5#14-6** 5 个 plugin.json 加 `appearsIn`，删 `iconLocation`/`viewRole` | ~15
- [x] **E5#14-7** `types.ts` + `plugin.schema.json` 删 `iconLocation`/`viewRole`/`keepSidebarOnFocus` | ~5
- [x] **E5#14-8** 验证——清缓存 + npm run check + 手动全功能回归

---

### 第 7 轮：剪贴板 Provider 归一化

> 📖 **专题文档：** [`02-归一化/剪贴板Provider.md`](02-归一化/剪贴板Provider.md)——含预测 Bug E5-15a/16a + ClipboardProviderRegistry 完整 API

#### E5#15 ClipboardProviderRegistry 桌子

- [x] **E5#15a** 新建 `src/core/ClipboardProviderRegistry.ts`——`register(pluginId, { when, onCopy, onCut, onPaste, onDelete, onSelectAll })` / `unregisterAll(pluginId)` / `resolve(focusContext)` | ~40 行
- [x] **E5#15b** 🛡️ 覆盖检测——同 `when` → `console.warn` 旧/新 pluginId | ~5 行
- [x] **E5#15c** 🛡️ 继承 `RegistryBase`——unregisterAll 自动对称 | ~3 行
**验证：** vitest——register→resolve 返回正确 provider→unregisterAll→resolve 返回 null。

#### E5#16 核心快捷键注册——Ctrl+C/V/X/A/Delete/F2

- [x] **E5#16a** 核心注册快捷键 `Ctrl+C` → `core.clipboardCopy` handler → `ClipboardProviderRegistry.resolve(focusContext)?.onCopy()` | `coreCommands.ts` ~10 行
- [x] **E5#16b** 同上——`Ctrl+V` / `Ctrl+X` / `Ctrl+A` / `Delete` / `F2` | `coreCommands.ts` ~30 行
- [x] **E5#16c** context key `inputFocus` 判定——input/textarea 聚焦时不走剪贴板 Provider（走浏览器原生）| `coreCommands.ts` ~3 行
**验证：** 在文件树中 Ctrl+C → 走 file-tree 的 onCopy→文件路径入剪贴板。在编辑器中 Ctrl+C → 走 Monaco 原生。

#### E5#17 现有插件迁移——声明式改 Provider

- [x] **E5#17a** file-tree 插件注册 ClipboardProvider `{ when: "explorerFocus", onCopy, onCut, onPaste, onDelete }` → 删旧快捷键声明 | `FileTreeContextMenu.tsx` + `plugin.json` ~15 行
- [x] **E5#17b** ~~editor 插件注册 ClipboardProvider~~ → N/A：Monaco 自行处理剪贴板，无需迁移 | —
**验证：** 文件树 Ctrl+C → 复制文件路径。编辑器 Ctrl+C → 复制文本。各自独立但快捷键只在一处注册。

---

### 第 8 轮：InlineInput 归一化

> 📖 **专题文档：** [`02-归一化/InlineInput组件.md`](02-归一化/InlineInput组件.md)——含预测 Bug E5-18a/18b + 两阶段聚焦模式 + 10+ 处迁移清单

#### E5#18 InlineInput 共享组件

- [x] **E5#18a** 新建 `src/components/shared/InlineInput.tsx`——props: `size`(compact/normal) / `width?` / `selectMode`(all/nameOnly) / `value` / `onConfirm(value)` / `onCancel()` / `autoFocus` / `placeholder?` / `onChange?` / `ref→getValue()` | 160 行
- [x] **E5#18b** 新建 `src/components/shared/InlineInput.css`——颜色/边框/聚焦环走 CSS 变量、两种 size token、width fill 模式、focus 过渡 150ms | 35 行
- [x] **E5#18c** 🔥 退出清理——useEffect + requestAnimationFrame 恢复全局快捷键 + 重聚焦。对标 E4V#27 教训 | `InlineInput.tsx` ~15 行
- [x] **E5#18d** 🛡️ 两阶段聚焦——第一阶段 ref 绑定到 DOM、第二阶段 `requestAnimationFrame` 选文本 + focus。对标 E4V#27 模式 | `InlineInput.tsx` ~10 行
**验证：** 组件独立可用——`<InlineInput size="compact" value="hello.ts" selectMode="nameOnly" onConfirm={...} />` → 只选中 "hello"。

#### E5#19 现有行内编辑迁移

- [x] **E5#19a** FileTree rename——替换原生 `<input>` 为 `<InlineInput size="compact" selectMode="nameOnly" />` + F2 归入壳 ClipboardProvider 分发 | `FileTreeNode.tsx` −37/+17 行
- [x] **E5#19b** 串口会话 rename——替换原生 `<input>` 为 `<InlineInput size="compact" />` + 双击 + F2 走壳分发 + ✓/✕ 按钮 | `SessionListItem.tsx` −31/+12 行
- [x] **E5#19c** 设置搜索框——替换原生 `<input>` 为 `<InlineInput size="normal" />` + `onChange` 即时模式 | `SettingsView.tsx` −7/+8 行
**验证：** 三个场景外观/行为一致。F2 重命名 → 输入框样式和串口发送区相同组件。

---

### 第 9 轮：FileService API 命名归一化

> 📖 **专题文档：** [`02-归一化/FileService-API命名.md`](02-归一化/FileService-API命名.md)——含完整重命名表 + IPC handler 同步检查

#### E5#20 API 重命名

- [x] **E5#20a** `mkdir` → `createDir`（与 `writeFile`/`readFile` 动词+名词一致）| `FileService.ts` + `electron/services/file-service.ts` + 全项目调用点 ~15 处 ✅
- [x] **E5#20b** `deleteEntry` → `remove`（与 preload 的 `remove` 一致）| 同上 ~10 处 ✅
- [x] **E5#20c** `watchFile` → `watch`（去掉误导性的 File 后缀——目录也能 watch）| 同上 ~5 处 ✅
- [x] **E5#20d** `listDir` 保留——`Dir` 缩写与 `createDir` 一致 | 零行 ✅
- [x] **E5#20e** `exists` 保留——语义准确 | 零行 ✅
**验证：** `grep "mkdir\|deleteEntry\|watchFile" src/ electron/ plugins/` 仅剩注释/文档。`npm run check` 零错误。

---

## 第 3 层：功能补全

> ⚠️ 不阻塞 04/05——可以在 04 进行中穿插。

---

### 第 5b 轮：IPC Channel 命名归一化（🆕 API 审计新增）

> 📖 **专题文档：** [`02-归一化/IPC与API命名归一化.md`](02-归一化/IPC与API命名归一化.md)——含 clipboard handler 补全 + 全部 channel 重命名表

#### E5#29 🔴 补 clipboard IPC handler——致命缺失

- [x] **E5#29a** 新建 `electron/ipc/clipboard-handlers.ts`——`registerClipboardHandlers()`: `clipboard:readText` / `clipboard:writeText` | +15 行 ✅
- [x] **E5#29b** `electron/main.ts` 中调 `registerClipboardHandlers()` | +2 行 ✅
- [x] **E5#29c** 验证——`npm run check` 零错误 ✅

#### E5#31 🔧 IPC channel 命名归一化

- [x] **E5#31a** `theme-changed` → `theme:changed`——全项目 grep 替换 | ~5 处 ✅
- [x] **E5#31b** `preload-ready` → `app:preloadReady` | ~3 处 ✅
- [x] **E5#31c** `heartbeat` → `app:heartbeat` | ~3 处 ✅
- [x] **E5#31d** `serial.getPorts` → `serial.listPorts`（preload-plugin.ts 方法名统一）| ~3 处 ✅
- [x] **E5#31e** `bridge:push-to-plugin` → `bridge:pushToPlugin` | ~3 处 ✅
- [x] **E5#31f** 验证——`grep "theme-changed\|preload-ready\|heartbeat\|getPorts\|push-to-plugin" electron/` 仅注释 ✅

#### E5#20 融入——类型补全 + 回调归一化

- [x] **E5#20f** `LinkDeskAPI` 补 `serial/clipboard/env/language.getInitial/language.onChange/pluginViews.notifyReady` 类型 | ~50 行——融入 E5#20 ✅
- [x] **E5#20g** 回调模式归一化——`makeListener` + 自定义 per-channel → 统一 `listenDirect` 模式 | 融入 E5#1 ShellEvents ✅

---

### 第 7b 轮：插件安装单一路径（🆕 API 审计新增）

> 📖 **专题文档：** [`02-归一化/插件安装单一路径.md`](02-归一化/插件安装单一路径.md)——两套安装/卸载 IPC 路径归一化

#### E5#32 🔧 插件安装/卸载单一路径

- [x] **E5#32a** 审计两条路径——`plugins:install`(shell 直调) vs `plugins:call`(bridge)——副作用差异清单 | ~20 行 ✅
- [x] **E5#32b** 归一化——bridge 为唯一入口。删 `plugins:install/uninstall/reinstall` handler，loader 走 linkdesk.filesystem | ~45/+23 行 ✅
- [x] **E5#32c** 验证——Marketplace 安装和手动复制安装都走 pluginManager API。tsc + vitest 通过。grep `plugins:install\|plugins:uninstall` 零残留 ✅

---

### 第 8b 轮：插件目录结构规范（🆕 目录审计新增）

> 📖 **专题文档：** [`02-归一化/插件目录结构规范.md`](02-归一化/插件目录结构规范.md)——标准模板 + 四个官方插件迁移方案

#### E5#30 规范定义

- [x] **E5#30a** 新 `docs/03-插件制造/09-插件目录规范.md`——标准目录结构 + 每类文件判断标准 | ~50 行 ✅
- [x] **E5#30b** 第三方开发者看任一官方插件——目录结构一致（E5#33 完成后验证）

#### E5#33 官方插件迁移

- [x] **E5#33a** editor——14 文件分类迁移（views/services/components/styles/__tests__）+ 补 icon.svg | ~20 文件移动 ✅
- [x] **E5#33b** file-tree——16 文件分类迁移（services/utils/components/styles + 保留 views）| ~16 文件移动 ✅
- [x] **E5#33c** marketplace——5 文件分类迁移（services/components/styles + 保留 views）| ~5 文件移动 ✅
- [x] **E5#33d** serial-monitor——8 文件分类迁移（services/hooks/components/styles + 保留 views/components）| ~8 文件移动 ✅
- [x] **E5#33e** 🔥 验证——`rm -rf node_modules/.vite` + `npm run check` + 全功能回归 ✅（tsc 零新错误，vitest 385 passed，UI 全功能回归通过）

---

> 📖 **专题文档：** [`03-功能补全/设置object编辑器.md`](03-功能补全/设置object编辑器.md)——含 ObjectEditor 组件完整代码

#### E5#21 SettingsView object 类型编辑器

- [x] **E5#21a** `SettingsView.tsx` `renderControl()` switch 加 `"object"` case——渲染键值对编辑器 | `SettingsView.tsx` ~50 行
- [x] **E5#21b** 全类型——添加行/删除行/toggle boolean for bool value + text input for string + number input for number + 嵌套 ObjectEditor for nested object + 数组编辑器 for array。不"暂显示"——现在就全覆盖 | ~40 行
- [x] **E5#21c** `renderControl()` switch 同时加 `"array"` case——和 object 共用 ObjectEditor（array 的 key 是数字索引）| ~10 行
- [x] **E5#21c** 对标 VS Code——每行一条键值对，key 列 + value 列（boolean → 切换开关、string → inline input）| `SettingsView.tsx` ~20 行
- [x] **E5#21d** 🔥 `FileTreeModel.refresh()` 清+重载原子化——提取 `_reloadItem()` 私有方法，有路径/无路径两分支共用。防"忘了一步就炸"的复制粘贴 bug | `FileTreeModel.ts` ~10 行
**验证：** `files.exclude` 在设置 UI 可编辑——添加 `"*.log": true` → 文件树 .log 文件消失。改过滤→立即刷新，无需手点。

---

### 第 9b 轮：硬编码路径归一化（🆕 路径审计新增）

> 📖 **专题文档：** [`02-归一化/硬编码路径归一化.md`](02-归一化/硬编码路径归一化.md)——render 路径相对化 + glob 常量化 + getAssetPath 审计

#### E5#34 🔧 plugin.json render 路径相对化

- [x] **E5#34a** 8 个 plugin.json——`render` 字段从 `../../plugins/builtin/.../src/views/...` 改为 `src/views/...`（相对于插件根）| ~8 处
- [x] **E5#34b** `loader.ts:467`——`import()` 前拼接插件根路径 | ~5 行
- [x] **E5#34c** 验证——侧栏全部 section 正常渲染

#### E5#35 🔧 glob 模式常量化

- [x] **E5#35a** 新建 `src/core/pluginPaths.ts`——`PLUGINS_DIR`/`PLUGIN_SUBDIRS`/`PLUGIN_ENTRY_FILES` 常量 + glob 工厂函数 | ~70 行
- [x] **E5#35b** `loader.ts`——glob 注释引用 pluginPaths.ts 为权威来源（Vite TS 类型不认 object 参数，工厂函数搁置，改用注释引用方案）| +2 行
- [x] **E5#35c** `plugin-shell-main.tsx`——glob 注释引用 pluginPaths.ts | +1 行
- [x] **E5#35d** `vite.config.ts:21`——`['builtin', 'user']` 改为 import `PLUGIN_SUBDIRS` | +3/−2 行

---

### 第 9c 轮：i18n 硬编码中文清理（🆕 翻译审计新增）

> 📖 **专题文档：** [`02-归一化/i18n硬编码中文清理.md`](02-归一化/i18n硬编码中文清理.md)——38 处硬编码中文 + 35 翻译 key 缺失

#### E5#37 🔥 先做——补 en.json 翻译（否则 E5#36 后英文用户看到中文 key 裸奔）

- [x] **E5#37a** `plugins/user/lang-defaults/en.json`——补 ~36 个缺失 key（三代理审计后去重）| ~37 行
- [x] **E5#37b** 验证——切英文 → 全部 UI 文字英文、无中文 key 裸奔

#### E5#36 🔧 硬编码中文 → t()（42 处——三代理扫描，原审计 38 + 新增 4）

- [x] **E5#36f** `MainContent.tsx`（2）+ `StatusBar.tsx`（1）+ `TabBar.tsx`（1）+ 🆕 `SidebarSection.tsx`（1）+ 🆕 `PluginDetailView.tsx`（2）| ~7 行
- [x] **E5#36d** `ThemeBrowser.tsx`（4）+ `LanguagePicker.tsx`（2）| ~6 行
- [x] **E5#36e** `WindowControls.tsx`（3）+ `ProgressBar.tsx`（1）+ `ToastContainer.tsx`（3）| ~7 行
- [x] **E5#36b** `NotificationCenter.tsx`——7 处（`formatTimeAgo()` + 来源标签——模块级需 `import i18n`）| ~7 行
- [x] **E5#36c** `plugin-shell-main.tsx`——5 处（插件加载错误文本——模块级 `i18n.t`）| ~5 行
- [x] **E5#36a** `App.tsx`——11 处（config description + 串口错误 toast + command title/category）| ~11 行
- [x] **E5#36g** 验证——tsc ✅ / ESLint 1 预存 error / vitest 385 passed（7 预存）

> 🆕 本轮不碰：plugin.json 命令/描述（~100+ 处，量太大）、EditorTab.tsx、FileTreeContextMenu.tsx、主题 JSON、StatusBar chord 提示——另开轮次

---

### 第 9d 轮：错误处理归一化（🆕 审计新增）

> 📖 **专题文档：** [`02-归一化/ErrorService统一错误处理.md`](02-归一化/ErrorService统一错误处理.md)——5 种错误处理归一化为一个 reportError()

#### E5#38 🔧 新建 ErrorService

- [x] **E5#38a** 新建 `src/core/ErrorService.ts`——`reportError({ message, source?, error?, severity?, silent? })`。零依赖——不 import 任何可能形成循环依赖的模块 | ~36 行
- [x] **E5#38b** 验证——ErrorService 零依赖（toast 动态 import，叶子节点），任何模块 import 无循环依赖

#### E5#39 🔧 逐处替换

- [x] **E5#39a** FileService——9 个方法加 try/catch + reportError。保留兜底返回值、加日志 | +28/−18 行
- [x] **E5#39b** ConfigurationService 监听器——`catch { }` → `catch (e) { console.error(...) }`——2 处 | +2/−2 行
- [x] **E5#39c** CommandRegistry——`import("./toast")` → `import { reportError }` | +5/−10 行
- [x] **E5#39d** App.tsx 串口——`setLastError` + 追加 `reportError()`（保留状态栏 + 新增 toast）| +4 行
- [x] **E5#39e** loader——错误 `pushToast` → `reportError()`——2 处 | +3/−3 行
- [x] **E5#39f** 验证——tsc ✅ / vitest 385 passed（7 预存）

---

### 第 11 轮：openInTerminal 配置化 + 设置 uiHint

> 📖 **专题文档：** [`03-功能补全/openInTerminal配置化.md`](03-功能补全/openInTerminal配置化.md)、[`03-功能补全/设置UI提示-uiHint.md`](03-功能补全/设置UI提示-uiHint.md)

#### E5#22 terminal.external.windowsExec 配置项（现有）

#### E5#57 🔧 设置 uiHint——声明式编辑控件

- [x] **E5#57a** `plugin.schema.json`——`ConfigurationProperty` 加 `uiHint?: "fontFamily" \| "color" \| "file" \| "directory"`。不认识的 hint 降级回 type 默认——不抛错 | ~8 行
- [x] **E5#57b** `SettingsView.tsx` `renderControl`——hint 优先于 type | ~20 行
- [x] **E5#57b1** `renderControl`——`hint === "fontSize"` → `<input type="number" min=8 max=72 step=1>`（数字步进，不是普通文本框）| ~3 行
- [x] **E5#57c** `FontFamilySelect`——`document.fonts` → Canvas 测等宽(`"i"` vs `"W"`) → SelectBox 下拉 | ~30 行
- [x] **E5#57d** `FilePathInput`——input + 📂 按钮 → Electron dialog | ~20 行
- [x] **E5#57e** 🔥 应用到编辑器配置——`editor.fontFamily` → `uiHint: "fontFamily"` / `editor.fontSize` → `uiHint: "fontSize"` / 有 enum 的自动变下拉（如 `editor.fontWeight` 声明 `enum: ["normal","bold",100,200,...,900]`）| `editor/plugin.json` ~5 行
- [x] ⚠️ 验证——打开设置 → 字体从文本框变成字体下拉 → 字号变成数字步进 → 有 enum 的变成 SelectBox。不再有输入框裸奔

- [x] **E5#22a** file-tree plugin.json 加 `terminal.external.windowsExec`（enum: powershell/cmd/wt/git-bash/**custom**）+ `terminal.external.customCommand`（自定义命令模板，`{{dirPath}}` 占位符）| `plugin.json` ~15 行
- [x] **E5#22b** `electron/main.ts` handler——switch 加 `case "custom"`：读 `customCommand` 模板→`replace("{{dirPath}}", dirPath)`→exec。**不硬编码 4 个选项——用户填 wsl/alacritty/wezterm 都行** | `main.ts` ~20 行
**验证：** 设置里改 `terminal.external.windowsExec` 为 `cmd` → 右键目录→在终端中打开→弹出 cmd 而不是 PowerShell。

---

## 第 4 层：技术债清理

> ⚠️ 最低优先级——不阻塞任何功能。

---

### 第 12 轮：FileEntry 类型合并

> 📖 **专题文档：** [`04-技术债/FileEntry类型合并.md`](04-技术债/FileEntry类型合并.md)——shared/types.ts 唯一定义

#### E5#23 合并到 shared/types.ts

- [x] **E5#23a** 新建 `shared/types.ts`——`FileEntry` 接口唯一定义（含 `isReadonly?` / `isDirectory` / `name` / `size?` / `mtime?`）| ~15 行
- [x] **E5#23b** `src/core/FileService.ts`——删本地 `FileEntry` 定义，import from `shared/types.ts` | ~5 行
- [x] **E5#23c** `electron/services/file-service.ts`——删本地 `FileEntry` 定义，import from `shared/types.ts` | ~5 行
- [x] **E5#23d** 🛡️ 验证——改 `FileEntry` 加一个字段 → tsc 在两端同时检查 | 零行（靠 tsc）
**验证：** `grep "interface FileEntry" src/ electron/` 只返回 `shared/types.ts` 一处。`npm run check` 零错误。

---

### 第 13 轮：Twistie 不同步修复

> 📖 **专题文档：** [`04-技术债/Twistie不同步修复.md`](04-技术债/Twistie不同步修复.md)——含完整调用链时序分析 + async await 修复

#### E5#24 DnD 后 twistie 状态同步

- [x] **E5#24a** 定位根因——`FileTreeDnD.ts` `handleDrop` 中 `refreshDir` 调用链 → `model._expanded` 状态和实际 UI 不同步 | 调查 ~10 行
- [x] **E5#24b** 修复——`refreshDir` 完成后显式同步 `_expanded` Set（从实际渲染的 children 状态反推）| `FileTreeDnD.ts` ~10 行
- [x] **E5#24c** 🛡️ 验证——拖放文件到目录→松手→twistie 图标与实际状态一致→点一次展开 | 手动测试
**验证：** 6 步 DnD 测试清单——拖放后 twistie 不再不同步。

---

### 第 12b 轮：防线加固——tsc + ESLint + vitest（🆕 防线审计新增）

> 📖 **专题文档：** [`04-技术债/防线加固-tsc-eslint-vitest.md`](04-技术债/防线加固-tsc-eslint-vitest.md)——vitest node→jsdom / tsc 覆盖 plugins/user/ / CardRegistry ESLint error

#### E5#45 🔧 vitest 环境修复

- [x] **E5#45** `vitest.config.ts` L7——`environment: "node"` → `"jsdom"` | 1 行 ✅
- [x] 验证——`npx vitest run` 328/335 通过（7 个预存失败：UTF-8 Uint8Array 比较 + FileTreeDnD showConfirm jsdom 适配，非 E5 引入）

#### E5#46 🔧 tsconfig 覆盖 plugins/user/

- [x] **E5#46** `tsconfig.json`——`exclude: ["plugins/user"]` → 删除 | −1/+1 ✅
- [x] 验证——`npx tsc --noEmit` 零新增错误 ✅

#### E5#47 🔧 CardRegistry ESLint error

- [x] **E5#47a** ✅
- [x] **E5#47b** E5#46 暴露的新 tsc 错误 = 0 个 ✅

---

### 第 13b 轮：E2E smoke test（🆕 集成测试）

#### E5#56 🔧 E2E 冒烟测试——5 个最关键用户路径

- [x] **E5#56a** 打开文件树 → 双击 .tsx → Monaco 语法高亮 → Ctrl+S 保存 → ● 消失 → F12 跳转定义 → 光标正确 → Ctrl+W 关闭
- [x] **E5#56b** 点 📁 → 侧栏展开 → 点 🪢 → 侧栏切换 → 点 📁 → toggle 关闭 → 侧栏折叠到 4px 竖条 → 再点 📁 → 恢复 280px
- [x] **E5#56c** 打开两个同名文件 → 标签页显示 `main.c • src/` 和 `main.c • tests/` → 去歧义正确
- [x] **E5#56d** Ctrl+C 复制文件 → Ctrl+V 粘贴到另一个目录 → 文件出现 → Ctrl+Shift+T → 标签页恢复
- [x] **E5#56e** F5 刷新 → 布局恢复 / 侧栏恢复 / 工作区恢复 / 展开状态恢复

### 第 14c 轮：壳内硬编码清理——加插件零壳改动（🆕 硬编码审计新增）

> 审计发现 3 处壳代码残留硬编码——每次加新插件或替换现有插件，壳代码需要改动。修复后对标 VS Code：插件 = 放入目录即生效，壳代码零改动。

#### E5#58 🔴 消灭 FALLBACK_META 标签表——兜底标签改为读 manifest.name

- [x] **E5#58a** `tabIdentity.ts`——FALLBACK_META 删 label 字段，fallbackLabel 从 manifest.name 自动读；保留 identityField 兜底 | −10/+5 行 ✅
- [x] **E5#58b** `FALLBACK_META`——`@deprecated`，仅剩 identityField ✅
- [x] **E5#58c** 验证——加新插件不在 FALLBACK_META 加行，标签名从 manifest.name 自动读取。`npm run check` 零错误 ✅

#### E5#59 🔴 WorkspaceService 去 "file-tree" 硬编码——活跃工作区走自有命名空间

- [x] **E5#59a** `WorkspaceService.ts` L64,119——`setPluginStateValue("file-tree", "activeWorkspace", ...)` → `setPluginStateValue("workspace", "activeWorkspace", ...)`。WorkspaceService 用自己名义存——不借 file-tree 的命名空间 | `WorkspaceService.ts` ~4 行
- [x] **E5#59b** 一次性迁移——读旧 key `file-tree.activeWorkspace` → 有值则搬到新 key `workspace.activeWorkspace` → 删旧 key | `WorkspaceService.ts` ~10 行
- [x] **E5#59c** 验证——`grep '"file-tree"' src/core/` 仅注释/文档。打开工作区 → F5 刷新 → 活跃工作区恢复。关闭再开 → 文件树展开状态恢复

---

- [x] **E5#25a** 三图标切换——📁→🪢→🛒→📁 每步内容正确、无闪烁、无空白
- [x] **E5#25b** 文件树——展开/折叠/拖放/搜索/重命名/右键菜单 正常
- [x] **E5#25c** 编辑器——双击打开/语法高亮/F12跳转/Ctrl+Click/保存/脏标记 正常
- [x] **E5#25d** 串口监视器——打开端口/收发数据/多标签页/侧栏设置 正常
- [x] **E5#25e** 插件市场——浏览/安装/卸载/齿轮菜单 正常
- [x] **E5#25f** 设置——修改配置/切主题/切语言 正常
- [x] **E5#25g** F5 刷新——布局/侧栏/工作区/标签页恢复 正常

#### E5#26 防线验证

- [x] **E5#26a** `npm run check` 零错误（tsc + ESLint + vitest）— 1 ESLint 已有(loader.ts replace) + 7 vitest 已有，非本次引入
- [x] **E5#26b** `grep "mkdir\|deleteEntry\|watchFile" src/ electron/ plugins/` → 仅注释/文档
- [x] **E5#26c** `grep "interface FileEntry" src/ electron/` → 仅 `shared/types.ts`
- [x] **E5#26d** `grep "pluginId === \"[a-z]" src/` → MainContent.tsx 两处（`"editor"`/`"serial-monitor"`）——多 WebView IPC 残余，单 WebView 下不执行。📌 随多 WebView 恢复时消灭
- [x] **E5#26e** `grep "?? \"top\"" src/pluginLoader/viewRegistry.ts` → 返回空（iconLocation 默认值已改）
- [x] **E5#26f** 新语言插件（Rust/Go 的 plugin.json）→ 不加 index.tsx → 语言高亮正常、无图标栏出现。已验证：python 插件（pluginRole: "data"，无 entry）

#### E5#27 vitest 补充——核心逻辑防线

- [x] **E5#27a** `ShellEvents.test.ts`——emit/on 类型匹配、unsubscribe 清理、事件追踪输出 | ~10 条 ✅ **测试已写，ShellEvents.ts 实现完后执行**
- [x] **E5#27b** `LayoutEngine.test.ts`——默认布局/自定义布局/重叠检测/像素对齐/resize 事件 | ~8 条<br>**📌 锁定到 E5#9：LayoutEngine.ts 实现完后立刻写，写完后才接入 App.tsx**
- [x] **E5#27c** `ClipboardProviderRegistry.test.ts`——register/resolve/覆盖检测/unregisterAll | ~6 条<br>**📌 锁定到 E5#15：ClipboardProviderRegistry.ts 实现完后立刻写**
- [x] **E5#27d** `loader.test.ts` 补充——pluginRole 推导/data 插件零视图/parseContributions 公共入口 | ~5 条<br>**📌 锁定到 E5#12：loader.ts 归一化后立刻写——这 5 条是删 loadThemePlugin 之前必须全绿的 checkpoint**
- [x] **E5#27e** `InlineInput.test.tsx`——渲染/selectMode/onConfirm/onCancel/自动 focus/清理恢复/onChange 即时回调 | 11 条<br>**📌 锁定到 E5#18：InlineInput.tsx 实现完后立刻写**

---

### 第 15 轮：AI 友好度验证

#### E5#28 AI 进场体验验证

- [x] **E5#28a** 新 AI 只读 `ShellEvents.ts` → 知道壳内全部通信事件 — 55 个 emit/on/event
- [x] **E5#28b** 新 AI 只读 `LayoutEngine.ts` → 知道加区域只需一行配置
- [x] **E5#28c** 新 AI 只读 `InlineInput.tsx` → 知道行内编辑用哪个组件——不用 grep 3 个文件
- [x] **E5#28d** 新 AI 只读 `plugin.schema.json` → 知道 `pluginRole` 是 data 还是 view — 24KB 完整 schema
- [x] **E5#28e** 新 AI 只读 `FileService.ts` → 知道 API 命名规则（动词+名词一致）— 18 个公有 API
- [x] **E5#28f** 🆕 `src/core/index.ts` barrel——71 行，导出 30+ 模块（事件 + 注册表 + 服务 + React + 数据 + 类型）。新 AI 进场 30 秒看清核心全部能力

---

## 完工验证

> 全部 E5#1–#28 勾完后执行。

- [ ] 加新壳区域（底部面板）→ 不改已有区域代码——新文件 + 1 行布局配置
- [ ] 换布局（侧栏换右边）→ 只改 LayoutEngine 布局配置——不改任何区域组件
- [ ] 加新语言插件（Rust/Go）→ 零代码，只声明 plugin.json（pluginRole: "data" + langDefs）
- [ ] 插件 console.log → 进自己的 WebView 控制台——不进壳控制台
- [ ] Ctrl+C 在任何焦点上下文 → 走到同一个核心 handler（`ClipboardProviderRegistry.resolve()`）
- [ ] 所有行内编辑 → 用同一个 `<InlineInput>` 组件——FileTree/串口/设置外观一致
- [ ] FileService API——`createDir`/`remove`/`watch` 命名统一
- [ ] `grep "interface FileEntry"` → 仅 `shared/types.ts`
- [ ] `grep "mkdir\|deleteEntry\|watchFile"` → 仅注释/文档
- [ ] `grep "?? \"top\"" src/pluginLoader/viewRegistry.ts` → 返回空
- [ ] `npm run check` 零错误（tsc + ESLint + vitest）
- [ ] 转 Tauri 框架 → 壳组件代码零改动（换 EventBus 底层实现即可）

---

## 执行顺序与依赖

```
第 1 层（骨架——先铺铁轨）
  第 1 轮：E5#1–#2   ShellEvents 类型系统        ← 全部依赖
  第 2 轮：E5#3–#8   壳内低耦合                   ← 依赖 E5#1–#2
  第 3 轮：E5#9      壳布局引擎                   ← 依赖 E5#3–#8
  第 4 轮：E5#10–#11 React Fallback 退役          ← 依赖 E5#3–#8 + E5#9

第 2 层（归一化——有类型系统后做更安全）
  第 5 轮：E5#12     插件加载归一化                ← 依赖 E5#1–#2
  第 6 轮：E5#13–#14 插件角色 + iconLocation       ← 依赖 E5#12
  第 7 轮：E5#15–#17 剪贴板归一化                  ← 依赖 E5#1–#2
  第 8 轮：E5#18–#19 InlineInput 归一化            ← 无依赖
  第 9 轮：E5#20     FileService 命名归一化        ← 无依赖

第 3 层（功能补全——可穿插 04/05）
  第 10 轮：E5#21    设置 object 编辑器            ← 无依赖
  第 11 轮：E5#22    openInTerminal 配置化         ← 无依赖

第 4 层（技术债——不阻塞任何功能）
  第 12 轮：E5#23    FileEntry 类型合并            ← 无依赖
  第 13 轮：E5#24    Twistie 修复                  ← 无依赖
  第 14 轮：E5#25–#27 回归 + vitest               ← 所有轮次
  第 15 轮：E5#28    AI 友好度验证                 ← 所有轮次
```

---

## 总结

| 层 | 内容 | 任务 | 行数 | 新增/删文件 |
|:--|------|:--:|:--:|:--|
| L1 | 壳通信骨架 | 47 (E5#1-#11 + E5#40-#44 + E5#48-#55 + E5#60 + 🆕 E5#61-#80) | ~1,130 | 8 新 |
| L2 | 归一化 | 33 (E5#12-#20 + E5#29-#39) | ~950 | 9 新 |
| L3 | 功能补全 | 2 (E5#21-#22) | ~120 | — |
| L4 | 技术债 + 回归 + E2E | 16 (E5#23-#28 + E5#45-#47 + E5#55-#56 + E5#58-#59) | ~305 + ~37 测试条 | 3 新 |
| **合计** | | **98** | **~2,500 + ~37 测试条** | **20 新文件** |

---

## 🔴 致命 Bug 速查（任务中的高风险点）

| Bug | 症状 | 任务 | 防线 |
|:--|:--|:--:|:--|
| Events 类型漂移 | emit/on 签名不匹配 → 运行时静默炸 | E5#1 | ShellEvents 类型表 → tsc 编译期检查 |
| 事件订阅泄漏 | unmount 后 handler 仍触发 → 白屏 | E5#3–#6 | useEffect cleanup 返回 unsubscribe |
| 布局坐标错位 | 区域重叠或空白 | E5#9 | 重叠检测 + 像素对齐 + 一次性 apply |
| iconLocation 图标消失 | 未声明插件图标全消失 | E5#14 | 先补声明再改默认值——不可颠倒 |
| 语言插件静默失效 | 归一化后 parseContributions 漏分支 | E5#12 | vitest 覆盖所有插件类型 |
| 剪贴板 Provider 覆盖 | 两插件同 when → 一个失效 | E5#15 | console.warn 覆盖检测 |
| React Fallback 白屏 | WebView 未 ready React 已关 | E5#10 | 等 ready 信号 + 5s 超时兜底 |
| 🆕 clipboard handler 缺失 | 插件调 `clipboard.readText()` → 异常 | E5#29 | 补 `electron/handlers/clipboard-handlers.ts` |
| 🆕 IPC channel 无命名空间 | `theme-changed`/`heartbeat` 裸奔 | E5#31 | 全部 `namespace:camelCase` |
| 🆕 安装双路径不同步 | 两套实现、不同副作用 | E5#32 | bridge 为唯一入口 |
| 🆕 插件目录迁移 import 炸 | 文件移动后 Vite 缓存旧路径 | E5#33 | `rm -rf node_modules/.vite` + 全量 check |
| 🆕 回调模式三足鼎立 | makeListener/eventSystem/自定义 | E5#20g | 融入 E5#1——统一 createEventSystem |
| 🆕 FALLBACK_META 残留 | 加新插件需在兜底表加行 → 标签名硬编码 | E5#58 | label 改为读 manifest.name——删表 |
| 🆕 WorkspaceService 认插件 | 核心服务直接引用 "file-tree" ID | E5#59 | 活跃工作区状态移到自有命名空间 |
| 🆕 UI/UX 合规——裸 hex | `PluginDetailView.css:172` / `OutputPanel.css:114,118` / `IconBar.css:80` 共 4 处硬编码颜色——token 已存在（`--warning`, `--error`, `--status-connected`）组件未引用 | E5#81a | 改 4 行 CSS——裸 hex → `var(--xxx)` |
| 🆕 UI/UX 合规——emoji 图标 | `PluginDetailView.tsx` / `WelcomeView.tsx` / `WorkspaceView.tsx` / `PluginIcon.tsx` ~12 处 emoji 用于导航/标签/按钮。根因：Lucide 未安装。修复：`npm i lucide-react` + PluginIcon 改 Lucide 优先 + 逐文件替换 | E5#81b | Lucide 化——零 emoji 在 UI 中 |
| 🆕 UI/UX 合规——z-index 随机 | `IconBar.css:162 99999` / `ColorPicker.css 9998+9999` / `WindowControls 3000` / `HamburgerMenu 2550` / `TitleBar 2548` / `ProgressBar 2546` / `StatusBar 2545` / `ToastContainer 2545`——~10 处未走分层 token | E5#81c | 归入分层表 `var(--z-*)` |
| 🆕 UI/UX 合规——间距 + 动画 | 结构间距 `10px`/`14px` 不在 4px 刻度 + `tab-drop-pulse 600ms` 超 300ms 上限 | E5#81d | 大块间距整到 4px + 动画缩到 ≤300ms |

---

## AI 友好度检查清单（每任务执行前自问）

> 来源：E3+E4 十堂课 + 硬约束 19 条

- [ ] 涉及路径拼接？→ `normalizePath()` / `joinPath()`——防 `\` 混入
- [ ] 涉及动态 import？→ 外裹 try/catch + console.error 明确报哪个文件
- [ ] 涉及事件订阅？→ useEffect cleanup 返回 unsubscribe——防 StrictMode 双重订阅 + 僵尸回调
- [ ] 涉及 useEffect + 回调 prop？→ 加 `if (!active) return` 守卫——防闭态副作用
- [ ] 涉及 async init？→ `_loadingPromise` 模式——防 StrictMode 竞态
- [ ] 涉及持久化？→ `PluginStateService` / `LayoutService`——禁止 localStorage
- [ ] 新建文件在 `plugins/` 下？→ `npm run lint` 必须扫到
- [ ] import 核心类型？→ `import type { ... } from "@src/core/..."`——不重定义
- [ ] 硬编码颜色/文字/路径/pluginId？→ CSS 变量 / `t()` / `getAssetPath()` / 声明字段
- [ ] 新增注册表逻辑？→ register + unregisterAll + onDidChange 三件套——缺一个就是 bug
- [ ] 卸载路径？→ `revertXxxIfCurrent` 在 cleanup 之前——不是之后
- [ ] 两个入口写同一逻辑？→ 归一化到一个函数——不走两条路径（L6 预防）
- [ ] emit 事件类型？→ `ShellEvents` 接口里声明——tsc 检查签名
- [ ] 加新壳组件？→ 只 emit/on ShellEvents——不 import 邻居

---

> **← 索引：** `00-README.md`
> **← 上一 Phase：** `../E4_文件树与编辑器_暂定/`
> **→ 下一 Phase：** `../../04-出厂制造/`（⚠️ 2026-09-13 重组：04-出厂制造 已拆并入 `docs/04-软件更新/` + `docs/05-插件更新/`，出厂批次概念退役）
> **对标模板：** E3.6 `05-执行清单.md` + E4 `06-执行清单.md`
