# 第 1.21b 轮 · `#109l-b`：**共享组件域类名归一 ＋ 作者轴发版**（件 4 收官·**下**）

> **一句话**：`src/components/shared/**` 里还有 **52 个非 `ldk-` 的独立类定义**（`.form-row` / `.inline-input` / `.number-input*` / `.ctx-*` / `.colorpicker-*` / `.sidebar-*` / `.theme-*` …）＋ **临时短前缀一族**（`.tbadge` / `.tname` / `.pv-dot` …，是 `.badge` 案当时随手起的）＋ **1 个关键帧**（`selectbox-in`）。本轮一律加 `ldk-`，把门禁**判据①** 也改成结构性判定 —— 于是**两个定义域**的规矩合起来只剩一句：**宿主与共享组件定义 ＝ `ldk-` 开头 · 插件定义 ＝ `<pluginId>-` 开头**。
> **🔴 本格与 1.21 的唯一区别：动的是 `@linkdesk/ui` 的单一真源。** `packages/linkdesk-ui` 的 `prepack` 从 `src/components/shared/` 重建产物，而共享组件 CSS 是**打进插件 bundle** 的（非 external）⇒ 改这里 = **同时动作者轴**：`@linkdesk/ui` 要真发 npm、4 只消费方要重发、必须**解包已发产物核**（`npm run check` 看不见）。这正是它从 1.21 拆出来的原因——**不是同一种活**。
> **编号**：`#109l-b`（接 `#109l` 的**同件下半**）；轮次号 1.21b **不等于执行序**
> **🔴 执行序：= [22 号档](22-收口总方案-跨方样式污染九件套.md) §四 轮次表第 2 序**，**紧接 1.21 之后**（同一件轴的另一半，紧接着做 ⇒ 作者轴只发一次版、插件只重发一次）；之后是 **1.22（运行时探针）**，**1.20 恒为最后**。
> 🔴 **本格定的名字是「终名」（用户 2026-09-16 硬约束：共享组件库以后只做加法、不改已有组件名）** ⇒ **本格不是「清存量」而是「给定终身」**：起名质量是本格**最高优先级之一**——临时短前缀一族必须并进各自族、加前缀后不得与任何既有名撞车（跨域也要核）。依据与推论见 [22 号档 §10.4](22-收口总方案-跨方样式污染九件套.md)。
> **本档怎么用**：开工先通读，§七 是「进场第一步」的复量命令；**面清单只用来对账，不许当输入**——进场自己跑一遍（记忆 `ai-relay-one-round-per-session`）。
>
> **非新能力声明（`check-design-flow` §8.4 ③）**：本轮是**存量类名归一 ＋ 一条既有门禁判据的形态变更 ＋ 一次既有包的常规发版**，不新增能力、不改视觉语义。⇒ **无需新能力设计前置**。

---

## 〇、拍板记录

> **为什么拆出来单开一格（2026-09-16 用户拍板）**：1.17 实测撞见这 52 个名字时，记的是**交接队列表第 18 行**；1.21 立格时把「并进同一轮 / 单开一格 / 挂账」留给执行者判。用户 2026-09-16 的收口指示（「补全全部，后面细分任务颗粒度……任务量不要太大也不要太少」）把它**拍板为「拆」**，理由：
> 1. **两种活**：1.21 是纯壳内改名（插件产物不受影响）；本格动 ui 真源 ⇒ **强制作者轴发版**（npm publish ＋ 4 只消费方重发 ＋ 解包复核 ＋ 目录/种子）。
> 2. **先例**：本系列「一仓一格」（件 2 落地拆成 ①–⑧），作者轴发版一向**自成一格**（1.14⑧）。
> 3. **粒度**：1.21 已是 244 名 / 334 站点的上限量；再加 52 名 / 58 站点会超「一轮的量」。
>
> **并入的原始账**：交接队列表**第 18 行**（「共享组件还有 52 个非 `ldk-` 的独立类定义，而尺子结构性看不见」——用户 2026-09-16 原话「**开个 1.21 任务，主要就是审查和完成你说的这个任务**」）。

---

## 一、事实与读数（**2026-09-16 实测，可复跑**——§七 给了命令）

### 1.1 权威读数（射程 = 门禁**判据①** 的域）

| 项 | 读数 |
|:--|:--|
| 扫描域 | `src/components/shared/**/*.css`（**16 个文件**） |
| **非 `ldk-` 独立定义** | **52 个名字** |
| 带祖先/复合定义点（非 `ldk-`） | 12 |
| 非 `ldk-` 关键帧 | **1**（`selectbox-in`） |
| TS(TSX) 站点（class 语境） | **9 文件 / 58 处** |
| **正控**（不许删光） | 已带 `ldk-` 的 **41 个**名字 |
| 三方名 | `codicon*` —— 与这 52 个**交集 = 0**（机械依据同 1.21 §1.3） |

### 1.2 🔴 跨仓代价：**只有 1 处**（这是本格能不能安心做的关键读数）

把 52 个名字拿去扫 **18 只官方插件仓**（`.css` ＋ `.ts(x)`，排除 `node_modules` / `dist`）⇒ **只有 1 个名字有跨仓消费**：

| 被消费的名 | 谁 | 在哪 | 形态 |
|:--|:--|:--|:--|
| `.theme-picker` | `settings` | `src/views/SettingsView-rows.css:127,131` | `.settings-row:has(.theme-picker)` ＋ `.settings-row:has(.theme-picker) .settings-row-control` |

⇒ **代价 = 1 处 / 1 只插件**。落地时**同笔**改 settings 那两行（`:has(.ldk-theme-picker)`）并走一次插件发版，否则**主题选择器那两张设置行的版式会静默失效**（`:has()` 不再匹配——**静默**，正是这一类问题的形态）。

### 1.3 ⚠️ 三种命名形态都在那 52 里，别只认第一种

| 形态 | 例子 | 处置 |
|:--|:--|:--|
| ① 裸语义名 | `.form-row` · `.inline-input`（＋`--compact`/`--normal`）· `.number-input*` · `.ctx-*` · `.colorpicker-*` · `.sidebar-*` · `.markdown-*` | 加 `ldk-` |
| ② **临时短前缀** | `.tbadge` · `.tname` · `.tname-name` · `.pv-bar` · `.pv-dot` · `.pv-dots`（`ThemePicker.css`）——**`.badge` 案当时随手起的短前缀**，既不属 `ldk-`、也不属任何插件 id | **一并归位**（`ldk-` 开头）——**别当成「已经有前缀了」跳过去** |
| ③ 状态类复合 | `.x.active` / `.x.on` / `.x.show` … | **不动**（只作复合后缀出现，本身就是硬约束 23 ③ 要的合法形态） |

### 1.4 时序与失效点（**决定发版顺序**，沿用 00 档 §1.3 的机制纠正）

- 共享组件 CSS **打进插件自己的 bundle** ⇒ **只改壳 ＋ 只发 ui，对已发布插件不构成即时破坏**（它们自带旧 ui 的 CSS，自洽）；
- **真正的失效点** = 插件下次**升级 `@linkdesk/ui`** 的那一下：它 bundle 内的 scoped 调优（§1.2 那一处）失配，样式**静默**不再生效；
- ⇒ **壳与 ui 可以先发（零破坏窗口），各消费方必须同批适配重发**，否则它们下次升 ui 时才炸。

---

## 二、要做（五件同笔，缺一件就是半成品）

### 1. 命名规则（与 1.21 同款，机械、保留可读性）

**规则 = 原样加 `ldk-` 前缀，区域段保留**（不做二次语义化、不做删减）：

| 现在 | 改后 |
|:--|:--|
| `.form-row` | `.ldk-form-row` |
| `.inline-input` / `--compact` / `--normal` | `.ldk-inline-input` / `.ldk-inline-input--compact` / `.ldk-inline-input--normal` |
| `.theme-picker` | `.ldk-theme-picker` |
| `.tbadge` / `.pv-dot`（临时短前缀族） | `.ldk-tbadge` / `.ldk-pv-dot` |
| 已带 `ldk-` 的 **41 个** | **一字不动** |

- ⚠️ **加前缀后不得与既有名字撞车**：跑「改前集合 × 改后集合」求交，**交集必须为空**。
- ⚠️ **关键帧同批判**：`selectbox-in` → `ldk-selectbox-in`，**同笔改 `animation:` 引用**（漏引用 = 动画静默消失，[1.14⑤](17-任务-件2落地③-⑦-改名轮通用作业书.md) 踩过）；它同时在 [1.15](11-任务-保留名清单单一真相源.md) 的登记表 `keyframes` 与两棵树 §12 关键帧表里 ⇒ **改名字要同笔改三处**（登记表 ＋ 中英两棵树 §12），`check-reserved-names-doc-sync` 双向盯着。

### 2. 源码面

**16 个 CSS 文件**的定义点 ＋ **9 个 TS(TSX) 文件的 58 处**渲染点（`className` / 模板串 / `classList` / 选择器常量表——⚠️ 非 JSX 消费点别漏）。

### 3. 门禁改形态（**本格只改判据①**）

`scripts/check-css-namespace.mjs` 里 `if (name.includes("-")) continue;` 的**第二处**（**判据①**，约 `:164`）⇒ 改成**结构性判定**（该域**顶层独立类定义必须 `ldk-` 开头**），并**同笔更新 `--self-test` 判据① 的正控/负控**：负控 = 往共享组件 CSS 塞一个**带连字符**的裸名（形如 `.bar-baz`）该红。🔴 **自测必须接线**（记忆 `gate-selftest-must-be-wired`）。
- ⚠️ **1.21 已把判据③ 改成结构性** ⇒ 本格改完后**两处都是结构性**（这是件 4 的终态）；自测要覆盖**混合态**（1.21 与本格之间的中间态）与**终态**两种。
- ⚠️ **语义变更别忘**：判据① 现在是「共享组件**新增**裸定义 ⇒ 红」（靠登记表比对），改完后是「**存在**非 `ldk-` 独立定义 ⇒ 红」。

### 3b. 🔴 门禁替换（**本格必须同笔做，否则会留下一个「失效的假判据」**）

> 2026-09-16 补测发现：**本格把共享组件的独立定义全部改成 `ldk-` 之后，门禁判据② 会自然失效**——它的输入是「共享组件的**裸**定义」，而裸定义在本格之后**清零** ⇒ 判据② 从此**报不出任何东西**（一个安静的假判据，正是**件 8** 要治的病）。

🔴 **本格必须**：判据② **退役**（连同它的自测负控一起改），**并补一条新判据⑥**——

> **判据⑥ · `ldk-` 名跨域唯一性**：**宿主域 与 共享组件域 各自「独立定义」的同名 `ldk-` 类 ⇒ 红**。
> 理由：1.21 ＋ 本格之后，**两个域共用同一个 `ldk-` 命名空间**——宿主定义 `.ldk-x` 与共享组件定义 `.ldk-x` 若同名，**宿主的元素会被共享组件的样式命中**（`.badge` 案同形），而今天**没有任何一条判据看着这件事**。
> **负控**：往宿主 CSS 塞一条共享组件已有的 `ldk-` 名 ⇒ 当场红。

**实测现状（2026-09-16）**：宿主域已有 `ldk-` 名 **6** 个（`ldk-about` · `ldk-input` · `ldk-rn` · `ldk-setting-group` · `ldk-titlebar` · `ldk-vta`）· 共享组件域 **40** 个 ⇒ **交集 = 0** ⇒ 判据⑥ 今天**零存量**（纯预防性，零重发成本）。

### 3c. 🔴 起名按「**族段**」（判据⑦ 的第一份输入就是本格定的那批新名字）

> 用户 2026-09-16 原则：**能机械防住就不要靠记忆 ＋ 文档**。共享组件已达 **24 个**目录、新组件（各类下拉菜单等）还没开始做 ⇒ 名字必须**按族归属**，不许自由发挥。

**族段规则（判据⑦ —— 本格起名时即遵守，门禁在 1.26 落地）**：
> 每个共享组件目录内定义的 `ldk-*` **独立定义**，其**族段必须与该目录的组件族一致**；族 = 该目录内那个「最短的 `ldk-` 根类名」去掉 `ldk-` 后的段。BEM 修饰 `--` 与元素后缀 `__` **允许**（实测 `segmented-radio__label` / `segmented-radio__option` 已在用 `__`）。

**实测（2026-09-16，探针 `scratch/probe-mechanizability.mjs`，可复跑）**：24 个组件目录里 **8 个目录今天没有任何 `ldk-` 名字** —— 那正是本格要清的 **49** 个：

| 目录 | 待命名 | 族段（应据此起名） |
|:--|:--:|:--|
| `color-picker` | 14 | `colorpicker`（既有 `ldk-` 名用的就是它） |
| `sidebar-section` | 12 | `sidebar-section` ⚠️ 内含 **族外** 名 `sidebar-pane-sash` |
| `context-menu` | 8 | `ctx`（既有 `ldk-` 名用的就是它） |
| `number-input` | 4 | `number-input` |
| `theme-picker` | 4 | `theme-picker` ⚠️ 内含 **族外** 名 `theme-card` / `theme-preview` |
| `inline-input` | 3 | `inline-input` |
| `segmented-radio` | 3 | `segmented-radio`（含 `__` 后缀） |
| `form-row` | 1 | `form-row` |

⚠️ **两处族外名字必须在本格裁决并记账**：`sidebar-pane-sash`（在 `sidebar-section/` 目录）· `theme-card` / `theme-preview`（在 `theme-picker/` 目录）—— 按族段规则它们该并进各自族（形如 `ldk-sidebar-section-*` / `ldk-theme-picker-*`）；**改还是留、为什么，必须写进交接段**（本系列「不许静默不做」的纪律）。
⚠️ 同笔订正 §1.3 形态② 那条：`.tbadge` / `.pv-dot` 一族**不是**改成 `ldk-tbadge` / `ldk-pv-dot` 就完事 —— **按族段规则它们要并进 `theme-picker` 族**（`tbadge` 不属于任何族）。**最终拼写由本格定，但必须族一致**。

### 3d. 🔴 空转判据盘点（**「无死代码」原则的门禁版**——缺这一条，本格会留下一批「每天跑绿、什么都拦不住」的假判据）

> 用户原则第 4 条是**无死代码**。本系列把一批名字清零之后，**有些判据的输入会跟着清零** ⇒ 它们会**继续在 `npm run check` 里跑绿，却永远报不出东西**。这**等价于死代码**，且比死代码更坏（它给人「有门禁」的错觉）。

**本格必须逐一处置（退役 ／ 保留并说明 ／ 改造），并写进交接段**：

| # | 判据 / 腿 | 为什么会空转 | 建议处置 |
|:--:|:--|:--|:--|
| 1 | 壳 `check-css-namespace` **判据②（跨组件借用）** | 输入 = `ownedBy`（共享组件的**裸**定义）⇒ 本格清零后为空 | **退役** ＋ 由**判据⑥**（`ldk-` 名跨域唯一性）接管同类保护（§3b） |
| 2 | 壳 `check-css-namespace` **判据⑤ 的 classes 部分**（`registry.shared` / `registry.host` 反向核对） | 登记表 `classes` 清空后无可核对 | **classes 部分退役**；⚠️ **`keyframes` 部分保留**（它还有真输入 —— 关键帧表 ＋ 两棵树 §12） |
| 3 | 🔴 **SDK 保留名腿**（`packages/plugin-sdk/src/eslint/checks/reserved-classes.ts`） | **脚本头自己写着**：「在『裸定义』这一轴上，**前缀判据完全覆盖本判据**」⇒ 它在**件 2 之后就已经空转**（**既有死判据，不是本格造成的**） | **点名给件 8 ／ 1.28 处置**（本格**不改 SDK**，避免跨轴）；交接段必须带上这条 |
| 4 | `check-reserved-names-doc-sync` 的**类名表**部分 | 两棵树 §12 的「保留名」表会空 | 判「**表与判据一起退役** ／ **保留空表并说明**」；⚠️ **关键帧表与它的判据保留** |

⚠️ **纪律**：退役任何判据**必须连同它的 `--self-test` 正负控一起改**（否则留下一个测着不存在东西的自测）；**不许**把空转判据塞进豁免清单了事（那是把「无死代码」变成「有名单」）。

### 4. 登记表与作者面

- `packages/plugin-sdk/schemas/reserved-class-names.json` 的 **`classes.shared`**（自件 1 起就是空数组）⇒ 判「**删字段 / 留空数组**」。**两处都结构性之后，`classes` 整块已无用** ⇒ 判「整块删」并在 §七 记账（**别留一个永远为空的字段当装饰**）。
- 同笔改：**作者面两棵树 §12**（中英同笔）· `scripts/check-reserved-names-doc-sync.mjs`（对账登记表 ↔ 两棵树 §12 表）· SDK 的 **schema 四份字节相等**纪律。
- ⚠️ `11-样式命名空间审计.md` 的重算**由 1.21 同笔做完**（那张「共享组件 111 / 裸定义 8」的表按两个域一起算）⇒ 本格**只核对、不重算**；若发现 1.21 漏了，当场补并记账。

### 5. 🔴 **作者轴发版（本格特有，顺序铁律）**

1. **`@linkdesk/ui` 真发**：按 [作者轴 npm 发版 runbook](../../../../06-发布管理/作者轴npm发版.md) 五步（`bump` → `publish --registry=https://registry.npmjs.org` → `release:mark` → 同笔提交）。⚠️ 本机默认源是**镜像**，`npm publish/update` 必须显式带 registry。
2. **4 只消费方重发**（settings / serial-monitor / file-tree / marketplace）：bump lock ＋ `verify` ＋ `build` ＋ GitHub Release ＋ 官方目录收录 ＋ 出厂种子。
   - ⚠️ **`file-tree` 与 `marketplace` 的 lock 仍停在 `ui@0.2.0`**（1.17 只动了前两只）⇒ 本格**顺手对齐**（这是 1.17 留下的尾巴账，与本格同轴，别再往后传）。
3. **`settings` 那 1 处 scoped 调优同笔改**（§1.2）——**它就是本格唯一的跨仓破坏面**。
4. 🔴 **解包复核（本格特有，不许省）**：解包**已发插件产物**（`.linkdesk-plugin` / `dist` 里的 `index.bundle.css`）核一遍——判据：**旧名零命中、新名有命中**。`npm run check` **看不见这一层**（1.17 的教训；也是 [E6#110](../../插件规范化层/07-任务-作者轴黄灯观察名单补齐.md) 的活体样本）。
5. **两棵树 §12.2（迁移说明）追加一节**：告诉作者「这些共享组件名也跟着加了 `ldk-`」（跟 1.10 那次同款）。
6. **顺序**：**发版要晚于所有消费方改完**（[1.14⑧](18-任务-件2落地⑧-发版与文档收口.md) 的顺序铁律）——本格动的是「壳内定义 ＋ ui 真源」，消费方（插件 bundle 里那份）**要跟着重发才能拿到新名**；读完 1.14⑧ 再定先后，别凭直觉。

---

## 三、判据

| 判据 | 要求 |
|:--|:--|
| **残留（本轮主判据）** | **共享组件域**的非 `ldk-` 独立定义 = **0**（改前 **52**）；非 `ldk-` 关键帧 = **0**（改前 1） |
| **正控** | 已带 `ldk-` 的 **41 个**名字**仍在**（不许一刀切删光） |
| **第三方名不被卷入** | `codicon*` 一字未动 |
| **形状证明** | 「改后 = 改前 ＋ 只加 `ldk-` 前缀」**逐字节证明**，零视觉声明变化 |
| **撞车** | 改前集合 × 改后集合 **交集 = 空** |
| **门禁** | `check-css-namespace` 绿（**判据①③ 双双结构性** ＋ `--self-test` 正控/负控覆盖混合态与终态）· `check-reserved-names-doc-sync` 绿 · `docs:check` 绿 · 作者面四道门禁绿 |
| **负控（必须真跑）** | 往**共享组件** CSS 塞 `.bar-baz`（**带连字符**）⇒ 当场红；还原后**逐字节相同** |
| **跨仓消费面** | `settings` 那 2 行**已改**且**重发后产物里为新名**（否则版式静默失效） |
| **🔴 解包复核** | 4 只消费方产物里**旧名零命中 / 新名有命中**（`npm run check` 看不见） |
| **全量** | `npm run check` **EXIT=0** |
| **实机** | 壳是活软件 ⇒ 实机核对（至少：设置页（含对象编辑器）· 侧栏 · 上下文菜单 · 主题选择器那两张设置行（§1.2 的破坏面）· 颜色选择器 · 内联输入 · 数字输入 · 分段单选 · Markdown 视图）；隔离 profile ＋ CDP；**做一条负控**：同文档里旧名 vs 新名各建一个元素读计算样式 |
| **发版** | `version-bump` skill §〇 多轴模型：**软件轴 ＋ ui 轴 ＋ 插件轴（4 只）**都要判；**种子/目录**同笔 |
| **探针记账** | 按 [22 号档](22-收口总方案-跨方样式污染九件套.md) §九.3：跑一遍静态探针（1.22 之前）并把**改前/改后**读数写进交接段 |

---

## 四、禁区

- 🔴 **不许引白名单 / 基线 / 棘轮**（本系列一贯禁区）。判据写「**独立定义**必须 `ldk-`」，三方名自动出射程。
- 🔴 **不许顺手改视觉**（只加前缀、不改一个属性值）。
- 🔴 **不许只改 CSS 不改 TSX**（漏一个渲染点 ＝ 那个元素**当场没样式**，而门禁查不出「名字对但漏改」）。
- 🔴 **不许顺手改本格射程外的东西**：宿主域（归 1.21）· **CSS 变量**（件 6 · token 轴）· **元素/通配/id 选择器**（件 7 · 选择器形态轴）。
- 🔴 **不许跳过解包复核**（§二.5.4）——「本地 check 全绿」在本格**证明不了**产物正确。
- 🔴 **不许并进 1.20**（1.20 是复核轮，只点名、不救火）。
- ⚠️ 照旧：不跳 `npm run check` · **壳仓另有写手 ⇒ 只按路径 `git add`** · 推前**现测代理**（`127.0.0.1:7890` 当晚会掉，**直连反而通**；推不动时两条各测一次）· 推完**必须 `git ls-remote --heads origin <branch>` 对 SHA** · ⛔ **撤版只能用户本人**。

---

## 五、交棒要求

1. 收尾四件套：本系列 [交接.md](交接.md) 顶部追加一段（只追加不改旧段）· [E6-执行清单.md 第 1 层头](../../E6-执行清单.md) 「轮次进度」行 · 勾格勾全套 · **队列表整张复制**并把自己那格改 ✅（⚠️ 那行进度是**单行四万字符级**的，写完必须回读结构）。
2. **下一棒 = 1.22（`#109m` · 运行时全表断言探针进仓 ＋ 首份基线）**——任务书 [24](24-任务-运行时全表断言探针.md) 已备可直接读。
3. 🔴 **本格完成后，「件 4」才真正收官** ⇒ 交接段必须写明「**件 4 = 宿主 ＋ 共享组件 双域终态读数**」（两格合起来才是终态，别只报自己那半）。

---

## 六、与其它格的关系（一句话各）

- **[1.21](21-任务-宿主类名统一前缀.md)（紧接前序）**：宿主域 244 名 ＋ 7 关键帧 ＋ **判据③** 形态变更 ＋ `classes.host` 清空。**本格接手时它已落地**；两格合起来 = 件 4 终态。
- **[1.22](24-任务-运行时全表断言探针.md)（紧接后序）**：把 CDP 探针提进 `scripts/` ＋ 首份基线；它会**对 `1.21b^` 与 `HEAD` 各跑一次**，补出类名/关键帧轴的**改前改后对账**。
- **1.20（恒最后）**：本身是它复核清单里的一项（「共享组件类名是否一律 `ldk-`」＋「4 只消费方产物是否已带新名」）。
- **[E6#110](../../插件规范化层/07-任务-作者轴黄灯观察名单补齐.md)（系列外）**：本格**正是它的活体样本第二次现场**（1.17 是第一次）——「改共享组件时 `@linkdesk/ui` 那盏黄灯不亮」⇒ **本格必须自己解包核**（§二.5.4）；若 E6#110 恰好排在本格附近，两格可互引读数，但**依赖方向是 E6#110 → 本格**（本格不因它延期）。
- **件 6（token 轴 · 1.23/1.24）**：本格**不动 CSS 变量**；但注意 `src/components/shared/SelectBox.css` / `MarkdownView.css` 各有 **1 处**自定义属性定义（合计 2 处）——那是 **1.23 的射程**，本格只加类名前缀时**别顺手改它们**（会污染两格的读数）。

---

## 六b、⚠️ 两把尺子差 3 名（**未对账**，进场以你自己的读数为准）

官方探针（`scratch/audit-109l-scope2.json`）报共享组件域**非 `ldk-` 独立定义 52**；2026-09-16 的另一把尺子（`scratch/probe-hostname-selectors.mjs` 里的 `defs()`）报 **49**。差 `sidebar-pane-view` / `sidebar-section` / `sidebar-section-body` 三个名（**只在官方那把里**）。
⚠️ 另注：官方那份 `名字清单` **只列非 `ldk-` 名**（不含已有前缀的 40 个）——追这个数时别把两件事混了。
⇒ **进场用 §七 ① 各跑一遍；与 49/52 不符就以你自己的为准并写明差异**（这正是 [22 号档](22-收口总方案-跨方样式污染九件套.md) §一 层 3 那条教训的又一次现场）。

## 七、🔴 进场第一步：怎么把读数复量出来（别信面清单）

```bash
# ① 共享组件域的射程（只读）
node scratch/audit-109l-scope2.mjs
#    取其中「共享组件域」那一行：16 CSS 文件 / 非 ldk- 独立定义 52 / 关键帧 1 / TS 9 文件 58 处
#    ⚠️ 脚本在 scratch/（.gitignore 内，不进仓）——丢了照它头部口径重写

# ② 跨仓消费面（= 本格唯一的破坏面；期望 = 1 处 / settings .theme-picker）
grep -rn "theme-picker" E:/linkdesk-plugins/official/*/src --include=*.css --include=*.tsx | grep -v node_modules

# ③ 消费方 lock 停在哪一版（1.17 留的尾巴账）
grep -n "@linkdesk/ui" E:/linkdesk-plugins/official/*/package.json E:/linkdesk-plugins/official/*/package-lock.json | grep -v node_modules | head

# ④ 门禁两处盲区的现状（1.21 之后应只剩判据① 一处）
grep -n 'includes("-")' scripts/check-css-namespace.mjs

# ⑤ 临时短前缀族（§1.3 形态②，最容易漏）
grep -rn "\.tbadge\|\.tname\|\.pv-dot\|\.pv-bar" src/components/shared/
```

**进场的第一个动作**：用 ① 的读数核对 §1.1 表；**与表不符就以你自己的读数为准**并写进交接段。
