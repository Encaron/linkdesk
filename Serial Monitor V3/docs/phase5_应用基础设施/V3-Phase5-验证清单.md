# Phase 5 验证清单

> 手动测试。每个 checkbox 需要手动操作后打勾。✅ = 通过，❌ = bug，⚠️ = 不符预期但不阻塞。
> 每次改完代码后跑一遍。
>
> **批次标记：** `[5a]` = Registry 暗线完成后可测，`[5b]` = 右键归一化完成后可测……`[5f]` = 全部完成后可测。

---

## 通用前提（每次都必须过）

- [ ] `[5a]` `npx tsc --noEmit` → 零错误
- [ ] `[5a]` `npx vitest run` → 全部通过
- [ ] `[5a]` `npx tauri dev` → 窗口正常打开
- [ ] `[5a]` 终端收发：开 COM → 发 AT → 看到回显
- [ ] `[5a]` 切亮色主题 → UI 不崩
- [ ] `[5a]` 切英文 → UI 不崩
- [ ] `[5a]` 关闭所有标签页 → 欢迎页出现
- [ ] `[5a]` 拖拽分屏 → 正常

---

## 一、命令系统 `[5c]`

- [ ] Ctrl+Shift+P → 命令面板弹出
- [ ] 搜索 "清空接收区" → Enter 执行 → 接收区清空
- [ ] 搜索 "暂停接收" → Enter 执行 → 暂停切换
- [ ] 搜索 "关闭标签页" → Enter 执行 → 当前标签页关闭
- [ ] 模糊搜索：输入 "清空" → 匹配 "清空接收区"
- [ ] 终端插件注册的命令出现在面板中
- [ ] 非终端插件注册的命令出现在面板中

## 二、快捷键 `[5d]`

- [ ] 终端清空 → 绑定快捷键 → 按下 → 执行清空
- [ ] 终端聚焦时快捷键生效 → 其他标签页聚焦时不生效（when 条件）
- [ ] 修改 keybindings.json → 重载后生效

## 三、右键菜单（归一化——重点） `[5b]`

### 标签页右键

- [ ] 标签页右键 → 菜单出现（关闭/关闭其他/关闭右侧/向下分屏/向右分屏）
- [ ] 点空白处 → **菜单消失**
- [ ] 按 Escape → **菜单消失**
- [ ] 拖动窗口 → **菜单消失**（window.blur）
- [ ] Alt+Tab 切走 → **菜单消失**

### 终端接收区右键

- [ ] 接收区右键 → 菜单出现（复制/全选/清空/暂停）
- [ ] 点空白处 → **菜单消失**
- [ ] 按 Escape → **菜单消失**
- [ ] 滚动 CM6 → **菜单消失**（scroll capture）
- [ ] 拖动窗口 → **菜单消失**
- [ ] Alt+Tab → **菜单消失**

### 快捷发送右键

- [ ] 快捷发送药丸右键 → 菜单出现（编辑/删除）
- [ ] 点空白处 → **菜单消失**
- [ ] 按 Escape → **菜单消失**

### 齿轮菜单（插件市场）

- [ ] 插件 ⚙ 点击 → 菜单出现（启用/禁用/卸载）
- [ ] 点空白处 → **菜单消失**
- [ ] 按 Escape → **菜单消失**
- [ ] 插件已禁用 → 齿轮菜单显示 "启用"
- [ ] 插件已启用 → 齿轮菜单显示 "禁用"

### 归一化确认

- [ ] 以上四处右键菜单 → 外观一致（样式/间距/字体）
- [ ] 以上四处右键菜单 → 失焦行为一致
- [ ] 同时弹出两个右键菜单 → 不可能（backdrop 阻断第二次右键）

### 右键内容正确性

- [ ] 不同位置右键 → 菜单内容不同（位置驱动，不是硬编码）
- [ ] 标签页右键 → 不出现终端的"清空/暂停"
- [ ] 终端接收区右键 → 不出现标签页的"分屏"

---

## 四、配置系统 + Settings Editor `[5e]`

- [ ] Ctrl+, → Settings Editor 标签页打开
- [ ] 左侧树：通用 / 终端 两个分组
- [ ] 点"终端"分组 → 右侧显示终端全部设置项
- [ ] 时间戳格式下拉 → 切换 → CM6 接收区格式即时变化
- [ ] 显示行号 toggle → 关闭 → CM6 行号消失
- [ ] 显示回显 toggle → 关闭 → 发送 AT 不再显示蓝色回显
- [ ] 编码下拉 → 切换 gb2312 → CM6 显示不乱码
- [ ] 搜索框输入 "编码"→ 过滤到编码相关设置项
- [ ] 搜索框输入 "baud"→ 过滤到波特率相关
- [ ] 自定义强调色：改 `app.accentColor` → 图标栏高亮 / 状态栏 / toggle 颜色变化
- [ ] 关掉 Settings Editor 标签页 → 设置值保留

## 五、协议系统 `[5e]`

- [ ] 终端工具栏出现协议下拉框
- [ ] 下拉框默认选中 "方括号协议"
- [ ] 切换协议 → 终端接收区解析方式变化
- [ ] SBQ 协议插件安装后 → 下拉框出现 "SBQ 心率协议"
- [ ] 卸掉 SBQ 协议插件 → 下拉框该项消失

## 六、context key + when 条件 `[5d]`

- [ ] 串口未打开 → 终端右键菜单"暂停"不出现（when: portOpen）
- [ ] 串口打开后 → "暂停"出现
- [ ] 非终端标签页聚焦 → 终端专属菜单项不出现
- [ ] 串口打开 → 状态栏 TX/RX 出现
- [ ] 串口关闭 → 状态栏 TX/RX 消失

## 七、迁移验证——旧功能全量回归 `[5f]`

### 终端

- [ ] COM 口列表正常加载
- [ ] 选 COM3 → 选 115200 → 点打开 → 连接成功
- [ ] 发 AT → 看到回显（白色接收 + 蓝色发送）
- [ ] 关闭串口 → 连接断开
- [ ] 暂停 → 数据缓冲，计数增加
- [ ] 恢复 → 缓冲数据一次性回放
- [ ] 回到底部按钮 → 出现/消失条件正确
- [ ] 行数上限裁剪生效（2000 行 → 砍 500）
- [ ] 搜索高亮 → 正常
- [ ] 实时过滤 → 不匹配行折叠
- [ ] 导出日志 → 文件保存正常
- [ ] HEX 模式切换 → 正常

### 标签页系统

- [ ] 新建标签页（+ 按钮 / Ctrl+T）
- [ ] 关闭标签页（× 按钮 / Ctrl+W）
- [ ] 关闭保持终端以外的标签页 → 欢迎页出现而不是白屏
- [ ] 标签页拖拽重排 → 顺序正确
- [ ] 分屏（右键 → 向下/向右分屏）
- [ ] 跨分屏拖拽 → 正常
- [ ] 多面板同时渲染 → CM6 数据不丢

### 图标栏

- [ ] 四个出厂图标（终端/工作台/市场/设置）全部显示
- [ ] 点击切换 → 高亮正确（只有一个蓝色光标）
- [ ] 图标栏拖拽重排 → 正常
- [ ] 卸载插件 → 对应图标消失

### 插件系统

- [ ] 市场搜索 → 正常
- [ ] 安装插件（文件夹选择器）→ 自动重载 → 图标出现
- [ ] 卸载插件 → 移到 .disabled/ → 图标消失 → toast
- [ ] 禁用插件 → 图标消失 → 市场"已禁用"分区 → 启用 → 图标恢复
- [ ] plugin-detail 标签页 → 显示插件信息
- [ ] 齿轮菜单（市场）→ 启用/禁用/卸载

### 通知系统

- [ ] 安装插件 → toast 弹出
- [ ] 卸载插件 → toast 弹出
- [ ] toast 最多 3 条堆叠
- [ ] toast 5s 自动消失
- [ ] toast 带 action 按钮 → 点击执行
- [ ] 铃铛图标 → 点击 → 通知面板
