# E1 执行清单——Electron 迁移（7 步，~1,190 行）

> 顺序不可变——每步依赖前一步。完工标准：软件行为与迁移前 100% 一致。
> 详细设计见 `04-迁移方案.md`。
>
> **🎉 2026-07-25：E1 全部完成。7 步 + 4 打包补丁，tsc 零错误，vitest 151/151，安装包验证通过。**

---

## 执行

- [x] **步 1：Electron 壳初始化**
  文件：`electron/main.ts`、`electron/preload-shell.ts`、`package.json` | ~200 行
  验证：`npm run electron:dev` → 壳 UI 正常渲染

- [x] **步 2：串口服务（serialport npm）**
  文件：`electron/services/serial-service.ts`、`electron/main.ts` | ~280 行
  验证：COM 枚举/打开端口/收发数据正常
  🔥 **缝 bug**：serial-service 漏掉 Rust `read_loop` 100ms 超时冲刷——设备不发 `\n` 时数据滞留缓冲区。修复：加 `flushTimer`。详见 memory `e1-flush-timeout-bug.md`。

- [x] **步 3：文件/配置/插件管理服务**
  文件：`electron/services/file-service.ts`、`electron/services/plugin-file-service.ts` | ~280 行
  验证：插件安装/卸载正常、配置读写正常、文件读取正常

- [x] **步 4：接前端 API**
  文件：27 处 `invoke(` → `window.linkdesk.*` 跨 9 文件 | ~200 行
  验证：`npx tsc --noEmit` 零错误 + `npx vitest run` 全过 + 手动验证全部功能

- [x] **步 5：linkdesk:// 协议 + preload-plugin**
  文件：`electron/protocol.ts`、`electron/preload-plugin.ts` | ~120 行
  验证：`linkdesk://` 协议注册成功 → 插件资源加载正常

- [x] **步 6：清理 + 测试** `10be023`
  文件：全量 grep `@tauri-apps`、`invoke(`、`tauri` → 删残留 | −17,413 行（src-tauri/ 全清）
  验证：`grep -r "@tauri-apps" src/` 零结果 + `grep -r "invoke(" src/` 零结果（除注释）

- [x] **步 7：Electron 打包** `0dedccb` + 4 补丁
  文件：`electron-builder.yml`、`package.json` | ~140 行
  验证：`npm run electron:build` → `release/LinkDesk Setup 0.1.0.exe` → 安装 → 运行正常

---

## 🔥 打包补丁——4 个同根 bug

> **根因：硬编码绝对路径在 Electron `loadFile()` 的 `file://` 协议下全部炸裂。Tauri 内置 web server 无此问题。**

| # | Commit | 文件 | 硬编码 | 症状 |
|:--:|------|------|------|------|
| 1 | `ded8fba` | `vite.config.ts` | 缺 `base: './'` → 构建产物 `/assets/xxx` | **白屏** |
| 2 | `a717d2e` | `file-service.ts` | `pluginsDir()` 遗漏 `app.isPackaged` | **插件全丢** |
| 3 | `d587768` | `iconUtils.ts` | `/assets/icons/` 字面量 | **图标全灭** |
| 4 | `1073bbb` | `ThemeEngine.ts` | `/themes/` 字面量 | **主题切换地雷** |

**归一化修复：** 新建 `src/core/assetPath.ts`——`getAssetPath()` 单一真相来源。CLAUDE.md 新增硬约束第 12 条——**禁止硬编码路径**。详见 memory `hardcoded-path-constraint.md`。
