# E6 剩余任务分档 · 派单序（2026-09-19 重分档）

> **这一份是什么**：**只写「按什么顺序、把哪一格交给哪个 AI」**——⛔ **不复制任务正文**（那是第二份真相）。
> **唯一任务真相源** = [E6-执行清单.md](E6-执行清单.md)；每条的状态、判据、读数一律以它和各格详案为准。
> **怎么用**：你按档自上而下派单；**每档的门槛不同**（第一/三档零等待，第二档要你先说一句话，第四/五档是一整层或要过设计稿）。
> **重分档时点**：2026-09-19，L9「UI 集中供给」收官 + `E6#128` 补正落地之后（用户：「按档次，重新从一档开始分」）。

---

## 〇 先对账：E6 未勾的账（2026-09-25 现状）

| 组成 | 条目 | 说明 |
|:--|:--|:--|
| **可派余额 8 条** | `#110` `#113` `#15o` `#37f` `#64e` `#53a-b` `#56a-b` `#30.8e` | 用户 2026-09-19 列出的那份（本档重新分档的对象） |
| **🆕 L10 十二格（2026-09-20 立案）** | `#132`-`#140`（9 执行格，3 会话编排）＋ `#141`-`#143`（待拍板） | 判据/修法/版本账全在案 ⇒ 9 执行格**一档零等待**；3 拍板格**二档**；详案 = [第三方作者实测反馈落地/](第三方作者实测反馈落地/00-整理档案.md) |
| **🆕 L11 十一格（2026-09-25 立案）** | `#144`-`#154`（11 执行格，**5 会话接力**） | 判据/读数/修法/版本账全在案 ⇒ **一档零等待**；⚠️ 层内两条硬次序（0.1.47 先上架 npm 货架 / 尺子必须等前四棒）；详案 = [插件测试覆盖层/](插件测试覆盖层/00-整理档案.md) |
| **L6 七格** | `#48a` `#48b` `#49a` `#50a` `#51a` `#52a` `#52b` | 整层未开工（当年拍板「L4/L6 跳过」）⇒ 单列第四档 |
| **划线留档 1 条** | `#111l` | 已由上一行收口、**原文留档备查 ⇒ 不是活**，不占任何档 |

> 🆕 **本次新立两格**（L9 收官留下的口子，用户 2026-09-19 同意立号）：**`E6#129`**（发布门禁：池供给插件必须声明 `minAppVersion`）· **`E6#130`**（钉线窗口机制）。已进清单：L9 汇总行改 **7＋3**，合计 **145 → 147**。
> 🆕 **2026-09-20 又新立一层**（用户立案）：**L10 第三方作者实测反馈落地**——生态首个脱产 AI 作者（geme-tihu-bicycle 鹈鹕骑车）12 坑＋4 建议实测，核对后立 9 执行格 ＋ 3 待拍板格；详案、16 条核对总表、版本账（**壳不动**，全落作者轴四包）见 [第三方作者实测反馈落地/00-整理档案](第三方作者实测反馈落地/00-整理档案.md)，合计 **156**（口径以清单 📌 注记为准）。
> 🆕 **2026-09-25 又新立一层**（用户立案）：**L11 插件测试覆盖层**（11 格 `#144`-`#154`）——把 8 份同体 `vitest.setup.ts` 收进 SDK 一个 subpath 真源（**用户点名排第一**）＋ 五仓三会话并行补纯逻辑与替身层测试 ＋ 一把**只报不拦**的覆盖尺；**壳不动、官方 5 仓不 bump、⛔ 第三方仓不代改**；四道方向题待拍板。详案、19 仓读数总表、版本账见 [插件测试覆盖层/00-整理档案](插件测试覆盖层/00-整理档案.md)，合计 **167**（口径以清单 📌 注记为准）。

---

## 一 分档总览

| 档 | 门槛（能不能现在派） | 条目 | 建议会话形态 |
|:--:|:--|:--|:--|
| **一档** | **零等待**——判据、读数、修法全在案，不需要你说任何话 | `#110` · `#113` · 🆕 **L11 十一格（`#144`-`#154`，5 会话）** | **1 个会话 1 格**（两格各自独立），或一个会话连做两格；L11 照下面五会话派 |
| **二档** | **等你一句话**（放行 / 定做不做 / 拍板），说完就能派 | `#37f` · `#15o` · `#64e ＋ #73o ＋ #129` | `#37f` 只需回写（0 工时）；另两项说完后按格派 |
| **三档** | **零等待，但非欠账**——不咬人，可插任意空档 | `#130` | 1 个会话 1 格，可与一档并行 |
| **四档** | **一整层**（L6 安全加固 7 格），开工令下了才动 | `#48a` `#48b` `#49a` `#50a` `#51a` `#52a` `#52b`（`#30.8e` 随 `#49` 带走） | 照总纲 §五默认编排拆 2–3 个会话（层档案在清单 §L6） |
| **五档** | **产出是文档＋你的拍板**，不是欠账 | `#53a-b` · `#56a-b` | 设计文档 → 你过目 → 才谈排期 |

**排序口径（说清楚，免得误读）**：分档按「**能不能立刻派出去**」排，不按「值不值得做」排。档内按**性价比**排序（成本低、判据齐的在前）。⚠️ 所以「第四档」不等于「最不重要」——它是**整层工程量**，只是要你先下开工令；「第五档」也不是边角料，是**下一条增长曲线**，只是它的交付物是设计稿。

---

## 二 逐档明细

### 一档 · 零等待（判据全在案）

#### `#110` 作者轴黄灯「观察名单」补齐 —— 成本约 0.5 天，**不 bump 壳版本**

- **干什么**：把 `@linkdesk/ui` 的黄灯 surface 从「只有 barrel 目录」补成**产物真源**（进包的共享组件目录）＋ 一条**覆盖面断言**（barrel 导出的目录 ⊆ 名单，缺一即红）＋ 重记 ui 基线。详案 = [插件规范化层/07](插件规范化层/07-任务-作者轴黄灯观察名单补齐.md)。
- **为什么排一档**：`scripts/check-npm-release.mjs:120` 的病根**原样未动**（改共享组件不亮黄灯）；而 2026-09-19 已把开工要用的四条读数全部备齐（清单该格的 2026-09-19 注：25 目录 / 22 进包 / 34 名面口径 / 复用 `scripts/lib/ui-surface.mjs`）。
- 🔴 **给这个 AI 的两条硬提醒**（都写在清单格里）：① **不许另写 barrel 解析器**——直接吃 L9#121 的 `scripts/lib/ui-surface.mjs`，否则又是一把会自我漂移的尺子；② 详案 §二那张 2026-09-15 的表**已过期**，开工按 2026-09-19 重测读数走；③ 详案里「软件用户拿得到修复、插件作者拿不到」那句**已不成立**（L9 后运行面由壳池供给）⇒ 判据要按新事实改。
- **动谁**：只动壳仓 `scripts/`（提交轴 = scripts，**不 bump 软件版本**）。收尾若需发一版 `@linkdesk/ui`（口径变了要重记基线，见详案），那一下要你说一声。

#### `#113` 插件侧死类清账 —— 成本约 1–1.5 天，**动 3 个插件仓**

- **干什么**：清 10 处 / 3 仓「有规则、无渲染方」的 CSS 死类（逐处裁决，⛔ 不许批量删）＋ 一把**只报不拦**的审计尺子 `scripts/audit-plugin-dead-css.mjs` ＋ 三仓 bump / CHANGELOG / 发版 / 官方目录两轮收录。详案 = [插件规范化层/09](插件规范化层/09-任务-插件侧死类清账.md)。
- **为什么排一档**：判据、十处清单（`file:line`）、误报控三条**全在案**，且三仓 CSS 源码自立案以来**一字未动**（#125/#128 只动清单/CHANGELOG/lock/AGENTS.md）⇒ 读数照旧可用。
- 🆕 **两条 2026-09-19 更新**（已写进清单格）：① **冲突窗口已关**——当年「别与 #113 并行」是因为 L8 的 8.5 也动 `marketplace` 仓，L8/L9 双双封层后没别的写者了；② **`ldk-*` 命中要先分「死类」还是「有意借用」**——L9 之后这些类的定义**只来自壳池**，按借用处理的不许删。
- ⚠️ **派单独占要求**：本格要动 file-tree / marketplace / serial-monitor 三仓 ⇒ **同一时间别派第二格去动这三仓**。三仓当前版本 = 1.0.13 / 1.0.39 / 1.0.20（#128 刚发过）⇒ 本格是它们**再一次 bump ＋ Release ＋ 目录收录**：**发版/推送那两下要你点头**（老规矩）。

> **一档的派单建议**：两个会话（`#110` 一个、`#113` 一个）可同时跑——文件域不重叠（壳仓 scripts ↔ 三个插件仓）。若只想开一个会话：先 `#110`（半天出活），再 `#113`。

#### 🆕 L10 会话一 · `#132`–`#135` SDK 修补与 dev 宿主保真 —— 约 1 天，SDK 0.1.42 → **0.1.43** 一批发

- 四格：`#132` css-hardcode 提示语对齐 token 库（含防再脱节单测）· `#133` publish 收尾「远端已更新，建议 git pull」提示 · `#134` dev 宿主保真两件（`#ld-root` 定高 ＋ `:root` 兜底 token 对齐 ui 包）· `#135` marketplace.schema.json 随包。
- **只动 `packages/plugin-sdk`**；详案 [01](第三方作者实测反馈落地/01-任务-SDK修补与dev宿主保真.md)。🔴 发版要你点头。

#### 🆕 L10 会话二 · `#138`–`#140` 作者面文档与脚手架 —— 约 1 天，docs 0.1.28 → **0.1.29** ＋ scaffold 0.1.12 → **0.1.13**

- 三格：`#138` 17 号组合行为矩阵 ＋ 00 号动线指针（三图模型——**文档是全的，动线断了**）· `#139` 脚手架四件套（icon 三图注释/资源模板/contributes 速查/README 过时句）· `#140` 作者面发布文档补「非交互 token」。
- 动作者面英文树 ＋ `plugin-docs` ＋ `create-linkdesk-plugin`；**与 会话一并行**（文件域零重叠）；详案 [03](第三方作者实测反馈落地/03-任务-作者面文档与脚手架.md)。🔴 发版要你点头。

#### 🆕 L10 会话三 · `#136`–`#137` SDK 新腿两件 —— 约 1–1.5 天，SDK 0.1.43 → **0.1.44**（＋官方 2 仓按裁决 PATCH）

- 两格：`#136` TSX 悬空类名腿（窄口子：只查 className 字面量、动态拼接跳过、口径锚定）· `#137` window keydown 腿（**官方 serial-monitor/settings 自己命中 3 处，逐处裁决**——settings 录制器可能正当豁免；⛔ `geme-tihu-bicycle` 第三方仓不代改，转达作者）。
- 🔴 **等会话一发完 0.1.43 再开工**（SDK 版本串行）；⚠️ **别与 `#113` 撞期**（都可能动 serial-monitor/settings）；详案 [02](第三方作者实测反馈落地/02-任务-SDK新腿两件.md)。本会话是 L10 最后一棒，出收口段。🔴 发版要你点头。

#### 🆕 L11 会话一 · `#144`–`#146` 测试地基收进 SDK ＋ 脚手架与五仓收敛 ＋ 文档 —— 约 1–1.5 天，**SDK 0.1.46 → 0.1.47** ＋ scaffold **0.1.14** ＋ docs **0.1.31** 三笔发

- 三格：`#144` `vitest.setup.ts` 真源搬进 SDK（**DOM 类型无关化改写** ＋ `exports["./vitest-setup"]` ＋ 5 例对账单测 ＋ 壳根收一行相对引源码）· `#145` 模板一行指针 ＋ 纪律段（模板 `AGENTS.md` 与 `sync-plugin-agents` 骨架**同笔**）＋ 两处 README ＋ `check-scaffold` **指针形态断言** ＋ **5 仓铺开**（＋18 仓 AGENTS.md）· `#146` 中文维护者面 ＋ 作者面 13 号「测试怎么写」一节（中英同笔）。
- 🔴 **三条硬提醒**：① `#144` 完成**不等于可以铺仓**——**0.1.47 没上架 npm 货架就铺指针 ⇒ 5 仓 CI 直接红**（它们的 lock 装到的 0.1.46 没有这个 subpath）；② ⛔ **`geme-tihu-bicycle` 不代改**（现存第 6 份副本保持原样，迁移建议转达作者）；③ ⛔ 模板文件里**不许出现内部任务号**（`check-scaffold` 有这条判据）。
- 详案 [01](插件测试覆盖层/01-任务-测试基建收敛SDK.md)。🔴 **三笔发版都要你点头**。

#### 🆕 L11 会话二 · `#147`–`#148` serial-monitor 起点仓 —— 约 1–1.5 天，**不 bump**

- 两格：`#147` 无替身纯逻辑 **16 单元**（`RingBuffer` / `autoFormatHex` / `portFilter` / `cm6` 五件 …——cm6 走 `EditorState` 反射断言）· `#148` 替身层 **6 必做 ＋ 2 可延后**，并把「共享 mock 只给最小面 ＋ 插件专属桩住本仓测试文件 ＋ 接管 `events.on` 抓 IPC 回调」做成**样板**（供会话三/四抄）。
- 🔴 **等会话一发完 0.1.47 再开工**；⛔ 不改共享 mock、⛔ 不为凑数写无断言测试。详案 [02](插件测试覆盖层/02-任务-serial-monitor补测.md)。

#### 🆕 L11 会话三 · `#149`–`#150` file-tree ＋ editor ＋ settings —— 约 1.5–2 天，**不 bump**

- 两格：`#149` file-tree 11 个**疑似**缺口——**第一道工序是裁决**（44 个纯逻辑单元逐条对 6 个既有测试文件；`FileTreeModel/*`、`FileTreeDnD/dropTarget` 很可能已被覆盖）· `#150` editor 6（`monaco-bootstrap` 可裁决延后）＋ settings 3（hook 那件要加 `@testing-library/react`——**版本照 `sync-plugin-ci.mjs` 的 `DEV_DEPS`**，lock 同笔；这是本层唯一允许动 `package.json` 的情形，仍不 bump）。
- 与二/四**并行**（文件域不重叠）；⛔ 不许往 file-tree/editor 里顺手引 RTL。详案 [03](插件测试覆盖层/03-任务-其余三仓零测补齐.md)。

#### 🆕 L11 会话四 · `#151`–`#152` marketplace 最大仓 —— 约 1.5–2 天，**不 bump**

- 两格：`#151` 纯逻辑 15 个**疑似**缺口（先读 10 个既有测试文件裁决——`marketCatalog/*` / `marketSources/*` / `updateDiscovery/plan` 三组很可能已覆盖）· `#152` 替身层 **6 个必做封顶**（`pluginsStore` / `catalogStore` / `installFlow`（🔴 断言「确认前不落任何安装动作」）/ `installGate` / `downloadCounts` / `useInstallAction`）＋ **35 行 `window.linkdesk` 直用单元的裁决表**（⛔ `.tsx` 一个不测，登记为后续轮次）。
- 详案 [04](插件测试覆盖层/04-任务-市场仓分层面补齐.md)。

#### 🆕 L11 会话五 · `#153`–`#154` 覆盖尺与收口（**最后一棒**）—— 约 1 天，**壳不 bump**

- 两格：`#153` `scripts/audit-plugin-tests.mjs`（**只报不拦**、退出码恒 0）＋ `audit:plugin-tests` script ＋ CLAUDE.md 一行 ＋ 19 仓实跑（判据**两条**：同名测试文件 ∨ 被任一测试文件引用——**防假红**）· `#154` 销账 `docs/05-插件更新/00-README.md §三` ＋ 全层收口报告 ＋ memory/skill 落盘 ＋ **四道待拍板整页带给用户**。
- 🔴 **等会话一~四全部收口**（尺子的读数对象就是它们补出来的测试）；⛔ 不进 `npm run check`/CI/`ci-verify.mjs`。详案 [05](插件测试覆盖层/05-任务-判据尺与收口.md)。

---

### 二档 · 等你一句话（说完即可派）

> ✅ **2026-09-19 三句已收齐（用户拍板；权威记录住清单各格）**：`#37f` 放行结案（已回写勾掉）· `#15o` = **修**（SDK 发版已授权，可派）· 依赖轴主线 = **`#73o` 自动装**（`#64e` 拦截装否决退役；`#129` 形态定 SDK lint 腿）——**二档清空，无待拍项**。
> ✅ **同日执行收官**：`#15o`／`#129` 已做完勾格（SDK **0.1.42** ＋ scaffold **0.1.12** ＋ docs **0.1.27** 三包已发、基线已记；种子转新随各仓自然发版，不催不立格）；二档剩余待办 = **`#73o` 主线派单**一项。
> ✅ **同日晚间 #73o 亦收官**——**二档全清（三项拍板当日全部执行完毕）**：壳 **0.2.14** ＋ ui **0.2.14** ＋ contracts **0.1.20** ＋ docs **0.1.28**（＋ 早前 sdk 0.1.42 / scaffold 0.1.12）；marketplace **1.0.42** / lang-defaults **1.0.20** 随官方目录收录、箱内种子追新。

#### `#37f` 启动日志清账 —— **0 工时，你放行即结**

- **状态更正（2026-09-19 已写进清单格）**：它是「**已查完、等放行**」不是「等调查」——审计把判据①的前提**证伪**了（纯干净 profile 0 处；`[object Object]` 是**对象被模板串吃掉的产物**，「哪个插件」本就无从回答 ⇒ 当年追的方向错）。
- **你要说的那句**：放行 ⇒ 派个 AI 把结论回写、关掉这条追了好几轮的线索（或你自己顺手结掉）。

#### `#15o` bundled zip 不可复现 —— **先定「做不做」，再谈派单**

- **病根**：SDK 打包时**隐式目录条目**盖的是 `new Date()`（文件条目已有固定时间戳）⇒ 带子目录的插件每次重打字节必变，`pack:bundled-json` 一跑 11 个 zip 恒脏 git status，而门禁**抓不到**它（内容指纹绕开了时间戳）。
- **现状**：2026-09-13 立案时**明确不修**（改 SDK 要跨轴 bump ＋ 重打 6 只种子），2026-09-14 复验「账仍成立、本轮仍不修」，用户当年**只问了难度、未定做不做**。
- **你要说的那句**：做 / 不做。做 ⇒ 修法两条现成（`zip.folder(name, {date})` 显式建目录，或 generateAsync 前遍历补 date），成本约 1 天（SDK 改 + bump 发版 + 6 只种子重打 + 门禁复跑）；**SDK 发版要你点头**。

#### `#64e ＋ #73o ＋ 🆕#129` 依赖声明轴 —— **一次拍板，别开三次**

- **三条同属一条轴「依赖/版本声明 → 强制」**：`#64e` = 缺依赖**拦截装**（新功能面）· `#73o` = 依赖链**自动装**（与 #64e **方向相反**，所以必须一起拍）· **`#129`** = 「池供给插件必须声明 `minAppVersion`」的**声明面门禁**（L9#128 明确留的口子；#127 的四仓已声明，但**下一个新插件没有任何机械件拦它**）。
- **你要说的那句**（三选一或组合）：**拦截**（装前查缺）/ **自动装**（装时补齐）/ **只声明不拦**（补门禁，不做运行期动作）。拍完再派单——`#129` 约 0.5 天（照 `linkdesk/no-ui-css-import` 的形状加一条 SDK lint 腿）；`#64e` 是新功能面，成本在你拍的方向里定。

#### 🆕 L10 三格待拍板（`#141` game 模板＋canvas mock · `#142` contentCanvas 一等声明 · `#143` catalog:submit）

- 三格真缺口核对成立，但方向或与既有拍板的关系要先定（game 模板形态 / contentCanvas 落点 / catalog:submit 是否绕收录人工闸门）——每格附「你要说的那句」，见 [04-待拍板方向题](第三方作者实测反馈落地/04-待拍板方向题.md)；开工前各过 design-flow。

---

### 三档 · 机制补全（零等待，但现在不咬人）

#### 🆕 `#130` 钉线窗口机制 —— 成本约 1–1.5 天

- **是什么**：`@linkdesk/ui` 的 **34 条导出面是终身承诺**（只加不删门禁守着），而**唯一的退休出口**写着「登记 ＋ 钉线窗口」——**窗口机制本身没实现**（`retired-ledger` 的 `uiExport` 档只登记、不执行）。
- **为什么不是一档**：当前「只加不删」完全够用，**真要退休才会撞上** ⇒ 不咬人。但它**零等待、不需要你说任何话**，所以适合塞进任何空档。
- **交付**：窗口设计（双导出 / 弃用警告期 → 移除的条件与时点）＋ 机械件 ＋ **一次 dry-run 演练判据**（假退休能红能绿）。来源 = [07 §残余边界①](UI集中供给/07-任务-收口发版与实机验收.md)。
- ⛔ **别与一档的两格并行开工**吗？不必——它只动壳仓 `scripts/` ＋ 门禁，与 `#110` 的文件域相近（都是 scripts），**两个同时跑要各自单独提交**，别揉一笔。

---

### 四档 · L6 安全加固整层（7 格，未开工）

> **门槛 = 你下开工令**（当年拍板「L4/L6 跳过」，所以它一直没动）。层内 7 格与验证口径都在清单 §L6 区块（`#48`–`#52`），此处只列派单用的骨架。

| 格 | 一句话 | 现状对接点（现成的东西） |
|:--|:--|:--|
| `#48a` | 子进程启动统一走 `confine(argv, policy)` 沙箱缝 | `lsp-handlers.ts:157` `spawn shell:true` ＋ `main.ts:224` `openInTerminal exec` 两处 |
| `#48b` | 无沙箱退化路径 = 显式抛错 `SANDBOX_UNAVAILABLE`（fail-closed） | — |
| `#49a` | 审批结果闭集 ＋ approval/asked+decided 审计对（保留 remember 模式） | `filesystem-guard.ts:91/166` 写侧审批 ＋ `guard-grants.json` 已有 |
| `#50a` | 关键日志 JSONL append-only ＋ 崩溃补闭环不截断 | `protocol-debug.log` append-only 已有 ＋ LogChannel 内存 500 条环 |
| `#51a` | 凭据隔离四件套（只存引用 / 每操作解析 / describe 不返回值 / env scrub） | 全域 grep 零命中 ⇒ **干净白板**，从零建 |
| `#52a` | `linkdesk.patch.yml`——用户按稳定 id 覆盖任何插件配置 | `seeds.ts:31` APPEARANCE_OVERRIDE_KEYS 13 键形态可参照 |
| `#52b` | `dumpPluginTree`——整棵运行插件树可读面（patch 的前置） | 数据源全现成（`getLoadDiagnostics` ＋ registrationTracker ＋ `plugins:readAllManifests` ＋ ConfigurationRegistry），**不依赖 #12** |
| ↳ `#30.8e` | 更新后权限重审批 → **随本层 `#49` 带走**（指针行，不是独立活） | — |

**派单建议**：照总纲 §五默认编排拆 **2–3 个会话**（例如 `#48a/#48b` 一格 · `#49a/#50a` 一格 · `#51a/#52a/#52b` 一格），每格各自收尾。⚠️ `#51a` 是白板（从零建），`#52b` 是纯读面（最容易被 AI 做过头 ⇒ 判据钉在「纯数据产出」上）。

---

### 五档 · 战略概念设计（产出 = 文档 ＋ 你的拍板）

| 格 | 一句话 | 现在的门槛 |
|:--|:--|:--|
| `#53a-b` | profile 概念设计——「这套环境 = base ＋ serial ＋ terminal ＋ 主题 X」按声明组装（对标 dsh cordis.yml） | 要你**看过设计稿**才谈排期。🆕 **L9 已交付一条可引用的先例**：`@linkdesk/ui` 的「壳池单实例供给 ＋ 版本同号锁步 ＋ 皮全开放/骨不开放」——profile 要回答的是同一类「什么由壳给、什么由声明组合」的问题，开工时列为判据来源（已写进清单格） |
| `#56a-b` | 自指插件工具集——会话内现场 define/run/stop（session-scoped 不落盘） | 同上要过设计稿；⚠️ 缺口里含**沙箱执行器**（≈ L6 `#48`）⇒ **逻辑上排在第四档之后**更顺 |

---

## 三 给派单用的三句话模板（照抄即可）

> 「你负责 **`E6#NNN`**。开工前依次读：`CLAUDE.md` 全文 → 本格所在的 `E6-执行清单.md` 条目（含其 2026-09-19 注）→ 该格详案 → 本档 §二里这一格的三条。做完照纪律收尾：**各自立案／判据读数／交接段／单笔提交**（⛔ 不许与其他格揉成一笔）。推送、发版、写别人的仓——**都等我点头**。」

---

## 四 本档与旧分档的差异（改了什么，为什么）

| 变化 | 内容 | 理由 |
|:--|:--|:--|
| **新增一档** | 旧表从「第二档」起排；现在拆出**一档 = 零等待、判据全在案** | 你要的是「派单序」⇒ 第一档必须是**不需要等你说话**的那些格，否则序列卡在等你 |
| `#110` 升档 | 旧在「第二档 门禁自身防腐」→ 现在一档 | 与 L9 强相关（口径/读取器都由 #121 备好了），**越早做越省**；再拖下去重测成本只会更高 |
| `#113` 升档 | 旧在「第三档 清账」→ 现在一档 | 它从来不是「健康度小活」：跨 3 个插件仓 ＋ 发版 ＋ 目录两轮；且唯一的并行禁令已随 L8/L9 封层消失 ⇒ 现在是最省事的窗口 |
| `#15o` 移到二档 | 旧在「第二档」→ 现在二档（同档换理由） | 它的真门槛不是成本，是**你还没定「做不做」**（当年只问了难度） |
| `#64e` 扩为一组 | `#64e ＋ #73o` → **`#64e ＋ #73o ＋ #129`** | L9 留下同轴的新口子，三条一起拍一次方向，别开三次 |
| `#130` / `#129` 新立 | L9 收尾遗留两格（钉线窗口 / 发布门禁） | 之前只躺在 07 档残余边界里，清单上无号 ⇒ 会丢 |
| `#37f` 状态更正 | 「等调查」→「**已查完、等放行**」 | 审计已证伪判据①的前提，留着会继续误导下一个人 |
| `#53a-b` 加输入 | L9 供给模型 ＋ 皮骨拍板列为设计判据来源 | 同一类问题已有一个跑通的先例，别从零推 |
| 🆕 **新增 L11（2026-09-25）** | 新立一层「插件测试覆盖层」11 格（`#144`-`#154`）落**一档** | 判据/读数/修法全在案（19 仓读数总表 ＋ 五份任务书），**零等待可派**；⚠️ 层内两条硬次序要守（0.1.47 先上架 / 尺子最后） |
| 🆕 **L10 状态更新** | 对账表的 L10 行：9 执行格 **✅ 2026-09-20 已收官**，只剩 `#141`-`#143` 三拍板格 | 收口报告 = [第三方作者实测反馈落地/交接.md](第三方作者实测反馈落地/交接.md)；⚠️ 别再按「12 格待办」派单 |
