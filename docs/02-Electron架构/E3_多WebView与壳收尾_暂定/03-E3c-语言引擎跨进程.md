# E3c — 语言引擎跨进程

> 2026-07-24。从旧 P7c 语言部分迁移，适配 Electron。
> **性质：** 让语言包成为一等公民插件类型 + 跨 WebView i18n 同步。和 E3b 主题引擎完全对称。

---

## 一、语言两层退路

| 层 | VS Code | LinkDesk |
|------|------|------|
| 第 1 层 | 语言包扩展的 NLS 翻译表 | `plugins/lang-en/` + `plugins/lang-zh/` 语言插件 |
| 第 2 层 | `localize(key, message)` 的 `message` 参数 = 英文原文 | `t(key)` → key 不在翻译表 → 返回 key 本身（key = 中文原文） |

```
#2 对应 VS Code 的 localize() fallback：
  i18next.init({
    fallbackLng: false,
    parseMissingKeyHandler: (key) => key  // t("终端") → "终端"（key 本身就是中文原文）
  })

  外加 ~30 个壳级关键 key 的显式英文兜底：
  resources: { en: { translation: { "终端": "Terminal", "设置": "Settings", ... } } }
```

---

## 二、语言贡献格式

```json
{
  "name": "日本語",
  "contributes": {
    "languages": [
      {
        "id": "ja",
        "label": "日本語",
        "path": "ja.json"
      }
    ]
  }
}
```

和主题完全对称。

**兼容逻辑：**
```
loader 检测语言：
  有 contributes.languages → 新格式注册
  有旧 file 字段 → 升级为 contributes.languages 等价注册（id = pluginId, label = 从文件名推断, path = file）
  都没有 → 不注册
```

出厂预装 en/zh，可卸载。卸载后 `t("终端")` → key 本身 "终端" 作为显示。

---

## 三、i18n 跨进程同步

多 WebView 后，每个插件 WebView 独立 i18next 实例：

```
切语言 → ja：
  1. 壳 i18next.changeLanguage("ja")
  2. 壳通过 IPC 广播：{ channel: "lang:changed", payload: { lang: "ja", resources: {...} } }
  3. 插件 WebView 收到 → i18next.addResourceBundle + changeLanguage
```

```typescript
// 壳侧
class LanguageEngine {
  async switchLanguage(lang: string): Promise<void> {
    await i18next.changeLanguage(lang);
    const resources = i18next.getResourceBundle(lang, null);
    ipc.broadcast("lang:changed", { lang, resources });
  }
}

// 插件侧
ipc.onEvent("lang:changed", ({ lang, resources }) => {
  Object.entries(resources).forEach(([ns, translations]) => {
    i18next.addResourceBundle(lang, ns, translations, true, true);
  });
  i18next.changeLanguage(lang);
});
```

---

## 四、语言选择器

```
状态栏语言指示器 "中:EN" → 点击弹出语言面板
  ├── 搜索框（搜索语言名："日本語" → 匹配）
  ├── 语言列表
  └── 选中即切换（不用 Enter，对标主题浏览器）
```

入口：状态栏 / 设置 Appearance 页面。

---

## 五、任务清单

| # | 任务 | 说明 | 行数 |
|:--:|------|------|:--:|
| 34 | LanguageRegistry + contributes.languages + 两层退路 | 引擎升级 | +60 |
| 35 | 出厂 en/zh 迁移到 contributes.languages 格式 | 插件改造 | +20 |
| 36 | i18n 跨进程同步（lang:changed IPC） | 多 WebView 适配 | +40 |
| 37 | 语言选择器 UI | UI 完善 | +80 |
| 38 | 插件 i18n 注册——内联翻译文件 | 通用 API | +40 |
| **合计** | | | **~240 行** |

任务 38 的 plugin.json 格式：
```json
{ "contributes": { "languages": [{ "id": "zh", "path": "zh.json" }] } }
```
loader → `i18next.addResourceBundle(lang, pluginId, json)`。插件 `t()` 先查自己的翻译表。

---

## 六、验证标准

```
切换语言 → ja：
  壳 WebView：菜单/状态栏/设置全部日语
  插件 WebView：终端/文件树/设置 全部日语
卸载 lang-zh 插件 → t("终端") 返回 "终端"（key = 中文原文）
出厂 en/zh 可卸载 → 卸载后语言选择器变空 → 第 2 层兜底
```

---

> **← 上一份：** `02-E3b-主题引擎跨进程.md`
> **→ 下一份：** `04-E3d-Profile与壳完善.md`
> **E3 索引：** `00-README.md`
