# 04 — Marketplace 增强

> 🔴 **2026-09-08 定位矫正（用户确认）**：本文 = **概念性 / 部分 UI 参照，非权威规格**（2026-07-24 产物，描的是「FloatingPanel 悬浮商店」时代的构想）。**大量交互逻辑与 UI 细节未定**——侧栏与主区之间的衔接（**转宽 / 共存——侧栏不会放弃**，日常装卸/启禁仍靠窄侧栏）、「显示所有插件」卡片页、详情页之间如何切换等均无细说。**未来商店落位 = marketplace 主区宽屏标签页**（E6 [03-市场交互设计 §五](../../02-Electron架构/E6_插件生态与发布/03-插件市场/03-市场交互设计.md)，标签页 = 通用表面容器），不是本文件的 FloatingPanel。**用户将来会详细重新设计市场插件**——实现/改造以 E6 文档 + 届时重设计为准，勿把本文当规格。**§搜索行为已矫正**（原记「vscode式 合并四组」= 误产前提；未来形态详见 03 §五·五）。

> **类型：市场插件** | **05-出厂制造 #05** | 2026-07-24（旧构想日期，见上矫正）。**早期构想：从侧边栏模式扩展为双模式（侧边栏 + 全屏商店视图）。**

---

## 定位

| | |
|---|---|
| 类型 | **纯插件 UI 升级**——改 `plugins/marketplace/` 内部代码 |
| 前提 | E3 架构完工 + v1.2 悬浮窗类型 B（FloatingPanel 组件） |
| 涉及架构改动 | **零。** 只改 marketplace 插件自己的 React 组件 |
| 对标 | VS Code Extensions 视图——侧边栏简洁模式 + 全屏商店浏览模式 |

---

## 两种显示模式

### 模式 1：侧边栏模式（当前已有）

> 侧栏中显示——紧凑列表。快速搜索、安装、启用/禁用。

保留当前 `sidebar.tsx` 的全部功能，作为默认模式。

### 模式 2：全屏商店视图（新增）

> 点击"浏览插件市场"→ 弹出 FloatingPanel（类型 B 悬浮面板）→ 大面积商店体验。

```
┌─────────────────────────────────────────────────┐
│  插件市场                              [×] [最大化] │
├─────────────────────────────────────────────────┤
│  🔍 搜索插件...                                  │
├──────────┬──────────────────────────────────────┤
│ 分类     │  ┌────────┐ ┌────────┐ ┌────────┐   │
│ 📡 串口  │  │ 图标   │ │ 图标   │ │ 图标   │   │
│ 🗺️ 地图  │  │ 名称   │ │ 名称   │ │ 名称   │   │
│ 🎨 主题  │  │ ★4.5   │ │ ★4.2   │ │ ★4.8   │   │
│ 🌐 语言  │  │ 简介   │ │ 简介   │ │ 简介   │   │
│ 📊 仪表  │  └────────┘ └────────┘ └────────┘   │
│ 🔧 工具  │  ┌────────┐ ┌────────┐              │
│ 🧩 其他  │  │ 图标   │ │ 图标   │              │
│          │  │ 名称   │ │ 名称   │              │
│ 筛选     │  └────────┘ └────────┘              │
│ ☑ 已安装 │                                       │
│ ☐ 免费   │  ── 页码 1/5  ← → ──                │
│ ☐ 付费   │                                       │
├──────────┴──────────────────────────────────────┤
│  [在主窗口中打开（编辑器）]  [最大化编辑器]  [关闭] │
└─────────────────────────────────────────────────┘
```

**布局：**
- **左侧分类栏：** 按插件类型/标签分组（串口/地图/主题/语言/仪表/工具/其他）
- **右侧卡片网格：** 插件卡片（图标 + 名称 + 评分 + 简介 + 安装按钮）
- **顶部搜索栏：** 全文搜索 + 标签过滤
- **底部操作栏：** "在主窗口中打开" / "最大化" / "关闭"

**与侧边栏的关系：** 不是替代——双模式。侧边栏用于日常快速操作（禁用/卸载），全屏商店用于发现和浏览。

---

## 模式切换机制

```typescript
// plugins/marketplace/plugin.json 加 contributes.commands

{
  "contributes": {
    "commands": [
      { "id": "marketplace.browse", "title": "插件市场: 浏览商店" },
      { "id": "marketplace.toggleView", "title": "插件市场: 切换侧边栏/商店视图" }
    ],
    "keybindings": [
      { "key": "Ctrl+Shift+X", "command": "marketplace.browse" }
    ]
  }
}
```

- `marketplace.browse` → 打开 FloatingPanel 全屏商店（如果文件树就绪 → 直接弹出；如果尚未 → 回退到侧边栏模式）
- `marketplace.toggleView` → 当前是侧边栏 → 弹出商店；当前是商店 → 关闭商店回到侧边栏

**实现：**
```typescript
// marketplace 插件内部
function MarketplaceEntry() {
  const [mode, setMode] = useState<'sidebar' | 'store'>('sidebar');

  // mode === 'sidebar' → 渲染 sidebar.tsx（当前侧栏内容）
  // mode === 'store'  → 渲染 <FloatingPanel><StoreView /></FloatingPanel>
  //                    （FloatingPanel 从壳 import）

  // "打开编辑器" → createTab("marketplace", { mode: "store" })
  //                → 标签页内全屏显示商店（非面板悬浮，等价于最大化）
}
```

---

## 增强功能

| 功能 | 侧边栏模式 | 全屏商店模式 |
|------|:--:|:--:|
| 搜索 | ✅ 紧凑搜索框——各组各自过滤（见 §搜索行为） | ✅ 大搜索框 + 筛选 |
| 分类浏览 | ❌ | ✅ 左侧分类树 |
| 插件卡片 | 列表项 | 卡片网格（图标+评分+简介） |
| 安装 | ✅ | ✅ 一键安装 + 进度 |
| 批量操作 | ❌ | ✅ 批量安装/更新 |
| 插件详情 | 右侧弹出 | 内嵌详情面板 |
| 评分/评论 | ❌ | ✅（未来接在线市场 API） |
| 更新通知 | 状态栏小红点 | 卡片上"有更新"角标 |

---

## 搜索行为——〔2026-09-08 矫正：#32a「vscode式 合并四组」误产，E6 内不实现〕

> **前提矫正（用户 2026-09-08 确认）。** 本节原记的「2026-08-17 拍板 vscode式」是**误产记录**：做计划/文档的 AI 把「市场未来在主区长成商店」误读成「现在要改侧栏搜索」，本节又把它固化成了行为定义，导致 E6 清单 #32a 照抄成任务。**真相：侧栏「已安装/内置/已禁用/探索插件」四组是壳渲染的 section**（头/折叠/分隔归壳），marketplace 的搜索状态是插件内部 state，**够不到壳 section**——「隐藏四头 + 真单列」只能发生在**插件自持 DOM** = 未来的主区大屏商店。E6 内**不做**侧栏单列搜索；侧栏搜索维持现状 = **各组各自过滤**（SearchView 按 name/pluginId/description 过滤，清空恢复各组）。

**误产内容存档（原计划，勿再作侧栏任务复活）：** 搜索非空 → 四组 section 头全隐藏合并单列（含已装匹配项带 [卸载]/[禁用]、清空恢复四组、匹配规则 = 现有 `matchSearch`）。——若未来要在商店做「搜索 = 全部匹配平铺 + 已装项带状态按钮」的浏览体验，那是**商店表面自持 DOM** 的事，与侧栏无关。

**未来商店（用户将详细重设计，本文件仅概念参照）：** 单一浏览表面——卡片网格 + 大搜索 + 分类 rail（本文上方 ASCII「商店主区」方向可参考；宿主由 FloatingPanel 改为 marketplace 主区标签页）；详情 = **新开详情标签页** 或 **同表面替换 + 返回箭头**（两种导航模式，见 03 §五·五）。完整叙述 → [03-市场交互设计 §五·五](../../02-Electron架构/E6_插件生态与发布/03-插件市场/03-市场交互设计.md)；因果与未来形态 → 项目记忆 `marketplace-future-main-area-store`。

---

## 任务清单

| # | 任务 | 行数 | 独立验证 |
|:--:|------|:--:|------|
| 1 | `StoreView.tsx` —— 全屏商店视图（分类栏 + 卡片网格 + 搜索栏） | ~150 | Ctrl+Shift+X → 商店弹出 |
| 2 | `PluginCard.tsx` —— 插件卡片组件（图标/名称/评分/简介/安装按钮） | ~60 | 卡片网格渲染 |
| 3 | 双模式切换——`marketplace.toggleView` 命令 | ~30 | 点切换按钮 → 侧边栏 ↔ 商店 |
| 4 | FloatingPanel 集成——`"打开编辑器"` / `"最大化"` / `"关闭"` 按钮 | ~20 | 面板内点"编辑器打开" → 标签页 |
| 5 | 分类系统——按 `plugin.json` 的 tags/keywords 自动分组 | ~40 | 分类栏出现"串口""地图""主题"等分组 |
| **合计** | | **~300 行** | |

> ⚠️ **不包含在线市场后端。** 以上是本地市场的 UI 增强。在线市场（评分/评论/远程安装）需要后端服务——那是 v3.0+ 的事。

---

## 为什么还是纯插件

Marketplace 增强不碰壳代码——全部改动在 `plugins/marketplace/` 内。用到的壳能力：

| 能力 | 来源 | 用途 |
|------|:--:|------|
| FloatingPanel | 壳组件（v1.2 提供） | 商店面板容器 |
| `getAllPlugins()` | viewRegistry | 列出所有已安装插件 |
| `installPlugin()` | loader | 安装新插件 |
| `executeCommand()` | CommandRegistry | `marketplace.toggleView` |
| `createTab()` | TabActionsContext | "打开编辑器"按钮 |

全部是已有 API——不需要新 IPC 通道或 preload 扩展。

---

> **← 04 索引：** `00-README.md`
> **← 依赖：** v1.2 悬浮窗类型 B（FloatingPanel 组件）
