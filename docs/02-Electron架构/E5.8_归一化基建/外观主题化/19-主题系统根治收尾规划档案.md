# 19-主题系统根治收尾规划档案（E5.8#128-#131，2026-08-28 撤销暂缓执行）

> 2026-08-27。用户质疑：「为什么主题系统这么难」「同一个 bug 为什么多次出现」「新 bug 为什么总出现」「安卓/苹果早就验证过这些功能，为什么做起来还难」。用户拍板：**立文档立任务，先别做**。
> 清单指针：E5.8-执行清单 Phase 11.19 #128-#131（🔒 暂缓 → **2026-08-28 用户拍板撤销暂缓**，批次 4 主战役）。

---

## 一、用户质疑与诚实归因

用户功能诉求全是手机主题软件的成熟功能：自由调圆角 / 自由换肤 / 自由调字体 / 玻璃质感 / 混搭（5 个主题用 a 的图标 + b 的壁纸 + c 的锁屏 + d 的字体）。需求描述清楚，功能非新事物。**难不在「功能」和「需求」，在 LinkDesk 的历史架构**。

### 根本差异：安卓是基建自带 token，LinkDesk 是盖好楼后装修

- **安卓/iOS**：Material Design 第一天就把颜色/圆角/字体定义成 token，系统 UI、组件库、主题引擎**一体设计**。换肤 = 换参数，消费面天生统一。
- **LinkDesk**：组件（顶栏/侧栏/主区/状态栏/浮层）E4-E5 用 CSS 一砖一瓦写起，**大量硬编码颜色、局部变量、各自枚举**。E5.8 才给已盖好的楼做主题系统 = 装修。

> 类比：老房子装中央空调——需求清楚（每间都凉快），施工要穿墙打孔；新楼盘从设计图就预留空调位。**安卓是后者，LinkDesk 是前者。**

### 「同一个 bug 多次出现」的真相：不是复发，是同一反模式逐面漏网

| 反模式 | 表现（用户感知为「同一 bug 多次出现」） |
|---|---|
| 枚举漏项 | 浮层磨砂枚举 10 选择器漏 3 → 每加新浮层，同样的「没磨砂」换个表面再来 |
| 参考系错锚 | ratio 换算 / 玻璃 wash 与 label 矛盾 / panorama 永不实——同一根因三个表现 |
| 多路径不一致 | recipe/mix/override/播种四路各写各的，改一条漏一条 |

根因：**系统性的全局改造被拆成「修这一处」的局部补丁**，补丁盖住已知面，盖不住未知面。

### 新 bug 为什么总出现：消费面隐式 + 组合验证盲区

- **CSS 变量消费是隐式的**——任何组件都能 `var(--xxx)` 或漏掉，**没有任何机械检查能告诉你哪个表面没迁移**（安卓有类型系统保证组件必须声明用哪个 token）。
- 组合空间巨大（6+ 主题 × 14 覆盖键 × 混搭来源 × 玻璃参数），单测只能测 token 计算逻辑（recipe/mix/seeds），测不到渲染层每表面实际消费。

### 为什么「描述清楚却难」

用户描述的是「**要什么**」（功能需求，准确清晰）；难在「**在 LinkDesk 实现时的工程成本**」（消费面迁移 / 跨进程 token 契约 / 隐式 CSS 消费）。需求理解 ≠ 改造容易。

---

## 二、根治方向（#131 层规则图先行 → #128-#130 三条，把「装修」翻新成「基建自带」）

> 目标不是「修好当前 bug」，是让「这一类 bug 不再能产生」——把隐式消费变机械可检查。
> **顺序**：#131 层规则图是 #128 的前置——无图则 token 化只是把分散硬编码搬进变量，仍各自为政。

### #131 层级/状态规则图先行（#128 前置——新增，2026-08-27 补）

> 2026-08-27 另一份 AI 分析校准，比初版表述更准确：**「代码收敛 ≠ 视觉行为收敛」**。代码层反模式在逐个消灭（#128-#130 的目的），但用户可感知的「玻璃 0→1 时背景/主表面/右键菜单/弹窗/分区图是否每次按同一规则变化」尚未稳定——玻璃链条（语义重定义 → 镜像 → 镜像失败 → 删镜像 → 分区图不淡出 → 补 #125）证明**基础语义在改、每次修复只覆盖当时看见的一层**。根治前提不是直接 token 化，而是先把「什么层负责什么」定成**不可变规则图**并用真实画面验证。

- **目标**：立一张层级/状态规则图，覆盖以下不可变规则：
  1. **玻璃透明度控制哪一层**——合成层语义（#125 教训：zone 图/主表面/浮层各自是否参与透明度合成）
  2. **渲染分层**——zone 切片图 / 全景图 / 磨砂 ::before 各在 z 哪层、谁采样谁（#117 删镜像、#126 #127 量测依据）
  3. **合成边界**——浮层 / 脱出窗 / 插件注入 DOM 在合成器里的位置（#93 缝系统、浮层权威）
  4. **优先级图**——用户覆盖 / 混搭来源 / 播种 / 主题配方，谁压谁（#88 生命周期归一）
  5. **概念键分离**——主题配方（审美）/ 配色变体（themeColor 选择）/ 混搭来源（域来源）三个产品概念不共用同一键；现状 colors 域来源并入 `app.themeColor`（E5.8#82 有意决策「配色域来源统一」，UI 已按外观模式动态切描述缓解）→ 落地时按此规则重审是否拆键；**若复用，须把「按当前模式解释」写死**：followTheme 下 `app.themeColor` = 当前配方内 colorway id；custom 下 = colors 域来源 recipe id；切换模式 / 切主题 / 重启恢复 / 复位 / 跨主题配色全走此解释、逐面测试（第三方 AI 校准，2026-08-27）
  6. **跨窗契约**——主窗口 / Pool / 脱出窗口的 CSS 变量所有权（谁写谁清）
- **验证**：真实画面回归——CDP **截图级**对比「玻璃 0→1」时全部表面一致变化（不只 token 几何）。
- **交付**：规则图沉档 + 每条规则映射到消费面代码。
- **前置关系**：#128 token 化依赖此图。

### #128 消费面全量 token 化（核心改造，前置 #131）—— 2026-08-28 预拆 #128.1-#128.9

- **现状**：组件 CSS 残留硬编码颜色/局部枚举；主题引擎统一广播但消费面有历史遗留。
- **目标**：审计全部组件 CSS，消灭残留硬编码，全部改走主题 token；**一次做完，不留补丁**。
- **范围**：顶栏/图标栏/侧栏/右栏/主区/状态栏/面板/浮层 + 组件库 + 插件视图。
- **验收（总）**：`grep` 硬编码 hex/rgba/hsla 只命中 token 定义处（零残留）；每表面消费主题 token，切主题全表面一致。
- **颗粒度（2026-08-28 用户拍板预拆，替代 2026-08-27 审视「执行时再拆」）**：按 #131 规则图的层级归属 + 表面组拆 **#128.1-#128.9**——每表面组一 commit + 终检门禁，禁单 mega-commit（#133.1-#133.5 先例）；「不留补丁」= 全部子任务完成后零残留，非「一次 commit」。**各子任务通用验收** = 该面 `grep` 硬编码零残留 + CDP 切主题全表面一致。

| # | 表面组 | 范围 |
|---|---|---|
| #128.1 | 顶栏 + 图标栏 | TitleBarZone（菜单/窗口控制/拖拽区文本）+ IconBar（左侧 42px） |
| #128.2 | 侧栏 / 右栏 | SidebarZone + RightSidebarZone（标题/分组/搜索入口） |
| #128.3 | 主区 | MainZone：GroupTabBar / ViewTitleActions / editor chrome（status-bar/breadcrumb/sep） |
| #128.4 | 状态栏 | StatusBarZone（chord/badge/notif 面板入口） |
| #128.5 | 面板 | PanelZone（switcher/tab/check/detach） |
| #128.6 | 浮层 | 浮层权威全部表面：Dialog / QuickPick / ContextMenu / FloatingPanel / Toast / 下拉 + 浮层内所有表面 |
| #128.7 | 组件库 | components/shared 控件集（ContextMenu/SelectBox/Combobox/InlineInput/ColorPicker/NumberInput/SegmentedRadio/SidebarSection/ThemePicker/Button） |
| #128.8 | 插件视图 | builtin + user 插件 CSS 硬编码色审计（file-tree/settings/marketplace/serial/demo）——与 #130 门禁覆盖域一致 |
| #128.9 | 终检门禁 | 全仓 grep 硬编码仅命中 token 定义处零残留 + 切主题全表面 CDP 截图级对比（对标 #182 三档） |

### #129 主题文件 formal schema（契约保护）

- **现状**：`appearance`/`colorways` 主题 JSON 契约**无 formal schema 校验**——只有类型推断 + 文档约束，格式错无机械拦截（运行时 API 类型在 contracts 里齐全，主题文件契约缺）。
- **目标**：主题 JSON 立 JSON Schema（对标 `plugin.schema.json`），格式错当场拦。
- **验收**：schema 校验脚本覆盖所有 `contributes.themes` 指向的 JSON 文件；错误主题文件加载时报错不静默。
- **机械挂靠（2026-08-27 审视补，§八）**：执行时挂靠 §八 机械审查三件套（audit-contributes 先例）——校验脚本进 `npm run check` 链，防新主题文件回潮（对标 #137）。

### #130 CSS 消费面机械审计进 check（长期门禁，防回潮）

- **现状**：硬编码颜色无机械拦截 → 新代码随手写 `#xxx` → 同类 bug 重新埋雷。
- **目标**：硬编码颜色/枚举的 grep 审计进 `npm run check` 作门禁（对标 E5.7 静态卫生 `--max-warnings 0`）。
- **验收**：check 增加审计步骤，硬编码 hex 即红灯；新代码无法回潮。
- **机械挂靠（2026-08-27 审视补，§八）**：审计脚本与 §八 三件套（E5.8#137 audit-api-contracts/config-baseline/contributes）同列 `npm run check`，成为第四道机械门禁。

**四者关系**：#131 是规则图（前置，给 #128 定「层负责什么」）；#128 是核心改造，一次性翻新；#129 是契约保护，格式错当场拦；#130 是长期门禁，**从机械层保证「同一 bug 不再回来」**。

---

## 三、为什么现在不做（用户拍板暂缓 → 2026-08-28 撤销）

> **2026-08-28 撤销暂缓**：用户拍板 Phase 11.19 执行（批次 4 主战役），本档案即任务入口。执行顺序 #131 → #128.1-#128.9 → #129 → #130（批次 4，修一验一逐任务验收），其后批次 5 #150/#151（玻璃端点，放 #128 后避免二次返工）。见 memory `e5.8-execution-order-verification`。
> 2026-08-28 用户同时拍板预拆 #128 → #128.1-#128.9（见 §二 #128），更新本档案 + 执行清单 Phase 11.19，独立 commit。

用户 2026-08-27 明确「先别做」（含 2026-08-27 补入的 #131）。理由记录在案：
- 四任务是 E6 级跨全组件改造，工作量大，需要专门排期，不搭当前 E5.8 收尾的车。
- 当前 E5.8 仍有余量收尾任务，按「一次一个小任务」节奏执行。
- 本档案即为任务入口——未来排期时从清单 Phase 11.19 直接开工，无需重新调研。

**暂缓 ≠ 放弃**：这是用户拍板的结构性安排，不是 AI 的「当前够了/未来再做」话术。

---

## 四、验收总则

1. 每任务独立 commit + `npm run check` 全绿（#130 本身是把审计加进 check）。
2. CDP 实机：切多主题 + 混搭组合零回归。
3. 实机用户视觉验收后回勾清单。
