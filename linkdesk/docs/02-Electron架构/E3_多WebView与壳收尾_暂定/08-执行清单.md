# E3 执行清单——多 WebView + 壳收尾 🏁 封站

> 四层顺序执行——每层依赖前一层。同层内 E3b∥E3c 可并行，E3d∥E3e∥E3f 可并行（E3d 额外依赖 E3b+E3c）。
> 详细设计见各子文档（E3a-E3j）。
>
> **🏁🏁🏁 E3 封站——2026-07-28。E3a-E3j 核心任务全部完成。**
> **E3a 🎉 13/13 | E3b 🎉 57/57 | E3c 🎉 13/13 | E3d 🎉 3/3 | E3e 🎉 5/5 | E3f 🎉 55/70 | E3g 🎉 3/4（#63 删除） | E3h 🎉 4/5（#65 跳过） | E3i 🎉 4/4 | E3j 🎉 12/12**
> **支线 `serial-port-refresh`：🆕 7 commits（#82a-#82g）——多标签页数据串流 + 端口刷新 + i18n。**
> **ESLint 机械防线：5 条自定义规则。CLAUDE.md 硬约束：19 条。**
>
> **下一步：E4——文件树 + Monaco 编辑器（34 任务，文档已就绪）。**

---

## 第 1 层：E3a 多 WebView 进程隔离（13 任务，~695 行）

> ⚠️ **必须最先做。** 新插件一出生就在独立 WebView 里——不用先单线程再迁移。

### 底座——WindowManager + PluginViewRegistry

- [x] **#24** WindowManager——WebContentsView 创建/销毁/聚焦 | `window-manager.ts`（新） ~330 | 创建→addChildView→关闭→webContents.close() 无泄漏
- [x] **#25** PluginViewRegistry——插件 ID→WebContentsView 映射 + bounds 管理 + 重载 | `plugin-view-registry.ts`（新） ~100 | Map 增删查 + 重载后 pluginId 不变
- [x] **#25a** 资源休眠——后台 WebView 降频 + 内存压力检测 | `window-manager.ts` ~40 | 隐藏标签页→setBackgroundThrottling(true)；每 30s getProcessMemoryInfo() 采样
- [x] **#25b** 🆕 保活宽限期——关闭标签页延迟销毁 WebView（60s 可复用）+ 内存压力联动 | `window-manager.ts` ~25 | 关终端→60s 内重开→WebView 复用零重建；内存>1GB→无视宽限期立即销毁

### 通信——IpcBridge

- [x] **#26** IpcBridge——核心服务代理（Config/Command/Menu IPC handler 注册） | `ipc-bridge.ts`（新）+ `IpcBridgeHandler.ts`（新） ~140 | ipcRenderer.invoke('plugin:ipc',...)→壳侧 handler→返回结果
- [x] **#27** IpcBridge——事件管道（Events/Data push 通道 + 请求队列串行化） | `ipc-bridge.ts` ~50 | 串口数据推送到插件 WebView，<16ms 延迟

### 插件侧

- [x] **#28** preload-plugin.ts——插件侧精选 API（含 filesystem 受限读写） | `preload-plugin.ts`（新） ~80 | contextBridge 白名单审计 + IPC 回调模板（ref 桥接 + cleanup + 超时）
- [x] **#29** MainContent 改为 WebContentsView placeholder 管理 | `MainContent.tsx` ~100 | 壳标签页切换→WebContentsView 显隐

### 壳侧归一化

- [x] **#29a** SidebarTabSync——侧栏↔标签页三向同步归一化 | `SidebarTabSync.ts`（新） ~30 | 侧栏点 session→主区切换→侧栏高亮同步

### 现有插件迁移——逐个独立

- [x] **#30** terminal 插件迁移——useSerialContext→useIpcSerialContext + useSendData→ipc | `plugins/terminal/` ~50
- [x] **#31** marketplace 插件迁移——plugin.json 列表读 IPC | `plugins/marketplace/` ~88 行，9 方法 + 异步改造
- [x] **#32** settings 插件迁移——配置读写 IPC（零行——core:true 壳组件 re-export）
- [x] **#32a** 🆕 workspace 插件迁移——卡片网格 IPC（零行——壳组件 re-export）

---

## 第 2 层：E3b 主题引擎 ∥ E3c 语言引擎（可并行）

> ⚠️ 依赖 E3a（多 View 才能跨进程广播）。两者互不依赖——可并行。

### E3b：主题引擎跨进程（批次 1-5 ✅ + UAT ✅ + #36f15 ✅，~470 行）

- [x] **#33** ThemeRegistry + contributes.themes + 三层退路 | `ThemeRegistry.ts`（新） +80 | 插件声明 contributes.themes→注册→三层退路逐层回退
- [x] **#34** 出厂 Dark/Light 迁移到 contributes.themes 格式 | `plugins/theme-dark/` + `theme-light/`（新） +20 | Dark/Light 以插件形式存在→可卸载
  → 🔥 **9 个 bug 连环**（查询面未归一化——第一个非视图插件踩进盲区）。教训见 memory `non-view-plugin-normalization-lesson`。
- [x] **#34a** 🆕 合并 theme-dark + theme-light → `plugins/theme-defaults/`（"官方主题"），一个 plugin.json 含 dark/light 两个 contributes.themes 条目 | ~5
- [x] **#35** CSS 变量跨进程广播（theme:changed IPC） | `ipc-bridge.ts` + `preload-shell.ts` + `preload-plugin.ts` + `ThemeEngine.ts` +55 | 壳切主题→所有插件 WebView 的 :root CSS 变量同步

### 🔥 #36 主题浏览器 UI——归一化路线（5 子任务，~230 行）

> 对标 VS Code `QuickPick`——CommandPalette / ThemeBrowser / LanguagePicker 共用一个浮动面板组件。

- [x] **#36a** QuickPick 归一化浮动面板组件——从 CommandPalette 提取公共壳 | `QuickPick.tsx`（新） +130 | Ctrl+Shift+P 行为和重构前完全一致
- [x] **#36b** CommandPalette 改用 QuickPick——删除已提取代码 | `CommandPalette.tsx` ~40（净减 ~90）
- [x] **#36c** ThemeBrowser 基于 QuickPick——预览（↑↓ 即时 apply + Esc 回退）+ 提交（Enter 写配置） | `ThemeBrowser.tsx`（新） +50
- [x] **#36d** 命令注册 + 快捷键 `Ctrl+K Ctrl+T`——`workbench.action.selectTheme` | `App.tsx` +5
- [x] **#36e** 齿轮菜单"选择颜色主题"入口——有 `contributes.themes` 的插件右键菜单加一项 | `MenuRegistry.ts` +5

### 🔥 #36e2 桌子管理规则落地——四条代码机制（4 任务，~77 行）

> 大厅模型补完——桌子管理规则不是靠人记，是代码自动保证。

- [x] **#36e2a** 机制 1——`RegistryBase` 基类：登记注销自动对称（不靠 lifecycle.ts 手动加） | `RegistryBase.ts`（新） ~40
- [x] **#36e2b** 机制 2——路由表自刷新：FactorySlots 订阅 lifecycle，插件进出自动重扫描 | `FactorySlots.ts` +20
- [x] **#36e2c** 机制 3——单真源：ThemeRegistry 改读 ThemeEngine，消除双写 | `ThemeRegistry.ts` + `ThemeEngine.ts` +15
- [x] **#36e2d** 机制 4——`FactoryRole` enum → `string`：槽位名不再硬编码枚举，消费者自由定义槽位 | `FactorySlots.ts` + `plugin.schema.json` +2

### 🔥 #36f 主题/配置审计与加固——13 漏洞（7 批次，~185 行）

> 2026-07-26 敌对审计。对标 VS Code 健壮性：主题安装/卸载/回退在任何时序下不出错。

**批次 1：主题注册归一化 + 内置 fallback（H2 + M2，~40 行）**
- [x] **#36f1** H2——`loadThemePlugin` 旧格式主题同步写入 ThemeRegistry | `loader.ts` +15 | 安装旧格式主题插件→卸载→当前主题正确回退
- [x] **#36f2** M2——App 启动注册内置 Dark/Light fallback 主题 | `App.tsx` + `ThemeEngine.ts` +25 | 卸载全部主题插件→设置下拉框仍有 Dark/Light

**批次 2：枚举更新归一化（M5 + M4，~20 行）**
- [x] **#36f3** M5——删除 App.tsx 二次 `registerConfiguration("app.theme", ...)`，枚举更新统一走 `syncAppThemeEnum` | `App.tsx:240-258` + `loader.ts` +10（净删 ~15）
- [x] **#36f4** M4——`loadPlugin` 中 `contributes.themes` 存在时跳过旧格式 `manifest.themes` | `loader.ts:427-441` +10

**批次 3：FactorySlots 热换（⏭️ 跳过——#36e2b 已覆盖）**
- [x] **#36f5** + **#36f6** H1——FactorySlots + lifecycle 消费端 → ⏭️ #36e2b 已覆盖（constructor 自订阅 lifecycle，优于 lifecycle.ts 手动调）

**批次 4：SettingsView 跳转（M1，~25 行）**
- [x] **#36f7** M1——`ConfigurationRegistry` 加 `onRequestSettingsGroup` Emitter | +10
- [x] **#36f8** M1——`SettingsView` 订阅 Emitter + mount 消费变量（双通道） | +15 | 设置已打开→齿轮"设置"另一插件→跳转到该插件分组

**批次 5：枚举验证（M3，~10 行）**
- [x] **#36f9** M3——`setConfigurationValue` 写入前做 enum 验证 | `ConfigurationService.ts` +10 | 手动写 settings.json `app.theme: "dead"`→重启→warn + 不崩
  > ⚠️ 验证需清 `localStorage.removeItem("v3_settings")`——StorageService 优先读 localStorage，直接改文件无效。

**批次 6：低严重度补强（L1-L5，~40 行）**
- [x] **#36f10** L1——gear menu onClose 清理 context keys | `sidebar.tsx` +8
- [x] **#36f11** L2——`syncAppThemeEnum` 时序注释标记 | `loader.ts` +3
- [x] **#36f12** L3——`ThemeBrowser` 订阅 `onPluginLifecycleChange` | `ThemeBrowser.tsx` +10 | ThemeBrowser 打开时卸载主题→列表即时收缩
- [x] **#36f13** L4——`revertThemeIfCurrent` 两处调用点加 `await` | `loader.ts` +2
- [x] **#36f14** L5——旧格式 `manifest.themes` / `manifest.languages` `else if` → 独立 `if` | `loader.ts` +4

**🔥 UAT 发现——已修（2 个）**
> 2026-07-27 Encaron 实测。`loadPlugin`↔`loadPluginRuntime` 两条路径不同步和生命周期时序问题的再次发作。

- [x] **L6b——卸载当前主题不回退**：`revertThemeIfCurrent` 在 `onWillUninstall` 之后执行 → 主题已注销 → revert 静默跳过。修复：revert 移到 onWillUninstall 之前。`1ba6a25`
- [x] **L6——重装后主题无颜色（空壳）**：`parseContributions` 中 `getPluginDataFile` 依赖 glob，runtime 插件不在 glob 中 → ThemeEngine 无颜色数据。修复：runtime 路径从文件系统 fetch JSON 补充注册。`cea1476`
- 两个 bug 根因相同——L6 两条路径不同步 + L6b 生命周期时序无机械保障。堵缝靠 #36j1。

**批次 7：生产环境加载（H3，~20 行）**
- [x] **#36f15** H3——`loadPluginRuntime` 生产环境用 `linkdesk://` 替代 `/@fs/` | `loader.ts` +12/−9

### 🔥 #36g 功能补全——齿轮菜单贡献检查对标 VS Code（4 任务，~20 行）

> 对照 VS Code per-plugin 齿轮菜单：`applyExtensionContextKeys` 设了 5 个 key，当前只消费 2 个。所有改动限于 `plugins/marketplace/src/sidebar.tsx`——核心零改动。

- [x] **#36g1** K1——`applyExtensionContextKeys` 加 `extensionHasKeybindings` | +2
- [x] **#36g2** K2——`ensureMarketplaceCommands` 加 `"workbench.action.openExtensionKeybindings"` 菜单项 | +3
- [x] **#36g3** K3——加语言菜单项——消费 `extensionHasLanguages` | +3
- [x] **#36g4** K4——加图标主题菜单项——消费 `extensionHasIconThemes` | +3

### 🔥 #36h E2b 归一化补全——串口术语迁出核心（3 任务，~60 行）

> 2026-07-26 扩展审计。E2b #7 #8 声明 DONE 但未落地——`portName`/`PortInfo`/`onDidChangePortState` 仍在核心代码。

- [x] **#36h1** M6a——`CoreEvents.onDidChangePortState` → `onDidChangeSourceState`，`portName` → `sourceName`；`PortInfo` → `SourceInfo` | `CoreEvents.ts` + `SourceStateContext.tsx` +10 | grep 零结果
- [x] **#36h2** M6b——`ContextKeyService.initCoreKeys`：`portOpen` → `sourceOpen`，`portName` → `sourceName` | +5
- [x] **#36h3** M6c——`resetTerminalCounter`/`resetWorkspaceCounter` → 通用 `resetPluginCounter(pluginId)`；App.tsx `terminal-` 正则 → `syncCountersAfterRestore` | `tabIdentity.ts` + `App.tsx` +45

### 🔥 #36i 硬约束修复——补违例（5 任务，~39 行）

- [x] **#36i1** 🔥 H4——`coreCommands.ts`：18 处 `title`/`category`/`group` 硬编码中文 → `t("...")` | +20 | grep `title:\s*"[一-鿿]` src/core/coreCommands.ts 零结果
- [x] **#36i2** 🔥 H5——`lifecycle.ts` 加 `unregisterPluginFileAssociations(pluginId)` 调用 | +1
- [x] **#36i3** 🔥 H6——`lifecycle.ts`：加消费端 i18n 资源移除——`i18n.removeResourceBundle(lang, pluginId)` | `lifecycle.ts` + `loader.ts` +10
- [x] **#36i4** M7——`tabIdentity.ts` `FALLBACK_META`：`identityField` 映射加 `@deprecated` 注释 | +5
- [x] **#36i5** M8——`LayoutService.ts` `LEGACY_TAB_IDS`：加 `@deprecated` 注释 | +3

### #36j 归一化清理——消重复（3 任务，~43 行）

- [x] **#36j1** 🔥 L6——`loader.ts`：提取 `applyPostLoadSteps(pluginId, manifest, reason)`——消 `loadPlugin`/`loadPluginRuntime` ~50 行重复 | +25
  > **这不是消重复——是堵缝。** `loadPlugin` 和 `loadPluginRuntime` 是两条独立函数但应该收敛到相同终态。历史上每次给 `loadPlugin` 加能力（侧栏→Bug 3、主题数据→今天），`loadPluginRuntime` 就漏掉。提取共享函数后，加新能力只需改一处，两条路径自动受益。**从此不再出现"glob 路径有、runtime 路径没有"这类 bug。**
  > 同类 bug 历史：Bug 3（runtime 无侧栏）、今天（runtime 无主题颜色）、#34 bug 6（重装不显示在列表）、`5668db3`（lifecycle toast displayName 时序）。
- [x] **#36j2** L8——`loader.ts`：提取 `extractThemeColors(data)`——消 3 个重复的 5 行颜色提取循环 | +8
- [x] **#36j3** L9——`lifecycle.ts`：提取 `updateIconOrder(pluginId, mode)`——合并 `appendToIconOrder`/`removeFromIconOrder` | +10

### #36k 设计债务——修边角（3 任务，~22 行）

- [x] **#36k1** D1——`preload-plugin.ts`：安全注释 vs 实际暴露不一致——删除 `dialog.open` 暴露或更新注释 | +2
- [x] **#36k2** M9——终端插件：命令注册加清理（`useEffect` 返回注销函数） | `index.tsx` +15 | 终端标签页关闭→命令面板 `terminal.*` 命令消失
- [x] **#36k3** D3——工作区插件：`viewRole: "sidebarPrimary"` 声明修复 | `plugin.json` +5

### 🔥 #36l 核心桌子层 vitest 覆盖——防漏洞重演（6 测试文件，~75 条）

> 当前 vitest 151 条覆盖低层工具，**核心 Registry/Service 层零覆盖。** #36f/#36i 发现的 6 个漏洞（H5/H6/H2/M5/M1/M9）如果当时有 vitest，全部能在提交前捕获。

- [x] **#36l1** `ThemeEngine.test.ts`——register/unregister/getAvailable/loadTheme/applyTheme/回退 | ~15 条
- [x] **#36l2** `ConfigurationRegistry.test.ts`——register/unregister/updateEnum/merge/request+consumeSettingsGroup | ~12 条
- [x] **#36l3** `ConfigurationService.test.ts`——三层合并/get/set/defaults/fallback | ~10 条
- [x] **#36l4** `RegistryLifecycle.test.ts`——🔥 核心测试：安装→卸载对称性。10 个注册表 register→uninstall→get 返回空 | ~15 条
- [x] **#36l5** `loader.test.ts`——loadPlugin/parseContributions/主题双重注册/旧格式兼容/i18n 注册→注销 | ~15 条
- [x] **#36l6** `FactorySlots.test.ts`——initialize/getPluginId/hasSlot/运行时 refreshFromPlugins | ~8 条

---

- [x] **#37** 产品图标主题（Product Icon Theme） | `IconRegistry.ts`（新） +70 | 插件声明 contributes.iconThemes→图标集切换

### E3c：语言引擎跨进程（13 任务，~320 行）

> 🔥 2026-07-26 重写。E3b 主题插件化踩了 6 个坑——每个都会在语言插件化重演。原 5 任务拆分为 10 任务 + 3 个 E3b 教训预检。

**🔥 E3b 教训预检——做 #38 之前先确认（3 子任务）**
- [x] **#37a** 确认 `IpcBridgeHandler.ts` `list` case 已包含 `contributes`——✅ 已修复
- [x] **#37b** 确认 `applyExtensionContextKeys` 已设 `extensionHasLanguages`——✅ 已存在
- [x] **#37c** 确认 `extensionHasLanguages` context key 有齿轮菜单项消费——#36g3 已做

**E3c 主体任务**
- [x] **#38** LanguageRegistry + contributes.languages + 两层退路 | `LanguageRegistry.ts`（新） +70
  > 🔥 **#38 必须在 `parseContributions` 中加 `contributes.languages` 分支**——否则合集语言插件（中英合一）静默失效。
- [x] **#38a** `parseContributions` 加 `contributes.languages` 处理——对标 `contributes.themes` 分支 | `loader.ts` +37/−11
- [x] **#38b** 🔥 语言注册归一化——提取 `registerLanguageBundle(langCode, data)` 共享函数 | `loader.ts` +17/−6 | `loadLanguagePlugin` 和 `parseContributions` 两处调同一个函数（对标 #36j2）
- [x] **#38c** `syncAppLanguageEnum()`——语言注册/注销后同步 `app.language` enum（对标 `syncAppThemeEnum()`） | `loader.ts` +11
- [x] **#39** 出厂 en/zh 迁移到 `contributes.languages` 格式——合集插件 `plugins/lang-defaults/` | `lang-defaults/`（新） +294/−6
  > 🔥 **Bug：Vite glob 缓存——`getPluginDataFile` 在 dev 模式下返回 plugin.json（7 keys）而非 zh.json（~118 keys）。** 修复：新增 `loadLanguageContributionData()` 用 `fetch()` 绕开 glob → #39a。
- [x] **#39a** 🔥 `getPluginDataFile` 全量迁移 `fetch()`——消 Vite glob 缓存风险 | `loader.ts` ~30 | 新插件目录→dev 模式→主题/语言数据文件正确加载
  > 影响范围：`parseContributions` 的 `contributes.themes`、`loadLanguagePlugin`、`loadThemePlugin`、`loadPlugin` 的 `manifest.file` 类型检测。详见 memory `vite-glob-cache-seam-bug`。
- [x] **#40** i18n 跨进程同步（lang:changed IPC） | `ipc-bridge.ts` + LanguageEngine +40 | 壳切语言→所有插件 WebView 的语言资源同步
- [x] **#41** 语言选择器 UI——基于 QuickPick 归一化组件（对标 ThemeBrowser） | `LanguagePicker.tsx`（新） +71
- [x] **#41a** `LanguagePicker` 订阅 `onPluginLifecycleChange`——对标 #36f12 | 含于 #41
- [x] **#42** 插件 i18n 注册——内联翻译文件 | `loader.ts` +24 | 新增 `syncLanguageBroadcast()`——新语言插件安装后即时广播到所有插件 WebView
  > 🔥 **UAT 发现：** 卸载当前语言插件不回退——新增 `revertLanguageIfCurrent()`（对标 `revertThemeIfCurrent`），和 L6b 同根。

---

## 第 3 层：E3d Profile ∥ E3e 通知 ∥ E3f 壳 UI（可并行）

> ⚠️ E3d 额外依赖 E3b+E3c（五维验证的主题和语言需跨进程广播）。E3e 和 E3f 仅依赖 E3a。

### E3d：Profile 与激活（3 任务，~250 行）

- [x] **#43** ProfileService——loadProfile / switchProfile + 五维验证 | `ProfileService.ts`（新） ~330 | 切 Profile→五维全过，失败回退+toast
- [x] **#44** activationEvents——`*` / onCommand / onFileOpen / onPortOpen / onLanguage / onView 触发 | `loader.ts` + `CommandRegistry.ts` + `types.ts` ~88
- [x] **#45** extensionDependencies——加载前检查缺失依赖 | `loader.ts` ~36 | 缺依赖→toast 提示 + 跳过加载

### E3e：通知系统全功能（5 任务，~575 行）✅

- [x] **#46** 通知进度条——showProgress + ProgressHandle | `NotificationService.ts` + `ProgressBar.tsx` ~290
- [x] **#47** 通知来源过滤 / Do Not Disturb | `NotificationService.ts` ~45
- [x] **#48** "Don't show again" 持久化 | `toast.ts` ~25 | `isCloseAffordance: true` → 点 × → 再发不弹；重启仍不弹
- [x] **#49** 完整 Notification Center 面板 | `NotificationCenter.tsx`（新） ~165 | 铃铛→未读/已读分组 + 时间标签
- [x] **#50** 通知 source 归类（按插件分组） | `NotificationCenter.tsx` ~50 | 面板里 terminal 和 marketplace 分开显示 + 未读徽章
  > 🔧 附带修复：铃铛面板打开时隐藏右下角 toast——`setToastsSuppressed()` 对标 VS Code。`3b748d0`

### E3f：壳 UI 收尾

**🔥 #52 第三次重写——TitleBar 自定义标题栏（HTML/CSS，插件可扩展）**

- [x] **#51** 标题栏暗色化——Electron nativeTheme + backgroundColor | `main.ts` + `preload-shell.ts` + `ThemeEngine.ts` ~25
- [x] **#52a** MenuRegistry 加 `children` 嵌套 ✅ — TitleBar + HamburgerMenu 共用
- [x] **#52b** 汉堡移入 IconBar 第一个 ✅ — `menuStyle: "hamburger"` 模式用
- [x] **#52c** 注册 File/View 菜单内容 ✅ — 两份渲染的共同数据源
- [x] **#52d** hover 展开子菜单 ✅ — TitleBar + HamburgerMenu 共用交互
- [x] **#52e** 原生 menubar 适配器 ❌ — 废弃，被 TitleBar 替代（#52i 清理）
- [x] **#52f** 🔥 TitleBar 组件——横排菜单 + 下拉面板 + 拖拽区 | `TitleBar.tsx`（新）+ `TitleBar.css`（新） ~60
- [x] **#52g** 🔥 `app.menuStyle` 配置项 + App.tsx 条件渲染 | `App.tsx` ~20 | 设置里切换 menuStyle → TitleBar/Hamburger 显隐
- [x] **#52h** IconBar 接收 `showHamburger` prop | `IconBar.tsx` ~5
- [x] **#52i** 删除原生 menubar 代码——5 文件（HamburgerMenu 保留） | ~−60
- [x] **#52j** coreCommands 菜单注册声明式——label + group + order | `HamburgerMenu.tsx` ~15

**🔥 #53 设置项齿轮——Settings Editor 每行 hover 齿轮（含 #59a 一键恢复默认，~65 行）**

> 对标 VS Code Settings Editor per-setting gear——四个齿轮的第三个。#59a "一键恢复默认" 是此齿轮中的一个菜单项。

- [x] **#53a** `MenuRegistry` 加 `MenuId.SettingItemGear` | +1
- [x] **#53b** 注册设置项齿轮菜单项——5 项 + when 条件（重置仅 `settingModified` 时显示） | `coreCommands.ts` +15
- [x] **#53c** `SettingsView.tsx` 每行加 hover 齿轮图标 + `<ContextMenu menuId={MenuId.SettingItemGear}>` | +20
- [x] **#53d** 齿轮打开前设 context key——`settingKey` + `settingModified` | `SettingsView.tsx` +10
- [x] **#53e** `core.openSettings` handler 支持 `scrollTo` 参数——跳转到设置中的具体配置项 | `coreCommands.ts` +19

**🔥 #53b 命令面板齿轮——QuickPick 每项 hover 齿轮（~50 行）**

> 对标 VS Code Command Palette per-command gear——四个齿轮的第四个。

- [x] **#53b1** `CommandRegistry` 注册时存储 `configurationKey` | `CommandRegistry.ts` + `loader.ts` +5
- [x] **#53b2** `MenuRegistry` 加 `MenuId.CommandPaletteItemGear` | +1
- [x] **#53b3** ~~注册命令面板齿轮菜单项——4 项 + when 条件~~ → ❌ **废弃**——VS Code 源码齿轮是单按钮，不做子菜单
- [x] **#53b4** `QuickPick.tsx` 加 `renderItemActions` prop + `CommandPalette.tsx` 齿轮按钮——点击复制命令 ID + toast | `QuickPick.tsx` + `CommandPalette.tsx` + `index.css` +68
  > 🔥 #59 升级：齿轮从"复制命令 ID"改为"配置快捷键"→ 打开快捷键设置页搜索框预填命令名。
- [x] **#53b5** 齿轮打开前设 context key | `CommandPalette.tsx` ~5
- [x] **#53b6** `plugin.schema.json` `contributes.commands` 加 `configurationKey` 字段 + 补 3 个 MenuId | +4

- [x] **#54** 输出面板 UI——频道选择器 + 日志列表 + 清空/导出 | `OutputPanel.tsx`（新） ~80
- [x] **#55** 欢迎页集成——三种状态切换 + recentFolders | `WelcomeView.tsx` +30
- [x] **#56** Workspace 导入导出 | `coreCommands.ts` + `App.tsx` ~40 | 导出→导入→布局/设置复原
- [x] **#57** 终端会话持久化——sessions.json 读写 | `useTerminalSessions.ts` +20
- [x] **#58** Developer: Toggle Plugin DevTools——QuickPick + WindowManager.toggleDevTools | `plugin-view-handlers.ts` + `App.tsx` ~20

**🔥 多 WebView 渲染激活（#58a-#58e）**

- [x] **#58a** 🔥 多 WebView 激活：IPC 线已通（`plugin-view:create` handler + `preload-shell` 暴露 + `loader.ts` 调用），E3a #24-#29 基础设施完整但从未有人调 `registerPlugin()`，#58a 补上了 create 这一步 ✅
- [x] **#58b** 🔥 多 WebView 渲染——插件自举入口：`plugin-view.html`（Vite 多页）+ `src/plugin-shell-main.tsx`。读 URL 参数 `?plugin-view=xxx` → 动态 import 插件模块 → `createRoot(document.body).render(<Component />)`。~35 行 ✅
- [x] **#58c** 🔥 多 WebView 渲染——URL 切换：`plugin-view-handlers.ts` 的 `create` handler 从占位 HTML 改成 `http://localhost:1420/plugin-view.html?plugin-view=${pluginId}`。~5 行 ✅
- [x] **#58d** 🔥 多 WebView 渲染——壳侧集成：`MainContent.tsx` 改渲染逻辑——有独立 WebView 的插件走 `setVisible` 显隐，壳内部类型仍走 React。~15 行 ✅
- [x] **#58e** 🔥 关掉 React fallback——WebView 渲染完成发 `plugin-view:ready` → 壳收到后才跳 React。6 文件信号链：plugin-shell → preload-plugin → main → preload-shell → MainContent readyWebViewIds state。渐进增强——不 ready 时 React 继续渲染兜底 ✅

### 🔥 #59 快捷键系统（~300 行）+ Encaron 实测 4 项（#59A-#59G，~184 行）

- [x] **#59** 设置页快捷键子栏——双 tab + 表格视图 + 冲突检测 | `SettingsView.tsx` + `KeybindingSettingsView.tsx` + `.css` + `KeybindingRegistry.ts` + `CommandPalette.tsx` + `coreCommands.ts` + `CoreEvents.ts` + `App.tsx` ~300

**🔥 #59 补完——Encaron 2026-07-28 实测**

- [x] **#59-A** 🔴 齿轮/命令跳转——先打开设置标签页再切快捷键 tab | `CoreEvents.ts` + `App.tsx` + `KeybindingRegistry.ts` ~9
- [x] **#59-B** 声明式自动索引——新插件快捷键表格自动刷新 | `KeybindingRegistry.ts` + `KeybindingSettingsView.tsx` ~15
- [x] **#59-C** 完整命令视图——所有已注册命令展示，无快捷键的显示 `—`，双击可新建绑定 | `KeybindingSettingsView.tsx` ~20
- [x] **#59-D** 行内编辑 ✓✕ 替代弹窗 + chord 双键捕获 + 字体归一化 `var(--font-mono)` | `KeybindingSettingsView.tsx` + `.css` ~80
- [x] **#59-E** 🔥 `removeUserKeybinding` 归一化——不限 source，删除该命令全部绑定后再注册 user 绑定 | `KeybindingRegistry.ts` ~5
- [x] **#59-F** 🔥 壳级快捷键开窗——补齐缺失命令 + 全部注册进 `registerKeybinding` + 删原始 keydown handler | `App.tsx` + `coreCommands.ts` ~30 | KeybindingRegistry 已成唯一路径
- [x] **#59-G** 快捷键行齿轮——有 user 绑定的行 hover ⚙ → "重置为默认" → 删 user 绑定 → 回退到 builtin/plugin 绑定 | `KeybindingSettingsView.tsx` ~15

**#59a 设置项一键恢复默认 → 已合并到 #53**

### 🔥 #59b FileDecorationRegistry——文件装饰器注册中心

- [x] **#59b** 🔥 FileDecorationRegistry | `FileDecorationRegistry.ts`（新） ~40 | Git 插件 `register({id:'git',provideDecoration})`→`getDecorations(uri)` 返回正确装饰→Git 卸载→`unregister('git')`→装饰消失

### 🔥 #59c `<SelectBox>` 归一化下拉组件——替代所有原生 `<select>`

> 原生 `<select>` 在 Electron 里走独立 OS 渲染通道——跟 Chromium 合成器节奏不同步，表现为无动画闪出。跟之前 `alert()`/`confirm()` 是同一类问题。解决方案一样——用自己画的 UI 替代原生控件。

- [x] **#59c** 🔥 `<SelectBox>` 归一化 | `SelectBox.tsx`（新） + 全项目替换 ~100 | ControlPanel COM/波特率/协议三个下拉→同组件；设置页/主题/语言选择器→同组件
  > **归一化要求：** 全局只有一个 `<SelectBox>` 组件——不同页面/插件传不同的 `options`、`value`、`onChange`。UI 行为统一：动画曲线、最大高度、搜索过滤（options > 8 时自动出现）、键盘导航（↑↓Enter Esc）。接口极简：`{ options: {value,label}[], value, onChange, disabled?, placeholder? }`。

### 🔥 #59d 强调色模式——跟随主题 vs 自定义（4 任务，~80 行）

> 设计决策：条件不满足时行**隐藏**（对标 VS Code）而不是灰色禁用。

- [x] **#59d0** 🔥 `ConfigurationProperty` 加 `dependsOn?: { key: string; value: unknown }`——声明式条件显隐 | `ConfigurationRegistry.ts` + `SettingsView.tsx` +10 | 任意配置项声明 `dependsOn: { key: "x", value: "y" }`→key 不是 y 时整行不渲染
  > **对标 VS Code `package.json` `when` 条件。5 行代码，一次写完，所有配置项通用。以后任何插件需要"选了 A 才出现 B"→ 加一行 `dependsOn` 声明。**
- [x] **#59d1** 注册 `app.accentMode` 配置项 + 提取 `getEffectiveAccentColor()` 归一化函数 | `App.tsx` + `ThemeEngine.ts` +15 | 三种路径——followTheme+有accent→主题色 / followTheme+无accent→自定义兜底 / custom→自定义
- [x] **#59d2** `onApply(app.theme)` + `ThemeBrowser.handleHighlight` 改走 `getEffectiveAccentColor()` | `App.tsx` + `ThemeBrowser.tsx` +20
- [x] **#59d3** 注册 `app.accentColor` 加 `dependsOn: { key: "app.accentMode", value: "custom" }` | `App.tsx` + `SettingsView.tsx` +15 | 选"跟随主题"→强调色行消失 / 选"自定义"→强调色行出现+色块
  > **这是 `dependsOn` 的第一个消费方——证明通用机制有效。**

### 🔥 #59e `<ColorPicker>` 归一化调色器——替代 `<input type="color">`（~200 行）

> 🔥 **UI 布局规格：** `06.5-ColorPicker-UI布局规格.md`——写 CSS 前必读。

- [x] **#59e** ColorPicker 归一化组件——二维饱和度面板+色相条+hex输入+预设色 | `ColorPicker.tsx`（新） ~200 | 接口：`{ value, onChange, presets? }`。布局：饱和度亮度面板+色相横条+hex输入框+预览方块+预设色圆点
- [x] **#59e2** 🔥 调色盘命令入口——`color-picker.pick` | `ColorPicker.tsx` ~5 | 插件调 `commands.execute('color-picker.pick', { initialColor: '#ff0000' })`→弹出浮层→用户选色→返回 hex；取消→返回 undefined
  > **Promise 桥接模式（对标 QuickPick #36a）：** handler 内 `new Promise(resolve => ...)` → `createRoot` 渲染 → `onChange`→`resolve(color)` + `root.unmount()`。插件只调 `commands.execute`，不知道浮层怎么弹出的。

### 🔥 #59f 注册覆盖静默→显式警告——防插件冲突（3 任务，~10 行）

> `console.warn` + 原注册者 pluginId——3 处各 ~3 行。和 #59 快捷键冲突红字互补：#59 管用户 UI 面，本任务管开发者日志面。

- [x] **#59f1** CommandRegistry：`registerCommand` 覆盖已有命令时 `console.warn` | `CommandRegistry.ts` +5
- [x] **#59f2** ConfigurationRegistry：`registerConfiguration` key 已存在时 `console.warn` | ⏭️ 已有（E2c #13 16.2 已实现）
- [x] **#59f3** FileAssociationService：`registerFileAssociation` ext 已有映射时 `console.warn` | `FileAssociationService.ts` +4

---

## 第 4 层：E3g API + V2 兼容（4 任务，~240 行）

> ⚠️ 最后做——消费 E3f 的 ☰ 基础版。

- [x] **#60** 动态 StatusBarItem——运行时创建 + dispose 自动清理 | `StatusBarService.ts`（新） ~50
- [x] **#61** contributes.icons——共享图标注册 + 引用 | `IconRegistry.ts` + `plugin.schema.json` ~30
- [x] **#62** ☰ 完整版——快捷键提示 + 禁用态灰显 + 插件菜单组 | `HamburgerMenu.tsx` ~100
- [~] **#63** V2 配置导入——已删除。无旧 V2 用户，不需要。

---

## E3h 美化专题——菜单归一化 + TitleBar 扩展（5 任务，~150 行）

> 🔥 **专门的美化 AI 做。** 核心：提取公共 `<MenuRenderer>` 归一化组件 → 消 TitleBar/HamburgerMenu 渲染重复。

- [x] **#64** `<MenuRenderer>` 归一化组件——从 TitleBar + HamburgerMenu 提取公共渲染逻辑 | `MenuRenderer.tsx`（新）+ `.css`（新） ~80 | 两个菜单行为和重构前完全一致
- [x] **#65** 快捷键显示归一化——⏭️ 跳过：TitleBar 是自己画的 HTML，保留快捷键
- [x] **#66** TitleBar 声明式扩展区域——左右 slot，插件 `contributes.titleBar` 注册 | `MenuRegistry.ts` + `TitleBar.tsx` + `loader.ts` ~40
- [x] **#67** 菜单 when 条件 + 禁用态——不满足条件灰显（不隐藏） | `MenuRenderer.tsx` ~20
- [x] **#68** CSS 统一审查——TitleBar/HamburgerMenu/ContextMenu 视觉一致性 | ~0 行（纯审查）

---

## E3i：插件全量净化——`terminal` → `serial-monitor`（4 任务，~110 行）

> 🔥 2026-07-27。新名 `serial-monitor`（串口监视器）——对标 Arduino IDE / PlatformIO。
> **执行顺序：68a 先 → 69 → 70 → 71。不可颠倒。**

- [x] **#68a** 🔥 前置归一化——提取 `APP_SCHEME` + `APP_NAMESPACE` 常量 + ESLint 补 `v3` 标识符拦截 | `constants.ts`（新）+ `main.ts` + `protocol.ts` + `preload-shell.ts` + `preload-plugin.ts` + `eslint.config.js` ~16 | grep `'linkdesk'` electron/ → 仅 `constants.ts` 中有字面量。**纯重构，零功能变化。**
- [x] **#69** 全量净化——目录 + 文件重命名 + plugin.json + 标识符（组件/类型/hook）+ CSS 类名 + 事件名 + localStorage key + i18n + 注释 + 测试 | `plugins/terminal/` → `serial-monitor/` + 4 文件重命名 + `src/components/terminal/` 迁入插件 ~80 | grep `terminal` plugins/serial-monitor/ → 零结果（除历史注释）
- [x] **#70** 布局迁移——`migratePluginIdRename("terminal", "serial-monitor")` | `LayoutService.ts` ~20 | 旧 `terminal-1`/`terminal-2` 标签页→自动变为 `serial-monitor-1`/`serial-monitor-2`；flag 幂等
- [x] **#71** 🔥 localStorage → PluginStateService——修掉双路径历史遗留 | `useSerialSessions.ts` ~15 | 2026-07-23 B3 补全时 AI 不知道 StorageService 已存在，直接写了 `localStorage.setItem`。ControlPanel 后来用了正确的 `setPluginStateValue`，但旧路径没对齐。改名后再修——避免先修再改名导致 key 也改两次。

---

## E3j：核心收口——时序归一化 + API 边界 + 双份渲染根因（12 任务，11 已完成，仅 #81 待新 AI）

> 🔥 2026-07-27。**E3 架构封站前最后一步——堵 IPC 消息乱序这道缝。**
>
> E3 执行过程中反复出现的时序 bug 分属两类根因：
> - **A 类（框架缝）**：跨进程/跨生命周期的异步操作执行顺序不确定。**LinkDesk 核心的责任。**
> - **B 类（React 生命周期）**：useEffect cleanup、stale closure、StrictMode 双重 effect。**每个 React 应用都有——插件作者自己处理的。**
>
> `#36j1`（`applyPostLoadSteps`）已堵了同进程内的加载路径缝。此处堵跨进程的 IPC 消息路径缝。
> **为什么不等 E4 之后：** E4 文件树+编辑器会产生大量跨进程 IPC。现在不堵 → E4 写出来的代码自带时序隐患 → 以后回头改 = V2 模式。

- [x] **#72** ✅ IPC 消息队列——同插件消息 FIFO 串行处理 | `ipc-bridge.ts` ~30 | `Map<pluginId, PromiseChain>`——每条消息入队后等前一条完成才执行；不同插件独立队列；插件卸载时清空队列。对调用方透明——`ipcRenderer.invoke()` 签名不变。
- [x] **#73** ✅ 验证高风险路径覆盖——插件加载/卸载/主题切换/语言切换走队列 | `ipc-bridge.ts` + `loader.ts` ~20 | IPC 队列加在 `handle()` 注册层——现有所有 `ipcRenderer.invoke()` 自动受益。

### 🔥 `linkdesk` API 边界——对标 VS Code `vscode` 命名空间（2 任务，~110 行）

> 当前插件代码手写 `ipcRenderer.invoke('plugin:ipc', { action: 'executeCommand', ... })`——action 名是隐式约定，参数和返回值无类型检查。
> **两层收口的关系：** IPC 队列（#72）保证消息顺序——底层保障。`linkdesk` API（#74）提供类型安全入口——顶层边界。插件不知道 IPC 细节，不知道队列存在。

- [x] **#74** ✅ 🔥 `linkdesk` API 命名空间——类型安全的插件 API 入口 | `linkdesk-api.ts`（新）+ `preload-plugin.ts` ~80 | 插件代码直接 `linkdesk.commands.executeCommand(...)`，不需要 import（对标 VS Code 扩展的全局 `vscode` 对象）。接口：`linkdesk.commands` / `linkdesk.configuration` / `linkdesk.menus` / `linkdesk.window` / `linkdesk.theme` / `linkdesk.language`
- [x] **#75** ✅ 🔥 插件代码迁移——`ipcRenderer.invoke('plugin:ipc', ...)` → `linkdesk.*` | `plugins/marketplace/` + `serial-monitor/` + `settings/` ~30 | grep `ipcRenderer.invoke\('plugin:ipc'` plugins/ 零结果（除 preload 自身）
- [x] **#76** ✅ 🔥 插件通知 API——`linkdesk.notifications.show()` | `preload-plugin.ts` + `IpcBridgeHandler.ts` ~30 | 插件调 `linkdesk.notifications.show("导出完成")`→右下角 toast；`show("处理中...", { progress: true })`→进度条模式

### 🔥 终端数据上桌——串口数据交到大厅（3 任务，~50 行）

> 终端是大厅里唯一的顾客——它收串口数据、显示、发回，全在自己房间内闭环。协议插件想解析数据、工作台卡片想展示字段——**拿不到。**

- [x] **#77** ✅ 🔥 终端推送原始串口数据到大厅数据管道 | `SerialContext.tsx` + `preload-plugin.ts` ~20 | 终端收到数据→同时调 `linkdesk.events.emit('serial:rawData', { portName, text })`；协议插件 `events.on('serial:rawData', callback)` 可订阅
  > **数据管道 = `events` 频道系统（preload 里的字符串管道）。** 频道名 `'serial:rawData'` 是终端自己起的字符串——核心不知道这个频道的存在。
- [x] **#77a** ✅ 🔥 events 归一化——提取 `electron/event-system.ts` 共享模块 | `event-system.ts`（新）+ `preload-plugin.ts` + `preload-shell.ts` ~90 | `createEventSystem(ipcRenderer, options)` → 一份代码，两处 import。改一处，两边都生效
- [x] **#77b** 🔥 ~~双份渲染无害共存~~ → ⚠️ 假设被推翻——回退守卫（~0 行净增）| 实测：串口数据只到打开端口的 webContents，不是两边都到。不存在重复 emit。#77b 的守卫把唯一管道堵了，已回退。崩的根因是壳缺 `events.emit`（#77a 修复），不是重复。
- [x] **#78** 🔥 终端连接状态推到大厅 events 频道 | `index.tsx` ~15 | `openPort` 成功→`events.emit('serial:connected', ...)`；USB 拔出→`serial:disconnected`
- [x] **#79** 🔥 终端发送能力——命令系统入口 | `index.tsx` ~15 | 终端注册命令 `serial-monitor.send`；工作台卡片调 `commands.execute('serial-monitor.send', data)`→数据经终端发到串口
  > **对标 VS Code：** terminal send 通过 `vscode.window.activeTerminal.sendText()`。
- [x] **#80** 🔥 QuickPick 命令入口——`quickpick.show` | `QuickPick.tsx` ~5 | 插件调 `commands.execute('quickpick.show', { title: '选择端口', items: [...] })`→弹出浮动列表→用户选→返回选中项；Esc→返回 undefined
  > **Promise 桥接模式——和调色盘 #59e2 同一模板。对标 VS Code `vscode.window.showQuickPick()`。**

### 🔥 #81 双份渲染根因修复——E3 封站最后一步 ✅

> 从 E3f #58d 到 E3j #77b，同一个问题捅了 5 次。前面的修修补补都是止血——#77a 归一化 events、#77b 回退守卫、#58e 信号链。三条根因从未被碰。

| 根因 | 症状 | 修复策略 |
|------|------|---------|
| ① 双份渲染同时存在 | 壳 fallback + WebView 各跑一份 SerialContext，互不知情 | **方向 B**：组件级生命周期 + 引用计数——不判断环境，mount 注册/unmount 清理 |
| ② `_initIPC()` 模块级永不清理 | 壳切空 div 后 IPC 监听器仍存活 | **方向 B 已修**：`_registerIPCListeners`/`_unregisterIPCListeners` 走 React useEffect |
| ③ 串口数据路由不确定 | 数据到壳还是 WebView 取决于谁先 openPort | **不修复**——#77b 实测推翻假设：数据只到一个地方 |

**策略：方向 B——组件级生命周期 + 引用计数。** 拆 `_initIPC` 为三函数：`_initOnce()`（一次性数据拉取，模块级 guard）/ `_registerIPCListeners()`（引用计数，第一个 consumer → 注册）/ `_unregisterIPCListeners()`（减引用，最后一个 consumer unmount → 全清）。壳 fallback 和 WebView 用同一套逻辑——不判断运行环境。壳切空 div → ControlPanel unmount → 自动清理监听器 → 零僵尸。**方向 A（环境感知）已废弃**——isPluginWebView 判别治标不治本。

- [x] **#81-1** 方向 A 尝试 → 废弃（commit `aef0e55` → `c7680c3` 修 onData 遗漏）
- [x] **#81-2** 🔥 方向 B——SerialContext 组件级生命周期 + 引用计数（commit `107b410`） | `SerialContext.tsx` ~60 行重写 | tsc 零错误 | vitest 220/220
- [x] **#81-3** 不需要修——壳 fallback 和 WebView 独立 JS 上下文（`registerCommand` 写入各自渲染进程的 `_commands` Map，物理隔离）
- [x] **#81-4** 不需要修——preload 层 IPC 监听器是应用生命周期级（`createEventSystem` 在 preload 中只加载不卸载，要一直活着接收广播）
- [x] **#81-5** 验证清单 8/8 全过——串口收发 / 壳 DevTools / 插件 DevTools / Console / 主题语言 / 插件市场 / 设置页 / tsc+vitest

### 🔥 支线：串口监视器多标签页修复（分支 `serial-port-refresh`，7 commits）

> 古早问题合集——端口列表不刷新 + 多标签页数据串流 + 翻译缺失。标签页诞生那天就存在，三层叠加。

- [x] **#82a** 端口下拉热插拔刷新——`refreshPorts` action + `SelectBox.onOpen` | `SerialContext.tsx` + `SelectBox.tsx` + `ControlPanel.tsx` ~14 行
- [x] **#82b** 系统消息端口名匹配——只让匹配的标签页激活 portOpenRef | `index.tsx` | 解析"已打开串行端口 COM13"→COM13，与 `activeSession.port` 比对
- [x] **#82c** ControlPanel per-tab connected——`openPort`/`closePort` 替代 `toggleOpen` 盲翻转 | `SerialContext.tsx` + `ControlPanel.tsx`
- [x] **#82d** `isMyPort` null 安全——新标签页未配端口时不匹配任何消息
- [x] **#82e** 🔥 `portOpenRef` 默认 false——新标签页出生即激活。根因：`useRef(true)` → 不等系统消息、不等打开端口，直接收数据
- [x] **#82f** ControlPanel mount 时刷新端口列表——避免首帧"无可用串口"
- [x] **#82g** 关闭会话弹窗翻译 key——`t("关闭会话「{{name}}」？")` 加 zh/en

> tsc 零错误 | vitest 220/220 全过 | 验证：两个标签页不同 COM 口 → 各自独立收发，互不串扰
