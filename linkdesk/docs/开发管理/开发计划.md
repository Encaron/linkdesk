# 开发计划

> 2026-07-24。**路线图权威。** Tauri 时代（P1-P5.5）已完成，下一站：Electron 迁移。

---

## 一、总览

```
Tauri 时代 (已完成)                Electron 时代 (即将开始)
═══════════════════              ═══════════════════════
Phase 1    架构 + 终端  ✅         E1   Electron 迁移
Phase 2    终端完善     ✅         E2   底层加固 + 侧栏扩展位
Phase 2.5  结构整理     ✅         E3   多 WebView + 壳收尾  🏁
Phase 3    标签页 + 分屏 ✅         此后：只写插件
Phase 3.5  品质打磨     ✅
Phase 4    插件系统     ✅
Phase 5    应用基础设施 ✅
Phase 5.5  交互对标    ✅
```

---

## 二、Tauri 时代——已完成

| Phase | 内容 | Git 分支 | 状态 |
|---|---|---|---|
| **Phase 1** | Tauri + React 脚手架 → 三栏布局 → 终端 CM6/Monaco → Rust 串口 → 全链路 | `phase5.5` | ✅ |
| **Phase 2** | HEX/发送历史/COM 枚举/编码/系统消息/TX-RX/右键/搜索/过滤/Command Palette | `phase5.5` | ✅ |
| **Phase 2.5** | TerminalView 拆分子组件 + 共享 UI + useTauriEvent + 单测 + Monaco 高亮 | `phase5.5` | ✅ |
| **Phase 3** | VS Code 风格标签页 + 递归分屏 + 拖拽 drop zone + keep-alive + 布局持久化 | `phase5.5` | ✅ |
| **Phase 3.5** | i18n 归一化/主题全覆盖/无障碍/动效/TS 收窄/去重/测试 109→151 | `phase5.5` | ✅ |
| **Phase 4** | 六类插件 + 插件市场 + 生命周期 + 通知系统 | `phase5.5` | ✅ |
| **Phase 5** | Registry 暗线 + 右键归一化 + 命令面板/齿轮 + when + StorageService + 类型去硬编码 + 运行时动态加载 | `phase5.5` | ✅ |
| **Phase 5.5** | 三栏交互对标 VS Code + 48/48 bug 修完 | `phase5.5` | ✅ |

> 完整历史：`../01-Tauri_P1至P5.5/`
> 最终锚点：`1e0b8a3`

---

## 三、Electron 时代——即将开始

### 前置操作（一次性）

```
1. git branch -m master main                    # 旧主线改名
2. git checkout main && git merge phase5.5       # Tauri 稳定版合入主线
3. git branch -m phase5.5 phase6                 # 冻结 Tauri
```

### E1：Electron 迁移

| | |
|---|---|
| **分支** | `electron-e1`（从 `main` 开出） |
| **任务** | 7 步——壳初始化 → 串口 → 文件/配置/插件 → 前端 API → linkdesk:// → 清理 → 打包 |
| **工时** | ~16h AI |
| **完工标准** | 软件行为与迁移前 100% 一致 |
| **设计文档** | `../02-Electron架构/E1_Electron迁移_暂定/` |
| **完成后** | `git merge electron-e1` → `main` |

### E2：底层加固 + 侧栏扩展位

| | |
|---|---|
| **分支** | `electron-e2`（从 `main` 开出） |
| **任务** | 23 项（#1-#23）——ErrorBoundary 增强 + 终端归一化 + 核心服务补齐 + 侧栏扩展位 |
| **完工标准** | ErrorBoundary 全覆盖 + 侧栏能挂文件树 |
| **设计文档** | `../02-Electron架构/E2_底层加固与侧栏扩展_暂定/` |
| **完成后** | `git merge electron-e2` → `main` |

### E3：多 WebView + 壳收尾

| | |
|---|---|
| **分支** | `electron-e3`（从 `main` 开出） |
| **任务** | 33 项（#24-#56）——多 WebView 进程隔离 + 主题/语言引擎跨进程 + Profile + 壳完善 |
| **完工标准** | 🏁 架构完工。此后框架永远不改 |
| **设计文档** | `../02-Electron架构/E3_多WebView与壳收尾_暂定/` |
| **完成后** | `git merge electron-e3` → `main` |

### 架构完工后——只写插件

```
main 分支上直接开发，不开新分支。

第一批：插件_文件树与编辑器/
第二批：插件_工作台与OLED/
第三批及以后：地图 / 逻辑分析仪 / ...
             任何新功能 = 写 plugin.json + React 组件，不碰框架
```

---

## 四、任务全表（69 项）

| Phase | 子任务 | 任务数 | 行数估算 |
|---|---|---|---|
| **E1** | Electron 迁移 7 步 | 7 | — |
| **E2** | E2a ErrorBoundary | 6 | ~140 |
| | E2b 终端归一化 | 6 | ~110/−95 |
| | E2c 核心服务补齐 | 7 | ~370 |
| | E2d 侧栏扩展位 | 4 | ~110 |
| **E3** | E3a 多 WebView | 5 | ~600 |
| | E3b 主题引擎 | 5 | ~290 |
| | E3c 语言引擎 | 5 | ~240 |
| | E3d Profile + 壳完善 | 18 | ~1,060 |
| **插件** | 文件树 + Monaco | 13 | ~930 |
| **合计** | | **69** | — |

> 详细任务清单：`../02-Electron架构/E1_Electron迁移_暂定/09-Phase重排-任务映射.md`
> 执行顺序：`../02-Electron架构/E1_Electron迁移_暂定/06-实施顺序.md`

---

## 五、文档索引

| 你要了解 | 读这个 |
|---|---|
| 当前状态 | `当前状态.md` |
| Git 策略 + 目录结构 | `工程管理与Git策略.md` |
| 日常开发 | `开发操作手册.md` |
| Tauri 时代详细记录 | `../01-Tauri_P1至P5.5/` |
| Electron 全部设计 | `../02-Electron架构/` |
| 未来插件设计 | `../03-插件制造/` |

---

> **现在：** `phase5.5` 分支，48/48 bug 已修，等待迁移。
> **下一个动作：** `master → main` → `merge phase5.5` → `phase5.5 → phase6` → 开 `electron-e1`
