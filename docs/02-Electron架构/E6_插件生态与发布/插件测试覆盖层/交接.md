# 交接 · L11 插件测试覆盖层（接力本）

> **怎么用**：每一棒收口时，**在最上面**加一段「会话 N 收口段」（⛔ 不回改旧段——倒序追加，最新在上）。内容照本页「接力规矩」第 3 条的六项写。
> **读的顺序（每棒开场）**：本页**顶部最新一段**（上一棒干了什么、下一棒从哪接）→ [00-整理档案](00-整理档案.md) §〇d（编排 ＋ 版本账 ＋ 6 条连带面）→ 自己那一格的 **`0N-任务-*.md`** → 起手干活。
> **状态**：🚧 **会话一（`#144`-`#146`）2026-09-25 已收口**（三格 ✅）；**会话二（`#147`/`#148`）2026-09-25 已收口**（两格 ✅）；**会话三（`#149`/`#150`）2026-09-26 已收口**（两格 ✅）；**会话四（`#151`/`#152`）2026-09-26 已收口**（两格 ✅，**最新段在顶部**）；**五排队（最后一棒）**。

---

## 接力记录（倒序，最新在上）

### 2026-09-26 · 会话四（`E6#151`/`E6#152`）· 收口段

**干了什么**

1. **`#151` marketplace 纯逻辑 15 疑似单元 —— 先裁决、后补测**（第一道工序是裁决，不是补测）：把立案的「15 个零测试」逐条 `grep -rn "<单元名>" src/__tests__/` ＋ **读 10 个既有测试文件实际断言了什么**，产出 15 行表（见下 **A 表**）。实测把「15 个的体力活」缩成 **7 个要动手的**——**4 个整文件真缺 ＋ 3 条藏在「已覆盖」单元里的薄通路**，其余 **7 条是假红**（`marketCatalog` / `marketSources` / `updateDiscovery` 三组同模块测试**按基名匹配看不见**）。
2. **`#151` 补 7 文件 / 47 例**（扁平 `src/__tests__/<单元名>.test.ts`，一单元一文件）：`searchState`(7) · `marketSourcesIO`(4) · `contribs`(11) · `catalogRowText`(7) ＋ 三条薄通路 `updateTargetFor`(6) · `marketSourcesFetch`(6) · `marketInstallStageLabel`(6)。文案类走**假 `t`**、网络类走**真 `fetch` 替身三态**、模块级单例走 `vi.resetModules()` ＋ 动态 `import()`（会话二/三的样板照抄，⛔ 未动共享 mock）。
3. **`#152` 35 行替身层裁决表 ＋ 6 个必做全部落地**（**硬上界一格没破**：`.tsx` 零改动、未从备用池扩范围、裁决表零空格）。35 行 = `grep -rl window.linkdesk` 实测名单（**25 `.ts` ＋ 10 `.tsx`**），其中 **3 个是注释假阳**（`downloadCounts` / `marketSources/index` / `updateDiscovery/index`——只在注释里提到它）⇒ **真依赖 32**。6 个必做 → **6 文件 / 99 例**：`pluginsStore`(15) · `catalogStore`(13) · `installFlow` **重试腿**(13) · `installGate`(11) · `downloadCounts`(18) · `useInstallAction`(29)。
4. 🔴 **`installFlow` 的发起腿早就有测试**（`marketplaceInstallDispatch.test.ts` 6 例——**经 `marketplaceShared` 桶命中、按文件名 grep 看不见**）⇒ 本笔只补它没覆盖的两条**重试腿**（`retryMarketInstall` / `retryMarketUpdate`），**没重复劳动**。这是本层「假红」的第二种机理（桶遮蔽）在本仓的落点。
5. ⛔ **共享 mock 一行未改**（`packages/plugin-sdk/src/vitest-setup.ts` 未碰，本仓 `vitest.setup.ts` 仍是一行指针）；⛔ **本仓生产码一行未改**（`git status` 只有 `src/__tests__/**`；另有一处**非本轮**的 `package.json` 残留，见「遗留」第 1 条）⇒ **两格都无 `fix:` 笔、插件版本 1.0.43 不 bump**。

**读数**

| 项 | 读数 |
|:--|:--|
| marketplace `npm test` | **23 文件 / 379 例全绿**，**连跑两遍**读数一致（会话一交接时：10 文件 / 233 例 ⇒ 本会话净增 **146 例**）|
| `#151` | 新增 **7 文件 / 47 例**（searchState 7 · marketSourcesIO 4 · contribs 11 · catalogRowText 7 · updateTargetFor 6 · marketSourcesFetch 6 · marketInstallStageLabel 6）⇒ **纯逻辑真缺整文件 4 → 0**（＋3 条薄通路补齐；另 7 条裁决为假红）|
| `#152` | 新增 **6 文件 / 99 例**（pluginsStore 15 · catalogStore 13 · marketplaceInstallRetry 13 · installGate 11 · downloadCounts 18 · useInstallAction 29）⇒ 验收判据「**安装链五件全部有测试**」达成（确认闸门 `installGate` · 闸门判据 `useInstallAction` · 目录 store `catalogStore` · 已装 store `pluginsStore` · 下载计数 `downloadCounts`）|
| marketplace `npm run verify` | 五段全过：lint ／ 跨插件 import ／ 字典 ／ 声明自洽 ／ 目录条目形态 |
| 本仓提交 | **两笔**（`#151` `841408a` · `#152` `a789d13`），⛔ 各自**只 stage `src/__tests__/`**；🟢 **两笔均已推 `origin/main`**，`git ls-remote --heads origin main` = `a789d13b17f514b1c820d694165e848fba10897c` 与本地 HEAD **逐字相同** |

**A 表（`#151` 交付物：15 条疑似逐条裁决）**

> **口径三条（先说清楚，否则表会被误读）**：
> ① **立案那句「44 个纯逻辑单元里 15 个零测试」用了两把尺子**——**44 = 宽口径**（非 barrel `.ts` 全量），**15 = 严口径**（再减 `use*` hook／直用 `window.linkdesk`／含 React）。我复核**严口径 = 17**，与账里「15」差的正是 `marketCategories.ts` 与 `installConfirmPayload.ts`——它们**有同名测试文件**、被基名匹配看见 ⇒ 立案读数把它们排除了。**同一格两个数两把尺子**，与会话三 file-tree 的 44/18 同形 ⇒ `#153` 写口径时**必须显式声明用哪把**。
> ② 「**已覆盖**」的判据 = **有既有断言穿过该文件的行为**（不是「每个导出都有专属一例」）——本层当下的**宽判据**，逐函数细账留给尺子（`#153`）。故「已覆盖」≠「测得很细」。
> ③ 表里唯一一条「移交」是 `useActionBits.ts`（名字是 hook ⇒ `#151` 详案明令归 `#152` 账；而它**不在** `#152` 的 35 行 `window.linkdesk` 名单里 ⇒ 最终归「后续轮次」，见下「后续轮次登记」）。

| # | 单元 | 行 | 裁决 | 依据（哪几条断言） |
|--:|:--|--:|:--|:--|
| 1 | `services/marketCatalog/parse.ts` | 58 | **已覆盖（假红）** | `marketCatalog.test.ts` 的 `describe("parseCatalog")` 组（头注「#30f 目录损坏空态判据」）＋ 该文件 **65 例**整体：`{version,plugins}` 缺字段 / 脏项 / 版本串 / 空态 |
| 2 | `services/marketCatalog/select.ts` | 136 | **真缺（一处）→ 已补** | `updateTargetFor`（`#73j` G6「住所闸」）在**全部既有测试里零引用**（既有只测 `stableLatestVersion` / `compareVersions` 等同类，见 `marketCatalog.test.ts:249`）⇒ 新 `updateTargetFor.test.ts` **6 例**（可更新判定的全站单点：`updateToVersion` ＋ 住所闸） |
| 3 | `services/marketCatalog/merge.ts` | 31 | **已覆盖（假红）** | `mergeCatalogs` 被 `marketCatalog.test.ts` 与 `updateDiscovery.test.ts` **两处**引用（多源合并取高） |
| 4 | `services/marketCatalog/semver.ts` | 52 | **已覆盖（假红）** | `marketCatalog.test.ts:149` `describe("compareVersions / isVersionNewer")`——`1.2.0` vs `1.10.0` 数值比较等 |
| 5 | `services/marketCatalog/sourceUrl.ts` | 85 | **已覆盖（假红）** | `marketCatalog.test.ts:48` `describe("isRawMarketplaceUrl / normalizeSourceUrl")`——`github.com/owner/repo` 页 → raw 归一 · 已是 raw 不动 |
| 6 | `services/marketSources/fetch.ts` | 58 | **真缺（真实 fetch 通路）→ 已补** | 既有测试**全走 `__setCatalogIO` 注入支**；`fetchText()` 的**真 `fetch`** ＋ `res.ok` 判定 ＋ `res.text()` ＋ **10s 超时 AbortController** 零断言 ⇒ 新 `marketSourcesFetch.test.ts` **6 例**（请求形 / 非 2xx / 坏 JSON / 超时 / 陈旧回落） |
| 7 | `services/marketSources/io.ts` | 77 | **真缺 → 已补** | `readCache` / `writeCache` / `removeCache` / `injectedFetch` / `CACHE_TTL_MS` 在**既有 10 个测试文件里零引用**（grep 实测）⇒ 新 `marketSourcesIO.test.ts` **4 例**（注入面 / 缓存读写 / 隐私模式退化） |
| 8 | `services/marketSources/load.ts` | 54 | **已覆盖（假红）** | `loadCatalog` 被 `marketSources.test.ts`（**23 例**：装载回落 / 状态机）与 `installGate.test.ts` 引用；`forceRefreshCatalog` 另被 `catalogStore.test.ts` 引用 |
| 9 | `services/updateDiscovery/plan.ts` | 88 | **已覆盖（假红）** | 三个导出 `planDiscovery` / `selectAutoCandidates` / `selectMetaEvictions` **均被 `updateDiscovery.test.ts`（68 例）直呼**（grep 实测逐个命中） |
| 10 | `services/marketplaceShared/messages.ts` | 136 | **部分真缺 → 已补** | 失败文案五件（`classifyInstallError` / `failText` / `installFailLabelKey` / `updateFailLabelKey` / `updateFailText`）已被 `marketplaceUpdateFailure.test.ts`（16 例）覆盖；**`marketInstallStageLabel` 八档只有 3 档被间接穿过** ⇒ 新 `marketInstallStageLabel.test.ts` **6 例** |
| 11 | `services/marketplaceShared/searchState.ts` | 26 | **真缺 → 已补** | 模块级订阅表，**零引用** ⇒ 新 `searchState.test.ts` **7 例**（`vi.resetModules()` ＋ 动态 import 取全新实例） |
| 12 | `services/marketSourceAdd.ts` | 31 | **已覆盖（假红）** | `settingsSwapSmoke.test.ts:54` / `:61` **直呼 `decideAddSource`**（加源决策：判重 / 归一） |
| 13 | `views/detail/DetailView/contribs.ts` | 48 | **真缺 → 已补** | 四组 contributes 解析，**零引用** ⇒ 新 `contribs.test.ts` **11 例**（⚠️ 其中一处实测不一致，见下「观察 1」） |
| 14 | `views/detail/DetailView/useActionBits.ts` | 81 | **移交（hook）** | 名字是 hook ⇒ 本格不碰（`#151` 详案明令归 `#152` 账）；实测它**不在** `#152` 的 35 行 `window.linkdesk` 名单里 ⇒ 最终归「后续轮次」 |
| 15 | `views/sidebar/ExploreView/catalogRowText.ts` | 27 | **真缺 → 已补** | 行文案派生，**零引用** ⇒ 新 `catalogRowText.test.ts` **7 例**（假 `t` 口径） |

**A 表裁决汇总**：**已覆盖（假红）7**（#1 · #3 · #4 · #5 · #8 · #9 · #12）＋ **真缺/薄覆盖 → 已补 7**（#2 · #6 · #7 · #10 · #11 · #13 · #15）＋ **移交 1**（#14）= **15** ✓。立案的「15 个零测试」里 **假红 7 条（47%）** ⇒ **把它当体力活会白干近一半**（会话三在 file-tree 上量到 27%、本仓近一半）。
⚠️ **勘误**：`#151` 那笔的**提交信息**把「真缺」写成了 3 个（漏列 `marketSources/io.ts`）——**以本表为准：真缺整文件 4 个**（`searchState` · `io` · `contribs` · `catalogRowText`）＋ **薄通路 3 条**。文件名与用例数（7 文件 / 47 例）两处一致，**不影响任何读数**。

**B 表（`#152` 交付物：35 行直用 `window.linkdesk` 的文件逐条去处）**

> **口径四条**：
> ① **「35」= `grep -rl "window.linkdesk" src`（`*.ts`/`*.tsx`，排除 `__tests__`）的实测名单**（25 `.ts` ＋ 10 `.tsx`），**不是**「纯逻辑单元」那把尺子——本表是**另一把尺**（与 A 表不可比）。
> ② 其中 **3 个是注释假阳**（`services/downloadCounts.ts` · `services/marketSources/index.ts` · `services/updateDiscovery/index.ts`——只在**注释**里提到 `window.linkdesk`）⇒ **真依赖 = 32**。
> ③ 「**已覆盖**」判据同 A 表（有既有断言穿过该文件的行为，宽判据）；「**移交后续轮次**」= 本层口径外（视图层 / hook / `register*` 装配 / 直用 IPC 的薄门面）——**不等于「不值得测」**。
> ④ **零空格**：必做 6 ＋ 已覆盖 9 ＋ 不需测 3 ＋ 移交后续轮次 17 = **35** ✓；⛔ `.tsx` 一个不测（本格硬上界）。

| # | 文件 | 行 | 类别 | 现有测试 | 裁决 |
|--:|:--|--:|:--|:--|:--|
| 1 | `components/ExtensionItem.tsx` | 131 | 视图 | — | 移交后续轮次（视图层） |
| 2 | `services/downloadCounts.ts` | 160 | 流（取数 ＋ hook） | —（**注释假阳**：只在注释里提 `window.linkdesk`） | **必做⑤ → 已补 18 例** |
| 3 | `services/installGate.ts` | 96 | 流（确认闸门） | — | **必做④ → 已补 11 例** |
| 4 | `services/installJobs.ts` | 153 | hook（job 镜像） | `installJobs.test.ts`（6 例） | 已覆盖 |
| 5 | `services/installedUpdateMeta.ts` | 179 | store／IPC | `installedUpdateMeta.test.ts`（20 例） | 已覆盖 |
| 6 | `services/marketSources/index.ts` | 34 | 桶（纯再导出） | — | **不需测**（纯桶；且属**注释假阳**） |
| 7 | `services/marketSources/sourceConfig.ts` | 59 | 流（读配置源） | `settingsSwapSmoke.test.ts`（`readConfiguredAuthorSources`） | 已覆盖（**经 `marketSources` 桶命中** ⇒ 按文件名 grep 看不见） |
| 8 | `services/marketplaceShared/catalogStore.ts` | 122 | store（目录数据源） | — | **必做② → 已补 13 例** |
| 9 | `services/marketplaceShared/commands.ts` | 105 | 侧效应注册（零导出） | — | 移交后续轮次（`register*` 装配面，同 file-tree 口径） |
| 10 | `services/marketplaceShared/installFlow.ts` | 148 | 流（安装 / 重试） | `marketplaceInstallDispatch.test.ts`（6 例，**经桶命中**） | **必做③ → 已补 13 例**（发起腿已覆盖，本笔补 `retryMarketInstall` / `retryMarketUpdate`） |
| 11 | `services/marketplaceShared/notifications.ts` | 105 | 流（失败通道） | 经安装链 / 更新链**间接**穿过（`settleInstallFailure` / `settleUpdateFailure` 无直呼） | 已覆盖（**间接**；直呼断言登记为可选加深项） |
| 12 | `services/marketplaceShared/pluginsStore.ts` | 170 | store（**全仓 UI 数据源**） | — | **必做① → 已补 15 例** |
| 13 | `services/packageFiles.ts` | 27 | 纯壳面转发 | —（`readInstalledPackageFile` 零引用） | **不需测**（27 行纯转发） |
| 14 | `services/updateDiscovery/apply.ts` | 26 | 流 | `updateDiscovery.test.ts`（`applyEngineAutoUpdate` 直呼） | 已覆盖 |
| 15 | `services/updateDiscovery/index.ts` | 71 | 桶 | — | **不需测**（**注释假阳** ＋ 桶） |
| 16 | `services/updateDiscovery/messages.ts` | 64 | 文案 | `updateDiscovery.test.ts`（`updateBellMessage` 直呼） | 已覆盖（部分：`notifyAutoResult` **零直呼** ⇒ 登记可选加深项） |
| 17 | `services/updateDiscovery/run.ts` | 180 | 主编排 | `updateDiscovery.test.ts`（`runUpdateDiscovery` / `__clearRunningDiscovery` / `runAutoUpdateIfDue` / `enableAutoUpdateAndRun` **四导出直呼**） | 已覆盖 |
| 18 | `services/updateDiscovery/schedule.ts` | 61 | 调度 | `updateDiscovery.test.ts`（`scheduleStartupDiscovery` / `__resetUpdateDiscovery` 直呼） | 已覆盖 |
| 19 | `services/updateDiscovery/snapshot.ts` | 58 | 纯组装 ＋ 读盘 | `updateDiscovery.test.ts`（`assembleInstalled` 直呼） | 已覆盖（部分：`readInstalledSnapshot` **零直呼** ⇒ 登记可选加深项） |
| 20 | `views/confirm/ConfirmInstall.tsx` | 123 | 视图 | — | 移交后续轮次 |
| 21 | `views/detail/DetailView.tsx` | 287 | 视图 | — | 移交后续轮次 |
| 22 | `views/detail/DetailView/FeaturesTab.tsx` | 83 | 视图 | — | 移交后续轮次 |
| 23 | `views/detail/DetailView/useCompatReading.ts` | 59 | hook | — | 移交后续轮次 |
| 24 | `views/detail/DetailView/useDetailActions.ts` | 133 | hook | — | 移交后续轮次（**在备用池**） |
| 25 | `views/detail/DetailView/useDetailIdentity.ts` | 134 | hook | — | 移交后续轮次（**在备用池**） |
| 26 | `views/detail/DetailView/useDetailVersionAction.ts` | 112 | hook | — | 移交后续轮次 |
| 27 | `views/detail/DetailView/useDetailVersions.ts` | 147 | hook | — | 移交后续轮次（**在备用池**） |
| 28 | `views/detail/DetailView/useInstallAction.ts` | 127 | hook（**用户点得到的那一下**） | — | **必做⑥ → 已补 29 例** |
| 29 | `views/sidebar/BuiltinListView.tsx` | 44 | 视图 | — | 移交后续轮次 |
| 30 | `views/sidebar/DisabledListView.tsx` | 99 | 视图 | — | 移交后续轮次 |
| 31 | `views/sidebar/ExploreView.tsx` | 133 | 视图 | — | 移交后续轮次 |
| 32 | `views/sidebar/ExploreView/useCatalogInstall.ts` | 53 | hook | — | 移交后续轮次 |
| 33 | `views/sidebar/InstalledListView.tsx` | 56 | 视图 | — | 移交后续轮次 |
| 34 | `views/sidebar/SearchView.tsx` | 121 | 视图 | — | 移交后续轮次 |
| 35 | `views/sidebar/SearchView/AddSourcePopup.tsx` | 105 | 视图 | — | 移交后续轮次 |

**B 表裁决汇总**：**必做 6 → 全落地**（6 文件 / 99 例）＋ **已覆盖 9**（#4 · #5 · #7 · #11 · #14 · #16 · #17 · #18 · #19）＋ **不需测 3**（#6 桶 · #13 纯转发 · #15 桶）＋ **移交后续轮次 17**（#1 · #9 · #20–#27 · #29–#35，其中 10 个是 `.tsx` 视图）= **35** ✓。**备用池 6 个一个没用上**（6 必做按原名单全部落地，**未发生替换**）。

**后续轮次登记清单（⛔ 本层不测，交会话五汇总——「登记 ≠ 遗漏」）**

- **10 个 `.tsx` 视图**：`ExtensionItem`(131) · `ConfirmInstall`(123) · `DetailView`(287) · `FeaturesTab`(83) · `BuiltinListView`(44) · `DisabledListView`(99) · `ExploreView`(133) · `InstalledListView`(56) · `SearchView`(121) · `AddSourcePopup`(105)——**硬上界明令不测**（视图测试要吃 jsdom 交互与壳面替身，成本是纯逻辑的 5–10 倍 ⇒ 属**另一层**）。
- **7 个 hook / 装配面**：`marketplaceShared/commands.ts`(105，纯侧效应注册) · `useDetailActions`(133) · `useDetailVersions`(147) · `useDetailIdentity`(134) · `useDetailVersionAction`(112) · `useCompatReading`(59) · `useCatalogInstall`(53)——均 React hook（取数面靠壳 IPC）。**其中三个在备用池**（`useDetailActions` / `useDetailVersions` / `useDetailIdentity`）。
- **`#151` 移交来的 1 个**：`DetailView/useActionBits.ts`(81)——不在 35 名单里（**未直用壳面**）。
- **备用池另外 1 个不在 35 名单里的**：`ExploreView/useCatalogStatus.ts`(89)——同因（未直用 `window.linkdesk`）⇒ 它是本层下一步**最明显的候选**，但按硬上界**不由本格接手**。
- **5 条「已覆盖但无直呼」的可选加深项**（现状 = 经链间接穿过，**不是**断言）：`updateDiscovery/snapshot.ts` 的 `readInstalledSnapshot` · `updateDiscovery/messages.ts` 的 `notifyAutoResult` · `marketplaceShared/notifications.ts` 的 `settleInstallFailure` / `settleUpdateFailure` / `MARKET_SOURCE`。⇒ ⚠️ 在尺子「按函数切」的口径下这 5 个会被判**未覆盖**——**这是判据要决定的事**（`#153`），不是本会话漏做。

**两条观察（⛔ 只报不改、⛔ 未写进任何断言——把待裁决的行为固化成正典是本层明令禁止的做法）**

1. 📝 **`services/.../DetailView/contribs.ts` 的 `commands` / `keybindings` 解析遇 `contributes` 里的 `null` 项会抛**（**缺陷候选，本轮未修**）：`cmdIdOf` **直读 `c.id`**（`contribs.ts:21`，从 `:33` 可达）⇒ `contributes.commands: [null]` 直接崩；而 `menus` 走 `it?.command` **免疫**。**为何是问题**：该文件头注写的是「个别坏项不崩整组」，**实测不符**。**为何生产可达**：壳侧 `validateInstallManifest`（`src/pluginLoader/discovery/manifest.ts:80`）**只校 `pluginId`/`version`/`name`**，`contributes` **在装载路径上零校验**；`public/schemas/plugin.schema.json` 只喂**作者侧**校验器（`packages/plugin-sdk/src/validate.ts`，jsonc ＋ ajv）——**两条路不是同一条**。⇒ 装一个 `contributes.commands: [null]` 的插件，详情页会整个报错。**本轮处置**：新 `contribs.test.ts` 的 fixture **不含 `null` 项**、**不把该抛写进断言**（`{}` / `{title}` 这类缺字段的坏项代码本就能容忍，照现状测）。⇒ **留给用户/后续轮次**：装在装载面补校验 ∨ `contribs` 加防御 ∨ 保持现状（谁改谁补钉子）。
2. 📝 **`services/marketplaceShared/pluginsStore.ts` 的三条生命周期订阅在卸载时撤不掉**（**缺陷观察，本轮未修**）：`useMarketplacePlugins` 的 `init()` 是 **async**，三条 `events.on(…)` 与 `_dataListeners.add(rerender)` 都在 **`init()` 内部**，而**撤销它们的清理函数是 `init()` 的 `return` 值**——它交给了 **promise**，**不是** effect 的返回值（effect 只 `return () => { active = false; }`）⇒ 组件卸载后：**三条订阅仍在**、`_dataListeners.delete(rerender)` **永不执行**。**表现**：壳侧每个 `plugin:installed` / `plugin:uninstalled` / `plugin-lifecycle:changed` 触发 **N 次重拉**（N = 历史挂载次数）、**已卸载组件的 `setTick` 仍被调用**。**对照**：同一 hook 里 `viewContainer:changed` 那条走的是 effect 返回的清理 ⇒ **卸载即撤**——**同一个 hook 两条通道行为不一致**，正是这条泄漏的旁证。**本轮处置**：`pluginsStore.test.ts` 里那条卸载用例只对 `viewContainer:changed` 断言（**它确实撤掉了**），并在注释里登记本条、**不写断言钉住现状**。⇒ **留给用户拍板**：修法 = 把三条订阅的注册挪进 effect 体内（或让 `init()` 把清理函数回传、由 effect 返回）——**属真缺陷候选，不止是测试问题**。

**遗留与延后（下一棒必须知道）**

1. ⚠️ **本仓工作树仍有一处 `package.json` 残留**（会话一记的「容器四处残留」之一：`devDependencies.@linkdesk/plugin-sdk` `^0.1.41` → `^0.1.46`，疑上一会话 `npm install <包>@0.1.46` 的副产物）——**本会话两笔一路未动**（提交时**只 stage `src/__tests__/`**）⇒ 仍在工作树里。**对正确性无影响**：`^0.1.41` 的 caret 覆盖 lock 里的 **0.1.47**，`npm ci` 装出来的版本一致（判据同会话一 `E6#146` 的负控：`npm ci` 只看「锁定版本满足范围」，该组合实测不红）。⛔ 本格验收写明「`package.json` 一字未动」⇒ **不由本格提交，留用户/会话五裁决**（提交这份声明与 lock 对齐 ∨ `git checkout` 丢掉）。
2. ⛔ **6 必做未发生任何替换，备用池 6 个一个未用**——本格**没有扩范围**。
3. **后续轮次登记**见上（17 移交 ＋ 5 条可选加深项 ＋ `#151` 移交的 `useActionBits` ＋ 不在 35 名单的 `useCatalogStatus`）。
4. **本仓仍无测试门禁**：`ci-verify.mjs` **无测试段**（有意，同会话一/二/三）；`#153` 尺子**只报不拦** ⇒ 四道待拍板题未决前**不许接任何门禁**。
5. ⛔ **没抢后面的活**：`#153`/`#154`（覆盖尺 ＋ 销账）**一格未碰**；第三方仓 `geme-tihu-bicycle` **零改动**（`git status --porcelain` 空）。

**下一棒提示（会话五：`#153`/`#154`，最后一棒）**

1. **本会话给尺子（`#153`）的三条输入**：① **口径必须显式声明**——本层同一个数字在两把尺子下不可比（file-tree 44 宽 / 18 严；marketplace 纯逻辑 **44 宽 / 17 严**、直用壳面 **35 宽 / 32 真依赖**）；② **A/B 两张表里「已覆盖」共 16 条，判据都是宽的**（「有断言穿过」≠「每个导出有一例」）——尺子若按函数切，会把这 16 条里的一部分翻成「未覆盖」，**这是判据题不是漏做**；③ **两条缺陷候选**（`contribs` 的 `null` 抛 · `pluginsStore` 的订阅泄漏）——尺子要不要把它们算成「该补测试的地方」、还是**先修再测**，见 `06` 第 ④ 题。
2. **`#153` 实跑 19 仓时本仓的预期读数**：纯逻辑层「真缺 → 0」（A 表 7 条已补）· 本仓**无测试文件门禁**（有意）· 5 条「已覆盖但无直呼」的导出（见上）在紧口径下会进报表 ⇒ **它们现在只是「经链穿过」，不是断言**。
3. **`#154` 销账**：本层五仓读数与两笔提交都在本页；`E6-执行清单.md` 的 `#151`/`#152` 两格**已补读数**；⚠️ `docs/05-插件更新/00-README.md §三` 的销账**仍归会话五**（本会话一格未碰）；⚠️ 收尾时把 `06` 四道题**整页**带给用户。

---

### 2026-09-26 · 会话三（`E6#149`/`E6#150`）· 收口段

**干了什么**

1. **`#149` file-tree —— 先裁决、后补测**（本格的第一道工序是裁决，不是补测）：把**44 个非 barrel 单元**逐条裁决成表（见下「裁决表」一栏，44 行带依据）。实测结果**推翻了立案的三条预判**——「疑似零测 11 条」= **假红 2**（`FileTreeModel/node.ts` 已被 `FileTreeModel.test.ts` 的 `sortOrder` 组穿过；`components/CompactFolder.ts` 已被 `CompactController.test.ts` 的 13 例穿过——**同名判据看不见跨文件覆盖**）＋ **纯类型零运行时 1**（`FileTreeModel/host.ts` 只有 `interface TreeHost`，一行运行时代码都没有）＋ **真缺 8** ⇒ 把 11 当体力活会白干 3 个。
2. **`#149` 真缺 8 单元全补**（扁平 `src/__tests__/<单元名>.test.ts`，一单元一文件；⛔ 未引 RTL——本仓本就不需要）：`FileTreeModel/emitter`（7）· `FileTreeModel/reload`（10）· `FileTreeDnD/dropTarget`（15）· `fileIconRuntime`（4）· `FileTreeKeyboard/scrollToItem`（6）· `utils/expandedUrisStateKey`（6）· `FoldersView/fsWatcher`（5）· `FileTreeContextMenu/commands/shared`（6）。替身**一律住本仓测试文件**（假 `TreeHost`／假 model／假 `window.linkdesk.notifications`），会话二那条「共享 mock 是最小面、插件专属桩留本仓」照抄。
3. **`#150` editor —— 6 单元全补，延后归零**（`monaco-bootstrap` 那个「本格唯一例外口」**没用上**）：`utils/MiniEmitter`（6）· `EditorView/reveal`（5，假 editor 对象）· `lsp-bridge` 门面（4，**引用相等**判转出）· `lsp-bridge/registry`（16）· `lsp-bridge/notice`（8）· `services/monaco-bootstrap`（7）。`monaco-bootstrap` 实测**在 vitest 下完全可做**——整包 mock `monaco-languageclient/vscodeApiWrapper` ＋ `.../workerFactory` ＋ `monaco-editor` ＋ 八个 `?url`/`?worker` 资产导入即可，零真 Worker；钉住四条竞态教训在代码里的形状（补丁前 `getMonaco()` 必抛 / `bootstrapMonaco()` 幂等且并发不各起一套 / 模块顶层 `MonacoEnvironment.getWorker` 按 label 分派 / `?url` 产物经 `new URL(…, import.meta.url)` 锚定后才交 workerFactory）。
4. **`#150` settings —— 3 单元按镜像路径补齐**：`views/SettingsView/filterGroups`（10）· `.../renderControl/stringList`（11）· `.../SettingRow/useSettingRowBadges`（13，hook）。hook 那件**按任务书走法 (a)**：加 `"@testing-library/react": "^16.3.2"`（**版本逐字抄 `scripts/sync-plugin-ci.mjs` 的 `DEV_DEPS`**，与 serial-monitor/marketplace 同版）＋ `npm install --registry=https://registry.npmjs.org` 刷 lock ⇒ **同笔提交**（⚠️ 见下「遗留」第 1 条：该笔入 `package.json` 时**不可避免地带上了会话一已报的 devDep 残留**）。
5. ⛔ **共享 mock 一行未改**（`packages/plugin-sdk/src/vitest-setup.ts` 三仓都没碰，各仓 `vitest.setup.ts` 仍是一行指针）；⛔ **三仓生产码一行未改**（`git status` 只有 `src/__tests__/**`；settings 另加 `package.json` ＋ `package-lock.json`）⇒ **三仓都无 `fix:` 笔、插件版本全不 bump**。

**读数**

| 项 | 读数 |
|:--|:--|
| file-tree `npm test` | **14 文件 / 128 例全绿**，**连跑两遍**读数一致（会话一交接时：6 文件 / 69 例 ⇒ 净增 **59 例**）｜随后的 `fix:` 笔改成 **130 例**（原「已展开才拉子项」那条把 bug 当预期，替换成两条钉子 ＋ 新增一条 ⇒ +2） |
| file-tree `#149` | 新增 8 文件 / 59 例（emitter 7 · reload 10 · dropTarget 15 · fileIconRuntime 4 · scrollToItem 6 · expandedUrisStateKey 6 · fsWatcher 5 · shared 6）⇒ **纯逻辑真缺零测试 8 → 0**（另 3 个「疑似」裁决为假红 2 ＋ 不需测 1，见裁决表） |
| file-tree `npm run verify` | 五段全过：lint 73 文件**零偏离** · 跨插件 import 71 文件 **0 互引** · 字典 72 key · 2 视图 render **兑现** · 目录条目图标绝对 URL。⚠️ 字典**黄灯 4 个 `t()` key**（`个文件/个匹配/打开文件夹/最近`）＝ `lang-defaults` 提供的**既有黄灯、不拦**，非本轮引入 |
| editor `npm test` | **8 文件 / 74 例全绿**，**连跑两遍**（会话一交接时：2 文件 / 28 例 ⇒ 净增 **46 例**） |
| editor `#150` | 新增 6 文件 / 46 例（MiniEmitter 6 · reveal 5 · lsp-bridge 4 · registry 16 · notice 8 · monaco-bootstrap 7）⇒ **纯逻辑零测试 6 → 0（延后 0）** |
| editor `npm run verify` | 五段全过：lint **42 文件零偏离** · 跨插件 import 40 文件 0 互引 · 字典 47 key · 2 视图 render 兑现 · 图标绝对 URL。⚠️ 字典**黄灯 3 个 `t()` key**（`无法打开文件/保存失败：/加载中…`）＝ 既有黄灯、不拦 |
| settings `npm test` | **9 文件 / 65 例全绿**，**连跑两遍**（会话一交接时：6 文件 / 31 例 ⇒ 净增 **34 例**） |
| settings `#150` | 新增 3 文件 / 34 例（filterGroups 10 · stringList 11 · useSettingRowBadges 13）⇒ **纯逻辑零测试 3 → 0**；devDeps 加 `@testing-library/react ^16.3.2` ＋ lock 同笔 |
| settings `npm run verify` | 五段全过：lint **37 文件零偏离** · 跨插件 import 35 文件 0 互引 · **本仓无字典声明**（45 个 `t()` key 里 44 个不在自有字典——`lang-defaults` 提供，既有黄灯不拦） · 1 视图 render 兑现 · 图标绝对 URL。⚠️ 附加腿（**报告不拦**）spacing-grid **5 处**、font-scale 0 处——审美校准档、本轮不是对象 |
| 三仓提交 | **各自一笔**（⛔ 未揉一笔）：file-tree `test:E6#149`（`c7d40d4`）· editor `test:E6#150`（`93c2db6`）· settings `test:E6#150`（`b6a2b60`，含 devDeps ＋ lock）；🟢 **三仓均已推 `origin/main`**，`git ls-remote --heads origin main` 三个 SHA 与本地**逐字相同** |

**裁决表（`#149` 交付物：file-tree 44 个非 barrel 单元逐条）**

> **口径两条（先说清楚，否则表会被误读）**：
> ① **「44」是宽口径** = `src/**/*.ts` 去掉 `.d.ts` / `.test.ts` / barrel（`types.ts`·`index.ts`·`constants.ts`）后的**全部**文件——立案读数表用的就是这把尺子。而同一格里的「**11**（其中零测试）」是**严口径**（再减 `use*` hook / 直用 `window.linkdesk` / 含 React 的文件）——**同一格两个数用了两把尺子**，见下「⚠️ 口径警告」。
> ② 本表「**已覆盖**」的判据 = **有既有断言穿过该文件的行为**（不是「每个导出都有专属一例」）——这是本层当下的宽判据，**逐函数细账留给尺子（`#153`）口径定稿**，故「已覆盖」≠「测得很细」。
> ③ 「**不需测**」= 纯类型零运行时，或**本层口径外**（hook / 视图交互层 / `register*` 装配层 / 直用 IPC 的薄门面）——口径外是**范围**判断，**不等于「不值得测」**，其中带实质逻辑的候选见下表后的「口径外候选清单」。

| # | 单元 | 行 | 裁决 | 依据 |
|--:|:--|--:|:--|:--|
| 1 | `components/CompactFolder.ts` | 42 | **假红 · 已覆盖** | `CompactController.test.ts`（13 例）的 `isCompacted` 5 例与 `getCompactedSegments` 3 例即本文件的 `getCompactedPath` 两条分支；`CompactController` 直接转调它（`services/CompactController.ts:10` 导入 · `:19`/`:29` 两处调用） |
| 2 | `components/FileTree/flatten.ts` | 62 | 口径外（视图装配） | `flattenTree` 是视图渲染的展平；同文件 `loadCompactFolders`/`setCompactFolders` 走 IPC（`window.linkdesk` = 1 处） |
| 3 | `components/FileTree/useTreeConfig.ts` | 72 | 口径外（hook） | React hook（`useTreeConfig`），IPC 2 处 |
| 4 | `components/FileTree/useTreeExpansion.ts` | 30 | 口径外（hook） | React hook（`useTreeExpansion`） |
| 5 | `components/FileTree/useTreeRename.ts` | 70 | 口径外（hook） | React hook（`useTreeRename`），IPC 3 处（重命名写盘） |
| 6 | `components/FileTree/useTreeResize.ts` | 51 | 口径外（hook） | React hook（`useTreeResize`）——拖拽宽度 |
| 7 | `components/FileTree/useTreeSelection.ts` | 111 | 口径外（hook） | React hook（`useTreeSelection`）——多选/框选 |
| 8 | `FileTreeContextMenu/commands/clipboard.ts` | 92 | 口径外（`register*` 装配） | `registerClipboardCommands` 是菜单项装配；**真正的复制/剪切逻辑在 `services/FileTreeClipboard.ts`，已被既有 6 例覆盖** |
| 9 | `…/commands/compare.ts` | 30 | 口径外（`register*` 装配） | `registerCompareCommands` 只装配"选中两项 → 开 diff"，判定与动作都在 `window.linkdesk.compare` 侧 |
| 10 | `…/commands/files.ts` | 119 | 口径外（`register*` 装配） | `registerFileCommands` 新增/删除/重命名等的菜单装配与可用性判定，写操作全落 IPC |
| 11 | `…/commands/menuItems.ts` | 67 | 口径外（`register*` 装配） | `registerFileTreeMenuItems` 菜单结构装配 |
| 12 | `…/commands/navigation.ts` | 115 | 口径外（`register*` 装配） | `registerNavigationCommands` 打开/揭示类菜单装配（IPC 4 处） |
| 13 | `…/commands/shared.ts` | 34 | **真缺 → 已补** | 新 `shared.test.ts`（6 例）：`placeholder` 的 warn 与 async 契约 · `refreshDirSafe` 先刷目录 / 已展开才拉子项 / 未展开不越懒加载边界 / 抛错不冒泡且**只走 FileTreeNotify 一个出口** / 成功路径零通知（⚠️ 其中「已展开才拉子项」那条**正是把真 bug 当预期** —— 它就是下面「观察 1」，已按用户拍板由 `fix:` 笔 `7bfe7e9` 换成「等 refresh 做完才算完」＋「已展开目录不读第二遍」两条钉子，本文件现 8 例） |
| 14 | `FileTreeContextMenu/host-bridge.ts` | 45 | 口径外（React 视图桥） | `h`/`OpenFileFn` 的 ref 桥（React 类型 + 读写 ref），属视图层 |
| 15 | `services/CompactController.ts` | 57 | **已覆盖** | 既有 `CompactController.test.ts` **13 例**：`isCompacted` 单链/二级链/多子目录/空/`children=null`、`isIncompressible` 三态、`expandCompact`/`collapseCompact` 的 `_uncompacted` 集合、`getCompactedSegments` 三态——每条都是本文件独有的分支 |
| 16 | `services/FileExcludeFilter.ts` | 152 | **已覆盖** | 既有 `FileExcludeFilter.test.ts` **12 例**：`*.log`/`**/.git/**`/`node_modules` 三类模式、并集、空模式、`false` 值、`setGitignore`/`clearGitignore`/重配置/`clear`、`buildNestingMap` 两态 |
| 17 | `services/FileTreeClipboard.ts` | 61 | **已覆盖** | 既有 `FileTreeClipboard.test.ts` **6 例**：cut/copy 的 `isCut`/`isEmpty` 四态、cut 后 `pull` 一次性清空 vs copy 后可多次、`file-tree.resourceCut` 上下文键的开与关 |
| 18 | `services/FileTreeDecoration.ts` | 84 | 口径外（直用 IPC） | `FileTreeDecorationService` 读配置走 IPC（1 处）⇒ 按 §四 口径属直用面；**内含实质逻辑**（装饰区间计算），见下候选清单 |
| 19 | `services/FileTreeDnD/dropTarget.ts` | 44 | **真缺 → 已补** | 新 `dropTarget.test.ts`（15 例）：`getDropTargetIndex` 取整/滚动量/负数/字号缩放 · `resolveDropTarget` 目录/文件/越界/空树 · `isAncestorOf` 后代/自身/源是文件/前缀非路径段/反向/未归一六态。⚠️ 注：既有 `FileTreeDnD.test.ts` 测的是 `executeSafeDrop`，它**自己内联**了祖先判据（`t.startsWith(s + "/")`），与 `isAncestorOf` 不是同一段代码 ⇒ 同名判据看不见本模块 |
| 20 | `services/FileTreeDnD/execute.ts` | 49 | **已覆盖** | 既有 `FileTreeDnD.test.ts` **10 例**：拦截三态（祖先→后代 / 自己→自己 / 同路径）、合法路径放行、多源部分拦截、空源、copy 不调 remove、move 调 copy＋remove、Windows 反斜杠归一、含 `..` 路径 |
| 21 | `services/FileTreeDnD/useFileTreeDnD.ts` | 138 | 口径外（hook） | React hook，IPC 2 处 |
| 22 | `services/FileTreeKeyboard/navigation.ts` | 154 | 口径外（直用 IPC） | `handleNavKey` 键位分派 + IPC 动作（2 处）；**内含实质逻辑**（键位表），见候选清单 |
| 23 | `services/FileTreeKeyboard/scrollToItem.ts` | 17 | **真缺 → 已补** | 新 `scrollToItem.test.ts`（6 例）：容器 null · 目标在视口上/下方 · 已可见不动 · 贴顶贴底相等算可见 · 字号缩放 ⇒ 滚动量随之变 |
| 24 | `services/FileTreeKeyboard/typeAhead.ts` | 47 | 口径外（hook） | 导出 `useTypeAhead`（React hook） |
| 25 | `services/FileTreeModel/emitter.ts` | 27 | **真缺 → 已补** | 新 `emitter.test.ts`（7 例）：订阅+派发 · 退订 · 无监听器静默 · **错误隔离**（前一个抛不挡后一个）· **错误隔离不粘** · **派发中退订**（本轮收不到、且持久）· `dispose` |
| 26 | `services/FileTreeModel/host.ts` | 17 | **不需测（纯类型）** | 全文只有 `export interface TreeHost`——**零运行时**；它的形状由新 `reload.test.ts` 的假 `TreeHost` 在**编译期**守住（TS 报错即红） |
| 27 | `services/FileTreeModel/locate.ts` | 125 | 口径外（直用 IPC） | 模块顶层 `const lk = window.linkdesk` ⇒ 按 §四 口径属直用面；**内含实质逻辑**（`getAncestorsOf`/`rollbackExpanded`/`revealToUri`），见候选清单 |
| 28 | `services/FileTreeModel/node.ts` | 48 | **假红 · 已覆盖** | `FileTreeModel.test.ts` 的 `sortOrder` **4 例**（default/foldersNestsFiles 目录优先 · filesFirst 文件优先 · type 按扩展名 · modified 按修改时间倒序）正是本文件 `sortItems` 的分支；`toExplorerItem` 经模型建树与 `reload.test.ts` 的假宿主穿过 |
| 29 | `services/FileTreeModel/reload.ts` | 51 | **真缺 → 已补** | 新 `reload.test.ts`（10 例）：已展开目录原子重载 · **懒加载边界不因刷新松动** · 未展开只清缓存不加载 · 文件/未知路径不动 · **三个辅助函数都不自己 fire** · 清缓存边界=已展开节点+所有根 · 根展开时重载根 · `getChildren` 抛错被吞（console.error）· 只碰"目录 && 已展开"子项 · `children=null` 早退 |
| 30 | `services/FileTreeNotify.ts` | 63 | **已覆盖**（薄 IPC 出口） | 新 `shared.test.ts` 通过假 `window.linkdesk.notifications` 断言：失败路径 `show` **恰好一次**、`opts = {type:"error", source:"file-tree", persistent:true}`、成功路径 **`show` 零调用**——即本文件 `notifyFailure` 的对外契约被钉住 |
| 31 | `services/fileIconRuntime.ts` | 23 | **真缺 → 已补** | 新 `fileIconRuntime.test.ts`（4 例）：默认走壳 codicon 保底 · 未换解析器前同实例 · `updateIconResolver(主题映射)` 主题生效且未命中回落主题顶层默认 · `updateIconResolver()` 回到 codicon（**卸载不是"保持上一次"**） |
| 32 | `utils/expandedUrisStateKey.ts` | 20 | **真缺 → 已补** | 新 `expandedUrisStateKey.test.ts`（6 例）：无参回旧 key `expandedUris`（既有数据零迁移）· `ws-2`/`ws-12` 带窗维度 · 与别的参数并存仍按 `wsWindow` · 六种脏值全部回退旧 key · 每次调用现算（不缓存） |
| 33 | `utils/layoutTokens.ts` | 28 | **已覆盖** | 既有 `layoutTokens.test.ts` **3 例**：基准行高 26px · scale 100 ⇒ 26px · scale 125 ⇒ 32.5px；另 `dropTarget`/`scrollToItem` 两个新文件用 `setUiFontScale` 反向钉住"行高随字号缩放"这条跨模块不变量 |
| 34 | `utils/pathUtils.ts` | 80 | **已覆盖（宽判据）** | `normalizePath` 由**两条既有断言直接钉**（`FileTreeModel.test.ts`"setRoots 去反斜杠——normalizePath 生效"、`FileTreeDnD.test.ts`"Windows 反斜杠——normalize 后再比较"＋"路径含双点——归一反斜杠但不解析 `..`"）；`joinPath`/`dirname`/`basename`/`extension` 经建树、寻找最近根、拖放目标几条链间接穿过。⚠️ **这条是本表最宽的"已覆盖"**——逐函数细账留给 `#153` 口径定稿 |
| 35 | `views/FoldersView/fsWatcher.ts` | 28 | **真缺 → 已补** | 新 `fsWatcher.test.ts`（5 例）：一批事件派发给每个订阅者 · 退订即摘 · **退订幂等**（React 严格模式 cleanup 可能重入）· 一个订阅者抛错不挡其余 · **空批照常派发** |
| 36 | `views/FoldersView/useExpandPersistence.ts` | 28 | 口径外（hook） | React hook，IPC 1 处；展开态落盘的**键生成**已在 `expandedUrisStateKey.test.ts` 里单测 |
| 37 | `views/FoldersView/useExplorerConfig.ts` | 85 | 口径外（hook） | React hook，IPC 1 处 |
| 38 | `views/FoldersView/useFsChangeRefresh.ts` | 61 | 口径外（hook） | React hook——把 `fsWatcher` 的事件接到刷新（本层已测两端的**纯逻辑部分**：`fsWatcher` 的派发与 `reload` 的重载） |
| 39 | `views/FoldersView/useWorkspaceRoots.ts` | 111 | 口径外（hook） | React hook，IPC 2 处 |
| 40 | `views/SearchView/openMatch.ts` | 20 | 口径外（直用 IPC） | 打开命中项——IPC 2 处 |
| 41 | `views/SearchView/useReplaceAll.ts` | 67 | 口径外（hook） | React hook，IPC 2 处 |
| 42 | `views/SearchView/useSearch.ts` | 144 | 口径外（hook） | React hook，IPC 1 处 |
| 43 | `views/SearchView/useSearchHistory.ts` | 26 | 口径外（hook） | React hook，IPC 3 处 |
| 44 | `views/SearchView/useSearchKeyboard.ts` | 40 | 口径外（hook） | React hook |

**裁决汇总**：**已覆盖 9**（#1 假红 · #15 · #16 · #17 · #20 · #28 假红 · #30 · #33 · #34）＋ **真缺→已补 8**（#13 · #19 · #23 · #25 · #29 · #31 · #32 · #35）＋ **不需测 1**（#26 纯类型）＋ **口径外 26** = **44** ✓。立案的「疑似 11」= #1 · #19 · #23 · #25 · #26 · #28 · #29 · #31 · #32 · #35 ＋ #13（`commands/shared.ts` 亦在疑似清单内）⇒ 其中**假红 2 ＋ 纯类型 1 ＋ 真缺 8**。

**口径外候选清单（带实质逻辑、留给后续轮次/`#153` 口径定稿时参考，⛔ 本层不测）**：`services/FileTreeModel/locate.ts`（125 行，`getAncestorsOf`/`rollbackExpanded`/`revealToUri`）· `services/FileTreeKeyboard/navigation.ts`（154 行，键位分派表）· `services/FileTreeDecoration.ts`（84 行，装饰区间计算）· `components/FileTree/flatten.ts`（62 行，展平+紧凑态读写）· `FileTreeContextMenu/commands/{files,menuItems,navigation,compare,clipboard}.ts`（30–119 行，菜单装配＋可用性判定）· `views/SearchView/*`（20–144 行）。**它们的共同形状 = "顶层 `const lk = window.linkdesk` 一行就把整文件挡在口径外"**——`#153` 定口径时要决定：这类文件算「口径外的装配层」还是「该按函数切出纯逻辑部分来测」（另见本页「待拍板」第 4 题）。

**⚠️ 口径警告（给 `#153` 尺子的输入，本轮实测发现）**：`00-整理档案` §〇b 的**括号定义**（「纯逻辑单元 = 去掉 `.d.ts`/`.test.ts`/barrel 后，**内容不含 `window.linkdesk` 与 React** 的那些」）与它**自己那一列的数字**（file-tree **44**）**用的不是同一把尺子**——按括号定义严格算，file-tree 只有 **18** 个（44 − 26 个口径外）；**44 是"全部非 barrel `.ts`"这个宽口径**。同格的「其中零测试 **11**」倒是严口径。⇒ **尺子（`#153`）写口径时必须显式声明用哪把尺子**，否则「零测试 16→0」这种读数在不同尺子下不可比。本层**不改** `00-整理档案`（⛔ 倒序追加、不回改旧档），把差异记在这里当输入。

**两条观察（补测笔当时 ⛔ 三仓生产码一行未改、无 `fix:` 笔；第 1 条后经用户拍板修根并发版，第 2 条仍只报不改）**

1. ✅ **`file-tree/src/components/FileTreeContextMenu/commands/shared.ts` 的 `refreshDirSafe` 少一个 `await`**（**真 bug；用户 2026-09-26 拍板「修根 ＋ 发版」⇒ 已修完、已发完**）：
   - **哪仓**：file-tree（`shared.ts:26-33` 附近）。
   - **症状**：`model.refresh(dir)` **未被 await**，紧接着 `await model.getChildren(item)` 在"目录已展开"分支上会**看到 `children === null`**（refresh 的磁盘重读还没回来）⇒ 触发**第二次并发 `listDir`**（与 refresh 内部那次重复），并**多派发一轮变更事件**（撤销展开/刷新时表现为多余的树重渲与滚动跳动）。若当初写了 `await`，`getChildren` 会在"已加载"分支早退、不再重复拉盘。
   - **判据**：`refresh` 是 async 且 `getChildren` 的口径是"`children !== null` 即早退"（`FileTreeModel` 的懒加载契约，`reload.test.ts` 的"未展开只清缓存不加载"与"三个辅助函数都不自己 fire"两条已把这个契约钉在测试里）；不 await ⇒ 两次调用**竞态**，谁先回来不由调用方决定。
   - **修法（实际采用 = 修根版）**：`await model.refresh(dir)` ＋ **删掉那三行补读**（`findClosest` / `isExpanded` / `getChildren`）。判据：`refresh` 对**已展开**目录自身就会重读子项并 `fire`（`refreshTree` 判的 `expanded.has(uri)` ≡ `isExpanded(uri)`——**同一个 `_expanded` Set**，已核源码），折叠目录又不需要子项 ⇒ 那次补读是**纯重复劳动**，不是"保险"。⚠️ 只加 `await` 也能消掉并发读盘，但**仍会多 fire 一轮**（`getChildren` 的"已加载"分支**也** fire）⇒ 修根版把两样一起去掉；顺带把一个**此前无人接的拒绝**（`refresh` 内部 async 抛）纳进了 `catch`。⚠️ 补测笔当时**故意不把「重复调两次 `getChildren`」写进断言**（会把竞态固化成正典）；这次**反过来**——既已修，就把「不读第二遍」钉死，防它哪天被当"保险"加回来（`shared.test.ts` 现 **8 例**，旧那条"已展开才拉子项"已被换掉）。
   - **涉及版本（已执行，2026-09-26）**：file-tree `fix:` **`7bfe7e9`**（⛔ 与补测笔 `c7d40d4` **不同笔**，6 文件 / +59 −13）⇒ PATCH **1.0.15 → 1.0.16**（`package.json` ＋ `plugin.json` 同值 ＋ `CHANGELOG.md` 段）⇒ `npm run build`（68330 B）⇒ `npm run publish -- --yes` = Release **v1.0.16**（资产下载回 **sha256 逐字节一致** `e09413c1…255a`）⇒ 目录收录 **`2d30e18`**（`sync-official-catalog` 增量：更新 1 / 新增 0 / 同版未动 18）⇒ 壳 `sync:bundled -- --latest` 把 ledger ＋ 种子盒追到 1.0.16。它是**出厂种子仓**，这一串缺一不可（同版不同内容 ⇒ `check-bundled-freshness` 判红）。
2. 📝 **`services/FileTreeDnD/dropTarget.ts` 的 `isAncestorOf` 在本仓零生产调用方**（观察，非 bug）：全仓 grep 只有定义处与 barrel `FileTreeDnD/index.ts:10` 的转出——`executeSafeDrop` **自己内联**了同语义前缀判（`execute.ts`：`t.startsWith(s + "/")`）。即：**同名判据看不见的那段代码，生产其实也没用**。⛔ 本轮不接线也不删（删导出属"改生产码"、可能与外部消费方契约冲突），新 `dropTarget.test.ts` 仍给它补了 5 例锁住语义（**将来谁接线，语义已经被钉住**）。⇒ 留给用户/后续轮次：**接线 ∨ 删 ∨ 保持**。

**遗留与延后（下一棒必须知道）**

1. ⚠️ **settings 那笔 `package.json` 不可避免地带上了会话一已报的 devDep 残留**：本仓工作树原有 `@linkdesk/plugin-sdk` 范围 `^0.1.41`→`^0.1.46` 的一笔**非本轮**改动（会话一记的"容器四处残留"之一），而 RTL devDep **必须写进同一个文件** ⇒ 无法绕开，**已随本笔一并提交并在提交信息里声明**。同一笔里还有会话一未处置的 **`package.json` 1.0.17 与 `plugin.json` 1.0.18 漂移**（⛔ 本轮一行未动，只此声明）。editor 的 `package.json` 残留（`^0.1.19`→`^0.1.46`）**本轮同样未动**（提交时**只 stage 了 `src/__tests__`**，故它仍在工作树里）。
2. ⚠️ **两仓字典黄灯**：file-tree 4 个 `t()` key · editor 3 个 · settings 44/45 个——**都是 `lang-defaults` 提供的既有黄灯、不拦**，非本轮引入（三仓 `verify` 五段均绿）。
3. **延后项归零**：本格任务书里唯一允许延后的两处（file-tree「非 RTL 不可」与 editor `monaco-bootstrap`）**都没有用上**——file-tree 的 8 个真缺全是纯逻辑（⛔ 未引 RTL），`monaco-bootstrap` 实测在 vitest 下可做 ⇒ **本格无"单元＋理由＋归属轮次"待登记项**。（同会话二的结论：**"疑似要真环境才能测"的延后，先量一量再登记**。）
4. **两仓仍无测试门禁**：`ci-verify.mjs` 三仓均**无测试段**（有意，同会话一）；`#153` 尺子**只报不拦** ⇒ 四道待拍板题未决前不许接任何门禁。
5. ⛔ **没抢后面的活**：`#151`-`#154`（marketplace 两格 / 覆盖尺 / 销账）**一格未碰**；第三方仓 `geme-tihu-bicycle` **零改动**。

**下一棒提示（会话四：`#151`/`#152` marketplace）**

1. **`#151` 的第一步同样是裁决，且本格已经证明它会改变计划**：会话三在 file-tree 上实测"疑似 11"里**假红 3 个（27%）**——marketplace 的三组（`marketCatalog/*` · `marketSources/*` · `updateDiscovery/plan.ts`）预计同形，**先读 10 个既有测试文件再动手**。
2. **抄会话三的裁决表格式**（本页上面那份 44 行）：列「单元 / 行 / 裁决 / **依据**」，且**先声明口径**（宽口径数=44 vs 严口径数=11 的教训；`#152` 要交的 35 行 `window.linkdesk` 直用单元表会**直接撞上"口径外还是切函数测"这个待拍板题**）。
3. **技法直接复用**：① 模块级容器（Map/Set 型注册表、`_bootstrapped` 型单次标志）⇒ `vi.resetModules()` ＋ 动态 `import()`（⛔ 不可与 `renderHook` 同用）；② 外部库整包 mock 的写法见 `editor/src/__tests__/monaco-bootstrap.test.ts`（`vi.mock` 库入口 ＋ 资产导入 ＋ `vi.hoisted` 承载共享替身状态）；③ hook 取 IPC 面 ⇒ `vi.mock` 掉**本仓自己的**取数 hook（settings 那份 `useSettingRowBadges.test.ts` 是样板），⛔ 不动共享 mock。
4. ⛔ 三仓已踩坑提示：**假 red 的根因是"同名判据看不见跨文件覆盖"**——`grep -rn "<单元名>" src/__tests__/` 只能帮你找到**同名**，真正的判据要读那些测试文件**实际断言了什么**（file-tree 的 #28 被 `sortOrder` 组覆盖、#1 被 `CompactController` 覆盖，都是读出来才发现的）。

### 2026-09-25 · 会话二（`E6#147`/`E6#148`）· 收口段

**干了什么**

1. **`#147` 起点仓纯逻辑 16 单元 → 零测试清零**：`src/__tests__/` 下**一单元一文件**共 16 份（`RingBuffer` · `autoFormatHex` · `portFilter` · `text` · `saveFile` · `pluginId` · `SerialMonitorView/settings` · `getters` · `SerialContext/status` · `receiveSnapshots` · `commandBridge` ＋ cm6 五件 `appendLine`/`decorations`/`scroll`/`search`/`theme`）。cm6 五件全走 `EditorState` **反射断言**（`EditorState.create({doc, extensions})` → `state.update({changes/effects}).state` → `state.field(f)`；装饰用 `RangeSet.between(from,to,cb)` 读；⚠️ `EditorView.theme()` 实测返回 `[prefixFacetProvider, styleModule, darkFlagProvider]`，取 `[1].value.getRules()` 才拿到可断言的 CSS 文本）——**零真视口、零 DOM**；文案类用**假 `t`**（未引真字典）。
2. **`#148` 替身层 6 必做 ＋ 2 个「可裁决延后」判为可做并做掉**：新文件 5 份（`useSerialSessions/store` · `useSerialSessions/persist` · `useReceiveStream` · `useReceiveLines` · `useSendData`；第 6 条必做 `useSerialSessions/useSerialSessions` 落在**既有**文件里）＋ **`useReceiveSnapshots`/`useSerialConnection` 不延后**——实测前者只要一个真 `RingBuffer` ＋ 真快照 Map、后者只要接管 `events.on`，**都不需要真视口/真 DOM**，「贵」的延后理由不成立 ⇒ **延后归零**。既有 `useSerialSessions.test.tsx` **只补 hook 面缺口例**（10→23：重复 id / 未知 id / 连续创建与色板轮转 / per-会话默认值隔离 ＋ 新增 `useSession`（per-tab）整段）。
3. **两条样板落进本仓测试文件当正典**（会话三/四照抄这两份，⛔ 别各自发明）——**非新能力**申报（§8.4 ③）：本段只是**既有 hook 替身写法的复述**，零新 API 面、零新增 `contributes` 条目，故无 8 维度设计前置：
   ```ts
   // ① 插件专属桩住本仓测试文件（⛔ 别改共享地基——改了波及全生态）
   window.linkdesk.serial = { sendText: vi.fn(), onData: vi.fn((cb) => { dataCb = cb; return () => {}; }) } as never;
   // ② 接管 IPC 通道抓回调：handler 存模块级 let/Map，测试再手动触发
   onSpy = vi.fn((name, h) => { handlers.set(name, h); return unsub; }); lk().events = { on: onSpy, emit: vi.fn() };
   ```
   正典文件：`src/__tests__/useReceiveStream.test.ts` 头部（①，含 serial 后门 `onData/onSystem` 两条通道）· `src/__tests__/useSerialConnection.test.ts` 头部（②，`events.on`）。
4. ⛔ **共享 mock 一行未改**：`packages/plugin-sdk/src/vitest-setup.ts` 没碰，本仓 `vitest.setup.ts` 仍是一行指针。

**读数**

| 项 | 读数 |
|:--|:--|
| serial-monitor `npm test` | **24 文件 / 260 例全绿**，**连跑两遍**读数一致（会话一交接时：1 文件 / 10 例 ⇒ 净增 **250 例**；提交 `9bf767a` 计量 **24 文件 +3,428 / −1 行**） |
| `#147` | **16 文件 / 138 例**（RingBuffer 8 · autoFormatHex 10 · portFilter 5 · text 12 · saveFile 3 · pluginId 3 · settings 8 · getters 9 · status 10 · receiveSnapshots 8 · commandBridge 8 · appendLine 13 · decorations 11 · scroll 8 · search 10 · theme 12）⇒ **纯逻辑零测试 16 → 0** |
| `#148` | **7 新文件 / 99 例**（store 16 · persist 9 · useReceiveStream 19 · useReceiveLines 14 · useSendData 17 · useReceiveSnapshots 14 · useSerialConnection 10）＋ 既有 `useSerialSessions.test.tsx` **10 → 23（+13 例）** ⇒ 本格 **112 例** |
| `npm run verify` | 五段全过：lint 80 文件**零偏离** · 跨插件 import **0 互引** · 字典 129 key 全在 · 2 视图 render **全兑现** · 目录条目图标绝对 URL。⚠️ 字典**黄灯 8 个 `t()` key**（`断开/打开/确定/取消/开/关/新会话/---- {{name}}：{{value}} ----`）＝ `lang-defaults` 提供的**既有黄灯、不拦**，非本轮引入 |

**两条踩坑（会话三/四/五照抄可省一轮红）**

1. 🔴 **`vi.useFakeTimers()` 之后再 `vi.setSystemTime()` ⇒ rAF 永远追不到**：rAF 的到期时刻按**旧时钟基**算，`advanceTimersByTime()` 到不了（实测 `fired 0`、`getTimerCount()` 恒 1；连带一批「`renderLine` 0 调用」的假失败）。**必须一次给全 `vi.useFakeTimers({ now: new Date(...) })`**（实测含反复 install/uninstall 均 `fired 1`）。
2. **`vi.resetModules()` 与 `renderHook` 不可同用**——要测模块私有状态（如 `persist.ts` 的 `_initDone`）走 dynamic `import()`，别裹进 RTL。

**裁决结论**：`#147`/`#148` 两格**都不 bump 版本**（测试不进 `.linkdesk-plugin` 产物，已解包实测）· 🟢 **已推三仓**（容器 serial-monitor / 壳 / 官方目录仓——用户 2026-09-25 对本任务系列内一次性授权）· 容器 = serial-monitor **两笔**：补测 `test:`（#147/#148 同笔）＋ 🔴 修复 `fix:` **`bd14f72`**（补测真发现 bug ⇒ 用户 2026-09-26 拍板，包 PATCH 1.0.22→1.0.23，⛔ 与测试笔不同笔；八条裁决里**只有 ⑦ 是缺陷**，其余七条有意设计／不可达／低危）· 第三方仓 `geme-tihu-bicycle` **零改动**。

**遗留与延后（下一棒必须知道）**

1. **本仓 8 条「观察到、照现状写进断言」的行为**——⛔ 本轮一行生产码未改，证据＝对应测试的断言与注释：① `_applyStore` 各字段**独立窄化**（`sessions` 是脏值时 `activeSessionId`/`colorIndex` 照样被写）② `_applyStore({activeSessionId:null})` **不清**本地 active id ③ `mergeStatus.lastError` **粘性**（新状态不清旧错误）④ `appendLine` 裁剪后仍留 ~1502 行（`CM6_TRIM_KEEP_LINES = 500`）⑤ `RingBuffer(capacity=0)` **静默丢一切** ⑥ 重复显式 id **建两条会话**、`setActiveSession(未知 id)` 指向不存在的会话 ⑦ `lineEnding` 存**转义文本**（`"\\r\\n"` 四字符）而 `quickSends` 默认值存**真控制符**——两条默认值口径不同 ⑧ 暂停缓冲告警文案里的 **2000 硬编码**（不是 `PAUSED_BUFFER_MAX`）。
   - 🔎 **八条已逐条裁决（2026-09-25 补查 memory ＋ git 历史，用户点名「这些插件当时是和壳一起做的」）⇒ 0 条现存可达 bug；详情全文见 `E6-执行清单.md` 的 `#148` 格「🔎 八条已逐条裁决」**：
     - ① **有意防御**（`_readLocal` 兼容改名前的旧键 `linkdesk:terminal:sessions` ⇒ 旧数据缺字段时跳过该字段、余者照收；同形防御见 `SerialContext/status.ts` 头注「历史 DTO 防御性字段」）。
     - ② **有意窄化 ＋ 现无实害**（能产 `null` 的只有「删最后一个会话」与 `resetAll`；前者 UI 紧接着关掉那个标签页、后者**生产零调用方**；残留 id 在 UI 上与 null 等价，也不会复活会话）⇒ 记下的不变量 = 「删会话必关其标签页」。
     - ③ **有意设计、有档案**（memory `e5.8-serial-multiport-phase6.md`「边界交代」：错误**不按口过滤**——同口二开拒绝到达时投影口名可能未更新，按口过滤会吞错误提示；P1 同句）⇒ 别当 bug 改。
     - ④ **行为有意、名字不副实（低危）**：常量早于可考历史（壳 `-S` 最老 `0bf6ed373 2026-07-22` 已在）；实算触发时保留 **~1502 行**（不是 500）——保留窗口更大对用户更有利、上限 2000 恒成立 ⇒ 功能无损，⛔ 本轮不动。
     - ⑤ **防御式下界、生产不可达**（生产只 `new RingBuffer(RING_BUFFER_CAPACITY)` 一处，512）。
     - ⑥ **生产不可达**（`createSession` 只被 `createSession(name)` 调；显式 id 写入者只有 per-tab 的 `useSession`）⇒ 宽门面，不是可达缺陷。
     - ⑦ 🔴 **唯一真缺口、修法分叉 ⇒ 已停手待用户拍板**：`quickSends` 是**正文**、只追加 ending、**原文照发**，故值必须是真控制符；但新会话默认值 `{ "AT": "AT\\r\\n" }` 是**转义文本** ⇒ **默认 AT 药丸会发出字面 `A T \ r \ n` 四个字符 ＋ 真 CRLF**，而 `settings.ts` 的无会话兜底恰是真控制符（还会叠成双换行）⇒ 同一功能两处默认值口径相反。⛔ **已排除「内联时丢了正文解转义」这条回归假设**（内联前 `06b96c13e^:src/core/react/useSendData.ts` 与今日逐字同构）⇒ 老约定、非本轮引入。修法 (a) 统一两处默认值为真控制符（`{ AT: "AT" }` 最干净，ending 反正追加 CRLF）／(b) 发送链对正文解转义（⚠️ 改掉已存真控制符数据的语义）——**选哪条要用户一句话** → ✅ **2026-09-26 用户拍板 (a)，已修**：容器**第二笔** `fix:` **`bd14f72`**（包 PATCH **1.0.22→1.0.23**，⛔ 与补测笔不同笔）——两处默认值统一 `{ AT: "AT" }`、口径规则写进源码与测试注释、`useSendData.test.ts` 补**两条回归钉子**（默认值走完整管道 ⇒ 线上恰好 `AT\r\n`；无会话兜底与 `DEFAULT_SESSION` 深同值）、`CHANGELOG.md` 补 v1.0.23 段；本仓 `npm test` 24 文件 / **262 例**绿 ＋ `verify` 五段绿。⚠️ **已存会话里的旧值不受影响**（仍按原文发；在药丸编辑框里改一次即成正文字节）——要不要补「旧默认值一次性迁移」另案；🟢 **已推送、已发版、已收录**（用户 2026-09-25 开口授权本任务系列内的推送与版本升级）：容器 `main` → `origin`（`da66431` ＋ 发布追加 `395f186`）· 壳 `electron` → `origin`（`bc8dd645e`）· Release **v1.0.23** · 官方目录仓 `7c014bb` · 账本追新到 1.0.23（纯市场、仅账）。⚠️ 途中两条真事实：**`publish` 不重新构建**（上传的是仓内现成的 `.linkdesk-plugin` ⇒ bump 后必须先 `npm run build`；我漏了，首版资产包内仍 1.0.22，被账本门禁「目录与产物不符」抓住，重建后 `gh release upload --clobber` 换资产 ＋ 下载回逐字节复核）；**release 直链有 CDN 缓存**（换资产后直链仍旧字节约 3 分钟，API/`gh` 即时可见）。
     - ⑧ **今日等价、潜在腐化（低危）**（`useReceiveStream.ts:72` 字面量 2000 ＋ `:73` 文案写死；常量一改即错位）⇒ 后续轮次/用户拍板。
   - ⚠️ **下一棒（会话三）不必重查这八条**，但若某仓出现同类形状（默认值/常量口径不一、字段独立窄化），照本页这段的查法办：**memory 找设计档案 → 壳仓 `git log -S` 找最早出现点 → 生产调用点 grep 判可达性**。
2. ⚠️ **既有遗留（会话一已报，本轮仍未动）**：serial-monitor 工作树里那份**未提交的 `package.json`**（devDep 范围 `^0.1.41`→`^0.1.46` 残留）——**本轮同样一行未动**；同仓 `package.json` 1.0.21 与 `plugin.json` 1.0.22 的**既有漂移**未动；容器根那份来历不明的未跟踪 `E6-执行清单.html` **未提交**。
3. **本仓仍无测试门禁**：`ci-verify.mjs` **无测试段**（有意，同会话一）；`#153` 尺子**只报不拦**——四道待拍板题未决前不许接任何门禁。
4. ⛔ **没抢后面的活**：`#149`-`#154`（其余三仓补测 / 覆盖尺 / 销账）**一格未碰**。

**下一棒提示（会话三：`#149`/`#150` file-tree ＋ editor ＋ settings）**：serial-monitor 已是最好的抄本——① `#149` **先出逐条裁决表再动手**（同名判据会假红，11 个里可能有一半已被跨文件覆盖）；② 桩与接管两条样板直接抄上面第 3 条点名的两份正典文件；③ `useFakeTimers({now})` 那条坑照抄；④ file-tree **没有 RTL devDep** ⇒ 照 `#149` 详案「非 RTL 不可就登记延后」办；⑤ ⛔ 不改共享 mock、⛔ 不为凑数写无断言测试、⛔ 不 bump 版本。

---

### 2026-09-25 · 会话一（`E6#144`-`E6#146`）· 收口段

**干了什么**

1. **`#144` 地基收进 SDK**：新真源 `packages/plugin-sdk/src/vitest-setup.ts`（照 SDK `lib:["ES2022"]`/`types:[]` 做 **DOM 类型无关化**改写——只把 `window`/`globalThis` 两处收进自声明 `TestGlobal`，**mock 语义零变化**）＋ `exports["./vitest-setup"]`（types ＋ default 双指 `dist/`）＋ `vitest-setup.test.ts` **5 例**（六命名空间在场 / `config === configuration` 同一 store ＋ on→off / `filesystem.watch` 退订可调用 / `path` 归一三态照现状 / **exports↔文件对账**）；壳根 `vitest.setup.ts` 90 行 → **3 行**（同仓相对路径引**源码**，⛔ 不引包名、⛔ 不引 gitignore 的 `dist/`）；壳 `vitest.config.ts` 一字未动。
2. **`#145` 收敛与铺开**：模板 `vitest.setup.ts` → **3 行指针**（`import "@linkdesk/plugin-sdk/vitest-setup";`）＋ 模板 `AGENTS.md` 第 6 节「Tests in this repo」＋ 模板/包两处 README 树行；`check-scaffold.mjs` **断言 12「指针形态」**（缺指针行 ∨ 残留 mock 标志串 ∨ 行数 >5 ⇒ 红）＋ `--self-test` 6→**8** 例；`sync-plugin-ci.mjs` **新增第三方仓排除**（表外仓不进写入集，打印「跳过…不代改」）；`sync-plugin-agents.mjs` 骨架加第 8 节（18 仓渲染）。⛔ 第三方仓 `geme-tihu-bicycle` **零改动**。
3. **`#146` 文档与记忆**：维护者面 `02-插件开发工具链/01-create-linkdesk-plugin脚手架.md` 三处改写（文件树 74/75 两行 ＋ §9.4 **追加**收敛记）＋ 作者面 `13-development-guide.md` **中英同笔**新增「插件仓里的测试怎么写」六条 ＋ 文档地图一行（双语仍 23:23）；顺手销掉两处**过期账**（`插件源码外移层/06-门禁与CI.md §七` 与同层 `交接.md` 的「今天 7 份逐字副本」行 → ✅ 已落地；清单 1,837 行「副本不变式」加收敛指针）；memory 三处（`plugin-repo-gate-model` / `plugin-test-coverage-layer` / `MEMORY.md` 原行改写，索引 48 行）。

**读数（唯一性 / 铺开 / 探针 / 门禁）**

| 项 | 读数 |
|:--|:--|
| 唯一性 grep | 真源 **1**（`packages/plugin-sdk/src/vitest-setup.ts` ＋ 其 5 例单测）· 指针 **7**（壳根 / 模板 / 官方 5 仓）· 第三方 **1 份副本原样** |
| 5 仓 lock | 均 **`@linkdesk/plugin-sdk@0.1.47`**、`resolved` = registry.npmjs.org（⚠️ 教训：`npm install` **不动**已锁的 0.1.46——范围被满足时 npm 不升级，须 `npm update @linkdesk/plugin-sdk --registry=…`） |
| 5 仓 `npm test` | serial-monitor 1 文件/10 例 · file-tree 6/69 · editor 2/28 · settings 6/31 · marketplace 10/233（**25 文件 / 371 例全绿**） |
| 18 仓 `AGENTS.md` | 落盘 18 处（第三方仓跳过），`sync-plugin-agents --check` 绿 |
| 探针工程 | `npx --registry=… create-linkdesk-plugin@0.1.14` 零起点：指针 ✓ / AGENTS 测试节 ✓ / README 指针行 ✓ / lock 落 0.1.47 ✓ / `npm test` 空集 **exit 0** ✓ / **内部任务号 0 处** ✓ |
| 壳门禁 | `npm run check` 全绿 **189 文件 / 2,577 例**；`check-scaffold --self-test` **8/8**；`docs:build` 47 文件 ＋ `docs:check` 逐字节绿；`check:npm-release` 绿 |

**裁决结论**：三笔发版 **SDK 0.1.47 / scaffold 0.1.14 / docs 0.1.31 全部已真发 ＋ 已 `release:mark`**（货架 `latest` 实测到位，`release:mark` 记账：contracts 0.1.20 / plugin-sdk 0.1.47 / create-linkdesk-plugin 0.1.14 / ui 0.2.15 / plugin-docs 0.1.31）；容器提交 = 官方 5 仓**各一笔**（`vitest.setup.ts` ＋ `package-lock.json` ＋ `AGENTS.md`）＋ 13 只声明式仓**各一笔**（`AGENTS.md`），⛔ **全部未推送**。

**遗留与延后（下一棒必须知道）**

1. ⚠️ **4 仓残留一笔非本轮的 `package.json` 改动**（serial-monitor / editor / settings / marketplace 的 devDep 范围 `^0.1.41`／`^0.1.19` → `^0.1.46`，疑上一会话 `npm install <包>@0.1.46` 的副产物）——**已报用户待裁，本轮一行未动**；实测 `npm ci` 的同步判据是「**锁定版本满足范围**」（负控：`^30.0.0` vs lock `29.1.1` ⇒ `Invalid: … does not satisfy …` 当场 exit 1），该组合**不红**（证据在清单 `E6#146` 备注）。
2. **三格都没有碰判据力度**：插件仓 `ci-verify.mjs` **仍无测试段**（有意），尺子（`#153`）**只报不拦**——四道待拍板题未决前不许接任何门禁。
3. ⛔ **没抢 `#154` 的活**：`docs/05-插件更新/00-README.md §三` 销账**未动**。
4. ⛔ **没推送任何仓**（壳 ＋ 容器 18 仓）。

**下一棒提示（会话二）**：基建已就位，**直接进 `#147`/`#148`**（serial-monitor 起点仓：纯逻辑 16 单元 ＋ 替身层 6 必做，做成样板给三/四抄）。⚠️ 该仓 `vitest.setup.ts` 现在是**一行指针**——插件专属桩（如 `window.linkdesk.serial`）一律用 `vi.fn()` 写在**测试文件里**，⛔ 不要改 SDK 那份共享 mock（改了会波及全生态）。

---

### 2026-09-25 · 会话零（写档人 = 用户点名的「只写文档、不执行代码任务」那一棒）· 立案段

**这一棒做了什么（就是给后面五棒铺路）：**

1. 建层：`docs/02-Electron架构/E6_插件生态与发布/插件测试覆盖层/`——`00` 总纲（含 **19 仓读数总表** ＋「虚掩的门」精确诊断 ＋ 版本账 ＋ 不做清单）＋ **五份任务书**（`01`-`05`，共 11 格）＋ `06` 四道待拍板 ＋ 本页。
2. 清单与派单：`E6-执行清单.md` 新增 **第 11 层** 段（`#144`-`#154` 十一格）＋ 总结表 L11 行 ＋ 📌 注 ＋ 执行顺序行；[E6-剩余任务分档-派单序.md](../E6-剩余任务分档-派单序.md) 进一档（零等待）。
3. 与用户四条点名问题**逐条对账**（答案都写进了 `00` / `01` / `06`）：
   - **「SDK 里介绍是不是要改？」** ⇒ 改，但只到「新增一个 subpath ＋ 一句头注」这一层：**头注写明「共享测试地基、插件专属桩不许加进来」**；SDK 的对外介绍面（README / 作者面文档）由 `#146` 在**作者面 13 号**补一节，⛔ 不重写 SDK README。
   - **「版本是不是要升级？」** ⇒ **要，但只在作者轴**：SDK 0.1.47 ＋ scaffold 0.1.14 ＋ docs 0.1.31（三笔 PATCH）；**壳 0.2.15 不动**（零 `src/`/`electron/`）；**5 个插件仓不 bump**（测试不进 `.linkdesk-plugin` 包——已解包实测；例外 = 补测发现真 bug ⇒ `fix:`，🟢 **2026-09-25 用户授权：先汇报再改，由此产生的版本变化被允许**）。
   - **「非视图插件怎么办？」** ⇒ 13 只声明式仓（10 主题 ＋ 2 语言包 ＋ python 空壳）**无可测单元、本层不碰**；它们的门禁就是 `ci-verify.mjs` 的结构与声明判据——**「没测试 ≠ 不合格」**，这句由 `#145` 写进模板 `AGENTS.md` 与 `sync-plugin-agents` 骨架，由 `#146` 写进作者面 13 号。
   - **「作者面文档是不是要改？」** ⇒ 改（`#146`，两棵树的 13 号 ＋ 两处 README 的树行）；**老仓迁移非强制**这句也在这里写明。
4. 「**把 `vitest.setup.ts` 收到 SDK 里——这件排第一**」已兑现为 `#144`（会话一 §一）：真源搬进 `packages/plugin-sdk/src/vitest-setup.ts`（DOM 类型无关化改写，⚠️ 那两处是本层**唯一需要动脑的改写**，其余 **body 逐字不动**）＋ `exports["./vitest-setup"]` ＋ 5 例对账单测 ＋ 壳根收成一行相对引源码。
5. **记忆与 skill 已落盘**（用户第 4 点要求）：memory `plugin-test-coverage-layer.md`（结论 ＋ 口径 ＋ 触发条件）＋ `MEMORY.md` 一行 ＋ `plugin-repo-gate-model` 订正；skill `plugin-test-coverage`（`.claude/skills/` 与 `.agents/skills/` 各一份，⚠️ 两目录**都不入库**，仓内只按名引用）。⇒ `#146`/`#154` 里的记忆动作 = **更新读数**，不是从零新建。

**这一棒没做什么（重要，别误解）**：

- ⛔ **没有执行任何代码任务**（用户明令）：没有动 SDK / 脚手架 / 壳 / 任何插件仓的一行代码，没有发任何版本，没有跑 `npm install`。
- ⛔ 没有 `git commit`（本层文档的提交归**用户的下一棒**或 `#144` 开工时同笔——照仓规：文档改动可与它服务的任务同笔）。
- ⛔ 没有动 `docs/05-插件更新/00-README.md` §三 的销账（那是 `#154` 的活，⚠️ 别抢）。

**下一棒从哪接**：会话一（`#144`）——详案 [01-任务-测试基建收敛SDK.md](01-任务-测试基建收敛SDK.md)；⚠️ 三个小闸（闸① 无需发布 / 闸② 发 SDK 0.1.47 要用户点头 / 闸③ 5 仓铺开不许抢跑）写在它 §二 末尾。

---

## 接力规矩（每棒开场先读）

1. **一棒 = 一个会话**（memory `ai-relay-one-round-per-session`）：本层编排见下「队列整表」；⛔ 不许一个 AI 全做，⛔ 也不许一个 AI 只做一格（用户 2026-09-20 拍板原话）。
2. **上位纪律**（每棒都在 `0N-任务-*.md` 头部复述了一遍）：⛔ 不推送（等用户点头）· ⛔ 发版等用户点头 · ⛔ 不写别人的仓（`geme-tihu-bicycle`）· ⛔ 不为凑数写无断言测试 · ⛔ 每仓单独提交。
3. **收口六项**（缺一条不算收口）：
   - ① 本仓 `npm test` ＋ `npm run verify` 全绿（补测类还要**连跑两遍**防间歇红）；
   - ② 读数回写 `E6-执行清单.md` 对应格 ＋ 本页顶部补一段（本页收口段格式：**干了什么 / 读数 / 裁决结论 / 遗留与延后 / 下一棒提示**）；
   - ③ 动了壳仓 ⇒ `npm run check` 全绿；动了 npm 包 ⇒ 发版（用户点头）＋ `release:mark`；
   - ④ **版本变动了才**跑 `sync:plugin-agents`（AGENTS.md 的版本句会过期，而 `--check` 就挂在壳的 `npm run check` 里）；真发版且是种子仓 ⇒ 还要 `sync:bundled` ＋ lock ＋ freshness；
   - ⑤ 提交：**每个仓单独一笔**、带类别前缀、⛔ 不推送；
   - ⑥ 把「下一棒需要知道的三件事」写在收口段最后（防信息断在交接缝里）。
4. ⛔ **不许跳棒**：会话五（尺子 ＋ 收口）**必须等**会话一~四全部收口（尺子的读数对象就是它们补出来的测试）；会话二/三/四**必须等**会话一的 0.1.47 **上架 npm 货架**（否则它们的 lock 装不到新 subpath ⇒ CI 直接红）。

---

## 队列整表（11 格 / 5 会话）

| 会话 | AI | 格 | 详案 | 开工门槛 | 状态 |
|:--:|:--|:--|:--|:--|:--|
| 一 | AI-A | `#144` 地基收进 SDK | [01 §一](01-任务-测试基建收敛SDK.md) | **零等待**（闸① 部分） | ⏳ |
| 一 | 〃 | `#145` 脚手架 ＋ 5/18 仓铺开 | [01 §二](01-任务-测试基建收敛SDK.md) | 🔴 闸② = **SDK 0.1.47 上架**（用户点头）；闸③ 铺开 | ⏳ |
| 一 | 〃 | `#146` 两棵树 ＋ memory 更新 | [01 §三](01-任务-测试基建收敛SDK.md) | 🔴 闸② = **docs 0.1.31 上架**（用户点头） | ⏳ |
| 二 | AI-B | `#147` serial-monitor 纯逻辑 16 | [02 §一](02-任务-serial-monitor补测.md) | 会话一 `#144` 完成（0.1.47 上架） | ⏳ |
| 二 | 〃 | `#148` serial-monitor 替身层 8 | [02 §二](02-任务-serial-monitor补测.md) | 接 `#147`（同仓，可同会话连做） | ⏳ |
| 三 | AI-C | `#149` file-tree 11（先裁决） | [03 §一](03-任务-其余三仓零测补齐.md) | 同上（可与二/四并行） | ⏳ |
| 三 | 〃 | `#150` editor 6 ＋ settings 3 | [03 §二](03-任务-其余三仓零测补齐.md) | 接 `#149` | ⏳ |
| 四 | AI-D | `#151` marketplace 纯逻辑 15（先裁决） | [04 §一](04-任务-市场仓分层面补齐.md) | 同上（可与二/三并行） | ⏳ |
| 四 | 〃 | `#152` marketplace 替身层 6 ＋ 裁决表 | [04 §二](04-任务-市场仓分层面补齐.md) | 接 `#151` | ⏳ |
| 五 | AI-E | `#153` 尺子 ＋ 19 仓实跑 | [05 §一](05-任务-判据尺与收口.md) | 🔴 **等一~四全收口** | ⏳ |
| 五 | 〃 | `#154` 销账 ＋ 全层收口 | [05 §二](05-任务-判据尺与收口.md) | 接 `#153`；**最后一棒** | ⏳ |

**并行图（一句话）**：一 →（二 ∥ 三 ∥ 四）→ 五。⚠️ 二/三/四 的文件域**互不重叠**（各自一只插件仓），可以同时派；同一时间**别**再派别的活去动这 5 个插件仓（撞期风险)。

---

## 编排与依赖（为什么是这个顺序）

- **一必须最先**：它是地基棒——不收 ⇒ 后面每仓补测试都要在自己那份 `vitest.setup.ts` 上再抄一遍 mock，本层产出立刻变成**新的漂移源**（用户点名这条排第一）。
- **二/三/四可并行**：每会话只动自己的仓；`#147`/`#149`/`#151` 是「纯逻辑 ＋ 先裁决」的主干，`#148`/`#150`/`#152` 是同会话的第二格（替身层/小仓），**同一 AI 连做**——这正是用户要的「一个 AI 多个任务，不太少也不太多」。
- **五必须最后**：尺子的判据要照**既成事实**写（先有正例、后定判据）；销账要引各仓最终读数。
- ⚠️ **别在二/三/四期间发版**：本层的五仓**默认不 bump**；唯一发版情形 = 补测**发现真 bug**（`fix:`＋发版＋目录收录；种子仓还要 `sync:bundled`）——那条链路要走就走全，别半套。
- 🟢 **2026-09-25 用户明示授权（这一条解除含糊）**：补测**发现真 bug ⇒ 先汇报、再改**——典型情形 = **某只插件里藏着一个用户从没撞上的隐藏 bug**（帧缓冲越界、状态合并漏字段这类算法 / 时序缺陷，测试一写就现形）；**因修复产生的版本变化（该仓 PATCH ＋ 发版 ＋ 目录收录；种子仓 ＋ `sync:bundled`）是被允许的**——⛔ 别因为「本层默认不 bump」而压着不改，也别只报不改。汇报要含五项：**哪仓 · 症状 · 判据（哪条测试抓到的）· 打算怎么修 · 涉及哪个版本号**；修复**单独一笔**提交，⛔ 不与测试同笔、⛔ 不推送。

---

## 版本账快照（开工前，⛔ 别凭记忆报数）

| 轴 | 立案时 | 本层目标 |
|:--|:--|:--|
| `@linkdesk/plugin-sdk` | 0.1.46 | **0.1.47**（`#144`，🔴 用户点头） |
| `create-linkdesk-plugin` | 0.1.13 | **0.1.14**（`#145`，🔴 用户点头） |
| `@linkdesk/plugin-docs` | 0.1.30 | **0.1.31**（`#146`，🔴 用户点头） |
| 壳（软件轴） | 0.2.15 | **不动**（零 `src/`/`electron/` 改动） |
| 5 只官方插件仓 | editor 1.0.14 · file-tree 1.0.15 · settings 1.0.18 · marketplace 1.0.43 · serial-monitor 1.0.22 | **不动**（※ 真 bug 例外——🟢 **已授权**：先汇报再改，版本随之动也是允许的） |
| 12 声明式仓 · 第三方仓 | — | **不动 / ⛔ 不代改** |

**每棒必跑的连带面（6 条）**：照 [00-整理档案 §〇d](00-整理档案.md) 那张清单逐条打勾（壳 `check` / `sync-plugin-agents --check` / 发版 ＋ `release:mark` / `docs:build` ＋ `docs:check` / 目录收录与种子 / 提交纪律）。

---

## 待拍板（本层**不挡开工**，收尾时整页带给用户）

见 [06-待拍板方向题](06-待拍板方向题.md)：① 尺子升不升「拦」· ② 覆盖口径定稿（结构判据 vs 百分比）· ③ 第三方仓要不要转达作者补测 · ④ 视图/交互层要不要立后续轮次。⚠️ 会话五收尾时**整页**带过去（别拆成四次问）。
