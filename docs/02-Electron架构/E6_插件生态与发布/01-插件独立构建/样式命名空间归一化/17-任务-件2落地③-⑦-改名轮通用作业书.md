# 任务 `#109h-b③…⑦`：件 2 落地·**改名轮通用作业书**——第 1.14③/④/⑤/⑥/⑦ 轮

> **一句话**：每一格 = **一只仓**——把该仓所有「裸定义类名 / 关键帧名」改成 `<pluginId>-*`，**改完一仓就提交一次、再实机看一眼（查询一次）**，然后 bump ＋ 重发。
> **本档怎么用**：每格的 AI 读**本档 §一（六步配方）＋ §二（实机核对 recipe）＋ §三（自己那一仓的附录）**——**别读别人那仓的附录**（各仓名单由工具生成，本档不手抄）。
> **前置**：[① SDK 判据腿](15-任务-件2落地①-SDK判据腿.md) ✅ ＋ [② 脚手架模板](16-任务-件2落地②-脚手架模板.md) ✅。
> **总纲**：[10-任务-插件前缀不变量落地.md](10-任务-插件前缀不变量落地.md)（执行序与禁区）｜拍板：[14-件2评估交付.md §5.0](14-件2评估交付.md)。
>
> **非新能力声明（`check-design-flow` §8.4 ③）：** 本档是**标识符改名 ＋ 重发的作业书**，规则对象是既有类名/关键帧名，不新增能力。⇒ **无需新能力设计前置**。

---

## 🔴 〇、用户已授权（2026-09-15）——**不要停下来问「能不能发」**

> 用户原话：**「同意发版，无论是软件层面还是 npm 那里，亦或是插件层面……该手动安装新版就安装新版。」**

| 已授权（链内，直接做） | 仍要做的（不是等点头，是守纪律） |
|:--|:--|
| 壳仓 / 插件仓的 `git push`（发版必然要推——plugin `publish` 有 fail-closed 前置「本地 HEAD 不在远端就拒发」） | **推前现测代理**（`curl -x http://127.0.0.1:7890 https://api.github.com`；读数会变，**永远现测**） |
| 插件 bump ＋ **GitHub Release** ＋ **官方目录收录** ＋ **出厂种子刷新** | 照 [作者轴npm发版.md](../../../../06-发布管理/作者轴npm发版.md) 与既有发布流程逐条核对（货架/目录读回逐字节） |
| `npm publish`（`@linkdesk/plugin-sdk` / `create-linkdesk-plugin` 等作者轴） | `git add` 后、`npm publish` **前**先真跑 `npx lefthook run pre-commit`（runbook 坑④） |
| **实机核对时真装新版**（隔离 profile 优先；用户已装实例非必要不动） | 每格收尾**记账**：发了什么版本 / 哪个 Release / 目录条目 / 种子 |
| — | ⛔ **撤版不在此列**（`npm unpublish` / 删 Release **只能用户本人**）｜⛔ **链外发版仍等用户开口** |

**顺序纪律优先于授权**：本链所有改名格（③–⑦）**必须全部完成**，才轮到 [⑧](18-任务-件2落地⑧-发版与文档收口.md) 发 SDK（理由：18 仓 `^0.1.19` caret ＋ 插件仓严格腿 fail-closed ⇒ SDK 一发布，未改名的仓 CI 当场红）。

---

## 一、每仓六步配方（**一步都不能省**）

### 步 1 · 进场取基线（**先证明「改之前是绿的」**）
```bash
cd <仓目录>                     # 例 E:\linkdesk-plugins\official\file-tree
git rev-parse HEAD              # 记下：改名前的提交（交接段要写）
npm run verify                  # 必须 EXIT=0；不为 0 ⇒ 先报告，别在红的地基上动土
node E:/linkdesk/scripts/plugin-css-prefix-audit.mjs . --json > <暂存>.json   # 工具在 ① 交付
```
> 工具产物 = **本仓旧名清单 ＋ 每条「旧名 → 新名」映射 ＋ 文件:行**。🔴 **不许手抄名单**（本仓打过的「手抄第二份清单」就是 1.15 花一整轮修的）。

### 步 2 · 改名（**形状 = 叠加，不是替换**）
- **类名**：在原名前**插入** `<pluginId>-`——`ms-item-name` → **`marketplace-ms-item-name`**；`session-item` → **`serial-monitor-session-item`**。
- **关键帧**：`fadeIn` → `serial-monitor-fadeIn`；**并同笔改 `animation:` 引用处**（漏了引用 = 动画静默消失）。
- **渲染点同笔改**：`className="…"` / `` className={`…`} `` / `querySelector` / `classList` / 测试断言里的类名——**一处不漏**（这就是本路线的主风险）。
- 🔴 **补扫（1.14④ 加的必做步）：工具的射程 = CSS 定义点，看不见「只在渲染点存在、CSS 零定义」的类名。** 实证：settings 的 `keybindings-col-key-cell` / `text-muted` 在 TSX 里各一处、**全仓 0 条 CSS 规则** ⇒ 工具从来不报；是补扫抖出来的。**每仓开工与收工各跑一遍**：
  ```bash
  node E:/linkdesk/scratch/scan-render-tokens-109h-b4.mjs "<仓目录>"   # 改仓路径即用（1.14④ 交付）
  ```
  **判据** = TSX 里每个 className token 只能三选一：① `<pluginId>-*` ② 宿主保留（`ldk-*` / `codicon*` / 登记表 `classes.host` 里的 `input`）③ **复合状态类**（CSS 里以 `.你的类.x` 形态出现，如 `.settings-keybindings-inline-btn.confirm`）。**不在这三类里的 = 漏网**，按「叠加」补前缀（这类通常零 CSS 规则 ⇒ 改名零视觉影响）。
- 🔴 **不许改**：颜色 / 间距 / 字号 / 圆角 / 任何视觉属性（硬约束 16）；**不许改词干**；**不许动 `className="input"`**（宿主保留名的 scoped 消费，属 [1.17](13-任务-宿主input裁决与落地.md)）。
- 🔴 **不许改**：`ldk-` 开头的名字（宿主命名空间——那是共享组件/宿主自己的，插件本来就不该有）。

### 步 3 · 机械证明（**两条，缺一不可**）
1. **token 判据 0 残留**：旧名在 **CSS ＋ 渲染点**里**整词**匹配命中 **0**（`marketplace-badge` ≠ `badge`），且**正控 ≥1**（拿一个确定在用的新名验证尺子有效）。脚本照 1.16 的 `scratch/judge-class-tokens.mjs`（带 `--control`）。
2. **形状证明**（走「叠加」时适用）：每个改动文件**逐字节等于「HEAD ＋ 逐行只插入 `<pluginId>-`」**——剥注释后仍相同 ⇒ **零视觉声明变化**。照 1.16 的 `scratch/prove-prefix-only-109j-a.mjs`。
   > 🔴 **证明脚本自己要有负控**：拿它去跑**改名前**的文件 ⇒ 必须**红**（否则证明是恒绿的，等于没有）。

### 步 4 · 该仓全绿
```bash
npm run verify     # 五段全过（含 ① lint 严格腿 = 新判据已生效）
npm run test       # 有测试的仓必跑（改名常撞断言，见 1.11 的 233/10/31 例）
npm run build      # 产物要能出
```

### 步 5 · **git 一次** ＋ 记账
```bash
cd <仓目录>
git add -A && git commit -m "refactor: <id> CSS 类名/关键帧带 pluginId 前缀（E6#109h-b 件 2）"
```
- bump：`package.json` ＋ `plugin.json` ＋ `CHANGELOG.md`（新段非空）**三处同笔**。
- 🔴 **一仓一提交**（本档的硬要求）——**不要把两只仓塞进一笔**：出错时要能单仓 revert。

### 步 6 · **实机界面查询一次**（见 §二）＋ 然后重发
- **先看界面、再发**：改名是「不报错、只是长得不对」的 bug 类，**眼睛是唯一体检手段**；发出去的版本必须是**看过的**那个。
- 重发顺序（每仓）：`publish`（GitHub Release）→ 官方目录收录 → 出厂种子（仅种子插件）→ `check:bundled-freshness` 6/6。
- 🔴 用**新发布的产物**再核一次货架/目录读数（不是只看本地 build）——用户授权里那句「**该手动安装新版就安装新版**」就是指这个：**允许真装、真跑**，别停在「tarball 里 grep 到新名字」。

---

## 二、实机核对 recipe（**把 1.12 / 1.16 踩过的坑一次性固化**）

**起法**（照 1.16）：
```bash
# 插件目录要进 vite 的白名单（临时配置放 scratch/，不进仓）
# 隔离 userData 必须放 %TEMP%（放仓内会让 vite 的 watcher EBUSY 崩掉）
npx vite &
electron . --remote-debugging-port=9223 \
  --user-data-dir=%TEMP%\ldk-109h-<仓名> \
  --disable-features=CalculateNativeWinOcclusion --disable-backgrounding-occluded-windows
```
- ⚠️ **`server.fs.allow` 只放行 `[仓库根, %APPDATA%\linkdesk\plugins]`** ⇒ 隔离 userData 下的插件 bundle 经 `/@fs/...` 取不到（表现为「视图落壳保底」）⇒ 临时 vite 配置加白名单（1.12 的 `scratch/vite.live.config.ts` 先例）。
- ⚠️ **重定向 `APPDATA` 环境变量对 Chromium 无效**（走 Known Folder API）⇒ 会撞用户已装实例的单实例锁，**秒退且 EXIT=0**、只留一行 `DevTools listening`。
- ⚠️ 装该仓的新构建到隔离 profile：**dev 下「市场 → 确认更新」会 `EPERM`**（vite 持着文件句柄）⇒ 手动把 `<id>.linkdesk-plugin` 解到 `%TEMP%\ldk-109h-<仓名>\plugins\<id>\` 再重启。
- ⚠️ 两个开关（`CalculateNativeWinOcclusion` / `backgrounding-occluded-windows`）**是必需的**：窗口被遮挡时 Chromium 不重绘 ⇒ 截出来是一片纯色。
- 🔴 **先搞清「这个视图怎么打开」，再动手点**（1.14④ 绕了半天的坑）：
  - **`location: auxiliarybar` 的容器在壳里恒不渲染**——`src/hooks/usePoolSync.ts:258` 把 `rightSidebar.visible` **写死 `false`**（注释：「壳侧暂无容器内容生产者」）。settings 的容器就是 `auxiliarybar`，它的视图实际落在**浮动面板**上：在池里执行 `window.linkdesk.commands.execute('core.openSettings')`（命令实现见 `core/commands/shell/settingsCommands.ts`：先 `focusTabByPluginId`、否则 `panel:reveal-floating`）。
  - 🔴 **图标栏那个同名按钮可能弹的是壳的右键菜单（`.ctx-menu`），不是插件视图**——面板长得像就能骗过眼睛。**验证法**：查 `document.querySelectorAll('*')` 里的类名有没有该插件的 `<pluginId>-*`，或查 `[...document.styleSheets]` 里有没有该插件的 CSS；**没有 = 那不是插件视图**（settings 第一遍就是这么被抓出来的）。
  - ⚠️ **隔离 profile 的 `plugins/` 目录可能整个消失**（而 `installed-plugins.json` 还在）⇒ 再启动走「异版本不动」分支、不重装、插件全灰。修法 = **整块重来**（删 profile → 起一次让它种 6 只 → 停 → 换成新件 → 再起），**别去手改 `installed-plugins.json`**。

**查询什么**（CDP `Runtime.evaluate`，照 memory `cdp-ui-automation`）：
| # | 查询 | 期望 |
|:--:|:--|:--|
| 1 | `document.querySelectorAll('[class*="<旧前缀>"]').length` | **0**（旧名一个都不剩——注意用**整词**判据，`file-tree-badge` 不等于 `badge`） |
| 2 | `document.querySelectorAll('[class*="<新前缀>"]').length` | **> 0**（新名真的渲染出来了） |
| 3 | 抽 1–2 个元素读 `getComputedStyle`（尺寸 / 颜色 token / 字号 / 内边距） | **与改名前一致**（这是「零视觉变化」的正面证据） |
| 4 | 控制台（`Log.enable` / console 消息） | 无新增报错 |
| 5 | 该仓**可见面**逐屏看（见 §三 附录） | 「**看到了**」——不是「跑通了」 |

**取证写进交接段**：面（哪个视图）、读数（选择器命中数）、抽到的 computed style 值、以及**任何看不出结论的面如实写**（1.12 的面 6「本机插件集无渲染点」就是如实记的先例）。

---

## 三、每仓附录（**自己只看自己那一节**）

> 下表是 **2026-09-15 的实测快照**；每格开工**先跑工具**（步 1），**以工具现读数为准**。

| 仓 | 不合规类名 | 选择器处数 | CSS 文件 | 关键帧 | 发布义务 | 可见面 |
|:--|--:|--:|:--|:--|:--|:--|
| `file-tree` | **28** | 36 | `SearchView.css` / `file-tree-node.css` / `file-tree-shell.css`（`src/styles/`） | — | **出厂种子** 1.0.8 → PATCH | 侧栏文件树（展开/选中/拖放/最近）＋ 搜索视图（输入行/选项/匹配） |
| `settings` | **28** | 49 | `views/SettingsView.css` / `-rows.css` / `-objectEditor.css` / `keybinding-settings/KeybindingSettingsView.css` | — | **出厂种子** 1.0.10 → PATCH | 设置导航 ＋ 各行类型（开关/滑杆/色板/对象编辑器）＋ 键位表 |
| `serial-monitor` | **59** | 128 | `ControlPanel.css` / `SerialMonitorSidebar.css` / `SerialMonitorView.css` / `-receive.css` / `-send.css` / `StatusBar.css` | **`fadeIn` / `slideDown`**（同在 `SerialMonitorView-receive.css:90/94` 与 `:156/160`） | **官方目录** 1.0.11 → PATCH | 视图（控制栏/接收区/发送栏/侧栏/状态栏）——⚠️ 本机无串口 ⇒ 部分面是「无匹配项/占位」 |
| `panel-demo`（**壳仓夹具**） | **19** | 40 | `plugins/panel-demo/src/styles/DemoViews.css` | — | ❌ **不发布**（随壳仓走） | 面板「输出」「待办」＋ 面板侧栏 |
| `marketplace` | **139** | 220 | `styles/MarketplaceView.css` / `MarketplaceConfirm.css` ＋ `styles/detail/**`（6）＋ `styles/sidebar/**`（3） | **`ms-icon-spin`**（`sidebar/sidebar-explore.css:42` 引用 / `:103` 定义） | **出厂种子** 1.0.32 → PATCH | 市场侧栏列表（探索/已装/更新）＋ 详情页（头部/正文/更新日志/特性）＋ 确认弹窗 |

> 🔴 **1.14④ 实测补正（读本表的两条口径）**：① 「不合规类名」列 = **工具口径**（CSS 裸定义站点去重后的名数），历轮都对得上；② 「选择器处数」列**只作量级参考，不是改名面**——settings 表里写 49，**实际改名面 = 6 文件 / 83 处**，多出的 2 处是「只在渲染点存在、CSS 零定义」的类名（`keybindings-col-key-cell` / `text-muted`），**工具的射程外** ⇒ 见 §一 步 2 的**补扫**那一条（每仓必跑）。

**⑤ 与 ⑦ 的额外注意**：
- **`serial-monitor`**：`fadeIn` / `slideDown` 是**关键帧**（拍板 Q3 纳入）⇒ 除改名外，**改 `animation:` 引用**；改完在**接收区**看动画是否还在。
- **`marketplace`**：🔴 **139 名 / 220 处 / 11 文件 = 本系列最大一笔** ⇒ **强烈建议再拆 2 格**：⑦a `detail/**`（`detail-header` 24 / `detail-body` 22 / `detail-features` 15 / `detail-changelog` 11 / `detail-actions` 7 / `detail-shell` 2 = **81 名**）、⑦b `sidebar/**` ＋ `MarketplaceConfirm.css` ＋ `MarketplaceView.css`（**58 名**，其中 `MarketplaceView.css` 的 6 个裸名**已合规**）。**拆格后各自仍是「一提交一次」**（同仓两笔提交，边界按目录）。
- **`panel-demo`**：改完跑**壳仓** `npm run check`（它在 `plugins/` 下，走壳仓门禁）；不 bump、不发布。
- **`file-tree` / `settings` / `marketplace`** 是出厂种子 ⇒ 改完要 `npm run sync:bundled -- --latest`（⚠️ asset 下载**必须带 `HTTPS_PROXY` ＋ `NODE_USE_ENV_PROXY=1`**，不带就 `ECONNRESET`）＋ `check:bundled-freshness` **6/6**。

### 🔴 ③ 那一格额外捎带一件事：把 `file-tree` 的 `@linkdesk/ui` 从 `^0.1.4` 抬到 `^0.2.0`

**这是 1.13 评估轮外新捡到的一笔账（2026-09-15 实测，无人认领过）**：

| 事实 | 读数 |
|:--|:--|
| 18 仓对 ui 的依赖 | **只有 `file-tree` 仍是 `^0.1.4`**；`marketplace` / `serial-monitor` / `settings` 已 `^0.2.0`（1.11 那轮升的）；其余 14 仓不依赖 ui |
| `file-tree.linkdesk-plugin`（1.0.8）的 `index.bundle.css` | **旧 8 个裸名 `badge`/`button`/`combobox`/`mdv`/`selectbox`/`sle`/`slider`/`toggle` 全在** · `ldk-` 名 **0** |
| 对照 `settings.linkdesk-plugin`（1.0.10） | 旧裸名 **0** · `ldk-` 名 **8 个全在** |

**为什么是它**：1.11 只给「有 scoped 调优的 3 只仓」升了 ui（file-tree 没有调优 ⇒ 当时判断不需要动）。但**没调优 ≠ 不该升级**——它 bundle 里那份 ui 0.1.7 的 CSS **仍在池样式表里占用那 8 个全局裸名**，而 file-tree 是**出厂种子**（新装用户必带）。

**为什么现在做最省**：③ 本来就要**重建并重发** file-tree（改名），顺手把依赖抬到 `^0.2.0` 再 build ⇒ **零额外发布周期、零额外实机面**。改完的正面样板就是 settings 的产物（旧名 0 / `ldk-` 8）。

**判据**：重打包后解 `file-tree.linkdesk-plugin` 的 CSS ⇒ **旧 8 裸名 0 ＋ `ldk-` 名齐全**（照上表口径跑一次即可）；`npm run verify` / `test` 绿；⚠️ file-tree **没有** scoped 调优要改（1.11 已核实），所以这一步是**纯依赖提升 + 重建**，不改任何选择器。


---

## 四、判据（**每格逐条给读数**；这是可勾清单，收尾要勾全套）

> ✅ **③④⑤⑥ 已按本清单逐格勾完**（`#109h-b③`/`④`/`⑤`/`⑥`）——逐条读数见各条引号内。**只剩 ⑦a/⑦b** 照抄本清单另起一份（本档不代下一代划格）。

- [x] 步 1 进场 `npm run verify` **EXIT=0**（并记下 `git rev-parse HEAD`）—— ③ ✅ 五段全过；`HEAD = 3b5aef1898cc0dd17ff5dea481082850fa863251`
- [x] 工具读数：本仓不合规 **N 名**（与 §三 快照对账；不一致 ⇒ 写清为什么）—— ③ ✅ **28 名 / 35 处 / 1 文件**。⚠️ **与 §三 快照的「36 处」不一致是「两把尺子」**：§三 那列是**改名 diff 量级**（含 scoped 消费），工具只数**裸定义站点**（1.14① 已澄清过的口径）；**名数 28 与快照一致**
- [x] 🔴 **token 判据 0 残留**（CSS ＋ 渲染点，整词匹配）＋ **正控 ≥1 命中** —— ③ ✅ 全仓整词 **0**；正控 = 新名 **CSS 37 / TSX 38**；**NC-d** 拿 HEAD 净树跑同一条判据 ⇒ 残留 >0（证明尺子不恒绿）
- [x] 🔴 **形状证明**：逐字节 = 「HEAD ＋ 只插入前缀」（＋ 该证明脚本的**负控**：对改名前文件跑 ⇒ 红）—— ③ ✅ **7/7 文件 / 75 站点**，剥 token 后逐字节相同 ＋ 逐站点映射核对；负控 3 条全红（NC-a 拿 HEAD 冒充 new · NC-b 替换词干 · NC-c 改视觉属性 1 处）。🔴 **口径（③ 踩出来的）**：要比 **`HEAD:路径` vs `:路径` 的 blob**——本机工作区是**既存 CRLF** 而提交字节是 LF，比工作区字节会**假红**
- [x] 关键帧（仅 `serial-monitor` / `marketplace`）：名字 ＋ **`animation:` 引用**双改，旧名 grep **0** —— ③ ✅ **本仓 N/A**（`@keyframes` / `animation:` 在 `src/` 零命中；工具读数关键帧 **0**）
- [x] 该仓 `npm run verify` ＋ `npm run test` ＋ `npm run build` 全绿 —— ③ ✅ `verify` EXIT=0（五段）· `test` **69/69** · `build` 出 335.6 KB
- [x] bump 三处同笔（`package.json` / `plugin.json` / `CHANGELOG.md`）—— ③ ✅ 三处 **1.0.8 → 1.0.9** ＋ CHANGELOG 新段非空。⚠️ **还有第四处**：`AGENTS.md` 的「当前版本」行会被自动重写，而 `publish` 有「工作区必须干净」前置 ⇒ 得**补一笔**（`6641819`；先例 `ca1144b`）
- [x] **git 一次**（一仓一提交）—— ③ ✅ 插件仓 `f9e0ded`（11 文件 / 88 增 81 删，含 `^0.1.4→^0.2.0`）＋ 上面那笔 AGENTS.md 文档提交；壳仓另记 `5d511a451`（种子刷新）
- [x] 🔴 **实机界面查询一次**：旧名命中 **0** / 新名命中 **>0** / computed style 与改前一致 / 无新报错 / 可见面逐屏看过（读数写进交接段）—— ③ ✅ 6 面（隔离 profile ＋ 9224）：DOM 旧裸名 **0** / 新名 **26 类**；文件树展开 20 节点 ＋ 选中态；输入行 / 过滤行 / 工具栏 / 替换行 / 匹配行 **computed style 逐条等于 CSS 声明**；**真搜索 13 文件 / 172 匹配**；**零 error/warning**；截图 5 张
- [x] 重发完成并读回核对（Release / 目录 / 种子；`check:bundled-freshness` 6/6 若适用）—— ③ ✅ Release **v1.0.9** · 官方目录 `f680184`（只动一行、读回 sha256 逐字节相同）· 种子刷新幂等读数「↑ 1.0.8 → 1.0.9」· **`check:bundled-freshness` 6/6**；箱内种子解包复核 **旧 8 裸名 0 / `ldk-` 112 / 旧裸 `search-*` 双 0**
- [x] `npm run check`（壳仓，若本格动了壳仓文件如 `panel-demo`）**EXIT=0** —— ③ ✅ **EXIT=0（156 文件 / 2,115 例 / 25.05s）**（本格动了壳仓的种子与账）

### ④ `settings`（2026-09-15，`#109h-b④`）——照抄上一份、填本格读数

> 基线 `eff4c9d` · 提交 插件仓 `660ce1a` ＋ publish API 提交 `da26815` · 壳仓 `ad9d4904a` · **v1.0.11 已发**

- [x] 步 1 进场 `npm run verify` **EXIT=0**（并记下 `git rev-parse HEAD`）—— ④ ✅ 五段全过（含两条**不拦的附加腿**黄灯：spacing-grid 5 处 / 字典 44 key，与 ③ 收尾读数同形态）；`HEAD = eff4c9d75bba93d89691ff135e74446498474d81`
- [x] 工具读数：本仓不合规 **N 名**（与 §三 快照对账；不一致 ⇒ 写清为什么）—— ④ ✅ **28 名 / 37 裸定义站点 / 2 文件**。⚠️ **与 §三 快照「49 处 / 4 文件」不一致又是「两把尺子」**：那列是**改名 diff 量级**（含 scoped 消费），工具只数**裸定义站点**；另两个 CSS 文件（`SettingsView.css` / `SettingsView-rows.css`）的类名**本来就带 `settings-` 前缀**（工具不报）；**名数 28 两把尺子一致**
- [x] 🔴 **token 判据 0 残留**（CSS ＋ 渲染点，整词匹配）＋ **正控 ≥1 命中** —— ④ ✅ **源码面 `src/**` 整词 0**；正控 = 新名 **88 处**；**NC-d / NC-e** 拿基线净树跑同一条判据 ⇒ 残留 >0（尺子不恒绿）。⚠️ **口径已改：双口径报数**——`src/` 之外命中 `CHANGELOG.md` **6 处**（旧→新对照的迁移说明，**具名声明**为合法例外）。原因：③ 写的「全仓 0（含注释与文档）」**只在 `src/` 口径下成立**（实测 file-tree 自己的 CHANGELOG 就点名了 3 个旧名、4 处）
- [x] 🔴 **形状证明**：逐字节 = 「基线 ＋ 只插入前缀」（＋ 该证明脚本的**负控**：对改名前文件跑 ⇒ 红）—— ④ ✅ **6/6 文件 / 83 站点**；剥 token 后与 **`eff4c9d` 的 blob 逐字节相同**。🔴 两条口径修正：**(i)** `--base <改名前 sha>` 必须显式给（本轮中途 amend 过 ⇒ 默认的 `HEAD` 会让射程**悄悄缩水**）；**(ii)** 负控要**自证真的变异了**——第一版 NC-a 命题恒真、NC-c 的替换字符串在文件里不存在，**两条假绿**，修好才作数
- [x] 🔴 **工具射程外的渲染点补扫**（**本轮新增的必做项**，此前的清单没有这条）—— ④ ✅ 全仓 TSX **74 个 className token**：带前缀 61→**63** · 宿主保留 8（`codicon*`）· 其余 5 ⇒ **修掉 2**（`keybindings-col-key-cell` / `text-muted`，**CSS 零定义 ⇒ 工具一辈子看不见**）后只剩 3 个合法项（`cancel`/`confirm` 复合状态类 · `input` 登记表 classes.host）
- [x] 关键帧（仅 `serial-monitor` / `marketplace`）：名字 ＋ **`animation:` 引用**双改，旧名 grep **0** —— ④ ✅ **本仓 N/A**（工具读数关键帧 **0**）
- [x] 该仓 `npm run verify` ＋ `npm run test` ＋ `npm run build` 全绿 —— ④ ✅ `verify` EXIT=0（五段）· `test` **31/31** · `build` 出 **323.2 KB**
- [x] bump 三处同笔（`package.json` / `plugin.json` / `CHANGELOG.md`）—— ④ ✅ 三处 **1.0.10 → 1.0.11** ＋ CHANGELOG 新段非空；**第四处 `AGENTS.md`**（publish 的「工作区必须干净」前置）**同笔一起改**（比 ③ 少一笔提交）
- [x] **git 一次**（一仓一提交）—— ④ ✅ 插件仓 `660ce1a`（10 文件 / 93 增 84 删）＋ publish 的 API 提交 `da26815`；壳仓另记 `ad9d4904a`（种子 ＋ 账）
- [x] 🔴 **实机界面查询一次**：旧名命中 **0** / 新名命中 **>0** / computed style 与改前一致 / 无新报错 / 可见面逐屏看过（读数写进交接段）—— ④ ✅ **5 面**（隔离 profile ＋ 9225）：DOM 旧裸名 **0** / 新名类 **30**；通用（行 `padding 8px 0px` / `gap 20px`、搜索栏 `8px 16px`、导航项 `6px 20px` = CSS 声明）；主题（**主题卡 ×2 ＋ `.tbadge` ×2**，「Dark」23×15 /「2 配色」31×16 = **强调色文字 ＋ 透明底**，v1.0.9 那个「纯色块」症状不复现；`ldk-selectbox` ×2）；快捷键表（**87 行** / 14 个新类 / 行 `padding 6px 16px` / **搜索「ctrl」过滤 87→18** / **双击行进 inline 编辑** → 1 编辑框 ＋ 2 按钮 ＋ 2 键位格，`border-radius 3px` 对账通过）；对象编辑器（`files.exclude`：2 glob ＋ 冒号 ＋ ✓/🗑 ＋ 「添加模式」，245×85 / `gap 4px` / 8 个新类 / **旧裸名 0**）；控制台**零 settings 相关报错**（唯一 error 是既存噪声：`plugins/core/src/index.tsx` 在壳仓里根本不存在）；截图 6 张
- [x] 重发完成并读回核对（Release / 目录 / 种子；`check:bundled-freshness` 6/6 若适用）—— ④ ✅ Release **v1.0.11** · **从 Release 真下载复核**（不是只看本地 build）：`curl -L` = 200 / **330,948 字节**、**sha256 与本地 build 逐字节相同**（`7b118446a2a6…`）、目录登记 `size` 同值；官方目录 `4ccb41f`（21→21、**只动 settings 一行**、顺序与其余 20 条逐字节未动、**远端 raw 读回 sha256 与本地提交逐字节相同**）· 种子刷新读数「↑ settings：1.0.10 → 1.0.11」＋ 其余 17 条「已是最新，指纹已核对」· **`check:bundled-freshness` 6/6**；箱内种子解包复核 **旧裸名 0 / `settings-*` 162 / `ldk-` 41**，且 **sha256 与 Release 件逐字节相同**
- [x] `npm run check`（壳仓，若本格动了壳仓文件如 `panel-demo`）**EXIT=0** —— ④ ✅ **EXIT=0（156 文件 / 2,115 例 / 33.32s）**（本格动了壳仓的种子与账）

### ⑤ `serial-monitor`（2026-09-15，`#109h-b⑤`）——照抄上一份、填本格读数

> 基线 `f6918e4b` · 提交 插件仓 `f8939b0` ＋ publish API 提交 `a53893e` · 壳仓账 `bc2456d5d` · 官方目录 `1046b00` · **v1.0.12 已发**
> 🔴 **本格两处刻意保留原名**（都不是本插件自有元素的类名 ⇒ 照抄工具的映射表会出事）：
> **① `.cm-panels`**＝CodeMirror 自己的类（CM6 原生搜索面板，DOM 由 `@codemirror/view` 生成并插进 `view.dom`）⇒ **加前缀 = 死规则 = 面板静默掉样式** ⇒ 改 **scoped**：`.serial-monitor-cm-container .cm-panels`（8 个选择器全数）。这正是腿自己写的「scoped 调优 = 合法消费」形态。
> **② `setting-group`**＝宿主 `src/index.css:228` 的**全局工具类**，本插件渲染它（与 `className="input"` 同一性质的消费）又在自有 CSS 里裸定义了一处调优（`padding: 4px 12px` 覆盖宿主的 `12px`）⇒ **字面加前缀 = 元素丢掉宿主的背景/圆角/外边距 = 真实视觉回归** ⇒ 改法＝**TSX 保留宿主类 ＋ 另加自有类** `serial-monitor-setting-group` 承载调优（4 个组 div）。

- [x] 步 1 进场 `npm run verify` **EXIT=0**（并记下 `git rev-parse HEAD`）—— ⑤ ✅ 五段全过；`HEAD = f6918e4b165d779002b6bc2c5cb2c90664343104`
- [x] 工具读数：本仓不合规 **N 名**（与 §三 快照对账；不一致 ⇒ 写清为什么）—— ⑤ ✅ **59 名（去重后）/ 84 裸定义站点 / 6 文件 ＋ 关键帧 2**。⚠️ **与 §三 快照「128 处」不一致又是「两把尺子」**（那列是改名 diff 量级、含 scoped 消费）——**名数 59 与快照一致**，关键帧 2 也一致
- [x] 🔴 **token 判据 0 残留**（CSS ＋ 渲染点，整词匹配）＋ **正控 ≥1 命中** —— ⑤ ✅ **源码面 `src/**` 整词 0**（64 个名字全扫，含 2 关键帧）；正控 = 新名 **238 处**；**NC-d / NC-e** 拿基线跑同一条判据 ⇒ 不恒绿。⚠️ **口径**：`src/` 之外命中 `CHANGELOG.md` / `marketplace.json` 各 **1 处** = **具名例外**（1.0.10 那轮的实录「四处 scoped 调优」里点名了 `.control-bar .selectbox-trigger`，**改写它 = 把历史说成今天的名字**；`marketplace.json` 是它的生成镜像）
- [x] 🔴 **形状证明**：逐字节 = 「基线 ＋ 只插入前缀」（＋ 该证明脚本的**负控**：对改名前文件跑 ⇒ 红）—— ⑤ ✅ **24/24 文件 / 238 站点**；剥掉插入后与 **`f6918e4b` 的 blob 逐字节相同** ＋ 逐文件「实得插入数 == 基线同名出现数」相等校验。🔴 **两处偏离按「声明式插入」并入同一套证明**（`deInsert` 撤两类插入后再比字节）：`.cm-panels` 的 `.serial-monitor-cm-container ` 8 处 ＋ `className="setting-group serial-monitor-setting-group"` 4 处 ⇒ **判据 B′** 单列读数 8 ＋ 4 ✓。⚠️ **计数口径踩了两次**（第一版 receive.css 期望 21/实得 29、第二版又反过来）——scoped 插进去的 `.serial-monitor-cm-container` **本身就是名字 `cm-container` 的一次前缀插入**（按名字计数看得见），而 TSX 那个自有类**不在名字表里**（按名字计数看不见）⇒ **两侧的加减必须分开写**
- [x] 🔴 **工具射程外的渲染点补扫**（**本轮新增的必做项**）—— ⑤ ✅ 全仓 TSX 抖出 **74 个 token**：带本仓前缀 6→**61** · 宿主保留 11（`codicon*`）· 其余 **5 → 修掉 5**（4 个 CSS 零定义 ＋ **1 个只在 scoped 里定义**的 `control-select-narrow`）⇒ 收工后**只剩 2 个**：`input`（登记表 `classes.host`，属 1.17）与 `setting-group`（**宿主工具类消费点，未登记 ⇒ 本轮新账**，见「留给后面」）
- [x] 关键帧（仅 `serial-monitor` / `marketplace`）：名字 ＋ **`animation:` 引用**双改，旧名 grep **0** —— ⑤ ✅ **本仓必做**：`fadeIn` / `slideDown` → `serial-monitor-fadeIn` / `serial-monitor-slideDown`；CSS 内旧名整词 **0**、新名 **4 处（2 定义 ＋ 2 处 `animation:` 引用）**；**实机 CSSOM 双向取证**：`defs = [serial-monitor-slideDown, serial-monitor-fadeIn]`、`refs = [同名两个]`，暂停横幅的 `animation-name` **计算值 = `serial-monitor-slideDown`**（`animation-duration 0.2s` 对账 PASS）。⚠️ `fadeIn` 的消费方 `.serial-monitor-back-to-bottom`：**本机无串口数据 ⇒ 接收区不滚动、按钮不出现**（如实记，不编）——它的引用由 CSSOM 的 `refs` 一并取证
- [x] 该仓 `npm run verify` ＋ `npm run test` ＋ `npm run build` 全绿 —— ⑤ ✅ `verify` EXIT=0（五段）· `test` **10/10** · `build` 出 **548.3 KB（4/4 表面）**
- [x] bump 三处同笔（`package.json` / `plugin.json` / `CHANGELOG.md`）—— ⑤ ✅ 三处 **1.0.11 → 1.0.12** ＋ CHANGELOG 新段非空（**把两处刻意保留原名逐条写进用户可读的变更说明**）；**第四处 `AGENTS.md`** 同笔（publish 的「工作区必须干净」前置）
- [x] **git 一次**（一仓一提交）—— ⑤ ✅ 插件仓 `f8939b0`（**28 文件** / 241 增 232 删）＋ publish 的 API 提交 `a53893e`；壳仓另记 `bc2456d5d`（出厂种子账——该仓 `seed:false`，读数「**纯市场，仅账**」）
- [x] 🔴 **实机界面查询一次**：旧名命中 **0** / 新名命中 **>0** / computed style 与改前一致 / 无新报错 / 可见面逐屏看过（读数写进交接段）—— ⑤ ✅ **5 面**（隔离 profile ＋ **CDP 9226**，真装 1.0.12）：会话列表（行 **264×48.2** / `padding 6px 8px 6px 6px` PASS / 计数 / 动作按钮 2）· 收发设置四组（**host 4 / own 4 / both 4** ＝ 宿主类与自有类同元素并存；宿主 **bg `rgb(45,45,45)` / radius `6px` / mb `8px`** 仍在；自有类 `padding 4px 12px` PASS；第 2 组 `border-top 1px` 在）· 主区 **34 个新类**（`control-bar padding 0px 8px`、`toolbar-btn` / `send-btn` `border-radius 4px`、`send-editor-wrapper`、`receive-stats` 字号、`cm-hex-pane display:none`、`quick-send-bar padding 6px 10px` 全 PASS；**scoped-only 类**规则实读 `.serial-monitor-control-bar .serial-monitor-control-select-narrow`、`min-width 64px` 生效）· **CM6 面板**（`.cm-editor` 的 `parentElement` 实读 `serial-monitor-cm-container`；scoped 下 `input`/`button` `border-radius 4px`、`label font-size 13px`、`accent-color rgb(0,120,212)` 全 PASS；`serial-monitor-cm-panels` 命中 **0**）；控制台**零 serial-monitor 相关报错**（唯一 error 是既存噪声 `动态加载插件 "workbench"`——`plugins/workbench` 在壳仓里根本不存在，与 ④ 那轮的 `"core"` 同种）。截图 6 张（`scratch/shot-109hb5-*.png`）
- [x] 重发完成并读回核对（Release / 目录 / 种子；`check:bundled-freshness` 6/6 若适用）—— ⑤ ✅ Release **v1.0.12** · **从 Release 真下载复核**（不是只看本地 build）：`curl -L` = **200 / 561,449 字节**、**sha256 与本地 build 逐字节相同**（`bd24083f166ff128…`）· 官方目录 `1046b00`（21→21、**只动 serial-monitor 一行**、**API 读回 sha256 与候选逐字节相同**）· `sync:bundled --latest` 读数「**↑ serial-monitor：1.0.11 → 1.0.12（纯市场，仅账）**」＋ 其余 17 条「已是最新，指纹已核对」· **`check:bundled-freshness` 6/6**（该仓 `seed:false` ⇒ **不随包**，发布义务 = 官方目录）
- [x] `npm run check`（壳仓，若本格动了壳仓文件如 `panel-demo`）**EXIT=0** —— ⑤ ✅ **EXIT=0**（本格动了壳仓的出厂种子账 + 本系列文档）

> 🔴 **⑤ 的两条口径教训（写给 ⑥⑦）**：**(i)** 工具给的「旧名 → 新名」映射表**不能照抄**——凡**渲染点里没有、只有 CSS 里出现**的名字，先问一句「这个 DOM 是谁生成的」：第三方库生成的（`cm-panels` 是活标本）**加前缀即死规则**；**(ii)** 一个名字**同时**是「宿主/共享组件的」和「我在调的」时（`setting-group`），正确形状是**两边各一**：宿主类继续渲染（消费）＋ 自有类承载调优，**不是二选一**。普查结论：**全容器只有 serial-monitor 有裸 `cm-`/`monaco-` 定义（17 处）** ⇒ ⑥⑦ 大概率不会再撞上第 ①类；第 ②类（宿主带连字符的全局工具类）**已知只有 `.setting-group` 被消费**。

### ⑥ `panel-demo`（壳仓夹具）（2026-09-16，`#109h-b⑥`）——照抄上一份、填本格读数

> 基线 `a78bc30cfccde6cdec9d572690210c092b20805d` · 提交壳仓 `1e9bad5d3` · **不 bump 不发布不推送** · 面 **4 文件 / 70 站点**
> 🔴 **本格不是插件仓**：`panel-demo` 住在**壳仓** `plugins/` 下 ⇒ ① 无 `verify`/`test` 脚本（`package.json` 只有 `build` / `lint`，都走 `packages/plugin-sdk/dist`）⇒ 步 1/步 4 由**壳仓 `npm run check` ＋ SDK 腿 `lint`** 代位；② **无 bump、无 Release、无目录、无种子**（夹具不进发货集，`E6#15` 起就是 dev-fixtures 用途）。

- [x] 步 1 进场基线：**N/A `verify`（夹具无此脚本）** ⇒ 以 **SDK 腿 `lint`** 代位 ＋ 记下基线提交 —— ⑥ ✅ 基线 `a78bc30cfccde6cdec9d572690210c092b20805d`；改名前 SDK 腿读数 **`check-css-namespace：32 处未处理偏离`**（＝工具那 32 个裸定义站点）
- [x] 工具读数：本仓不合规 **N 名**（与 §三 快照对账；不一致 ⇒ 写清为什么）—— ⑥ ✅ **19 名 / 32 裸定义站点 / 1 文件**（`src/styles/DemoViews.css`）＋ **关键帧 0**。⚠️ §三 快照写的「19 名 / 40 处」——**名数 19 与快照一致**，「40 处」是**改名 diff 量级**（两把尺子里另一把），**实际改名面 = 4 文件 / 70 站点**（见下条）
- [x] 🔴 **token 判据 0 残留**（CSS ＋ 渲染点，整词匹配）＋ **正控 ≥1 命中** —— ⑥ ✅ 射程内（4 文件）旧名整词 **0**；正控 = 新名 **70**；**NC-d** 拿基线 4 文件跑同判据 ⇒ 残留 **70 > 0**；**NC-e** 拿基线净树整仓跑 ⇒ **72 > 0**。⚠️ **口径（双口径报数，④ 定的规矩）**：`src/` 之外的 2 处命中 = **`plugin.json` 的 view id**（`demo-output` / `demo-todo`，**不是类名**）——**具名例外**，理由见下「🔴 视图 id 陷阱」
- [x] 🔴 **形状证明**：逐字节 = 「基线 ＋ 只插入前缀」（＋ 该证明脚本的**负控**：对改名前文件跑 ⇒ 红）—— ⑥ ✅ **4/4 文件 · 70 站点**；剥 `panel-demo-` 后与 **`a78bc30cfc` 的 blob 逐字节相同**（比**索引 blob**，不比工作区：本机检出是 CRLF、提交字节是 LF）；工作区 CRLF 行数 263/133/114/82 逐文件报出（证行尾未被动）。**负控 5/5 真红**：NC-a 基线冒充 new（红 4/4）· NC-b 替换词干（红 4/4）· NC-c 改一处视觉属性 `22px→23px`（红 **1/4 = 恰好被改的那个文件**）· NC-d/Nc-e 见上条。⚠️ NC-c 第一版期望写成「红 4/4」**误报**——只改了一个文件就该只红一个，**负控的期望值也要精确**
- [x] 🔴 **工具射程外的渲染点补扫**（**本轮由 ① 的必做项升级为「必须用修正版」**，见新事实 1）—— ⑥ ✅ 改名前 **24 token**（19 漏网 ＋ 5 `codicon*`）⇒ 改名后 **19 带本仓前缀 / 0 漏网**（`scratch/scan-render-tokens-109h-b6.mjs`）
- [x] 关键帧（仅 `serial-monitor` / `marketplace`）：名字 ＋ **`animation:` 引用**双改，旧名 grep **0** —— ⑥ ✅ **本仓 N/A**（工具读数关键帧 **0**；`@keyframes` / `animation:` 在 `src/` 零命中）
- [x] 该仓 `verify` ＋ `test` ＋ `build` 全绿 —— ⑥ ✅ **`verify`/`test` N/A（夹具无脚本）**；代位读数：**SDK 腿 `lint` 的 `check-css-namespace` 由「32 处未处理偏离」→「✅ 零偏离」**（⚠️ `lint` 两次都 `EXIT=0`——三档设计「警告不封锁」；**红的是插件仓 CI 的严格腿，壳仓夹具没有那条腿**，所以本格的「转红→转绿」只能靠**偏离条数**证）· `npm run build`（SDK bin）出 **`panel-demo.linkdesk-plugin`（7.8 KB / 3/3 表面）**
- [x] bump 三处同笔（`package.json` / `plugin.json` / `CHANGELOG.md`）—— ⑥ ✅ **N/A：夹具不发布、不 bump**（`plugin.json` / `package.json` 版本恒 **1.0.1**）。**有门禁依据**：`check-version-bump.mjs` 的 `SOFTWARE_CODE_RE = /^(src|electron)\/.*\.tsx?$/` **锚在仓根** ⇒ `plugins/**` 不算软件侧，实测门禁原文「**无软件侧代码 staged——放行**」
- [x] **git 一次**（一仓一提交）—— ⑥ ✅ 壳仓 `1e9bad5d3`（**4 文件 / 65 增 65 删**）。⚠️ **壳仓另有写手在动**（本格工作期间工作区还有 `docs/04-软件更新/待抉择池/AI友好化-全自动操作/01-设计.md` 的未暂存改动）⇒ 只按**显式 4 条路径** `git add`
- [x] 🔴 **实机界面查询一次**：旧名命中 **0** / 新名命中 **>0** / computed style 与改前一致 / 无新报错 / 可见面逐屏看过（读数写进交接段）—— ⑥ ✅ **22 PASS / 0 FAIL**（隔离 profile ＋ CDP 9227，dev 源码树直读）：**面 1 面板「输出」**（19 行日志 / warn 6 / error 1；`.panel-demo-demo-toolbar` `padding 6px 8px`、`gap 4px`、hint `12px`、icon-btn `calc(22px*1)=22px`、`.warn`/`.error` 复合规则 accent/warning/error 色全部 CSS↔计算 **PASS**）· **面 2 面板「待办」**（items 3 / done 1 / check 3；input 24px、item 26px、check 16px、remove `opacity 0.55`、footer 12px **PASS**；`.done` 的勾选底色实读 **`rgb(0,120,212)` = `--accent` `#0078d4`**，规则原文 `background: var(--accent)`；`.done` 文本 `line-through` PASS；**交互**：加一条 → 4 条，切末尾勾选 → `done 2` 且 class = `panel-demo-demo-todo-item done`）· **面 3 侧栏演示视图**（空态「暂无日志输出」＋ `padding 8px 12px`/`13px`/`text-muted` PASS；执行 `panel-demo.sidebarAddLog` 后 empty→0、lines 23）· **面 3b** 删空待办 ⇒ `.panel-demo-demo-todo-empty` 现身（**19/19 类全数取证**）· **面 4**：**DOM 旧裸名整词 0**（19 类名全扫）/ 新名 13 类 / **view id 未被误改**（`wrong: []`）/ 控制台**零 panel-demo 报错**（唯一 warning 是既存 Electron CSP 安全告警）。截图 4 张（`scratch/shot-109hb6-*.png`）
- [x] 重发完成并读回核对（Release / 目录 / 种子；`check:bundled-freshness` 6/6 若适用）—— ⑥ ✅ **N/A：不发版**。代位**产物取证**（重建夹具包本地产物，非货架）：`panel-demo.linkdesk-plugin` 10 文件 · **旧裸名 0 命中** · **新名 148 命中** · `plugin.json` 版本 **1.0.1（未 bump）** · **三个 view id 完好**（`demo-sidebar` / `demo-output` / `demo-todo`）
- [x] `npm run check`（壳仓，**本格动了壳仓文件 ⇒ 必跑**）**EXIT=0** —— ⑥ ✅ **EXIT=0（156 文件 / 2,115 例 / 23.19s）**

> 🔴 **⑥ 的三条新事实（写给 ⑦⑧）**：
> **(1) 🔴 渲染点补扫脚本（④ 交付的 `scan-render-tokens-109h-b4.mjs`）有真缺陷——`className={`…`}` 全部隐形。** 它的 token 正则要求 `className=` **紧跟引号**（`(?:"…"|'…'|`…`)`），而 JSX 模板字面量写作 `className={`…`}`（`=` 后面先来一个 `{`）⇒ **整条式子匹配不上**。实证：panel-demo 的 `className={`demo-log-line ${l.level}`}`（`DemoOutputView.tsx:125`）、`className={`demo-todo-item${x.done ? " done" : ""}`}`（`DemoTodoView.tsx:82`）、`className={`demo-log-line ${l.level}`}`（`DemoSidebarView.tsx:75`）**三处都没被抖出来**——而**同期写的 1.16 判据** `scratch/judge-class-tokens.mjs:49` 用的正是正确形状（带 `\{`）。**后果**：⑧ 的「全量复核」若还用它，**「只在渲染点存在、CSS 零定义、且写在模板字面量里」的类名两个工具都看不见** ⇒ **交付修正版** `scratch/scan-render-tokens-109h-b6.mjs`（补 `{…}` 形态 ＋ 三元表达式内全部字面量 ＋ token 三分类「CSS 零定义 / 复合状态类 / 自有规则」并打印来源形态）。⚠️ 顺带：**⑤ 报的「74 token」是旧尺子的读数**，别拿它当基线对账。
> **(2) 🔴 视图 id 与类名同名——整仓 walk 式改名会改掉视图 id。** `plugins/panel-demo/plugin.json` 里的 view id 就叫 `demo-output` / `demo-todo`，与 CSS 类名**逐字相同**。若照 ⑤ 的写法整仓 walk，`"id": "demo-output"` 会变成 `"id": "panel-demo-demo-output"` ＝ **换掉视图 id**：作者面 `panel.reveal("demo-output")` 与 `docs/03-*` 两棵树当场失效，**而且没有任何门禁会拦**。⇒ 本格射程收成**显式 4 文件白名单**（CSS 定义面 ＋ 3 个 TSX 渲染面）。**⑦ 开工前先 grep 一遍 marketplace 的 view id 有没有和类名同名**。
> **(3) ⚠️ 表达式派生的状态类不在 token 射程（「复合状态类」判据的盲区）。** `className={`demo-log-line ${l.level}`}` 会渲染出 `info`/`warn`/`error`；`${x.done ? " done" : ""}` 渲染出 `done`——扫描器按设计**丢弃 `${…}`**（结果不是静态 token），这四个裸状态名**永不出现在补扫清单里**。实测：`warn`/`error`/`done` 都有复合规则（`.panel-demo-demo-log-line.warn …` / `.panel-demo-demo-todo-item.done …`）＝**合法的第三类**；**`info` 零规则**（走默认文字色）。跨容器核查 `src/` ＋ `plugins/` 里**无裸 `.info`/`.warn`/`.error`/`.done` 定义（0 命中）** ⇒ 今天无害、按纪律**不动**。⚠️ 但记着：**将来谁定义一条裸 `.info` 就会命中面板日志的每个 info 行**——这是「复合状态类」允许清单尚未封闭的缺口。

## 五、负控（照 `#109e`：真跑红 ＋ 逐字节还原）

| # | 注入 | 期望 |
|:--:|:--|:--|
| NC1 | 只改 CSS 一侧，把某个已改名类名改回旧名 | token 判据 🔴 红（`verify` **不一定**红 ⇒ 证明「判据必须跑」不是客套） |
| NC2 | 只改 TSX 一侧（CSS 不动） | token 判据 🔴 红 |
| NC3 | 手动把某处做成「替换词干」（`ms-item` → `marketplace-item`） | 形状证明 🔴 红 |
| NC4 | 关键帧只改定义、不改 `animation:` 引用 | 旧名 grep **>0** ⇒ 红 |
| NC5 | 把形状证明脚本对着**改名前**的文件跑 | 🔴 红（证明它不恒绿） |
| NC6 | 反向：改名正确后 | 全绿（且文件逐字节可还原） |

## 六、禁区

1. **不许并仓**：一仓一格一笔提交（⑦ 拆格后同仓两笔，边界按目录）。
2. **不许改视觉/语义**：只动标识符；发现必须动视觉 ⇒ **停下先调 design skill**。
3. **不许改词干**、**不许动 `className="input"`**、**不许碰 `ldk-` 名**。
4. **不许跳过实机查询**（判据是「看到了」不是「跑通了」）。
5. **不许先发后看**（发出去的版本必须是看过的那个）。
6. **不许拿「本地 build 里有新名」当发布证据**（要从货架/目录/种子读回核）。
7. **不许顺手修别的账**（`.input` 属 1.17；`.divider` 属另一件事；`.search-input` 撞车会在 ③/⑤ 后**自然消失**，不要为它单独立改动）。
