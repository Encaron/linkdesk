# E2 执行清单——底层加固 + 侧栏扩展位（40 任务，~1,195 行）

> 机械执行。做完一步打勾一步。四批顺序执行，同批内可并行。
> 详细设计见各子文档（E2a-E2d）。
> **零新功能。** 所有任务纯加固——行为不变，架构变。

---

## 第 0 批：E2a ErrorBoundary 增强（6 任务，~140 行）

> ⚠️ **必须最先做。** 多进程前必须的兜底——先装安全气囊。
> 详细设计见 `01-E2a-ErrorBoundary增强.md`。

- [ ] **#1** ErrorBoundary 增强——加 `pluginId`/重试按钮/fallback prop/componentDidCatch
  文件：`src/components/shared/ErrorBoundary.tsx` | +50 | 验证：pluginId prop → 崩溃显示 "「插件名」已崩溃 [重试]"

- [ ] **#2** 主区壳视图包 ErrorBoundary——welcome + plugin-detail
  文件：`src/components/MainContent.tsx` | +10 | 验证：欢迎页崩溃 → ErrorBoundary 兜底

- [ ] **#3** 插件 WebView 包 ErrorBoundary——每个插件外层包
  文件：`src/components/MainContent.tsx` | +20 | 验证：插件崩溃 → ErrorBoundary 兜底 → 其他插件不受影响

- [ ] **#4** 侧栏包 ErrorBoundary——sidebar 各面板独立兜底
  文件：`src/components/SidePanel.tsx` | +10 | 验证：侧栏面板崩溃 → ErrorBoundary 兜底

- [ ] **#5** 心跳检测——200ms interval ping + 2s 超时检测
  文件：`src/hooks/useHeartbeat.ts`（新）、`electron/main.ts` | +40 | 验证：JS 死循环 → 2s 后弹窗 "应用无响应 [刷新] [等待]"

- [ ] **#6** 内存监控——performance.memory 采样 + 阈值告警
  文件：`src/hooks/useMemoryMonitor.ts`（新） | +30 | 验证：RSS > 阈值 → toast 建议关闭不活跃插件

---

## 第 1 批：E2b 终端归一化（8 任务，~170/−105 行）

> 终端是 E3 新插件的模板——模板本身必须干净。
> 详细设计见 `02-E2b-终端归一化.md`。

- [ ] **#7** SerialContext 迁出 core/ → plugins/terminal/ + v3Api 改用 useSourceState
  文件：`plugins/terminal/SerialContext.tsx` + `App.tsx` + `v3Api.ts` + `src/core/useSourceState.ts`（新） | +40/−40 | 验证：grep "SerialContext" src/core/ 零结果

- [ ] **#8** 术语迁移——portOpen→sourceOpen、portName→sourceName
  文件：终端插件 + IPC handlers | +30/−30 | 验证：grep "portOpen\|portName" src/ 零结果（除注释/第三方库）

- [ ] **#9** useIpcEvent 归一化——裸 ipcRenderer.on() → useIpcEvent
  文件：`index.tsx` | +20/−15 | 验证：grep "ipcRenderer.on" plugins/terminal/ 零结果

- [ ] **#10** 模块级可变状态消灭——let → const / hook / session
  文件：`index.tsx` | +10/−10 | 验证：grep "^let _" plugins/terminal/ 零结果（允许 const）

- [ ] **#11** 命令路由修复——cmViewMap key = sourceId（保证唯一）+ WeakMap dispose
  文件：`index.tsx` | +20/−10 | 验证：两个终端标签页 → Ctrl+Shift+P 清空 → 只清空活跃标签页

- [ ] **#12** 硬编码审计——grep 四类模式 + 逐条判断 + 🔥 中文硬编码 → t()
  文件：全量 grep | −X | 验证：UI文字走 t()、注释通用化、变量名通用化
  > 额外 grep 范围：`grep -rn "[一-鿿]" plugins/terminal/ --include="*.tsx"` → 逐条加 `t()`。
  > 对标 03-插件制造硬约束 #2：所有 UI 文字走 t()。

- [ ] **#12a** statusBar.tsx——TX/RX 动态渲染标准化 + 🔥 侧栏 onMouseDown
  文件：`plugins/terminal/statusBar.tsx`（新） + `plugins/terminal/sidebar.tsx` | +25 | 验证：开串口→状态栏 TX/RX 实时跳动 + 侧栏会话列表点击→`onMouseDown` 替代 `onClick`
  > 对标 03-插件制造 UI 规约 §7：侧栏垂直列表选中条目用 `onMouseDown`——快速点击跨元素不丢事件。

- [ ] **#12b** 终端状态栏开关 + 配置注册到 ConfigurationRegistry
  文件：`plugins/terminal/plugin.json` + `index.tsx` | +30 | 验证：Settings Editor "串口监视器"分组出现→两个开关→关掉 TX/RX→消失→F5 仍关

---

## 第 2 批：E2c 基础设施缺口（22 任务，~775 行）

> E2b 拆干净后 core/ 边界更清晰。FileService 必须成为唯一 fs 入口。
> 详细设计见 `03-E2c-基础设施缺口.md`。
> 同批内：#13-#15（FileService/WorkspaceService/DialogService）→ 其余可任意顺序。

- [ ] **#13** FileService——listDir / readFile / writeFile / watch
  文件：`src/core/FileService.ts`（新） | +80 | 验证：listDir("/tmp")→文件列表、writeFile+readFile→内容一致、watch→外部修改 emit 事件

- [ ] **#13a** FileAssociationService——扩展名→插件映射
  文件：`src/core/FileAssociationService.ts`（新） | +80 | 验证：双击 .dxf → 查找对应插件 → 打开

- [ ] **#13b** 插件数据目录约定——E1 env-service + plugin-file-service
  文件：`electron/env-service.ts` + 新文件 | +30 | 验证：每个插件有自己的数据目录

- [ ] **#14** WorkspaceService——openFolder / onDidChangeFolders
  文件：`src/core/WorkspaceService.ts`（新） | +80 | 验证：openFolder→Electron dialog→rootPath 更新→订阅者收到通知

- [ ] **#15** DialogService——替代 window.confirm
  文件：`src/core/DialogService.ts`（新） + `<Dialog>` 组件 | +60 | 验证：3 处 window.confirm() 全部替换 + 暗色/亮色主题正确

- [ ] **#16** Chord 快捷键——Ctrl+K Ctrl+S 两段式
  文件：`KeybindingRegistry.ts` | +40 | 验证：Ctrl+K Ctrl+S 触发命令、Ctrl+K 等 2s 无反应

- [ ] **#17** keybindings.json——FileService 读写 + watch 热更新
  文件：`KeybindingRegistry.ts` + loader | +50 | 验证：改 keybindings.json→即时生效→删文件→回退出厂

- [ ] **#17a** 🆕 KeybindingResolver——冲突检测 + 优先级仲裁 + when 条件排序
  文件：`KeybindingRegistry.ts` | +30 | 验证：两插件同快捷键→detectConflicts() 返回冲突列表→resolve() 正确仲裁

- [ ] **#18** CommandPalette 抬升——mv shared/ + CSS 搬入壳 + 模糊搜索 + 🔥 触发键归一化
  文件：`CommandPalette.tsx` + `index.css` + `coreCommands.ts` + `KeybindingRegistry.ts` | +65 | 验证：卸载终端→Ctrl+Shift+P→浮窗正常（CSS 不受影响）+ fuzzyScore 匹配 "tgl"→"Toggle Terminal" + Ctrl+Shift+P 走 KeybindingRegistry 而非 App.tsx 硬编码

- [ ] **#19** CoreEvents 补漏——新增事件类型定义
  文件：`CoreEvents.ts` | +20 | 验证：tsc 零错误

- [ ] **#19a** G14 fix——PluginDetailView 订阅 onDidUnregister
  文件：`PluginDetailView.tsx` | +10 | 验证：打开插件详情页→卸载该插件→详情页即时显示 "已卸载"

- [ ] **#19b** `||` vs `??` 审计——全量 src/ 检查
  文件：`src/` 全量 | ~15 | 验证：grep "\|\|" src/ 逐条判断→`false`/`0`/`""` 应走 `??` 的改为 `??`

- [ ] **#19c** 旧代码 I/O 归一化——ConfigurationService/LayoutService/StorageService 走 FileService 唯一入口
  文件：`ConfigurationService.ts` + `LayoutService.ts` + `StorageService.ts` | ~30 | 验证：全部 I/O 经 FileService——grep "fs\." src/ 仅 FileService.ts 有

- [ ] **#19d** tabIdentity 声明驱动化——消灭 TAB_IDENTITY 硬编码表
  文件：`tabIdentity.ts` → plugin.json `tabBehavior` 字段消费 | +20/−30 | 验证：grep "TAB_IDENTITY" src/ 零结果

- [ ] **#19e** factoryRole + FactorySlots.ts——系统插槽机制（settings + marketplace）
  文件：`src/core/FactorySlots.ts`（新）+ `plugin.schema.json` | +40 | 验证：FactorySlots.getPluginId("settings") 返回 settings 插件 ID

- [ ] **#19f** 壳去终端化——App.tsx 移除 "terminal"/"settings" 引用
  文件：`App.tsx` + `coreCommands.ts` | +15/−10 | 验证：grep "pluginId === \"terminal\"" src/core/ src/App.tsx 零结果

- [ ] **#19g** statusBar configurable 字段——自动注册配置项 + 注入 visible prop
  文件：`types.ts` + `plugin.schema.json` + `loader.ts` + `StatusBar.tsx` | +20 | 验证：终端 statusBar 声明 configurable:true→Settings Editor "串口监视器"出现开关

- [ ] **#19h** ThemeEngine 5 缺口修复——清旧变量+冲突检测+unregisterTheme+补内置主题+onDidChangeTheme.fire
  文件：`ThemeEngine.ts` + `lifecycle.ts` + `CoreEvents.ts` | +40 | 验证：切主题无残留变量、同名注册 warn、卸载主题后 getAvailableThemes 不含、onDidChangeTheme 触发

- [ ] **#19i** statusBar onClick 消费——渲染为 button + executeCommand
  文件：`StatusBar.tsx` | +10 | 验证：声明 onClick:"terminal.toggleConnection"→点击→命令执行

- [ ] **#19j** WelcomeView + TabBar emoji 声明驱动化——删两处硬编码 map
  文件：`WelcomeView.tsx` + `TabBar.tsx` + `src/components/shared/pluginIcon.ts`（新） | +20 | 验证：grep "TYPE_ICON\|getPluginEmoji" src/ 走 plugin.json icon 字段

- [ ] **#19k** manifest 11 零消费字段清理——viewRole 消费 / 其余标注 Phase
  文件：`IconBar.tsx` + `loader.ts` + `plugin.schema.json` + `types.ts` | +20 | 验证：viewRole:"tabOnly"→点击图标直接开标签页

- [ ] **#19l** 🆕 deriveType 排他分类——theme>language>protocol>view
  文件：`PluginDetailView.tsx` | +10 | 验证：纯主题插件→类型显示"theme"、纯视图插件→"view"

---

## 第 3 批：E2d 侧栏扩展位（4 任务，~110 行）

> 独立于 E2b/E2c——可和第 1-2 批并行。依赖 E2a（ErrorBoundary 兜底）。
> 详细设计见 `04-E2d-侧栏扩展位.md`。

- [ ] **#20** plugin.json schema 加 `sidebarRole` 字段
  文件：`plugin.schema.json` | +10 | 验证：schema 校验通过

- [ ] **#21** viewRegistry 加 `getBySidebarRole()` + `getActiveTagFollower()`
  文件：`viewRegistry.ts` | +30 | 验证：getBySidebarRole("persistent") 返回文件树插件

- [ ] **#22** SidePanel 改为双槽位渲染——primary + persistent
  文件：`SidePanel.tsx` | +50/−20 | 验证：文件树声明 persistent→侧栏常驻→切标签页不消失

- [ ] **#23** IconBar 区分 persistent vs tagFollower 图标行为
  文件：`IconBar.tsx` | +20 | 验证：点文件树图标→不 toggle 侧栏（persistent 常驻）
