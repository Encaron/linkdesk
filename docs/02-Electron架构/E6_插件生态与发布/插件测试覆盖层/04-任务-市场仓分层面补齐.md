# 04 · 会话四（AI-D）：marketplace——最大仓的分层补齐 —— `E6#151`–`E6#152`

> **本会话动谁**：`E:\linkdesk-plugins\official\marketplace`——**只加测试文件**（`src/__tests__/**`，沿用本仓既有的扁平命名）。
> **发版**：**不 bump**（测试不进 `.linkdesk-plugin` 包）。⚠️ 例外：发现真 bug ⇒ `fix:` ＋ 该仓 PATCH ＋ 发版 ＋ 目录收录（🔴 用户点头）；🟢 **2026-09-25 用户明示授权：发现就先汇报再改，因修复产生的版本变化是被允许的**——⛔ 别压着不改、也别只报不改（流程 = 汇报 → 用户拍板 → 改 → 链路走全）。
> **上位纪律**：⛔ 不改共享 mock（插件专属桩住本仓测试文件）· ⛔ 不为凑数写无断言测试 · ⛔ 不推送 · ⛔ 不写 `CHANGELOG.md` · ⛔ 不碰 `plugin.json`。
> **为什么单开会话**：本仓 **6,935 行**（19 仓里最大）／2,992 行测试／**已有 10 个测试文件**——「有测试」与「测到了」在这里差距最大，**必须先把既有 10 个文件读透再动手**，否则会重复劳动或写出假裁决。它不该和别人挤一格。
> **本会话同样第一道工序是裁决**：读数里的「15 个零测试纯逻辑单元」是**机械读数**（按「无同名测试文件且无测试引用」口径出的名单）——本仓存在 `marketCatalog.test.ts` / `marketSources.test.ts` / `updateDiscovery.test.ts` 这类**按模块命名**的测试文件，它们**按基名匹配看不见**。逐条用 `grep -rn "<单元名>" src/__tests__/` 核过才算数。

---

## §一 `E6#151` 纯逻辑 15 单元（疑似清单 → 裁决 → 补测）

### 前因

- 立案读数：44 个纯逻辑单元里 **15 个零测试**——但其中 **`marketCatalog/{merge,parse,select,semver,sourceUrl}`**、**`marketSources/{fetch,io,load}`**、**`updateDiscovery/plan.ts`** 三组很可能已被同模块测试覆盖（`marketCatalog.test.ts` / `marketSources.test.ts` / `updateDiscovery.test.ts` 就在 `src/__tests__/` 下）。
- ⇒ 本格预期**真缺远少于 15**；把它当「15 个的体力活」会白干——**先花半小时裁决，省掉一整天**。

### 修哪里

**第一步 · 裁决（必做，产出表格）**

1. 读本仓 10 个既有测试文件，记录每个**实际 import 了哪些单元、断言了哪些行为**：`installConfirmPayload.test.ts` · `installJobs.test.ts` · `installedUpdateMeta.test.ts` · `marketCatalog.test.ts` · `marketCategories.test.ts` · `marketSources.test.ts` · `marketplaceInstallDispatch.test.ts` · `marketplaceUpdateFailure.test.ts` · `settingsSwapSmoke.test.ts` · `updateDiscovery.test.ts`。
2. 对下列疑似清单逐条 `grep -rn "<单元名>" src/__tests__/`：
   - 命中 ⇒ 裁决「**已覆盖 by X.test.ts**」（依据列写它测了哪几条），⛔ **不补**；
   - 未命中 ⇒ 裁决「**真缺**」⇒ 进第二步；
   - 判为「不值一测」（纯常量表 / 纯类型装配）⇒ 裁决「**不需测**」＋ 一句理由。
3. 顺带核出**第二类缺口**：既有测试文件里**只测了导出名、没测行为**的情况（`settingsSwapSmoke.test.ts` 听名字像冒烟 ⇒ 重点看它）——若发现「有名无实」，登记在表里交给 `#152` 判断要不要加深。

**疑似清单（立案读数，裁决后可能大幅缩水）**：

| 单元 | 预判 | 该测什么 |
|:--|:--|:--|
| `src/services/marketCatalog/parse.ts`（58 行） | 多半**已覆盖** | 目录 JSON 解析：缺字段 / 脏项 / 版本串 |
| `src/services/marketCatalog/select.ts`（136 行） | 多半**已覆盖** | 选取/排序：过滤条件、去重 |
| `src/services/marketCatalog/{merge,semver,sourceUrl}.ts` | 多半**已覆盖** | 合并策略 / semver 比较 / 源 URL 归一 |
| `src/services/marketSources/{fetch,io,load}.ts`（58/77/54 行） | 多半**已覆盖** | 取源（网络替身）/ 读写 / 装载回落 |
| `src/services/updateDiscovery/plan.ts`（88 行） | 多半**已覆盖** | 更新计划：版本比较 → 动作集 |
| `src/services/marketplaceShared/messages.ts`（136 行） | 待裁 | 文案装配（**注意**：本仓有中英字典，断言走假 `t`） |
| `src/services/marketplaceShared/searchState.ts` | 待裁 | 搜索状态机（若判为纯 ⇒ 本格；含 hook ⇒ 挪 `#152`） |
| `src/services/marketSourceAdd.ts` | 待裁 | 加源流程的纯部分（校验 / 归一） |
| `src/views/detail/DetailView/contribs.ts` | 待裁 | 贡献点摘要组装 |
| `src/views/detail/DetailView/useActionBits.ts`（81 行） | **归 `#152`** | 名字是 hook ⇒ 本格不碰（登记移交） |
| `src/views/sidebar/ExploreView/catalogRowText.ts` | 待裁 | 行文案派生（同 `messages` 的假 `t` 口径） |

### 怎么修

1. 裁决表先落盘（写进交接段）：`单元 | 行 | 裁决（已覆盖 by X / 真缺 / 不需测） | 依据`。
2. 只对「真缺」建文件：一单元一文件，`src/__tests__/<单元名>.test.ts`（**沿用本仓扁平习惯**——本仓 10 个既有测试全部是扁平的，⛔ 别改成镜像目录）。
3. **文案类单元统一用假 `t`**：`(k: string) => k` 或只断言 key（本仓走 i18n 字典，⛔ 不引真字典、⛔ 不断言中文串——字典会变，测试不该跟着碎）。
4. **网络类单元用替身**：`marketSources/fetch.ts` 若真缺 ⇒ `vi.stubGlobal("fetch", vi.fn())`，断言「请求形（URL/头）＋ 回落（非 2xx / 超时 / 坏 JSON）」三态。
5. 断言要有牙（自查：「改坏生产代码它会红吗」）；跑绿后把裁决表的最终形态补齐。

### 验收

| 项 | 判据 |
|:--|:--|
| 绿 | `npm test` 全绿（既有 10 个 ＋ 新增）；`npm run verify` 绿 |
| 裁决表 | 疑似 15 条逐条在（含「已覆盖 by X」的依据）——**本格的主交付物** |
| 判据 | 「真缺」项 = 0 残留（除移交 `#152` 的 hook） |
| 零生产改动 | `git status` 只有 `src/__tests__/**`（＋真 bug 那笔单独） |

### 版本与连带

不 bump；**单独一笔**提交，⛔ 不推送；本仓若是出厂种子 —— 只有**真发版**才谈 `sync:bundled`，本格不动。

---

## §二 `E6#152` 替身层：6 个必做 ＋ 一张 35 行裁决表（**本格的硬上界**）

### 前因

- 本仓实测 **35 个文件直用 `window.linkdesk`**（`grep -rl` 读数，规则：`*.ts`/`*.tsx` 排除 `__tests__`），其中大量是视图/动作链。**本层不追求覆盖它们全部**——那是另一个层（见 [总纲 §〇f](00-整理档案.md)）。
- 本格的价值：把**动作链**（安装 / 更新 / 源）里风险最高的 6 个单元测掉，**其余逐条写进裁决表并登记为「留给后续轮次」**——**登记 ≠ 遗漏**，这是本层对「不假装测全了」的交代方式。
- ✅ 本仓 `devDependencies` **已有** `@testing-library/react` ⇒ hook 可直接 `renderHook`（这是它与 file-tree/editor/settings 不同的地方）。

### 修哪里

**第一步 · 35 行裁决表**（覆盖上面前因里那 35 个文件，一律用 `grep -rl` 的实测名单）：

| 列 | 内容 |
|:--|:--|
| 文件 | 路径（35 行） |
| 类别 | store / 流 / hook / 视图 |
| 现有测试 | 命中哪个既有测试文件（用 `grep -rn` 核）或 `—` |
| 裁决 | **必做（进 6）** / 已覆盖 / 移交后续轮次（视图层） / 不需测（纯壳面转发） |

**第二步 · 6 个必做单元**（按风险排序，**这就是本格的动作链主干**）：

| # | 单元 | 行 | 该测什么 | 替身要点 |
|:--|:--|--:|:--|:--|
| 1 | `src/services/marketplaceShared/pluginsStore.ts` | 170 | 已装插件 store：装载 / 订阅派发 / 注销 / 变更通知（**全仓 UI 的数据源**） | `window.linkdesk.plugins.*` 三个读面（`readAllManifests` 等）桩成 `vi.fn()` 返回**本仓契约形状**的假清单；断言派发次数与顺序 |
| 2 | `src/services/marketplaceShared/catalogStore.ts` | 122 | 目录 store：加载态迁移（idle→loading→ready/error）、刷新、失效 | 同上，外加 `events.on` 接管写法（照 [02 §二](02-任务-serial-monitor补测.md) 样板） |
| 3 | `src/services/marketplaceShared/installFlow.ts` | 148 | 安装流编排：**确认闸门** ⇒ 提交 ⇒ 结果三态（成功 / 用户取消 / 失败）——🔴 断言「**确认前不落任何安装动作**」（与壳的「安装必须用户确认」纪律同源） | 用户确认面桩成可控 resolve/reject 的 promise；断言调用顺序 |
| 4 | `src/services/installGate.ts` | 96 | 安装闸门：兼容判据（`minAppVersion` 等）→ 放行/拦下/理由 | 壳版本面桩成常量；表驱动：低于 / 等于 / 高于 / 缺失 |
| 5 | `src/services/downloadCounts.ts` | 160 | 下载计数：缓存命中 / 过期重取 / 取失败**不炸页面**（回落零值） | 定时用 `vi.useFakeTimers()`；每例后 `vi.useRealTimers()` |
| 6 | `src/views/detail/DetailView/useInstallAction.ts` | 127 | 安装动作 hook：按钮态 → 动作 → 结果（**用户点得到的那一下**） | `renderHook` ＋ `act`；文件头 `/** @vitest-environment jsdom */` |

**备用池（6 个做完若有余力，或 6 里某个裁决不测时的替换）**：`useDetailActions.ts`(133) · `useDetailVersions.ts`(147) · `useDetailIdentity.ts`(134) · `marketplaceShared/commands.ts`(105) · `marketplaceShared/notifications.ts`(105) · `views/sidebar/ExploreView/useCatalogStatus.ts`(89)。

**🔴 硬上界（写死在格子里，防这一格变成无底洞）**：

- **必做 6 个封顶**；替补只从「备用池」里换，**不许**自行扩大范围；
- **`.tsx` 视图一个都不测**（26 个 `.tsx` 全归后续轮次登记）——理由：视图测试要吃 jsdom 交互与壳面替身，成本是纯逻辑的 5–10 倍，属于**另一层**的活；
- 裁决表里每一行都要有去处（必做 / 已覆盖 / 后续轮次 / 不需测），**零空格**。

### 怎么修

1. **先做裁决表**（35 行），再动测试——⛔ 不许先写一个测试再补表。
2. **替身口径**：插件专属桩住本仓测试文件（`(window.linkdesk as any).plugins = { … }`）；⛔ 不碰共享 mock。已装插件清单的**假数据要用本仓契约形状**（照 `@linkdesk/contracts` 的类型），⛔ 不用真插件名（memory：**测试夹具不许用真插件名/真 UI 文案**）。
3. **时间的坑**：周期/缓存类（第 5 个）用假定时器；**模块级 store 要在 `beforeEach` 重置**（本仓 store 是模块单例 ⇒ 不重置会跨例串味——memory `flaky-full-suite-tests`）。
4. **断言要有牙**：尤其第 3 个「确认前不落动作」——要**断言「没调用」**（`expect(installSpy).not.toHaveBeenCalled()`），这类否定断言是安装链最值钱的一条。
5. 全套**连跑两遍**都绿（防间歇红）。

### 验收

| 项 | 判据 |
|:--|:--|
| 绿 | `npm test` 全绿（含新 6 个文件）；连跑两遍全绿；`npm run verify` 绿 |
| 裁决表 | 35 行逐条有去处；「后续轮次」项逐条登记（这是本层的收口口径） |
| 判据 | 安装链五件（确认闸门 / 闸门判据 / 目录 store / 已装 store / 下载计数）**全部有测试** |
| 范围 | `.tsx` 零改动；`plugin.json` / `package.json` / `CHANGELOG.md` 一字未动 |

### 版本与连带

不 bump；**单独一笔**（`#151`、`#152` 可各一笔）提交，⛔ 不推送。🔴 **本仓是出厂种子**（6 只随包插件之一）——**只有真发版**才谈 `sync:bundled` ＋ 锁定版 ＋ freshness 门禁；本格不发版 ⇒ 全不涉及。

---

## §三 本会话收尾清单

1. 两轮 `npm test`（各连跑两遍）＋ `npm run verify` 全绿。
2. **两张裁决表**（15 行纯逻辑 ＋ 35 行替身层）抄进 [交接.md](交接.md)「会话四收口段」＋ `E6-执行清单.md` 的 `#151`/`#152` 行补读数（真缺数 / 新增文件数 / 用例数 / 后续轮次登记条数）。
3. 提交：**本仓单独一笔**（建议两笔：纯逻辑一笔、替身层一笔），⛔ 不推送。
4. 后续轮次登记：`.tsx` 视图与「已覆盖但有名无实」的那些 —— 逐条列名 ＋ 一句话理由，交会话五汇总。
