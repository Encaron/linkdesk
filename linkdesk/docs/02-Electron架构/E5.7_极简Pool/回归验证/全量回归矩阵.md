# 全量回归矩阵

> 极简Pool 迁移完成后的全量验证。每个场景列验证点 + 回归风险。

---

## 场景 1：侧栏交互

| 验证点 | 操作 | 期望 |
|:--|:--|:--|
| 图标栏切换 | 点文件树图标 → 点搜索图标 → 点市场图标 | 侧栏视图切换正确，图标高亮跟随 |
| 折叠/展开 | 点侧栏顶部分隔线 / Ctrl+B | 150ms 动画，EditorZone 自动扩展 |
| 拖宽 | 拖侧栏右侧 4px 分隔线 | 实时 resize，鼠标移出分隔线不丢事件 |
| 视图切换 | 侧栏内 tab 切换 | keep-alive 状态保持 |

**回归风险：** SidebarZone 从独立 WCV 变同 DOM 组件——插件组件必须还能 `import.meta.glob` 动态加载。

## 场景 2：编辑器

| 验证点 | 操作 | 期望 |
|:--|:--|:--|
| 打开文件 | 文件树双击 .c/.py/.ts 文件 | 编辑器打开，LSP 启动 |
| 切标签页 | 点多个 tab | 编辑器状态保持（Monaco 不重载） |
| 关闭标签页 | 点 tab × | 卸载正确，脏标记确认弹窗 |
| 脏标记 | 修改文件 | tab title 显示 ● |
| F12 跳转 / Ctrl+Click | 函数跳转 | LSP 正常 |

## 场景 3：分屏

| 验证点 | 操作 | 期望 |
|:--|:--|:--|
| 左右分屏 | 拖 tab 到编辑器右半区 | Glassmorphism 预览，drop 后分屏 |
| 上下分屏 | 拖 tab 到底部 | 同上 |
| 拖拽分屏线 | 拖分隔线 | 实时 resize |
| 合屏 | 拖 tab 回原 group | 分屏合并 |
| 深度限制 | 4 层分屏后继续拖 | 拒绝第 5 层 |

## 场景 4：浮层

| 验证点 | 操作 | 期望 |
|:--|:--|:--|
| 右键菜单——文件树 | 右键文件 | 菜单显示在鼠标位置，不被裁切 |
| 右键菜单——编辑器 | 右键代码 | 同上 |
| 右键菜单——标签页 | 右键 tab | Close/Split 等命令 |
| 菜单边界 | 右键窗口右下角 | 菜单自动翻转，不出屏 |
| 菜单消失 | 点菜单外 / Escape | 关闭 |
| 命令面板 | Ctrl+Shift+P | 居中显示，输入过滤，防抖 150ms |
| Toast | 触发通知 | 右下角显示，5s 自动消失，新 Toast 在下方 |
| Dialog | 关未保存文件 | 模态居中，焦点陷阱，Escape/backdrop 关闭 |

**回归风险：** ContextMenu 从 OverlayWindow 独立窗口改同 DOM position:fixed——z-index 必须高于所有 zone（3000+），pointer-events 切换正确。

## 场景 5：底部面板

| 验证点 | 操作 | 期望 |
|:--|:--|:--|
| 打开 | Ctrl+J | PanelZone 显示，EditorZone 缩小 |
| 切换视图 | 点 Terminal/Output/Problems | keep-alive 切换 |
| [+] 按钮 | 点 + | 新建终端 |
| 拖高 | 拖面板顶部分隔线 | 实时 resize |
| Toast 位置 | 面板打开时触发 Toast | Toast 仍在窗口右下角（25px from bottom），不被面板推走 |

## 场景 6：串口监视器

| 验证点 | 操作 | 期望 |
|:--|:--|:--|
| 多会话 | 打开 COM3 + COM5 | 两个 tab 独立 |
| 收发数据 | 发送/接收 | 实时滚动 |
| 右键菜单 | 右键数据区 | 菜单正常（clear/copy 等） |
| 协议下拉 | 打开协议选择 | 下拉不裁切，列表正确 |
| 关闭连接 | 点断开 | 状态更新 |

**回归风险：** 之前修复的 3 个串口 bug（右键空/无数据/Monaco 主题污染）在新架构下依然有效。

## 场景 7：插件热重载

| 验证点 | 操作 | 期望 |
|:--|:--|:--|
| 修改插件代码 | 改文件后保存 | import.meta.glob 更新 |
| 重载插件 | 市场里 Reload | 状态保持 |
| 新装插件 | 装新插件 | 无需重启壳 |

## 场景 8：崩溃恢复

| 验证点 | 操作 | 期望 |
|:--|:--|:--|
| renderer 崩溃 | 插件 while(true){} / kill renderer | 2-4s 自动重建 |
| layout 恢复 | 崩溃后 | 标签页/分屏/侧栏/面板全恢复 |
| Hot Exit | 编辑未保存内容 → 崩溃 | 重建后内容恢复 + dirty 标记 |
| 壳崩 | kill 壳 renderer（BrowserWindow webContents） | 全窗口重建 + tabState 持久化恢复 + lastLayout 兜底 |
| 连续崩溃 | 10s 内 3 次 | 停止重建，显示错误页 |

## 场景 9：脱出窗口

| 验证点 | 操作 | 期望 |
|:--|:--|:--|
| 拖出 | 拖 tab 超出窗口 30px | 新窗口跟随鼠标 |
| 独立编辑 | 在脱出窗口编辑 | 功能正常 |
| 关闭回归 | 关脱出窗口 | tab 回主窗口，状态保持 |
| 漂移面板 | PanelZone ⤢ 按钮 | 面板独立窗口化 |

## 场景 10：收尾检查

- [ ] `npx tsc --noEmit` 零错误
- [ ] `npx eslint` 零新增 warning/error
- [ ] vitest 基线测试通过
- [ ] grep 零残留：`OverlayWindow` / `sidebarPoolView` / `pluginViews` / `instanceId` / `?zone=`
- [ ] 进程数 = 3（主进程 + 壳 + Pool）——任务管理器验证
- [ ] F5 刷新——会话恢复、布局恢复

---

> 📖 执行清单 → [E5.7-执行清单.md](../E5.7-执行清单.md) Phase 16
