# 第 1.48 轮 · `#111n-7`：**官方仓清账 · 其余插件**

> 🔄 **§〇c 口径修订（2026-09-16 第二次拍板）——先读 [00 档 §〇c 口径修订令](00-整理档案.md)。**
> **被撤回的旧结论**：~~存量不改名 ／ 只报不改~~ ⇒ **改名 ＋ 迁移，规则零例外**。**根治的机械定义 = 官方仓需改处全 0**（`node scratch/audit-plugin-scope.mjs` 全 0）。
> **上位约束的新措辞**：**操作体验零变化**（**名字可变，取值与功能不许变**）。
> **本档因此新增的必做项**：任何改名**同笔带迁移** ＋ **逐项操作实测**。
>
> **一句话**：这一格**大概率是"零改动"**——但**它不是空跑**：它的产出是**一份逐项签字的"无面确认"**（每只插件为什么不需要改 ＋ 读数），以及 🔴 **把「三类全局概念名不改」这条口径在真实仓上钉死**（`python` 的 `.py`/`.pyi` 与 `editor` 的 41 个文件关联是同一类，两格结论必须一致）。
>
> **编号**：`#111n-7`
> **🔴 执行序与前置**：**前置 = [1.41 迁移基建](11-任务-迁移基建.md) 收官**；**建议排在 [1.43](13-任务-清账-editor.md) 之后**（要引它「文件关联不改」的登记结论）。**与其它清账轮互不依赖**。
> **拍板记录**：无新拍板；继承 §〇c 的三类「全局概念名」豁免口径（[00 §〇c.2](00-整理档案.md)）。

> **非新能力声明（`check-design-flow` §8.4 ③）**：本格是**清账复核与登记**（预期零改名），零视觉变化、零功能变化、零新增能力。⇒ **无需新能力设计前置**。

---

## 〇、本格在轴上的位置

| 项 | 值 |
|:--|:--|
| 射程 | **5 只**：`lang-defaults` · `lang-test-ja` · `python` · `theme-iconset-pastel`（⚠️ **已移出本格，见 §1.3**）· 壳仓夹具 `panel-demo` |
| 不射程 | 🔴 **`theme-iconset-pastel`**（它有 1 处要改 ⇒ **已归 [1.47 主题族](17-任务-清账-主题族.md)**）· 别的插件（1.42–1.47）· 壳侧判据与账 |
| 产出 | **逐项「无面确认」＋ 读数** ＋ **三类全局概念名的实证登记** |

---

## 一、事实与读数（**2026-09-16 实测**）

### 1.1 逐仓清账面（**全部 0**）

| 插件 | 需改 | 它有什么 | 为什么 0 | `seed` | 版本 |
|:--|:--:|:--|:--|:--:|:--|
| `lang-defaults` | **0** | 语言包 id `zh` · `en`；**i18n 顶层键 129**（它**就是应用级字典本体**） | 语言码是**国际标准标识符** ⇒ 三类全局概念名之一；i18n 键**跨仓不可判** ⇒ 天然黄灯（[1.39](09-任务-i18n与黄灯族评估.md)） | **true** | 1.0.18 |
| `lang-test-ja` | **0** | 语言包 id `ja` | 同上 | false | 1.0.4 |
| `python` | **0** | 语言定义扩展名 `.py` · `.pyi` | 扩展名是**全局概念名** ⇒ 加前缀 = 语义错误（与 `editor` 的 41 个文件关联**同类**，[1.43 §1.1](13-任务-清账-editor.md)） | false | 1.0.6 |
| `panel-demo`（壳仓夹具） | **0** | CSS 类名 | **已由 CSS 系列 1.14⑥ 清账**（19 个裸类名 → `panel-demo-*`）；非样式面实测无 | — | 1.0.1 |

### 1.2 🔴 本格真正的产出：**三类全局概念名的实证登记**（不是"什么都没干"）

| 类 | 实证 | 结论 |
|:--|:--|:--|
| **文件关联扩展名** | `editor` 声明 **41 个**（`.ts` / `.md` / `.py` …，[1.43 §1.1](13-任务-清账-editor.md)） | **不改** —— ① `.ts` 不可能是"editor 的 ts"；② [`FileAssociationService.ts:36-40`](../../../../../src/core/services/files/FileAssociationService.ts) 明写「同一扩展名允许多个插件注册——"打开方式…"选择器消费全部」⇒ **加性设计，不是覆盖型花名册** |
| **语言定义扩展名** | `python` 的 `.py` / `.pyi` | 同上（与文件关联同一性质） |
| **语言包语言码** | `lang-defaults` 的 `zh` / `en`；`lang-test-ja` 的 `ja` | **不改** —— 语言码是国际标准标识符（BCP-47 风格），不是命名空间 |

⇒ 🔴 **登记去向**：[1.49](19-任务-门禁与作者面收口.md) 的作者面「**三类全局概念名为什么不需要带前缀**」段（[1.19 → 19 号档 §2.3 第 3 条](19-任务-门禁与作者面收口.md)）。**本格把材料整理好交上去。**

### 1.3 ⚠️ 一处**口径订正**（第 0 格的探针盲区，如实记）

第 0 格的探针只统计了 `themes[].id` 与配方 id，**没有把 `iconThemes[].id` 计入"需改处数"** ⇒ `theme-iconset-pastel` 在 [00 §〇c.4](00-整理档案.md) 表里被写成 **0 处**，**实际有 1 处**（图标主题 id = `ld-iconset-pastel`，不带本插件归属）。
⇒ **处置**：已**移出本格、归入 [1.47 主题族](17-任务-清账-主题族.md)**（那格是主题/图标 id 的射程）。本格**只复核确认它不在这里**。

---

## 二、要做（**含「同笔面」**）

### 2.1 逐仓复核（**跑读数，不是看一眼**）

跑 §六 ①②，**逐仓**给出：
- 该仓在**本轴 5 个面**（命令 id / 设置键 / 外观族 id / 上下文旗子 / 协议 id）上的**读数**（名数 ＋ 不合规数）；
- 若为 0 ⇒ **写明"为什么 0"**（是"没有这类声明"还是"声明了但属豁免类"）；
- 若**不为 0** ⇒ 🔴 **不许在本格顺手改** ⇒ **在交接段点名 ＋ 建议归哪一格**（**不许静默放过**）。

### 2.2 三类全局概念名的登记材料（本格的主要产出）

- 整理成一段**可直接进作者面**的文字（三类各一条：**是什么 / 为什么没有"主" / 证据出处**）；
- 🔴 **与 [1.43](13-任务-清账-editor.md) 的结论对齐**（同一类不许两格给两种说法）；
- 交给 [1.49](19-任务-门禁与作者面收口.md)。

### 2.3 i18n 观察台账的核对（与 [1.39](09-任务-i18n与黄灯族评估.md) 衔接）

- 复核 `lang-defaults`（**129 键**）与 `lang-test-ja` 的键**是否有跨插件共写**（用 [1.39 §2.1](09-任务-i18n与黄灯族评估.md) **订正过口径**的读法：**剔掉同一插件的中英两份**）；
- 🔴 **结论只作观察，不进任何门禁判定路径**（[1.39 §2.4](09-任务-i18n与黄灯族评估.md) 的台账边界）。

### 2.4 「无面确认」的签字形态（**本格的交付物形态**）

一张表：**5 只插件 × 5 个面 × （读数 / 是否豁免 / 豁免依据出处）** ＋ 每只插件一行结论。
🔴 **必须带可复跑命令**（否则下一棒无法复核）。

---

## 三、判据表（**含必须真跑过的负控**）

| 判据 | 要求 | 负控（**必须真能红**） |
|:--|:--|:--|
| **① 逐仓读数齐全** | 5 只 × 5 面 × 读数（名数 ＋ 不合规数），**带复跑命令** | — |
| **② 不误伤（零改动证明）** | 本格 `git diff` **只含文档**；5 只插件仓 `git status` **干净** | 断言插件仓零改动 |
| **③ 三类全局概念名有实证** | 三类各一条 ＋ 出处（`file:line`） | — |
| **④ 与 [1.43](13-任务-清账-editor.md) 口径一致** | 文件关联扩展名与语言定义扩展名**同结论** | 两格结论不一致 ⇒ 本条红（**这是"跨格一致性"的机械检查**） |
| **⑤ 探针盲区已登记** | `iconThemes[].id` 的盲区 ＋ `theme-iconset-pastel` 的归属（§1.3） | 若本格把它当 0 处漏掉 ⇒ 本条红 |
| **⑥ 若有非 0 面 ⇒ 已点名** | 逐条写进交接段 ＋ 建议归属格 | 有非 0 面却写"本格 0 处" ⇒ 本条红 |
| **⑦ 壳仓门禁** | `npm run check` **EXIT=0** | — |

---

## 四、禁区

- 🔴 **不许给三类全局概念名加前缀**（文件关联扩展名 / 语言定义扩展名 / 语言包语言码 —— 加前缀 = 语义错误，[00 §〇c.2](00-整理档案.md)）。
- 🔴 **不许把 i18n 键判红**（跨仓不可判；硬判必出假红）。
- 🔴 **不许在本格顺手改别的仓的东西**（发现非 0 面 ⇒ **只点名 ＋ 建议归属格**）。
- 🔴 **不许改 `theme-iconset-pastel`**（它已归 [1.47](17-任务-清账-主题族.md)）。
- 🔴 **不许动 CSS 类名**（已由 CSS 系列 1.14⑥ 清账）。
- 🔴 **不许引白名单 / 基线 / 棘轮**。
- 🔴 **不许改 `pluginId`**（硬约束 11）。
- ⚠️ **上位约束**：**操作体验零变化**（名字可变，取值与功能不许变）· 只做加法且名字一次定对 · [29 号档](../样式命名空间归一化/29-主题系统保护条款.md) 优先 · 对外宽容。
- ⚠️ 照旧：不跳 `npm run check` · 推前现测代理 · 推完 `ls-remote` 对 SHA · **提交只按路径 `git add`**。

---

## 五、交棒要求

1. **收尾四件套**（交接段顶部追加 · 清单轮次进度行 · 勾格全套 · **队列表整张复制**）。
2. 🔴 **回报三组东西**：① **「无面确认」表**（5 只 × 5 面 × 读数 ＋ 复跑命令）；② **三类全局概念名的登记材料**（交 [1.49](19-任务-门禁与作者面收口.md)）；③ **§1.3 探针盲区的登记**（含 `theme-iconset-pastel` 的归属说明）。
3. 🔴 **判「本格不做某条」必须写明理由**（本格大概率全是"不做" ⇒ **每一条都要有理由与读数，不许写"无面"了事**）。
4. **下一棒 = [1.49 门禁与作者面收口](19-任务-门禁与作者面收口.md)（`#111k`）** —— 🔴 **最后一棒清账轮**，之后就是**收紧为红 ＋ 账定稿 ＋ 作者面 ＋ 发版**。
5. 跑 [00 档 §八](00-整理档案.md) 探针并记账。

---

## 六、🔴 进场第一步（命令）

```bash
# 0) 确认自己是哪一棒
sed -n '1,40p' "docs/02-Electron架构/E6_插件生态与发布/01-插件独立构建/非样式命名空间归一化/交接.md"

# 1) 逐仓自量（面清单只用来对账）
node scratch/audit-plugin-scope.mjs | sed -n '/── lang-defaults/,$p'

# 2) 三类全局概念名的出处（登记材料的依据）
sed -n '30,45p' src/core/services/files/FileAssociationService.ts     # 加性设计
grep -n "langDefs" -A 6 "E:/linkdesk-plugins/official/python/plugin.json"

# 3) i18n 键规模（含订正口径：剔掉同插件中英两份）
node -e "
const fs=require('fs');
for (const d of ['lang-defaults','lang-test-ja']) {
  for (const f of fs.readdirSync('E:/linkdesk-plugins/official/'+d).filter(x=>x.endsWith('.json'))) {
    try { const j=JSON.parse(fs.readFileSync('E:/linkdesk-plugins/official/'+d+'/'+f,'utf8'));
      console.log(d+'/'+f, Object.keys(j).length); } catch {}
  }
}"

# 4) 零改动证明（本格 `git diff` 只含文档）
git status --short | grep -v "^?? .*非样式命名空间归一化" | head

# 5) 收尾（禁跳）
npm run check
```

---

## 七、与其它格的关系

- **[1.43 清账 · `editor`](13-任务-清账-editor.md)**：🔴 **口径必须一致**——它的 41 个文件关联扩展名与本格 `python` 的语言定义扩展名**是同一类**（判据④的机械检查点）。
- **[1.47 清账 · 主题族](17-任务-清账-主题族.md)**：🔴 **`theme-iconset-pastel` 归它**（本格只复核"它不在这里"）。
- **[1.39/1.40（i18n）](09-任务-i18n与黄灯族评估.md)**：本格复核它的台账口径（**剔掉同插件中英两份**）。
- **[1.49 门禁与作者面收口](19-任务-门禁与作者面收口.md)**：🔴 **本格的两份材料交给它**（三类全局概念名的登记 ＋ 无面确认表）。
