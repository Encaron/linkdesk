# E6 清单第二轮精简下沉（2026-09-13）

> 🔒 **非新能力声明**：本文是清单精简的历史执行记录存档，不含任何新能力/新 API/新贡献点设计——文中出现的 `window.linkdesk.*` / `contributes.*` / `auxiliarybar` 等字样均为**既有实现的事后描述**（design-flow 门禁要求，照 05-迁移指南 banner 先例）。
> 本文 = 当日第二轮清单精简所删叙述的收容所（第一轮见各任务对应档案与 commit message）。清单行只留「一句话结论 + 指针」；本文按清单出处组织，需要细节按节回查。

---

## 一、展望区 E6#60（主题/图标作者面 schema 收编进 plugin-sdk）——已收口 2026-09-06

**E6#60a 收编形状**：icon-theme.schema.json 重写对齐引擎实收（stale 字体 glyph 契约→E5.8#133 mappings 匹配表：plural 段 files/extensions/folders/foldersExpanded × 双形态 glyph{class,color}/image{imagePath} + 顶层 singular 默认五键 + font 元；additionalProperties:false + anyOf 两分支）；theme/icon-theme 双副本字节 cp 进 plugin-sdk/schemas；check-plugin-schema-sync 按文件分组守卫盖 3 组 7 份（plugin×3 + theme×2 + icon-theme×2）。

**E6#60b sdk validate 覆盖主题数据文件**：SDK `validateThemeJson`/`validateIconThemeJson` 立——validate.ts 按 schema `$schema` 自动选 ajv 构造（2020-12→Ajv2020；draft-07→Ajv 默认）+ 数据文件严格 JSON 读（非 JSONC，对齐引擎/repo 机械闸）；`check-theme-schema.mjs` 兼扫 `contributes.iconThemes`——repo 机械闸 10 主题 + 1 图标 mappings 全合规；check-npm-release 黄灯 surface 盖 `packages/plugin-sdk/schemas/**`。

**E6#60c 作者档/模板同步**：03-插件制造 11-主题制作 §② 配方改指 npm 通道（`./node_modules/@linkdesk/plugin-sdk/schemas/theme.schema.json`）+ repo 内 `../../../public/schemas/theme.schema.json`；03-contributes 3.6 theme 契约句 + 3.7 iconThemes 补 singular 默认五键说明 + mappings 契约句；真相源行补 theme/icon schema。#22b 模板待第 2.1 轮脚手架落（届时带 `$schema` 指 npm 包）。

**收官（2026-09-06，用户拍板「#60 自成一小轮」+「icon-theme.schema 重写后再收编」+「真发 0.1.2」）**：@linkdesk/plugin-sdk@0.1.2 真发 npmjs（0.1.1→0.1.2 向后兼容；传播 ~30s；release:mark 基线记/灯灭）。仓库外实证：`E:\linkdesk-build\scratch\e6-60-outofrepo` 新工程 `npm i @linkdesk/plugin-sdk@0.1.2` → schemas/{theme,icon-theme}.schema.json 齐 + validateThemeJson/validateIconThemeJson 跑真实主题/图标 mappings 全过、篡改负例全拦（与 check-theme-schema.mjs 同源）。主软件不 bump。

## 二、展望区 E6#61（右侧 auxiliarybar——E6 内不做）——61a 摸底实录

**61a 摸底（2026-09-07 代码级实证）**：① settings 可见面 = 主区 singleton 标签，非 sidebar 槽也非右栏（entry 直出 SettingsView + FactorySlots.getActive("settings") 开主区标签；`viewsContainers.settings location="auxiliarybar"` 的容器/视图注册无 surface 路由消费）。② 右栏零生产者 = 真 gap 非错觉：rightSidebar zone 常驻（LayoutEngine.ts:352-358 addZone edge:right 300/180-600）但 usePoolSync.ts:221-242 硬推 visible:false + containerId:null + views:[]（注释自承「壳侧暂无容器内容生产者」）；RightSidebarZone 不自扫容器、仅被 layout.rightSidebar 喂内容；全 src 零 `getViewContainers("auxiliarybar")` 消费者（sidebar/panel 各有消费，auxiliarybar 无）；loader `hasSidebarContainers` 亦把 auxiliarybar 排除出左栏——schema location 三值里 auxiliarybar = 定义在、2 声明者（settings + floating-panel-demo，均 tab 型 entry 插件）、零 surface 消费。③ 若将来做 61b：settings/floating-panel-demo 现都主区标签可见，做通用「auxiliarybar 容器→右栏」生产者会把二者拉成右栏双表面——生产接法须含「容器入右栏后主区标签归属/去重」判定（对标 VS Code 容器落辅助栏语义）。

**61b 设计要点（保留作文档）**：生产者落 usePoolSync（同 sidebar 同款：`getViewContainers("auxiliarybar")` 有容器+有视图才推 visible:true）+ hideIfEmpty（无声明 → 维持现状零 DOM）+ 与 sidebar↔rightSidebar swap 规则 / iconBar 入口协同 + RightSidebarZone 消费字段对齐。

**退役拍板**：#61 E6 内不做（2026-09-07 用户拍板，conscious 非 deferral，memory `e6-right-sidebar-auxiliarybar-retired`）；61a 摸底入库不废（区域保持 dormant）；残留另议 = schema location 枚举 auxiliarybar + 03-插件制造/08 第三方承诺句 dormant 标注（2026-09-07 提请用户拍板）。

## 三、第 1 层 E6#17d（壳 loader 纯声明化）——修复轮与绿态实机证叙述

**修复轮（993daf80f，0.1.2）**：首轮实机 ❌——壳侧声明信号 ✅（DTO 含 serial-monitor:component marker）但池渲染组件不可达 = serial statusBar.tsx post-#17d 零 import 方 → build-plugins.mjs 整文件丢弃（物化 bundle grep 全 0）→ PoolStatusBarComponent 三探针 linkdesk:// 全 Failed to fetch（CDP 实证）→ return null → 左区空（比 #17d 前静态 connection 点还糟）；叠加池静态 glob mis-root（`../../plugins` 自 src/pool/shared/pool-status-bar/ 仅上 2 级）未兜住。修复 = 池 glob 修根 `../../../../plugins`（真达项目根）→ 构建时 glob-内 内置插件 statusBar 源静态编入池 chunk（rollup 产物 statusBar-*.js，asar 内 grep serial-status ✅）→ 打包态实机复验：池左区渲染 `.serial-status-conn`/`.serial-status-led`（title=未连接、color=var(--text-muted) 正确熄灭态——修复前整区空）。绿态过渡 = serial 会话 UI 逻辑（裸 openPort 报 path not defined 印证须会话驱动），dev 已证、打包态可达性本轮补上。

**绿态实机证（9a78da9e7，盲点 B 终判据全通）**：fresh UD 打包版（0.1.2）内 UI 驱动——侧栏 serial 会话列表「新建会话」→ 开出真监视器 tab（新会话 1）→ 工具栏端口选择 COM3（用户真连 STMicro 设备）→ 点「打开」→ 会话日志「已打开串行端口 COM3」（05:26:56:600）+ 工具栏翻接收态（COM3 · 115200）→ 状态栏 LED 熄灭态 `rgb(138,138,138)`（var(--text-muted)）转 `rgb(34,197,94)` = var(--serial-monitor-ok)（#22C55E）亮绿——开/关两态 CDP computed-color 双点实测钉死（statusBar.tsx:87 isOpen 三目直驱动）。第三方 dist 插件自绘状态栏正式通道归 ①a 立案 E6#62 子任务④（dist statusBar export 约定）。

## 四、第 5.1 轮 E6#41——命名空间矩阵过期（已刷新+门禁）与三条门禁缺陷（✅ 已修 6239b187b）

**命名空间矩阵过期刷新 + 机械门禁（2026-09-12 当日完成）**：矩阵停 2026-08-20（自称 40 命名空间，实际 45/242 方法/14 域接口）。差集：少 6（appearance·floatingPanelHost·panel·settings·factorySlots·app，全是 E5.8/E6 新增作者可见面）+ 多 1（toast——已随「通知面唯一」E6#72 整删）。🔴 原文「多 2 个」写错被实测订正（config 在契约里活着）——差集断言必须由脚本产出，不能人眼比两列。为什么必须修：01-插件API契约 §二 把该矩阵定为「唯一真相源」，人对的入口指向过期一个月的表。做出来的门禁 = `scripts/check-namespace-matrix.mjs` 挂 check（五条判据：① 契约每个命名空间在矩阵有行 ② 矩阵无死面行 ③ pool/shell/mock 三列逐名对真实注入源——格值必须对源，只查行集的洞实测踩过：toast 的 mock 格填错、appearance/app 的 shell 格漏标行集检查全看不见 ④ §2 表尾统计行四数字 ⑤ §1 速览表数字）。证明在旧行为上会红五路全测（删行/改格/改统计/加死面/把 toast 加回去，全部 EXIT=1 指名到具体格）；每测完恢复、末后校验字节一致。同源过期一并修：§1 速览表数字 39/22/40/12→44/24/45/13、「无机械校验」断言删（E5.8#20 起两个 preload 各带 satisfies 编译期卡死）、表头平行复述数字的话删数字改指针。残余不查（脚本头注明）：契约标法/IPC 通道组/备注三列是人脑知识，错了仍需人读。

**三条门禁缺陷登记（C1）——✅ 2026-09-13 已修（6239b187b，用户放行）**：① 矩阵 §3「32 组/127 通道」无机械对账（嵌套子对象/动态工厂形状平扫漏计，平扫 153 是下界；修法 = 先写对计数器再谈门禁：递归下降 + 认 as const + 认工厂函数返回值 + 组名集合与每组通道数之和两个独立量互证，真值回填 §3 后挂 check-namespace-matrix——同一份文档不开第二个脚本）② check-api-contracts.mjs 正则洞：`[a-zA-Z][a-zA-Z]*` 匹配不了 p2p 这类带数字名字（换 `\w+` 与 contract-parse.mjs 同口径 + 用 p2p 当断言样本证明旧正则会漏）③ check-ipc-audit.mjs 不剥注释：loadIpcMap 的 findMatchingBrace 跳字符串不跳注释，channels.ts 注释里写引号 ⇒ 括号配对错位 ⇒ 尾部三组通道从 ipcMap 消失 ⇒ 报「源码拼错」而实际是审计瞎了（修法 = loadIpcMap 过 stripComments + 解析出的通道组数与 channels.ts 组数对账，不齐报「审计失效」）。三条共同判据：修完必须在旧实现上会红（不是「改了就算」）。

## 五、第 5.0 轮 E6#37e——console-message 弃用警告清账（实测记录）

**判据①旧日志留证**：0.1.47 打包产物启动输出抓到原话 `(electron) 'console-message' arguments are deprecated…`（存档 E:\linkdesk-build\scratch\dep-before.log）。**探针法**：win-unpacked\LinkDesk.exe --user-data-dir=<scratch>——必须用一次性 profile 绕开单实例锁（main.ts:44 requestSingleInstanceLock），且必须先 unset ELECTRON_RUN_AS_NODE（VS Code 子进程自带 =1 ⇒ 二进制退化成 Node 报 bad option，electron-run-as-node-trap 同型）。**判据③**：check EXIT=0（152 文件/2060 测试）。**判据②代码层**：0.1.48 打包产物启动后 stdout/stderr 两流对该警告零命中；再经 CDP 从池渲染进程发对照两条——console.log('CRIT-POOL-INFO')→stdout、console.error('CRIT-POOL-ERROR')→stderr ⇒ 分流按等级工作（两侧都验排除「全塞一处」假绿）；同批壳渲染进程的 CRIT-SHELL-ERROR 落 protocol-debug.log ⇒ main.ts 那处仍写盘。**旧行为会红实证**：('error' >= 3) === false 而对照 (3>=3)===true、('error'==='error')===true ⇒ 病根是类型从数字变字符串不是比较写错。**grep 复核三类命中**：main.ts:156 + window-manager.ts:163 = 已改两处（新签名）；本清单正文记的旧签名；E5.5 PerTab 设计文档一行 = 已废弃架构文档非代码（doc-code-citation-shape-trap 假命中），归档文档不擅改登记备查。**探针环境观察（登记不立案）**：0.1.47 隔离 profile 探针启动后出现一次壳渲染进程崩溃；0.1.48 同法未复现，差异未定位不猜。**装机层复核（9400b280e，0.1.47→0.1.48）**：用户正向覆盖安装后从装机版真 profile 启动——弃用警告两流 0 命中 + 启动正常无崩溃；装机版池渲染进程对照两条 INFO→stdout/ERROR→stderr ⇒ 装机层分流同样成立。判据①②③含装机层三条全绿。

## 六、第 5.2 轮 E6#42d——NSIS 覆盖安装实测长注

**正向（0.1.47→0.1.48 用户实测全绿）**：0.1.47 装着不动直接双击 0.1.48 包；未出现「已检测到旧版」提示直接进入安装；向导路径仍是 D:\01link\LinkDesk；版本三处独立证据全 0.1.48（注册表 DisplayName/DisplayVersion + app.asar!package.json 真值 + exe 版本资源 ProductVersion/FileVersion）；安装目录 mtime 全刷新；InstallLocation 未变。**反向（关键格）**：源码实证 = 逐份读 app-builder-lib/templates/nsis/ 全部模板无任何版本比较逻辑（version 只在 BrandingText 外观 + WriteRegStr 只写从不读）⇒ NSIS 默认不拦旧版覆盖；实测确证（手里只有 0.1.47 老包——守卫是 0.1.49 才加的）覆盖已装 0.1.49 静默装上：退出码 0、DisplayVersion 掉回 0.1.47、mtime 回退 ⇒ 源码推断升格实测确证。**结构事实**：守卫编进安装包 ⇒ 只管「从这一版起往后构建的包」，已发老包（≤0.1.48）双击进来自带放行 ⇒ 安装器侧无论怎么写都保护不了它们 ⇒ 安装器守卫与应用层提示是两道闸各管各的。⚠️ 本次实测把用户机器从 0.1.49 降成 0.1.47，已当场重装回 0.1.49 并核三处版本证据。**守卫落地（0.1.49）**：载体 = build/installer.nsh（electron-builder 自动 include 约定，没动 electron-builder.yml ⇒ 不与 5.3 右键注册抢配置）；挂点 customInit（initMultiUser 之后画界面之前）；比对源 = 卸载项 DisplayVersion（installer.nsh:125 每次安装无条件写）；口径 = 用户拍板「更旧/同版都问一句默认取消」；六路实测：① 本包更新不问直接进向导 ② 同版弹窗 ③ 更旧弹窗（②③ 弹窗文字/中文渲染/按钮焦点截图存证 dlg-same.png/dlg-older.png）④ 点否退出码真值 1602（ERROR_INSTALL_USEREXIT；bash $? 会截成 66，狗值须 Start-Process -PassThru 的 ExitCode）且四项逐字未变 ⑤ 裸按回车同样 1602 ⇒ 默认按钮确实是「否」 ⑥ 静默 /S：同版 1602；0.1.48→0.1.49 升级退出码 0。另证不是死路：点是正常进安装向导。**判据订正**：原「直接复用 updateTargetDirection()」做不到——NSIS 是独立编译的安装器脚本跑不了 TS，改用 NSIS 自带 ${VersionCompare} + 三行策略；「不许各写一份判断」不撤——方向决策仍唯一在 semverUtils，安装器层只「覆盖前问一句」不参与语义。⚠️ 打包跑了两趟：首跑 NSIS 编译中止——两个 Var 在 -DBUILD_UNINSTALLER 那遍没人引用 ⇒ makensis warning 6001 而 electron-builder 按警告即错误编（-WX）⇒ 修法 = Var 包进 !ifndef BUILD_UNINSTALLER（变量可见性须与宏可见性对齐），坑写进 build/installer.nsh 行内注释。**应用层降级提示（0.1.57 四条判据全过含变异负控四条实红）**：判据① 记住最高版本 = 壳侧 StorageService 新键 update-highest-version（零新 IPC），runVersionDowngradeCheck 只增不减（无记录播种/比账上高抬高/持平不动/比账上低只提示不写账——写了等于自己抹掉证据）；判据② 出口 = notifyVersionDowngrade（useUpdateNotifications 新增 ⑧，app.update source 唯一生产者）走铃铛面板常驻 ttl:0 唤醒 × 可关 + 一颗 [检查更新]；消息带两个版本号（抑制键 = ${source}::${message}，只写「被降级了」会连坐）；频度 = 每次启动都提示直到点 × 或升回去（用户拍板）；判据③ 升级路径走 raise 支通知器零调用有专门用例钉住；判据④ 变异负控四条条条实红（删 skip ⇒ 4 红；降级也写账 ⇒ 2 红；升级也报警 ⇒ 1 红；消息丢 highest ⇒ 2 红）。**动手前最大单点风险已解决**：StorageService.read 分不出「文件不存在」与「读出错」（都返 null），同仓 releaseNotesOnLaunch 的账正着用，本账必须反着来 ⇒ 判据用 exists() 先问「有没有」再问「是多少」；有记录却读不出来 ⇒ 什么都不做（宁可这轮不报也不许拿当前低版本覆盖账上高版本）。**设计提醒（撞 bug 类②）**：判据①的「历史最高版本」（只增不减）与 #57.13d 的 lastSeenVersion（每次启动都写当前版本）是两个不同的问题——合成一个变量一次降级就把信号自己抹掉 ⇒ 两个 key 必须分开。

## 七、第 5 层头部——开工前必读六条（照第 3.5 层照镜子的产物）

① **图是权威，代码是执行**——本层每个带 UI 的任务行内写明 mockup 帧号（mockups/0X Frame N），没写帧号 = 没验收基准；执行时先开图再写码。② **图与实机 CSS 不一致 → 以「实机 CSS + 用词校正后的图」为准并当场改图**（mockups/02 画的是已删载体 ToastHost + 默认折叠 + 无进度条，与 E6#72a/#71h/#71i/#73k 后实况相反 ⇒ 已重画删旧稿）。③ **通知类 UI 载体只有一个 = 状态栏铃铛宽面板**（E6#72 归一）；条目恒全显（71h）、进度走 4px 真进度条（71i+73k）、来源名由通知面机制解析不写进 source（73g S5）；行文「toast」一律读作通知面条目。④ **安装/升级逻辑先抄插件侧踩过坑的那条**——plugin-install-handlers.ts 锚①（同版恒拒/旧版默认拒/显式 allowOlder 才放）；主软件侧同型风险见 #57.6 方向守卫，不许各写一份。⑤ **下载类逻辑必须回答「中断了怎么办」**——插件侧 .part 惯例（01-GitHub-Releases方案 §四·五），主软件侧 #57.6d/e；失败不许塌成「未知错误」、不许没出口（#57.5c）。⑥ **文件体量三档制自查**（壳目录规范 §7.8）；本层新壳侧文件上限 800、.css 300；超限唯一正解 = feature-folder 不许同域平铺多文件。

## 八、第 5.4 轮执行序表「为什么在这个位置」理由列

① #57.12h 第一发排最前：零代码纯发布动作，是「能发布」这个事实的第一次落地，后面每格都站在它上面。② #57.8e 升第 2 位：#57.13b 的硬前置，不先落地则 #57.13 无从取数（用户 2026-09-13 改序）。③ #57.13 升第 3 位：依赖②已通 + ①已有 Release 可看；用户原话意图 = 尽快看到那张标签页真的能用。④ #57.15e 立尺子随改序降位——代价如实记：#57.8e 与 #57.13 两次 bump 发生在尺子之前，版本判定是临场判断（规则早已在硬约束 22 与 version-bump skill，是「没把尺子收口进机械门禁」不是「没有尺子」）。⑤ #42d 应用层降级提示必须在实机批之前——#57.16e 倒测第④步（拿老包覆盖回去）正是它的验收现场，不落地则第④步验的是空气。⑥⑦ #57.14 与 #57.13 同批（都是软件内改动同批发一版）；#57.13h 第二发在两格开发半收尾后——「两个版本」前提到这一步才具备。⑧ #57.16 收口 = 本清单唯一端到端验收格，A 类实机欠账全在此销；2026-09-13 编排拍板：先发 0.1.59 再一局做完 A13/A14/A15（A15 物理上要等第二个 ≥0.1.57 的版本，0.1.59 本来就必须发——两件事一次解决，省掉同一套实机动线第二遍）。**2026-09-13 改序记录**：用户问「为什么不先做 #57.13」⇒ 说明硬依赖后选「先 #57.8e 再 #57.13」；#57.15e 与 #42d 子项挪到 #57.13 之后；唯一被动摇的约束 = 「尺子先于 bump」；#42d 先于实机批没动。**预案声明**：执行序表是排出来的打算不是锁——前提不成立/被吸收/需调整都正常，照实改并记账；判据落在格子正文可证伪的断言上，不落在顺序表上。

## 九、其余轮头/层头删减（已确认在对应档案，直删不留沉）

- **第 3.5 层头「🔒 铁律」行** = 硬约束 16（设计 skill）+ 硬约束 2（t()）+ 硬约束 22（version-bump）的复述，全局约束在 CLAUDE.md，删。3.6/3.7 层头同款铁律行同删。
- **第 3.6 层头「本层收的账」四条**：34 处超限实测 → [文件整理层/00-整理档案.md §总量](文件整理层/00-整理档案.md)（line ~172）；休眠的哨兵 → memory `snapshot-shadows-truth-bug-class` 第 ③ 类实例二；夹宽判据 → 00 档案 + memory `file-split-thresholds`；2 真拆 → 00 档案 02-主软件整理。全在档，直删。
- **第 3.7 层头「四条账」**：链路断三截/四个死字段/教具歪/样板手写 → [插件规范化层/00-整理档案.md](插件规范化层/00-整理档案.md)（诊断节全在），直删。
- **第 4 层头「执行时点 = 最后统一做」叙述**：与第 5 层头 + 第 5.0 轮「由来」三处重复，保留第 5.0 轮一处 + 本文存档。原文要点：2026-09-11 用户拍板「不要那个新建插件，等新建插件前置的所有做完之后统一再新建一个插件」＋「把第四层和第五层耦合的那个任务就放下来，放在第五层做」；L4 四件事整体是收官验收不是铺路，L5 任何任务不许以「等 L4」为由停摆；E6#37 已整颗迁 L5 5.0 轮（L5 是唯一同时消费它的层）；#38/#39 对 #37 依赖不变，跑到时 5.0 已绿不必请示；不许拿 electron:dev 冒充安装版（packaging-run-sop）。
- **第 5.0 轮头「由来」叙述**：#37 消费方跨三轮（5.2 #42d / 5.3 实机三格 / 5.4 #57.16）任何一轮不该独占；排在 5.1 之前 = 本层任何实机格开工时它已绿；不许与 electron:dev 混为一谈（本轮是「把打包当一轮正式验证做」）；用户曾提议省掉安装版——核实后同意不省（覆盖安装/卸载程序/右键写注册表三件事在免安装版上不存在那条路径）。
- **第 1.3 轮头「组件特权」叙述**：第三方没有 @src 想用壳组件只能求壳改代码违反插件独立铁律 → #54 是 #15 硬前置；核心无知原则叙述（内置插件焊死壳 = 壳带着插件专属依赖被污染）——已在 [01-插件独立构建/07-共享组件独立分发设计.md](01-插件独立构建/07-共享组件独立分发设计.md) 与 09-塌平决策，直删。
- **第 3.5.7 轮头硬约束叙述**：ⓐ 顺序不许反（先删静默闸 = 无上限并发窗口且点着四处 N=1 单例）/ ⓑ 73e 不得后置（无下载超时卡死 job 永久占槽）→ 已随 73 批全闭，过程档在 [18-通知系统全账与设计定案.md](03-插件市场/18-通知系统全账与设计定案.md) §八；开工首步两项实机实测（面板不遮默认布局安装钮 ⇒ 让位降级条件项；确认卡弹不了 7 张 ⇒ 不补排队基建）→ 18 档 §八⑳㉑。
