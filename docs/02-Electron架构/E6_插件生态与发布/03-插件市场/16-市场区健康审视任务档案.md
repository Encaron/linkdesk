# 16-市场区健康审视任务档案

> **立案**：2026-09-09（用户拿官方目录第一只真插件 Hello LinkDesk 实机连环操作，撞到安装/更新/弹窗/目录一批「看着不对」的地方，要求逐条给源码级根因；AI 查完按四类定性 + 主动加查此区其它 bug，用户随后拍板「全部列任务档案，然后列任务清单」）。本档案 = 用户原话留档 + 现状事实（读码 + 远端目录实测）+ 四类定性 + 任务拆分（E6#71a-g + toast 批 71h/71i/71j）。细节载体，执行清单只留指针行（E6#71，仓库纪律同 [13](13-详情页HTML对账任务档案.md)/[14](14-市场图标系统任务档案.md)/[15](15-详情页说明展示任务档案.md)）。
> **性质**：健康审视/缺陷与缺口收拢——市场区多批高速推进后的 **doc-code seam + 分类字典缺口 + 挂起未做 + 偏离已定设计** 四类残留，非新能力设计。
> **相关档案**：[08-信任与安全.md](08-信任与安全.md)（富确认设计 §四）/ [10-市场UI拥有权.md](10-市场UI拥有权.md)（§四·三「壳 Dialog 容器 + 市场自绘内容」）/ [05-更新与版本.md](05-更新与版本.md)（classify 归因/版本下拉）/ [13](13-详情页HTML对账任务档案.md)（壳保底 + B3 大小行尾注）/ [15](15-详情页说明展示任务档案.md)（README 两来源两态）/ [E6-执行清单 L3.5 📌挂起](../E6-执行清单.md)（Q4 已在案）。设计流程记忆 `new-capability-design-flow` / 审视节奏记忆 `e6-review-rhythm-execution-batch`。

---

## 〇、一句话总览（大白话）

用户用官方第一只真发布的插件 **Hello LinkDesk** 在软件里实际操作，一条条撞见：装了旧版却不提示有新版本、选了最新却更新失败还说不出为什么、安装确认弹窗歪在左上角还不压暗、侧栏和详情两个安装入口行为不一样、没装时详情页只有一句话、已下载的大小 3.5 KB 不变蓝点不进文件夹。用户要 AI 把每一条都解释到源码级，并顺带自查这片区域还有没有别的坑。查下来**没有一条是「原则失效」**——每条都能落到具体来源：有的是**设计如此但造成困惑**（壳保底文件夹、横幅逻辑本身是对的），有的是**系统性设计缺口**（更新失败被统一误报成「未知错误」），有的是**偏离了已定设计**（安装确认弹窗没按「壳 Dialog 容器」做，自绘漏了定位/遮罩），有的是**挂起未做**（已装大小行开目录），有的是**作者/发布侧缺口**（官方样板自己的目录条目没填 readmeUrl）。另续一条新报（Q5，2026-09-09）：**安装失败 toast 点 [重试] 没反应、只有详情/侧栏有效**——注册源缺口（失败 toast 走命令派发，其命令唯一注册源在 #62b 撤「壳 import 插件入口」后失去「始终在场」保证，F8）。本档案把每条拆成任务（E6#71a-g），**已跟踪不排期、用户点名才进批**。

同日续 **toast 反馈批（用户点名、先落档待排批，见 §二 F9-F11）**：查 71g 失败 toast 时用户点名 toast 本身三处反馈链路行为改——**① 删简略模式、toast 恒全显 + 完整版文字可鼠标拉取复制**（原只有简略版能复制）/ **② 下载/安装装真进度条**（数字文字保留、条照加）/ **③ 下载失败 toast 长期驻留到手动关、下载成功几秒自动关**——拆 **E6#71h/71i/71j**。

---

## 一、📌 来源与用户观察留档（2026-09-09，大白话逐条 + AI 定性）

> 元问题先答：用户问「这些是不是因为我最近没重申那 10 条工程原则导致的？」——**不是**。逐条归因见下表「来源」，没有一条需要靠「背原则」才能避免；共同点 = 市场区近几轮高速多批推进，doc-code seam + 分类字典没随更新域一起长 + 挂起项自然积累。机械纪律（ESLint / check / 硬约束）一直在场，但管不住「一个弹窗有没有居中」这种视觉/行为细节。

| # | 用户观察（原意大白话） | 定性 | 归因 |
|:--|:--|:--|:--|
| Q1 | 壳里还有 `plugin-detail` 文件夹？不是说过详情页归市场插件吗 | **设计如此**（保底宿主，非壳重占详情页） | E6#30.11 + 10 档 §六·五 明写；附带 F7 死 CSS |
| Q2.1 | 侧栏「探索插件」点了直接装，详情页会先弹窗——两处安装不一样？ | **归一缺口**（同一动作两入口行为不一致） | 设计 mockup 02 把侧栏行内安装也归「点安装先弹确认」，实现只做了详情入口 |
| Q2.2 | 没装时详情页只显示一句话简介，装完才变全文 README | **发布侧缺口**（官方样板目录条目没填 readmeUrl） | 远端 marketplace.json 实测无 readmeUrl → 预览态降级 description |
| Q2.3 | 卸载后装 v0.1.0，没有「有新版本可用 v0.1.1」提示 | **疑似刷新时序**（横幅逻辑本身对） | hasUpdate 依赖详情页已装态快照；装完未及时刷新则横幅暂不出现 |
| Q2.4 | 选了 v0.1.1 点更新却还提示「有新版本可用」，点更新又报「未知错误」 | **真缺陷两连** | ① 横幅如实（更新没成功，还在 v0.1.0）；② 归类字典只覆盖安装域词，更新域报错全落 unknown（F1） |
| Q2.5 | 整体更新/版本系统有重大问题 | 部分属实 | 见 F1 + 71a 实机取证（已取证落 F5.1：**非机制瘫痪**——早前轮真链验证闭过；实机 = 下载腿 github 间歇断为主因，UI 面「超时遮蔽 + 域消息归因黑洞」把它变成看不懂的失败） |
| Q3 | 安装确认弹窗在最左上角、背后不变暗、失焦不消失、跟一般弹窗不一样 | **真 bug**（偏离已定设计） | mpd-confirm 自绘但漏居中/遮罩；设计本要「壳 Dialog 容器」（F2） |
| Q4 | 大小 3.5 KB 不变蓝不可点，不能进所在文件夹 | **挂起未做**（你记得没错） | 已在 E6 清单 📌 L3.5 挂起有案（已装态开目录 API），未排期（F6，交叉引用不重复建任务） |
| 加查 | （用户要求顺带查此区其它 bug） | 见 §三 F1-F7 全部 | — |
| Q5 | 安装失败 toast 点「重试」没反应，只有详情页和侧栏的重试有效 | **真缺陷**（doc-code seam——#30.9b toast 动作依赖的命令注册，其唯一注册源被 E6#62b 撤了「始终在场」保证） | 见 F8 + 71g |

---

## 二、🔬 现状事实（读码 + 远端目录实测，file:line 证据）

### F1 更新失败归因系统性缺口 —— 「更新失败：未知错误」黑洞（真缺陷）

**现象**：更新失败 toast 永远只给「更新失败：未知错误，请重试」，不说真正原因。

**根因链（两截，都读码实证）**：
1. 归类字典是**照「安装」域报错文案**写的（[marketplaceShared.ts:346-360](../../../../plugins/marketplace/src/services/marketplaceShared.ts#L346-L360)）：`NET_RE = 下载中断|下载失败|HTTP|超时|网络|fetch|socket…`、`INT_RE = checksum|校验|哈希|大小不符…`、`ENV_RE = 磁盘|空间不足|权限…`、`PKG_RE = 解压|zip|损坏|plugin.json…`、`CON_RE = 已存在安装目录|覆盖…`。
2. 引擎**「更新」域**报错是另一套词（[update.ts:84-173](../../../../src/pluginLoader/lifecycle/update.ts#L84-L173)）：`插件 XX 未找到安装目录` / `插件 XX 不在用户安装区` / `缺少更新包来源` / `包内版本与当前版本相同 XX——无需更新` / `新版本需高于当前版本 XX——降级需在版本下拉显式选择旧版` / `替换失败，已恢复旧版: …`。**一个 token 都不在字典里** → `classifyInstallError` 全落 `unknown` → `updateFailLabelKey` 默认「更新失败：未知错误，请重试」（[marketplaceShared.ts:380-395](../../../../plugins/marketplace/src/services/marketplaceShared.ts#L380-L395)）。

**附带缺口**：`updateFailLabelKey` 连 conflict 分支都没写（:380-381 注释自认「conflict 对更新不适用 → 归 unknown 兜底」）；失败原始 error 只在 title 悬停留一注，用户实际只看到笼统 toast，无从判断是下载/目录/替换/版本方向哪一环。

<a id="f1-bua-71b"></a>
> **补案（E6#71b 闭，2026-09-09）**：此缺口已修——见 §三 71b ✅ 行。要点：不做「扩更新域词入安装类目」（会把「不在用户安装区」误标成「插件包损坏」，误导 > 笼统），改 **unknown 时直显引擎原文**（更新域报错本就是完整可读句）+ conflict 分支防御性补上 + default 撤「未知错误」谎 → 「更新失败，请重试」。实机喂出的证据③ 串（`插件 "…" 不在用户安装区…`）现在是 toast 终局文案，不再黑洞。

### F2 安装确认弹窗偏离已定设计 —— 左上角 + 无遮罩（真 bug）

**现象**：点「确认安装」弹出的富确认卡片贴窗口左上角、背后不变暗、与一般弹窗观感差异大；代码注释自称「居中卡片」。

**根因链**：
1. **为什么自绘是对的**：壳 `dialog.confirm` 契约只收一句纯文字（[preload-shell.ts:275-276](../../../../electron/preload-shell.ts#L275-L276) `confirm: (message: string)`），DialogHost 只渲染 title + message。安装确认要展示来源/发布者/许可证/版本/大小/官方徽标，壳给不了富内容 → 市场自绘。**此步合理**。
2. **错在没按已定设计走壳 Dialog 容器**：设计 [10-市场UI拥有权.md §四·三](10-市场UI拥有权.md) 写的是「壳 Dialog 容器 + 市场自绘内容」——壳 DialogHost 自带居中 + 全屏遮罩（[DialogHost.css](../../../../src/pool/floating/dialog/DialogHost.css)：panel `fixed; top:50%; left:50%; translate(-50%,-50%)`、backdrop `inset:0; rgba(0,0,0,.5)`）。实现却绕用它，改用 [OverlayPortal.tsx](../../../../src/components/shared/overlay-portal/OverlayPortal.tsx)——这组件**只做 portal + 外点关 + Esc 关，不管定位不管遮罩**（[OverlayPortal.tsx:116](../../../../src/components/shared/overlay-portal/OverlayPortal.tsx#L116) wrapper 仅 `position:relative`）。
3. **自绘又漏了定位**：卡片 CSS [MarketplaceDetail.css:837-847](../../../../plugins/marketplace/src/styles/MarketplaceDetail.css#L837-L847) 只有 width/bg/圆角/阴影，**无一条定位规则** → 普通块级落浮层根部 = 窗口左上角；无遮罩平面 → 背后不压暗。代码注释「居中卡片」（[DetailView.tsx:1094-1096](../../../../plugins/marketplace/src/views/DetailView.tsx#L1094-L1096) + [MarketplaceDetail.css:836](../../../../plugins/marketplace/src/styles/MarketplaceDetail.css#L836)）与实现**直接矛盾**。

**连锁副作用**（对应用户 Q3 的「行为怪」）：卡片顶部压着窗口顶部 30px 拖拽区（硬约束 18 老坑）那条不可点；无遮罩 = 无「点旁即关」的可感知提示；OverlayPortal 外点判定用整层 wrapper（块级横贯整行）→ 点卡片同一排空白区不算「框外」不关。

<a id="f2-bua-71c"></a>
> **补案（E6#71c 闭，2026-09-09）**：此缺口已修——见 §三 71c ✅ 行。要点：**壳 DialogHost 补富内容槽 `content:{pluginId, renderPath, payload}`**（[poolDialog.ts](../../../../src/core/types/pool/poolDialog.ts)）——present 时替代 title/message/默认按钮渲染，弹窗机制（居中/遮罩/Esc/Tab 焦点锁/点遮罩取消）由壳给（[DialogHost.tsx](../../../../src/pool/floating/dialog/DialogHost.tsx)）；富内容 = 插件视图经 `PluginComponent` 挂载（仿 FloatingPanel 声明寻址范式，壳零持渲染器、核心无知）。**三确认归一终态**：装 = `linkdesk.dialog.confirmContent({pluginId, viewId, payload})` 富内容卡（市场 ConfirmInstall 视图自画排版/按钮，§四·三「排版、按钮市场自由画」兑现）；卸 + 降级 = 同容器纯文字 `dialog.confirm` 原语不变——装/卸/降级共用同一 DialogHost 容器。**payload 不透明序列化载荷**随打开参数过壳→回池结构克隆（无跨 bundle 会话 store——prod 各视图独立 surface bundle、无模块单例，detailview 与 confirm 视图跨 bundle 共享内存是 dev-only 假象）。**viewId→renderPath 解析**走 `ViewContainerService.getView` 读 loader 运行时附挂 `_pluginId/_renderPath`（仿 floatingPanelReveal `resolveFloatingPanelView`）；解析失败防御回落纯文字 message 确认（弹窗仍出、不静默死）。**Enter 防双触发**：富内容模式 DialogHost 不注册面板 Enter→confirm（内容自带按钮/链接，聚焦其上 Enter 冒泡会与原生 click 双 settle）——`handleKeyDown` 遇 content 早退。老坑根因（壳 `dialog.confirm` 只收纯文字）仍在但被富内容槽绕开——无需扩纯文字契约收富内容（不把排版收归壳模板，保住 §四·三自由画）。

### F3 两个安装入口行为不一致（归一缺口）

**现象**：侧栏「探索插件」行内「安装」点了直接装；详情页「安装」先弹富确认。

**证据**：[ExploreView.tsx:110-127](../../../../plugins/marketplace/src/views/ExploreView.tsx#L110-L127) `handleInstall` 直装（无任何确认）；[DetailView.tsx:551-555](../../../../plugins/marketplace/src/views/DetailView.tsx#L551-L555) `handleInstallClick` → `setConfirming(true)` → 富确认。

**设计背景**：mockup 02 状态总表把「探索插件组 + 行内『安装』」也归到「点安装先弹确认（帧 8）」同一行——即设计意图两入口都确认。但 08 §四 / 任务行只写了详情入口，实现便漏了侧栏。

<a id="f3-bua-71d"></a>
> **补案（E6#71d 闭，2026-09-10）**：此缺口已修——见 §三 71d ✅ 行。要点：**用户点名定「两处都先确认」（待定点② 落定）**——侧栏「探索插件」行内安装对齐详情页，**不引入「列表快装」差异档**（mockup 02 两入口都确认本意完整落地）。改动 = [ExploreView.tsx handleInstall](../../../../plugins/marketplace/src/views/ExploreView.tsx) 在 `startMarketInstall` 前加详情页同款富内容确认：`linkdesk.dialog.confirmContent({pluginId:"marketplace", viewId:"marketplace-install-confirm", payload: installConfirmPayload(entry)})`——`confirmContent`/`ConfirmInstall` 视图/`installConfirmPayload` 全复用 71c 现成机制（零新壳能力、零新视图声明、零新 i18n key），市场只加一个 import + 一段确认闸。**版本诚实**：payload 缺省 installVer → version = `entry.version`，正等于探索行所显 `v{version}`、所装 `entry.downloadUrl` 对应版本——确认卡不撒谎。确认闸落在既有三门禁（离线/缺下载地址/缺安装面）之后、`startMarketInstall` 之前；`confirmContent` 面缺失保守 no-op（与详情页同款，老 preload 防御）。**类属**：marketplace 插件 1.0.4→1.0.5 + changelog + 随包 zip 重建；软件零改动（confirmContent 71c 已在壳）→ 0.1.25 不 bump。实机 CDP 9222 五证据闭：侧栏点「安装」→ content 模式富确认卡正中 442×388 + 全屏遮罩显（非直装）/ 取消 → 卡关未装行仍「安装」/ 再点确认安装 → 真装成功行翻「已安装」/ 装回 Hello 与测试前备份字节级一致 / 备份清理零残留。

### F4 官方样板目录条目缺 readmeUrl —— 预览态只显一句话（发布侧缺口）

**现象**：Hello LinkDesk 未安装时，详情页只显示一句 description；装完才显示完整 README。

**铁证（抓远端实测）**：`https://raw.githubusercontent.com/encaron/linkdesk-marketplace/main/marketplace.json` 里 hello 条目只有 `id/name/version/description/author/downloadUrl/size/publishedAt/versions[]`——**没有 readmeUrl**。链路：[marketCatalog.ts:34/126](../../../../plugins/marketplace/src/services/marketCatalog.ts#L34) `readmeUrl?` 同名键透传，远端没有即 undefined → [DetailView.tsx:613-629](../../../../plugins/marketplace/src/views/DetailView.tsx#L613-L629) 未装态 `mode:"none"` → 降级 description。设计本如此（04 详情页 §三：未装 = fetch readmeUrl；无 → 降级），但**官方第一只样板恰好没示范 readmeUrl**，把「市场预览看到完整 README」这条路自己堵死。与 [12-README说明区媒体契约.md](../../../docs/03-插件制造/12-README说明区媒体契约.md) §一 表格「市场预览态需 readmeUrl 才显远程 README」互相印证。

### F5 实机待取证 —— 更新 0.1.0→0.1.1 失败真因（前置取证任务）

**现象（用户 2.4）**：已装 v0.1.0 → 详情页更新到 v0.1.1 → toast「更新失败：未知错误」。更新机制早前轮次**验证通过过**（执行清单记录过 hello 0.1.0→0.1.1 真链实测闭），故怀疑是**特定场景路径**（降级到旧版后升回、或当前文件被占用）下的失败，而非机制整体瘫痪。

**代码能证明的**：F1 说明无论哪个引擎错误，界面只会显示「未知错误」；失败归到 update.ts 的哪一行，**静态读码无法断言**——必须先实机抓原始 error 串。

**附加疑点（本机自测，不敢坐实）**：远端 v0.1.1 release 资产 HEAD 200，v0.1.0 反复 HTTP 000（网络间歇/限流 vs 资产丢失，未定；用户当日早些时候降级装成功过，资产应存在）。另 Q2.3「装完 0.1.0 无横幅」疑似详情页已装态刷新时序，一并取证。

### F5.1 实机取证结果（E6#71a，2026-09-09）—— 更新失败真因 = 下载腿 github 间歇断 + 10s 桥遮蔽 + 域消息归因黑洞

**复现方式**：dev 实机（CDP 9222）经 `window.linkdesk.pluginManager.update('hello-linkdesk', { url: v0.1.0 资产, allowOlder:true })` —— 与详情「更新」按钮同一引擎入口（`plugins:call 'update'` → 壳 updatePlugin）。顺带②远端可达性当日已验：两资产曾有 HTTP 200。

**证据① —— 下载腿失败 + 10s 桥遮蔽（时间戳主进程日志）**：
- `13:40:23.595 [main] [ipc:plugins:stage-update] → 入 "hello-linkdesk", …v0.1.0 资产, "0.1.1", true`
- `13:40:43.442 [main] [ipc:plugins:stage-update] ✕ 错 19847ms TypeError: fetch failed`（cause `read ECONNRESET`）

fetch 卡 **19.8s** 才 ECONNRESET，落点 [plugin-download.ts:64](../../../../electron/services/plugin-download.ts#L64)（`downloadPackage` 内 `fetch(url,{redirect:"follow"})`）→ 主进程 stage-update handler 抛（下载分支 [plugin-install-handlers.ts:175-178](../../../../electron/ipc/handlers/plugin-install-handlers.ts#L175-L178)）→ 壳 [update.ts:113](../../../../src/pluginLoader/lifecycle/update.ts#L113) stage await 拒 → [update.ts:169-172](../../../../src/pluginLoader/lifecycle/update.ts#L169-L172) catch 包 `{success:false,error}`。

但池侧**先**收到 10s 桥超时：main↔壳 IpcBridge 请求超时 = **10 秒**（[ipc-bridge.ts:195-198](../../../../electron/ipc/ipc-bridge.ts#L195-L198)）。19.8s 下载 > 10s → 池 `pm().update` **reject** `[IpcBridge] 请求超时: plugins:call (requestId=bridge-285-…)`（非 resolve `{success:false}`）→ [DetailView.tsx:455-457](../../../../plugins/marketplace/src/views/DetailView.tsx#L455-L457) catch → classify 命中 NET「超时」→ network → toast「更新失败：网络连接不可用」。**真错误晚 ~10s 落在无人接听的桥后**。

**推论①'（比遮蔽更重）**：`update` 全流程 >10s 即桥超时（下载一腿常态 >10s）——**即便下载最终成功、磁盘已原子换新，池仍判失败 → 成功更新被误报失败 + 用户无感知磁盘已变**。结构性成因：pool `pluginManager.update` 走 request-response 桥裸 invoke 无进度 keepalive（[namespaces-plugin.ts:32](../../../../electron/preload-pool/namespaces-plugin.ts#L32)），与下载秒级~十秒级时长不匹配。治本方向另立（71b 非本注范围）。

**证据② —— 网络根因（下载腿为何断）**：取证当下两 release 资产 curl 全 HTTP 000（~20-21s 卡死；v0.1.0 连试 5 次全败、v0.1.1 亦 000）——当日早些时候两资产均有 200 → **github 间歇性限流/重置（本机网络）**，正是用户报的「v0.1.0 反复 HTTP 000」症状。Q2.4「更新失败」大概率即此下载腿。

**证据③ —— 「未知错误」真产生者 = 域消息（已实机喂出一条，零网络 2ms）**：池端 `pm().update('zzz-no-such-plugin',{url})` resolve `{success:false, error:"插件 "zzz-no-such-plugin" 不在用户安装区——仅安装包可更新（app 树插件走重新构建）"}` = [update.ts:47](../../../../src/pluginLoader/lifecycle/update.ts#L47) throw。该串无 NET/INT/ENV/PKG/CON 任一 token → classify unknown → [updateFailLabelKey:392-394](../../../../plugins/marketplace/src/services/marketplaceShared.ts#L392-L394) 默认「更新失败：未知错误，请重试」。update.ts 域 throw 全集：`:44 未找到安装目录` / `:47 不在用户安装区` / `:87 壳缺三段 handler` / `:105-107 缺少来源·市场未提供下载地址` / `:127 commit 替换失败 rethrow` + main stage 校验 [plugin-install-handlers.ts:196-200](../../../../electron/ipc/handlers/plugin-install-handlers.ts#L196-L200)（同版/降级拒）。

**收口**：用户 Q2.4 历史精确串无用户会话不可逆推，但 UI 可显两条路均已实机闭环——(a) 下载腿失败 → bridge reject「请求超时」或 resolve「fetch failed」→ 均已归 network（非黑洞）；(b) 域消息（update.ts/install-handlers stage 校验段）→ unknown → 「未知错误」。F5 原「特定场景路径（降级后升回/文件占用）」假设：**非下载环失败若发生（如 commit 遇 Windows 文件占用 → 替换失败已复原）确走 (b) 黑洞**；机制本身未瘫痪，证伪 F5 悲观假设。

**顺带① Q2.3（装完旧版无横幅）—— 代码机制读出，全链活体验证待网**：可更新提示由启动发现（[updateDiscovery.ts:322-339](../../../../plugins/marketplace/src/services/updateDiscovery.ts#L322-L339)）驱动——市场池首载后 ~10s **一次性**（`_scheduled` 单发 guard）拉远端目录比版本 → 有新版推铃铛 + 落候选（#33b 徽标/升级入口）。三处结构性特征正好解释 Q2.3：① 单发不重跑——装完 v0.1.0（晚于发现时刻）后无任何「重跑发现」触发点，下趟提示要等重启/下次首载；② github 目录 fetch 静默失败（catch 空——后台不打扰）——网络抖一趟即无候选，**与证据② 同根**；③ 详情「可更新/更新」入口另走 DetailView 挂载时读当前本地版对目录——能反映新装态，但需重开详情才重算。全链活体验证（真装 0.1.0 → 观察横幅）被 github 断网阻塞，网络窗口内可补验；发现触发点缺口（装/卸后重跑）是否立案由用户点名定，71a 不重开任务。

**71b 喂料**：classify 扩词覆盖证据③ 全集即治 (b)；(a) 已归 network 无需扩；10s 桥遮蔽属结构性（update 改长任务通道），另立不入 71b。原文透传（title 悬停已有雏形）建议提升为失败 toast 可展开原始串，一并对治。

### F6 已装态「大小行开目录」 —— 挂起未做（交叉引用，不重复建任务）

用户 Q4。**已在案**：[E6-执行清单 📌 L3.5 对账挂起「已装态开目录 API」](../E6-执行清单.md)（源自 13 档案 B3 尾注 + mockup 04 已画 + [DetailView.tsx:777-778](../../../../plugins/marketplace/src/views/DetailView.tsx#L777-L778) 注释明言非本批）。用户记忆「说过但 UI 没体现」属实 = **挂起未做**，点亮即进批。术语校正：文档写的是「行值变**链接**『打开所在位置』」不是「变蓝」，意图一致（点击开资源管理器定位安装目录）。

### F7 壳保底详情页死 CSS（卫生）

**发现**：[PluginDetailPoolView.tsx](../../../../src/pool/views/plugin-detail/PluginDetailPoolView.tsx)（保底宿主）只渲染极简卡 + 启用/禁用/卸载钮，但其样式 [PluginDetailView.css](../../../../src/pool/views/plugin-detail/PluginDetailView.css)（~430 行）大量类未在组件使用（`.pd-btn-install` / `.pd-recommend-section` / `.pd-changelog` / `.pd-reverse-warn` / `.pd-icon-img` / `.pd-description` / `.pd-recommend-*` 等）——详情页搬去市场插件时的搬运残留。违反「无死代码」。

### F8 失败 toast「重试」依赖的命令注册在 #62b 后失去「始终在场」保证（真缺陷，用户 Q5）

**现象**：安装失败 → 右下角 error toast「安装失败，网络连接不可用」+ [重试]；点 toast [重试] **没反应**，而详情页 / 侧栏的同款 [重试] 有效。

**为什么行内有效**：详情页/侧栏重试 = 本地 React 直调 `retryMarketInstall`（[DetailView.tsx:937](../../../../plugins/marketplace/src/views/DetailView.tsx#L937) / [ExploreView.tsx:187](../../../../plugins/marketplace/src/views/ExploreView.tsx#L187)，经 [marketplaceShared.ts:44](../../../../plugins/marketplace/src/services/marketplaceShared.ts#L44) import）——不经命令注册表，模块函数直接跑，必然有效。

**为什么 toast [重试] 无效（全链读码）**：
1. toast [重试] ≠ 本地回调——是 **command action**：[settleInstallFailure](../../../../plugins/marketplace/src/services/marketplaceShared.ts#L546-L563) 构造 actions：`command: "marketplace.retryInstall"` + args `[{pluginId, downloadUrl}]`（自给自足）。pool 经 `notifications.show`（[preload-pool/namespaces-workspace.ts:35-42](../../../../electron/preload-pool/namespaces-workspace.ts#L35-L42)）→ IPC `showNotification` → 壳侧建 **onClick 闭包**：`executeCommand(a.command, undefined, ...a.args)`（[IpcBridgeHandler/ui.ts:230-238](../../../../src/core/services/plugins/IpcBridgeHandler/ui.ts#L230-L238)）→ 点击 = 壳 `executeCommand("marketplace.retryInstall", undefined, {pluginId,downloadUrl})`。
2. 壳注册表无此命令条目时 → [CommandRegistry.ts:263-266](../../../../src/core/registry/commands/CommandRegistry.ts#L263-L266) `console.warn("命令未注册") + return`——**静默空操作**（用户只看到「点了没反应」）。
3. **唯一注册源 = [index.tsx](../../../../plugins/marketplace/src/index.tsx#L70-L80) 模块顶层** `ensureMarketplaceCommands()`（:98）。该模块 = 市场「插件管理」**落地页入口**（entry `src/index.tsx`），只在池 import **主区落地 tab** 时才执行；而**真正发起安装/产生失败 toast 的视图**（DetailView/ExploreView/InstalledListView）是 plugin.json `contributes.views` 的独立 render 模块（各走 `renderPath` URL 动态 import，[PluginComponent.tsx:50-52](../../../../src/pool/shared/plugin-component/PluginComponent.tsx#L50-L52)），**直 import marketplaceShared、不 import index.tsx** → 视图装/败路径**不保证命令已注册**。注册只经 pool `commands:register` IPC 上壳（[preload-pool/commands.ts:75-80](../../../../electron/preload-pool/commands.ts#L75-L80)）→ 壳占位条目，同样依赖 index.tsx 先跑过。
4. **#62b 前的架构本无此缝**：壳 glob loader 启动时 import 所有插件入口 JS（双进程执行）→ marketplace.retryInstall 无条件在场。**E6#62a/62b（2026-09-08）退役源码 glob 轨、「壳不再 import 任何插件入口 JS」（[state.ts:57-63](../../../../src/pluginLoader/resolution/state.ts#L57-L63)）** →「始终在场」保证静默丢失，#30.9b toast 动作与 #62b 入口退役从未对账 = doc-code seam 回归。
5. **E6#62e on-command 激活救不了这类**：激活兜底（[commands.ts:43-62](../../../../electron/preload-pool/commands.ts#L43-L62)）只在**壳已有占位条目**时才把 executeRequest 转发到池并按需 import 属主 entry（[entryActivation.ts](../../../../src/pool/shared/plugin-component/entryActivation.ts)）。而 marketplace.retryInstall 是**纯运行时命令**（plugin.json `contributes` 无 commands 声明 → 壳无静态占位），占位条目恰恰要等 index.tsx 跑过才生成——**鸡生蛋**：没注册 → 壳连转发都不做。

**结论**：注册保证 =「本池进程里，市场落地页（插件管理 tab / MarketplaceView）曾开过一次」。用户本次会话直进详情/探索装 → 失败 → 点 toast [重试] → 注册缺失 → 静默空操作；行内直调不受影响。修法方向（71g）：把失败 toast 的落点从「命令派发」改成与行内同款本地直调 / 或把 marketplace 命令组改为**按插件激活即注册**（视图模块 import 副作用注册，随任一 market 视图 mount 执行）——详见 §三 71g，点名后 design-flow 细化。

### F9 toast 简略模式删除定案 —— 恒全显 + 完整版文字可鼠标拉取复制（用户点名 #3）

**现状（两态模式，读码）**：toast 有「简略/完整」两态。卡片本地 `expanded` state + chevron（[ToastHost.tsx:174-243](../../../../src/pool/floating/toast/ToastHost.tsx#L174-L243)）——平时只显示图标+单行 message+chevron，**details-row（来源行 + 动作按钮）CSS `display:none` 藏起**，仅 `.toast-expanded` 时 `flex`（[ToastHost.css:164-175](../../../../src/pool/floating/toast/ToastHost.css#L164-L175)）；双击卡片切展开态、chevron 仅含主按钮的 toast 才渲染、文字选中只在**简略版单行**好使（完整版展开后点选/拖选被双击 handler 干扰或整卡 select 约束）。

**用户点名 #3 原话（大白话）**：toast 我要**鼠标拉取复制**功能——原先只有简略版那个能拉取复制；现在我要求把简略版删了、只留完整版，且**完整版也要能鼠标拉取复制**。

**定案（2026-09-09 用户拍板，含 #3 复制方案 A）**：删两态——toast 恒全显（有来源/动作就一直显示 details-row，纯消息 toast 维持单行无空行）；删双击展开交互（**保文字选中不受双击干扰**）；message/source 文字 `user-select: text` 可拖选复制。**用户拍板 A：toast 提示文字允许自动换行、长文多行全显示**——被单行截断（现在 `.toast-message` 是 `white-space:nowrap + ellipsis`）挡住的那半截从此可见可拖选可复制（改 CSS 允许 wrap；卡片随内容变高是接受的）。**含义**：与 71g 修后「失败 toast 常显 [重试] 按钮」叠用——按钮无需展开即见 + 失败原文整段可复制（用户 #4 强调失败信息重要要能取走）。

### F10 下载进度条 —— 数字已有、真条缺渲染字段（用户点名 #2，用户拍板数字和条都显示）

**现状（全链读码）**：下载真网络段有真 percent——`downloadPackage`（[plugin-download.ts:81-83](../../../../electron/services/plugin-download.ts#L81-L83)）Content-Length>0 时 `onProgress(消息, Math.round(received/total*100))` → 主进程 `emitProgress("downloading",{message,percent})`（[plugin-install-handlers.ts:75-76](../../../../electron/ipc/handlers/plugin-install-handlers.ts#L75-L76)）→ 广播 `plugin:installProgress` → 市场 `ingestInstallProgress` 把 `percent` **存进会话**（[marketplaceShared.ts:482-503](../../../../plugins/marketplace/src/services/marketplaceShared.ts#L482-L503)）；但 `syncProgressToast`（:465-474）只 `handle.update(msg)`，把 percent **揉进文字**「正在安装 xx…62%」——**percent 从未离开市场插件**。validating/extracting/loading 段无 percent（→ 不定态动画）。

**渲染链缺字段（读码）**：池 toast 快照 DTO `PoolToastItem` 无 `progress/percent`（[poolToast.ts:19-27](../../../../src/core/types/pool/poolToast.ts#L19-L27)）；壳 `Toast` 无 progress 旗标、`updateToast(id,msg)` 不收 percent（[toast.ts:122-129](../../../../src/core/services/ui/toast.ts#L122-L129)）；`NotificationHandle.update(message)` 仅字符串（[namespaces-workspace.ts:46](../../../../electron/preload-pool/namespaces-workspace.ts#L46) + linkdesk-api/types.ts:263）。**加字段零通道改动**——壳→池快照走 `IPC.pool.toast`（bridges.ts:196 → toast-dialog.ts）整对象结构化克隆，DTO 加字段自动随包。

**用户点名 #2 + 拍板**：给下载/安装装**真进度条**；进度数字（「正在安装 X…62%」文字）**保留，条照加**（数字和条都显示）。

### F11 失败 toast 长驻 —— 手动关才消失 / 成功几秒自动关（用户点名 #4）

**现状（读码）**：toast 默认 `ttl=6000` 自动消失；error 类 8000（[toast.ts:49-53](../../../../src/core/services/ui/toast.ts#L49-L53) `TOAST_TTL_ERROR`）；progress 类 toast `ttl=0` 不自动消失（[IpcBridgeHandler/ui.ts:241](../../../../src/core/services/plugins/IpcBridgeHandler/ui.ts#L241)——进度条模式靠 finish/cancel 收）。市场 `settleInstallFailure` 失败 toast 用 error → 8 秒自动消失（[marketplaceShared.ts:541-565](../../../../plugins/marketplace/src/services/marketplaceShared.ts#L541-L565)）。

**用户点名 #4 原话（大白话）**：下载失败那些 toast（往往显示很多重要信息）要**长期驻留、除非我手动关掉**；下载成功则**几秒后自动关**（维持现状）。

**定案（2026-09-09 用户拍板「其余同意你的想法」）**：① 机制 = `notifications.show` 增可选 `persistent`（显式新选项 → 壳 `ttl:0` 不自动消失、等用户点 ×）——**不静默改 error 默认行为**；② 范围 = **下载/安装失败 toast（settleInstallFailure 产出的 error、带归因诊断）置 persistent 长驻**，其它 error 照旧 8 秒自动关；③ 成功 toast 保持数秒自动关（维持现状）；④ **堆积上限**：常驻类也设上限（防一直不点 × 越摞越多）——最多 N 条常驻、再来顶掉最老的（具体 N 点名后 design-flow 定，倾向 3~5）。

---

## 三、🎯 任务拆分（E6#71 系列，未开工 · 点名后按 design-flow 细化再锁定义）

> 与 [15 §四](15-详情页说明展示任务档案.md) 同款纪律：子任务行 = 未开工、点名后细化。**编号预占 E6#71a-g + toast 批 71h/71i/71j**（清单 71-73 未占用，若清单已占以清单为准）。Q4/F6 已在他处挂起，本系列**不重复**建任务，只交叉引用。**类属说明：71a-g = 本档案原市场健康审视范围（待点名）；71h/71i/71j = 2026-09-09 用户点名 toast 反馈链路三改（F9/F10/F11），用户话已指令、doc 先行落档——点名即开工，与 71a-g「待点名」不同轨。**

| ID | 任务 | 解决 | 前置 | 类属 |
|:--|:--|:--|:--|:--|
| **E6#71a** ✅2026-09-09 取证毕 | **实机取证**：复现「已装 v0.1.0 → 详情页更新到 v0.1.1 失败」抓引擎原始 error（CDP 9222 / protocol-debug.log）→ 归到 update.ts 具体行；顺带验 ① 装完已装态刷新时序（Q2.3）② 远端 v0.1.0 release 资产可达性 | ✅ 证据注已落 [F5.1](#f51-实机取证结果e671a2026-09-09--更新失败真因--下载腿-github-间歇断--10s-桥遮蔽--域消息归因黑洞)（下载腿 github 间歇断 + 10s 桥遮蔽 + 域消息黑洞，三条 UI 路径实机闭环）；顺带②验毕（当日 200、取证时 000 = 间歇非资产丢失）；顺带① 阻塞待网 | CDP + 真装 | 取证（前置） |
| **E6#71b** ✅2026-09-09 闭 | **更新失败归因补齐 + 原文透传**：更新域报错终结「未知错误」黑洞。**实现与原始描述有偏离（拍板记档）**：不做「classify 扩更新域词入安装类目」（会把「不在用户安装区」误标成「插件包损坏」——安装域五类词对更新域状态/策略错不成立）→ 改 `updateFailLabelKey` 补 conflict 分支（防御性）+ default 撤「未知错误」谎，新增纯函数 `updateFailText(t, reason, raw)`：归因可认 → 归因短语（t 译当前语言）；认不出（unknown）→ **直显引擎原文**（更新域报错本就是完整可读句，原文比归类诚实）；空原文兜底通用重试语。DetailView 三失败路径接线（缺面 :437 / resolved 归因 :453 / catch 原文透传 :457）。vitest +7 虚构 fixture（marketplaceUpdateFailure.test.ts）；marketplace 1.0.2→1.0.3 + changelog + 随包 zip 重建 | F1（见 [F1 补案](#f1-bua-71b)） | 71a（实喂原始串） | 缺陷修复 |
| **E6#71c** ✅2026-09-09 闭 | **安装确认弹窗归位壳 Dialog + 三确认归一**：mpd-confirm 改走壳 Dialog 容器（[10 档 §四·三](10-市场UI拥有权.md) 原设计）→ 居中 + 遮罩 + trap 齐活；装=壳 Dialog 富内容 / 卸+降级=壳 confirm 同一原语。**实现与原始描述有偏离（拍板记档）**：不扩 `dialog.confirm` 纯文字收富内容（不把排版收归壳模板）→ 改壳 DialogHost 补可选富内容槽 `content:{pluginId, renderPath, payload}` + 新 API `linkdesk.dialog.confirmContent`（向后兼容，MINOR 向）。**实修与补案见 [F2 补案](#f2-bua-71c)**：壳 DialogHost 富内容槽（content present → PluginComponent 挂载插件视图替代 title/message/默认按钮，机制不变）；市场 DetailView 自画 OverlayPortal mpd-confirm 整删 → confirmContent + ConfirmInstall 新视图（inert 声明，排版/按钮市场自画）；payload 不透明序列化载荷过壳→回池；viewId→renderPath 经 ViewContainerService 读 loader 附挂；解析失败回落纯文字；富内容模式 Enter 防双触发早退。vitest +16（installConfirmPayload / resolveDialogContentView / confirmContent ×3 / DialogHost content 模式）；marketplace 1.0.3→1.0.4 + changelog + 随包 zip 重建；软件 0.1.24→0.1.25 | F2（见 [F2 补案](#f2-bua-71c)） | — | 缺陷修复 + 归一 |
| **E6#71d** ✅2026-09-10 闭 | **安装入口行为归一**：侧栏「探索插件」行内「安装」对齐详情页确认流程。**拍板（2026-09-10 用户点名定）**：两处都先确认（选项 A）——不引入「列表快装」差异档。**实现与补案见 [F3 补案](#f3-bua-71d)**：ExploreView handleInstall 加详情页同款富内容确认（复用 71c confirmContent + ConfirmInstall 视图 + installConfirmPayload，零新壳能力/零新声明/零新 i18n），确认闸在既有三门禁后、startMarketInstall 前。marketplace 1.0.4→1.0.5 + changelog + 随包 zip 重建；软件零改动不 bump。实机 CDP 五证据闭（开卡居中+遮罩 / 取消不装 / 确认真装回 / Hello 字节级还原 / 备份清零） | F3（见 [F3 补案](#f3-bua-71d)） | — | 归一 |
| **E6#71e** | **官方目录条目补 readmeUrl**：hello-linkdesk 条目补 readmeUrl（预览态显完整 README，官方样板补示范）；改动在远端 encaron/linkdesk-marketplace 仓库 | F4 | — | 发布侧缺口 |
| **E6#71f** | **壳保底死 CSS 清理**：PluginDetailView.css 未用类删除（纯删死代码，零行为） | F7 | — | 卫生 |
| **E6#71g** | **失败 toast [重试] 修命**：点 toast [重试] 无反应（注册在 #62b 后失保，F8）。方向二选一（点名后 design-flow 细化）：A = toast action 改不依赖命令注册（settleInstallFailure 带本地重试口——行内同款本地直调；或壳 toast onClick 闭包直接派发到 pool 的既有安装会话，绕过注册表）；B = 命令组改「随任一 market 视图激活即注册」（视图模块顶层注册 / 命令声明进 plugin.json contributes.commands 使壳静态占位 + #62e 激活兜底可救） | F8 | — | 缺陷修复（doc-code seam） |
| **E6#71h** 🔴2026-09-09 点名 | **删 toast 简略模式 → 恒全显 + 完整版文字可鼠标拉取复制**（用户点名 #3）：池 ToastHost 去两态（expanded state/chevron/双击切换/`.toast-expanded`）→ details-row 有来源/动作即恒显、纯消息维持单行；删双击 handler（防干扰选中）。**复制方案已拍板 A**（2026-09-09）：message 允许自动换行、长文多行全显可复制——`.toast-message` 去 nowrap/ellipsis 改 wrap（原单行截断挡住的那半截从此可拖选可复制，卡片随内容变高接受）；message/source 文字 `user-select: text`。**与 71g 叠用**：失败 toast [重试] 常显 + 错误原文可复制 | F9 | 71g（同文件同卡） | 壳 UI 行为改（浮层 toast） |
| **E6#71i** 🔴2026-09-09 点名 | **下载/安装装真进度条**（用户点名 #2 + 拍板「数字和条都显示」）：percent 全链带真条——poolToast DTO/壳 Toast `progress+percent` → `updateToast/updateNotification/NotificationHandle.update` 带 percent → 池画条（定态 percent 0-100 宽 / 无 percent 不定态动画）；marketplace `syncProgressToast` 传 `s.percent`；进度文字保留照旧。CSS 先 ui-ux-pro-max（硬约束 16） | F10 | 71h（同卡渲染基础） | 壳+市场 数据通道扩 |
| **E6#71j** 🔴2026-09-09 点名 | **失败 toast 长驻到手动关 / 成功几秒自动关**（用户点名 #4）：下载失败那批 toast 往往带重要信息、要能取走 → **长驻到用户点 ×**；下载成功维持几秒自动关。**拍板（2026-09-09 用户同意）：① 机制 = `notifications.show` 增可选 `persistent` 旗标（→ 壳 `ttl:0`）——显式选项、默认自动消失，不静默改 error 默认行为；② 范围 = 下载/安装失败（settleInstallFailure 产出的 error 带归因诊断）置长驻，其它 error 照旧 8 秒自动关；③ 成功 toast 数秒自动关（维持现状）；④ 常驻类设上限（最多 3~5 条、再来顶掉最老）**。marketplace settleInstallFailure 置长驻 + 常驻上限实现，N 点名后 design-flow 定 | F11 | 71h（恒全显是长驻可读前提） | 壳 API + 市场 调用 |
| —（交叉引用） | F6 已装态开目录 API | 已在 📌 L3.5 挂起 | 用户点名 | 挂起中 |

---

## 四、⚠️ 待定点（开工前定，别默认）

1. **71c 壳 Dialog 富内容槽**：需先确认壳 Dialog 是否支持「自绘内容 + 壳容器」插槽（10 档 §四·三设想过）。若无 → 先补 Dialog 富内容槽（壳能力批，MINOR 向），再做 71c。
2. ~~**71d 二选一**~~：侧栏安装到底要不要确认？文档倾向「要」（mockup 02 摘要），但「探索列表点装 = 快速上手」也有产品理由。**✅ 已定（2026-09-10 用户拍板）**：**两处都先确认**（选项 A，F3 补案已记）——不引入「列表快装」差异档，mockup 02 本意完整落地。见 F3 补案 + 71d ✅ 行。
3. **71e 改动位置**：官方目录 = 独立远端仓库 encaron/linkdesk-marketplace，本仓只存证据；执行时改远端 + 相应 bump。
4. **71b 原文透传形态**：失败原文进 toast vs 行内 [重试] title 悬停（与安装 M4 三同款）——照既有「行内归因 + 原文 title」定案，不另造通道。
5. **71a 取证需真装**：复现路径涉及真下载/真引擎，属实机门禁类（memory `task-list-first-real-machine-gate`），不纯单测可代。
6. ~~**71j 持久语义二选一**：`notifications.show` 加可选 `persistent` 旗标（显式新选项，默认可自动消失）vs 单开 progress 的 `ttl:0` 语义档（error+诊断默认长驻）。倾向显式 `persistent`——「失败长驻」只对失败 toast 开、不让所有 error 自动变常驻，避免静默改 error 默认行为。~~ **✅ 已定（2026-09-09 用户同意）**：显式 `persistent` 旗标方向（不静默改 error 默认）；范围 = 下载/安装失败带诊断长驻；常驻上限 3~5 条顶最老（N 点名后 design-flow 定）。见 F11 + 71j 行。
7. **71h 纯消息 toast 维持单行**（⚠️ 实现守卫，非待用户拍板——F9/71h 行已定）：删两态后，纯消息（无来源无动作）toast 不应多出空详情行——details-row 渲染条件 = 有 sourceText 或 actions 才渲染。实现时别把「恒全显」做成「恒有空行」。

---

## 五、状态

- 2026-09-09 **立案**：用户连环实机观察 Q1-Q4 + AI 静态审计（F1-F7）+ 远端目录实测（F4 铁证）。执行清单 L3.5 注册「第 3.5.5 轮 · 市场区健康审视（16 档案）」指针行，E6#71a-f **未勾待点名**。
- 2026-09-09 **补案（Q5 → F8 → 71g）**：用户新报「安装失败 toast 点重试没反应，只有详情/侧栏有效」——读码全链定位 = 命令注册源缺口（E6#62a/62b 撤壳入口 import 后 #30.9b 命令失去始终在场保证）。16 档案 + F8 + E6#71g 已补，**未勾待点名**。
- 2026-09-09 **71a 取证毕（用户点名开工）**：证据注落 [F5.1](#f51-实机取证结果e671a2026-09-09--更新失败真因--下载腿-github-间歇断--10s-桥遮蔽--域消息归因黑洞)——三截证据实机闭环（下载腿 19.8s fetch ECONNRESET + 10s 桥遮蔽 + 域消息 unknown 喂出实串）。**71a 勾、71b-f 未勾待点名**。
- 2026-09-09 **补案②（toast 反馈链路三改，用户点名 71h/71i/71j）**：用户在 71g 追查现场点名 toast 本身三处行为改——删简略模式恒全显+完整版可复制（#3）/ 下载装真进度条（#2，数字和条都显示）/ 失败 toast 长驻手动关+成功几秒自动关（#4）。F9/F10/F11 + 71h/71i/71j 已落档，**用户点名即开工**（doc 先行落地，实现待排批）。
- 2026-09-09 **71b 闭（用户点名开工）**：更新失败归因 + 原文直显（见 §三 71b ✅ 行 + F1 补案）——归因可认走归因短语、unknown 直显引擎原文，终结「未知错误」黑洞；`updateFailText` 纯函数 + DetailView 三失败路径接线；marketplace 1.0.2→1.0.3 + changelog + 随包 zip 重建；vitest +7；check EXIT=0（129 文件/1699 测试）。**71a/b 勾、71c-g 待点名**。
- 2026-09-09 **71c 闭（用户点名开工）**：安装确认归位壳 Dialog + 三确认归一（见 §三 71c ✅ 行 + F2 补案）——壳 DialogHost 富内容槽 + `confirmContent` 新 API（软件 0.1.24→0.1.25，向后兼容）+ 市场 DetailView OverlayPortal 自画整删 → ConfirmInstall 新视图（marketplace 1.0.3→1.0.4 + 随包 zip 重建）；vitest +16；check EXIT=0（132 文件/1730 测试）。**71a/b/c 勾、71d-g 待点名**。
- 2026-09-10 **71d 闭（用户点名 d-g 全做 + 拍板「两处都先确认」）**：安装入口行为归一（见 §三 71d ✅ 行 + F3 补案）——侧栏「探索插件」行内安装对齐详情页富确认，`ExploreView handleInstall` 加 71c 同款 confirmContent 闸（复用现成机制，零新壳能力/零新声明/零新 i18n）；marketplace 1.0.4→1.0.5 + changelog + 随包 zip 重建；软件零改动（0.1.25 不 bump）；check EXIT=0（132 文件/1730 测试）。实机 CDP 五证据闭（开卡居中+遮罩 / 取消不装 / 确认真装回 / Hello 字节级还原 / 备份清零），用户「信 CDP 直接提交」。**71a/b/c/d 勾、71e/f/g 待点名**。
- 待办：71a/b/c/d **已勾**（用户 2026-09-10 已点名 71d-g 全批 + toast 批 71h-j，d 先排已闭）；71e/f/g 用户点名即按 design-flow 细化排批（AI 建议序：71g+71h/i/j toast 反馈批 → 71f 顺手 → 71e 远端）。Q4/F6 点亮即进批（已在他处挂起）。
