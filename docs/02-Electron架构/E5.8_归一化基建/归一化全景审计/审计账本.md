# 归一化全景审计——审计账本

> E5.8#6.6（2026-08-19 Phase 1.5 执行）。对照 AI 友好三层表格（memory `ai-friendliness-three-layers`）的「唯一方式」逐项 grep 实证。
> 原则：**不当场修复就带理由豁免**（不设基线放过，同 Phase 1 纪律）。所有豁免必须落账本，禁用「当前够了」话术。
> 机械哨兵落地后，本账本 = ESLint 硬约束 1（hex）/2（中文）的豁免白名单凭据。

---

## 审计五类总览

| # | 类别 | 唯一方式 | 实证手段 | 检出数 | 处置 |
|:-:|------|---------|---------|:--:|:--:|
| 1 | 颜色 | `var(--xxx)` CSS 变量 | grep hex（tsx/ts + css，排除 themes） | TS/TSX 16 + CSS 4 | 4 CSS 修复 + 16 豁免 |
| 2 | 文字 | `t()` i18n | 扩 no-hardcoded-chinese 覆盖 src/** 后实跑 | 中文 3 JSX + 3 盲区 + 4 QuickPick placeholder + 1 池兜底 + 1 serialize | 全修复 |
| 3 | 插件能力 | plugin.json 声明字段 | grep 插件 ID 字面量硬编码 / 能力分支 | 0 | 干净 |
| 4 | 跨插件通信 | Registry / 后门 IPC | grep 插件互 import / 私有 channel | 0 | 干净 |
| 5 | 资产路径 | `getAssetPath()` | grep `/assets/` `/plugins/` 绝对路径字面量 | 1 | 豁免（dev fixture） |

**基线修正记录（2026-08-19 Phase 1 验证实证）**：`no-hardcoded-chinese` 原作用域仅 `plugins/**`（eslint.config.js 158-180 块 `files` 限定）——`src/**` 中文从未被查。**先扩规则覆盖 src/** 再判卷**，基线「中文 0」不成立。同理 hex 基线 294（tsx/ts 186 + CSS 108，排除 themes）是「排除 themes 后仍含数据类」的毛计数——判卷逐项豁免数据类后归零。

---

## ① 颜色——`var(--xxx)`

### 真违规：4 处 CSS 硬编码 hex → 已修复（全改 var()）

| 文件 | 原值 | 修复 | 位置 |
|------|------|------|------|
| `src/components/shared/number-input/NumberInput.css` | `color: #fff` | `color: var(--text-on-accent)` | `.number-input-btn:active` |
| `src/components/views/settings/SettingsView.css` | `color: #fff` | `color: var(--text-on-accent)` | `.settings-action-btn` |
| `plugins/builtin/marketplace/src/styles/MarketplaceSidebar.css` | `color: #4caf50` | `color: var(--status-connected, #4caf50)` | `.ms-item-enable-btn` |
| `plugins/builtin/marketplace/src/styles/MarketplaceView.css` | `color: #fff` | `color: var(--text-on-accent, #fff)` | `.marketplace-badge-core` |

> `var(--x, #hex)` 回退值是合规形态（var() 主值 + hex 兜底）——审计不视为违规，no-hardcoded-hex 规则也豁免 `var(` 串。

### 豁免：TS/TSX 16 处（数据类，非样式硬编码）——带理由

| 文件 | 值 | 豁免类别 |
|------|-----|---------|
| `electron/main.ts:71/284` + `electron/windows/window-manager.ts:147`（5 处） | `#1e1e1e` / `#f5f5f5` | **OS 层窗口背景色**——BrowserWindow/WebContentsView 初始背景防启动白屏，渲染进程 CSS 变量不可达（主进程无 DOM）。inline eslint-disable 带理由 |
| `src/App/startup.ts:111` + `src/core/services/ui/ThemeEngine.ts:196`（2 处） | `#0078d4` | **配置默认值数据**——`app.accentColor` 配置项默认值 + 读取兜底，用户可改，非样式硬编码。inline eslint-disable 带理由 |
| `src/components/views/settings/SettingsView/SettingRow.tsx`（6 处） | 取色器预设 6 色 | **取色器预设色板**——「颜色即数据」（用户可选值），提为模块级常量 `COLOR_PICKER_PRESETS` + inline eslint-disable 带理由 |
| `plugins/user/serial-monitor/src/hooks/useSerialSessions.ts`（6 处） | SESSION_COLORS 6 色 | **会话标签色板**——「颜色即数据」（多会话轮换标签色），inline eslint-disable 带理由 |
| `src/components/shared/color-picker/ColorPicker.tsx`（2 处） | `#f00` / `#0078d4` | **取色器组件**——路径白名单豁免（`/color-picker/`） |

> **豁免类别总纲**：主题定义文件 / i18n 资源 / 取色器组件与色板 / Canvas 绘图 / 测试 fixture / dev preview——「颜色即数据」与「OS 层不可达」两类。其余硬编码 hex = 违规。

---

## ② 文字——`t()`

### 扩规则覆盖 src/**（盲区修复）

1. **eslint.config.js**：新增 `src/**` 块 `no-hardcoded-chinese`（warn→error）；**plugins/** 块覆盖保留**（原被替换导致 plugins 裸奔——已恢复）。
2. **规则盲区修复**（eslint-local-rules.js）：原只查 `JSXAttribute` / `JSXExpressionContainer` 直接父级——`placeholder={cond ? "选择目录…" : "选择文件…"}` 的 Literal 父级是 `ConditionalExpression`，漏报。新增 `isInJsxContext()` 向上穿透 Conditional/LogicalExpression 链。

### 真违规：全部修复（11 处）

| 文件 | 原值 | 修复 |
|------|------|------|
| `src/components/shared/number-input/NumberInput.tsx` | `aria-label="减少"/"增加"` | `aria-label={t("减少")}/{t("增加")}` |
| `src/components/shared/font-family-select/FontFamilySelect.tsx` | `placeholder="选择等宽字体…"` | `placeholder={t("选择等宽字体…")}` |
| `src/components/shared/file-path-input/FilePathInput.tsx`（3 处，规则盲区） | `filters: [{ name: "所有文件" }]` + `placeholder` + `title` 三元 | 全部 `t()` 包裹 |
| `src/core/commands/palette/commandPalette.ts:21` | `placeholder: "输入命令…"` | `placeholder: i18n.t("输入命令…")` |
| `src/components/shared/language-picker/LanguagePicker.tsx:26` | `placeholder: "选择语言…"` | `placeholder: i18n.t("选择语言…")` |
| `src/components/shared/theme-browser/ThemeBrowser.tsx:48` | `placeholder: "选择颜色主题…"` | `placeholder: i18n.t("选择颜色主题…")` |
| `src/core/commands/shell/developerCommands.ts:39` | `placeholder: "选择 WebView…"` | `placeholder: i18n.t("选择 WebView…")` |
| `src/pool/views/shell-renderer/ShellViewRenderer.tsx:58` | `{shellType ? "未知壳视图: …" : "壳视图"}` | `t()` 包裹 |

### 豁免（判卷定案）

| 项 | 判定 |
|----|------|
| 命令 title/category/label 定义处裸中文 | **i18n key**——定义处裸中文 = key，消费处 `t(contrib.title)` 解析（SettingsView.tsx:66 实证）。合规 |
| QuickPickService.show 的 serialize 显示文本 | **显示文本铁律**——壳侧 `i18n.t()` 解析后推池，池原样渲染（QuickPickHost.tsx:6 注释实证）。label/category/detail 已 t() 化；本次补齐 developerCommands serialize 内联文本 |
| `src/pool/dev/sampleLayout.ts` | **dev fixture**——头注释文档化：「扮演壳 t() 解析后数据形态」，非硬编码违规 |
| `src/pool/floating/quick-pick/QuickPickHost.test.tsx` placeholder | **测试 fixture**——测试数据 |
| quickpick.show 插件命令 serialize `item.label` | **插件数据**——壳不译插件内容（Content vs Space Ownership），插件作者自持文本 |

---

## ③ 插件能力——plugin.json 声明

grep `pluginId ===` / `switch(pluginId)` / `BOTTOM_ICONS` / `PLUGIN_ICON_PATH` 等字面量硬编码模式：**0 残留**（E5.7#95 no-plugin-id-hardcode 规则 + 声明式 registry 消费已闭环）。

---

## ④ 跨插件通信——Registry / 后门 IPC

- 插件互 import：**0**（E5#116 no-core-import-in-plugin + no-restricted-imports 双防线）。
- 私有 channel 直连：**0**（E5.8#6.5 ipc-audit 哨兵——数据推流 42 文件零裸 `webContents.send`，serial/lsp/file 归一化走 `IpcBridge.broadcast`）。

---

## ⑤ 资产路径——`getAssetPath()`

| 文件 | 值 | 判定 |
|------|-----|------|
| `src/pool/dev/sampleLayout.ts:57` | `logoUrl: "/assets/logo.svg"` | **dev fixture 豁免**——头注释文档化（扮演壳 getAssetPath 解析后数据形态） |

其余全走 `getAssetPath()`（hardcoded-path-constraint）。

---

## 机械哨兵（本任务落地）

1. **`linkdesk/no-hardcoded-hex`**（eslint-local-rules.js 新增，error 级）：拦截字符串字面量 `#hex`（3/4/6/8 位）。
   - 豁免（规则内建 path 白名单）：`/themes?/`、`/i18n/`、`/color-picker/`、`canvas`、`.test/.spec`。
   - 豁免（语法层）：`var(--x, #hex)` 回退串（合规形态）。
   - 豁免（inline eslint-disable 带理由）：OS 层背景色 / 配置默认值 / 取色器预设色板 / 会话标签色板——本账本凭据。
2. **`no-hardcoded-chinese` warn→error**（src + plugins 双块）：规则盲区已修（三元/逻辑链穿透）。

### 哨兵自检

```
npx eslint src/ plugins/ electron/ --max-warnings 0   # 0 错误
npm run check                                          # 全链绿（含 ipc-audit）
```

---

## 验收对照

- ✅ grep 实证全零或全豁免（本账本）
- ✅ 审计账本与 ESLint 硬约束 1/2 对照闭环（hex/中文两哨兵即机械凭据）
- ✅ 新哨兵入 check 且全绿（#6.6 执行注 + 门禁全绿记录）
