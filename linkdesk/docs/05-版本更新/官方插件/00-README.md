# 官方插件 — Marketplace 可选

> **类型：市场插件** | 2026-07-24 → 2026-07-28 重组。**不改壳，不绑版本号。** v1.0 出了之后，哪个做好了就上架哪个。
> 主题制作器（原 02）已迁移到 [04-出厂制造 #07 Theme Maker](../../04-出厂制造/07-Theme-Maker-主题制作器/)。
> 工作台与 OLED 已拆分——工作台废弃（被 01-卡片工作台 详细蓝图替代），OLED 独立。

---

## 定位

| | |
|---|---|
| 发布方式 | **Marketplace 独立发布**——不是壳版本的一部分 |
| 安装 | 用户在 Marketplace 搜索 → 点安装 → 图标栏出现 |
| 卸载 | 跟普通插件一样——卸载后消失，壳不动 |
| 前提 | v1.0（插件基础设施全部就绪） |

---

## 插件列表

### 01 — 卡片工作台

> **类型：市场插件** | 无限画布仪表盘——10 种卡片 + 网格布局 + 实时数据流。

`react-grid-layout` 拖拽式布局。内置 10 种卡片类型。第三方可注册新卡片类型。

**文档：** [01-卡片工作台/](01-卡片工作台/00-README.md)（7 份完整蓝图 + HTML 预览）

### 02 — OLED 模拟器

> **类型：市场插件** | Canvas 逐像素渲染 128×64 OLED 屏幕。

I2C 数据 → 主进程 → Canvas `requestAnimationFrame` → 逐像素渲染。照抄 E1 串口服务模式。

**文档：** [02-OLED/](02-OLED/00-README.md)

### 03 — AI 与智能体

> **类型：市场插件** | 内嵌 AI 对话 + 代码补全 + 自主执行任务。对标 VS Code Copilot Chat。

侧栏对话界面 + Monaco 内联建议。支持 Anthropic / OpenAI / DeepSeek / 本地模型。

**文档：** [03-AI与智能体/](03-AI与智能体/01-AI与智能体.md)

---

## 🆕 远期规划——更多市场插件

> 以下全是 **市场插件**——不改壳一行代码。LinkDesk 是通用容器，核心不知道插件的用途。串口监视器只是恰好第一个插件——下面这些方向同样合法。

### 04 — 🗺️ 地图插件

> **类型：市场插件** | Leaflet + GPS 轨迹 + 热力图 + GPX 导入。

嵌入式场景——无人机/机器人/车载——GPS 数据第一展示就是地图。串口收 NMEA 句子 → 解析经纬度 → 地图上实时画点。历史轨迹回放 + 热力图（哪里停留最久）。

**为什么是 LinkDesk：** Google Earth 不是开发工具。嵌入式工程师需要一个轻量、可接串口数据的地图。

**技术：** Leaflet（开源，零依赖）+ GPX 解析 + 串口数据管道消费。

### 05 — 逻辑分析仪

> **类型：市场插件** | 数字信号时序图——8/16 通道。

嵌入式调试标配。Saleae Logic 的软件层——硬件是用户自己的（Cypress FX3 / USB 逻辑分析仪 ~$10）。不做硬件，只做渲染。

```
┌─ 逻辑分析仪 ──────────────────────────────────┐
│ CH0: ──┐   ┌──┐   ┌──────┐   ┌──┐           │
│ CH1:   └───┘  └───┘      └───┘  └──────     │
│ CH2: ──────┐     ┌─┐  ┌─┐  ┌────────────    │
│            0     1   2   3   4   (ms)        │
└───────────────────────────────────────────────┘
```

**技术：** Canvas 2D 逐行渲染 + 信号协议解码（I2C/SPI/UART 标注在波形上方）。

### 06 — 协议调试器

> **类型：市场插件** | MQTT / Modbus / CAN 总线——三大 IoT 协议。

串口只能看字节流。协议调试器能解析帧结构——显示 Topic/Payload（MQTT）、寄存器地址/值（Modbus）、CAN ID/数据（CAN）。不只是"显示"——能**构造并发送**协议帧。

```
┌─ MQTT 调试器 ─────────────────────────────────┐
│ Broker: [192.168.1.100:1883    ] [连接]       │
│                                                │
│ 订阅: [device/+/temperature      ]            │
│                                                │
│ ── 收到 ──────────────────────────────────── │
│ 14:32:05  device/1/temperature  26.3°C        │
│ 14:32:06  device/2/temperature  31.8°C        │
│ ── 发送 ──────────────────────────────────── │
│ [device/1/led] [ON]               [发送]      │
└────────────────────────────────────────────────┘
```

### 07 — 固件烧录工具

> **类型：市场插件** | 对接 openocd / jlink / st-flash——消费壳的烧录服务（主进程 spawn 外部工具）。

嵌入式开发者装 CubeIDE / Keil 只为烧录。LinkDesk 里有终端 + 串口——就差烧录。选固件文件 → 选烧录器 → 点烧录 → 终端看进度 → 串口监视器看输出。全在同一个软件里。

```
固件: [firmware.hex           ] [浏览]
烧录器: [STM32F407 (ST-Link)   ▾]
[烧录] [校验] [擦除]

输出:
> openocd -f stlink.cfg -f stm32f4x.cfg
> Programming... 100%
> Verified OK
> 用时: 3.2s
```

### 08 — 3D 模型查看器

> **类型：市场插件** | Three.js + STL/OBJ/STEP 加载。

3D 打印 + 机械设计 + PCB 外壳。不是所有人需要——但需要的人会因为这个装 LinkDesk。拖入 STL → 旋转/缩放/剖切 → 截图分享。

---

## 🎨 创作类

### 09 — 像素画编辑器

> **类型：市场插件** | 调色板 + 图层 + 导出 spritesheet。对标 Aseprite。

游戏开发者——跟硬件调试是同一批人。Canvas 逐像素渲染——跟 OLED 模拟器同样的技术栈。

### 10 — SVG 矢量绘图

> **类型：市场插件** | 路径编辑 + 图标设计。对标 Figma 的轻量替代。

做插件图标、UI 素材——不需要 Figma 全量功能，够用就好。

### 11 — 音频可视化

> **类型：市场插件** | 麦克风/音频文件 → 频谱/波形图 FFT 渲染。

音乐制作人——跟嵌入式完全无关。但 FFT 渲染可以当作卡片工作台的一张卡片用——数据源是音频，不是串口。

---

## 📝 写作与效率

### 12 — Markdown 笔记本

> **类型：市场插件** | 侧栏文件树 + Monaco 编辑器 + 实时预览。

对标 Obsidian——但 LinkDesk 里它只是一个插件。笔记 + 串口 + 地图可以同时开。Monaco 已就绪——编辑器 + 预览 = 两个组件拼一下。

### 13 — 思维导图

> **类型：市场插件** | 无限画布 + 节点拖拽 + 连线。

脑暴工具。跟卡片工作台同一个画布引擎——LinkDesk 天然适合这种"在画布上摆东西"的插件。

### 14 — 看板

> **类型：市场插件** | 拖拽卡片 + 列管理。对标 Trello。

项目管理。轻量插件即可——react-beautiful-dnd 几行代码的事。

---

## 🔧 开发者工具

### 15 — API 测试器

> **类型：市场插件** | REST/GraphQL 请求构建 + 响应查看。对标 Postman。

LinkDesk 有 Monaco 编辑器——JSON 高亮天然就绪。有分屏——左边 API 请求，右边串口数据，开发调试全在一个窗口。

### 16 — 数据库浏览器

> **类型：市场插件** | SQLite/MySQL 表浏览 + SQL 查询。对标 DBeaver。

装 DBeaver 只为看一眼 SQLite——LinkDesk 里是个插件就够了。Monaco 写 SQL + 结果表格渲染。

### 17 — 正则表达式测试器

> **类型：市场插件** | 输入文本 + 正则 → 高亮匹配结果。

每个开发者偶尔用到——Monaco 的高亮能力直接复用。~80 行的小插件。

---

## 📊 数据与科学

### 18 — CSV 数据表

> **类型：市场插件** | 表格视图 + 过滤 + 排序 + 导出图表。

数据科学家——csv 拖进来就能看、就能画折线图。串口数据也可以导出为 csv 再在这里分析。

### 19 — 数学笔记本

> **类型：市场插件** | LaTeX 公式渲染 + 计算 + 2D/3D 绘图。对标 Jupyter 轻量版。

学生/研究者。Monaco 写公式 + MathJax 渲染 + Canvas 绘图。

---

## 🎮 娱乐与生活

### 20 — 电子书阅读器

> **类型：市场插件** | EPUB/PDF 解析 + 书架 + 阅读进度。对标 Kindle/Apple Books。

程序员看技术书——跟编辑器在同一个软件里。epub.js 渲染，零依赖。

### 21 — 音乐播放器

> **类型：市场插件** | 本地音频文件 + 播放列表 + 波形显示。

一边调代码一边听歌——不切窗口。HTML5 Audio API 原生支持。

---

## 🛠 实用小工具

### 22 — 剪贴板管理器

> **类型：市场插件** | 历史记录 + 搜索 + 固定常用项。对标 Maccy/Ditto。

开发者高频工具。Electron 有 `clipboard` API——读剪贴板历史（只记录用户主动复制到管理器中的内容）。

### 23 — 二维码生成/识别

> **类型：市场插件** | 文本 → QR / 截图 → 解码。

~50 行小插件。但每个人都偶尔需要。qrcode.js 零依赖。

### 24 — 颜色调色板生成器

> **类型：市场插件** | 从图片提取主色调 / 色轮选色 / 渐变生成。

设计师 + 主题制作者——跟 Theme Maker（04-出厂制造 #07）互补。

---

## 为什么独立发布

| 内置（壳版本） | 官方插件（Marketplace） |
|------|------|
| 所有人大概率需要 | 特定人群需要 |
| 随壳更新 | 独立版本号 |
| 例：Git、终端、菜单 | 例：卡片工作台、OLED、AI |
| 例：项目管理器、自动更新 | 例：地图、Markdown、API 测试、像素画 |

---

> **← 05 索引：** `../00-README.md`
> **← 另一路：** `../壳版本/00-README.md`
