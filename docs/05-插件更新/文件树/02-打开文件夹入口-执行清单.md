# 文件树补丁执行清单——打开文件夹侧栏入口

> 2026-09-13 立案。归属：[`docs/05-插件更新/文件树/`](./00-README.md)——文件树专项补丁清单。
> **进度真相源口径：** 本清单是**文件树补丁**的唯一真相源；E6 主线进度真相源仍是 [E6-执行清单.md](../../02-Electron架构/E6_插件生态与发布/E6-执行清单.md)——两清单互不吞并。⚠️ **开工前置项：** E6 清单头部目前**没有**指向本清单的指针（2026-09-13 核实），FT#1 开工时需在 E6 清单头部挂一行指针，否则读 E6 的新 AI 不知道文件树在工作。
> **前缀定案：`FT#`**（File Tree）。任务号 `FT#1`、`FT#2`…，前缀标识**清单血缘**而非 phase——E5.7/E5.8 各有 Phase 1 但前缀互异，同理本清单 Phase 1 不会与任何其他清单的 Phase 1 混淆（用户授权 AI 定，见设计文档 §四.2）。
> **设计依据：** [01-打开文件夹入口-设计.md](./01-打开文件夹入口-设计.md)（8 维度）+ [mockups/01-打开文件夹入口-竞标.html](./mockups/01-打开文件夹入口-竞标.html)（A ✅ 定稿 / B·C 已淘汰）。
> **开工前置：** ~~竞标拍板~~ ✅ 已完成（2026-09-13 A 版定稿，见 Phase 0）。下一步 = FT#1 开工信号（§0.3：设计批准 ≠ 实现授权）。

---

## Phase 0（前置）：竞标拍板 ✅

> **✅ 2026-09-13 用户拍板：A 定稿**（常驻 section：按钮 + 最近 5 条）。拍板理由（C 形态 E4V#44 死锁史）与 B/C 淘汰去向 → [01-设计.md §二/§二·五](./01-打开文件夹入口-设计.md)。FT#1 起按 A 版执行。

## Phase 1（FT#1–FT#4）：实现

- [ ] **FT#1** OpenFolderView 组件骨架 + 侧栏 section 注册
  - 落点：`plugins/file-tree/src/views/OpenFolderView/`（feature-folder：门面 + css）；`plugin.json` `contributes.views.explorer` 增 `order: 2` 视图（静态声明 + useViewRegistration 动态注册照 FOLDERS/SEARCH 先例）；`i18n/en.json` 补键；命令接线复用 `explorer.openFolder`（不新增 handler）。
  - 🔴 红线（bug-atlas §D3 / E4V#44 死锁教训，见设计文档 §二·五）：`onDidChangeFolders` 等订阅只挂**永不卸载**的常驻组件；空态/有根态只做组件内部条件渲染，禁止外部替换整个组件。
  - 验收：`npm run check` 全绿；侧栏搜索下方出现常驻 section，点击按钮弹系统目录框，选中后进文件树。
- [ ] **FT#2** 最近文件夹列表
  - 落点：OpenFolderView 内最近子件——`pluginState.get("app","recentFolders")` 只读 + `onDidChangeFolders` 刷新，≤5 条，点击 → `workspace.addFolder(path)`；素材参考孤儿 `WelcomeView.tsx` 的既有实现（见 FT#3）。
  - 验收：从欢迎页/侧栏打开过的目录出现在侧栏最近列表，点击可加根。
- [ ] **FT#3** WelcomeView 孤儿整删 + FoldersView 空态提示
  - `plugins/file-tree/src/views/WelcomeView.tsx`（119 行，自 2026-08-03 `231257d16` 删 registerViewEmptyContent 后无人 import = 死代码）：其最近列表素材折进 FT#2 后**当场整删**（§5.3 死代码不留）；顺手给 FoldersView 空态（`roots.length===0`）补一行 muted 占位文案（C 版精华，消灭空白反模式，不带按钮——按钮职责归常驻 section）。
  - 验收：grep 全仓 `WelcomeView` 引用归零；空态有一行 muted 文案，无按钮无死代码。
- [ ] **FT#4** 视觉走查 + 门禁 + 插件版本 bump + bundled 重打包
  - 落点：亮/暗主题 × 空态/有根态 × 216px 窄栏矩阵走查（token 全 `var(--xxx)`，零硬编码 hex）；`npm run check`；`plugin.json` version 1.0.6 → 1.0.7 + `plugins/file-tree/CHANGELOG.md` 补段。
  - 🔴 **bundled 发货链（file-tree 是 `distribution: builtin` 随包插件——2026-09-13 重组后「分类」退役但发货链不变）**：改源码必须 ① bump `plugin.json` version——否则 `check-bundled-version-bump` **红拦**（同版本 + 内容指纹变 = 红）；② 重打插件包并把 `plugins/file-tree/file-tree.linkdesk-plugin` 同步进 `bundled-plugins/`（当前 zip = 341,200 字节 / version 1.0.6；**具体打包命令开工时按 `scripts/` 链核实后跑，禁猜**）。
  - 验收：走查矩阵零破格，check 全绿（含 bundled gate），`bundled-plugins/file-tree.linkdesk-plugin` 内 `plugin.json` version = 1.0.7。

## Phase 2（FT#5）：验收收口

- [ ] **FT#5** CDP/实机验收 + 邻近区域回归 + 回勾
  - 主链路（用户原痛点）：关闭欢迎页 → 清空文件树目录 → **全软件仍有一处 UI 能打开文件夹**（侧栏入口）→ 目录进树 → 欢迎页最近列表同步。
  - 邻近回归：欢迎页按钮/最近列表写入、FOLDERS 动态标题、搜索 section、命令面板 `explorer.openFolder`、主题切换两插件变色不受扰。
  - 🔴 **实机判据必须在 fresh userData 或 `--force-rematerialize-bundled` 下跑**——boot 只补缺失、**永不刷新已装**（同版异版都不覆盖，memory `version-and-release` §3.1 四实证）：在同一份旧 userData 上验「补丁生效」会得到假阴性。同理，**已有安装的用户拿到本补丁的路径** = 全新安装 / 清空插件目录 / 未来 bundled 上架市场按版本更新（真欠账 **E6#26b**）——本行如实记账，不写成「发版即达人」。
  - 验收：实机截图/读数折进本行 ✅；commit 前按 CLAUDE.md 自检（feat 前缀 + 版本号同笔 CHANGELOG）。

---

> **执行纪律照旧：** 一次一个任务、每任务一 commit、`npm run check` 全绿才提交、实机验收后才回勾、攒批更新清单（Encaron 通知清理时统一打勾）。
