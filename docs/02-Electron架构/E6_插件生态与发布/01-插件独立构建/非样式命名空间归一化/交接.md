# 非样式命名空间归一化——AI 接力交接

> **用途（沿用 CSS 系列 2026-09-15 用户拍板）：一个 AI 会话只执行一个任务。** 每个 AI 做完自己那一棒，就在本文件**顶部**追加一段「给下一个 AI 的话」；用户把那段话**整段复制**给下一个 AI 当进场上下文即可。
> **段结构（固定四点）：** ⑴ 本轴是干什么的 → ⑵ 各轮次是干什么的 → ⑶ 重点介绍下一个 AI 即将执行的那一棒 → ⑷ 提醒：同笔续写 [E6-执行清单.md 第 1 层头的轮次进度行](../../E6-执行清单.md)。
> **规矩：只追加不改旧段**（最新在顶）；旧段是被执行轮次的历史交接，留着对账。
> **🔴 段内必带「剩余任务队列」（2026-09-15 用户加的要求）：** 用户**中途不看清单**，全靠 AI 之间一段接一段传递 ⇒ 每段的交接里**必须带上队列现状**（把自己那一格改成 `✅`），**只写「下一棒做什么」而不交代后面还剩什么 = 队列会断、系列烂尾**。
> **🔴 段内必带「单格交付说明」**（2026-09-16 第五棒补，见 CSS 系列交接）：**派工单的格数是上限不是配额**；用户会把上一棒的**整段**消息复制给下一个 AI（**不裁剪**）⇒ 交接段**开头就要写「你是谁 / 你这一棒是什么」**，否则接棒的 AI 会把自己读成复核者或派工者。
>
> **非新能力声明（`check-design-flow` §8.4 ③）：** 本档是**跨轮交接说明与执行实录**，不含新能力设计——文中出现的命令 id、设置键、context key、注册表函数名、门禁脚本名均为**引用既有实现**，本档只是把它们**记下来 / 验一遍**。⇒ **无需新能力设计前置**。
>
> **权威方案**：[00-整理档案.md](00-整理档案.md)（真根因 · 全量读数 · 收口定义 · 残余边界 · 原则对照 · **轮次总表**）——🔴 **其中 [§〇c 口径修订令](00-整理档案.md) 压过本档其余一切结论**。
> **接力总览（20 轮）**：**件 1 命令 id**（1.31＋1.32）／**件 2 设置键**（1.33＋1.34）／**件 3 外观族 id**（1.35＋1.36）／**件 4 上下文旗子**（1.37＋1.38）／**件 5 i18n 与黄灯族**（1.39＋1.40）／**🔴 迁移基建**（1.41）／**🔴 官方仓清账 7 格**（1.42–1.48）／**收紧与作者面收口**（1.49）／**🔴 收口复核 1.50（恒最后）**。**队列 21 行：1 行已完成（第 0 格），剩 20 行。**

---

> 🔧 **路径变更通告（2026-09-17 · **非轮次** · 只改路径，**口径与读数一字未动**）**：本轴的「**机械定义**」那把尺子已从 `scratch/` **搬进仓内** —— **`scratch/audit-plugin-scope.mjs` ⇒ `scripts/audit-plugin-scope.mjs`**（同搬数据文件 `scripts/host-reserved.json` ＋ 其生成器 `scripts/gen-host-reserved.mjs`；npm 入口 **`npm run audit:plugin-scope`** ／ **`:regen`**）。**原因**：它被包括 **1.50 验收判据**在内的约 10 份文档引作「根治的机械定义」，而 `scratch/` 是 **gitignore** 的 ⇒ 换机器 / 别人 clone 后**那个文件根本不存在**（`check-doc-links` 只管 `docs/` 互链，**看不见「文档 → 脚本」**）。**拍板**：用户 2026-09-17 原话「**同意a**」（形态 (b) `git add -f` 已否）。**同笔**：24 份文档 49 处引用改路径。
> **搬迁后真跑复现基线（不是推断）**：`file-tree` **75** · `marketplace` **6** · `settings` **4** · `serial-monitor` **2** · `editor` **1** · 其余插件 **0** ⇒ 逐仓读数与本轴已登记值**完全一致**。**本轴轮次、判据、口径零改动**（1.31 该做什么，仍以下面那段的队列为准 —— 见 ⑷ 的队列表）。
> 📌 **读本档的历史段落时**：凡见到写 `scratch/audit-plugin-scope.mjs` 的，都是**这同一个文件的旧路径**（那个时代它确实不在仓里）。

---

---

## ← 第 1.38 轮 · 上下文旗子归属 · **落地已完成**（`#111h` · 2026-09-17）：给下一个 AI（**下一棒 = 1.39**）

> 🔴 **先说你是谁**：**你是第 9 棒，要做的是 [1.39 i18n 与黄灯族 · **评估**](09-任务-i18n与黄灯族评估.md)**——**这一棒只出方案、动零行代码**（同 1.33／1.35／1.37 的「评估棒」形态）。
> **你不是复核者，也不是派工者**——派工在第 0 格就做完了。用户会把上一棒的**整段消息**复制给你（不裁剪），里面可能有别的东西，**以本段为准**。
> ⚠️ **如果你拿到的上下文里没有本段**，说明交接还没交到你手上 ⇒ **先问一句「我是 1.39 吗」，别默认自己在做汇报或复核**。

---

### ⑴ **本轴是干什么的**

E6 第 1 层（插件独立构建）的**第二个专项**，紧接 [样式命名空间归一化](../样式命名空间归一化/00-整理档案.md)（CSS 系列，已收官）。

**真问题一句话**：**软件里有一批「公共花名册」——谁都能往上写一个名字，写的时候不核对归属，撞了只小声嘟囔（或连嘟囔都没有）。**

**同形判据（四条全中才算）**：名字空间**扁平** ＋ **多方都能写** ＋ **写时无归属校验** ＋ **冲突静默**（最多 `console.warn`）。🔴 **第四条是关键**：`console.warn` **对用户不可见，对「被覆盖的那一方作者」也不可见**——因为他在自己机器上没装对方插件，一切正常。**「被覆盖」这件事，受害方永远收不到通知。**

**真根因（每一棒收尾都要能复述这句）**：

> **不是名字起坏了，是「谁的名字」被寄托在「取名自觉」上——账本要么没有，要么只用来打印一行字，而宿主自己的名字和第三方名字长着同一张脸。**

**🔄 收口口径（§〇c 修订后，此后一切以此为准）**：**根治 = 规则零例外**（**官方仓全部改名**，**机械定义 = `npm run audit:plugin-scope` 全 0**）＋ **运行时归属仲裁**兜住静态判不了的那类 ＋ 🔴 **每一处改名都同笔带迁移 ＋ 逐项操作实测**。**上位约束 = 操作体验零变化**（**名字可变，取值与功能不许变**）。**唯一保留的黄灯** = i18n 字典键；**三类「全局概念名」永久豁免**（文件关联扩展名 / 语言定义扩展名 / 语言包语言码）。

**件 4（上下文旗子）现在的状态（1.38 收官读数）**：**账已拆两段**（宿主专用 **7** ／ 宿主公开约定面 **4**）⇒ **判据① 红（仅宿主专用）＋ 判据③ 黄（归属）已落地**；**官方侧待改 19 处**（`file-tree` 11 ＋ `marketplace` 6 ＋ `serial-monitor` 2；`settings` 4 **登记为约定面、不改名**）⇒ **1.42–1.46 执行改名**。机械定义那把尺子仍读 **106**（**本格零插件仓改动，读数不变**）。

---

### ⑵ **各轮次是干什么的**（🔴 **剩余任务队列整表 —— 把你那一格改成 ✅ 再整表传给下一棒**）

| # | 轮 | 编号 | 任务 | 详案 | 前置 | 提交轴 |
|:--:|:--|:--|:--|:--:|:--|:--|
| 0 | 第 0 格 | `#109r` | ✅ **审查 ＋ 派工**（总方案 ＋ 20 份任务书 ＋ 清单任务格 ＋ 本交接） | [00](00-整理档案.md) | — | 文档 |
| 1 | **1.31** | `#111a` | ✅ **命令 id 与协议 id 归属 · 评估** | [01](01-任务-命令id归属评估.md) | 第 0 格 | 文档 |
| 2 | **1.32** | `#111b` | ✅ **命令 id 归属 · 落地**（归属从身份来 ＋ 两条腿 ＋ 账定稿） | [02](02-任务-命令id归属落地.md) | 1.31 | 壳 ＋ SDK |
| 3 | **1.33** | `#111c` | ✅ **设置键归属 · 评估**（复量对账 ＋ 归属语义裁决 (丙) ＋ fail-fast 永久否决 ＋ 19 键映射表 ＋ 1.34 任务书） | [03](03-任务-设置键归属评估.md) | 1.32 | 文档 |
| 4 | **1.34** | `#111d` | ✅ **设置键归属 · 落地**（保护区 ＋ 首撞保留 ＋ SDK 新腿 ＋ 账口径扩宽 ＋ 运行时模块 ＋ 四向对账 ＋ SDK PATCH） | [04](04-任务-设置键归属落地.md) | 1.33 | 壳 ＋ SDK |
| 5 | **1.35** | `#111e` | ✅ **外观族 id 归属 · 评估**（复量五空间 ＋ 四问裁决 ＋ **25 条改名映射表** ＋ **五键两表迁移设计** ＋ 账拆四栏＆证照 ＋ **06 正式版**） | [05](05-任务-外观族id归属评估.md) | 1.34 ✅ | 文档 |
| 6 | **1.36** | `#111f` | ✅ **外观族 id 归属 · 落地**（账拆四栏 ＋ 证照 ＋ **五处仲裁三条分支** ＋ **SDK 新腿** ＋ **两张映射表 ＋ 版本 6 迁移** ＋ 探针口径修正 ＋ SDK／docs PATCH） | [06](06-任务-外观族id归属落地.md) | 1.35 ✅ | 壳 ＋ SDK |
| 7 | **1.37** | `#111g` | ✅ **上下文旗子归属 · 评估**（复量对账 ＋ 宿主旗子生成式全集 ＋ 两条路径分工 ＋ 仲裁语义裁决「状态写 ≠ 注册」＋ `settings` 两条路线代价逐条算 ＋ `inputFocus` 还回方案 ＋ **07 正式版 ＋ 08 正式任务书**；🔴 **头号发现：生成器 `when:` 抽取截断 = 假绿**） | [07](07-任务-上下文旗子归属评估.md) | 1.36 ✅ | 文档 |
| 8 | **1.38** | `#111h` | ✅ **上下文旗子归属 · 落地**（**本段就是我，已做完** —— 账拆两段 7／4 ＋ **修生成器 `when:` 截断缺陷（假绿）** ＋ 约定面自动派生 ＋ SDK 新腿 `context-ownership` ＋ **运行时出声放行** ＋ 作者面两棵树 ＋ **负控 ①–⑨ 真跑** ＋ 壳／SDK／docs 三轴 PATCH） | [08](08-任务-上下文旗子归属落地.md) | 1.37 ✅ | 壳 ＋ SDK |
| 9 | **1.39** | `#111i` | **← 你在这** i18n 与黄灯族 · **评估** | [09](09-任务-i18n与黄灯族评估.md) | 1.38 ✅ | 文档 |
| 10 | 1.40 | `#111j` | i18n 与黄灯族 · 落地 | [10](10-任务-i18n与黄灯族落地.md) | 1.39 | 壳 ＋ SDK ＋ 文档 |
| 11 | **1.41** | `#111m` | 🔴 **迁移基建**（7 个清账轮的公共前置） | [11](11-任务-迁移基建.md) | 1.40 | 壳 |
| 12 | **1.42** | `#111n-1` | 🔴 **清账 · `file-tree`**（75 处，最大的一格 —— 含 1.34 交出的 18 个设置键改名 ＋ 本件新增：旗子 11 处改名 ＋ `inputFocus` 还回） | [12](12-任务-清账-file-tree.md) | **1.41** | 插件仓 ×1 ＋ 壳账 |
| 13 | 1.43 | `#111n-2` | **清账 · `editor`**（1 处 —— `files.autoSave` → `editor.autoSave`） | [13](13-任务-清账-editor.md) | 1.41 | 插件仓 ×1 ＋ 壳账 |
| 14 | 1.44 | `#111n-3` | **清账 · `serial-monitor`**（旗子 2 —— 🔴 **含死旗子 `serialSessionFocus` 的去留裁决**） | [14](14-任务-清账-serial-monitor.md) | 1.41 | 插件仓 ×1 ＋ 壳账 |
| 15 | 1.45 | `#111n-4` | **清账 · `marketplace`**（旗子 6） | [15](15-任务-清账-marketplace.md) | 1.41 | 插件仓 ×1 ＋ 壳账 |
| 16 | **1.46** | `#111n-5` | **清账 · `settings`**（🔴 1.37 裁决后风险大幅下降：4 个旗子**不改名**，走「宿主公开约定面」登记 ⇒ **零改动**；仍需处置的是**设置键 19 键改名**那部分） | [16](16-任务-清账-settings.md) | 1.41 ＋ 1.37 裁决 ✅ | 插件仓 ×1 ＋ 壳 |
| 17 | **1.47** | `#111n-6` | 🔴 **清账 · 主题族 9 只**（主题系统最高审查级 —— 映射表在 [05 §十三](05-任务-外观族id归属评估.md)） | [17](17-任务-清账-主题族.md) | 1.41 | 插件仓 ×9 ＋ 壳账 |
| 18 | 1.48 | `#111n-7` | **清账 · 其余插件**（0 处，但要「无面确认表」） | [18](18-任务-清账-其余插件.md) | 1.41 | 文档 |
| 19 | 1.49 | `#111k` | **收紧为红 ＋ 账定稿进 SDK ＋ 两棵树 ＋ 五轴发版** | [19](19-任务-门禁与作者面收口.md) | **1.42–1.48 全清完** | SDK(npm) ＋ 文档 ＋ 壳 |
| 20 | **1.50** | `#111l` | 🔴 **本轴收口复核**（**恒最后**） | [20](20-任务-本轴收口复核.md) | 全部 | 文档 |

**两条排序纪律（写死在 [00 §四](00-整理档案.md)）**：① **1.32/1.34/1.36/1.38 的判据先以「黄灯 ＋ 账」落地**，**不立刻判红**；② **1.49 才收紧为红**——那是「零例外」真正生效的一刻。
> 🔴 **1.38 已照旧兑现排序纪律**：SDK 腿 `context-ownership` 里**只有「插件设宿主专用旗子」判红**，**判据③（旗子须带归属）是黄**（进 `advisories`，**不进退出码**）⇒ 官方 18 仓跑这条腿 = **红 0**。
> ⚠️ **格数是上限，不是配额**：本轴 20 轮里**没有一格能省**；判「这条不做」必须把结论与理由写进交接段（1.38 的「不做」在本段 📦 末）。

---

### ⑶ **你这一棒 = 1.39（`#111i` i18n 与黄灯族 · 评估）**

**形状**：**只出方案、动零行代码**（同 1.33／1.35／1.37）。**本格不写代码、不发版**，产出 = 「复量对账 ＋ 裁决 ＋ **10 正式版任务书**」三项。

**🔴 进场先读（按序）**：① 工作区 `AGENTS.md`（指针）→ ② `E:/linkdesk/CLAUDE.md` **全文**（总纲）→ ③ **本段** → ④ [09 任务书](09-任务-i18n与黄灯族评估.md) → ⑤ [00-整理档案.md](00-整理档案.md) 的 **§〇c 口径修订令**（🔴 **压过其余一切结论**）。**盘面读数自己跑命令取，不要凭记忆。**

**1.38 能给你什么（全是本格实测，不是推断）**：

1. 🔴 **本轴的四件已全部落地**（命令 id／设置键／外观族 id／上下文旗子），**你的件 5（i18n 与黄灯族）是收尾的那一件**——前四件建立的形态（**账 ＋ 两/四栏 ／ SDK 腿两条判据（红＋黄） ／ `--self-test` ＋ `--check` 四向对账 ／ 出口形状坑 4**）可**原样照搬**，别新造版式（1.37 的 §六 已把「i18n 是唯一保留的黄灯」写进口径）。
2. **本格确立的「两段账」形态可复用**：一段是**禁设面**（判红）、一段是**约定面**（只登记不判）。⚠️ **判据是「谁该写」而不是「名字长什么样」** 时才用两段；**i18n 字典键属「永久豁免」那一类**（三类全局概念名之一），**先判它到底要不要判**，别默认照抄成两段。
3. 🔴 **假绿比假红危险（本格最大的方法论收获）**：1.37 发现、1.38 修掉的「生成器 `when:` 截断」是一条**静默漏报**——账少一条、门禁显示为绿，**没人会发现**。⇒ **1.39 复量时，凡「读数比预期小」都要先怀疑「是不是我没扫到」，而不是先相信它合规**。本格的对照证据：`audit-nonnaming` ⑫ 读键数**修复后从 3 变 6**（旧读数 3 就是截断漏的）。
4. **探针纪律（本格踩过一次）**：`gen-host-reserved.mjs` **不带 `--check` 是写操作**；**还原账该用「重生成」（`node scripts/gen-host-reserved.mjs`）而不是 `git checkout --`**——后者退的是**上一版账**，重生成退的是**真实现状**（本格如实记于清单：误用 checkout 后已重生成并四向复核）。
5. **收尾四件套**照 ⑷ 执行；**你这一棒不写代码 ⇒ `npm run check` 仍须跑一遍留证**（本格基线：**EXIT=0 / 168 文件 / 2316 测试**）。

**⚠️ 你这一棒最容易写错的地方**：

- 🔴 **别抢跑改插件仓**：逐仓改名归 **1.42–1.46**，`settings` = 1.46。
- 🔴 **别把 i18n 字典键判红**：口径明写它是**唯一保留的黄灯**；三类「全局概念名」**永久豁免**。
- 🔴 **「评估棒」的定义是动零行代码**——`git diff` 里 `src/`／`packages/` 应当**一行未改**，以 `npm run check` 仍 EXIT=0 为证（1.37 就是这么自证的）。

---

### ⑷ 收尾四件（**缺一不可**）

1. 在**本文件顶部追加一段**（只追加不改旧段），段内**必带上面的剩余任务队列整表**（把自己那格改 ✅）；
2. 同笔续写 [E6-执行清单.md 第 1 层头的轮次进度行](../../E6-执行清单.md)；
3. 勾格要勾全套；
4. **🔴 不许静默不做**：判「这条不做」必须把结论与理由写进交接段。

---

### 📦 1.38 交付说明（**补记：本格实际做到什么 / 哪条没做**）

**落地面六项全做**（照 [08 §〇](08-任务-上下文旗子归属落地.md)）：账拆两段 ✅ ／ 生成器修截断 ＋ 约定面派生 ＋ `renderRuntimeModule` 两段 ✅ ／ SDK 新腿 `context-ownership` ✅ ／ **运行时仲裁「出声 ＋ 放行」** ✅ ／ 活体样本 `settings` 走「登记为约定面 ＋ 三件加固」✅ ／ 作者面两棵树 ＋ `docs:build` ✅。**官方仓改名未做 —— 按 §〇 明文归 1.42–1.46，本格不许抢跑**（不是遗漏，是排期）。

**九条「不做」逐条带理由**（承接 1.37，本格一条未破）：

| 不做 | 理由 |
|:--|:--|
| 不给 `contextKey:set` 通道加身份 | 要对**跨仓公共契约**动刀，超射程；且**本机制不需要**（出声已够） |
| 不引 `setsContextKeys` | 新能力 ＋ 越界 ＋ 解决不了根本问题 |
| 不动 `overrides` 路径 | **合法用法**（`settings` 的 `SettingRow.tsx` 正在用），一行不许动 |
| 不改 `serialSessionFocus` | **死旗子**（写了全仓无人读）⇒ 本格只报，**去留裁决移交 1.44** |
| 不改探针口径（挪到 1.38） | 1.37 的「不做」；**本格已做**（探针两处口径随账拆两段同步） |
| 不抢跑改插件仓 | 归 1.42–1.46 |
| 不把旗子归属判据写成红 | **排序纪律**：1.49 才收紧 |
| 不修「写了无人读」的两键（`editorHasSelection`／`editorCount`） | 仍进**宿主专用**段（它们确是宿主状态面）——本格只管归属，不管死键 |
| 不发软件包 | 壳侧用户可见行为零变化（只多一条 `console.error`）⇒ **不 bump**（但实为 **0.2.5**：运行时改了 ⇒ 按「完成一轮就 bump」纪律走 PATCH） |

**壳侧「实机逐项」未做**（负控 ⑧）：本格**无 UI 变更**（运行时可观测差异仅一条 `console.error`），且该项要求用户实机操作齿轮菜单／F2 重命名 ⇒ **留待用户实机确认**（不是遗漏）。

**⚠️ 一次误跑如实记**：复现「⑤ 只改账」探针后用 `git checkout -- scripts/host-reserved.json` 还原，**该命令把账退回提交前版本（整轮的账改动被抹掉）**；已立刻 `node scripts/gen-host-reserved.mjs` **重生成** ＋ `--check` 四向复核 ✅。**教训：账是生成物，还原该用「重生成」而不是「git checkout」。**

---

## ← 第 1.37 轮 · 上下文旗子归属 · **评估已完成**（`#111g` · 2026-09-17）：给下一个 AI（**下一棒 = 1.38**）

> 🔴 **先说你是谁**：**你是第 8 棒，要做的是 [1.38 上下文旗子归属 · **落地**](08-任务-上下文旗子归属落地.md)**——**这一棒动代码**（账拆两段 ＋ SDK 新腿 ＋ 运行时出声 ＋ 修生成器截断缺陷）。**1.37 只出方案、动零行代码**（本格 `git diff` 里 `src/` / `packages/` **一行未改**，`npm run check` 仍 EXIT=0 为证）。
> **你不是复核者，也不是派工者**——派工在第 0 格就做完了。用户会把上一棒的**整段消息**复制给你（不裁剪），里面可能有别的东西，**以本段为准**。
> ⚠️ **如果你拿到的上下文里没有本段**，说明交接还没交到你手上 ⇒ **先问一句「我是 1.38 吗」，别默认自己在做汇报或复核**。

---

### ⑴ **本轴是干什么的**

E6 第 1 层（插件独立构建）的**第二个专项**，紧接 [样式命名空间归一化](../样式命名空间归一化/00-整理档案.md)（CSS 系列，已收官）。

**真问题一句话**：**软件里有一批「公共花名册」——谁都能往上写一个名字，写的时候不核对归属，撞了只小声嘟囔（或连嘟囔都没有）。**

**同形判据（四条全中才算）**：名字空间**扁平** ＋ **多方都能写** ＋ **写时无归属校验** ＋ **冲突静默**（最多 `console.warn`）。🔴 **第四条是关键**：`console.warn` **对用户不可见，对「被覆盖的那一方作者」也不可见**——因为他在自己机器上没装对方插件，一切正常。**「被覆盖」这件事，受害方永远收不到通知。**

**真根因（每一棒收尾都要能复述这句）**：

> **不是名字起坏了，是「谁的名字」被寄托在「取名自觉」上——账本要么没有，要么只用来打印一行字，而宿主自己的名字和第三方名字长着同一张脸。**

**🔄 收口口径（§〇c 修订后，此后一切以此为准）**：**根治 = 规则零例外**（**官方仓全部改名**，**机械定义 = `npm run audit:plugin-scope` 全 0**）＋ **运行时归属仲裁**兜住静态判不了的那类 ＋ 🔴 **每一处改名都同笔带迁移 ＋ 逐项操作实测**。**上位约束 = 操作体验零变化**（**名字可变，取值与功能不许变**）。**唯一保留的黄灯** = i18n 字典键；**三类「全局概念名」永久豁免**（文件关联扩展名 / 语言定义扩展名 / 语言包语言码）。

**件 4（上下文旗子）现在的状态（1.37 收官读数）**：**账已存 11 项但没有分级**（宿主专用 7 与约定面 4 **挤在同一栏**）⇒ **1.38 拆两段**；**官方侧实改 19 处**（`file-tree` 11 ＋ `marketplace` 6 ＋ `serial-monitor` 2；`settings` 4 **改为登记、不改名**）⇒ **1.42–1.46 执行改名**。机械定义那把尺子仍读 **106**（**本格零代码改动，读数不变**）。

---

### ⑵ **各轮次是干什么的**（🔴 **剩余任务队列整表 —— 把你那一格改成 ✅ 再整表传给下一棒**）

| # | 轮 | 编号 | 任务 | 详案 | 前置 | 提交轴 |
|:--:|:--|:--|:--|:--:|:--|:--|
| 0 | 第 0 格 | `#109r` | ✅ **审查 ＋ 派工**（总方案 ＋ 20 份任务书 ＋ 清单任务格 ＋ 本交接） | [00](00-整理档案.md) | — | 文档 |
| 1 | **1.31** | `#111a` | ✅ **命令 id 与协议 id 归属 · 评估** | [01](01-任务-命令id归属评估.md) | 第 0 格 | 文档 |
| 2 | **1.32** | `#111b` | ✅ **命令 id 归属 · 落地**（归属从身份来 ＋ 两条腿 ＋ 账定稿） | [02](02-任务-命令id归属落地.md) | 1.31 | 壳 ＋ SDK |
| 3 | **1.33** | `#111c` | ✅ **设置键归属 · 评估**（复量对账 ＋ 归属语义裁决 (丙) ＋ fail-fast 永久否决 ＋ 19 键映射表 ＋ 1.34 任务书） | [03](03-任务-设置键归属评估.md) | 1.32 | 文档 |
| 4 | **1.34** | `#111d` | ✅ **设置键归属 · 落地**（保护区 ＋ 首撞保留 ＋ SDK 新腿 ＋ 账口径扩宽 ＋ 运行时模块 ＋ 四向对账 ＋ SDK PATCH） | [04](04-任务-设置键归属落地.md) | 1.33 | 壳 ＋ SDK |
| 5 | **1.35** | `#111e` | ✅ **外观族 id 归属 · 评估**（复量五空间 ＋ 四问裁决 ＋ **25 条改名映射表** ＋ **五键两表迁移设计** ＋ 账拆四栏＆证照 ＋ **06 正式版**） | [05](05-任务-外观族id归属评估.md) | 1.34 ✅ | 文档 |
| 6 | **1.36** | `#111f` | ✅ **外观族 id 归属 · 落地**（账拆四栏 ＋ 证照 ＋ **五处仲裁三条分支** ＋ **SDK 新腿** ＋ **两张映射表 ＋ 版本 6 迁移** ＋ 探针口径修正 ＋ SDK／docs PATCH） | [06](06-任务-外观族id归属落地.md) | 1.35 ✅ | 壳 ＋ SDK |
| 7 | **1.37** | `#111g` | ✅ **上下文旗子归属 · 评估**（**本段就是我，已做完** —— 复量对账 ＋ 宿主旗子生成式全集 ＋ 两条路径分工 ＋ 仲裁语义裁决「状态写 ≠ 注册」＋ `settings` 两条路线代价逐条算 ＋ `inputFocus` 还回方案 ＋ **07 正式版 ＋ 08 正式任务书**；🔴 **头号发现：生成器 `when:` 抽取截断 = 假绿**） | [07](07-任务-上下文旗子归属评估.md) | 1.36 ✅ | 文档 |
| 8 | **1.38** | `#111h` | **← 你在这** 上下文旗子归属 · **落地**（账拆两段 ＋ SDK 新腿 `context-ownership` ＋ 运行时出声放行 ＋ **修生成器截断缺陷**） | [08](08-任务-上下文旗子归属落地.md) | 1.37 ✅ | 壳 ＋ SDK |
| 9 | 1.39 | `#111i` | i18n 与黄灯族 · 评估 | [09](09-任务-i18n与黄灯族评估.md) | 1.38 | 文档 |
| 10 | 1.40 | `#111j` | i18n 与黄灯族 · 落地 | [10](10-任务-i18n与黄灯族落地.md) | 1.39 | 壳 ＋ SDK ＋ 文档 |
| 11 | **1.41** | `#111m` | 🔴 **迁移基建**（7 个清账轮的公共前置） | [11](11-任务-迁移基建.md) | 1.40 | 壳 |
| 12 | **1.42** | `#111n-1` | 🔴 **清账 · `file-tree`**（75 处，最大的一格 —— 含 1.34 交出的 18 个设置键改名 ＋ 🔴 **本件新增：旗子 11 处改名 ＋ `inputFocus` 还回**） | [12](12-任务-清账-file-tree.md) | **1.41** | 插件仓 ×1 ＋ 壳账 |
| 13 | 1.43 | `#111n-2` | **清账 · `editor`**（1 处 —— `files.autoSave` → `editor.autoSave`） | [13](13-任务-清账-editor.md) | 1.41 | 插件仓 ×1 ＋ 壳账 |
| 14 | 1.44 | `#111n-3` | **清账 · `serial-monitor`**（旗子 2 —— 🔴 **含死旗子 `serialSessionFocus` 的去留裁决**） | [14](14-任务-清账-serial-monitor.md) | 1.41 | 插件仓 ×1 ＋ 壳账 |
| 15 | 1.45 | `#111n-4` | **清账 · `marketplace`**（旗子 6） | [15](15-任务-清账-marketplace.md) | 1.41 | 插件仓 ×1 ＋ 壳账 |
| 16 | **1.46** | `#111n-5` | **清账 · `settings`**（🔴 **1.37 裁决后风险大幅下降**：4 个旗子**不改名**，走「宿主公开约定面」登记 ⇒ **零改动**；仍需处置的是**设置键 19 键改名**那部分） | [16](16-任务-清账-settings.md) | 1.41 ＋ **1.37 裁决 ✅** | 插件仓 ×1 ＋ 壳 |
| 17 | **1.47** | `#111n-6` | 🔴 **清账 · 主题族 9 只**（主题系统最高审查级 —— 映射表在 [05 §十三](05-任务-外观族id归属评估.md)） | [17](17-任务-清账-主题族.md) | 1.41 | 插件仓 ×9 ＋ 壳账 |
| 18 | 1.48 | `#111n-7` | **清账 · 其余插件**（0 处，但要「无面确认表」） | [18](18-任务-清账-其余插件.md) | 1.41 | 文档 |
| 19 | 1.49 | `#111k` | **收紧为红 ＋ 账定稿进 SDK ＋ 两棵树 ＋ 五轴发版** | [19](19-任务-门禁与作者面收口.md) | **1.42–1.48 全清完** | SDK(npm) ＋ 文档 ＋ 壳 |
| 20 | **1.50** | `#111l` | 🔴 **本轴收口复核**（**恒最后**） | [20](20-任务-本轴收口复核.md) | 全部 | 文档 |

**两条排序纪律（写死在 [00 §四](00-整理档案.md)）**：① **1.32/1.34/1.36/1.38 的判据先以「黄灯 ＋ 账」落地**，**不立刻判红**；② **1.49 才收紧为红**——那是「零例外」真正生效的一刻。
> 🔴 **1.38 照旧兑现排序纪律**：SDK 腿 `context-ownership` 里 **只有「插件设宿主专用旗子」判红**（进 `violations`），**判据③（插件旗子须带归属）是黄**（进 `advisories`，**不进退出码**）⇒ 官方 18 仓跑这条腿应 = **红 0**。
> ⚠️ **格数是上限，不是配额**：本轴 20 轮里**没有一格能省**；判「这条不做」必须把结论与理由写进交接段（1.37 的「不做」在本段 📦 末）。

---

### ⑶ **你这一棒 = 1.38（`#111h` 上下文旗子归属 · 落地）**

**形状**：**动代码**——把 [1.37](07-任务-上下文旗子归属评估.md) 的结论落成：**账拆两段** ＋ **SDK 新腿 `context-ownership`** ＋ **运行时出声放行** ＋ 🔴 **修生成器 `when:` 截断缺陷**。**本格不发软件版**（壳侧无用户可见变更）；**SDK 大概率发 PATCH**。

**🔴 1.37 给你的可直接复用的六件（全是本格实测，不是推断）**：

1. **账的现状**：`contextKeys` **11 项**（= 写 ∪ 读，`gen-host-reserved.mjs:283` 已是并集）⇒ **拆两段**：**宿主专用 7**（`activeEditor` · `editorHasSelection` · `editorCount` · `sidebarPosition` · `updateActionable` · `updateButtonLabel` · `inputFocus`）／**宿主约定面 4**（`settingKey` · `settingFollowTheme` · `settingResetsToDefault` · `settingModified`）。**逐条谁写谁读见 [07 §10.2](07-任务-上下文旗子归属评估.md)**。
2. 🔴 **头号发现（本格最值钱的一条）：生成器 `when:` 抽取正则在第一个单引号处截断**（`gen-host-reserved.mjs:214` 的 `[^"'`]+` 把 `'` 当终止符）⇒ `when: "settingKey == 'x' && myFlag"` **只抓到 `settingKey == `，`myFlag` 静默不进账** = **假绿**（漏报，比假红危险）。⚠️ **副作用**：今天 `left`/`right`/`app.backgroundImage` **没进账，不是因为过滤器对，是因为正则错**——**账对，理由错**。⇒ 1.38 **必修** ＋ **负控 ⑥′**（`== 'x' && myFlag` ⇒ **`myFlag` 必须进账**）。
3. 🔴 **仲裁语义裁决**：**旗子是「状态写」不是「注册」⇒ 不许照搬 1.36 的「先者保留」**。依据是**代码事实**：`electron/ipc/ipc-bridge.ts:204-208` 的 `contextKey:set` **过桥时不带身份**（对比同文件 `commands:register` **显式追加 `windowId`**）⇒ 运行时**根本拿不到「谁是先者」**；且 `setValue` 幂等（`ContextKeyService.ts:328-329`）⇒ **后写者覆盖是"正常"不是"侵占"**。⇒ **仲裁 = `console.error` 点名 ＋ 放行**（不拒绝、不抛错、不先者保留）。**三个「为什么不行」的逐条理由在 [07 §12.2](07-任务-上下文旗子归属评估.md)**。
4. 🔴 **两条路径的分工（别把合法用法误判成污染）**：**A 全局 `set`**（要管的那条）vs **B per-call `overrides`**（`menu.getItems(menuId, context)` → `_readKey` 优先读 `overrides`）——**B 是合法用法**，`settings` 的 `SettingRow.tsx:118` `context={{settingKey}}` 正在用，**一行不许动**。🔴 **`settingKey` 同时走两条路**（A 供命令处理器读 `args[0].settingKey`；B 供菜单 `when` 求值）⇒ **改名必须两条同改**。见 [07 §十一](07-任务-上下文旗子归属评估.md)。
5. 🔴 **`settings` 4 个旗子的路线 = 登记为「宿主公开约定面」（不改名）**——理由是**代价可测**：改名要动 **2 仓 15 处**（`gearMenu.ts` 8 写 ＋ `coreCommands.ts` 3 读 ＋ `SettingRow.tsx:118` 1 overrides ＋ 命令处理器 3）＋ **`settings` 发版 ＋ 目录回填 ＋ 出厂种子** ＋ 打破第三方兼容；**且 `MenuId` 是开放字符串**（`public/schemas/plugin.schema.json` 明写）⇒ **第三方可加入 `settingItemGear` 槽读这 4 个名字** ⇒ 这 4 个**事实上已是公开约定面**，「只授权 `settings`」**机制上无法实现**。**三件加固**（单独成段／双向对账／出声＋作者面明写）见 [07 §13.5](07-任务-上下文旗子归属评估.md)。
6. **出口的形状（坑 4，你会原样再踩）**：新增运行时报点文案**必须落在 `console.*` 调用之内**（写成**「出口」**而非「返回值里的 message」），否则 `audit-i18n.mjs --strict` 判缺翻译、`npm run check` 在 i18n 处断。**⛔ 别动 `EXCLUDE_FILES` / `EXCLUDE_RANGES` 豁免表**。

**⚠️ 两处最容易写错的地方（1.37 已探明）**：

- 🔴 **`HostReservedNames` 是「单一接口收全部家族」**（`packages/plugin-sdk/src/eslint/checks/command-ownership.ts:56-63`）⇒ **加 REQUIRED 字段 = 所有既有 fixture 同时断**（1.34 踩过）。**动手前先 `grep -rn "HostReservedNames" packages/plugin-sdk/src/`**，逐个补空数组。
- 🔴 **不许抢跑改插件仓**：逐仓改名归 **1.42–1.46**（`settings` = 1.46）。**1.38 只建账与判据 ＋ 落运行时仲裁**。

**⚠️ 你这一棒是代码轮** ⇒ `npm run check` **必须全绿才提交**（1.37 收尾基线：**EXIT=0 / 167 文件 / 2292 测试**）。

---

### ⑷ 收尾四件（**缺一不可**）

1. 在**本文件顶部追加一段**（只追加不改旧段），段内**必带上面的剩余任务队列整表**（把自己那格改 ✅）；
2. 同笔续写 [E6-执行清单.md 第 1 层头的轮次进度行](../../E6-执行清单.md)；
3. 勾格要勾全套；
4. **🔴 不许静默不做**：判「这条不做」必须把结论与理由写进交接段。

---

### 🔴 两条通用红线（每一棒都适用）

- **提交只按路径 `git add <本格文件>`，⛔ 禁 `git add -A` / `git add .`** —— 壳仓随时有别的会话在途（**索引是共享的**）。
- **不推**：`git push` 必须等**用户本人当次点头**；推必带代理；推完 `git ls-remote --heads origin <branch>` **对 SHA**。npm 发版照 [作者轴 npm 发版 runbook](../../../../06-发布管理/作者轴npm发版.md) 五步（**显式 `--registry`**）；⛔ **撤版只能用户本人**。

---

### 📦 本格（1.37）交付实录 —— 下面这些结论你可以直接引用

**做了什么（一句话：复量对账 ＋ 宿主旗子生成式全集 ＋ 两条路径分工量清 ＋ 仲裁语义裁决「状态写 ≠ 注册」＋ `settings` 两条路线代价逐条算 ＋ `inputFocus` 还回方案 ＋ 07 正式版 ＋ 08 正式任务书；🔴 头号发现 = 生成器 `when:` 抽取截断，假绿）**：

| 面 | 交付 |
|:--|:--|
| **07 正式版** | [07](07-任务-上下文旗子归属评估.md) **§〇–§七 原文保留**，**新增 §八–§十八**（18 节）：复量对账 · 宿主旗子全集逐条 · 两条路径分工 · 仲裁裁决 · `settings` 两条路线代价 · 插件旗子归属与改名映射 · 残余边界 · **负控 ①–⑨（含 ⑥′）** · 盘面命令 · 「不做」清单。 |
| **08 正式任务书** | [08](08-任务-上下文旗子归属落地.md) **全档重写**：落地面表 ＋ `file:line` 面清单 ＋ 账两段 ＋ 判据最终措辞 ＋ 运行时仲裁三个「不许」 ＋ 活体样本处置 ＋ 插件改名映射 ＋ 不发软件版／SDK PATCH 发版轴。 |
| **账的两段（1.38 落）** | **宿主专用 7** ＋ **宿主约定面 4**——🔴 **不许合并**（今天挤一栏 ⇒ 看不出分级 ⇒ 官方 `settings` 当场假红，**与 1.36「外观账一栏混两空间」同一种病**）。 |
| **仲裁裁决（核心）** | 🔴 **旗子 = 状态写 ⇒ 出声 ＋ 放行**。**结构性依据**：`ipc-bridge.ts:204-208` 的 `contextKey:set` **零身份**（对比 `commands:register` 追加 `windowId`）＋ `setValue` 幂等 ⇒ **「先者」概念不存在**。**三个「不行」**：先者保留（拿不到先者 ＋ 语义也错）／拒绝（打断 `handleGearClose` 的合法写回 ＋ 静默更隐蔽）／抛错（[00 §2.7](00-整理档案.md) 明禁）。 |
| **两条路径量清** | **A 全局 `set`**（要管）vs **B per-call `overrides`**（`ContextMenu.tsx:132` 消费 `SettingRow.tsx:118` 的 `context`）——**B 合法，一行不许动**；🔴 **`settingKey` 两条都走**（A 供命令处理器读 `args[0]`，B 供菜单 `when` 求值）⇒ 改名两条同改。 |
| **`settings` 裁决** | 🟠 **走「登记为宿主公开约定面」，不改名**。代价对照：**（乙）改名 = 2 仓 15 处 ＋ 一次发版 ＋ 打破第三方兼容** vs **（丙）登记 = 0 行源码 ＋ 0 发版**。🔴 **`setsContextKeys` 声明 = 新能力，越界，本轴不做**（[07 §13.4](07-任务-上下文旗子归属评估.md)：schema ＋ loader ＋ 校验 ＋ 作者面，且 **`set` 的 key 仍是任意字符串**，解决不了根本）。 |
| **`inputFocus` 还回方案** | `file-tree/src/hooks/useTreeRename.ts:35,41` → **`file-tree.inputFocus`** ＋ 🔴 **`plugin.json:150` 的 `when` 同笔跟改**（**漏读点 = 重命名快捷键静默失效，不报错**）⇒ **宿主侧一行不动**。**代价 = 只改一个仓**（仓外无人读）。 |
| **改名后的形态变化（量清）** | UI **不变** · 功能**不变**（前提 = 写读同笔）· 🔴 **漏改 ⇒ 静默失效**。⇒ **这就是「操作体验零变化」在旗子面的确切含义：改名本身零变化，漏改才是真 UI 变化** ⇒ 1.42 必须**逐项操作实测**。 |
| **🔴 头号发现（假绿）** | `gen-host-reserved.mjs:214` 的 `when:` 抽取正则 `[^"'`]+` **在第一个单引号处截断** ⇒ `when: "settingKey == 'x' && myFlag"` **只抓到 `settingKey == `，`myFlag` 静默漏账**。**副作用**：今天 `left`/`right`/`backgroundImage` 没进账**不是过滤器对，是正则错**（**账对，理由错**）。⇒ 1.38 必修 ＋ 负控 ⑥′。**实测证据在 [07 §10.4／§十七](07-任务-上下文旗子归属评估.md)**。 |
| **口径澄清** | 探针 ④「多方共写 **6**」**全部是 `file-tree` 一个仓内的多写点**（`index.tsx`/`Menu.tsx`/`FoldersView.tsx`/`FileTreeClipboard.ts`）——**同 pluginId 内部，不是跨方冲突** ⇒ **本轴旗子面的「跨方共写」今天是 0**。 |
| **探针记账** | `audit-nonnaming`：④ **23 / 6**（与 1.36 所交一致）· ⑩b **∩账 = 5**（同）· ⑪ 3 写点 · ⑫ 6 读键。`audit-plugin-scope`：**`file-tree` 11 · `marketplace` 6 · `settings` 4 · `serial-monitor` 2 · `editor` 1**（**逐仓与 1.36 交付值完全一致，本格零代码改动 ⇒ 读数不变**）。`gen-host-reserved --check`：✅ 四向一致（context key **11**）。**机械定义尺子仍读 106**。 |
| **`npm run check`** | **EXIT=0 · 167 文件 / 2292 测试全绿**（与 1.36 收尾基线**逐项相同**，证明本格零代码改动）。 |

**🔴 有意偏离与「本格不做」（🔴 不许静默不做 —— 逐条写理由）**：

| # | 事项 | 处置与理由 |
|:--:|:--|:--|
| ① | **不给 `contextKey:set` 的 IPC 通道加身份** | 改动 `electron/ipc/ipc-bridge.ts` 通道 **＋ 公共 API 签名**（`ui.ts:49-52` 是对外契约）⇒ **超本件射程**；且**不需要**（出声 ＋ 放行已够）。**运行时拿不到的 pluginId 就如实不写**——**不许为了让日志好看去改通道**。 |
| ② | **不引 `setsContextKeys` 声明** | **是新能力**（schema ＋ loader ＋ 面校验 ＋ 作者面），越 §〇c 界；且 **`contextKey.set` 的 key 仍是任意字符串**（签名不改就无法约束）⇒ 声明只是**纸面自律**，与今天「约好名字」无本质区别 ⇒ **投入产出不成立**。 |
| ③ | **不动 `overrides` 路径** | `SettingRow.tsx:118` 的 `context={{settingKey}}` 是**合法用法**（**不写状态**，只参与本次 `evaluate`）⇒ **一行不许动**；静态判据与运行时仲裁**都只扫全局 `set` 路径**。 |
| ④ | **不裁 `serialSessionFocus` 去留** | 🔴 **它是死旗子**（`views/SessionListView.tsx:55,56` 写，**全仓无人读**）——**本格只报**；**删代码超评估轮射程** ⇒ **移交 1.44**（`serial-monitor` 清账格）改名时同笔裁决。 |
| ⑤ | **不改探针口径** | `audit-nonnaming` ⑪ **射程有限**（就地扫，扫不到常量与 `_state` 直写）——**已在 [07 §九／§15.3](07-任务-上下文旗子归属评估.md) 注明**；改探针会动读数口径 ⇒ **移交 1.38**。 |
| ⑥ | **不改 `editorHasSelection` / `editorCount`** | 两者**写而无人读**（`initCoreKeys:428,429`）——**仍进「宿主专用」**（内核 API ＋ 面向未来）⇒ **登记在案，不改**。 |
| ⑦ | **不把旗子判据写成红** | §〇c 排序纪律：**先黄 ＋ 账，1.49 才收紧**（`explorer*` 9 个是反例）⇒ 1.38 的判据③维持 🟡。 |
| ⑧ | **不许抢跑改插件仓** | 逐仓改名归 **1.42–1.46**（与 1.33/1.34／1.35/1.36 对「改名执行」的同一口径）⇒ **1.38 只建账与判据 ＋ 落运行时仲裁**。 |
| ⑨ | 🔴 **一次误跑的处置（如实记）** | 起手为证明「新旗子名会被读侧收进账」，**误跑了一次不带 `--check` 的 `gen-host-reserved.mjs`**（**它是写操作**）⇒ 账与 SDK 副本被写入 `ghostFlagX`。**当场发现**（`git status --porcelain` 两个 ` M`）⇒ `git checkout -- scripts/host-reserved.json packages/plugin-sdk/schemas/host-reserved.json` **还原**，复验工作区干净 ＋ `--check` 四向一致。**⚠️ 移交下一棒：做探针一律带 `--check`，或事后还原。** |
| ⑩ | **其它「不做」（与 1.36 一致，本格照旧）** | 不动 `when` 解析器与 `matches()` 求值语义（既有安全设计）· 不动多窗广播（`ipc-bridge.ts:205-207`）· **不把「快捷键 chord 缺陷」拉进来**（已立案、不属本轴）· **不抛错** · **不引白名单/基线/棘轮** · **不合并两段账**。 |

**本格改动面（按路径）**：**只动文档**——`07-任务-上下文旗子归属评估.md`（新增 §八–§十八）· `08-任务-上下文旗子归属落地.md`（全档重写为正式版）· `交接.md`（本段）· `E6-执行清单.md`（轮次进度行 ＋ 勾格）。**`src/` / `electron/` / `packages/` / `scripts/` 一行未改**（`npm run check` EXIT=0 与 1.36 基线逐项相同为证）。

---

## ← 第 1.36 轮 · 外观族 id 归属 · **落地已完成**（`#111f` · 2026-09-17）：给下一个 AI（**下一棒 = 1.37**）

> 🔴 **先说你是谁**：**你是第 7 棒，要做的是 [1.37 上下文旗子归属 · **评估**](07-任务-上下文旗子归属评估.md)**——🔴 **这一棒只出方案、动零行代码**（产物 = `07` 档正文本体 ＋ `08` 正式任务书 ＋ 本交接段）。1.36 刚把「五处仲裁 ＋ 一条腿带五空间 ＋ 三条分支 ＋ 按 id 记证照」跑通，**旗子那条可以直接复用这个形状**（见 ⑶）。
> **你不是复核者，也不是派工者**——派工在第 0 格就做完了。用户会把上一棒的**整段消息**复制给你（不裁剪），里面可能有别的东西，**以本段为准**。
> ⚠️ **如果你拿到的上下文里没有本段**，说明交接还没交到你手上 ⇒ **先问一句「我是 1.37 吗」，别默认自己在做汇报或复核**。

---

### ⑴ **本轴是干什么的**

E6 第 1 层（插件独立构建）的**第二个专项**，紧接 [样式命名空间归一化](../样式命名空间归一化/00-整理档案.md)（CSS 系列，已收官）。

**真问题一句话**：**软件里有一批「公共花名册」——谁都能往上写一个名字，写的时候不核对归属，撞了只小声嘟囔（或连嘟囔都没有）。**

**同形判据（四条全中才算）**：名字空间**扁平** ＋ **多方都能写** ＋ **写时无归属校验** ＋ **冲突静默**（最多 `console.warn`）。🔴 **第四条是关键**：`console.warn` **对用户不可见，对「被覆盖的那一方作者」也不可见**——因为他在自己机器上没装对方插件，一切正常。**「被覆盖」这件事，受害方永远收不到通知。**

**真根因（每一棒收尾都要能复述这句）**：

> **不是名字起坏了，是「谁的名字」被寄托在「取名自觉」上——账本要么没有，要么只用来打印一行字，而宿主自己的名字和第三方名字长着同一张脸。**

**🔄 收口口径（§〇c 修订后，此后一切以此为准）**：**根治 = 规则零例外**（**官方仓全部改名**，**机械定义 = `npm run audit:plugin-scope` 全 0**）＋ **运行时归属仲裁**兜住静态判不了的那类 ＋ 🔴 **每一处改名都同笔带迁移 ＋ 逐项操作实测**。**上位约束 = 操作体验零变化**（**名字可变，取值与功能不许变**）。**唯一保留的黄灯** = i18n 字典键；**三类「全局概念名」永久豁免**（文件关联扩展名 / 语言定义扩展名 / 语言包语言码）。

**件 3（外观族 id）现在的状态**：**规则已上线、账已拆栏、官方仓还没改名** ⇒ 机械定义那把尺子读 **106**（改前 118，本格降的 12 处**全是探针口径修正**，不是改名）⇒ **1.42–1.48 才把它压到 0**。

---

### ⑵ **各轮次是干什么的**（🔴 **剩余任务队列整表 —— 把你那一格改成 ✅ 再整表传给下一棒**）

| # | 轮 | 编号 | 任务 | 详案 | 前置 | 提交轴 |
|:--:|:--|:--|:--|:--:|:--|:--|
| 0 | 第 0 格 | `#109r` | ✅ **审查 ＋ 派工**（总方案 ＋ 20 份任务书 ＋ 清单任务格 ＋ 本交接） | [00](00-整理档案.md) | — | 文档 |
| 1 | **1.31** | `#111a` | ✅ **命令 id 与协议 id 归属 · 评估** | [01](01-任务-命令id归属评估.md) | 第 0 格 | 文档 |
| 2 | **1.32** | `#111b` | ✅ **命令 id 归属 · 落地**（归属从身份来 ＋ 两条腿 ＋ 账定稿） | [02](02-任务-命令id归属落地.md) | 1.31 | 壳 ＋ SDK |
| 3 | **1.33** | `#111c` | ✅ **设置键归属 · 评估**（复量对账 ＋ 归属语义裁决 (丙) ＋ fail-fast 永久否决 ＋ 19 键映射表 ＋ 1.34 任务书） | [03](03-任务-设置键归属评估.md) | 1.32 | 文档 |
| 4 | **1.34** | `#111d` | ✅ **设置键归属 · 落地**（保护区 ＋ 首撞保留 ＋ SDK 新腿 ＋ 账口径扩宽 ＋ 运行时模块 ＋ 四向对账 ＋ SDK PATCH） | [04](04-任务-设置键归属落地.md) | 1.33 | 壳 ＋ SDK |
| 5 | **1.35** | `#111e` | ✅ **外观族 id 归属 · 评估**（复量五空间 ＋ 四问裁决 ＋ **25 条改名映射表** ＋ **五键两表迁移设计** ＋ 账拆四栏＆证照 ＋ **06 正式版**） | [05](05-任务-外观族id归属评估.md) | 1.34 ✅ | 文档 |
| 6 | **1.36** | `#111f` | ✅ **外观族 id 归属 · 落地**（账拆四栏 ＋ 证照 ＋ **五处仲裁三条分支** ＋ **SDK 新腿** ＋ **两张映射表 ＋ 版本 6 迁移** ＋ 探针口径修正 ＋ SDK／docs PATCH —— **本段就是我，已做完**） | [06](06-任务-外观族id归属落地.md) | 1.35 ✅ | 壳 ＋ SDK |
| 7 | **1.37** | `#111g` | **← 你在这** 上下文旗子归属 · **评估**（宿主旗子全集 ＋ 两条活体 ＋ 仲裁语义裁决——**只出方案、零代码**） | [07](07-任务-上下文旗子归属评估.md) | 1.36 ✅ | 文档 |
| 8 | 1.38 | `#111h` | 上下文旗子归属 · 落地 | [08](08-任务-上下文旗子归属落地.md) | 1.37 | 壳 ＋ SDK |
| 9 | 1.39 | `#111i` | i18n 与黄灯族 · 评估 | [09](09-任务-i18n与黄灯族评估.md) | 1.38 | 文档 |
| 10 | 1.40 | `#111j` | i18n 与黄灯族 · 落地 | [10](10-任务-i18n与黄灯族落地.md) | 1.39 | 壳 ＋ SDK ＋ 文档 |
| 11 | **1.41** | `#111m` | 🔴 **迁移基建**（7 个清账轮的公共前置） | [11](11-任务-迁移基建.md) | 1.40 | 壳 |
| 12 | **1.42** | `#111n-1` | 🔴 **清账 · `file-tree`**（75 处，最大的一格 —— 含 1.34 交出的 18 个设置键改名） | [12](12-任务-清账-file-tree.md) | **1.41** | 插件仓 ×1 ＋ 壳账 |
| 13 | 1.43 | `#111n-2` | **清账 · `editor`**（1 处 —— `files.autoSave` → `editor.autoSave`） | [13](13-任务-清账-editor.md) | 1.41 | 插件仓 ×1 ＋ 壳账 |
| 14 | 1.44 | `#111n-3` | **清账 · `serial-monitor`**（旗子 2） | [14](14-任务-清账-serial-monitor.md) | 1.41 | 插件仓 ×1 ＋ 壳账 |
| 15 | 1.45 | `#111n-4` | **清账 · `marketplace`**（旗子 6） | [15](15-任务-清账-marketplace.md) | 1.41 | 插件仓 ×1 ＋ 壳账 |
| 16 | **1.46** | `#111n-5` | 🔴 **清账 · `settings`**（风险最高，跨仓配对） | [16](16-任务-清账-settings.md) | 1.41 ＋ 1.37 裁决 | 插件仓 ×1 ＋ 壳 |
| 17 | **1.47** | `#111n-6` | 🔴 **清账 · 主题族 9 只**（主题系统最高审查级 —— 🔴 **1.35 订正**：原写「10 只」，`theme-iconset-pastel` 的**图标主题 id 本轮不改名** ⇒ 实际动手 **9 只**；映射表在 [05 §十三](05-任务-外观族id归属评估.md)） | [17](17-任务-清账-主题族.md) | 1.41 | 插件仓 ×9 ＋ 壳账 |
| 18 | 1.48 | `#111n-7` | **清账 · 其余插件**（0 处，但要「无面确认表」） | [18](18-任务-清账-其余插件.md) | 1.41 | 文档 |
| 19 | 1.49 | `#111k` | **收紧为红 ＋ 账定稿进 SDK ＋ 两棵树 ＋ 五轴发版** | [19](19-任务-门禁与作者面收口.md) | **1.42–1.48 全清完** | SDK(npm) ＋ 文档 ＋ 壳 |
| 20 | **1.50** | `#111l` | 🔴 **本轴收口复核**（**恒最后**） | [20](20-任务-本轴收口复核.md) | 全部 | 文档 |

**两条排序纪律（写死在 [00 §四](00-整理档案.md)）**：① **1.32/1.34/1.36/1.38 的判据先以「黄灯 ＋ 账」落地**，**不立刻判红**；② **1.49 才收紧为红**——那是「零例外」真正生效的一刻。
> 🔴 **1.36 兑现了排序纪律**：SDK 腿 `appearance-ownership` 里 **只有「顶替宿主兜底面」判红**（进 `violations`），**判据 ①（新 id 必须带归属段）与「同插件跨配方重名」都是黄**（进 `advisories`，**不进退出码**）⇒ 今天官方 18 仓跑这条腿 = **红 0**。1.35 立的那条「**① 的红/黄分级绑在 1.42 上、改名轮被砍就回退到黄**」**本格用不着回退**（1.42 未砍，且它本来就还是黄）。
> ⚠️ **格数是上限，不是配额**：本轴 20 轮里**没有一格能省**；判「这条不做」必须把结论与理由写进交接段（1.36 的「不做」在下面 📦 段末）。

---

### ⑶ **你这一棒 = 1.37（`#111g` 上下文旗子归属 · 评估）**

**形状**：**零代码、零发版**——把 [07 任务书](07-任务-上下文旗子归属评估.md) 从草稿写成**正式版**（面清单 ＋ 判据措辞 ＋ 负控 ＋ 存量逐条），并把 [08](08-任务-上下文旗子归属落地.md) 写成**可直接开工的正式任务书**（照 1.35 → 1.36 的先例）。

**你要答的五个问题（07 档 §一 已列，本格不重复裁）**：

1. **宿主旗子全集**——生成式账（含「**谁读**」一侧：旗子不是只被写，还被 `when` 子句读）。
2. 🔴 **活体样本处置**：`settings` 的 **4 个裸名旗子**（`settingKey` / `settingFollowTheme` / `settingResetsToDefault` / `settingModified`）其实是**宿主的齿轮菜单**在用 ⇒ 两条路线逐条算代价：**连两侧改名** ／ **登记为「宿主公开约定面」**。
3. **插件旗子归属**：`file-tree` 11 个，其中 🔴 **`inputFocus` 是借共享组件来的**（不是它自己的概念）⇒ **必须还回**。
4. 🔴 **仲裁语义裁决**：**旗子是「状态写」不是「注册」** ⇒ **不能照搬「先者保留」**（同名旗子被两方先后写是**正常运行时行为**，不是冲突）——这一条是 07 的核心，别把 1.36 的三条分支直接搬过来。
5. **量清两条路径**：全局 `contextKey.set` vs per-call `overrides`，**别把合法用法误判成污染**。

**🔴 1.36 给你的可直接复用的四件（本格实测，不是推断）**：

1. **读数**（`node scripts/audit-nonnaming.mjs`）：**④ 插件设置的 context key —— 独立名 23 · 多方共写 6**；**⑩b 插件旗子 ∩ 宿主账（11 项）= 5**（`inputFocus` / `settingKey` / `settingFollowTheme` / `settingResetsToDefault` / `settingModified`）。`node scripts/audit-plugin-scope.mjs` 逐仓「旗子需处理」列表现成：**`file-tree` 11**（`explorerResourceCut` / `explorerClipboardEmpty` / `explorerResourceMoveableToTrash` / `explorerFocus` / `explorerItemIsFile` / `explorerResourceReadonly` / `explorerViewletCompressedFocus` / `viewHasSomeCollapsibleItem` / **`inputFocus`** / `explorerItemIsDir` / `explorerItemIsRoot`）· **`marketplace` 6** · **`settings` 4** · **`serial-monitor` 2** ⇒ 官方侧旗子面 **23 处**（与 ④ 的独立名 23 对上）。
2. **`appearanceIdGrants` 这个形状**（**按 id 记证照**，不是白名单）：`{"light": ["theme-defaults"]}`——「某个宿主保留名被**指定的一方**合法持有」的写法，1.37 判「宿主公开约定面」时**同形可用**。
3. 🔴 **出口的形状（坑 4，1.38 会原样再踩）**：新增运行时报点文案**必须落在 `console.*` 调用之内** ⇒ 写成**「出口」**（`logAppearanceIdRejection(tag, verdict, …)`）**而不是「返回值里的 message」**；`audit-i18n.mjs --strict` 的非 UI 排除是**逐行**的，写成返回值 helper 会判缺翻译、`npm run check` 在 i18n 处断。**⛔ 别动 `EXCLUDE_FILES` / `EXCLUDE_RANGES` 豁免表**（1.36 实测：改成出口形状后**零豁免**自然通过）。
4. **排序纪律**：08 落地时判据先「**黄灯 ＋ 账**」，**1.49 才收紧为红**——别在 1.38 就把旗子判据写成红。

**⚠️ 你这一棒仍是文档轮** ⇒ `npm run check` **应仍全绿**（1.36 收尾读数：**472 个 ✅ / 167 文件 / 2292 测试 / EXIT=0**）。

---

### ⑷ 收尾四件（**缺一不可**）

1. 在**本文件顶部追加一段**（只追加不改旧段），段内**必带上面的剩余任务队列整表**（把自己那格改 ✅）；
2. 同笔续写 [E6-执行清单.md 第 1 层头的轮次进度行](../../E6-执行清单.md)；
3. 勾格要勾全套；
4. **🔴 不许静默不做**：判「这条不做」必须把结论与理由写进交接段。

---

### 🔴 两条通用红线（每一棒都适用）

- **提交只按路径 `git add <本格文件>`，⛔ 禁 `git add -A` / `git add .`** —— 壳仓随时有别的会话在途（**索引是共享的**）。
- **不推**：`git push` 必须等**用户本人当次点头**；推必带代理；推完 `git ls-remote --heads origin <branch>` **对 SHA**。npm 发版照 [作者轴 npm 发版 runbook](../../../../06-发布管理/作者轴npm发版.md) 五步（**显式 `--registry`**）；⛔ **撤版只能用户本人**。

---

### 📦 本格（1.36）交付实录 —— 下面这些结论你可以直接引用

**做了什么（一句话：一条运行时仲裁 ＋ 账拆四栏＆证照 ＋ 一条 SDK 新腿 ＋ 两张映射表 ＋ 版本 6 迁移 ＋ 两个探针的口径修正 ＋ 三处 PATCH 发版）**：

| 面 | 交付 |
|:--|:--|
| **账（拆四栏 ＋ 证照）** | `scripts/host-reserved.json` 的旧一栏 `appearanceIds["dark","dark-fallback","light"]`（**一栏混两空间**）⇒ **四栏 ＋ 证照**：`appearanceRecipeIds ["dark","light"]` · `appearanceColorwayIds ["dark-fallback","light"]` · `appearanceIconThemeIds ["default"]` · `appearanceSentinels ["followTheme"]` · `appearanceIdGrants {"light":["theme-defaults"]}`（⚠️ `light` **同时在配方栏与配色栏**＝两个空间各自的一条兜底，不是重复）。生成器 ⑤ 的**盲切 1200 字符 ＋ 一正则混两空间**已修（改为按 `registerRecipe` / `colorways` 分别抓 ＋ 片段自检）；`renderRuntimeModule()` 从「四族不进运行时」改为**外观四栏 ＋ grants 进** ⇒ 新建第三份产物 `src/core/registry/host-reserved.generated.ts`。 |
| **运行时仲裁（五处 ＋ 三条分支）** | 新建 `src/core/registry/appearance/appearanceOwnership.ts`（判据机 ＋ **拒绝出口**）⇒ **五处注册本挂同一个出口**：`ThemeRegistry` 元数据本 `:152` · 配方本 `:230` · **配色子判 `:248`（带成因）** · `IconRegistry` 图标主题 `:67` · 共享图标 `:147` · `ThemeEngine/registry` flat 本 `:57`。**三条分支**：① **异插件同 id ⇒ 先者保留 ＋ 拒后者 ＋ `console.error` 点名双方**（此前是**静默 last-wins**）；② **后者有 pluginId 而前者是宿主兜底条目（`pluginId === undefined`）** ⇒ 在 `appearanceIdGrants[id]` 内**静默上位**（唯一一例 `theme-defaults` 接 `light`），否则拒 ＋ 出声；③ **同 pluginId 重注册 ⇒ 接受且不出声**（装配多阶段的合法路径）。🔴 **全程不抛错**（抛错 = 整只插件装不上，比现状更坏）。 |
| **配色被拒 ⇒ 拒整条配方** | 配方是**原子单位**——只剔一条配色 = 悄悄改掉主题内容（且把「策略」变成「静默裁剪」）⇒ 整条配方不放行，**日志里带成因句**（`（成因：配方 "x" 的配色之一被拒 ⇒ 整条配方不放行）`）。 |
| **SDK 新腿** | `packages/plugin-sdk/src/eslint/checks/appearance-ownership.ts`（**一条腿带五个空间参数**：`recipe` / `colorway` / `iconTheme` / `sharedIcon` / `language`；`language` 恒空转＝**声明但不读**的留位）＋ `lint.ts` 接线 ＋ `index.ts` 导出 ＋ `CHECK_IDS.appearanceOwnership` ＋ **23 例单测**。**严重级**：仅「顶替宿主兜底面」= 红（`violations`）；①无归属段 ＋ 同插件跨配方重名 = **黄**（`advisories`）。**18 仓实跑：红 0 · 黄 37 → 34**（34 是修掉一处腿缺陷后的读数，见下）。 |
| **🔴 SDK 腿缺陷（本格发现并修）** | 首版腿把**宿主兜底 id**（`dark` / `light` / `default` / `followTheme` / `dark-fallback`）也判成「①无归属段」⇒ **官方 `theme-defaults` 添假黄**。修法：宿主兜底栏命中 ⇒ **跳过判据 ①**（它本来就不是「插件起的名字」）。⇒ 黄灯 37 → 34。**这正是 1.35 头号发现（宽容度模型）的第二次落地**。 |
| **迁移（两张表 ＋ 版本 6）** | `ThemeEngine/constants.ts:224 / :239-241` 落两张映射表：`RECIPE_ID_MIGRATIONS`（9）＋ `COLORWAY_ID_MIGRATIONS`（16）——🔴 **不许合并**（`mint-soda` 双身份：既是配方 id 又是配色 id）；`normalizeThemeColorValue = normalizeRecipeId(normalizeColorwayId(v))`（**顺序无关**，resolver-gated：`resolver(mapped) && !resolver(value)` ⇒ 映，否则恒等 ⇒ **fail-safe = 恒等**）。**版本 6**：`registerConfigMigration({version:6, name:"E6-111f appearance-id-ownership", migrate})`，五键 = `app.theme` / `app.themeColor` / `app.mixFont` / `app.mixBackground`（本格动）＋ `app.iconTheme`（**留位不写**，见下 判定⑦）。**消费点挂接** ＋ 单测 26 绿 ＋ App 层 5 绿。 |
| **作者面两棵树** | `docs/03-插件制造/` 与 `docs/03-plugin-authoring/` 各两处（`03-contributes规范` ＋ `06-plugin.json规范`）⇒ `docs:build` 47 文件重生成 ＋ `docs:check` 绿；`packages/plugin-docs` 同步发版。 |
| **探针口径修正** | ①（06 §2.6 指定）`audit-nonnaming` ③d 剔 `langDefs`（**4 → 3** 真值）· ③e 只收 `id`（**20 → 10** 真值）；②（同笔对齐，非收紧门禁）**③e′ 显示名单列一栏**（**独立名 10**，明确标注「**不是 id**、不参与任何归属判据」）· **⑩b 加两条对账行**（配方 id ∩ 兜底配方栏 = 0，另有 1 项**持证照合法**：`light`；图标主题 id ∩ 保底栏 = 0）· ①a/①b/①c/⑩b 补**宿主命令 id 就地扫描**（**0 → 53**，此前扫不到）。③（06 §2.6 指定）`audit-plugin-scope` 把 `file.name:<显示名>` 从「配方 id」里**分列**（同笔：**显示名不再进判据** ⇒ 每只主题仓 −1）。 |
| **`npm run check`** | **EXIT=0 · 472 个 ✅ / 167 文件 / 2292 测试全绿**。（中途被拦两次并修掉：**i18n** 一处 → 改按出口形状，**零豁免**；**jscpd** 两处克隆 → 抽 `useArbitrationCleanup()` / `expectGrantAccepted()`。） |
| **发版（三处 PATCH）** | `@linkdesk/plugin-sdk` **0.1.31 → 0.1.32**（47 文件，**显式 `--registry`**）· `@linkdesk/plugin-docs` **0.1.11 → 0.1.12**（48 文件）· 🔴 **壳 `0.2.3 → 0.2.4`**（CHANGELOG 同笔）· `contracts` / `create-linkdesk-plugin` / `@linkdesk/ui` **未动**。货架轮询第 5 次翻号 ⇒ `npm run release:mark` **原子**落基线（五轴：`contracts 0.1.18` / `plugin-sdk 0.1.32` / `create-linkdesk-plugin 0.1.10` / `ui 0.3.0` / `plugin-docs 0.1.12`）⇒ `check:npm-release` 黄灯灭。 |

**🔴 探针记账（四条，改前 / 改后双读数 —— 改前用 `git show HEAD:scripts/*` 归档到 `/tmp/probe-pre/` 后用**旧脚本**跑同一批插件仓，插件仓本轮零改动）**：

| 探针 | 改前 | 改后 |
|:--|:--|:--|
| ① 官方 18 仓外观族存量 | recipe **10** · cw **17** · icon **1** · shared **0** · lang **3**（`en`/`ja`/`zh`） | **同值**（`recipe 10` 名单逐字相同；**插件仓本轮零写入** —— 18 仓 `git status --porcelain` 全 **0**，最后提交均 `2026-09-16`） |
| ② `audit-nonnaming` | ③d **4** · ③e **20** · ③a 10 · ③b 1 · ③c 0 · ③f 0 · ④ 独立名 23 / **共写 6** · 宿主命令 id **0**（扫不到） | ③d **3** · ③e **10** · ③a 10 · ③b 1 · ③c 0 · **③e′ 10（新）** · ③f 0 · ④ 23 / **6** · 宿主命令 id **53** · **⑩b 新增两行对账全 0** |
| ③ `audit-plugin-scope`（机械定义那把尺子） | **118**（`theme-*` 各 **3**、`theme-zones` **6**、`theme-defaults` **3**——含**假阳性**「主题 id 不带归属 1: light」） | **106**（`theme-*` 各 **2**、`theme-zones` **4**、🔴 **`theme-defaults` 3 → 0 假阳性已除**；其余仓未变：`file-tree 75` · `marketplace 6` · `settings 4` · `serial-monitor 2` · `editor 1`） |
| ④ `gen-host-reserved --check` | 四向一致（6 / 35 / 3 / 11 / **3（混栏）** / 1） | 四向一致（命令前缀 **6** · `app.*` 键 **35** · 伪 pluginId **3** · context key **11** · 兜底配方 **2** · 兜底配色 **2** · 保底图标主题 **1** · 哨兵 **1** · 协议 **1**） |

**🔴 负控 1–19 真跑（[06 §三](06-任务-外观族id归属落地.md) 全表）**：1/2/5 三处「改插件仓看红」——**红如期 ＋ 还原后 sha256 逐字节相同**；3/4 **绿如期（3 ＝ `appearanceIdGrants`、4 ＝ 拆栏的直接证据）**；6 五处仲裁**各一条单测**（先者条目仍在 ＋ 日志点名双方）；7 同 pluginId 重注册**第二次生效 ＋ 日志零输出**；8 **flat 本（第二本账）同款形态同语义**（最容易漏的一本）；9 **反向负控**：`theme-defaults` 接替宿主兜底 ⇒ **静默上位 ＋ 日志零输出**；10 兜底 id 写进映射表 ⇒ **测试红**；11 `followTheme` 原样保留；12 flat 显示名过两个归一函数 ⇒ **恒等**；13 版本 6 跑两遍 ⇒ **第二遍零写**；14 全新安装 ⇒ **不产出该键**；15/16 profile 导入与卸载回退链两种盘面；**17a 只改账 ⇒ 红**／**17b 只改实况 ⇒ 红**（双向都真跑）；18 **旧门禁未空转**（逐条确认没有哪条旧 check 被本格输入清零后跑绿）。
> **19 · 主题能力零回归（[29 号档 §四.4](../样式命名空间归一化/29-主题系统保护条款.md)）**：切主题（多配色 ＋ 混搭）· 玻璃三参（含 s=0 端点）· 圆角六档 ＋ 滑杆 0/16/32 · **字号 85/100/125/150** · 字体族 · 背景（panorama/mask）· 亮暗切换 —— **读数与 1.35 一致**。🔴 **三处诚实边界**（不许写成「全项实测」）：(a) `#ld-float-layer` 的**磨砂地板**当轮**盘面上没有可复现的磨砂场景** ⇒ 只核了「声明 ＋ token 链未动」；(b) **monaco 内容孤岛未挂载**（当轮没开 monaco 实例）⇒ 只核了字号/颜色两条链代码未动；(c) **`window.zoomLevel` 正交未做探针验证**（需真机改缩放）⇒ 只核了字号四档。**面清单实测**：`commitTokens` · `theme:changed` 广播链 · `preload-pool` 写池 `:root` · `#ld-float-layer` · `[data-theme="light"]` · 域表/混搭/配方键 **一处未动**（`git diff` 逐行核过：本格对主题相关的改动**全在「归属判据与注册出口」上**）。

**有意偏离与「本格不做」（🔴 不许静默不做 —— 逐条写理由）**：

| # | 事项 | 处置与理由 |
|:--:|:--|:--|
| ① | **存量 25 条改名的「执行」不在本格** | 25 条只出现在**官方插件仓**（`E:\linkdesk-plugins\official\` 18 仓本轮零改动）；**本格提交轴 = 壳 ＋ SDK**，改名执行权归 **1.42–1.48**（与 1.33/1.34 对设置键的同一口径）⇒ 判据 ① **因此仍是黄**（排序纪律：1.49 才收紧为红），**无需回退**（1.42 未砍）。 |
| ② | **i18n 零豁免** | 起手曾照 `ProfileService.ts` 先例往 `EXCLUDE_FILES` 加条目，读到坑 4 后**整条回退** ⇒ 改按出口形状重写，审计靠「文案在 `console.error` 调用内」自然通过，**豁免表一字未动**。 |
| ③ | 出口文案的空间写 **ASCII 码**（`空间 "recipe"`）而非中文别名 | 与作者在 SDK 腿里看到的一致；中文别名多一层翻译面，且与 `.id` 并读时更歧义。 |
| ④ | **配色被拒 ⇒ 拒整条配方** | 见上表（原子性 ＋ 不把策略做成静默裁剪）。 |
| ⑤ | **两条存量测试夹具随仲裁迁移** | `loader.test.ts` 原来拿**宿主兜底 id** `dark` 当主题 id ⇒ 改 `${TEST_PLUGIN_ID}.dark`；`registrationDisposers.test.ts` 的「同名覆盖」夹具原是**异插件同 id** ⇒ 改**同 pluginId**——新判据当场把它们判红，**是判据在替旧夹具纠错**（不是判据放宽）。 |
| ⑥ | SDK 腿**修了一处自身缺陷** | 宿主兜底 id 不判 ①（理由见上表）——若留着，**官方仓添假黄**。 |
| ⑦ | 🔴 **`app.iconTheme` 不改名（判定⑦）** | 它的选项文字就是 **id 原文**（走普通 `SelectBox`，**不是** `app.theme` 那条 `.name` 链）⇒ 改名 = **设置页可见文字变化** = 违**上位约束「操作体验零变化」**。要改必须**同笔**给它加 label 映射，**那是功能改动、属另一条轴** ⇒ 本格**不动**（版本 6 迁移里该键**留位不写**），登记为**残余边界**。 |
| ⑧ | **显示名（`themes[].name` / `label`）不改名、不入账** | 入账会造假红（它不是 id），改名是可见变化（1.35 裁决 4 已裁）⇒ 只**单列一栏 ③e′** 作对照读数。 |
| ⑨ | 「不做」清单（与 1.35 一致，本格照旧） | 两本账**不合并**（踩 29 号档 P1–P7）· 改 theme JSON **文件名** ❌ · 把 **token**（`--*`）拉进来 ❌ · 引**白名单/基线/棘轮** ❌ · `warn` → **`throw` 永久否决**（出口用 `console.error`）· **不给** `check-theme-audit` / `check-theme-schema` / `check-font-scale-audit` 顺手收紧（只修 06 §2.6 的两处口径 bug）· **卸载时清 `app.theme` 的新增补救** ❌（改行为；`revertThemeIfCurrent` 等**既有回退链已覆盖**）· **语言包 id / `langDefs[].id` / 图标主题 id** 不改名。 |

**本格改动面（按路径）**：壳 `src/` **25 个文件**（外观 5 ＋ ThemeEngine 9 ＋ ProfileService 2 ＋ IpcBridgeHandler/theme ＋ LanguageRegistry ＋ host-reserved.generated ＋ appearanceApplier 2 ＋ 三个测试夹具）· `packages/plugin-sdk/`（`schemas/host-reserved.json` ＋ 4 个 eslint 源 ＋ **新腿两文件**）· `packages/plugin-docs/`（4 份文档 ＋ `package.json`）· `scripts/`（`audit-nonnaming` / `audit-plugin-scope` / `gen-host-reserved` / `host-reserved.json` / `npm-release-state.json`）· `docs/` 两棵树 4 份 ＋ `CHANGELOG.md` ＋ `package.json`/lock。

---

## ← 第 1.35 轮 · 外观族 id 归属 · **评估已完成**（`#111e` · 2026-09-17）：给下一个 AI（**下一棒 = 1.36**）

> 🔴 **先说你是谁**：**你是第 6 棒，要做的是 [1.36 外观族 id 归属 · **落地**](06-任务-外观族id归属落地.md)**——**这一棒动代码**（壳 ＋ SDK）。**本档已由 1.35 写齐为正式版**，照它执行即可。
> **你不是复核者，也不是派工者**——派工在第 0 格就做完了。用户会把上一棒的**整段消息**复制给你（不裁剪），里面可能有别的东西，**以本段为准**。
> ⚠️ **如果你拿到的上下文里没有本段**，说明交接还没交到你手上 ⇒ **先问一句「我是 1.36 吗」，别默认自己在做汇报或复核**。

---

### ⑴ **本轴是干什么的**

E6 第 1 层（插件独立构建）的**第二个专项**，紧接 [样式命名空间归一化](../样式命名空间归一化/00-整理档案.md)（CSS 系列，已收官）。

**真问题一句话**：**软件里有一批「公共花名册」——谁都能往上写一个名字，写的时候不核对归属，撞了只小声嘟囔（或连嘟囔都没有）。**

**同形判据（四条全中才算）**：名字空间**扁平** ＋ **多方都能写** ＋ **写时无归属校验** ＋ **冲突静默**（最多 `console.warn`）。🔴 **第四条是关键**：`console.warn` **对用户不可见，对「被覆盖的那一方作者」也不可见**——因为他在自己机器上没装对方插件，一切正常。**「被覆盖」这件事，受害方永远收不到通知。**

**真根因（每一棒收尾都要能复述这句）**：

> **不是名字起坏了，是「谁的名字」被寄托在「取名自觉」上——账本要么没有，要么只用来打印一行字，而宿主自己的名字和第三方名字长着同一张脸。**

**🔄 收口口径（§〇c 修订后，此后一切以此为准）**：**根治 = 规则零例外**（**官方仓全部改名**，**机械定义 = `npm run audit:plugin-scope` 全 0**）＋ **运行时归属仲裁**兜住静态判不了的那类 ＋ 🔴 **每一处改名都同笔带迁移 ＋ 逐项操作实测**。**上位约束 = 操作体验零变化**（**名字可变，取值与功能不许变**）。**唯一保留的黄灯** = i18n 字典键；**三类「全局概念名」永久豁免**（文件关联扩展名 / 语言定义扩展名 / 语言包语言码）。

---

### ⑵ **各轮次是干什么的**（🔴 **剩余任务队列整表 —— 把你那一格改成 ✅ 再整表传给下一棒**）

| # | 轮 | 编号 | 任务 | 详案 | 前置 | 提交轴 |
|:--:|:--|:--|:--|:--:|:--|:--|
| 0 | 第 0 格 | `#109r` | ✅ **审查 ＋ 派工**（总方案 ＋ 20 份任务书 ＋ 清单任务格 ＋ 本交接） | [00](00-整理档案.md) | — | 文档 |
| 1 | **1.31** | `#111a` | ✅ **命令 id 与协议 id 归属 · 评估** | [01](01-任务-命令id归属评估.md) | 第 0 格 | 文档 |
| 2 | **1.32** | `#111b` | ✅ **命令 id 归属 · 落地**（归属从身份来 ＋ 两条腿 ＋ 账定稿） | [02](02-任务-命令id归属落地.md) | 1.31 | 壳 ＋ SDK |
| 3 | **1.33** | `#111c` | ✅ **设置键归属 · 评估**（复量对账 ＋ 归属语义裁决 (丙) ＋ fail-fast 永久否决 ＋ 19 键映射表 ＋ 1.34 任务书） | [03](03-任务-设置键归属评估.md) | 1.32 | 文档 |
| 4 | **1.34** | `#111d` | ✅ **设置键归属 · 落地**（保护区 ＋ 首撞保留 ＋ SDK 新腿 ＋ 账口径扩宽 ＋ 运行时模块 ＋ 四向对账 ＋ SDK PATCH） | [04](04-任务-设置键归属落地.md) | 1.33 | 壳 ＋ SDK |
| 5 | **1.35** | `#111e` | ✅ **外观族 id 归属 · 评估**（复量五空间 ＋ 四问裁决 ＋ **25 条改名映射表** ＋ **五键两表迁移设计** ＋ 账拆四栏＆证照 ＋ **06 正式版** —— **本段就是我，已做完**） | [05](05-任务-外观族id归属评估.md) | 1.34 ✅ | 文档 |
| 6 | **1.36** | `#111f` | **← 你在这** 外观族 id 归属 · 落地（**正式版任务书 1.35 已写齐**） | [06](06-任务-外观族id归属落地.md) | 1.35 ✅ | 壳 ＋ SDK |
| 7 | 1.37 | `#111g` | 上下文旗子归属 · 评估（含两条活体的路线与代价） | [07](07-任务-上下文旗子归属评估.md) | 1.36 | 文档 |
| 8 | 1.38 | `#111h` | 上下文旗子归属 · 落地 | [08](08-任务-上下文旗子归属落地.md) | 1.37 | 壳 ＋ SDK |
| 9 | 1.39 | `#111i` | i18n 与黄灯族 · 评估 | [09](09-任务-i18n与黄灯族评估.md) | 1.38 | 文档 |
| 10 | 1.40 | `#111j` | i18n 与黄灯族 · 落地 | [10](10-任务-i18n与黄灯族落地.md) | 1.39 | 壳 ＋ SDK ＋ 文档 |
| 11 | **1.41** | `#111m` | 🔴 **迁移基建**（7 个清账轮的公共前置） | [11](11-任务-迁移基建.md) | 1.40 | 壳 |
| 12 | **1.42** | `#111n-1` | 🔴 **清账 · `file-tree`**（75 处，最大的一格 —— 含 1.34 交出的 18 个设置键改名） | [12](12-任务-清账-file-tree.md) | **1.41** | 插件仓 ×1 ＋ 壳账 |
| 13 | 1.43 | `#111n-2` | **清账 · `editor`**（1 处 —— `files.autoSave` → `editor.autoSave`） | [13](13-任务-清账-editor.md) | 1.41 | 插件仓 ×1 ＋ 壳账 |
| 14 | 1.44 | `#111n-3` | **清账 · `serial-monitor`**（旗子 2） | [14](14-任务-清账-serial-monitor.md) | 1.41 | 插件仓 ×1 ＋ 壳账 |
| 15 | 1.45 | `#111n-4` | **清账 · `marketplace`**（旗子 6） | [15](15-任务-清账-marketplace.md) | 1.41 | 插件仓 ×1 ＋ 壳账 |
| 16 | **1.46** | `#111n-5` | 🔴 **清账 · `settings`**（风险最高，跨仓配对） | [16](16-任务-清账-settings.md) | 1.41 ＋ 1.37 裁决 | 插件仓 ×1 ＋ 壳 |
| 17 | **1.47** | `#111n-6` | 🔴 **清账 · 主题族 9 只**（主题系统最高审查级 —— 🔴 **1.35 订正**：原写「10 只」，`theme-iconset-pastel` 的**图标主题 id 本轮不改名** ⇒ 实际动手 **9 只**；映射表在 [05 §十三](05-任务-外观族id归属评估.md)） | [17](17-任务-清账-主题族.md) | 1.41 | 插件仓 ×9 ＋ 壳账 |
| 18 | 1.48 | `#111n-7` | **清账 · 其余插件**（0 处，但要「无面确认表」） | [18](18-任务-清账-其余插件.md) | 1.41 | 文档 |
| 19 | 1.49 | `#111k` | **收紧为红 ＋ 账定稿进 SDK ＋ 两棵树 ＋ 五轴发版** | [19](19-任务-门禁与作者面收口.md) | **1.42–1.48 全清完** | SDK(npm) ＋ 文档 ＋ 壳 |
| 20 | **1.50** | `#111l` | 🔴 **本轴收口复核**（**恒最后**） | [20](20-任务-本轴收口复核.md) | 全部 | 文档 |

**两条排序纪律（写死在 [00 §四](00-整理档案.md)）**：① **1.32/1.34/1.36/1.38 的判据先以「黄灯 ＋ 账」落地**，**不立刻判红**；② **1.49 才收紧为红**——那是「零例外」真正生效的一刻。
> ⚠️ 🔴 **一条 1.35 新立的口径（写给 1.36 和 1.42）**：**判据 ①（新 id 必须带归属段）的红/黄分级是绑在 1.42 上的**——1.35 把它定为**红**（因为存量本轮同时改名 ⇒ 反例消失），**但若 1.42 改名轮被砍，1.36 必须把它回退到黄**（否则拿新尺子量旧库存，假红一片）。出处 [05 §2.2 ★](05-任务-外观族id归属评估.md)（= [06 §二.2](06-任务-外观族id归属落地.md)）。
> ⚠️ 🔴 **另一条（账的形状变了）**：1.34 交出的 `appearanceIds` 一栏 **1.36 要拆成四栏 ＋ 新增 `appearanceIdGrants`**（照 [06 §二.1](06-任务-外观族id归属落地.md) 的 JSON 逐字落地）。**不拆栏 ⇒ 官方 `theme-defaults` 当场假红**（它的**配色** id `dark` 会撞进**配方**栏）——这是本格实测的头号发现。
> ⚠️ **格数是上限，不是配额**：本轴 20 轮里**没有一格能省**；判「这条不做」必须把结论与理由写进交接段（1.35 的十条「不做」在 [05 §二十](05-任务-外观族id归属评估.md)）。

---

### ⑶ **你这一棒 = 1.36（`#111f` 外观族 id 归属 · 落地）**

**形状**：把 [1.35](05-任务-外观族id归属评估.md) 的裁决落成代码 —— **账拆四栏 ＋ 记证照** · **一条腿 `appearance-ownership` 带五个空间参数** · **五处注册本加运行时三条分支仲裁** · **存量 25 条改名 ＋ 五键两条腿迁移** · **修两个探针的口径 bug**。**面清单在 [06 §一](06-任务-外观族id归属落地.md)（`file:line`），判据措辞在 [06 §二](06-任务-外观族id归属落地.md)（照抄），负控 19 条在 [06 §三](06-任务-外观族id归属落地.md)（必须真跑）。**

**⚠️ 你这一棒的坑（坑 1–3 是 1.32/1.34 留的，坑 4 是 1.35 新踩的——1.38 也会原样再踩）**：

- **坑 1 · 报点文案属于判据的输出面**：写新文案后**发版前拿真样本读一遍真输出**，别只跑测试（1.32 栽过：建议值算错，测试全绿而作者看到的名字是错的）。
- **坑 2 · 黄灯不是「跑绿就行」**：`bin lint` 退出码只看 eslint 红级 ⇒ 黄灯天然不进退出码。**要看 advisories 读数**，并且**同时证明它不红**（判据 ④ 就是一条黄灯，别把它写成红）。
- **坑 3 · 🔴 「探针绿」与「门禁绿」都不等于口径接上了**：1.34 实测——**只改账**会让 `--check` 同时报 `ledger-stale` ＋ `sdk-drift` ＋ `runtime-drift`；**只改实况**报 `ledger-missing`。**两条都要真跑**。
- **坑 4 · 新增运行时报点文案 ⇒ 必须落在 `console.*` 调用之内**：`audit-i18n.mjs --strict` 的非 UI 排除是**逐行**的，只认 `console.* / throw new / reportError(` 起行。**写成「返回值 helper」⇒ 判缺翻译、`npm run check` 在 i18n 处断**（1.34 实测被拦一次）。形状 = **「出口」而非「返回文案」**：照 `logHostReservedRejection()`（`src/core/registry/ConfigurationRegistry.ts:139`）写 `logAppearanceIdRejection()`。**别动 `EXCLUDE_RANGES` 豁免表**。
  - ⚠️ **同源的（SDK 侧）**：`HostReservedNames` **一个接口收全部家族**——加家族 = 加**必填**字段 ⇒ **所有既有夹具同时失效**（1.34 实测：`command-ownership.test.ts` 2 处字面量让 SDK `typecheck` 直接红）。**加家族时同笔 `grep -rn "HostReservedNames"` 全扫**，夹具补空数组。
- **坑 5 · 🔴 本格新踩的（主题族专有）**：**设置面的可见文案全部来自 `.name`，不是 `.id`** —— 改 id 之前先照 [05 §12.3](05-任务-外观族id归属评估.md) 的证据链逐条验一遍；🔴 **`app.iconTheme` 是唯一例外**（它走普通 `SelectBox`，选项文字就是 id 原文）⇒ **1.35 据此判它不改名**，你若改判必须写明理由。

**🔴 1.35 给你的四件可直接复用的东西**：

1. **一张定稿的改名映射表**（[05 §十三](05-任务-外观族id归属评估.md)，25 条，含所在仓 ＋ 声明站点）——**照抄，别再算一遍**。
2. **迁移的两个落点 ＋ 两张表的形状**（[05 §14.3](05-任务-外观族id归属评估.md)）：`RECIPE_ID_MIGRATIONS`（9）＋ `COLORWAY_ID_MIGRATIONS`（16），**必须两张**（`mint-soda` 双身份是实锤）。
3. **账的定稿 JSON**（[06 §二.1](06-任务-外观族id归属落地.md)）——四栏 ＋ `appearanceIdGrants`，**逐字落地**；生成器的 ⑤ 扫描器要顺手修（盲切 1200 字符 ＋ 一个正则混两空间）。
4. **运行时裁决的三条分支表**（[06 §二.3](06-任务-外观族id归属落地.md)）——其中「宿主兜底可被 grants 内的仓接替，其余拒」是本格新裁的一档，**1.37 会原样复用**这个形状。

**⚠️ 本格**零代码改动**——`05`/`06`/`00 §三` 三份文档 ＋ 本交接段是全部产出；`npm run check` 仍应全绿。**

---

### ⑷ 收尾四件（**缺一不可**）

1. 在**本文件顶部追加一段**（只追加不改旧段），段内**必带上面的剩余任务队列整表**（把自己那格改 ✅）；
2. 同笔续写 [E6-执行清单.md 第 1 层头的轮次进度行](../../E6-执行清单.md)；
3. 勾格要勾全套；
4. **🔴 不许静默不做**：判「这条不做」必须把结论与理由写进交接段。

---

### 🔴 两条通用红线（每一棒都适用）

- **提交只按路径 `git add <本格文件>`，⛔ 禁 `git add -A` / `git add .`** —— 壳仓随时有别的会话在途（**索引是共享的**）。
- **不推**：`git push` 必须等**用户本人当次点头**；推必带代理；推完 `git ls-remote --heads origin <branch>` **对 SHA**。npm 发版照 [作者轴 npm 发版 runbook](../../../../06-发布管理/作者轴npm发版.md) 五步（**显式 `--registry`**）；⛔ **撤版只能用户本人**。

---

### 📦 本格（1.35）交付实录 —— 下面这些结论你可以直接引用

**做了什么（一句话：一次复量 ＋ 六条裁决 ＋ 一张 25 条映射表 ＋ 一份五键两表迁移设计 ＋ 账拆栏＆证照 ＋ 06 正式版）**：

| 面 | 交付 |
|:--|:--|
| **复量读数** | 官方 18 仓：**配方 id 10 · 配色变体 id 17 · 图标主题 id 1 · 共享图标 0 · 语言包 id 3**（`langDefs[].id` 另有 1 个 `python`，**但不是名字空间**——`LangDefRegistry` 按**扩展名**作键）· **跨插件共写五空间全 0**。🔴 **并纠了总探针两处口径 bug**：③e「配方 id 20」真值 **10**（把显示名也塞进桶）· ③d「语言包 4」真值 **3**（把 `langDefs[].id` 混进语言码） |
| **裁决 1 · 两本主题账** | **不合并**（合并属重构，踩 29 号档 P1–P7）＋ **登记「两本账的键不是同一个字段」**（`ThemeRegistry` 键 = `id`，`ThemeEngine/registry` 键 = **显示名 `label`**）＋ 两侧共用同一归属判据 |
| **裁决 2 · 几条腿** | **一条腿 ＋ 五个 id 空间参数**（否决五条腿：五份重复逻辑 = 下一把漏尺子）；腿名 `appearance-ownership` |
| **裁决 3 · 配色变体 id** | 🔴 **入账 ＋ 单列一栏**（它是 `app.themeColor` 的持久化载体 ＋ `MIX_DOMAIN_KEYS.colors` 的来源 ⇒ 真害成立；**且与配方 id 不共享命名空间 ⇒ 混栏造假红**） |
| **裁决 4 · `.id` vs `.name`** | 🔴 **两个字段、两个概念、三条链**（元数据/配方本用 `id`；flat 本用 `name`；`app.theme` enum = 配方 id ∪ flat 显示名）⇒ **显示名是「第二命名空间」且今天无保护** ⇒ 入账会造假红 ⇒ 判**不入账、不改名 ＋ 登记残余边界** |
| **裁决 5 · 改名形状** | 🔴 **`<pluginId>.<旧id>`（点号）**——与**已发布**的 `judgeCommandId:137-138`（裸名支 = `pluginId + "." + 旧名`）一致；**据此同笔订正 [00 §三](00-整理档案.md) 的 `<pluginId>-` 写法** |
| **裁决 6 · 改名成不成立** | ✅ **成立**——05 §四 逃生口两条**同时**满足：**零 UI 变化**（设置面显示的是 `.name`／色点，`.id` 只用于取值与判选中）＋ **`app.theme` 无损迁移**（`normalizeThemeValue` 已覆盖 15 处消费路径，且**扩展点被明文指定**）⇒ **改名 25 条** |
| **改名映射表** | **9 配方 id ＋ 16 配色 id ＝ 25 条**（[05 §十三](05-任务-外观族id归属评估.md)）；**不改**：宿主兜底 ×5 / **图标主题 id ×1**（改名＝设置页可见文字变化）/ 语言包 id ×3 / 显示名 · 文件名 |
| **迁移设计** | **五键**（`app.theme` / `app.themeColor` / `app.mixFont` / `app.mixBackground` / `app.iconTheme`——🔴 **不是任务书写的三键**）＋ **两表**（`RECIPE_ID_MIGRATIONS` 9 / `COLORWAY_ID_MIGRATIONS` 16，**不许合并**）+ **两腿**（版本 6 配置迁移 ＋ 读时归一） |
| **账** | **拆四栏**（配方/配色/图标主题/哨兵）＋ **补两条漏项**（`default` / `followTheme`）＋ **新增 `appearanceIdGrants`**（**按 id 记证照**，不是白名单）；生成器 ⑤ 的**盲切 1200 字符 ＋ 一正则混两空间**要修；`renderRuntimeModule()` 从「四族不进运行时」改为**外观四栏 ＋ grants 进** |
| **运行时** | 🔴 **新发现**：六个注册本**每条 entry 都带 `pluginId`**（`RegistryBase.track`），**宿主兜底条目 `pluginId === undefined`** ⇒ **「是不是宿主」运行时直接可读**；但**「是不是保底 id」判不出来** ⇒ 必须读账（这是「外观四栏要进运行时副本」的依据） |
| **06 正式版** | 七项全备（面清单 `file:line` / 判据最终措辞 / 账新家族 / 负控 19 条 / 存量逐条 / 两本账结论 / 发版轴） |
| **发版** | 🔴 **本格零发版**（文档轮）。**1.36 会动 `packages/plugin-sdk/schemas/**` ⇒ SDK PATCH ＋ 壳 PATCH**（判据在 [06 §五](06-任务-外观族id归属落地.md)） |

**🔴 头号发现（1.36 必须处理，否则判据一上线就假红）**：**不拆栏 ＋ 不记证照 ⇒ 官方自己的 `theme-defaults` 当场判红**——
① `theme-defaults` 是宿主兜底 `light` 的**官方实现者**（`ThemeEngine/registry.ts:40-42` 注释明写「覆盖 fallback 主题不告警——插件主题上位是预期行为」）；② 它**同时**声明了**配色** id `dark`，而现行账 `["dark","dark-fallback","light"]` 是**一栏混两空间** ⇒ 撞上 ⇒ **假红**；③ 更直白的一条：`audit:plugin-scope` **今天就在对 `theme-defaults` 报「主题 id 不带归属 1: light」**——**它连宿主保底 id 都判成「不带归属」**。⇒ 宽容度模型（**假红会让真红失效**）在这里**已经成立了**。

**本格「不做」的十条（🔴 不许静默不做 —— 逐条理由在 [05 §二十](05-任务-外观族id归属评估.md)）**：合并两本账 ❌ · 五条腿 ❌ · **图标主题 id 改名 ❌**（可见文字变化）· **显示名改名/入账 ❌** · 卸载时清 `app.theme` 的**新增**补救 ❌（改行为；且 `revertThemeIfCurrent` 等**既有回退链已覆盖**，本格只登记）· 改 theme JSON 文件名 ❌ · 顺手收紧 `check-theme-*` ❌（**只修口径 bug**）· `warn`→`throw` ❌（永久）· token 拉进来 ❌ · 白名单/基线/棘轮 ❌。

**探针基线（本格实跑，1.36 改前 / 改后各记一次）**：`audit:plugin-scope` 汇总 = `file-tree 75 · theme-zones 6 · marketplace 6 · settings 4 · theme-* 3×7 · serial-monitor 2 · editor 1 · 其余 0` · `gen-host-reserved --check` = **四向一致**（6 / 35 / 3 / 11 / **3** / 1）· `audit:nonnaming` 读数见 [05 §9.4](05-任务-外观族id归属评估.md)。

**顺带订正的两处（同笔）**：① [00 §三](00-整理档案.md) 第 ③ 行 `<pluginId>-` → **`<pluginId>.`**（对齐 1.31/1.33 与已发布 SDK 判据）＋ 存量口径「主题 10 ＋ 配方 ≈34」→ **25 条 = 9 配方 ＋ 16 配色**；② [06](06-任务-外观族id归属落地.md) 从「待裁项草稿」重写为**正式版**（原「起点读数 10/1/20/0/4」中的 **20 和 4 是坏口径**，已订正为 **10 和 3**）。

---

## ← 第 1.34 轮 · 设置键归属 · **落地已完成**（`#111d` · 2026-09-17）：给下一个 AI（**下一棒 = 1.35**）

> 🔴 **先说你是谁**：**你是第 5 棒，要做的是 [1.35 外观族 id 归属 · **评估**](05-任务-外观族id归属评估.md)**——**这一棒只出方案、动零行代码**（复量读数 ＋ 裁决 ＋ 给 1.36 写正式任务书）。
> **你不是复核者，也不是派工者**——派工在第 0 格就做完了。用户会把上一棒的**整段消息**复制给你（不裁剪），里面可能有别的东西，**以本段为准**。
> ⚠️ **如果你拿到的上下文里没有本段**，说明交接还没交到你手上 ⇒ **先问一句「我是 1.35 吗」，别默认自己在做汇报或复核**。

---

### ⑴ **本轴是干什么的**

E6 第 1 层（插件独立构建）的**第二个专项**，紧接 [样式命名空间归一化](../样式命名空间归一化/00-整理档案.md)（CSS 系列，已收官）。

**真问题一句话**：**软件里有一批「公共花名册」——谁都能往上写一个名字，写的时候不核对归属，撞了只小声嘟囔（或连嘟囔都没有）。**

**同形判据（四条全中才算）**：名字空间**扁平** ＋ **多方都能写** ＋ **写时无归属校验** ＋ **冲突静默**（最多 `console.warn`）。🔴 **第四条是关键**：`console.warn` **对用户不可见，对「被覆盖的那一方作者」也不可见**——因为他在自己机器上没装对方插件，一切正常。**「被覆盖」这件事，受害方永远收不到通知。**

**真根因（每一棒收尾都要能复述这句）**：

> **不是名字起坏了，是「谁的名字」被寄托在「取名自觉」上——账本要么没有，要么只用来打印一行字，而宿主自己的名字和第三方名字长着同一张脸。**

**🔄 收口口径（§〇c 修订后，此后一切以此为准）**：**根治 = 规则零例外**（**官方仓全部改名**，**机械定义 = `npm run audit:plugin-scope` 全 0**）＋ **运行时归属仲裁**兜住静态判不了的那类 ＋ 🔴 **每一处改名都同笔带迁移 ＋ 逐项操作实测**。**上位约束 = 操作体验零变化**（**名字可变，取值与功能不许变**）。**唯一保留的黄灯** = i18n 字典键；**三类「全局概念名」永久豁免**（文件关联扩展名 / 语言定义扩展名 / 语言包语言码）。

---

### ⑵ **各轮次是干什么的**（🔴 **剩余任务队列整表 —— 把你那一格改成 ✅ 再整表传给下一棒**）

| # | 轮 | 编号 | 任务 | 详案 | 前置 | 提交轴 |
|:--:|:--|:--|:--|:--:|:--|:--|
| 0 | 第 0 格 | `#109r` | ✅ **审查 ＋ 派工**（总方案 ＋ 20 份任务书 ＋ 清单任务格 ＋ 本交接） | [00](00-整理档案.md) | — | 文档 |
| 1 | **1.31** | `#111a` | ✅ **命令 id 与协议 id 归属 · 评估** | [01](01-任务-命令id归属评估.md) | 第 0 格 | 文档 |
| 2 | **1.32** | `#111b` | ✅ **命令 id 归属 · 落地**（归属从身份来 ＋ 两条腿 ＋ 账定稿） | [02](02-任务-命令id归属落地.md) | 1.31 | 壳 ＋ SDK |
| 3 | **1.33** | `#111c` | ✅ **设置键归属 · 评估**（复量对账 ＋ 归属语义裁决 (丙) ＋ fail-fast 永久否决 ＋ 19 键映射表 ＋ 1.34 任务书） | [03](03-任务-设置键归属评估.md) | 1.32 | 文档 |
| 4 | **1.34** | `#111d` | ✅ **设置键归属 · 落地**（保护区 ＋ 首撞保留 ＋ SDK 新腿 ＋ 账口径扩宽 ＋ 运行时模块 ＋ 四向对账 ＋ SDK PATCH —— **本段就是我，已做完**） | [04](04-任务-设置键归属落地.md) | 1.33 | 壳 ＋ SDK |
| 5 | **1.35** | `#111e` | **← 你在这** 外观族 id 归属 · 评估（＋ 主题/配方 id 改名映射表 ＋ 三键迁移设计） | [05](05-任务-外观族id归属评估.md) | 1.34 ✅ | 文档 |
| 6 | 1.36 | `#111f` | 外观族 id 归属 · 落地 | [06](06-任务-外观族id归属落地.md) | 1.35 | 壳 ＋ SDK |
| 7 | 1.37 | `#111g` | 上下文旗子归属 · 评估（含两条活体的路线与代价） | [07](07-任务-上下文旗子归属评估.md) | 1.36 | 文档 |
| 8 | 1.38 | `#111h` | 上下文旗子归属 · 落地 | [08](08-任务-上下文旗子归属落地.md) | 1.37 | 壳 ＋ SDK |
| 9 | 1.39 | `#111i` | i18n 与黄灯族 · 评估 | [09](09-任务-i18n与黄灯族评估.md) | 1.38 | 文档 |
| 10 | 1.40 | `#111j` | i18n 与黄灯族 · 落地 | [10](10-任务-i18n与黄灯族落地.md) | 1.39 | 壳 ＋ SDK ＋ 文档 |
| 11 | **1.41** | `#111m` | 🔴 **迁移基建**（7 个清账轮的公共前置） | [11](11-任务-迁移基建.md) | 1.40 | 壳 |
| 12 | **1.42** | `#111n-1` | 🔴 **清账 · `file-tree`**（75 处，最大的一格 —— **含 1.34 交出的 18 个设置键改名**） | [12](12-任务-清账-file-tree.md) | **1.41** | 插件仓 ×1 ＋ 壳账 |
| 13 | 1.43 | `#111n-2` | **清账 · `editor`**（1 处 —— **`files.autoSave` → `editor.autoSave`**） | [13](13-任务-清账-editor.md) | 1.41 | 插件仓 ×1 ＋ 壳账 |
| 14 | 1.44 | `#111n-3` | **清账 · `serial-monitor`**（旗子 2） | [14](14-任务-清账-serial-monitor.md) | 1.41 | 插件仓 ×1 ＋ 壳账 |
| 15 | 1.45 | `#111n-4` | **清账 · `marketplace`**（旗子 6） | [15](15-任务-清账-marketplace.md) | 1.41 | 插件仓 ×1 ＋ 壳账 |
| 16 | **1.46** | `#111n-5` | 🔴 **清账 · `settings`**（风险最高，跨仓配对） | [16](16-任务-清账-settings.md) | 1.41 ＋ 1.37 裁决 | 插件仓 ×1 ＋ 壳 |
| 17 | **1.47** | `#111n-6` | 🔴 **清账 · 主题族 10 只**（主题系统最高审查级） | [17](17-任务-清账-主题族.md) | 1.41 | 插件仓 ×10 ＋ 壳账 |
| 18 | 1.48 | `#111n-7` | **清账 · 其余插件**（0 处，但要「无面确认表」） | [18](18-任务-清账-其余插件.md) | 1.41 | 文档 |
| 19 | 1.49 | `#111k` | **收紧为红 ＋ 账定稿进 SDK ＋ 两棵树 ＋ 五轴发版** | [19](19-任务-门禁与作者面收口.md) | **1.42–1.48 全清完** | SDK(npm) ＋ 文档 ＋ 壳 |
| 20 | **1.50** | `#111l` | 🔴 **本轴收口复核**（**恒最后**） | [20](20-任务-本轴收口复核.md) | 全部 | 文档 |

**两条排序纪律（写死在 [00 §四](00-整理档案.md)）**：① **1.32/1.34/1.36/1.38 的判据先以「黄灯 ＋ 账」落地**，**不立刻判红**；② **1.49 才收紧为红**——那是「零例外」真正生效的一刻。
> ⚠️ 🔴 **一条 1.33 立、1.34 复核过的口径说明**：03 档那句「必须改名存量 19 键」＝ 「**最终**必须改名 ＋ 迁移」，**执行权在 1.42/1.43**（映射表 [03 §10.2](03-任务-设置键归属评估.md)，19 行逐键新名）。**理由：1.34 的提交轴 = 壳 ＋ SDK，物理上不含插件仓。** 1.34 已把**接口**留好：账口径一扩，清账轮把迁移表落进 `src/App/config/**` 后，那 19 个旧名**自动进保留面**（退役键语义），后来者占不了。
> ⚠️ **格数是上限，不是配额**：本轴 20 轮里**没有一格能省**；判「这条不做」必须把结论与理由写进交接段。

---

### ⑶ **你这一棒 = 1.35（`#111e` 外观族 id 归属 · 评估）**

**形状（一句话）**：**复量外观族 id 的归属现状**（主题 id / 配方 id / 图标主题 id）＋ **裁决归属规则与改名形状**＋ **交出一张改名映射表 ＋ 三键迁移设计**＋ **给 1.36 写正式任务书**。**只出方案、动零行代码。**

**🔴 我（1.34）要顺手回答你的前置问题**（任务书 §十.4 点的名）：

> **`app.theme` 是「宿主保留键（进账）」这件事，与「外观族 id 的归属」是两个维度——前者是键名，后者是取值。**

具体说：`app.theme` 这个**键**已进 `configKeys` 账（1.34 落），任何插件不得声明它；但 `app.theme` 的**取值**（`"dark"` / `"theme-aurora-glass"` 这类外观族 id）**归谁、怎么命名、要不要带归属前缀，是 1.35 的面**——1.34 **一个字没动**（04 档禁区「不许动 1.35 的面」逐字保留）。同理 `app.themeColor` / `app.iconTheme` 的取值面也归你。**别把两件事算成一件**：键进了账 ≠ 取值也定了归属。

**🔴 我（1.34）给你的四件可直接复用的东西**：

1. **一套跑通的机制**（1.35 近乎零成本复用）：**生成式账**（`scripts/host-reserved.json`，家族可加）＋ **四向对账**（`--check`：账 ↔ 现场重扫 ↔ SDK 副本 ↔ 运行时模块）＋ **两条腿的形状**（`checks/<family>.ts` ＋ `lint.ts` 接线 ＋ `index.ts` 导出）＋ **保护区 ＋ 首撞保留的运行时两档**（`ConfigurationRegistry` 里的 `logHostReservedRejection` / `_configKeyOwner` 是现成模板）。
2. **`configKeys` 家族已含你会用到的宿主键**：`app.theme` / `app.themeColor` / `app.iconTheme` 等 35 个键都在账里（生成式，别手写清单）。
3. **运行时模块的形状**：`src/core/registry/host-reserved.generated.ts`（`HOST_RESERVED_CONFIG_KEYS` ＋ `HOST_PSEUDO_PLUGIN_IDS`）——**静态副本是"宿主从不注册的键"唯一拦得住的地方**（1.34 的 N2 就为证明这条）。你若新增家族，**照它的样子加生成物 ＋ 加对账向**。
4. **探针**：`npm run audit:nonnaming -- --json`（② 面读的是**账**，账没扩宽它不会红——**口径是不是真接上，只有负控能证明**）。

**⚠️ 你这一棒的四个坑（前两个是 1.32 留的，第三、四是 1.34 收尾真跑 `npm run check` 新踩的——1.36/1.38 会原样再踩）**：

- **坑 1 · 报点文案属于判据的输出面**：写新文案后**发版前拿真样本读一遍真输出**，别只跑测试（1.32 栽过：建议值算错，测试全绿而作者看到的名字是错的）。
- **坑 2 · 黄灯不是「跑绿就行」**：`bin lint` 退出码只看 eslint 红级 ⇒ 黄灯天然不进退出码。**要看 advisories 读数**，并且**同时证明它不红**。
- **坑 3 · 🔴 「探针绿」与「门禁绿」都不等于口径接上了**：1.34 实测——**只改账**会让 `--check` 同时报 `ledger-stale` ＋ `sdk-drift` ＋ `runtime-drift`（三处一起红，因为账是三份产物的源）；而**只改实况**（在 `src/App/startup.ts` 加假键）报 `ledger-missing`。**两条都要真跑**，别只跑一条就当证明。
- **坑 4 · 新增运行时报点文案 ⇒ 必须落在 `console.*` 调用之内（1.34 被 `audit-i18n.mjs --strict` 拦了一次）**：审计的非 UI 排除是**逐行**的，只认 `console.* / throw new / reportError(` 起行 ＋ 括号平衡续行。**写成「返回值 helper」（`function …Message(): string`）⇒ 中文字面量不在 console 行上 ⇒ 判缺翻译、`npm run check` 在 i18n 处断**。形状 = **「出口」而非「返回文案」**（`logXxxRejection()` 内部直接 `console.error(` 多行调用，与 `CommandRegistry.ts:148` 同形）；**别动 `EXCLUDE_RANGES` 豁免表**（那是白名单，本轴红线禁止）。⇒ 你若给外观族 id 加运行时报点，**第一版就照这个形状写**。
  - ⚠️ **另一条同源的（SDK 侧）**：`HostReservedNames` 是**一个接口收全部家族**的——加家族 = 加**必填**字段 ⇒ **所有既有夹具同时失效**（1.34 实测：`command-ownership.test.ts` 的 2 处 `HostReservedNames` 字面量让 SDK `typecheck` 直接红）。**加家族时同笔 `grep -rn "HostReservedNames"` 全扫**，夹具补空数组（夹具钉判据形状，不依赖真账内容）。

**🔴 三条命门的读数我贴在下面（你可以直接引用）**：N2（静态常量真的接上了）· N6（保护区不误伤宿主）· N11（口径真的扩宽了）——**三条都是真跑，不是推断**。

---

### ⑷ 收尾四件（**缺一不可**）

1. 在**本文件顶部追加一段**（只追加不改旧段），段内**必带上面的剩余任务队列整表**（把自己那格改 ✅）；
2. 同笔续写 [E6-执行清单.md 第 1 层头的轮次进度行](../../E6-执行清单.md)；
3. 勾格要勾全套；
4. **🔴 不许静默不做**：判「这条不做」必须把结论与理由写进交接段。

---

### 🔴 两条通用红线（每一棒都适用）

- **提交只按路径 `git add <本格文件>`，⛔ 禁 `git add -A` / `git add .`** —— 壳仓随时有别的会话在途（**索引是共享的**）。
- **不推**：`git push` 必须等**用户本人当次点头**；推必带代理；推完 `git ls-remote --heads origin <branch>` **对 SHA**。npm 发版照 [作者轴 npm 发版 runbook](../../../../06-发布管理/作者轴npm发版.md) 五步（**显式 `--registry`**）；⛔ **撤版只能用户本人**。

---

### 📦 本格（1.34）交付实录 —— 下面这些结论你可以直接引用

**做了什么（一句话：一条运行时仲裁 ＋ 一个保护区 ＋ 一条 SDK 新腿 ＋ 账口径扩宽 ＋ 一份运行时静态副本 ＋ 四向对账 ＋ 一次 SDK PATCH）**：

| 面 | 交付 |
|:--|:--|
| **壳运行时** | `ConfigurationRegistry` 两档裁决：**保护区**（非宿主身份 ＋ 键在账内 ⇒ 拒这一个键 ＋ `console.error`）＋ **首撞保留**（跨 pluginId 同键 ⇒ 归先到者，后到者的这一个键被拒 ＋ 点名双方）；**同 pluginId merge 语义一个字未动**（N5 看门）；disposer **只摘本次被接受的键**（N4 补 / 补二）；`registerConfigurationDefaults` **对称**两档（保护区同判 ＋ 跨插件同键 **warn 不拒**）；**不抛错**（拒键不拒插件） |
| **账（生成式）** | `configKeys` 口径扩宽：`src/App/config/**` ＋ **`src/App/startup.ts`** ＋ **`SCHEMA_VERSION_KEY`** ⇒ **28 → 35 键**（新增 7：`app.language` / `app.menuStyle` / `app.osIntegration.{fileMenu,dirMenu,fileAssoc}` 5 个活键 ＋ 未注册的内部标志键 `app.schemaVersion` ＋ 退役键 `app.themeColorMode`）；`$comment` 补「**退役键不腾位**」 |
| **运行时静态副本** | **新建** `src/core/registry/host-reserved.generated.ts`（生成物，进 git）——这是「宿主**从不注册**的键」唯一拦得住的地方（正确性**不押注册顺序**） |
| **四向对账** | `gen-host-reserved.mjs --check` 从三向变**四向**（新增 `runtime-drift` / `runtime-missing`）；`--self-test` **11 例全绿**（3 正 ＋ 8 负） |
| **SDK 新腿** | `checks/config-ownership.ts`（三面：声明 / 弱默认值 / 运行时身份）＋ `lint.ts` 接线（**黄灯走 `configAdvisories`**，不进退出码）＋ `index.ts` 导出 ＋ `disable.ts` 的 `CHECK_IDS.configOwnership` ＋ **15 例单测** |
| **schema ＋ 身份** | 四份 `plugin.schema.json` 的 `pluginId` pattern 加三禁（`app` / `appearance` / `update`）＋ `ldk-` 前缀（**逐字节相等已核**）；`validate.ts` ＋ 壳 `manifest.ts` 两份**同笔**加显式伪身份分支（从账读清单，不硬编码） |
| **作者面文档** | 两棵树（中英）各两处：`03-*contributes规范` 的 §3.4 / §3.5（键归属两级判据 ＋ 保留面账 ＋ defaults 三态）＋ `06-plugin.json规范` 的「**身份的三个禁用**」（含报错形态）；`docs:build` 47 文件重生成 |
| **发版** | `@linkdesk/plugin-sdk` **0.1.30 → 0.1.31**（PATCH）· `@linkdesk/plugin-docs` **0.1.10 → 0.1.11**（第五根轴，文档内容变）· 壳 **0.2.2 → 0.2.3**（0.x 向后兼容 ⇒ patch 位，CHANGELOG 同笔）· `contracts` **未动**（无 `window.linkdesk.*` 面变化） |

**负控实测读数（全部真跑，不是推断）**：

| # | 形态 | 实测 |
|:--:|:--|:--|
| **N1** | 插件声明 `app.theme` | ✅ 红：键不进注册表 ＋ 一条 error（单测） |
| **N2** 🔴**命门** | 插件声明 `app.schemaVersion` | ✅ 红：**只有静态副本拦得住**（宿主从不注册它 ⇒ 判据 3 够不着）——**证明 A8 的静态常量真接上了** |
| **N3** | 无前缀非保留键 `files.ghost` | ✅ **黄不是红**（壳侧不拒 ＋ 零输出；黄灯在 SDK 腿） |
| **N4** | `alpha` / `beta` 同键 `x.y` | ✅ 红：值仍属 alpha ＋ error 点名两侧；**补**：beta 被拒后 dispose **不摘** alpha 的键；**补二**：alpha dispose 后 beta 可重新注册（不残留所有权） |
| **N5** | 同 pluginId `{a,b}` 再 `{b,c}` | ✅ **不变**：`{a,b,c}` ＋ **日志零输出**（反向负控） |
| **N6** 🔴**命门** | 宿主身份注册 `app.*` | ✅ **不变**：`app` / `appearance` / `update` 三个身份全正常进 ＋ 零输出（**保护区不误伤宿主**） |
| **N8** | 插件 defaults 建议 `app.theme` | ✅ 红：不登记 ＋ error（同一次调用里的合规键照常登记 = 拒键不拒整份） |
| **N9** | 两插件 defaults 同键 | ✅ 黄：warn 出声 ＋ **两边都登记**（不拒） |
| **N10** | 只改账（加假 `app.ghost`） | ✅ 红，且**三处一起红**：`ledger-stale` ＋ `sdk-drift` ＋ `runtime-drift`（账是三份产物的源，改一处即三处不一致——这是**正确**形态） |
| **N11** 🔴**命门** | 只改实况（`src/App/startup.ts` 加假 `app.zzz`） | ✅ 红：`ledger-missing` 点名 `[app.zzz]`；**还原后 sha256 与改前逐字节相同**（`1b304351…` → 改 → 同值）＋ `--check` 回绿。**这就是口径真的扩宽了的直接证据**（旧口径扫不到 `startup.ts`，这条不会红） |
| **N12** | 改运行时副本一个字 / 删文件 | ✅ `runtime-drift`（`app.theme`→`app.themeX`）／`runtime-missing`（删文件），逐条贴了报错行，还原后绿 |
| **N13** | schema 拷贝不同步 | ⚠️ **任务书配方需校正**：`check-plugin-schema-sync` 守的是 **3 份**（`public/` live ＋ `docs/03-插件制造/` ＋ `packages/plugin-sdk/schemas/`），**「四份改三漏一」对它无效**（那三份正是它的比对集，同改即同步——实测 ✅ 绿）。**真红配方 = 3 份里改任意 ≤2 份**（实测红，报 `docs/03-插件制造/plugin.schema.json 与 public/… 不一致`）；**第 4 份**（`packages/plugin-docs/docs/zh/`，生成物）**由 `generate-plugin-docs.mjs --check` 守**（实测：只改它 ⇒ 红） |
| **N14** | 18 仓 ＋ 官方目录 ＋ `third-party/` 零迁移面 | ✅ **复量 = 全 0**：18 仓 `pluginId` ∩ 保留面 = **0** · **声明键 47 个 ∩ 宿主账 = 0**（零个 `app.*`）· `configurationDefaults` 命中 = 0 · `third-party/` 只有 `README.md`。⚠️ 同时量到**读面**：`file-tree` 读 `app.uiFontScale` · `settings` 读 `app.theme` / `app.appearanceMode` / `app.themeColor` / `app.surfaceRadius` 等 · `@linkdesk/ui` dist 读 `app.theme` / `app.appearanceMode`——**读不被禁**（判据只看**声明面**），但这批读者是**后续任何"宿主键改名"动作必须先算清的连带面**（给 1.35 及之后） |

**四条有意偏离（都写明了理由，不是漏做）**：

1. **账口径 28 → 35（不是任务书写的 34）**：03/04 档按「`startup.ts` 5 个活键 ＋ `SCHEMA_VERSION_KEY` 1 个」算成 +6 ⇒ 34；**实测 +7**——多出的 `app.themeColorMode` 是**退役键**（宿主仍读/删它：`startup.ts:246-251`），而**退役键不腾位**。**35 是正确读数**，34 是当时的估算。
2. **壳 bump 了（0.2.2 → 0.2.3），04 §八 写的「壳不 bump」是错的**：`check-version-bump.mjs` 只要求**类别前缀**，但**硬约束 22 的「bump ＋ CHANGELOG」是软件轴规矩**，且 **1.32 先例正是 0.2.1 → 0.2.2**（有 `src/**` 生产代码改动）。⇒ 照纪律 bump，CHANGELOG 同笔。
3. **`validate.ts` 没把伪身份折进 `SAFE_PLUGIN_ID` 正则**（04 §六表格 :218 的字面写法），改为**显式分支 ＋ 从账读清单**：那个正则的语义是「字符集 ＋ 路径安全」，三个调用点共用 ⇒ 把「身份禁用」塞进去会让**错误消息指错方向**（作者以为字符不合法）。⇒ 报点独立成一条，理由写在代码注释里。
4. **5 个存量测试文件的夹具迁移**（04 档没预见）：保护区一上线，**壳自己的测试夹具**里「非宿主 pluginId 注册 `app.*` 键」的写法会被拦（测机制变成了测保护区）。按两条原则修：**测的是宿主键 ⇒ 用宿主身份注册**（`HOST_CFG_ID = "appearance"`）；**纯机制测试 ⇒ 键名改带自己前缀**（`test-plugin.*`）。⇒ **1.33 §八「零迁移面」只覆盖了插件声明面，没覆盖壳测试夹具**——这是本格补上的一块。

**顺带订正的两处（同笔）**：① `getMergedSchema()` 里那条「重复 key ⇒ warn ＋ last-wins」分支在改造后**已不可达** ⇒ 改为 **defensive tripwire**（`console.error` ＋ 注释写明「⛔ 不许删成"已确认死代码"」——它是防退化触发线）；② `ConfigurationRegistry.ts:120-121` 那段陈旧自述（「待 E2c #19b 审计后改 fail-fast」）按 04 §2.6 原文替换，**fail-fast 永久否决**的三条理由写进代码。

**🔴 收尾 `npm run check` 拦下的两处（都已修 —— 这是本节 ⑶「坑 4」的实证）**：① 保护区拒绝文案原先写成**返回值 helper** ⇒ 中文字面量不在 `console.*` 行上 ⇒ `audit-i18n.mjs --strict` 判缺翻译、门禁在 i18n 处断 ⇒ 改为 **`logHostReservedRejection()`「出口」形状**（内部直接 `console.error(` 多行调用，文案仍只有一份）；② `HostReservedNames` 加家族 ⇒ **既有两个 `HostReservedNames` 夹具同时失效** ⇒ SDK `typecheck` 直接红 ⇒ 夹具补空数组。**修完重跑：`npm run check` EXIT=0**（162 文件 / **2208 测试全过**；eslint `--max-warnings 0` · duplication · knip 均绿；四向读数 6 / **35** / 3 / 11 / 3 / 1）。

**发版实测读数**（`npm publish --registry=https://registry.npmjs.org` **真发**，**货架 `npm view <pkg> version --registry=…` 核对后**才落基线）：
- `@linkdesk/plugin-sdk@0.1.31` —— **45 文件**（上一版 43；+2 = 新腿 `dist/eslint/checks/config-ownership.{js,d.ts}`）✔ 货架可见
- `@linkdesk/plugin-docs@0.1.11` —— **48 文件** ✔ 货架可见
- `npm run release:mark` ⇒ 五轴基线同笔落：`contracts 0.1.18`(2 文件) · `plugin-sdk 0.1.31`(45) · `create-linkdesk-plugin 0.1.10`(18) · `@linkdesk/ui 0.3.0`(2) · `plugin-docs 0.1.11`(48)
- ⚠️ **货架有复制延迟**（实测：docs 先可见，SDK 晚约 2 分钟；其间 `npm view @linkdesk/plugin-sdk` 仍报 0.1.30）——**看到旧版本号不是没发成功，别重发**，以轮询 `npm view` 为准。
- `contracts` 未发（无 `window.linkdesk.*` 面变化，0.1.18 保持）。**下一棒（1.35）若要在作者仓里验 lint，用的是 0.1.31 那份。**

---

## ← 第 1.33 轮 · 设置键归属 · **评估已完成**（`#111c` · 2026-09-17）：给下一个 AI（**下一棒 = 1.34**）

> 🔴 **先说你是谁**：**你是第 4 棒，要做的是 [1.34 设置键归属 · 落地](04-任务-设置键归属落地.md)**——🔴 **这一棒要动代码**（壳运行时的归属仲裁与保护区 ＋ **SDK 新腿** ＋ 账的口径扩宽 ＋ 一次 **SDK PATCH**）。**任务书已经由我（1.33）写齐**，你不需要再做评估。
> **你不是复核者，也不是派工者**——派工在第 0 格就做完了。用户会把上一棒的**整段消息**复制给你（不裁剪），里面可能有别的东西，**以本段为准**。
> ⚠️ **如果你拿到的上下文里没有本段**，说明交接还没交到你手上 ⇒ **先问一句「我是 1.34 吗」，别默认自己在做汇报或复核**。
> ⚠️ **额外提示**：**1.32 与 1.33 是同一个会话连着做完的**（用户当次拍板「你继续 1.33 轮」，例外于「一个会话只做一格」）⇒ 下面 1.32 的那一段也是我写的，两段可以放心互相引用。

---

### ⑴ **本轴是干什么的**

E6 第 1 层（插件独立构建）的**第二个专项**，紧接 [样式命名空间归一化](../样式命名空间归一化/00-整理档案.md)（CSS 系列，已收官）。

**真问题一句话**：**软件里有一批「公共花名册」——谁都能往上写一个名字，写的时候不核对归属，撞了只小声嘟囔（或连嘟囔都没有）。**

**同形判据（四条全中才算）**：名字空间**扁平** ＋ **多方都能写** ＋ **写时无归属校验** ＋ **冲突静默**（最多 `console.warn`）。🔴 **第四条是关键**：`console.warn` **对用户不可见，对「被覆盖的那一方作者」也不可见**——因为他在自己机器上没装对方插件，一切正常。**「被覆盖」这件事，受害方永远收不到通知。**

**真根因（每一棒收尾都要能复述这句）**：

> **不是名字起坏了，是「谁的名字」被寄托在「取名自觉」上——账本要么没有，要么只用来打印一行字，而宿主自己的名字和第三方名字长着同一张脸。**

**🔄 收口口径（§〇c 修订后，此后一切以此为准）**：**根治 = 规则零例外**（**官方仓全部改名**，**机械定义 = `npm run audit:plugin-scope` 全 0**）＋ **运行时归属仲裁**兜住静态判不了的那类 ＋ 🔴 **每一处改名都同笔带迁移 ＋ 逐项操作实测**。**上位约束 = 操作体验零变化**（**名字可变，取值与功能不许变**）。**唯一保留的黄灯** = i18n 字典键；**三类「全局概念名」永久豁免**（文件关联扩展名 / 语言定义扩展名 / 语言包语言码）。

---

### ⑵ **各轮次是干什么的**（🔴 **剩余任务队列整表 —— 把你那一格改成 ✅ 再整表传给下一棒**）

| # | 轮 | 编号 | 任务 | 详案 | 前置 | 提交轴 |
|:--:|:--|:--|:--|:--:|:--|:--|
| 0 | 第 0 格 | `#109r` | ✅ **审查 ＋ 派工**（总方案 ＋ 20 份任务书 ＋ 清单任务格 ＋ 本交接） | [00](00-整理档案.md) | — | 文档 |
| 1 | **1.31** | `#111a` | ✅ **命令 id 与协议 id 归属 · 评估**（读数复量 ＋ 四路线裁决 ＋ 26 名映射表 ＋ 1.32 正式任务书） | [01](01-任务-命令id归属评估.md) | 第 0 格 | 文档 |
| 2 | **1.32** | `#111b` | ✅ **命令 id 归属 · 落地**（归属从身份来 ＋ 两条腿 ＋ 账定稿） | [02](02-任务-命令id归属落地.md) | 1.31 | 壳 ＋ SDK |
| 3 | **1.33** | `#111c` | ✅ **设置键归属 · 评估**（复量对账 ＋ 归属语义裁决 **(丙)** ＋ fail-fast 永久否决 ＋ **19 键改名映射表 ＋ 迁移设计** ＋ 伪 id 全量 ＋ 相邻账结论 ＋ **1.34 正式任务书** —— **本段就是我，已做完**） | [03](03-任务-设置键归属评估.md) | 1.32 | 文档 |
| 4 | **1.34** | `#111d` | **← 你在这** 设置键归属 · 落地（保护区 ＋ 仲裁 ＋ 新腿 ＋ **账口径 28→34** ＋ 运行时模块 ＋ SDK PATCH） | [04](04-任务-设置键归属落地.md) | 1.33 ✅ | 壳 ＋ SDK |
| 5 | 1.35 | `#111e` | 外观族 id 归属 · 评估 | [05](05-任务-外观族id归属评估.md) | 1.34 | 文档 |
| 6 | 1.36 | `#111f` | 外观族 id 归属 · 落地 | [06](06-任务-外观族id归属落地.md) | 1.35 | 壳 ＋ SDK |
| 7 | 1.37 | `#111g` | 上下文旗子归属 · 评估 | [07](07-任务-上下文旗子归属评估.md) | 1.36 | 文档 |
| 8 | 1.38 | `#111h` | 上下文旗子归属 · 落地 | [08](08-任务-上下文旗子归属落地.md) | 1.37 | 壳 ＋ SDK |
| 9 | 1.39 | `#111i` | i18n 与黄灯族 · 评估 | [09](09-任务-i18n与黄灯族评估.md) | 1.38 | 文档 |
| 10 | 1.40 | `#111j` | i18n 与黄灯族 · 落地 | [10](10-任务-i18n与黄灯族落地.md) | 1.39 | 壳 ＋ SDK ＋ 文档 |
| 11 | **1.41** | `#111m` | 🔴 **迁移基建**（7 个清账轮的公共前置） | [11](11-任务-迁移基建.md) | 1.40 | 壳 |
| 12 | **1.42** | `#111n-1` | 🔴 **清账 · `file-tree`**（75 处，最大的一格 —— **含本格交出的 18 个设置键改名**） | [12](12-任务-清账-file-tree.md) | **1.41** | 插件仓 ×1 ＋ 壳账 |
| 13 | 1.43 | `#111n-2` | **清账 · `editor`**（1 处 —— **`files.autoSave` → `editor.autoSave`**） | [13](13-任务-清账-editor.md) | 1.41 | 插件仓 ×1 ＋ 壳账 |
| 14 | 1.44 | `#111n-3` | **清账 · `serial-monitor`**（旗子 2） | [14](14-任务-清账-serial-monitor.md) | 1.41 | 插件仓 ×1 ＋ 壳账 |
| 15 | 1.45 | `#111n-4` | **清账 · `marketplace`**（旗子 6） | [15](15-任务-清账-marketplace.md) | 1.41 | 插件仓 ×1 ＋ 壳账 |
| 16 | **1.46** | `#111n-5` | 🔴 **清账 · `settings`**（风险最高，跨仓配对） | [16](16-任务-清账-settings.md) | 1.41 ＋ 1.37 裁决 | 插件仓 ×1 ＋ 壳 |
| 17 | **1.47** | `#111n-6` | 🔴 **清账 · 主题族 10 只**（主题系统最高审查级） | [17](17-任务-清账-主题族.md) | 1.41 | 插件仓 ×10 ＋ 壳账 |
| 18 | 1.48 | `#111n-7` | **清账 · 其余插件**（0 处，但要「无面确认表」） | [18](18-任务-清账-其余插件.md) | 1.41 | 文档 |
| 19 | 1.49 | `#111k` | **收紧为红 ＋ 账定稿进 SDK ＋ 两棵树 ＋ 五轴发版** | [19](19-任务-门禁与作者面收口.md) | **1.42–1.48 全清完** | SDK(npm) ＋ 文档 ＋ 壳 |
| 20 | **1.50** | `#111l` | 🔴 **本轴收口复核**（**恒最后**） | [20](20-任务-本轴收口复核.md) | 全部 | 文档 |

**两条排序纪律（写死在 [00 §四](00-整理档案.md)）**：① **1.32/1.34/1.36/1.38 的判据先以「黄灯 ＋ 账」落地**，**不立刻判红**（此时官方仓还没改名，判红 = 插件仓一段无谓的红窗）；② **1.49 才收紧为红**——那是「零例外」真正生效的一刻。
> ⚠️ 🔴 **一条 1.33 新增的口径说明（针对 03 档 §四禁区里那句「必须改名存量 19 键」）**：那条要求的是「**最终**必须改名 ＋ 迁移」，**不是**「必须在 1.34 这一格里改名」。**执行权在 1.42/1.43**（[00 §四 轮次表](00-整理档案.md) 派的），理由很硬：**1.34 的提交轴 = 壳 ＋ SDK，物理上不含插件仓** ⇒ 它改不了插件侧的 `plugin.json` 与 25 个源码消费点。**映射表（19 行，逐键给了新名）见 [03 §10.2](03-任务-设置键归属评估.md)**；12/13 档已写「映射表来源：1.33」＝ 我**交表**、清账轮**执行**。
> ⚠️ **格数是上限，不是配额**：本轴 20 轮里**没有一格能省**（1.41 与 1.42–1.48 是 §〇c 扩面后新加的）；判「这条不做」必须把结论与理由写进交接段。

---

### ⑶ **你这一棒 = 1.34（`#111d` 设置键归属 · 落地）**

**直接读 [04-任务-设置键归属落地.md](04-任务-设置键归属落地.md)**——**我已把它重写成正式任务书**（面清单带 `file:line` · 判据最终措辞 · 账口径 · **14 条负控** · 存量处置 · 注释订正 · 发版轴七项全备）。**本段只是导读；数字与措辞以 04 档为准。**

**你这一棒的形状（一句话）**：**一条运行时归属仲裁（首撞保留）＋ 一个保护区（宿主 34 个键）＋ 一条 SDK 新腿（红黄两级）＋ 账口径从 28 扩到 34＋ 三份产物同源**。

**🔴 我（1.33）给你留了三件可直接复用的东西 —— 别重新设计**：

1. **`04` 档的面清单是逐行核过的、带 `file:line`**：壳侧 A1–A8 · 生成侧 B1–B8 · SDK 侧 C1–C6 · schema D1–D6 · 文档 E1–E4。**照单改，别自己找**（我核过的行号：`ConfigurationRegistry.ts:108-109/120-131/132-138/140-153/216-239`、`contributions.ts:49/53/98`、`command-ownership.ts:52-66/105-135`、`lint.ts:195-215`、`validate.ts:51/159`、`manifest.ts:64/92`、四份 `plugin.schema.json:16`）。
2. **19 键改名映射表已定稿**（[03 §10.2](03-任务-设置键归属评估.md)，19 行，含**逐键新名**）＋ **形状裁决 = (A) 只换第一段、词干零变化**（`explorer.confirmDelete` → `file-tree.confirmDelete`）。⚠️ **两种形状我都机械核过、都零碰撞**——选 (A) 是因为**全轴一条形状**（1.32 已发的 SDK 报点文案说的就是这句），不是因为碰撞。
3. **四份 schema 的 `pattern` 现状已核**：`public/` · `docs/03-插件制造/` · `packages/plugin-sdk/schemas/` · `packages/plugin-docs/docs/zh/`（**第 4 份是生成的**）——`check-plugin-schema-sync` 守着，**改 3 份漏 1 份当场红**。

**⚠️ 你这一棒的三个坑（前两个是 1.32 留的、我确认仍在；第三个是我这一格新发现的）**：

- **坑 1 · 报点文案属于判据的输出面**（1.32 踩过）：本格你要写**新的报点文案**（[04 §二](04-任务-设置键归属落地.md) 已给最终措辞）⇒ **发版前拿真样本读一遍真输出**，别只跑测试。
- **坑 2 · 黄灯不是「跑绿就行」**：`bin lint` 的**退出码只看 eslint 红级** ⇒ 本格新增的**黄灯（前缀）天然不进退出码**。**别把「exit 0」当判据通过的证据**——要看 `configAdvisories` 读数（N3 就是为这条写的：**要同时证明它不红**）。
- **坑 3 · 🔴 探针读的是账，不是源码**：`npm run audit:nonnaming` 的 ② 面**从账里读宿主键数**。账没扩宽前它显示 28、扩宽后显示 34——**两次都"绿"**，你唯一能分辨的方法是**看那个数字**。⇒ 改完口径**必须真跑 N11**（在 `src/App/startup.ts` 加一个假 `app.zzz` 键，`--check` 该红）。**看不见的红 = 没接上。**

**🔴 三条命门（我判据表里标出来的，你收尾必须贴读数）**：**N2**（`app.schemaVersion` 被拦 ⇒ 证明**静态常量真的接上了**，因为宿主从不注册这个键、判据 3 够不着它）· **N6**（宿主自己注册 `app.*` 不被误伤）· **N11**（口径真的扩宽了）。⚠️ N2 断言时**别真写 `settings.json`**（会污染迁移标志），用内存夹具。

---

### ⑷ 收尾四件（**缺一不可**）

1. 在**本文件顶部追加一段**（只追加不改旧段），段内**必带上面的剩余任务队列整表**（把自己那格改 ✅）；
2. 同笔续写 [E6-执行清单.md 第 1 层头的轮次进度行](../../E6-执行清单.md)；
3. 勾格要勾全套；
4. **🔴 不许静默不做**：判「这条不做」必须把结论与理由写进交接段。

---

### 🔴 两条通用红线（每一棒都适用）

- **提交只按路径 `git add <本格文件>`，⛔ 禁 `git add -A` / `git add .`** —— 壳仓随时有别的会话在途（**索引是共享的**；实测工作区躺着别的会话的 `docs/06-记忆系统整理/02-索引重写规范.md` —— 2026-09-17 已由用户点头单独提交，**但那不代表别的文件也能顺手带上**）。
- **不推**：`git push` 必须等**用户本人当次点头**；推必带代理；推完 `git ls-remote --heads origin <branch>` **对 SHA**。npm 发版照 [作者轴 npm 发版 runbook](../../../../06-发布管理/作者轴npm发版.md) 五步（**显式 `--registry`**）；⛔ **撤版只能用户本人**。

---

### 📦 本格（1.33）交付实录 —— 下面这些结论你可以直接引用

**做了什么（一句话：只出方案、动零行代码 —— 两个文档 ＋ 四件收尾）**：

| 面 | 落点 | 干了什么 |
|:--|:--|:--|
| **评估结论** | [03 档](03-任务-设置键归属评估.md) **新增 §八–§十七**（原 §〇–§七 任务书**原文保留**） | 复量对账（**逐项相符，口径写明"处/名/站点"**）· **伪 id 全量实测 = 3** · **零迁移面实证** · **19 键映射表 ＋ 51 站点引用清单 ＋ 迁移设计 ＋ 三条体验实证** · **归属语义裁决 (丙) = (甲)＋(乙)** · **fail-fast 永久否决** · **相邻账结论** · 判据草案 ＋ 两腿分工 · 残余边界 9 条 ＋ 探针记账 |
| **1.34 正式任务书** | [04 档](04-任务-设置键归属落地.md) **整篇重写** | 八项要做 ＋ **面清单 A1–A8 / B1–B8 / C1–C6 / D1–D6 / E1–E4（带 `file:line`）** ＋ **六条判据的最终措辞** ＋ **14 条负控（含 2 条反向）** ＋ 账口径（28→34）＋ 存量处置 ＋ 注释订正原文 ＋ 发版轴 |
| **旧结论作废** | 04 档旧版里的「**不许改名存量 19 键**」「大概率 = 不改名 ＋ 登记为共享命名空间」 | 🔴 **已按 §〇c 与 1.33 裁决重写**（改名 ＋ 迁移，执行方 = 1.42/1.43）——**别在别处再引用那两条旧措辞** |

**判据自检（[03 §三](03-任务-设置键归属评估.md) 十一条，逐条 ✅）**：读数自量 ✅ · 归属语义有裁决 ✅ · 欠账正面回答 ✅ · 存量 19 键有结论 ✅ · 伪 pluginId 量全 ✅ · 零迁移面实证 ✅ · 相邻账点名 ✅ · 判据草案可落地 ✅ · 1.34 任务书写齐 ✅ · **零代码改动 ✅** · 探针记账 ✅。

**🔴 本格头号发现（1.34 的头号交付物）**：**账 `configKeys` 少 6 个**——生成器只扫 `src/App/config/**`，而宿主真键还分布在 `src/App/startup.ts`（**5 个活键**：`app.language` · `app.menuStyle` · `app.osIntegration.{fileMenu,dirMenu,fileAssoc}`）与 `SCHEMA_VERSION_KEY`（**`app.schemaVersion`，未注册的内部标志键**）。**真害两条**：插件声明 `app.language` 等宿主键 ⇒ **顶替宿主设置而判据看不见**；插件声明 `app.schemaVersion` ⇒ **与迁移版本标志同键**（迁移 `deleteMany`／版本提升会把插件的值当版本号用）。⇒ **1.34 扩口径 ＋ 加运行时静态常量**（[04 §四](04-任务-设置键归属落地.md)）。

**探针记账（改前 = 改后，本格零代码改动）**：

| 读数 | 改前（本格开工） | 改后（本格收尾） | 说明 |
|:--|:--:|:--:|:--|
| 设置键家族（独立名 / 合规 / 不合规） | 47 / 28 / 19 | **47 / 28 / 19** | **必须相同**——这就是「零代码改动」判据的证据 |
| 宿主 `app.*` 键（账） | 28 | **28** | 本格**只量不改**（扩宽归 1.34） |
| 伪 pluginId（账） | 3 | **3** | 同上 |
| `npm run audit:nonnaming` ⑩b（插件键 ∩ 账） | 0 | **0** | 同上 |
| `npm run check` | 绿 | **绿** | 本格末次真跑：**161 文件 / 2183 测试 全绿** |

⚠️ **探针的落点**：本格的取证脚本（声明面 / 形状核验 / 引用点）是**临时件**（`%TEMP%\e6-1.33-{probe,shape,refs}.mjs`，**不在仓内**）——评估轮不动仓。**复跑命令见 [03 §十六](03-任务-设置键归属评估.md)**。若 1.34 需要长期用某一条，照 1.32 的先例搬进 `scripts/`（并接进 check 链时要带 `--self-test`）。

**🔴 发现不修（诚实记账 —— 都是「归别格」不是「不做」）**：

| # | 发现 | 为什么不修 | 归谁 |
|:--:|:--|:--|:--|
| 1 | **账少 6 个宿主键** | 改扫描口径 = **改公共面判据**，必须与 SDK 副本 ＋ `--check` **同笔**；本格是评估轮（零代码） | **1.34** |
| 2 | **19 键改名** | 本格提交轴 = 文档；改名要动插件仓（声明的 `plugin.json` ＋ 25 个源码消费点） | **1.42（18）/ 1.43（1）** |
| 3 | **K 迁移表落点** | 迁移代码要落进 `src/App/config/**`（壳源码）⇒ 与改名同笔 | **1.42/1.43**（前置 1.41） |
| 4 | **`configurationDefaults` 面没进 1.32 探针** | 1.32 只量了 `contributes.configuration`；本格**已补量**（1 仓 2 键、全合规）但**没改探针** | **1.34 的 SDK 新腿**（判据不许有盲面） |
| 5 | **E4 期历史文档 ~20 处旧键名** | 历史存档，改了反而与当时的实现不符 | 不修（**明文决定**） |
| 6 | **`SettingsView` 显示的是键名原文**（`SettingRow.tsx:65`） | 改名后标签会变 = **明文的体验取舍**（写进迁移说明） | 1.42/1.43 的迁移说明 |

**两条我最想让你别再走一遍的弯路**：① 我第一版探针把 `configKeys` **当对象**处理，于是打印出「② 账 configKeys（28）：0 · 1 · … · 27」——**它是 28 个字符串的数组**；② 我用 `registerConfiguration(…){0,4000}` 懒匹配去扫引用点，**漏掉了整个 `appearance.ts`**，差点把「账里有而脚本没扫到」报成真缺陷（**识别为脚本假象后改口径重扫**）。⇒ **读数先问口径，假红会让真红失效。**

---

## ← 第 1.32 轮 · 命令 id 与协议 id 归属 · **落地已完成**（`#111b` · 2026-09-17）：给下一个 AI（**下一棒 = 1.33**）

> 🔴 **先说你是谁**：**你是第 3 棒，要做的是 [1.33 设置键归属 · 评估](03-任务-设置键归属评估.md)**——**评估轮：只出方案，不动一行代码**（改代码是 1.34）。1.31 就是这样交给我的，我这一棒才动的手。
> **你不是复核者，也不是派工者**——派工在第 0 格做完了。用户会把上一棒的**整段消息**复制给你（不裁剪），里面可能有别的东西，**以本段为准**。
> ⚠️ **如果你拿到的上下文里没有本段**，说明交接还没交到你手上 ⇒ **先问一句「我是 1.33 吗」，别默认自己在做汇报或复核**。

---

### ⑴ **本轴是干什么的**

E6 第 1 层（插件独立构建）的**第二个专项**，紧接 [样式命名空间归一化](../样式命名空间归一化/00-整理档案.md)（CSS 系列，已收官）。

**真问题一句话**：**软件里有一批「公共花名册」——谁都能往上写一个名字，写的时候不核对归属，撞了只小声嘟囔（或连嘟囔都没有）。**

**同形判据（四条全中才算）**：名字空间**扁平** ＋ **多方都能写** ＋ **写时无归属校验** ＋ **冲突静默**（最多 `console.warn`）。🔴 **第四条是关键**：`console.warn` **对用户不可见，对「被覆盖的那一方作者」也不可见**——因为他在自己机器上没装对方插件，一切正常。**「被覆盖」这件事，受害方永远收不到通知。**

**真根因（每一棒收尾都要能复述这句）**：

> **不是名字起坏了，是「谁的名字」被寄托在「取名自觉」上——账本要么没有，要么只用来打印一行字，而宿主自己的名字和第三方名字长着同一张脸。**

**🔄 收口口径（§〇c 修订后，此后一切以此为准）**：**根治 = 规则零例外**（**官方仓全部改名**，**机械定义 = `npm run audit:plugin-scope` 全 0**）＋ **运行时归属仲裁**兜住静态判不了的那类 ＋ 🔴 **每一处改名都同笔带迁移 ＋ 逐项操作实测**。**上位约束 = 操作体验零变化**（**名字可变，取值与功能不许变**）。**唯一保留的黄灯** = i18n 字典键；**三类「全局概念名」永久豁免**（文件关联扩展名 / 语言定义扩展名 / 语言包语言码）。

---

### ⑵ **各轮次是干什么的**

| # | 轮 | 编号 | 任务 | 详案 | 前置 | 提交轴 |
|:--:|:--|:--|:--|:--:|:--|:--|
| 0 | 第 0 格 | `#109r` | ✅ **审查 ＋ 派工**（总方案 ＋ 20 份任务书 ＋ 清单任务格 ＋ 本交接） | [00](00-整理档案.md) | — | 文档 |
| 1 | **1.31** | `#111a` | ✅ **命令 id 与协议 id 归属 · 评估**（读数复量 ＋ 四路线裁决 ＋ 26 名映射表 ＋ 1.32 正式任务书） | [01](01-任务-命令id归属评估.md) | 第 0 格 | 文档 |
| 2 | **1.32** | `#111b` | ✅ **命令 id 归属 · 落地**（归属从身份来 ＋ 两条腿 ＋ 账定稿 —— **本段就是我，已做完**） | [02](02-任务-命令id归属落地.md) | 1.31 | 壳 ＋ SDK |
| 3 | **1.33** | `#111c` | **← 你在这** 设置键归属 · 评估（＋ **19 键改名映射表 ＋ 迁移设计**） | [03](03-任务-设置键归属评估.md) | 1.32 | 文档 |
| 4 | 1.34 | `#111d` | 设置键归属 · 落地 | [04](04-任务-设置键归属落地.md) | 1.33 | 壳 ＋ SDK |
| 5 | 1.35 | `#111e` | 外观族 id 归属 · 评估 | [05](05-任务-外观族id归属评估.md) | 1.34 | 文档 |
| 6 | 1.36 | `#111f` | 外观族 id 归属 · 落地 | [06](06-任务-外观族id归属落地.md) | 1.35 | 壳 ＋ SDK |
| 7 | 1.37 | `#111g` | 上下文旗子归属 · 评估 | [07](07-任务-上下文旗子归属评估.md) | 1.36 | 文档 |
| 8 | 1.38 | `#111h` | 上下文旗子归属 · 落地 | [08](08-任务-上下文旗子归属落地.md) | 1.37 | 壳 ＋ SDK |
| 9 | 1.39 | `#111i` | i18n 与黄灯族 · 评估 | [09](09-任务-i18n与黄灯族评估.md) | 1.38 | 文档 |
| 10 | 1.40 | `#111j` | i18n 与黄灯族 · 落地 | [10](10-任务-i18n与黄灯族落地.md) | 1.39 | 壳 ＋ SDK ＋ 文档 |
| 11 | **1.41** | `#111m` | 🔴 **迁移基建**（7 个清账轮的公共前置） | [11](11-任务-迁移基建.md) | 1.40 | 壳 |
| 12 | **1.42** | `#111n-1` | 🔴 **清账 · `file-tree`**（75 处，最大的一格） | [12](12-任务-清账-file-tree.md) | **1.41** | 插件仓 ×1 ＋ 壳账 |
| 13 | 1.43 | `#111n-2` | **清账 · `editor`**（1 处） | [13](13-任务-清账-editor.md) | 1.41 | 插件仓 ×1 ＋ 壳账 |
| 14 | 1.44 | `#111n-3` | **清账 · `serial-monitor`**（旗子 2） | [14](14-任务-清账-serial-monitor.md) | 1.41 | 插件仓 ×1 ＋ 壳账 |
| 15 | 1.45 | `#111n-4` | **清账 · `marketplace`**（旗子 6） | [15](15-任务-清账-marketplace.md) | 1.41 | 插件仓 ×1 ＋ 壳账 |
| 16 | **1.46** | `#111n-5` | 🔴 **清账 · `settings`**（风险最高，跨仓配对） | [16](16-任务-清账-settings.md) | 1.41 ＋ 1.37 裁决 | 插件仓 ×1 ＋ 壳 |
| 17 | **1.47** | `#111n-6` | 🔴 **清账 · 主题族 10 只**（主题系统最高审查级） | [17](17-任务-清账-主题族.md) | 1.41 | 插件仓 ×10 ＋ 壳账 |
| 18 | 1.48 | `#111n-7` | **清账 · 其余插件**（0 处，但要「无面确认表」） | [18](18-任务-清账-其余插件.md) | 1.41 | 文档 |
| 19 | 1.49 | `#111k` | **收紧为红 ＋ 账定稿进 SDK ＋ 两棵树 ＋ 五轴发版** | [19](19-任务-门禁与作者面收口.md) | **1.42–1.48 全清完** | SDK(npm) ＋ 文档 ＋ 壳 |
| 20 | **1.50** | `#111l` | 🔴 **本轴收口复核**（**恒最后**） | [20](20-任务-本轴收口复核.md) | 全部 | 文档 |

**两条排序纪律（写死在 [00 §四](00-整理档案.md)）**：① **1.32/1.34/1.36/1.38 的判据先以「黄灯 ＋ 账」落地**，**不立刻判红**（此时官方仓还没改名，判红 = 插件仓一段无谓的红窗）；② **1.49 才收紧为红**——那是「零例外」真正生效的一刻。

> ⚠️ **格数是上限，不是配额**：本轴 20 轮里**没有一格能省**（1.41 与 1.42–1.48 是 §〇c 扩面后新加的）；判「这条不做」必须把结论与理由写进交接段。

---

### ⑶ **你这一棒 = 1.33（`#111c` 设置键归属 · 评估）**

**直接读 [03-任务-设置键归属评估.md](03-任务-设置键归属评估.md)**——**评估轮：出方案、写映射表、写 1.34 的正式任务书，不动代码**。

**🔴 本格（1.32）给你留了三件可直接复用的东西——别重新设计**：

1. **账的形状已定稿**（[02 §三](02-任务-命令id归属落地.md)）：`scripts/host-reserved.json`（生成式 ＋ `--check` 双向对账 ＋ `--self-test` 8 例 ＋ **已接进 `npm run check`**）＋ **SDK 副本 `packages/plugin-sdk/schemas/host-reserved.json`**（**随包走**，已发 `0.1.30`）。**今天账里六个家族**：`commandPrefixes` 6 · `configKeys` **28** · `pseudoPluginIds` 3 · `contextKeys` 11 · `appearanceIds` 3 · `protocolIds` 1。⇒ **1.33 只需「往账里加家族」或「让消费方读它」**，⛔ **不要重新设计账**，⛔ **不许在规则里内联一份键清单**（内联 = 手抄表 = 假判据）。
2. **探针已搬进仓内且能跑**：`npm run audit:nonnaming`（机读 `npm run audit:nonnaming:json`）——**②家族（设置键）现成**，1.33 量设置键面直接读它，**改前 = 改后 47 名**（本格只修了命令家族字段，设置键家族读数未变）。
3. **腿的形状照抄** `packages/plugin-sdk/src/eslint/checks/command-ownership.ts`（三面映射 ＋ `judgeCommandId` 纯函数 ＋ **fail-closed**（没 `plugin.json` 就报一条并停止源码扫描）＋ disable 索引三面 ＋ 12 例正负控）＋ 账读法 `loadHostReserved()`。壳侧对账照 `scripts/gen-host-reserved.mjs --check`。

**⚠️ 你这一棒的两个已知坑（我踩过，替你标出来）**：

- **报点文案属于判据的输出面**：我这一格发完 SDK `0.1.29` 后复核真形态输出，发现报点里那句「前缀只插入、不改词干」与建议值（把 `workbench.` **换成**自己的前缀）**不是一回事** ⇒ 只好改文案 ＋ 补一条钉住该形状的断言 ＋ 重发 `0.1.30`。**报点文案发版前要拿真样本读一遍输出**，别只跑测试。
- **黄灯不是「跑绿就行」**：`bin lint` 的退出码只看 eslint 红级 ⇒ **check 腿的偏离天然不进退出码**（这正是本格 ①② 只判黄能成立的原因）。**别把「exit 0」当成判据通过的证据**——要看报点读数。

---

### ⑷ 收尾四件（**缺一不可**）

1. 在**本文件顶部追加一段**（只追加不改旧段），段内**必带上面的剩余任务队列整表**（把自己那格改 ✅）；
2. 同笔续写 [E6-执行清单.md 第 1 层头的轮次进度行](../../E6-执行清单.md)；
3. 勾格要勾全套；
4. **🔴 不许静默不做**：判「这条不做」必须把结论与理由写进交接段。

---

### 🔴 两条通用红线（每一棒都适用）

- **提交只按路径 `git add <本格文件>`，⛔ 禁 `git add -A` / `git add .`** —— 壳仓随时有别的会话在途（**索引是共享的**；实测工作区躺着别的会话正在改的 `docs/06-记忆系统整理/02-索引重写规范.md`，**别碰、别提交**）。
- **不推**：`git push` 必须等**用户本人当次点头**；推必带代理；推完 `git ls-remote --heads origin <branch>` **对 SHA**。npm 发版照 [作者轴 npm 发版 runbook](../../../../06-发布管理/作者轴npm发版.md) 五步（**显式 `--registry`**）；⛔ **撤版只能用户本人**。

---

### 📦 本格（1.32）交付实录 —— 下面这些结论你可以直接引用

**做了什么（一句话：一条归属解析 ＋ 两条腿 ＋ 一张账）**：

| 面 | 落点 | 干了什么 |
|:--|:--|:--|
| **归属解析（壳）** | `src/core/registry/commands/CommandRegistry.ts` | 优先级 **①声明面查表 → ②注册方显式申报 → ③名字第一段（降级为「推定」）**；**异归属 ⇒ 不覆盖 ＋ `console.error` 点名双方**（⛔ 不抛错——抛错 = 整只插件装不上）；**同身份重注册维持原样**（只更新 handler/title/placeholder，禁区 3 逐字保留）；**声明晚到 ⇒ 重挂归属**（**另一条触发路径**，没塞进那个分支）；**注销按真归属摘** |
| **池侧** | `electron/preload-pool/commands.ts` | `commandOwnerId`（名字切第一段）**退役** → 改问壳要真归属（`IPC.plugins.call/resolveCommandOwner`）；**推定档不缓存**（声明/自报晚到能翻盘）、declared/reported 才入缓存；`unregisterCommands` 按**真归属**摘；⛔ `_poolCommandWindows` / `_poolPending` 一个字未动 |
| **桥面** | `src/core/services/plugins/IpcBridgeHandler*` ＋ `contracts/linkdesk.d.ts` | `meta.pluginId` **可选字段透传**（加法，通道形状未动）＋ 注册回执**返回解析出的归属** |
| **账** | `scripts/host-reserved.json` ＋ `packages/plugin-sdk/schemas/host-reserved.json` | 生成器升格为门禁：**`--check` 三向对账**（账 ↔ 现场重扫 ↔ SDK 副本，逐字节）＋ **`--self-test` 8 例**（2 正 6 负）＋ **接进 `npm run check`**；🔴 **本格加了第六个家族 `protocolIds`**（理由见下「未做项」） |
| **SDK 腿** | `packages/plugin-sdk/src/eslint/checks/command-ownership.ts` | 判据 **①`<pluginId>.` 前缀 ② 宿主保留面 ⑦ 协议 id 同族**（三面：声明 / 运行时 / 协议）＋ 12 例正负控 |
| **探针** | `scripts/audit-nonnaming.mjs`（＋ 两个 npm 入口） | **从 gitignore 的 `scratch/` 搬进仓内**（同笔删掉 scratch 原件，照 1.20 搬 `audit-plugin-scope.mjs` 的先例）＋ **修命令家族字段** ＋ 命令家族**分三面报**（声明 / 运行时 / 引用）＋ 新增 **⑩b 账背对账** |

**判据与负控实测（真跑，不是推断）**：

| 判据 | 实测读数 / 负控结果 |
|:--|:--|
| ①② 插件侧（SDK，**黄**） | `file-tree` 上 SDK 腿读数 **声明面 21 名 ／ 运行时面 25 名 ＝ 46 处不合规** —— 与 1.31 §0.3 的 **46 处**逐字相符。N1（删前缀）／N2（`workbench.action.showCommands`）／N3（真形态样本 `editor.selectForCompare` ⇒ 建议值 `file-tree.selectForCompare`）**全部真红**，报点带 `file:line` |
| ③④⑤⑥ 壳侧（**红**） | N4 两个不同真身份注册同一 id ⇒ **不覆盖**（先者 handler 在）＋ `console.error` 点名双方 ＋ **不抛错**；N5 **同身份重注册 ⇒ 不出声且生效**（这条是「没改坏既有设计」的证）；N6 `beta` 注册 `acme.foo`、卸载 `alpha` 后 `acme.foo` **仍在**；N9 `editor.selectForCompare` 池内 miss ⇒ **激活 `['file-tree']`**（旧实现激活的是 `['editor']` ＝ 根因 H3′ 当场复现并修掉） |
| ⑧ 账 ↔ 实况（**红**） | `--self-test` **8/8**（负控自证真变异）；N7 **只改账**（加假前缀）⇒ 红；N8 **只改实况**（宿主加一条新命令）⇒ 红 |
| ⑦ 协议 id | 并入同一条腿（同账同归属解析同自测）；协议面在 18 仓读数 **0**，撞宿主内置协议 id ⇒ 红 |

**判据⑨ 「旧门禁未空转」——实测（照 CSS 系列件 8 的做法，不是看代码猜）**：

| 旧门禁 | 实测读数 | 结论 |
|:--|:--|:--|
| `scripts/audit-plugin-scope.mjs`（**读同一本账**，我改过它） | `file-tree` **75** 需改处 ·「合规：命令 0 / 设置键 0」· 逐仓 75/6/4/2/1/0…… | **非零 ⇒ 没被清零** ✅ |
| `check-namespace-matrix.mjs` | 契约 **46** 命名空间 / 15 域接口 · pool 45 · shell 25 · mock 13 | 非零 ✅ |
| `check-contributes.mjs` | 16 字段 schema/文档/消费**全对齐** | ✅ |
| `check-gate-health.mjs` | 绿（两条已登记缺口的「输入为何为空」原样保留） | ✅ |
| SDK 五块腿**共存** | `file-tree` 一次运行里 **五块读数全部打印**（没有哪块因新腿非零而闭嘴） | 新腿**没吃掉**旧块的输出 ✅ |
| 新腿**没改退出码** | `file-tree` / `editor` `exit 0`（黄不过闸）⇒ **18 仓 CI 没有无谓红窗** | ✅ |

**🔴 探针改前 / 改后读数（口径：处 = 出现点 · 名 = 去重名）**：

| 项 | 改前（`scratch/audit-nonnaming.mjs`） | 改后（`scripts/audit-nonnaming.mjs`） |
|:--|:--|:--|
| ① 命令 id | **「独立名 12」—— 读错字段（`x.command` 恒 undefined）⇒ 该桶里其实是 `menus[].command` ＝ 菜单引用数** | **①a 声明面 名 35 / 处 35** · **①b 运行时面 名 25 / 处 25** · **①c 引用面 名 18 / 处 20** |
| ⑩ 插件 ∩ 宿主（命令 id） | **0** —— **拿上面那个错集合算的**（漏掉全部 21 条 `explorer.*`）⇒「跨方交集 0」这句**当时没被验证** | **声明∪运行时 ∩ 宿主 = 0**（就地扫）· **∩ 账（权威，6 项）= 0** ✅ **这句现在有证据了** |
| 其余家族 | 设置键 47 · 主题 10 · 图标主题 1 · 共享图标 0 · 语言包 4 · 配方 20 · contextKey 23（共写 6）· i18n 414（17）· 键位 6 · 视图 11（1）· 容器 5 · 菜单槽 3 · 标题栏 0 · 扩展名 2 | **逐项相同**（本格只修命令家族字段 ＋ 新增 ③f 协议 id = 0 与 ⑩b） |

**用修好的字段复算「需改」口径（对账 1.31 基线）**：**声明面 21 处 ＋ 运行时面 25 处 ＝ 46 处 / 26 名，全部落在 `file-tree` 一仓** ⇒ 与 1.31 §0.3 的 **46 处 ＝ 声明 21 ＋ 运行时 25**、**26 名** **逐字相符**（声明面总名 35 ＝ 需改 21 ＋ 已合规 14）。

**发版读数（下一棒可能要用新 SDK）**：

- `@linkdesk/plugin-sdk` **0.1.28 → 0.1.29 →（同轮又发）0.1.30**（112 文件，**含 `schemas/host-reserved.json`**）
- `@linkdesk/contracts` **0.1.17 → 0.1.18**（4 文件；`meta.pluginId` 可选字段 ＋ 注释）
- **两条现场证**：① 消费者 `node_modules` 版本 **＝ 0.1.30** ＋ 账**随包存在**（`schemas/host-reserved.json` 在包里）；② **塞违规看它红**——在**全新目录**用**货架件**（非本仓 dist）跑 `lint`，`workbench.action.showCommands` 当场报「落在**宿主保留面**（`workbench.`）……改成 `acme.action.showCommands`（只换第一段、`workbench.` 之后的词干一个字不改）」
- `npm run release:mark` ✔️ **五轴基线全记**（contracts 0.1.18 / plugin-sdk 0.1.30 / create-linkdesk-plugin 0.1.10 / ui 0.3.0 / plugin-docs 0.1.10）⇒ **`check:npm-release` 黄灯灭**
- 壳（软件）**0.2.1 → 0.2.2**（0.x 向后兼容 ⇒ patch 位；`CHANGELOG.md` 已同笔）
- ⚠️ **为什么 SDK 同轮发了两次**：0.1.29 发完复核真形态输出，发现报点文案与建议值不一致（见 ⑶ 的第一个坑）⇒ 修文案 ＋ 补一条钉住该形状的断言 ＋ 重发一位。**多发的这一位是文案不一致的代价，不是手滑。**

**⚠️ 本格发现、但本格不修的东西（点名回队列，不救火）**：

1. 🔴 **给 1.37（上下文旗子）的真线索**：探针 **⑩b 实测「插件设的 context key ∩ 宿主账（11 项）＝ 5」**——`inputFocus` / `settingKey` / `settingFollowTheme` / `settingResetsToDefault` / `settingModified`：**插件正在写宿主保留面里的 context key**。这是 1.37 的实击样本（那时才有裁决权），**本格只报读数**。
2. **纯数据插件跑 SDK lint 会报 eslint 空集错（`exit 1`）**：官方 18 仓里 **12 仓**（`lang-defaults` / `lang-test-ja` / 主题族 10 只）**没有一行 TS/JS** ⇒ eslint 报「All files matched … are ignored」。🔴 **已核不是本格引入**：**已发布 SDK `0.1.19` 同样如此**（同一命令、同一报错）。但它意味着那 12 仓的 CI 只要跑 lint 就是常红——**归 1.49（收紧与作者面收口）或另一条 SDK 轮**，本格不夹带修（改 lint 的调用形状 = 动 SDK 行为，超出本格判据面）。
3. **命令引用面 ∩ 账 ＝ 1**（`workbench.view.explorer`）：插件**引用**宿主命令（菜单/键位里指向它）——**合法**，⑩b 单列报读数，**不构成顶替**（顶替看的是声明/运行时那个名字面）。
4. **存量 46 处改名**：**不归本格**（[02 §五](02-任务-命令id归属落地.md) 明写：归 **1.42**，前置 **1.41**）——本格只交「判据 ＋ 归属来源修正 ＋ 账」。

**未做项与理由（🔴 不许静默不做）**：

- **存量改名 46 处**：不归本格（同上第 4 条）。
- **本格往账里加了第六个家族 `protocolIds`**：[02 §三](02-任务-命令id归属落地.md) 说「五个家族今天已全在，**形状无需重新设计**」——**形状我没动**（同一份生成式账 ＋ 同一套 `--check` / `--self-test`），加家族是因为**判据⑦ 要求「只许读账、不许在规则里内联清单」**（§三 🔴 红线），而当时账里**没有**协议 id 面 ⇒ 不加就得内联，那是手抄表。**加的是数据面，不是结构。**
- **本格没有动 `ProtocolRegistry.ts` 的注册实现**：[02 §0.2](02-任务-命令id归属落地.md) 把它列入「要加的面」，但 1.32 判据⑦ 的落点是「**协议 id 与命令 id 同族判**（同账同腿）」，**今天协议 id 插件使用 ＝ 0**，改注册表实现是**没有消费者的行为变更** ⇒ 留给真正需要它的轮次（如 1.49 收紧时）。

---

## ← 第 1.31 轮 · 命令 id 与协议 id 归属 · **评估已完成**（`#111a` · 2026-09-17）：给下一个 AI（**下一棒 = 1.32**）

> 🔴 **先说你是谁**：**你是第 2 棒，要做的是 [1.32 命令 id 归属 · 落地](02-任务-命令id归属落地.md)**——**这一棒才动代码**（上一棒只出方案，没动一行代码）。
> **你不是复核者，也不是派工者**——派工在第 0 格做完了，评估在第 1.31 做完了。用户会把上一棒的**整段消息**复制给你（不裁剪），里面可能有别的东西，**以本段为准**。
> ⚠️ **如果你拿到的上下文里没有本段**，说明交接还没交到你手上 ⇒ **先问一句「我是 1.32 吗」，别默认自己在做汇报或复核**。

---

### ⑴ **本轴是干什么的**

E6 第 1 层（插件独立构建）的**第二个专项**，紧接 [样式命名空间归一化](../样式命名空间归一化/00-整理档案.md)（CSS 系列，已收官）。

**真问题一句话**：**软件里有一批「公共花名册」——谁都能往上写一个名字，写的时候不核对归属，撞了只小声嘟囔（或连嘟囔都没有）。**

**同形判据（四条全中才算）**：名字空间**扁平** ＋ **多方都能写** ＋ **写时无归属校验** ＋ **冲突静默**（最多 `console.warn`）。🔴 **第四条是关键**：`console.warn` **对用户不可见，对「被覆盖的那一方作者」也不可见**——因为他在自己机器上没装对方插件，一切正常。**「被覆盖」这件事，受害方永远收不到通知。**

**真根因（每一棒收尾都要能复述这句）**：

> **不是名字起坏了，是「谁的名字」被寄托在「取名自觉」上——账本要么没有，要么只用来打印一行字，而宿主自己的名字和第三方名字长着同一张脸。**

**🔄 收口口径（§〇c 修订后，此后一切以此为准）**：**根治 = 规则零例外**（**官方仓全部改名**，**机械定义 = `npm run audit:plugin-scope` 全 0**）＋ **运行时归属仲裁**兜住静态判不了的那类 ＋ 🔴 **每一处改名都同笔带迁移 ＋ 逐项操作实测**。**上位约束 = 操作体验零变化**（**名字可变，取值与功能不许变**）。**唯一保留的黄灯** = i18n 字典键；**三类「全局概念名」永久豁免**（文件关联扩展名 / 语言定义扩展名 / 语言包语言码）。

---

### ⑵ **各轮次是干什么的**

| # | 轮 | 编号 | 任务 | 详案 | 前置 | 提交轴 |
|:--:|:--|:--|:--|:--:|:--|:--|
| 0 | 第 0 格 | `#109r` | ✅ **审查 ＋ 派工**（总方案 ＋ 20 份任务书 ＋ 清单任务格 ＋ 本交接） | [00](00-整理档案.md) | — | 文档 |
| 1 | **1.31** | `#111a` | ✅ **命令 id 与协议 id 归属 · 评估**（读数复量 ＋ 四路线裁决 ＋ 26 名映射表 ＋ 1.32 正式任务书） | [01](01-任务-命令id归属评估.md) | 第 0 格 | 文档 |
| 2 | **1.32** | `#111b` | **← 你在这** 命令 id 归属 · 落地（归属从身份来 ＋ 两条腿 ＋ 账定稿） | [02](02-任务-命令id归属落地.md) | 1.31 | 壳 ＋ SDK |
| 3 | 1.33 | `#111c` | 设置键归属 · 评估 | [03](03-任务-设置键归属评估.md) | 1.32 | 文档 |
| 4 | 1.34 | `#111d` | 设置键归属 · 落地 | [04](04-任务-设置键归属落地.md) | 1.33 | 壳 ＋ SDK |
| 5 | 1.35 | `#111e` | 外观族 id 归属 · 评估 | [05](05-任务-外观族id归属评估.md) | 1.34 | 文档 |
| 6 | 1.36 | `#111f` | 外观族 id 归属 · 落地 | [06](06-任务-外观族id归属落地.md) | 1.35 | 壳 ＋ SDK |
| 7 | 1.37 | `#111g` | 上下文旗子归属 · 评估 | [07](07-任务-上下文旗子归属评估.md) | 1.36 | 文档 |
| 8 | 1.38 | `#111h` | 上下文旗子归属 · 落地 | [08](08-任务-上下文旗子归属落地.md) | 1.37 | 壳 ＋ SDK |
| 9 | 1.39 | `#111i` | i18n 与黄灯族 · 评估 | [09](09-任务-i18n与黄灯族评估.md) | 1.38 | 文档 |
| 10 | 1.40 | `#111j` | i18n 与黄灯族 · 落地 | [10](10-任务-i18n与黄灯族落地.md) | 1.39 | 壳 ＋ SDK ＋ 文档 |
| 11 | **1.41** | `#111m` | 🔴 **迁移基建**（7 个清账轮的公共前置） | [11](11-任务-迁移基建.md) | 1.40 | 壳 |
| 12 | **1.42** | `#111n-1` | 🔴 **清账 · `file-tree`**（75 处，最大的一格） | [12](12-任务-清账-file-tree.md) | **1.41** | 插件仓 ×1 ＋ 壳账 |
| 13 | 1.43 | `#111n-2` | **清账 · `editor`**（1 处） | [13](13-任务-清账-editor.md) | 1.41 | 插件仓 ×1 ＋ 壳账 |
| 14 | 1.44 | `#111n-3` | **清账 · `serial-monitor`**（旗子 2） | [14](14-任务-清账-serial-monitor.md) | 1.41 | 插件仓 ×1 ＋ 壳账 |
| 15 | 1.45 | `#111n-4` | **清账 · `marketplace`**（旗子 6） | [15](15-任务-清账-marketplace.md) | 1.41 | 插件仓 ×1 ＋ 壳账 |
| 16 | **1.46** | `#111n-5` | 🔴 **清账 · `settings`**（风险最高，跨仓配对） | [16](16-任务-清账-settings.md) | 1.41 ＋ 1.37 裁决 | 插件仓 ×1 ＋ 壳 |
| 17 | **1.47** | `#111n-6` | 🔴 **清账 · 主题族 10 只**（主题系统最高审查级） | [17](17-任务-清账-主题族.md) | 1.41 | 插件仓 ×10 ＋ 壳账 |
| 18 | 1.48 | `#111n-7` | **清账 · 其余插件**（0 处，但要「无面确认表」） | [18](18-任务-清账-其余插件.md) | 1.41 | 文档 |
| 19 | 1.49 | `#111k` | **收紧为红 ＋ 账定稿进 SDK ＋ 两棵树 ＋ 五轴发版** | [19](19-任务-门禁与作者面收口.md) | **1.42–1.48 全清完** | SDK(npm) ＋ 文档 ＋ 壳 |
| 20 | **1.50** | `#111l` | 🔴 **本轴收口复核**（**恒最后**） | [20](20-任务-本轴收口复核.md) | 全部 | 文档 |

**两条排序纪律（写死在 [00 §四](00-整理档案.md)）**：① **1.32/1.34/1.36/1.38 的判据先以「黄灯 ＋ 账」落地**，**不立刻判红**（此时官方仓还没改名，判红 = 插件仓一段无谓的红窗）；② **1.49 才收紧为红**——那是「零例外」真正生效的一刻。

**系列外（不占上表序，可插空档，⛔ 不许塞进 1.50）**：**E6#110**（作者轴黄灯观察名单）· 🆕 **E6#112**（插件域关键帧引用判据）· 🆕 **E6#113**（插件侧死类清账）· 🔴 **键盘 chord 静默死键**（已立案、**不属本轴**）。

---

### ⑶ **你这一棒 = 1.32（`#111b` 命令 id 归属 · 落地）**

**直接读 [02-任务-命令id归属落地.md](02-任务-命令id归属落地.md)**——**它已是正式版**（1.31 重写，面清单 / 判据措辞 / 账设计 / 负控清单 / 存量分档 / 发版轴五项全备，**没有占位**）。评估结论的正文在 **[01 档 §八–§十五](01-任务-命令id归属评估.md)**。

**1.31 的裁决（一句话）**：**选「甲：让运行时注册带真身份」为主干**；「乙：名字前缀结构化」是它的**静态前置**（必做，但单独不够——运行时的归属仍从名字推）、「丙：运行时仲裁」是它的**运行时候裁语义**（必做，但**检测条件本身是坏的**、必须先有甲才判得出来）；「丁：复合键」**否决**（砸公共 API，且收益 ≈ 乙而代价大一个量级）。

**1.31 的三条硬读数（你进场先复跑对账）**：
- **需改 46 处 = 声明 21 ＋ 运行时 25**（**26 个不同名**），全部在 `file-tree`，**引用点 96**；🔴 **壳仓 / 其余 17 仓 / 官方目录引用 = 0**。
- 🔴 **行号已偏移**：`commandId.split(".")[0]` 实测在 **`CommandRegistry.ts:148` / `:178`**（登记值 150/186）⇒ **按符号找，别按行号找**。
- 🔴 **探针有缺陷**：`scratch/audit-nonnaming.mjs:74` 读 `x.command`，而真字段是 **`id`** ⇒ 探针 ① 报的「命令 id 独立名 12」**其实是菜单引用数**，而 ⑩ 的「插件 ∩ 宿主 = 0」**是拿这个错集合算的**（漏掉 21 条 `explorer.*`）⇒ **1.32 同笔修字段 ＋ 把两个探针一起搬进 `scripts/`**（后者今天仍在 gitignore 的 `scratch/`，却被 20 份任务书引用）。

**🔴 两条最容易做错的地方（先记住）**：
- **不许抛错**（命令注册调用点在 `parseContributions` 内 ⇒ 抛错 = **整只插件装不上，比现在更坏**）；
- **不许改坏「同插件重注册」的合法语义**（`CommandRegistry.ts:59-61`：loader 先注册元数据 → 组件 mount 重注册 handler，**只更新 handler/title/placeholder、不动元数据**）——1.32 新增的「声明到达时重挂归属」**必须是另一条触发路径**，别塞进这个分支。

**🔴 本格只判黄、不判红（排序纪律）**：①②（插件命令 id 前缀 / 不占宿主前缀）**先黄**（官方仓还没改名）；**唯一提前判红的是壳仓自己的四条**（归属从身份取 / 不覆盖＋`console.error` / 注销只删自己 / on-command 用真身份）＋ **账 ↔ 实况双向对账**。

**🔴 本格会触发一次 SDK PATCH**（动 `packages/plugin-sdk/schemas/**` ＋ 新 ESLint 规则）⇒ 照 [作者轴 npm 发版](../../../../06-发布管理/作者轴npm发版.md) **五步**（显式 `--registry`）；**发版读数要写进你的交接段**（下一棒可能要用新 SDK）。

---

### ⑷ 收尾四件（**缺一不可**）

1. 在**本文件顶部追加一段**（只追加不改旧段），段内**必带上面的剩余任务队列整表**（把自己那格改 ✅）；
2. 同笔续写 [E6-执行清单.md 第 1 层头的轮次进度行](../../E6-执行清单.md)；
3. 勾格要勾全套；
4. **🔴 不许静默不做**：判「这条不做」必须把结论与理由写进交接段。

---

### 🔴 两条通用红线（每一棒都适用）

- **提交只按路径 `git add <本格文件>`，⛔ 禁 `git add -A` / `git add .`** —— 壳仓随时有别的会话在途（**索引是共享的**；实测工作区躺着别的会话正在改的 `docs/06-记忆系统整理/02-索引重写规范.md`，**别碰、别提交**）。
- **不推**：`git push` 必须等**用户本人当次点头**；推必带代理；推完 `git ls-remote --heads origin <branch>` **对 SHA**。npm 发版照 [作者轴 npm 发版 runbook](../../../../06-发布管理/作者轴npm发版.md) 五步（**显式 `--registry`**）；⛔ **撤版只能用户本人**。

---

## ← 第 0 格 · 审查 ＋ 派工已完成（`#109r` 并入）：给下一个 AI（**下一棒 = 1.31**）

> 🔴 **先说你是谁**：**你是第 1 棒，要做的是 [1.31 命令 id 与协议 id 归属 · 评估](01-任务-命令id归属评估.md)**——**评估轮（改代码的是 1.32，不是你）**。
> **你不是复核者，也不是派工者**——派工已经在**第 0 格**做完了（本段就是它的交付）。用户会把上一棒的**整段消息**复制给你（不裁剪），里面可能有别的东西，**以本段为准**。
> ⚠️ **如果你拿到的上下文里没有本段**（只拿到 CSS 系列的交接），说明**本轴的交接还没交到你手上** ⇒ **先问一句「我是 1.31 吗」，别默认自己在做汇报**。

---

### ⑴ **本轴是干什么的**

E6 第 1 层（插件独立构建）的**第二个专项**，紧接 [样式命名空间归一化](../样式命名空间归一化/00-整理档案.md)（CSS 系列）。

起因：用户 2026-09-16 问「**除了 UI 这部分外，其余部分有没有类似的问题？如数据通信部分？……又或者声明的东西，又或者命令类。**」＋「**不仅仅是我提到的，你还可以自己再找一找。**」；**第二次拍板**（同日晚些）：「**我需要一个极其完善的，根治的方案，所以我问你是否是根治方案。**」＋「**要新增官方仓的插件整理工作对吧，工作量是不是会大一点？如果是的话，建议加任务**」＋「**那十几个主题插件、普通测试插件、语言翻译插件等可以一到两个 AI 做了**」。

**真问题一句话**：**软件里有一批「公共花名册」——谁都能往上写一个名字，写的时候不核对归属，撞了只小声嘟囔（或连嘟囔都没有）。**

**同形判据（四条全中才算）**：名字空间**扁平** ＋ **多方都能写** ＋ **写时无归属校验** ＋ **冲突静默**（最多 `console.warn`）。
🔴 **第四条是关键**：`console.warn` **对用户不可见，对「被覆盖的那一方作者」也不可见**——因为他**在自己机器上没装对方插件**，一切正常。**「被覆盖」这件事，受害方永远收不到通知。**

**真根因（每一棒收尾都要能复述这句）**：

> **不是名字起坏了，是「谁的名字」被寄托在「取名自觉」上——账本要么没有，要么只用来打印一行字，而宿主自己的名字和第三方名字长着同一张脸。**

**🔄 §〇c 修订后的收口口径（**此后一切以此为准**）**：
- **根治 = 规则零例外**（**官方仓全部改名**，不是"登记例外"；**机械定义 = `node scripts/audit-plugin-scope.mjs` 全 0**）
  ＋ **运行时归属仲裁**兜住静态判不了的那类（两个不同第三方 / 动态拼名）
  ＋ 🔴 **每一处改名都同笔带迁移 ＋ 逐项操作实测**（改名的设置项仍能切换/值沿用 · 改 id 的快捷键仍触发 · 改 id 的主题仍跟随）。
- **上位约束**：**操作体验零变化**（**名字可变，取值与功能不许变**）—— 旧说法「除版本号外不要有任何 UI 变化」**已被用户本人修订**。
- **唯一保留的黄灯**：**i18n 字典键**（跨仓不可判）；**三类「全局概念名」永久豁免**：文件关联扩展名 / 语言定义扩展名 / 语言包语言码。
- **两条统一机制（都在仓里有现成先例，不必发明）**：**A 宿主保留名账进 SDK**（照 `packages/plugin-sdk/schemas/reserved-class-names.json`）· **B 运行时首撞者保留 ＋ 后来者降级不覆盖 ＋ `console.error`**（🔴 **不许抛错**——`parseContributions` 无 try/catch ⇒ 抛错 = **整只插件装不上，比现在更坏**）。

**第 0 格已交付**：① [00 总方案](00-整理档案.md)（含 **§〇c 口径修订令** ＋ 全量普查表 ＋ 收口定义 ＋ 残余边界十一条 ＋ 原则对照 ＋ **20 轮总表**）；② **20 份任务书**（[01](01-任务-命令id归属评估.md)–[20](20-任务-本轴收口复核.md)，含**迁移基建**与**官方仓清账 7 格**）；③ E6 清单任务格；④ 本交接文件。

**第 0 格的实测发现（后面每一棒都要用到）**：
- 🔴 **三条活体（今天正在发生的跨方冲突）**：① 官方 `file-tree` **注册了两个 `editor.*` 命令**（`src/components/FileTreeContextMenu/commands/compare.ts:15,19`）⇒ 卸载它永不清理、卸载 `editor` 反而会删掉它们；② `file-tree` **在设共享组件的旗子 `inputFocus`**（`useTreeRename.ts:35,41`）⇒ 两方同屏互相踩；③ `theme-defaults` 声明的主题 id **就是宿主兜底 id `light`**（**有意交接**，但顺带证明兜底 id 是**无主空位**）。
- 🔴 **归属「从名字推」**：命令 id 的运行时路径用 `commandId.split(".")[0]` 当身份（`src/core/registry/commands/CommandRegistry.ts:150,186`）⇒ **同名前缀时覆盖告警被整段吞掉**。
- 🔴 **宿主伪 pluginId `app` / `appearance` / `update` 一个都没设防** ⇒ 第三方可合法同名，**连 warn 都不响**。
- **逐插件清账面**：`file-tree` **75** · `marketplace` **6** · `settings` **4** · `serial-monitor` **2** · `editor` **1** · 主题族 **1–6 各** · 其余 **0**（≈ **123 处**）。

---

### ⑵ **各轮次是干什么的**

| 轮 | 编号 | 一句话 | 状态 |
|:--|:--|:--|:--:|
| 第 0 格 | `#109r` | **审查 ＋ 派工**（不做任务） | ✅ |
| **1.31** | `#111a` | **命令 id 与协议 id 归属 · 评估**（**下一棒**） | ⬜ |
| 1.32 | `#111b` | 命令 id 归属 · 落地（判据 ＋ 归属来源修正 ＋ 双腿） | ⬜ |
| 1.33 | `#111c` | 设置键归属 · 评估（＋ **19 键改名映射表 ＋ 迁移设计**） | ⬜ |
| 1.34 | `#111d` | 设置键归属 · 落地 | ⬜ |
| 1.35 | `#111e` | 外观族 id 归属 · 评估（＋ **主题/配方 id 改名映射表 ＋ 三键迁移设计**） | ⬜ |
| 1.36 | `#111f` | 外观族 id 归属 · 落地 | ⬜ |
| 1.37 | `#111g` | 上下文旗子归属 · 评估（含两条活体的路线与代价） | ⬜ |
| 1.38 | `#111h` | 上下文旗子归属 · 落地 | ⬜ |
| 1.39 | `#111i` | i18n 与黄灯族 · 评估（跨仓不可判的边界定死） | ⬜ |
| 1.40 | `#111j` | i18n 与黄灯族 · 落地（🔴 本件一条红都不加） | ⬜ |
| **1.41** | `#111m` | 🔴 **迁移基建**（**7 个清账轮的公共前置**；交付「清账轮作业包」） | ⬜ |
| **1.42** | `#111n-1` | 🔴 **官方仓清账 · `file-tree`**（**75 处，最大的一格**；含还回 `editor.*` 与 `inputFocus`） | ⬜ |
| 1.43 | `#111n-2` | **官方仓清账 · `editor`**（1 处；41 个文件关联不改要实证） | ⬜ |
| 1.44 | `#111n-3` | **官方仓清账 · `serial-monitor`**（旗子 2） | ⬜ |
| 1.45 | `#111n-4` | **官方仓清账 · `marketplace`**（旗子 6 ↔ 齿轮菜单 7 条目） | ⬜ |
| **1.46** | `#111n-5` | 🔴 **官方仓清账 · `settings`**（**风险最高**：旗子是宿主命令的 `when`，**跨仓配对**） | ⬜ |
| **1.47** | `#111n-6` | 🔴 **官方仓清账 · 主题族 10 只**（**主题系统最高审查级**；`theme-defaults` 的 `light` 是宿主交接位） | ⬜ |
| 1.48 | `#111n-7` | **官方仓清账 · 其余插件**（0 处，但产出「无面确认表」＋ 钉死三类全局概念名） | ⬜ |
| 1.49 | `#111k` | **收紧为红 ＋ 账定稿进 SDK ＋ 作者面两棵树 ＋ 五轴发版 ＋ 目录/种子** | ⬜ |
| **1.50** | `#111l` | 🔴 **本轴收口复核**（**恒最后**） | ⬜ |

**为什么这么切**（口径照 CSS 系列，用户原话「**一个 AI 一个任务，任务量不要太大也不要太少**」）：
- **每个面至少一格 ＋ 评估轮与落地轮分开**（落地面数字要靠评估轮实测产出 —— **评估轮的下一格任务书，由评估轮收尾时写齐**）；
- **迁移基建单独立一格**：它是 7 个清账轮的**公共前置**（「只改名字不写迁移」是禁区）；
- **官方仓清账按仓切**（与 CSS 系列「一仓一格」同款）：**5 个重要插件各一格 ＋ 主题族一格 ＋ 其余一格**；**7 格互不依赖**（各改各的仓）⇒ 次序可换、甚至并行，但**都在 1.41 之后、1.49 之前**；
- **收紧为红放在 1.49**（清账完 ＋ 账定稿）——**这是「零例外」真正生效的那一刻**，也是第三方作者从此被自己仓库拦下的那一刻；
- **收口复核恒为最后一格**（1.50）。

---

### ⑶ **你这一棒 = 1.31（`#111a` 命令 id 与协议 id 归属 · 评估）**

**直接读 [01-任务-命令id归属评估.md](01-任务-命令id归属评估.md)**——它已按「专门的 AI 拿来就能干」写好：读数、复跑命令、候选路线、判据草案、禁区、进场第一步全备。

**为什么它排第一**：命令 id 是**最危险的一条**（功能级静默：**界面正常、点下去执行的是别人的 handler**），而且实测发现它**比 CSS 系列 30 号档记的还坏一层**：

> 🔴 **运行时注册的命令，它的「归属」是从名字的第一段推出来的**（`src/core/registry/commands/CommandRegistry.ts:150,186` 的 `commandId.split(".")[0]` ＋ `electron/preload-pool/commands.ts:36-39` 的 `commandOwnerId()`）。
> ⇒ **两个插件都运行时注册 `acme.doThing`，推导归属都是 `"acme"`** ⇒ 覆盖检测的 `pid !== pluginId` **恒 false** ⇒ 🔴 **静默覆盖，连 `console.warn` 都不会响。**

**你要产出的三件事**：
1. **归属来源的方案裁决**（甲「让运行时注册带真身份」/ 乙「名字前缀结构化」/ 丙「运行时仲裁」/ 丁「复合键」——**四条逐条裁，选出一个，不许只列不选**）；
2. **存量 46 个不合规命令 id 的改名映射表**（21 声明 ＋ 25 运行时注册；含 🔴 **`file-tree` 借用的 `editor.*` 两个** —— 它们**既是本格的映射项，也是 [1.42](12-任务-清账-file-tree.md) 的还回项，两处口径必须一致**）；
3. **[1.32 落地](02-任务-命令id归属落地.md) 的正式任务书**（面清单 + 判据措辞 + 账家族 + 负控清单 + 发版轴）。

🔴 **两条最容易做错的地方（先记住）**：
- **不许抛错**（`contributions.ts` 的命令注册调用点在 `parseContributions` 内 ⇒ 抛错 = 整只插件装不上）；
- **不许改坏「同插件重注册」的合法语义**（`CommandRegistry.ts:59-61`：loader 先注册元数据 → 组件 mount 时重注册 handler，**只更新 handler/title、不动元数据**——**这是既有设计，逐字保留**）。

🔴 **进场第一件事**：**自己 grep 一遍全仓读数**——[01 档 §一](01-任务-命令id归属评估.md) 的数字与 CSS 系列 30 号档的读数**都只用来对账，不许当输入**（记忆 `ai-relay-one-round-per-session` 的面清单纪律：**面数字只用来对账**；报面**要写明口径**）。

---

### ⑷ 收尾四件（**缺一不可**）

1. 在**本文件顶部追加一段**（只追加不改旧段），段内**必带下面的剩余任务队列整表**（把自己那格改 ✅）；
2. 同笔续写 [E6-执行清单.md 第 1 层头的轮次进度行](../../E6-执行清单.md)；
3. 勾格要勾全套；
4. **🔴 不许静默不做**：判「这条不做」必须把结论与理由写进交接段。

---

### 🔴 剩余任务队列（**20 行** —— 每轮收尾**整表复制**进新段，把自己那格改 ✅）

| # | 轮 | 编号 | 任务 | 详案 | 前置 | 提交轴 |
|:--:|:--|:--|:--|:--:|:--|:--|
| 0 | 第 0 格 | `#109r` | ✅ **审查 ＋ 派工**（总方案 ＋ 20 份任务书 ＋ 清单任务格 ＋ 本交接） | [00](00-整理档案.md) | — | 文档 |
| 1 | **1.31** | `#111a` | **命令 id 与协议 id 归属 · 评估** | [01](01-任务-命令id归属评估.md) | 第 0 格 | 文档 |
| 2 | 1.32 | `#111b` | 命令 id 归属 · 落地 | [02](02-任务-命令id归属落地.md) | 1.31 | 壳 ＋ SDK |
| 3 | 1.33 | `#111c` | 设置键归属 · 评估 | [03](03-任务-设置键归属评估.md) | 1.32 | 文档 |
| 4 | 1.34 | `#111d` | 设置键归属 · 落地 | [04](04-任务-设置键归属落地.md) | 1.33 | 壳 ＋ SDK |
| 5 | 1.35 | `#111e` | 外观族 id 归属 · 评估 | [05](05-任务-外观族id归属评估.md) | 1.34 | 文档 |
| 6 | 1.36 | `#111f` | 外观族 id 归属 · 落地 | [06](06-任务-外观族id归属落地.md) | 1.35 | 壳 ＋ SDK |
| 7 | 1.37 | `#111g` | 上下文旗子归属 · 评估 | [07](07-任务-上下文旗子归属评估.md) | 1.36 | 文档 |
| 8 | 1.38 | `#111h` | 上下文旗子归属 · 落地 | [08](08-任务-上下文旗子归属落地.md) | 1.37 | 壳 ＋ SDK |
| 9 | 1.39 | `#111i` | i18n 与黄灯族 · 评估 | [09](09-任务-i18n与黄灯族评估.md) | 1.38 | 文档 |
| 10 | 1.40 | `#111j` | i18n 与黄灯族 · 落地 | [10](10-任务-i18n与黄灯族落地.md) | 1.39 | 壳 ＋ SDK ＋ 文档 |
| 11 | **1.41** | `#111m` | 🔴 **迁移基建**（7 个清账轮的公共前置） | [11](11-任务-迁移基建.md) | 1.40 | 壳 |
| 12 | **1.42** | `#111n-1` | 🔴 **清账 · `file-tree`**（75 处，最大） | [12](12-任务-清账-file-tree.md) | **1.41** | 插件仓 ×1 ＋ 壳账 |
| 13 | 1.43 | `#111n-2` | **清账 · `editor`**（1 处） | [13](13-任务-清账-editor.md) | 1.41 | 插件仓 ×1 ＋ 壳账 |
| 14 | 1.44 | `#111n-3` | **清账 · `serial-monitor`**（旗子 2） | [14](14-任务-清账-serial-monitor.md) | 1.41 | 插件仓 ×1 ＋ 壳账 |
| 15 | 1.45 | `#111n-4` | **清账 · `marketplace`**（旗子 6） | [15](15-任务-清账-marketplace.md) | 1.41 | 插件仓 ×1 ＋ 壳账 |
| 16 | **1.46** | `#111n-5` | 🔴 **清账 · `settings`**（风险最高，跨仓配对） | [16](16-任务-清账-settings.md) | 1.41 ＋ 1.37 裁决 | 插件仓 ×1 ＋ 壳 |
| 17 | **1.47** | `#111n-6` | 🔴 **清账 · 主题族 10 只**（主题系统最高审查级） | [17](17-任务-清账-主题族.md) | 1.41 | 插件仓 ×10 ＋ 壳账 |
| 18 | 1.48 | `#111n-7` | **清账 · 其余插件**（0 处，但要「无面确认表」） | [18](18-任务-清账-其余插件.md) | 1.41 | 文档 |
| 19 | 1.49 | `#111k` | **收紧为红 ＋ 账定稿进 SDK ＋ 两棵树 ＋ 五轴发版** | [19](19-任务-门禁与作者面收口.md) | **1.42–1.48 全清完** | SDK(npm) ＋ 文档 ＋ 壳 |
| 20 | **1.50** | `#111l` | 🔴 **本轴收口复核**（**恒最后**） | [20](20-任务-本轴收口复核.md) | 全部 | 文档 |

> 🔴 **两条排序纪律（写死在 [00 §四](00-整理档案.md)，这里重述一遍）**：
> 1. **1.32/1.34/1.36/1.38 的判据先以「黄灯 ＋ 账」落地**（不立刻判红）——此时官方仓还没改名，判红会让插件仓 CI 处于一段无谓的红窗；
> 2. **1.49 才收紧为红**（清账完 ＋ 账定稿）——**这是「零例外」真正生效的那一刻**。

**系列外（不占上表序，可插任意空档；⛔ 不许塞进 1.50）**：
- **E6#110** 作者轴黄灯「观察名单」补齐（原属 CSS 系列外账）。
- 🆕 **E6#112** 插件域**关键帧引用**判据（原属 CSS 系列外账：壳侧判据⑧ 只做了宿主域＋共享域一半，**SDK 腿为空** ⇒ 插件写 `animation: foo` 而没有 `@keyframes foo` 时**动画静默消失、零报错**）——详案 [插件规范化层/08](../../插件规范化层/08-任务-插件域关键帧引用判据.md)。
- 🆕 **E6#113** 插件侧**死类**清账（原属 CSS 系列外账：实测 **10 处 / 3 仓**「有规则、无渲染方」＋ 收两条陈旧注释 ＋ 一把**只报不拦**的尺子进仓）——详案 [插件规范化层/09](../../插件规范化层/09-任务-插件侧死类清账.md)。
- 🔴 **键盘 chord 静默死键**（`normalizeKey` 把 `ctrl+k ctrl+h` 打乱成 `ctrl+h+k ctrl`）——**已立案、不属本轴**（它是单方的解析缺陷，不是「多方共写一本花名册」）。

> ⚠️ **登记来源（2026-09-17 补）**：`E6#112`／`E6#113` 是 CSS 系列「**系列外账**」五行终审时开的两个新号（终审表见 [样式命名空间归一化/35-系列收口报告.md §九](../样式命名空间归一化/35-系列收口报告.md)）。🔴 **它们是样式轴的账**（本轴只登记、不执行）；**登记在这里的唯一目的**：别让 **1.50 收口复核**时看不见它们。

---

### 🔴 交棒链与启动时机（**一处订正常见误记**）

```
CSS 系列：… → 1.28 → 1.29 → 1.20（收口 · 恒最后）
                                    ↓ 收口那一棒的交接段
                          第 0 格（= 本格：审查 ＋ 派工，不做任务）✅ 已完成
                                    ↓ 本段（给 1.31 的话）
                          1.31 → … → 1.41（迁移基建）→ 1.42–1.48（官方仓清账 7 格）
                               → 1.49（收紧＋作者面＋发版）→ 1.50（本轴收口复核 · 恒最后）
```

⚠️ **订正一处常见误记**：**1.29 之后还有 1.20**（用户明令「最后一个任务就是 1.20，不管怎么样反正这个任务必须有」）；**真正把棒交给本轴第 1 格的是 CSS「1.20 收口」那一棒**。

🔴 **两条 2026-09-16 订正（以此为准）**：
1. **审查 ＋ 派工这一格不必等 CSS 系列全部收口**（只读代码 ＋ 只写新档 ⇒ 工作区干净即可开工）——本格就是这么做的。
2. 🔴 **真正必须排队的是后续各格的执行**：它们要动 `src/` / `scripts/` / SDK / **官方插件仓**，**会与 CSS 系列抢文件** ⇒ **本轴第 1 格（1.31）必须在 CSS「1.20 收口」之后开工**。

🔴 **给 CSS 系列 1.20 收口那一棒的交代**：你收官时，**不用改本文件**——把**本段（第 0 格 · 「给下一个 AI」那一段）整段复制**给下一个 AI，并告诉他「**你是本轴第 1 棒（1.31）**」即可。若你在收官过程中发现**本轴的读数有出入**，**不要顺手改本轴的档**（会与派工基线打架）⇒ **写进你自己的交接段点名**，由 1.31 进场时对账。

🔴 **给所有清账轮（1.42–1.48）的交代**：**你们的作业包是 1.41 交付的**（改名执行器 / 形状证明 / 映射表格式 / 三条实证跑法）。**1.41 没交付就不要开工**。你们**互不依赖**（各改各的仓）⇒ 次序可换、甚至并行，但**都在 1.41 之后、1.49 之前**。

---

### 🔴 两条通用红线（每一棒都适用）

- **提交只按路径 `git add <本格文件>`，⛔ 禁 `git add -A` / `git add .`** —— 壳仓随时有别的会话在途（**第 0 格开工时实测工作区躺着 CSS 系列的 30+ 个改动**；且**索引是共享的**——第 0 格实测暂存区里躺着别的会话的 38 个文件 ⇒ **提交用 `GIT_INDEX_FILE` 建一个临时索引**，别把别人没做完的东西一起提上去）。
- **不推**：`git push` 必须等**用户本人当次点头**；推必带代理；推完 `git ls-remote --heads origin <branch>` **对 SHA**。npm 发版照 [作者轴 npm 发版 runbook](../../../../06-发布管理/作者轴npm发版.md) 五步（**显式 `--registry`**）；⛔ **撤版只能用户本人**。
