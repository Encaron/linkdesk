# 插件 — 文件树与 Monaco 编辑器

> 2026-07-24。**E3 架构完工后，第一批消费者插件。** 不占用编号——纯插件实现。

---

## 定位

| | |
|---|---|
| 类型 | **消费者插件**——使用 E1-E3 建的架构能力 |
| 前提 | E2d（侧栏扩展位）+ E3a（多 WebView）+ E2c（FileService）全部就绪 |
| 涉及架构改动 | **零。** 只写 `plugin.json` + React 组件 |

## 包含

| 插件 | 内容 | 依赖的架构能力 |
|---|---|---|
| **文件树** | 侧栏常驻面板——树形目录浏览、点击打开文件 | E2d 侧栏扩展位 + E2c FileService |
| **Monaco 编辑器** | 标签页内容——代码编辑、语法高亮、IntelliSense | E3a 独立 WebContentsView（Monaco 重型，需要独立进程） |

## 为什么它们不是架构

- 文件树：调 `window.linkdesk.filesystem.readdir()` → 渲染 `<Tree>` → 用户点文件 → 打开编辑器标签页。**壳不知道文件树的存在。**
- Monaco：`import * as monaco from 'monaco-editor'` → React 组件 → 渲染到自己的 WebContentsView。**壳不知道 Monaco 的存在。**

跟终端插件一样：壳只看到"有个标签页，里面是插件 X 的 WebContentsView"。壳不关心这个插件是终端、是编辑器、还是地图。

## 设计文档

| 文件树 | 内容 |
|---|---|
| 功能设计 | 10 任务 / ~710 行，详见 `01-文件树插件.md` |
| 侧栏坑位 | E2d 新设计 → `../E2_底层加固与侧栏扩展_暂定/04-E2d-侧栏扩展位.md` |

| Monaco | 内容 |
|---|---|
| 功能设计 | 4 任务 / ~220 行，详见 `02-Monaco编辑器.md` |
| 进程隔离 | E3a 提供 → `../E3_多WebView与壳收尾_暂定/01-E3a-多WebView进程隔离.md` |

---

> **← 依赖：** `../E3_多WebView与壳收尾_暂定/`（架构完工后才做）
> **同级别插件：** `../../04-未来规划/插件_工作台与OLED_暂定/`
