# 多串口——Phase 6 档案（#26-#30）

> 2026-08-17 立案（用户拍板升格正式 Phase）+ 同日第二轮反馈修订。**设计真相源 → [01-多串口设计.md](01-多串口设计.md)**。
> 清单条目 → `E5.8-执行清单.md` Phase 6（#26-#30，一个任务 = 一次 commit + 独立验收）。

## 一句话

SerialService 单口字段组 → `Map<portName, PortState>`；**有意设计"顶替"语义改为端口共存**；推送载荷加 portName（**现有通道演化——已有 API 优先：零新壳 API 面**）；同口二开拒绝 + 卸载连坐。

## 执行前必读

1. [01-多串口设计.md](01-多串口设计.md) §1 有意设计变更声明——先理解旧"顶替"语义为什么存在、现在为什么改
2. §2 细节级单口假设 16 条（S1-S16）——每条 = 文件:行 + 多口语义，执行时逐条消灭
3. §3 八决策 D1-D8 已拍板定死；§5 基建联动表（每个任务消耗 E5.8 Phase 0-5 什么铺垫）；§6 壳改动最小化——为什么必须动壳（三层论证）+ 为什么不是插件自己的功能 + 铁律分能力 + 专属假设 vs 版本化演进（壳 3 文件 / 插件 6 文件）；§7 **已有 API 优先审计**（新建壳 API 面 = 0，扩展现有 3 处——全部"没有它做不成"的最小集）；§8 函数级细节 + §9 行数估算；§10 回归验证（用户实机清单 7 项 + 矩阵三行补测）
4. 前置依赖：Phase 1.5 broadcast（#6.5）+ Phase 4 契约生成（#28 载荷变更走生成流程）+ #22.5 运行期校验兜底 + Phase 2 可逆注册主进程机制（#8——卸载连坐挂钩）
5. 破坏性变更窗口 = E5.8（上架前无第三方存量插件）——论证见设计文档 §8 #28

## 目录

```
多串口/
├── README.md           ← 本文件——开工说明
└── 01-多串口设计.md     ← 设计真相源（有意设计变更 + 单口假设全景 16 条 + 八决策 + 基建联动 + 壳改动最小化 + API 通用性 + 函数/行数 + 回归矩阵）
```
