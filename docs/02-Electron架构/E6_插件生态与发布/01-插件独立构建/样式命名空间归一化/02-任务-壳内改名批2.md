# 任务 `#109g-2`：壳内改名·批 2（select-box 家族）——第 1.6 轮

> **一句话**：把 SelectBox 家族的 16 个类名加 `ldk-` 前缀（含它的下拉骨架组件与 3 个消费它的测试文件），并从保留名清单摘掉 `selectbox`。
> **前置**：第 1.5 轮（批 1）已 ✅。
> **规模**：约 35 处 · 6 个文件。
> **提交轴**：壳（**不 bump 版本**——见 [01 §四](01-任务-壳内改名批1.md)）。
> **通用禁区 / 提交与收尾**：照 [01 §四、§五](01-任务-壳内改名批1.md)（同一条纪律，不重述）。

---

## 一、面清单（逐条实测）

### 1.1 组件自身（家族 16 个名字）

`src/components/shared/select-box/SelectBox.css` —— **21 处选择器**，涉及：

```
selectbox · selectbox-arrow · selectbox-arrow-up · selectbox-disabled · selectbox-dropdown
selectbox-empty · selectbox-item · selectbox-item-focus · selectbox-item-selected · selectbox-label
selectbox-list · selectbox-open · selectbox-search · selectbox-search-input · selectbox-swatch · selectbox-trigger
```

⇒ 全部加前缀：`.selectbox-trigger` → `.ldk-selectbox-trigger`（**只加前缀，词干一个字母都不改**）。

### 1.2 组件渲染点

| 文件 | 行 | 现状 |
|:--|:--|:--|
| `src/components/shared/select-box/SelectBox.tsx` | 135（根，模板串：`selectbox` + `selectbox-open` + `selectbox-disabled`）· 141（`-trigger`）· 146（`-label`）· 147（`-swatch`）· 150（`-arrow` + `-arrow-up`）· 161（`-search`）· 165（`-search-input`）· 174（`-empty`）· 179（`-item` + `-item-focus` + `-item-selected`）· 183（`-swatch`） | 全部加前缀 |
| `src/components/shared/select-box/SelectBoxDropdown.tsx` | 84（`selectbox-dropdown`）· 96（`selectbox-list`） | 加前缀 |

### 1.3 测试与消费方（**别漏**——它们在别的组件目录里）

| 文件 | 行 | 现状 |
|:--|:--|:--|
| `src/components/shared/select-box/SelectBoxDropdown.test.tsx` | 49 | `querySelector(".selectbox-dropdown")` |
| `src/components/shared/select-box/DynamicSelect.test.tsx` | 56 · 58 · 81 · 91 · 93 · 241 · 251 | `.selectbox-trigger` / `-item` / `-swatch` / `-label` |
| `src/components/shared/font-family-select/FontFamilySelect.test.tsx` | 32 · 34 · 35 · 76 · 98 | `.selectbox-label` / `-item` / `-trigger` |

### 1.4 保留名清单（**同笔必做**）

`packages/plugin-sdk/schemas/reserved-class-names.json` 的 `classes.shared` 组里**摘掉 `selectbox` 一项**（其余三组不动）。

---

## 二、步骤

1. 先改 `SelectBox.css`（21 处，单文件），再改两个 TSX，最后三个测试文件。
2. 摘 JSON 里的 `selectbox` 项。
3. `npm run check` EXIT=0。

**改名规则**：CSS `\.selectbox(?![a-zA-Z0-9_])` → `.ldk-selectbox`（这样 `.selectbox-open` → `.ldk-selectbox-open`、`.selectbox-trigger` → `.ldk-selectbox-trigger` 一并到位）。
⚠️ **`@keyframes selectbox-in` 不许动**——它是**关键帧**命名空间（另有登记表），本批只管类名。

---

## 三、判据（做完逐条给读数）

- [ ] `npm run check` EXIT=0（贴末尾三行）
- [ ] `grep -rnE "\.selectbox" src --include=*.css --include=*.tsx --include=*.ts | grep -v "\.ldk-selectbox"` 的命中**只剩注释与 `@keyframes selectbox-in`**（贴输出）
- [ ] `npx vitest run src/components/shared/select-box src/components/shared/font-family-select` 全绿
- [ ] JSON 里 `classes.shared` 不含 `selectbox`
- [ ] `git diff --stat`

---

## 四、禁区（本批专属）

1. **不许拆组件间的族名借用**：`Combobox` 渲染 `selectbox-open` / `selectbox-disabled`（借用 SelectBox 的修饰名）是**有意设计**——本批**只加前缀**，不重命名、不拆开、不改 CSS 归属（[00 §六·3](00-整理档案.md)）。
2. **不许动 `combobox` / `sle` / `mdv`**（批 3、批 4 的任务）。
3. **不许动插件仓**（第 1.11 轮）。
4. **不许改 `selectbox-in` 关键帧**。
