# 07 — 执行计划

> E3 架构完工后执行。纯插件——不碰 `src/core/`、不碰 `electron/`、不碰 `App.tsx`。

---

## 前置依赖

| 依赖 | 状态 | 说明 |
|------|:--:|------|
| E3a 多 WebView | ✅ | 工作台 = 独立 WebContentsView |
| CardRegistry | ✅ Phase 5 | 卡片类型注册 |
| RingBuffer + DataDispatch | ✅ Phase 5 | 按 cardId 推数据 |
| ProtocolParser | ✅ `src/core/` | 方括号协议解析 |
| ProtocolRegistry | ✅ `src/core/` | 协议插件注册 |
| E2c FileService | ✅ | 布局 JSON 读写 |
| E3b ThemeEngine | ✅ | CSS 变量跨进程 |
| DialogService | ✅ | 删除确认弹窗 |
| #59c `<SelectBox>` | 📋 E3f | 弹窗下拉框 |
| #59d0 `dependsOn` | 📋 E3f | 弹窗颜色条件显隐 |
| #59e `<ColorPicker>` | 📋 E3f | 弹窗颜色选择器 |

---

## 阶段 1：画布底座（~250 行）

> 目标：无限画布 + 网格 + 缩放，能看到卡片占位。

| # | 任务 | 行数 | 验证 |
|:--:|------|:--:|------|
| W1 | 工作台 plugin.json + index.tsx 骨架——`viewRole: "tab"` | ~30 | 图标栏出现工作台图标→点击→空白画布 |
| W2 | `<InfiniteCanvas>`——CSS `transform: translate/scale` + 滚轮/中键拖拽/抓手 | ~80 | 滚轮滚动→画布平移→Ctrl+滚轮→缩放→中键拖拽→平移 |
| W3 | `<GridOverlay>`——CSS `background-image` 点阵/线阵显示网格 | ~30 | 缩放时网格跟随变化——80px 间距 dot pattern |
| W4 | 缩放控件——右下角浮动条（⊖/⊘/⊕/滑块/百分比） | ~50 | 点 ⊕→放大→百分比更新→滑块位置更新→⊘所有卡片刚好在视野 |
| W5 | `<CardChrome>` 底座——标题栏 + 边框 + 圆角 + 拖拽 + resize 把手（不含内容区） | ~50 | 右键空白区→选类型→底座出现在网格位置→内容区渲染对应 type 组件 |
| W6 | 网格吸附 + 拖拽 + 碰撞检测 | ~30 | 拖卡片→松手→吸附到最近网格点→不能重叠 |

---

## 阶段 2：卡片实例系统（~250 行）

> 目标：占位变成真实卡片——绑定 cardId + 接收数据。

| # | 任务 | 行数 | 验证 |
|:--:|------|:--:|------|
| W7 | `CardInstance` 数据模型——id/type/cardId/x/y/w/h/settings | ~20 | TypeScript 接口定义 + tsc 零错误 |
| W8 | `useCardInstances()` hook——状态管理：增删改查 + 位置更新 | ~60 | 添加卡片→状态更新→渲染→删除→状态移除 |
| W9 | CardRegistry 消费——`getCardTypes()` → 添加卡片面板 | ~30 | 添加卡片面板列出所有已注册类型 |
| W10 | 卡片渲染引擎——按 type 渲染对应 React 组件 | ~40 | 占位替换为真实卡片组件 |
| W11 | cardId 绑定——DataDispatch 订阅 + OnData 回调 | ~40 | 终端数据到达→卡片实时更新显示 |
| W12 | 布局持久化——`FileService` 读写 `workspace/*.json` | ~60 | 拖卡片→1s 后自动保存→F5→布局恢复 |

---

## 阶段 3：内置卡片类型——显示类（~300 行）

> 目标：5 种显示类卡片完整实现。

| # | 任务 | 行数 | 验证 |
|:--:|------|:--:|------|
| W13 | `ValueCard`——数值大字 + 单位 + 小数位 | ~50 | 绑定 cardId→数据到达→数字显示→数字变化→过渡动画 |
| W14 | `GaugeCard`——SVG 弧形仪表盘 + 指针 + min/max/颜色阈值 | ~80 | 数据到达→指针转动→超阈值变红 |
| W15 | `LineChartCard`——Canvas 折线图/面积图 + 时间窗口 | ~80 | 数据到达→push 到历史→Canvas 重绘→窗口 60s 后旧数据滚出 |
| W16 | `ProgressCard`——水平填充条 + 百分比 + fill/segments 样式 | ~40 | 数据到达→填充宽度变化→300ms 过渡 |
| W17 | `IndicatorCard`——彩色圆点 + 状态映射 + 条件规则表 | ~60 | 数据到达→规则求值→第一条匹配→对应颜色+闪烁→默认规则兜底 |

---

## 阶段 3b：内置卡片类型——虚拟类（~120 行）🆕

| # | 任务 | 行数 | 验证 |
|:--:|------|:--:|------|
| W17a | `ExpressionCard`——公式编辑器 + 依赖自动追踪 + 实时求值 | ~70 | 输入 `temp_c * 9/5 + 32`→自动订阅 temp_c→数值卡片更新→表达式卡片同步 |
| W17b | `MultiFieldCard`——多字段展示 + 3D 方向点 Canvas + 柱状图 | ~50 | `[imu, 0.12, -0.05, 9.81]`→三个字段显示→方向点 Canvas 更新 |

---

## 阶段 4：内置卡片类型——控制类（~200 行）

> 目标：3 种控制类卡片 + 双向数据流。

| # | 任务 | 行数 | 验证 |
|:--:|------|:--:|------|
| W18 | `SliderCard`——拖动条 + 数值 + 松手发送 | ~70 | 拖滑块→本地值变→松手→发送 `[cardId, value]`→MCU 回发→确认 |
| W19 | `ButtonCard`——按钮 + 点击发送 + 状态回显 | ~50 | 点击→发送 `[cardId, label]`→MCU 回发→状态文字更新 |
| W20 | `SwitchCard`——拨动开关 + ON/OFF + 双向确认 | ~80 | 点开关→动画拨动→发送→MCU 回发→以回发值为准 |

---

## 阶段 5：卡片交互 + 弹窗 + Chrome（~300 行）

> 目标：完整的交互体验——hover/右键/弹窗/全屏。

| # | 任务 | 行数 | 验证 |
|:--:|------|:--:|------|
| W21 | `<CardChrome>`——标题栏 + 双击重命名 + ✕/≡ hover 出现 | ~50 | hover 2s→chrome 淡入→双击标题→原地编辑→回车确认 |
| W22 | `<CardSettingsDialog>` 弹窗框架——分组 + 下拉 + 确定/取消/重置 | ~60 | 右键→弹窗出现→改设置→确定→卡片即时更新 |
| W23 | 弹窗内容——外观组（大小枚举/颜色 dependsOn） | ~40 | 改大小→卡片即时缩放→改颜色→强调色变 |
| W24 | 弹窗内容——数据组（单位/小数位/绑定 cardId/协议） | ~30 | 改单位→显示更新→改 cardId→接收新数据源 |
| W25 | 弹窗内容——子项组（勾选/排序/样式配置） | ~40 | 勾选进度条→子项出现→勾掉→子项消失 |
| W26 | Resize 把手——右下角拖拽循环切换尺寸 | ~30 | 拖把手→循环 allowedSizes→松手吸附 |
| W27 | 全屏浮层——折线图/仪表盘最大化 | ~50 | 双击折线图→全屏遮罩→Esc 退出 |

---

## 阶段 6：侧栏 + 顶栏 + 卡片集管理（~200 行）

> 目标：完整的区域布局——侧栏/顶栏/画布三区。

| # | 任务 | 行数 | 验证 |
|:--:|------|:--:|------|
| W28 | `<WorkspaceSidebar>`——卡片集列表 + 新建/重命名/删除 + 协议激活勾选 | ~80 | 侧栏列出卡片集→点切换→勾选/取消协议→下拉框同步更新 |
| W29 | `<WorkspaceToolbar>`——新建卡片集/导入/导出 + 协议选择器（仅显示已勾选协议） | ~50 | 协议下拉只显示侧栏勾选的协议 |
| W30 | 右键画布空白区——"添加卡片"菜单（列出所有类型） | ~30 | 右键→菜单→选"数值卡片"→画布出现新卡片 |
| W31 | 导入/导出——`.linkdesk-workspace` 打包/解包 | ~40 | 导出→选文件夹→生成文件→另一台电脑导入→布局恢复 |
| W32 | 撤销/重做——操作历史栈 | ~30 | 拖卡片→Ctrl+Z→卡片回原位→Ctrl+Shift+Z→卡片回到新位置 |

---

## 阶段 7：测试 + 打磨（~150 行）

| # | 任务 | 行数 | 验证 |
|:--:|------|:--:|------|
| W33 | 卡片类型单元测试——每个类型 OnData 正确渲染 | ~40 | vitest 8 条——每个卡片类型 1 条 |
| W34 | 布局持久化测试——保存→加载→一致性 | ~30 | vitest 4 条 |
| W35 | 网格吸附 + 碰撞检测测试 | ~30 | vitest 4 条 |
| W36 | 交互集成测试（需要 Electron 环境——手动验证） | ~50 | 完整手动验证清单 |

---

## 总览

| 阶段 | 内容 | 行数 |
|------|------|:--:|
| 1 | 画布底座 | ~250 |
| 2 | 卡片实例系统 | ~250 |
| 3 | 内置卡片——显示类 | ~300 |
| 4 | 内置卡片——控制类 | ~200 |
| 5 | 交互 + 弹窗 + Chrome | ~300 |
| 6 | 侧栏 + 顶栏 + 卡片集 | ~200 |
| 7 | 测试 + 打磨 | ~150 |
| **合计** | | **~1,650** |

---

> **← 上一份：** [06-子项系统](06-子项系统.md)
> **← 索引：** [00-README](00-README.md)
