# E6#95e：本包**无作用域**（`create-linkdesk-plugin`），所以不能用 `@linkdesk:registry=` 那条——
# 必须直接改**默认源**，否则 `npm publish` 会落到用户级默认源（本机实测 = registry.npmmirror.com，
# 镜像只读、发不上去）。根工程 .npmrc 的那条作用域规则**管不到子目录**（npm 只读当前目录的 .npmrc，
# 不向上递归），故每个可发布包目录下都各有一份——本文件即 create-linkdesk-plugin 的那一份。
# 新增可发布包时照抄一份（有作用域 → 用 `@linkdesk:registry=`；无作用域 → 用 `registry=`）。
registry=https://registry.npmjs.org/
