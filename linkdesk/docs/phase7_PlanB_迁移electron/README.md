# Phase 7 Plan B — Electron 迁移

> 此文件夹包含 Tauri → Electron 迁移的完整设计文档。
> 与 `phase7_多WebView与编辑能力/` 是两套独立方案——前者 Tauri add_child，此处 Electron WebContentsView。

---

## 给新 AI 的阅读顺序

**按编号顺序读。** 每份文档末尾有"下一份"的链接。不跳读——前因后果累积。

| # | 文档 | 内容 | 为什么读 |
|:--:|------|------|------|
| 1 | `01-设计概述.md` | 总体设计、决策记录、架构全景 | 知道"选了 Electron、选了独立进程、选了 React 组件"三个核心决策和为什么 |
| 2 | `02-插件隔离模型.md` | 为什么选独立 WebContentsView 而非共享 ExtHost | 这是整个方案最关键的架构决策——VS Code 的 ExtHost 是历史妥协，不是最佳 |
| 3 | `03-插件入口方案.md` | React 组件 vs activate()——利弊、AI 友好度、取舍 | 决定"插件怎么写"——一人 + AI 开发模式下的最优解 |
| 4 | `04-迁移方案.md` | 通查工程后的逐文件迁移范围、7 步执行计划 | 执行手册——改什么不改什么，每步可验证 |
| 5 | `05-API与协议设计.md` | window.linkdesk API、preload 设计、linkdesk:// 协议 | 插件开发者的接口契约——迁移前定稿 |
| 6 | `06-实施顺序.md` | 阶段划分、分支策略、与 Phase 6-8 的关系 | 大局观——现在做什么、之后做什么 |
| 7 | `07-体积与内存分析.md` | 安装包体积 + 运行时内存——Tauri vs Electron 真实数字 | 不美化——大了就是大了。然后看能不能接受 |
| 8 | `08-多WebView-vs-ExtHost-开销对比.md` | 独立 WebContentsView vs 共享 ExtHost——进程拓扑、内存、启动、崩溃逐项对比 | 冷冰冰的数字——两种隔离模型的真实开销。进程级 WebviewView 反而更贵 |

---

## 核心决策速查

| 决策 | 结论 | 一句话理由 |
|------|------|------|
| 框架 | **Electron** | 多进程管理是 Chromium 标配，WebContentsView 是 stable API |
| 插件隔离 | **独立 WebContentsView per 插件** | 共享 ExtHost 是 VS Code 的历史包袱——2015 年内存妥协。进程级 WebviewView 比 WebContentsView 更费内存（多一个 ExtHost 进程），详见 08 |
| 插件入口 | **React 组件** | AI 训练数据最密集的范式——写一个文件 = 一个插件 |
| 系统权限 | **preload + contextBridge** | 对标 Tauri Capability——声明式权限，运行时不可绕过 |
| 资源加载 | **linkdesk:// 自定义协议** | 归一化入口，未来可扩展至网络/数据库/加密包 |

---

## 文档规范（后续 AI 写新文档时参照）

1. **一份文档一个主题。** 不把架构决策、API 设计、迁移步骤塞进同一份
2. **"为什么"在"怎么做"前面。** 先讲前因后果，再列执行步骤
3. **决策有证据。** 说"VS Code 如何"要有官方文档或源码引用。说"更好"要有对比
4. **文件名格式：** `两位数字-中文标题.md`。数字表示阅读顺序
5. **末尾有导航：** 每份文档结束时标注"上一份 / 下一份"
6. **元数据行：** 文档首行 = `> 日期。定位——一句话说明此文档在整套方案中的位置`
