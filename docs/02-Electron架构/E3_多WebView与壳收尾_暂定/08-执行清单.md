# E3 执行清单——多 WebView + 壳收尾（76 任务，~2,914 行）

> 机械执行。做完一步打勾一步。四层顺序执行——每层依赖前一层。
> 同层内 E3b∥E3c 可并行，E3d∥E3e∥E3f 可并行（E3d 额外依赖 E3b+E3c）。
> 详细设计见各子文档（E3a-E3g）。

---

## 第 1 层：E3a 多 WebView 进程隔离（13 任务，~695 行）

> ⚠️ **必须最先做。** 新插件一出生就在独立 WebView 里——不用先单线程再迁移。
> 详细设计见 `01-E3a-多WebView进程隔离.md`。

### 底座——WindowManager + PluginViewRegistry

- [x] **#24** WindowManager——WebContentsView 创建/销毁/聚焦
  文件：`electron/window-manager.ts`（新） | ~330 | 验证：创建→contentView.addChildView→关闭→webContents.close() 无泄漏

- [x] **#25** PluginViewRegistry——插件 ID→WebContentsView 映射 + bounds 管理 + 重载
  文件：`electron/plugin-view-registry.ts`（新） | ~100 | 验证：Map 增删查 + 重载后 pluginId 不变

- [x] **#25a** 资源休眠——后台 WebView 降频 + 内存压力检测
  文件：`electron/window-manager.ts` | ~40 | 验证：隐藏标签页→setBackgroundThrottling(true)；每 30s getProcessMemoryInfo() 采样

- [x] **#25b** 🆕 保活宽限期——关闭标签页延迟销毁 WebView（60s 可复用）+ 内存压力联动
  文件：`electron/window-manager.ts` | ~25 | 验证：关终端→60s 内重开→WebView 复用零重建；超时→销毁；内存>1GB→无视宽限期立即销毁

### 通信——IpcBridge

- [x] **#26** IpcBridge——核心服务代理（Config/Command/Menu IPC handler 注册）
  文件：`electron/ipc-bridge.ts`（新）+ `src/core/IpcBridgeHandler.ts`（新） | ~140 | 验证：ipcRenderer.invoke('plugin:ipc',...)→壳侧 handler→返回结果

- [x] **#27** IpcBridge——事件管道（Events/Data push 通道 + 请求队列串行化）
  文件：`electron/ipc-bridge.ts` | ~50 | 验证：串口数据推送到插件 WebView，<16ms 延迟

### 插件侧

- [x] **#28** preload-plugin.ts——插件侧精选 API（含 filesystem 受限读写）
  文件：`electron/preload-plugin.ts`（新） | ~80 | 验证：contextBridge 白名单审计 + IPC 回调模板（ref 桥接 + cleanup + 超时）

- [x] **#29** MainContent 改为 WebContentsView placeholder 管理
  文件：`src/components/MainContent.tsx` | ~100 | 验证：壳标签页切换→WebContentsView 显隐

### 壳侧归一化

- [x] **#29a** SidebarTabSync——侧栏↔标签页三向同步归一化
  文件：`src/core/SidebarTabSync.ts`（新） | ~30 | 验证：侧栏点 session→主区切换→侧栏高亮同步

### 现有插件迁移——逐个独立

- [x] **#30** terminal 插件迁移——useSerialContext→useIpcSerialContext + useSendData→ipc
  文件：`plugins/terminal/` | ~50 | 验证：终端收发不变

- [x] **#31** marketplace 插件迁移——plugin.json 列表读 IPC（~88 行，9 方法 + 异步改造）
  文件：`plugins/marketplace/` | 验证：插件市场列表正常

- [x] **#32** settings 插件迁移——配置读写 IPC（零行——core:true 壳组件 re-export，无独立数据路径）
  文件：`plugins/settings/` | 验证：设置页读写正常

- [x] **#32a** 🆕 workspace 插件迁移——卡片网格 IPC（零行——壳组件 re-export，无独立数据路径）
  文件：`plugins/workspace/` | 验证：工作台卡片正常

---

## 第 2 层：E3b 主题引擎 ∥ E3c 语言引擎（可并行）

> ⚠️ 依赖 E3a（多 View 才能跨进程广播）。两者互不依赖——可并行。
> 详细设计见 `02-E3b-主题引擎跨进程.md` + `03-E3c-语言引擎跨进程.md`。

### E3b：主题引擎跨进程（10 任务，9/10 ✅，~440 行）

- [x] **#33** ThemeRegistry + contributes.themes + 三层退路
  文件：`src/core/ThemeRegistry.ts`（新） | +80 | 验证：插件声明 contributes.themes→注册→三层退路逐层回退

- [x] **#34** 出厂 Dark/Light 迁移到 contributes.themes 格式
  文件：`plugins/theme-dark/` + `plugins/theme-light/`（新） | +20 | 验证：Dark/Light 以插件形式存在→可卸载
  → 🔥 **9 个 bug 连环**（查询面未归一化——第一个非视图插件踩进盲区）。教训见 memory `non-view-plugin-normalization-lesson`。

- [x] **#34a** 🆕 合并 theme-dark + theme-light → `plugins/theme-defaults/`（"官方主题"），一个 plugin.json 含 dark/light 两个 contributes.themes 条目
  文件：`plugins/theme-defaults/plugin.json`（合并） | ~5 | 验证：插件市场→"官方主题"→齿轮→卸载全部=无主题可用

- [x] **#35** CSS 变量跨进程广播（theme:changed IPC）
  文件：`electron/ipc-bridge.ts` + `preload-shell.ts` + `preload-plugin.ts` + `ThemeEngine.ts` | +55 | 验证：壳切主题→所有插件 WebView 的 :root CSS 变量同步

### 🔥 #36 主题浏览器 UI——归一化路线（5 子任务，~230 行）

> 对标 VS Code `QuickPick`——CommandPalette / ThemeBrowser / LanguagePicker 共用一个浮动面板组件。
> 不做归一化 → 3 个组件各写一遍 = 复制粘贴地狱。归一化 → 第一遍 ~220 行，后面每个 ~40 行。

- [x] **#36a** QuickPick 归一化浮动面板组件——从 CommandPalette 提取公共壳
  文件：`src/components/shared/QuickPick.tsx`（新） | +130 | 验证：不引入行为变化——Ctrl+Shift+P 行为和重构前完全一致（暂不重构 CommandPalette 先验证组件可用性）

- [x] **#36b** CommandPalette 改用 QuickPick——删除已提取代码，import QuickPick
  文件：`src/components/shared/CommandPalette.tsx` | ~40（重构，净减 ~90） | 验证：Ctrl+Shift+P 搜索/选中/执行和重构前完全一致

- [x] **#36c** ThemeBrowser 基于 QuickPick——预览（↑↓ 即时 apply + Esc 回退）+ 提交（Enter 写配置）
  文件：`src/components/ThemeBrowser.tsx`（新） | +50 | 验证：打开面板→↑↓预览主题→Enter 切换→Esc 回退原始主题

- [x] **#36d** 命令注册 + 快捷键 `Ctrl+K Ctrl+T`——`workbench.action.selectTheme`
  文件：`src/App.tsx` | +5 | 验证：Ctrl+K Ctrl+T 弹出主题浏览器

- [x] **#36e** 齿轮菜单"选择颜色主题"入口——有 `contributes.themes` 的插件右键菜单加一项
  文件：`src/core/MenuRegistry.ts` | +5 | 验证：官方主题齿轮→选择颜色主题→浮动面板弹出

### 🔥 #36e2 桌子管理规则落地——三条代码机制（3 任务，~75 行）

> 2026-07-26。大厅模型补完——桌子管理规则不是靠人记，是代码自动保证。
> 三条机制做完后，#36f 的 H2/M1/L1 等漏洞自然消失或大幅简化。
> 详细设计见 `hall-architecture-model.md` §桌子管理规则§落地机制。

- [ ] **#36e2a** 机制 1——`RegistryBase` 基类：登记注销自动对称（不靠 lifecycle.ts 手动加）
  文件：`src/core/RegistryBase.ts`（新） | ~40 | 验证：新 Registry 继承 → 调 `markPlugin` → 插件卸载时 `unregisterAll` 自动被调用

- [ ] **#36e2b** 机制 2——路由表自刷新：FactorySlots 订阅 lifecycle，插件进出自动重扫描
  文件：`src/core/FactorySlots.ts` | +20 | 验证：卸载设置插件→slot 清空→安装新设置插件→slot 更新→Ctrl+, 打开新插件

- [ ] **#36e2c** 机制 3——单真源：ThemeRegistry 改读 ThemeEngine，消除双写
  文件：`src/core/ThemeRegistry.ts` + `src/core/ThemeEngine.ts` | +15 | 验证：`loadThemePlugin` 旧格式主题→`revertThemeIfCurrent` 能找到归属→正确回退

### 🔥 #36f 主题/配置审计与加固——13 漏洞（7 批次，~185 行）

> 2026-07-26 敌对审计。详细设计见 `03-E3b-主题配置审计与加固.md`。
> 对标 VS Code 健壮性：主题安装/卸载/回退在任何时序下不出错。

#### 批次 1：主题注册归一化 + 内置 fallback（H2 + M2，~40 行）

- [ ] **#36f1** H2——`loadThemePlugin` 旧格式主题同步写入 ThemeRegistry
  文件：`src/pluginLoader/loader.ts` | +15 | 验证：安装旧格式主题插件→卸载→当前主题正确回退（不退化到裸 CSS）

- [ ] **#36f2** M2——App 启动注册内置 Dark/Light fallback 主题
  文件：`src/App.tsx` + `src/core/ThemeEngine.ts` | +25 | 验证：卸载全部主题插件→设置下拉框仍有 Dark/Light

#### 批次 2：枚举更新归一化（M5 + M4，~20 行）

- [ ] **#36f3** M5——删除 App.tsx 二次 `registerConfiguration("app.theme", ...)`，枚举更新统一走 `syncAppThemeEnum`
  文件：`src/App.tsx:240-258` + `src/pluginLoader/loader.ts` | +10（净删 ~15） | 验证：安装主题→设置下拉框即时更新（不走两次注册竞争）

- [ ] **#36f4** M4——`loadPlugin` 中 `contributes.themes` 存在时跳过旧格式 `manifest.themes`
  文件：`src/pluginLoader/loader.ts:427-441` | +10 | 验证：两个格式同存的插件→不重复注册→不产生 console.warn

#### 批次 3：FactorySlots 热换（H1，~30 行）

- [ ] **#36f5** H1——`FactorySlots` 加 `refreshFromPlugins()` 方法
  文件：`src/core/FactorySlots.ts` | +15 | 验证：安装新设置插件→Ctrl+, 打开新插件

- [ ] **#36f6** H1——lifecycle 消费端调 `factorySlots.refreshFromPlugins()`
  文件：`src/pluginLoader/lifecycle.ts` | +15 | 验证：卸载旧设置插件→slot 清空→安装新设置插件→slot 更新

#### 批次 4：SettingsView 跳转（M1，~25 行）

- [ ] **#36f7** M1——`ConfigurationRegistry` 加 `onRequestSettingsGroup` Emitter
  文件：`src/core/ConfigurationRegistry.ts` | +10 | 验证：齿轮"设置"→事件 fire→SettingsView 收到

- [ ] **#36f8** M1——`SettingsView` 订阅 Emitter + mount 消费变量（双通道）
  文件：`src/components/views/SettingsView.tsx` | +15 | 验证：设置已打开→齿轮"设置"另一插件→跳转到该插件分组

#### 批次 5：枚举验证（M3，~10 行）

- [ ] **#36f9** M3——`setConfigurationValue` 写入前做 enum 验证
  文件：`src/core/ConfigurationService.ts` | +10 | 验证：手动写 settings.json `app.theme: "dead"`→重启→warn + 不崩（退到 index.css fallback）

#### 批次 6：低严重度补强（L1-L5，~40 行）

- [ ] **#36f10** L1——gear menu onClose 清理 context keys
  文件：`plugins/marketplace/src/sidebar.tsx` | +8 | 验证：齿轮菜单关闭→context keys 全为 false

- [ ] **#36f11** L2——`syncAppThemeEnum` 时序注释标记（不修改代码）
  文件：`src/pluginLoader/loader.ts` | +3 | 验证：注释准确描述 mutate-onDidInstall 时序依赖

- [ ] **#36f12** L3——`ThemeBrowser` 订阅 `onPluginLifecycleChange`
  文件：`src/components/ThemeBrowser.tsx` | +10 | 验证：ThemeBrowser 打开时卸载主题→列表即时收缩

- [ ] **#36f13** L4——`revertThemeIfCurrent` 两处调用点加 `await`
  文件：`src/pluginLoader/loader.ts` | +2 | 验证：disablePlugin/uninstallPlugin 中 revert 完成后才 syncAppThemeEnum

- [ ] **#36f14** L5——旧格式 `manifest.themes` / `manifest.languages` `else if` → 独立 `if`
  文件：`src/pluginLoader/loader.ts` | +4 | 验证：同时声明旧格式 theme+language 的插件→两者都加载

#### 批次 7：生产环境加载（H3，~20 行，需先确认 linkdesk:// 协议路由）

- [ ] **#36f15** H3——`loadPluginRuntime` 生产环境用 `linkdesk://` 替代 `/@fs/`
  文件：`src/pluginLoader/loader.ts` | +20 | 验证：`electron:build`→安装插件→插件可用（零重启）

### 🔥 #36g 功能补全——齿轮菜单贡献检查对标 VS Code（4 任务，~20 行）

> 对照 VS Code per-plugin 齿轮菜单：`applyExtensionContextKeys` 设了 5 个 key，当前只消费 2 个。
> 缺 `extensionHasKeybindings`（key 都没设）+ `extensionHasLanguages`/`extensionHasIconThemes`（key 设了但没菜单项消费）。
> 所有改动限于 `plugins/marketplace/src/sidebar.tsx`——核心零改动（`gear-menu-normalization.md` 机械规则）。

- [ ] **#36g1** K1——`applyExtensionContextKeys` 加 `extensionHasKeybindings`
  文件：`plugins/marketplace/src/sidebar.tsx` | +2 | 验证：声明 `contributes.keybindings` 的插件→`extensionHasKeybindings`=true

- [ ] **#36g2** K2——`ensureMarketplaceCommands` 加 `"workbench.action.openExtensionKeybindings"` 菜单项
  文件：`plugins/marketplace/src/sidebar.tsx` | +3 | 验证：有 `contributes.keybindings` 的插件→齿轮出现"键盘快捷方式"

- [ ] **#36g3** K3——`ensureMarketplaceCommands` 加语言菜单项——消费 `extensionHasLanguages`
  文件：`plugins/marketplace/src/sidebar.tsx` | +3 | 验证：有 `contributes.languages` 的插件→齿轮出现语言入口

- [ ] **#36g4** K4——`ensureMarketplaceCommands` 加图标主题菜单项——消费 `extensionHasIconThemes`
  文件：`plugins/marketplace/src/sidebar.tsx` | +3 | 验证：有 `contributes.iconThemes` 的插件→齿轮出现图标主题入口

---

### 🔥 #36h E2b 归一化补全——串口术语迁出核心（3 任务，~60 行）

> 2026-07-26 扩展审计。E2b #7 #8 声明 DONE 但未落地——`portName`/`PortInfo`/`onDidChangePortState` 仍在核心代码。
> 详细设计见 `03-E3b-主题配置审计与加固.md` §六.2。

- [ ] **#36h1** M6a——`CoreEvents.onDidChangePortState` → `onDidChangeSourceState`，`portName` → `sourceName`；`SourceStateContext.PortInfo` → `SourceInfo`；`baudRate` 标注为串口特定
  文件：`src/core/CoreEvents.ts` + `src/core/SourceStateContext.tsx` | +10 | 验证：grep "portName\|PortInfo\|onDidChangePortState" src/core/ 零结果（除注释/迁移说明）

- [ ] **#36h2** M6b——`ContextKeyService.initCoreKeys`：`portOpen` → `sourceOpen`，`portName` → `sourceName`
  文件：`src/core/ContextKeyService.ts` | +5 | 验证：grep "portOpen\|portName" src/core/ContextKeyService.ts 零结果

- [ ] **#36h3** M6c——`resetTerminalCounter`/`resetWorkspaceCounter` → 通用 `resetPluginCounter(pluginId)`；App.tsx `terminal-` 正则 → `syncCountersAfterRestore`
  文件：`src/hooks/tabIdentity.ts` + `src/App.tsx` | +45 | 验证：grep "resetTerminalCounter\|resetWorkspaceCounter" src/ 零结果；`_counters["terminal"]` → `_counters[type]`

---

### 🔥 #36i 硬约束修复——补违例（5 任务，~39 行）

> 2026-07-26 扩展审计。4 个硬约束违规 + 1 个数据泄漏。
> 详细设计见 `03-E3b-主题配置审计与加固.md` §六.1 + §六.3。

- [ ] **#36i1** 🔥 H4——`coreCommands.ts`：18 处 `title`/`category`/`group` 硬编码中文 → `t("...")`
  文件：`src/core/coreCommands.ts` | +20 | 验证：grep `title:\s*"[一-鿿]` src/core/coreCommands.ts 零结果（所有 title/category/group 都走 `t()`）

- [ ] **#36i2** 🔥 H5——`lifecycle.ts` 消费端 2b：加 `unregisterPluginFileAssociations(pluginId)` 调用
  文件：`src/pluginLoader/lifecycle.ts` | +1 | 验证：grep "unregisterPluginFileAssociations" src/pluginLoader/lifecycle.ts 有结果 + 在 `onWillUninstall` handler 内

- [ ] **#36i3** 🔥 H6——`lifecycle.ts`：加消费端 i18n 资源移除——`i18n.removeResourceBundle(lang, pluginId)`
  文件：`src/pluginLoader/lifecycle.ts` + `src/pluginLoader/loader.ts` | +10 | 验证：安装语言插件→卸载→i18next.getResourceBundle(lang, pluginId) 返回 undefined

- [ ] **#36i4** M7——`tabIdentity.ts` `FALLBACK_META`：`identityField` 映射加 `@deprecated` 注释——计划迁入 `plugin.json` `tabBehavior.identityField`
  文件：`src/hooks/tabIdentity.ts` | +5 | 验证：注释准确描述迁移方向

- [ ] **#36i5** M8——`LayoutService.ts` `LEGACY_TAB_IDS`：加 `@deprecated` 注释 + B78 迁移日期
  文件：`src/core/LayoutService.ts` | +3 | 验证：注释准确描述遗留性质

---

### #36j 归一化清理——消重复（3 任务，~43 行）

> 2026-07-26 扩展审计。4 处代码重复——提取共享函数。
> 详细设计见 `03-E3b-主题配置审计与加固.md` §六.4。

- [ ] **#36j1** L6——`loader.ts`：提取 `applyPostLoadSteps(pluginId, manifest, reason)`——消 `loadPlugin`/`loadPluginRuntime` ~50 行重复
  文件：`src/pluginLoader/loader.ts` | +25 | 验证：两个函数调同一个 `applyPostLoadSteps`；tsc 零错误

- [ ] **#36j2** L8——`loader.ts`：提取 `extractThemeColors(data)`——消 3 个重复的 5 行颜色提取循环
  文件：`src/pluginLoader/loader.ts` | +8 | 验证：grep "Object.entries.*colors\|Object.keys.*colors" loader.ts 仅 `extractThemeColors` 函数体内出现

- [ ] **#36j3** L9——`lifecycle.ts`：提取 `updateIconOrder(pluginId, mode)`——合并 `appendToIconOrder`/`removeFromIconOrder`
  文件：`src/pluginLoader/lifecycle.ts` | +10 | 验证：两个消费端调同一个 `updateIconOrder`

---

### #36k 设计债务——修边角（3 任务，~22 行）

> 2026-07-26 扩展审计。4 个设计级问题——取 3 个可立即修的。
> 详细设计见 `03-E3b-主题配置审计与加固.md` §六.5。

- [ ] **#36k1** D1——`preload-plugin.ts`：安全注释 vs 实际暴露不一致——删除 `dialog.open` 暴露或更新注释（按 Encaron 决策）
  文件：`electron/preload-plugin.ts` | +2 | 验证：代码和注释一致

- [ ] **#36k2** M9——终端插件：命令注册加清理（`useEffect` 返回注销函数）
  文件：`plugins/terminal/src/index.tsx` | +15 | 验证：终端标签页关闭→命令面板 `terminal.*` 命令消失

- [ ] **#36k3** D3——工作区插件：`viewRole: "sidebarPrimary"` 声明修复——删除或补 `sidebar` 入口点
  文件：`plugins/workspace/plugin.json` | +5 | 验证：声明与代码一致

---

### 🔥 #36l 核心桌子层 vitest 覆盖——防漏洞重演（6 测试文件，~75 条）

> 2026-07-26。当前 vitest 151 条覆盖了低层工具（RingBuffer/ProtocolParser/DataConverter/ContextKeyService）和标签页系统（useTabManager/useDragReorder/splitTree），**核心 Registry/Service 层零覆盖。**
> #36f/#36i 发现的 6 个漏洞（H5/H6/H2/M5/M1/M9）如果当时有 vitest，全部能在提交前捕获。
> 详见 memory `vitest-coverage-gaps`（新建）。

- [ ] **#36l1** `src/core/__tests__/ThemeEngine.test.ts`——register/unregister/getAvailable/loadTheme/applyTheme/回退到 CSS 兜底
  文件：`src/core/__tests__/ThemeEngine.test.ts`（新） | ~15 条 | 验证：注册→getAvailableThemes 含该主题→applyTheme→CSS 变量正确→unregister→getAvailableThemes 不含

- [ ] **#36l2** `src/core/__tests__/ConfigurationRegistry.test.ts`——register/unregister/updateEnum/merge/request+consumeSettingsGroup
  文件：`src/core/__tests__/ConfigurationRegistry.test.ts`（新） | ~12 条 | 验证：注册→getMergedSchema 含该 key→updateConfigurationEnum→enum 更新→unregister→getMergedSchema 不含→requestSettingsGroup→consumeSettingsGroup 返回正确值

- [ ] **#36l3** `src/core/__tests__/ConfigurationService.test.ts`——三层合并/get/set/defaults/fallback
  文件：`src/core/__tests__/ConfigurationService.test.ts`（新） | ~10 条 | 验证：get→default 值→set→get 新值→init→从 StorageService 恢复→inspect 三层正确

- [ ] **#36l4** `src/core/__tests__/RegistryLifecycle.test.ts`——🔥 核心测试：安装→卸载对称性。10 个注册表 register→uninstall→get 返回空
  文件：`src/core/__tests__/RegistryLifecycle.test.ts`（新） | ~15 条 | 验证：每个 Registry 在 `unregisterPlugin*` 后清空——commands/keybindings/menus/protocols/cards/channels/themes/ThemeRegistry/fileAssociations/configurations

- [ ] **#36l5** `src/pluginLoader/__tests__/loader.test.ts`——loadPlugin/parseContributions/主题双重注册/旧格式兼容/i18n 注册→注销
  文件：`src/pluginLoader/__tests__/loader.test.ts`（新） | ~15 条 | 验证：新格式 contributes.themes→ThemeRegistry.register 被调用→旧格式 manifest.themes→registerTheme 被调用→loadLanguagePlugin→i18n.addResourceBundle 被调用→uninstallPlugin→i18n.removeResourceBundle 被调用

- [ ] **#36l6** `src/core/__tests__/FactorySlots.test.ts`——initialize/getPluginId/hasSlot/运行时 refreshFromPlugins
  文件：`src/core/__tests__/FactorySlots.test.ts`（新） | ~8 条 | 验证：initialize→getPluginId("settings") 返回正确插件→卸载设置插件→refreshFromPlugins→slot 更新

---

- [ ] **#37** 产品图标主题（Product Icon Theme）
  文件：`src/core/IconRegistry.ts`（新） | +50 | 验证：插件声明 contributes.iconThemes→图标集切换

### E3c：语言引擎跨进程（5 任务，~240 行）

- [ ] **#38** LanguageRegistry + contributes.languages + 两层退路
  文件：`src/core/LanguageRegistry.ts`（新） | +60 | 验证：插件声明 contributes.languages→注册→两层退路回退到 key 原文

- [ ] **#39** 出厂 en/zh 迁移到 contributes.languages 格式
  文件：`plugins/lang-en/` + `plugins/lang-zh/`（新） | +20 | 验证：en/zh 以插件形式存在→可卸载

- [ ] **#40** i18n 跨进程同步（lang:changed IPC）
  文件：`electron/ipc-bridge.ts` + LanguageEngine | +40 | 验证：壳切语言→所有插件 WebView 的语言资源同步

- [ ] **#41** 语言选择器 UI
  文件：`src/components/LanguagePicker.tsx`（新） | +80 | 验证：状态栏语言按钮→下拉列表→选择→所有 WebView 切换

- [ ] **#42** 插件 i18n 注册——内联翻译文件
  文件：loader + i18next | +40 | 验证：插件声明 contributes.languages [{id:"ja",path:"ja.json"}]→i18next.addResourceBundle

---

## 第 3 层：E3d Profile ∥ E3e 通知 ∥ E3f 壳 UI（可并行）

> ⚠️ E3d 额外依赖 E3b+E3c（五维验证的主题和语言需跨进程广播）。
> E3e 和 E3f 仅依赖 E3a。
> 详细设计见各子文档。

### E3d：Profile 与激活（3 任务，~250 行）

- [ ] **#43** ProfileService——loadProfile / switchProfile + 五维验证
  文件：`src/core/ProfileService.ts`（新） | ~150 | 验证：切 Profile→五维全过，失败回退+toast

- [ ] **#44** activationEvents——`*` / onCommand / onFileOpen / onPortOpen / onLanguage / onView 触发
  文件：`src/core/activationService.ts`（新） | ~60 | 验证：cad.dxf→只注册不加载→双击才激活

- [ ] **#45** extensionDependencies——加载前检查缺失依赖
  文件：`src/core/activationService.ts` | ~40 | 验证：缺 file-tree→toast 提示

### E3e：通知系统全功能（5 任务，~230 行）

- [ ] **#46** 通知进度条——showProgress + ProgressHandle
  文件：`src/core/NotificationService.ts` | ~50 | 验证：长操作→状态栏进度条→finish 消失

- [ ] **#47** 通知来源过滤 / Do Not Disturb
  文件：`src/core/NotificationService.ts` | ~40 | 验证：关掉 terminal 通知→terminal 的 toast 不弹

- [ ] **#48** "Don't show again" 持久化
  文件：`src/core/NotificationService.ts` | ~10 | 验证：关掉某通知→重启→仍然不弹

- [ ] **#49** 完整 Notification Center 面板
  文件：`src/components/NotificationCenter.tsx`（新） | ~100 | 验证：铃铛图标→通知列表→按时间/已读过滤

- [ ] **#50** 通知 source 归类（按插件分组）
  文件：`src/components/NotificationCenter.tsx` | ~30 | 验证：面板里 terminal 和 marketplace 的通知分开显示

### E3f：壳 UI 收尾（12 任务，~605 行）

- [ ] **#51** 标题栏暗色化——Electron titleBarStyle/backgroundColor
  文件：`electron/main.ts` | ~20 | 验证：标题栏颜色=主题色

- [ ] **#52** 菜单栏双模式——hamburger（默认）+ menubar（Electron 原生），共享 MenuRegistry 数据源
  文件：`src/components/HamburgerMenu.tsx`（新）+ `electron/menu-builder.ts`（新） | ~120 | 验证：点 ☰→四组菜单；切 menubar→原生菜单栏内容一致

### 🔥 #53 设置项齿轮——Settings Editor 每行 hover 齿轮（合并 #59a，~65 行）

> 2026-07-26 重写。对标 VS Code Settings Editor per-setting gear——四个齿轮的第三个。
> 原 #53 "齿轮菜单完整版" 描述模糊，现已厘清为四种齿轮。详见 `06-E3f-壳UI收尾.md` §三.4。
> #59a "一键恢复默认" 是此齿轮中的一个菜单项——合并到此。

- [ ] **#53a** `MenuRegistry` 加 `MenuId.SettingItemGear`
  文件：`src/core/MenuRegistry.ts` | +1 | 验证：`MenuId.SettingItemGear` 存在

- [ ] **#53b** 注册设置项齿轮菜单项——5 项 + when 条件（重置仅 `settingModified` 时显示）
  文件：`src/core/coreCommands.ts` | +15 | 验证：`registerMenuItems(MenuId.SettingItemGear, APP_PLUGIN_ID, [...])` 
  菜单项：重置此设置 (when: `settingModified`) / 复制设置 ID / 复制为 JSON / 复制为 URL (when: false—Phase 6) / 同步此设置 (when: false—Phase 6)

- [ ] **#53c** `SettingsView.tsx` 每行加 hover 齿轮图标 + `<ContextMenu menuId={MenuId.SettingItemGear}>`
  文件：`src/components/views/SettingsView.tsx` | +20 | 验证：hover 设置行→齿轮出现→点击→弹出菜单→"重置"仅修改过的值出现

- [ ] **#53d** 齿轮打开前设 context key——`settingKey` + `settingModified`
  文件：`src/components/views/SettingsView.tsx` | +10 | 验证：`ContextKeyService.setValue("settingKey", key)` + `settingModified` = 当前值 ≠ default

- [ ] **#53e** `core.openSettings` handler 支持 `scrollTo` 参数——跳转到设置中的具体配置项 + 滚动定位（"重置选项"消费）
  文件：`src/core/coreCommands.ts` | +19 | 验证：齿轮→"重置此设置"→confirm→`ConfigurationService.reset(key)`→值回 default→SettingsView 刷新

### 🔥 #53b 命令面板齿轮——QuickPick 每项 hover 齿轮（新增，~50 行）

> 2026-07-26 新增。对标 VS Code Command Palette per-command gear——四个齿轮的第四个。
> Ctrl+Shift+P 命令项 hover 时右侧齿轮——toggle 命令显示"重置选项"（跳转到设置项），全部命令显示"复制命令 ID" + "打开插件详情"。
> 详见 `06-E3f-壳UI收尾.md` §三.5。

- [ ] **#53b1** `CommandRegistry` 注册时存储 `configurationKey`——toggle 命令关联的设置项 key
  文件：`src/core/CommandRegistry.ts` + `src/pluginLoader/loader.ts` | +5 | 验证：`parseContributions` 从 `contributes.commands[].configurationKey` 读取→`registerCommand` 存储

- [ ] **#53b2** `MenuRegistry` 加 `MenuId.CommandPaletteItemGear`
  文件：`src/core/MenuRegistry.ts` | +1 | 验证：`MenuId.CommandPaletteItemGear` 存在

- [ ] **#53b3** 注册命令面板齿轮菜单项——4 项 + when 条件
  文件：`src/core/coreCommands.ts` | +15 | 验证：重置选项 (when: `commandHasSetting`) / 打开插件详情 (when: `commandPluginId`) / 复制命令 ID / 报告问题 (when: false—Phase 6)

- [ ] **#53b4** `QuickPick.tsx` 每行加 hover 齿轮图标 + `<ContextMenu menuId={MenuId.CommandPaletteItemGear}>`
  文件：`src/components/shared/QuickPick.tsx` | +15 | 验证：Ctrl+Shift+P→hover 命令项→齿轮出现→点击→"重置选项"仅 toggle 命令出现

- [ ] **#53b5** 齿轮打开前设 context key——`commandId` + `commandPluginId` + `commandHasSetting`
  文件：`src/components/shared/QuickPick.tsx` | +10 | 验证：context key 正确映射；`commandHasSetting` = `CommandRegistry` 有 `configurationKey`

- [ ] **#53b6** `plugin.schema.json` `contributes.commands` 加 `configurationKey` 字段
  文件：`public/schemas/plugin.schema.json` | +4 | 验证：schema 中 `configurationKey` 字段定义正确；`npx tsc` 零错误

- [ ] **#54** 输出面板 UI——频道选择器 + 日志列表 + 清空/导出
  文件：`src/components/OutputPanel.tsx`（新） | ~80 | 验证：切频道→日志内容切换

- [ ] **#55** 欢迎页集成——三种状态切换 + recentFolders
  文件：`src/components/views/WelcomeView.tsx` | ~40 | 验证：打开文件夹→状态 B→关闭→状态 A

- [ ] **#56** Workspace 导入导出
  文件：`src/core/WorkspaceService.ts` | ~50 | 验证：导出→导入→布局/设置复原

- [ ] **#57** 终端会话持久化——sessions.json 读写
  文件：`plugins/terminal/` | ~30 | 验证：关闭→重启→会话列表恢复

- [ ] **#58** Developer: Toggle Plugin DevTools——QuickPick + WindowManager.toggleDevTools
  文件：`src/components/CommandPalette.tsx` + `electron/window-manager.ts` | ~10 | 验证：Ctrl+Shift+P→选插件→DevTools 弹出/关闭

- [ ] **#59** 设置页快捷键子栏——双 tab + 表格视图 + 冲突检测
  文件：`src/components/views/SettingsView.tsx` | ~60 | 验证：打开设置→快捷键 tab→所有快捷键可搜索→双击改绑定→冲突红字

- [x] **#59a** ~~设置项一键恢复默认~~ → 已合并到 #53（设置项齿轮菜单的一个菜单项）

- [ ] **#59b** 🔥 FileDecorationRegistry——文件装饰器注册中心，Git 注册/文件树消费
  文件：`src/core/FileDecorationRegistry.ts`（新） | ~40 | 验证：Git 插件 `register({id:'git',provideDecoration})`→`getDecorations(uri)` 返回正确装饰→Git 卸载→`unregister('git')`→装饰消失

- [ ] **#59c** 🆕 🔥 `<SelectBox>` 归一化下拉组件——替代所有原生 `<select>`
  文件：`src/components/shared/SelectBox.tsx`（新） + 全项目替换 | ~100 | 验证：ControlPanel COM/波特率/协议三个下拉→同组件；设置页/主题/语言选择器→同组件；所有下拉统一 UI
  >
  > **Why 现在做：** 原生 `<select>` 在 Electron 里走独立 OS 渲染通道——跟 Chromium 合成器节奏不同步，表现为无动画闪出。
  > 跟之前的 `alert()`/`confirm()` 是同一类问题（[[dialog-normalization-requirement]]）。解决方案也一样——用自己画的 UI 替代原生控件。
  >
  > **归一化要求（对标 Ctrl+Shift+P = CommandPalette）：**
  > - 全局只有一个 `<SelectBox>` 组件——不同页面/插件传不同的 `options`、`value`、`onChange`
  > - UI 行为统一：动画曲线、最大高度、搜索过滤（options > 8 时自动出现）、键盘导航（↑↓Enter Esc）
  > - 主题色适配：下拉面板背景 `var(--bg-window)`、选中项 `var(--accent)`、分隔线 `var(--separator)`
  > - 对标 VS Code `SelectBox`——`renderSelectBox(data: ISelectBoxData)` 在 `src/vs/base/browser/ui/selectBox/`
  >
  > **替换范围（grep `<select` 全项目）：**
  > - `plugins/terminal/src/ControlPanel.tsx`——COM/波特率/协议（3 个）
  > - 其他插件/组件中的 `<select>`（grep 确认）
  > - 替换后：项目里不允许新的 `<select>` 出现（ESLint 规则可选——先 grep 驱动）
  >
  > **AI 友好度：**
  > - 组件接口极简：`{ options: {value,label}[], value, onChange, disabled?, placeholder? }`
  > - 文件位置明确：`src/components/shared/SelectBox.tsx`——跟 `ErrorBoundary`/`ConfirmDialog` 同级
  > - 新插件用下拉 → `import SelectBox from "@src/components/shared/SelectBox"`
  > - 不依赖任何插件——纯壳级 UI 组件

---

## 第 4 层：E3g API + V2 兼容（4 任务，~240 行）

> ⚠️ 最后做——消费 E3f 的 ☰ 基础版。
> 详细设计见 `07-E3g-API与V2兼容.md`。

- [ ] **#60** 动态 StatusBarItem——运行时创建 + dispose 自动清理
  文件：`src/core/StatusBarService.ts`（新） | ~50 | 验证：插件 mount→状态栏出现→unmount→消失

- [ ] **#61** contributes.icons——共享图标注册 + 引用
  文件：`src/core/IconRegistry.ts` + `plugin.schema.json` | ~30 | 验证：插件 A 贡献图标→插件 B 的 icon 字段引用→渲染

- [ ] **#62** ☰ 完整版——快捷键提示 + 禁用态灰显 + 插件菜单组
  文件：`src/components/HamburgerMenu.tsx` | ~100 | 验证：☰→右侧灰字快捷键→when 不满足项灰色→插件菜单多一列

- [ ] **#63** V2 配置导入——映射表 + 文件选择 + 逐 key 迁移
  文件：`src/core/V2Migration.ts`（新） | ~60 | 验证：选 V2 prefs.json→波特率/编码/主题导入
