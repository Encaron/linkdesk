# E1 执行清单——Electron 迁移（7 步，~1,190 行）

> 机械执行。做完一步打勾一步。顺序不可变——每步依赖前一步。
> 完工标准：软件行为与迁移前 100% 一致。
> 详细设计见 `04-迁移方案.md`。

---

## 执行

- [ ] **步 1：Electron 壳初始化**
  文件：`electron/main.ts`（新）、`electron/preload.ts`（新）、`package.json`
  行数：~200
  验证：`npm run dev:electron` → 壳 UI 正常渲染（图标栏/侧栏/主区/状态栏）
  依赖：无

- [ ] **步 2：串口服务（serialport npm）**
  文件：`electron/serial-service.ts`（新）、`electron/main.ts`（IPC 注册）
  行数：~150
  验证：COM 端口枚举列表正常、打开端口→收发数据正常
  依赖：步 1

- [ ] **步 3：文件/配置/插件管理服务**
  文件：`electron/file-service.ts`（新）、`electron/config-service.ts`（新）、`electron/plugin-service.ts`（新）
  行数：~280
  验证：插件安装/卸载正常、配置读写正常、文件读取正常
  依赖：步 1

- [ ] **步 4：接前端 API**
  文件：`src/core/v3Api.ts`、`src/` 全量 grep `invoke(` → 替换为 `window.linkdesk.*`
  行数：~200
  验证：`npx tsc --noEmit` 零错误 + `npx vitest run` 全过 + 手动验证全部功能
  依赖：步 2 + 步 3

- [ ] **步 5：linkdesk:// 协议 + preload-plugin + WebContentsView 骨架**
  文件：`electron/protocol.ts`（新）、`electron/preload-plugin.ts`（新）、`electron/main.ts`
  行数：~120
  验证：`linkdesk://` 协议注册成功 → 插件资源加载正常；preload API 契约定义完成
  依赖：步 4

- [ ] **步 6：清理 + 测试**
  文件：全量 grep `@tauri-apps`、`invoke(`、`tauri` → 删残留
  行数：~100
  验证：`grep -r "@tauri-apps" src/` 零结果 + `grep -r "invoke(" src/` 零结果（除注释）+ 端到端手动验证全部功能
  依赖：步 5

- [ ] **步 7：Electron 打包**
  文件：`electron-builder.yml`（新）、`package.json`
  行数：~140
  验证：`npm run build:electron` → 生成安装包 → 安装 → 运行正常
  依赖：步 6
