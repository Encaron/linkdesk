# 格 5 · 任务 `E6#118`：**市场显示状态**——把读数标在商店里

> **一句话**：格 4 算出了读数，**用户还看不见**。本格把它标在**市场插件**里（**详情页信息栏 ＋ 悬停帮助卡**；⛔ **侧栏列表不标**——用户 2026-09-18 拍板：侧栏显示密度已经很大），让**用户**一眼看懂「这只插件还能不能好好用」。**这是本批最贵的一格**——它碰 UI、跨到仓库外的插件仓、还要动随包种子。
>
> 🔴 **文案口径（用户 2026-09-18 两次纠偏后定稿）**：这块界面**是给用户看的**（安装版用户），所以只许出现**五个词**（正常 / 兼容 / 部分不适配 / 不适配 / —）＋ 用户自己能做的动作；⛔ **不许出现**处数、名字、依赖、源码、文件行号、「待修」这类说法（用户原话：「我从没见过手机厂给用户发的版本更新里写他们改的什么源码」）。**唯一真相源 = [00 号档 §〇d](00-整理档案.md)**。
>
> **编号**：`E6#118` ｜ **轮次**：第 8.5 轮 ｜ **队列位置**：**第 5 棒（共 6 棒）**
> **前置**：**格 4（`E6#117`，读数）** ＋ 🔴 **用户对 HTML 设计图点头** ｜ **提交轴**：插件仓 `marketplace`（PATCH ＋ CHANGELOG ＋ 官方目录两轮收录 ＋ **随包种子同步**）＋ 壳（若显示宿主算的读数）
> **规模**：最大的一格；**分两段**（**图 → 点头 → 落地**，⛔ 不许跳过点头）
> **设计前置**（`check-design-flow` §8.4 ③）：见下面 §二——**本格的 8 维度设计前置已写好**（本轮唯一真正碰 UI 的一格）；⚠️ **改真插件 CSS 之前必须按硬约束 16 先 `Skill(ui-ux-pro-max)` 拿设计系统**。

---

## 〇、本格在轴上的位置与**两段式**

| 段 | 做什么 | 出口 |
|:--|:--|:--|
| **段 1（设计）** | 用已经画好的 [mockups/01-市场兼容状态-细节详解.html](mockups/01-市场兼容状态-细节详解.html) 向用户汇报：**五个状态词**各长什么样、**标在哪一处**（🔴 只有详情页信息栏那一颗，标题旁不挂）、文案怎么写 | 🔴 **用户点头**（没点头不许进段 2） |
| **段 2（落地）** | 市场插件实现（i18n 两棵树同笔）→ 插件仓 PATCH/CHANGELOG → 官方目录两轮收录 → 随包种子同步 → 实机四状态读数 | 实机截图 ＋ `npm run check` 绿 |

> 🟢 **2026-09-18：段 1 的闸门已过**——用户原话「**注意，我同意按设计图走**」⇒ **段 2 解锁**（本格的 AI 进场即可动插件代码，⛔ 不必再等第二次点头）。点头范围 = **现行** [mockups/01](mockups/01-市场兼容状态-细节详解.html)（**含 2026-09-18 当天砍掉「标题旁状态标」的版本**：五词 ＋ **状态标全页唯一** ＋ 悬停卡 ＋ 不进侧栏 ＋ 不写提示行 ＋ 不挂 `title`）。**记录全文见 §9.2**；⚠️ 这一点头**不覆盖** `git push` / `npm publish` / 推插件仓——那几件各自**单独等点头**。

**它碰什么（跨面清单）**：① 插件仓 `E:\linkdesk-plugins\official\marketplace`（壳仓外，**推要用户点头**）② 随包种子（壳仓 `bundled-plugins/`）③ 壳仓（若要新增宿主读数面，但那在格 4）。

## 一、事实与读数（**今天的确切落点，进场自己重跑**）

1. **市场插件 UI 落点**（`pluginId = marketplace`；侧栏容器 5 视图：search/installed/explore/disabled/builtin；主区 2 视图：`plugin-detail` → `src/views/detail/DetailView.tsx`、`marketplace-install-confirm`）：
   - 🔵 **侧栏列表行 / 行态角标（记录备查，⛔ 本格不用）**：`src/views/sidebar/ExploreView.tsx:118-130` ＋ `ExploreView/CatalogRow.tsx:49-82`（行结构 `.marketplace-ms-extension-item.catalog` / `-item-icon` / `-item-header`（name ＋ `v{entry.version}`）/ `-item-desc` / `-item-author` / 两个 `-item-tag` / `CatalogRowAction`）；行态角标线 = `CatalogRowAction.tsx:108-110`（`.marketplace-ms-catalog-status` ＋ `.update` / `.installing` / `.disabled` / `.marketplace-ms-item-fail`）。**用户 2026-09-18 拍板：兼容状态不进侧栏**（侧栏显示密度已经很大）⇒ 这两处**只备案、不落标**。
   - **详情页**：`DetailView.tsx:158-282`（DetailHeader 图标 52 ＋ NavBar 三 tab 详情/功能/更改日志 ＋ `.marketplace-mpd-body` ＋ `<DetailInfoSidebar>`）；**信息组**在 `DetailView/useInfoGroups.tsx:73-118`（顶部小段：标识符/作者/版本/大小/数据位置；组「市场」= 来源/首次发布/**更新时间**（`lastUpdate`，`.slice(0,10)`）/下载；组「类别」；组「资源」；组「依赖 · 环境」= **需 LinkDesk**（minApp）/依赖/被依赖）；概览 tab `DetailOverviewTab.tsx:41-114`。
2. **今天没有主区大屏商店网格**（只在 `docs/05-插件更新/Marketplace-Store/marketplace-preview.html` 与 memory `marketplace-future-main-area-store` 里）⇒ 本格**只在既有的详情页上做**（骨架不新造），⛔ **不许顺手建大屏商店**。
3. **i18n 两棵树**：`i18n/en.json`（**key = 中文原文**，174 行；新增中文文案**必须同笔进英文树**）＋ `i18n/zh.json`（15 行，只 `category.*`）。
4. **样式与命名**：类名一律 `marketplace-` 前缀（壳侧门禁 `check-plugin-css-prefix` 一族管这个）；**角标用共享组件 `ldk-badge`**（⛔ 别自造 `.badge` —— 那正是 `settings` v1.0.9 撞车的历史病根）；padding/margin/gap 走 **4px 网格**；字号走 **`var(--font-size-*)`**；颜色一律 `var(--xxx)`、**零硬编码 hex**。
5. **数据不用新建源**：「更新时间」老早就在渲染（`useInfoGroups.tsx:76-84`）⇒ 本格吃格 4 的读数 ＋ 这条现成时间事实。
6. **参考图（既有）**：`docs/02-Electron架构/E6_插件生态与发布/03-插件市场/mockups/01-插件详情页-竞标.html`（**骨架可参考，但它的 `.icon-bar{width:48px}` 与现行 zone 表 42px 不符 ⇒ 别抄那个数**）· `02-插件详情页-全状态详解.html`（**细节详解类范本**，本格的图照它的形态）· `docs/02-Electron架构/E3.5-软件生态美化/设置界面美化/preview-两级导航.html`（**双主题 token 覆盖写法**：`body.light` 覆盖 token）。
7. **本批自带的设计图**：[mockups/01-市场兼容状态-细节详解.html](mockups/01-市场兼容状态-细节详解.html)——**五态 ＋ 全轮廓 ＋ 双主题 ＋ 每帧 frame-caption/frame-note**（帧① 详情页信息栏 · 帧② 悬停帮助卡 · 帧③ 五态并列）。⚠️ 它是**示意图**（`var(--xxx)` 走主题引擎，落地时以真实 token 为准）。
8. **帮助卡的容器今天就有**：`@linkdesk/ui` 已把壳共享件 **`OverlayPortal`** 导出给插件（壳仓 `src/components/shared/overlay-portal/OverlayPortal.tsx`；导出面 `packages/linkdesk-ui/src/index.ts`）——portal 目标（`#overlay-root`）/ z 序 / Esc / 焦点全归它。市场插件**今天就在用**：`src/views/sidebar/SearchView/AddSourcePopup.tsx:14,68`（`<OverlayPortal onClose triggerRef trapFocus>`）⇒ **本格的卡照它的形状写**：零新增壳组件、零 SDK 发版。⛔ 别自己写 portal / 定位 / 外点关闭。
   - 🔴 **但本格用法与 `AddSourcePopup` 有两点不同**（因为那是「点开的表单」、本格是「瞄一眼的帮助卡」）：**① 不传 `trapFocus`**（不循环焦点、不自动聚焦）；**② `closeOnOutsideClick={false}`**（鼠标按下不该关掉一张只靠「移开」消失的卡）。`onClose` 仍要传——Esc 走它。
   - 🔴 **触发 = 鼠标停在状态标上出、移开消失**（用户 2026-09-18 纠正：要的是「不影响主视线的辅助帮助卡」，不是点击弹层）。配套三条：**悬停意图延时约 400ms**（防划过闪一下）· 键盘 <kbd>Tab</kbd> 聚焦同样出卡、失焦 / Esc 关 · **触屏点一下出、点外关**（没有 hover 的退化路径）。⛔ 卡内不可交互（`pointer-events:none`，纯文本、无链接无按钮）。
   - 🔴 **「悬停出字」这地方已有最朴素的做法，本格要用更好的那个**：壳共享角标 **`ldk-badge` 自带 `title` 提议**（`src/components/shared/badge/Badge.tsx:19`），市场插件今天已有 **41 处** 用原生 `title` 当悬停提示——包括 `src/components/ExtensionItem.tsx:91` 那颗「等待依赖」标挂 `title={plugin.pendingReason}`（**正是同一种「标 ＋ 原因」的用法**）。⇒ **为什么要卡不用 `title`**：原生 `title` 是系统画的，**不能换行排版、不跟主题、鼠标移上去还慢半拍，键盘聚焦与触屏都摸不到**，装不下这三行说明。⛔ **但两者不许并存**——状态标上不挂 `title`，否则原生提示与卡同屏冒出来打架。
   - ⛔ **信息栏里不写提示行**、不靠 `cursor` 变样（用户 2026-09-18 砍掉「说明 · 鼠标停上来看」；壳里今天零 `cursor:help` 惯例——grep 过 `src/**/*.css`）。

## 二、8 维度设计前置（**本格必须逐条落实**）

| 维度 | 答案 |
|:--|:--|
| **① 能力边界（含保底）** | 只**显示**格 4 的读数 ＋ 一句说明；**不改任何按钮行为**（拒装/禁用仍归 `minAppVersion` 老机制）。**保底**：读数拿不到 ⇒ 显示 `—`（⛔ 不显示「不适配」三个字）。 |
| **② API** | **只消费**格 4 的读数（唯一入口，⛔ 不在插件里复算状态）。 |
| **③ 通信** | 走格 4 定的那条只读通道（若格 4 选 (b)「宿主导算随 catalog 下发」，则本格零 IPC）。 |
| **④ 壳侧代码** | **本格零壳侧改动**（除非格 4 的读数面需要壳侧小修——那就回格 4，⛔ 不许在这格顺手改壳）。 |
| **⑤ 插件侧代码** | 市场插件：`useInfoGroups.tsx`（详情信息栏新增「兼容性」组（🔴 **全页唯一的状态标就在这一行**，标题旁不挂）；⛔ **不动 `CatalogRow.tsx`**）· 悬停帮助卡（新组件，放 `src/views/detail/` 邻近，**容器复用 `@linkdesk/ui` 的 `OverlayPortal`**——`closeOnOutsideClick={false}`、不传 `trapFocus`；卡体 `pointer-events:none`）· `i18n/en.json` ＋ `i18n/zh.json` · 对应 CSS（`marketplace-` 前缀）。 |
| **⑥ 显示** | **本格的主战场**：五个状态词（正常 / 兼容 / 部分不适配 / 不适配 / —）＋ **一处标 ＋ 一张卡**（状态标**只在详情页信息栏**那一行，⛔ 标题旁不挂、⛔ 侧栏不标；悬停帮助卡贴这一颗标）＋ 帮助卡的三句大白话文案与 hover 触发。 |
| **⑦ 配置壳给 or 插件给** | **无配置**（⛔ 不许做开关/阈值项），**也不加 hover 延时配置项**（400ms 写死在插件里）。**文案归属**：五个词与帮助卡解释**是插件自己的预设文案**（住市场插件 `i18n/en.json`）；**壳只给机器态**（`state` 枚举 ＋ 两个真日期 ＋ `minAppVersion`）⇒ ⛔ 壳不下发任何用户可见句子。 |
| **⑧ 验收** | 实机四状态读数（截图）＋ 中英各一遍 ＋ 空/无数据/离线三态不崩 ＋ 插件仓 CI ＋ `npm run check` ＋ 用户点头。 |

## 三、要做（**六步，缺一即半成品**）

### 第一步：设计段——把图交给用户、拿点头（🔴 闸门）

- 用 [mockups/01](mockups/01-市场兼容状态-细节详解.html) ＋ 一句话说清**五个状态词分别长什么样**、**标在哪一处**（🔴 只有详情页信息栏那一颗；**标题旁不挂、侧栏不标**——用户 2026-09-18 砍掉标题旁那颗）——⚠️ **不要在图上预设「老 = 坏」**。
- 🔴 **设计段要一并回答用户 2026-09-18 问过的四件**：① 帮助卡**容器是谁造的**（答：壳共享件 `OverlayPortal`，零新增壳件）② 它**是不是通用件**、能不能给别的小提示复用（答：容器已通用；「瞄一眼就出」的内容组件今天没有 ⇒ 要做就是新能力，建议**单独立格**）③ **触发方式**（答：**鼠标停在状态标上出、移开消失**——用户当日纠正；＋ 键盘聚焦可达、触屏点按退化；⛔ 不是右键、不是 toast/弹窗/暗屏、不是点击弹层）④ **字是不是预设、住在哪**（答：预设，住市场插件 `i18n/en.json`；壳只给机器态）。答案落在设计图「规范速查 ⑦」。
- ⛔ **没点头不许写插件代码**（这是本批唯一的人工闸门）。🟢 **本闸门 2026-09-18 已过**（用户「注意，我同意按设计图走」）⇒ 本格 AI 进场直接进第二步，**不要再向用户要一次点头**；记录见 §9.2。

### 第二步：改真 CSS 之前先拿设计系统（硬约束 16）

- `Skill(ui-ux-pro-max)` 拿设计系统 ⇒ 再动市场插件 CSS。**这一步是纪律，不是可选项**。

### 第三步：落两处显示

| 落点 | 显示什么 | 依据（今天的代码位置） |
|:--|:--|:--|
| **详情页信息栏**（**唯一显示处**） | 「依赖 · 环境」组内补一行 **`需 LinkDesk ≥ x.y.z`**（已有 `minApp` 那行 ⇒ **复用不新造**）＋ 「市场」组里的**更新时间**（已有）＋ 新增 **「兼容性」组**（⛔ **只一行状态**——用户 2026-09-18 砍掉「说明 · 点状态看详情 ›」那行：悬停本身就是入口，写出来既多余又是指使鼠标）——🔴 **这颗标是全页唯一的一颗**（标题旁不挂：同一条信息出现两次 = 重复，用户 2026-09-18 砍掉） | `useInfoGroups.tsx:73-118` |
| **悬停帮助卡** | 「这个标是什么意思」，**三句以内**：① 它还能不能装、能不能用 ② 会不会影响我的文件和数据 ③ 我该做什么（更新 LinkDesk / 更新插件）＋ 一句「这不是评分」。⛔ **不写作者该改哪一行**（那是插件作者面报告的事） | ⚠️ **两条路，二选一**：**(a)** 若格 7（`E6#120`，[08](08-任务-通用悬停提示卡.md)）**已落地** ⇒ **直接用共享件** `<HintCard>`（⛔ 别自己写卡）；**(b)** 否则先用**插件自有卡**（`marketplace-` 前缀，本格临时件）——**容器两路相同** = `OverlayPortal`（`closeOnOutsideClick={false}`、不传 `trapFocus`），照 `SearchView/AddSourcePopup.tsx` 的形状；**格 7 那一棒负责把 (b) 替换成 (a)**（替换清单写进本格交接段） |

### 第四步：显示口径四条 ＋ 帮助卡纪律两条（**写死，判据要机械核**）

1. 🔴 **只说用户能感知的事**：能不能装、会不会显示不正常、他该做什么。⛔ **不出现处数、名字、依赖、源码、文件行号、「重新打包」**——那些是插件作者面的说法（三套词表 = [00 号档 §〇d](00-整理档案.md)；领域类比：手机厂给用户的更新说明不写改了哪个文件）。
2. 🔴 **只陈述事实，⛔ 不派活给用户**：改造插件是插件作者的事；用户能做的只有「更新 LinkDesk」「更新插件」两件。
3. **不设阈值**：「久未更新」**不是罪**，日期只做事实展示 ⇒ **新增文案里不许出现**「超过 N 天 / 过期 / 不推荐」这类词。
4. **状态词定稿五个**（正常 / 兼容 / 部分不适配 / 不适配 / —）：与 [00 号档 §〇d](00-整理档案.md) **逐字一致**，⛔ 不许临时换词、不许加第六个词；**只在真有问题时给警示色**（部分不适配 / 不适配）；动作与状态分离：**标不改按钮行为**。
5. 🔴 **帮助卡只走既有共享件 ＋ 悬停触发**：容器 = `@linkdesk/ui` 的 `OverlayPortal`（照 `SearchView/AddSourcePopup.tsx`）——⛔ 不许插件自己写 portal / 定位 / z 序（⚠️ **卡体本身**：格 7 已落地就用共享件 `HintCard`，见 §二第三步；本格临时自有卡的情形，那一棒负责替换）；**触发 = 鼠标停在状态标上出、移开消失**（约 400ms 悬停意图延时；键盘 <kbd>Tab</kbd> 聚焦同样出卡、失焦 / Esc 关；触屏点一下出、点外关）。⛔ 不上右键（本行右键已是齿轮菜单）、⛔ 不做 toast / 弹窗 / 暗屏大面板、⛔ 不做「点击弹出」的弹层、⛔ 卡内不放链接与按钮（`pointer-events:none`）。
6. 🔴 **入口不靠文字行、也不靠原生 `title`**：信息栏「兼容性」组**只留一行状态**，⛔ 不写「点这里看说明」「鼠标停上来看」这类提示（悬停本身就是入口；而且那是指使鼠标，触屏用户看不懂）；⛔ 状态标上**不挂 `title`**（`ldk-badge` 自带 `title` 提议，但原生提示会与卡同屏打架）。

### 第五步：发版链（**同笔，缺一即半成品**）

- 插件仓 `marketplace`：PATCH bump ＋ CHANGELOG ＋ `npm publish`（作者轴）＋ **官方目录收录两轮**（照 E6#106/#107 的口径）。
- **随包种子同步**：`bundled-plugins/marketplace.linkdesk-plugin` ＋ `bundled-plugins.lock.json` 的 seed 条目 ＋ `npm run sync:bundled` ⇒ `check-bundled-freshness` **6/6**。
- 发版顺序纪律：**插件轴先发、壳最后**；🔴 **推 GitHub / 发 npm / 推 18 仓前一律等用户点头**。

### 第六步：清单与交接（**同笔**）

- 勾 `E6#118` 全行 ＋ 第 8 层轮次进度行 ＋ 交接段（顶部追加 ＋ 队列表整张复制）。

## 四、判据（**逐条给读数**）

- [x] ① **用户点头**（设计段）——**2026-09-18 已拿到**（原话「注意，我同意按设计图走」，全文见 §9.2）✅
- [ ] ② **硬约束 16 已走**：改 CSS 前 `Skill(ui-ux-pro-max)` 已调（写进交接段）。
- [ ] ③ **实机四状态截图**：正常 / 兼容 / 部分不适配 / 不适配（用格 4 给的真样本；缺真样本用桩并**标注**）。
- [ ] ④ **三态不崩**：空态（无插件）/ 无数据（读数缺）/ 离线（无 catalog）⇒ 显示 `—`，**不显示「不适配」**。
- [ ] ⑤ **i18n 两树**：新增文案在 `en.json` **同笔**；中英各跑一遍无漏。
- [ ] ⑥ **文案无阈值＋无禁词**：用户面文案集合 grep `-iE "超过 ?[0-9]+ ?天|阈值|threshold|过期|不推荐|评分|排行"` **无命中**，且 grep `"待修|悬空|契合度|依赖|重新打包|源码|处|老"` **无命中**（禁词表见 [00 §〇d](00-整理档案.md) §三铁律 3）。
- [ ] ⑥b **词表照抄**：界面上出现的状态词与 [00 §〇d](00-整理档案.md) 的五个词**逐字一致**（不许改词、不许加第六个）。
- [ ] ⑦ **门禁全绿**：插件仓 CI ＋ 壳仓 `check-plugin-css-prefix` 一族 ＋ `check-spacing-grid` ＋ `check-font-scale-audit` ＋ `audit-i18n` ＋ `check-bundled-freshness` 6/6。
- [ ] ⑧ **种子同步**：`bundled-plugins/` 与货架/PATCH 版本一致。
- [ ] ⑨ **收尾**：`npm run check` **EXIT=0**（末尾读数写进档）。
- [ ] ⑩ **帮助卡归属与触发已按档**：容器 = `OverlayPortal`（⛔ 插件里没自写 portal；**不传 `trapFocus`、`closeOnOutsideClick={false}`**）＋ **悬停出、移开消**（⛔ 无右键 / toast / 暗屏 / 点击弹层；卡内无链接按钮）＋ 文案全在插件 `i18n/en.json`（⛔ 壳没下发用户可见句子）——⚠️ **卡体走哪条路要写明**：共享件 `HintCard`（格 7 已落地）**或**本格临时自有卡（格 7 替换）；两条路**文案都归本插件**（共享件只管机制，不吃句子）。
- [ ] ⑪ **无 hover 也能用**：键盘 <kbd>Tab</kbd> 到状态标能出卡、<kbd>Esc</kbd> 能关；触屏（无 hover）点一下能出、点外能关——两条都用实机或桩验一遍（⛔ 不许只验鼠标）。
- [ ] ⑫ **没有多余入口**：信息栏「兼容性」组 grep `说明|提示|鼠标停|点击查看|看详情` **无命中**；状态标上 grep `title=` **无命中**（原生提示会与卡打架）。
- [ ] ⑬ 🔴 **状态标唯一（用户 2026-09-18 拍板）**：详情页那一颗标**只出现一次**——`DetailHeader` / 标题那一带（含 `.marketplace-mpd-title`、`.marketplace-ms-item-*` 标题行）grep `ldk-badge` **零命中**；⛔ 不许在标题旁再挂一颗（同一条信息两处出现 = 重复，会让人以为是两回事）。**触发点也只有一个**（悬停卡只贴信息栏这一颗）。

## 五、禁区

1. ⛔ **不许建主区大屏商店**（那是另一条轴）。
2. ⛔ **不许改宿主拒装/拒载规则**（`minAppVersion` 机制一行不动）。
3. ⛔ **不许做评分/排行/推荐/「一键升级」**。
4. ⛔ **不许把阈值写进文案**；⛔ 不许自造 `.badge`（用 `ldk-badge`）。
4b. 🔴 **不许把作者面/维护者面的词搬上用户面**（「N 处待修」「悬空名」「升依赖 ＋ 重新打包」这类一律不进市场界面；禁词表 = [00 §〇d](00-整理档案.md)，判据 = §四⑥ 的 grep）。
5. ⛔ **不许改详情页骨架结构**（E6#63 已定稿，本格**只加行**）；⛔ 不许改 `window.linkdesk.*` 对外面（若真需要 ⇒ 回格 4）。
6. ⛔ **不许在没点头的情况下动插件代码**；⛔ 不许未获点头就 `git push` / `npm publish`。
7. ⛔ **不许把状态标加回侧栏列表**（用户 2026-09-18：侧栏显示密度已经很大）——本格的显示面**只有详情页信息栏**。
8. ⛔ **不许为这个帮助卡新增壳组件 / SDK 面**（容器已有 `OverlayPortal`）。若要把「瞄一眼就出、移开就消」做成**全壳通用件** ⇒ **已单独立格 = `E6#120`**（[08 号档](08-任务-通用悬停提示卡.md) · 第 8.7 轮 · 本批最后一棒；那是新能力：8 维度 ＋ `@linkdesk/ui` 版本档位 ＋ 作者面 docs 第五轴 ＋ 消费方 lock 抬版；**不涉 SDK / contracts / 18 仓**——实测见 §八），不在本层做——本层只写插件自有的卡（⛔ **不许在本格里顺手把共享件也做了**：那一格有自己的 8 维度与判据）。
9. ⛔ **不许把帮助卡做成点击弹层 / 带遮罩 / 带按钮**（用户 2026-09-18 纠正的正是这一点：要的是不影响主视线的辅助卡，不是把注意力拽过去的弹窗）；⛔ 不许只做 hover 而不做键盘与触屏退化路径（否则键盘与触屏用户永远看不到说明）。
10. ⛔ **不许在信息栏里写「怎么看说明」的提示行**（用户 2026-09-18 砍掉「说明 · 鼠标停上来看」）；⛔ **状态标上不许挂 `title`**（原生提示会与悬停卡同屏打架）。

## 六、交棒要求

1. **收尾四件套**（交接段顶部追加 · 清单第 8 层轮次进度行 · 勾格全套 · **队列表整张复制**）。
2. 🔴 **下一棒 = 格 6 · `E6#119` 判据铺开**（详案 [06](06-任务-判据铺开.md)）——**你不是复核者也不是派工者**。
3. 🔴 **必须交接给格 6 的东西**：① 市场侧**最终显示的文案与口径**（格 6 的作者面说明要与它对齐，别写出两套说法）② 本格改动过的**插件仓与版本号**。
4. **判「这条不做」必须写明理由**。

## 七、🔴 进场第一步（复制即可跑）

```bash
# ① 先看本批自带的设计图（含五态与全轮廓）
start "" "docs/02-Electron架构/E6_插件生态与发布/插件兼容机械化/mockups/01-市场兼容状态-细节详解.html"

# ② 市场插件的两处落点（壳仓外；按需改路径）
sed -n '70,100p' E:/linkdesk-plugins/official/marketplace/src/views/detail/DetailView/useInfoGroups.tsx
# ③ 悬停帮助卡的形状照它抄（容器 = 壳共享件 OverlayPortal；本格不传 trapFocus、closeOnOutsideClick={false}）
sed -n '1,110p' E:/linkdesk-plugins/official/marketplace/src/views/sidebar/SearchView/AddSourcePopup.tsx
grep -rn "OverlayPortal" src/components/shared/overlay-portal/OverlayPortal.tsx packages/linkdesk-ui/src/index.ts | head -3
# 🔵 侧栏行（CatalogRow.tsx / CatalogRowAction.tsx）**本格不用**——备案即可，⛔ 别落标

# ④ i18n 两棵树（新增文案的落点）
node -e "const e=require('E:/linkdesk-plugins/official/marketplace/i18n/en.json');console.log(Object.keys(e).length+' keys')"

# ⑤ 共享角标组件（⛔ 别自造 .badge）
grep -rn "ldk-badge" E:/linkdesk-plugins/official/marketplace/src | head -5

# ⑥ 种子与新鲜度
node -e "const l=require('./bundled-plugins/bundled-plugins.lock.json');console.log(JSON.stringify(l).slice(0,400))"
npm run check:bundled-freshness 2>/dev/null || node scripts/check-bundled-freshness.mjs

# ⑦ 格 4 的读数入口（本格的前提；不在 ⇒ 本格先停）
grep -rn "getCompatibility\|compat" src/core/ 2>/dev/null | head

# ⑧ 收尾（禁跳）
npm run check
```

## 八、与其它格的关系

| 格 | 关系 |
|:--|:--|
| 格 4 `E6#117` | **硬前置**：读数由它给。⛔ 本格不许复算状态。 |
| 格 1 `E6#114` | 间接（悬空读数经格 4 来）。 |
| 格 6 `E6#119` | **下游**：作者面说明要与本格显示的文案**同一套说法**。 |
| `03-插件市场/mockups/01` | **参考骨架**（⚠️ 它的 icon-bar 48px 是错例，现行 zone 表是 42px）。 |
| E6#73 通知系统 | **不碰**：本格的警示是**常驻标**，不是通知（避免噪声）。 |
| `@linkdesk/ui` 的 `OverlayPortal` | **复用**：悬停帮助卡的容器（市场插件今天已在 `SearchView/AddSourcePopup.tsx` 用同一条链）⇒ 本格零新增壳件。⚠️ 本格用法：**不传 `trapFocus`、`closeOnOutsideClick={false}`**。 |
| **通用帮助卡（🔵 已单独立格：`E6#120` · 第 8.7 轮，详案 [08](08-任务-通用悬停提示卡.md)）** | 用户 2026-09-18 问「这悬浮窗能不能给各种小提示复用」⇒ **容器已通用，卡没有**。要做成全壳通用件就是**新能力**（8 维度 ＋ `@linkdesk/ui` 版本档位 ＋ 作者面 docs 第五轴 ＋ 消费方 lock 抬版）；**落点 = 壳 `src/components/shared/hint-card/`**（照 [壳目录规范](../../../开发管理/壳目录规范.md) 的共享件命名铁律：描述「它是什么」、⛔ 不带消费方字样）。🔴 **2026-09-18 实测更正：不涉「18 仓接腿」**——`@linkdesk/ui` 今天只被 **4 只**官方插件消费（marketplace / file-tree / serial-monitor / settings，声明均 `^0.3.0`，实测 `E:/linkdesk-plugins`），且 **`plugin-sdk` / `contracts` 两根轴零改动**（UI 零件不经 `linkdesk.*` 面、不经 SDK 面）⇒ 本候选格**不发 SDK、不铺 18 仓**，只动壳 ＋ `@linkdesk/ui` ＋ 真要用它的那几只插件；准入先例通（[E6 07 号档](../01-插件独立构建/07-共享组件独立分发设计.md)：`@linkdesk/ui` 零件按「多提供方 = 任何插件消费 / 多消费方 / 壳不懂业务」判为壳能力）。⚠️ **名字与分工已在设计图「规范速查 ⑧」定稿候选**（建议 `HintCard` / `ldk-hint-card`，不是 `Tooltip` 也不是 `HoverCard`——理由与壳/插件分工见那一节）。**本格不做**——**2026-09-18 已按此单独立格 `E6#120`**（详案 [08-任务-通用悬停提示卡.md](08-任务-通用悬停提示卡.md)，第 8.7 轮、第 7 棒）。两条路都通：格 7 **先**落地 ⇒ 本格**直接用共享件**（免做两次）；格 7 **后**落地 ⇒ 本格先用插件自有卡，格 7 那棒负责**替换成本**（替换清单写进格 5 的交接段）。 |

---

<!-- §九 起 = 本格执行记录 -->

## 九、执行记录（**待格 5 那一棒填写**）

### 9.1 落地表（`file:line` 可点验）

- 待填：插件仓文件清单 ＋ 壳仓种子文件 ＋ npm script 输出。

### 9.2 用户点头记录（**谁、什么时候、点头了什么**）

- ✅ **2026-09-18 · 用户本人**（原话逐字：「**注意，我同意按设计图走**」）。
  - **点头对象** = **现行** [mockups/01-市场兼容状态-细节详解.html](mockups/01-市场兼容状态-细节详解.html)——即**当天已按用户要求改过的那一版**（砍掉插件详情页大标题旁的「部分不适配」标，状态词**只保留信息栏那一处**）。
  - **点头覆盖的范围（照此执行，偏离要写 §9.3）**：① **五个状态词**（正常/兼容/部分不适配/不适配/—，照抄 [00 §〇d](00-整理档案.md)）；② **状态标全页唯一**（详情页信息栏那一行，**标题旁不挂、侧栏不标**）；③ **悬停帮助卡**（停在标上出卡、移开消失；容器 `OverlayPortal`，不传 `trapFocus`、`closeOnOutsideClick={false}`）；④ **信息栏不写「鼠标停上来看」提示行**；⑤ **状态标不挂 `title`**；⑥ **不进侧栏列表**。
  - ⚠️ **不覆盖**（各自单独等点头）：`git push` 壳仓 / 推插件仓 `marketplace` / `npm publish` / 官方目录收录。
  - **闸门效力**：段 1 的出口判据 = 本条 ⇒ **判据①已达成**（§四① 的勾由本格进场时补）。

### 9.3 设计段与落地段的差异（**图 ≠ 实现的地方，逐条说明**）

- 待填（设计图是示意图；任何偏离都要有理由）。

### 9.4 版本账（marketplace 插件版本 / 种子 / 目录）

- 待填。

### 9.5 实机四状态读数

- 待填（截图或 CDP 读数）。

### 9.6 残余边界（诚实清单）

- 待填。

### 9.7 `npm run check` 末尾三行

- 待填。

### 9.8 交接（下一棒 ＋ 剩余队列）

- 待填（**队列表整张复制**，自己那格改 ✅）。
