# 格 4 · 任务 `E6#117`：**兼容读数**——把「这份插件跟当前版本搭不搭」算成读数

> **一句话**：插件与当前主软件搭不搭，今天**没有任何机械读数**（只能靠人记）。本格把它算出来：**格 1 的悬空名读数** ＋ **`minAppVersion` 与当前壳比** ＋ **两个真日期比大小**（插件最后发版日 vs 当前壳构建日，`product.json` 的 `date`）⇒ **四个用户面词（正常 / 兼容 / 部分不适配 / 不适配）由读数自动得出**。核心一句：**把「版本号对齐」换成「兼容读数」**。
>
> **编号**：`E6#117` ｜ **轮次**：第 8.4 轮 ｜ **队列位置**：**第 4 棒（共 6 棒）**
> **前置**：**格 1（`E6#114`）**——判据与读数直接复用它的口径 ｜ **提交轴**：壳仓 `src/`（**若新增只读面 ⇒ `contracts` 轴同时 PATCH**）＋ 文档；**软件轴 PATCH**（有 `src/` 改动）
> **规模**：一个读数模块 ＋ 单测 ＋ 真样本读数。**预期不动加载器/注册表/主题引擎一行**
> 🔴 **词表口径**：本格算的是**读数**；**用户面看到的五个词（正常/兼容/部分不适配/不适配/—）与其机械判据，唯一真相源 = [00 号档 §〇d](00-整理档案.md)**——⛔ 不许在本格自己发明显示用词（用户 2026-09-18 明确否掉「老但兼容」「N 处待修」这类说法）。
> **设计前置**（`check-design-flow` §8.4 ③）：本格**可能新增一个只读对外面**（兼容读数）。⇒ **下面 §二第一步就是 8 维度设计前置，未填完不许动代码**（⛔ 不许「先写代码再补设计」）。

---

## 〇、本格在轴上的位置

| 件 | 射程 | **不射程** | 产出 |
|:--|:--|:--|:--|
| 兼容读数 | **壳侧算**：三来源 ⇒ 五个用户面词（正常/兼容/部分不适配/不适配/—）⇒（格 5 去显示） | ❌ 不改拒装/拒载（`runtime.ts:202-210` 一行不动）❌ **不设时间阈值**（只做两个真日期比大小）❌ 不碰 UI（格 5）❌ 不写回插件仓 ❌ 不做「使用量」统计 | 读数模块 ＋ 单测 ＋ 四类样本读数 ＋ 清单勾格 ＋ 交接段 |

## 一、事实与读数（**进场自己重跑**）

1. **三来源全部现成**：
   - ① **悬空名**：格 1 的产物层读数（**口径以格 1 为准，本格只消费**）。
   - ② **`minAppVersion`**：`packages/plugin-sdk/schemas/plugin.schema.json:226`（字段）；**唯一硬机制**的出口 `src/pluginLoader/resolution/runtime.ts:202-210`（`minAppVersion > 当前壳` ⇒ toast「需要新版主软件」＋ `markLoadFailed`）；市场侧拒装见 E6#30.8c。
   - ③ **时间事实**：catalog 每条版本 `publishedAt`（`packages/plugin-sdk/src/publish.ts:425`，值 = 发版那一刻 `new Date().toISOString()`，同一动作里先建 GitHub Release ⇒ 与 Release 时间同源不同取法）· catalog 顶层 `updatedAt`（`:492`）；本地已装插件的**版本**读 `plugin.json`。🔴 **2026-09-18 实测更正：本地 `plugin.json` 只有版本、没有日期字段**（键实测 = `$schema/pluginId/name/version/icon/marketIcon/description/author/distribution/core/factoryRole/entry/appearsIn/tabBehavior/contributes`；`packages/plugin-sdk/schemas/plugin.schema.json` 里也无日期字段；SDK 建包 `vite-config.ts:546` 写清单时**不注入日期**）⇒ **「插件最后发版日」的唯一来源 = catalog 的 `publishedAt`**。本地另有账本 `installedAt`（`src/core/services/PluginInstallService.ts:38`），但语义是**安装日**（首次装那天，重装/升级保原值）——⛔ **不许拿它顶替发版日**。两处都拿不到（本地/侧载插件、离线无目录）⇒ `lastUpdate: null` ⇒ 用户面 `—`（照 00 §〇d：⛔ 不许显示成「不适配」）。
2. 🔴 **时间口径（用户已追问过，写死不许自己发明）**：
   - **时间只做事实展示，不设阈值**；**绝不能用版本号当时间轴**（版本是**攒着发**的——用户 2026-09-18 明确「接受跳版本、多个攒一起发」）。
   - ⇒ 本格**不产出**「超过 N 天未更新 = 死插件」这种判定（见总纲 §〇c 与 §六第 5 条）。
3. **生命周期三档的原始表述与出处**：memory `plugin-lifecycle-three-tier.md`（用户 2026-09-04 首次表述、2026-09-18 才有书面落点）——新插件（首次上架 ＋ 仓 CI 全绿）/ 活跃插件（每次发版自动重跑判据）/ 久未发版的插件（跑一次判据出读数；**不拒装、不卸载，把清单挂给作者**；只有触发 2.0 断裂才轮到拒绝墙）。**第一原则 = 把「版本号对齐」换成「兼容读数」**。
   🔴 **本格只产出读数，不产出「老 / 死 / 过期」这类判定**（时间不设阈值）；**用户面用哪五个词、各自的机械判据，见 [00 号档 §〇d](00-整理档案.md)**——本格是那些判据的**实现**，⛔ 不是词的发明者。
4. **「插件发版日 vs 当前壳构建日」两个日期今天都拿得到——但来源在两边，不在同一个地方**：
   - **壳构建日 = 壳自己随身带的一行**（⛔ **不联网、不问 GitHub**）：发布时 `scripts/write-product-json.mjs` 用 `buildDate(new Date())` 写进 `electron/product.json` 的 `date` 字段，**随 asar 打进安装包**；运行时 `electron/product.ts:59-71` 读它，经 `app:getProductInfo` 下发渲染侧（`src/core/types/ipc/product.ts` 的 `Product.date`）。**dev 占位空 ⇒ 运行时降级成 `'—'`**（`scripts/publish.mjs` 发布后还把 product.json **还原**成占位 ⇒ 仓库里永远是空的，只有安装包里那份带日期）。
   - **插件发版日 = 目录条目里的一行**（**来自 GitHub**——官方目录 = 仓 `encaron/linkdesk-marketplace` 的 `marketplace.json`，由 SDK 发版时写；第三方源同形状）：`publishedAt`（`packages/plugin-sdk/src/publish.ts:51/425`）。本地 `plugin.json` **没有日期字段**（§一.1③ 实测更正）⇒ 离线/无目录来源的插件落 `—`。
   ⇒ 「正常 vs 兼容」的分界**不需要发明阈值**，只是这两个日期比大小；**壳日期是 `'—'` 时落到「兼容」**（保守且是真话）。
5. **硬机制的边界（本格最容易越界的地方）**：`minAppVersion` 是**全批唯一**能拦人的机制，**已经落地并实测**；本格**一行不改**它，只把它**读出来**（格 5 显示成「不适配」）。

## 二、要做（**五步，缺一即半成品**）

### 第一步：8 维度设计前置（**未填完不许写代码**）

| 维度 | 本格要回答的 |
|:--|:--|
| **① 能力边界（含保底）** | 读数是**只读**的：输入 = 已装插件列表 ＋ 当前壳版本 ＋ **当前壳构建日期** ＋（可选）catalog 条目；输出 = 每只插件一条**兼容状态**（五个用户面词之一，判据见 [00 号档 §〇d](00-整理档案.md)）＋ 三来源读数。**保底**：任何一项算不出来 ⇒ 出「—」，⛔ **不许**因此判成「不适配」。 |
| **② API** | 二选一：**(a)** 宿主新增只读面（建议落在 `window.linkdesk.plugins.*` 域内，例如 `plugins.getCompatibility()`；**只加不删**，会被格 2 的门禁自证）；**(b)** 宿主算好，随 catalog 条目**下发**（市场插件不自己算）。⇒ **推荐 (a)**，理由：**判据必须单点**，第三方市场插件不该各自复刻一套口径（复刻 = 口径漂移）。 |
| **③ 通信** | 若走 (a)：IPC 一条只读通道（`invoke` 形态，与既有 `app:getProductInfo` 同族）；**无广播**（读数随请求算，变化只在装/卸/更新时发生）。 |
| **④ 壳侧代码** | 新增读数模块（建议 `src/core/compat/` 或既有 plugins 域的邻近位置，**照现有 feature-folder 规范**）＋ 一行 IPC 注册 ＋ 单测。 |
| **⑤ 插件侧代码** | **本格零插件侧改动**（消费在格 5；格 5 只调这一个方法）。 |
| **⑥ 显示** | **本格不做显示**（格 5）；本格在档里给出**读数的稳定形状**（字段名与取值域）＋ **五个用户面词与读数的固定对应**（照抄 §〇d，⛔ 不改词），供格 5 直接用。 |
| **⑦ 配置壳给 or 插件给** | 读数**无配置**（不吃设置项、不吃 flag）；⛔ 不许把阈值做成可配置项（本批不设阈值）。 |
| **⑧ 验收** | 四个用户面词（正常 / 兼容 / 部分不适配 / 不适配）**每类至少一个真实样本**（见 §三判据③）＋ 读数可复跑 ＋ `npm run check` EXIT=0。 |

### 第二步：读数模块（口径写死，⛔ 不许在别处复刻）

- 输入 → 输出（形状建议）：
  ```
  { pluginId, state, dangling: { count, names[] }, minAppVersion: "0.2.10"|null,
    shellVersion: "0.2.11", minAppSatisfied: true|false|null,
    lastUpdate: "2026-08-14"|null, shellBuiltAt: "2026-09-12"|null, unknown: ["…"] }
  ```
- **状态算法（逐条写进代码头注，判据唯一；用户面文案照抄，⛔ 不在本格发明词）**：
  | `state`（机器值） | 判据（**读数驱动，不靠人打标**） | 用户面词（[00 §〇d](00-整理档案.md) 定稿） |
  |:--|:--|:--|
  | `incompatible` | `minAppSatisfied === false`（`minAppVersion > 当前壳`）——**已有硬机制**（市场拒装 ＋ 加载器拒载），本格只是**读出**它 | **不适配** |
  | `drifted` | `minAppSatisfied !== false` 且 `dangling.count > 0` | **部分不适配** |
  | `current` | `minAppSatisfied !== false` 且 `dangling.count === 0` 且 `lastUpdate >= shellBuiltAt` | **正常** |
  | `compatible` | `minAppSatisfied !== false` 且 `dangling.count === 0` 且（`lastUpdate < shellBuiltAt` **或** 任一日期为 `null`） | **兼容** |
  | `unknown` | 关键输入缺失（未跑尺子 / 拿不到壳日期且拿不到插件日期 / 离线） | **—** |
  - 🔴 **两个日期的比较不是阈值**：`lastUpdate`（插件最后发版日，catalog `publishedAt` 或本地 `plugin.json`）与 `shellBuiltAt`（当前壳构建日，`product.json` 的 `date`）**都是真日期**，只比大小；⛔ **不许**出现「超过 N 天」这类发明出来的判据。
  - ⚠️ **壳日期缺失（dev 下 `product.json` 的 `date` 是 `'—'`）⇒ 落到 `compatible`（兼容）**：保守、且是真话（它确实还能跑）。
  - ⚠️ **`dangling` 口径以格 1 为准**（本格零复刻）；读数缺失 ⇒ `unknown`，**不是** `drifted`（缺数据 ≠ 有问题）。
- ⚠️ **宿主定义集的运行时来源——本格必须拍一个（⛔ 不许两条都做）**：**(a) 构建期生成清单随包下发**（与 `scripts/gen-host-reserved.mjs` → `src/core/registry/host-reserved.generated.ts` 同族的既有模式，**推荐**；代价 = 生成链要多一栏）／**(b) 运行时读自己打包好的 CSS**（省一栏生成，但把「读自己的产物」变成运行期依赖）。**今天账里没有这份类名清单**（`reserved-class-names.json` 只剩 8 条关键帧、`host-reserved.json` 无类名栏，见 [00 号档 §二第 15 条](00-整理档案.md)）⇒ 选 (a) 就得先在生成链里加它，并同笔跑 `--check` 对账。
- **只读、无副作用**：不写盘、不改任何插件的状态、不触发下载。

### 第三步：单测（正控 ≥ 负控，且**桩必须造得出来**）

- **正控**：桩 4 只插件 ⇒ 四类**可判**状态各一只（`incompatible` / `drifted` / `current` / `compatible`；含 `minAppVersion` 高于当前壳那条）。`unknown` 由负控 1 覆盖。
- **负控 1**：读数输入缺失（无 catalog / 无 `minAppVersion`）⇒ 出 `unknown: []`-填项 与 `null`，**不崩、不误判**。
- **负控 2**：`dangling` 读数缺失（未跑格 1 的尺子）⇒ 状态**不因此变成 `drifted`**（缺数据 ≠ 有问题）。
- **负控 3**：宿主版本低于插件 `minAppVersion` 但**插件还没装**（只有 catalog 条目）⇒ 读数照样能出（市场要靠它）。

### 第四步：真样本读数（四类**可判**状态，**不许只用桩交差**）

- 本机随包 6 只 ＋ 官方 18 仓取 ≥3 只样本；**有哪类跑不出真样本，就用桩补并明确标注「桩」**，同时**在交接段写明哪一类缺真样本**。

### 第五步：清单与交接（**同笔**）

- 勾 `E6#117` 全行 ＋ 第 8 层轮次进度行 ＋ 交接段（顶部追加 ＋ 队列表整张复制）。
- 若新增对外面：**同笔**跑 `contracts` 轴（`npm run contracts:*` 一链）＋ 按多轴口径 bump（`contracts` PATCH，软件 PATCH）。

## 三、判据（**逐条给读数**）

- [ ] ① **8 维度前置先填完**（本档 §二第一步那 8 行**全部有答案**）——⛔ 没填完不许动代码。
- [ ] ② **判据单点**：全仓只有**一处**算兼容状态（`grep` 那五个 `state` 值，只有读数模块 ＋ 单测命中）；市场插件里**零**状态算法。
- [ ] ③ **四类可判状态的真样本**（`unknown` 由负控 1 覆盖）：`current` / `compatible` / `drifted` / `incompatible` **每类至少一条读数**（缺真样本的用桩并标注）。
- [ ] ③b **用户面词与读数一一对应**：§二第二步那张表里的五个 `state` × 五个用户面词**逐字**与 [00 §〇d](00-整理档案.md) 相同（⛔ 本格不许自造第六个词、不许改词）；`current` 与 `compatible` 的分界只依赖两个日期，**全仓 grep 不到任何「N 天」类常量**。
- [ ] ④ **机制未动**：`git diff src/pluginLoader/resolution/runtime.ts` **为空**（拒载逻辑一行没改）。
- [ ] ⑤ **无阈值**：新增代码与文案里 `grep -iE "30 ?天|90 ?天|过期|阈值|threshold"` **无命中**。
- [ ] ⑥ **负控不崩**：三条负控实跑（缺输入 / 缺悬空读数 / 未安装）⇒ 正常出值，不抛。
- [ ] ⑦ **契约**（若新增面）：`npm run contracts:*` 全绿 ＋ `check-api-contracts` 不红（新面必须被文档认领，**且格 2 的门禁不红**）。
- [ ] ⑧ **收尾**：`npm run check` **EXIT=0**。

## 四、禁区

1. 🔴 **不改机制**：加载器 / 注册表 / 市场拒装规则 / 主题引擎**一行不动**。
2. 🔴 **不设阈值**：不产出「多久没更新 = 死」；不许把阈值做成配置项。
3. ⛔ **不许用版本号当时间轴**（版本攒着发）。
4. ⛔ **不许把读数写回插件仓 / 不许改插件源码**（那是格 5 与作者的事）。
5. ⛔ **不许碰 UI**（格 5 专属）；⛔ 不许在读数里加「评分/排行/推荐」这类语义。
6. ⛔ **缺数据不许当坏消息**：算不出来 ⇒ `null`/`未知`，⛔ 不许默认 `drifted`（防误伤）。
7. ⛔ **不许自己实现第二套悬空判据**（用格 1 的口径；格 1 若没落地，本格**先停**，不许自造）。

## 五、交棒要求

1. **收尾四件套**（交接段顶部追加 · 清单第 8 层轮次进度行 · 勾格全套 · **队列表整张复制**）。
2. 🔴 **下一棒 = 格 5 · `E6#118` 市场显示状态**（详案 [05](05-任务-市场显示状态.md)）——**你不是复核者也不是派工者**。
3. 🔴 **必须交接给格 5 的东西（逐条列清，别让下一棒去翻代码）**：① 读数的**稳定形状**（字段名 ＋ 取值域 ＋ `null` 语义）② 调用的**唯一入口**（方法名/通道名）③ **四个用户面词各自的真实样本读数**（格 5 直接拿去截图）④ 哪一类**只有桩**⑤ `shellBuiltAt` 的实际取法（`product.json` 的 `date` 走哪条路径到读数模块）。
4. **判「这条不做」必须写明理由**（尤其是「时间阈值」那条，理由 = 用户口径）。
5. ⚠️ **格 5 的设计图已经在 `mockups/` 里画好**（[01-市场兼容状态-细节详解.html](mockups/01-市场兼容状态-细节详解.html)）⇒ 本格收尾时**对着那张图核一遍「读数够不够画出来」**，不够就在交接段点名（这是设计图能反过来纠读数形状的唯一次机会）。

## 六、🔴 进场第一步（复制即可跑）

```bash
# ① 唯一硬机制（本格不许改的那几行）
sed -n '195,215p' src/pluginLoader/resolution/runtime.ts

# ② minAppVersion 字段
grep -n "minAppVersion" packages/plugin-sdk/schemas/plugin.schema.json | head

# ③ 时间数据（catalog 侧）
grep -n "publishedAt\|updatedAt" packages/plugin-sdk/src/publish.ts | head

# ④ 三档原话（记忆库，仓库外）
node -e "const fs=require('fs');const p=process.env.USERPROFILE+'/.claude/projects/e--linkdesk/memory/plugin-lifecycle-three-tier.md';console.log(fs.readFileSync(p,'utf8').split(/\r?\n/).slice(24,60).join('\n'))"

# ⑤ 只读面的既有同族（照它的形状加）
grep -rn "app:getProductInfo" src/electron* src/core 2>/dev/null | head

# ⑥ 格 1 的口径（本格的前提；不在 ⇒ 本格先停）
ls scripts/plugin-dangling-name-audit.mjs 2>/dev/null || echo "（格 1 未落地 ⇒ 本格不许自造悬空判据）"

# ⑦ 收尾（禁跳）
npm run check
```

## 七、与其它格的关系

| 格 | 关系 |
|:--|:--|
| 格 1 `E6#114` | **硬前置**：悬空读数来自它。**口径以格 1 为准**，本格零复刻。 |
| 格 2 `E6#115` | **自证关系**：若本格新增只读面，格 2 的门禁会要求**只加不删**——本格正好是它的第一个真实受益者。 |
| 格 3 `E6#116` | 无关（读数不看退役账；但若读数要提示「你用了退役名」，那是格 6 的取舍）。 |
| 格 5 `E6#118` | **下游（消费者）**：格 5 只调一个入口、只画读数。 |
| 格 6 `E6#119` | **下游**：把悬空判据搬给作者；读数本身**不下发**（作者看的是自己仓里的红黄，不是状态）。 |

---

<!-- §八 起 = 本格执行记录 -->

## 八、执行记录（**待格 4 那一棒填写**）

### 8.1 落地表（`file:line` 可点验）

- 待填。

### 8.2 8 维度设计的最终答案（**本格的裁**）

- 待填（8 行逐条；尤其是 ② 选了 (a) 还是 (b)，以及**为什么**）。

### 8.3 读数形状（**格 5 要照抄的东西**）

- 待填：字段名 / 取值域 / `null` 语义 / 唯一入口名。

### 8.4 真样本读数（四类**可判**状态；含哪类是桩）

- 待填（`current` / `compatible` / `drifted` / `incompatible` 各至少一条）。

### 8.5 残余边界（诚实清单）

- 待填：例如「`current` 与 `compatible` 只靠两个真日期区分，dev 下壳日期为 `—` ⇒ 一律落到 `compatible`」「离线时 catalog 缺失 ⇒ `unknown`」等。

### 8.6 `npm run check` 末尾三行

- 待填。

### 8.7 交接（下一棒 ＋ 剩余队列）

- 待填（**队列表整张复制**，自己那格改 ✅）。
