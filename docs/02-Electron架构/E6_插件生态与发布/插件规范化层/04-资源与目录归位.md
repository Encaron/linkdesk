# 04 · 资源与目录归位（L3.7 第 3.7.4 轮）

> **🔖 非新能力文档（2026-09-11 判定）：** 本文出现的 `contributes.i18n` 标记**是「引用既有贡献点做对照」**——本层**不新增任何壳 API、不新增贡献点**，本文只做**文件位置的归位与随包规则审计**。判定理由见 [00 号档案](00-整理档案.md) 卷首的「非新能力文档」条。
>
> **本轮两件事**：① 把 editor 散在插件根的三张图归位到 `resources/`；② 顺手把**资源随包规则**盘一遍——因为「图放哪」不是审美问题，是**装进软件后还在不在**的问题。
> 顺带纠正一个用户观察里的半对半错（§〇）。

---

## 〇、先纠一个错：settings 的「空」有两回事，别混着答

**用户观察**：「为什么 settings→src 下有文件是空的？难道是因为 settings 是早期的插件吗？」

**答案 = 一半对、一半错，而且这两半必须分开说：**

| 观察到的现象 | 真因 | 结论 |
|:--|:--|:--|
| **`src/` 下有空目录** | **不是历史遗留** —— 是**本仓 AI 自己的探针残留**：做 E6#88 门禁负测试时，脚本 `88c-probe.mjs` 为验证「超限检测」造了一批临时目录，用完只删了脚本没删目录。**已 `rmdir` 清理，当前全仓空目录数 = 0**（2026-09-11 实测） | ❌ 与「早期插件」无关 |
| **settings 没有 `i18n/`** | 🔴 **也不是问题** —— 初判「缺译文」，实测**证伪**：`t()` 走**全局** bundle，settings 的 46 条 key 有 **44 条已在 `plugins/lang-defaults/en.json`**（壳的默认语言包），`audit-i18n.mjs --strict` 早就报 0 缺。**与「早期插件」无关**（[03 §四](03-官方插件补齐.md) 有完整纠正过程） |
| **settings 的 CSS 散在 `src/views/`**（其余 5 只用 `src/styles/`） | **确实是历史遗留** —— settings 是**并列最老**的插件（首提交 **2026-07-22**），后来的插件才统一到 `src/styles/` | ✅ 与「早期插件」有关（[05 §2.7](05-脚手架换代.md) 记规则，**本轮不回填**） |

**🔴 这次事故真正要沉档的不是那三个空目录，是一个机制盲区：**

> **`git status` 看不见未跟踪的空目录。** Git 不记录目录，只记录文件——一个空目录在工作区里存在**一万年**，`git status` 也报 clean。所以「提交前看了一眼 git status 是干净的」**不能**作为「仓库没有垃圾目录」的证据。

- **门禁落点**：把「空目录扫描」加进机械门禁（[06 号档案](06-门禁扩域与验收.md)），而不是靠人记得看。
- **纪律落点**：**造测试用的一次性目录，造在 `E:\linkdesk-build+scratch\`，不要造在 `plugins/` / `src/` 里**（承 memory `dev-artifact-hygiene`：开发产物只落 build+scratch，仓内零残留）。

---

## 一、editor 图归位（本轮主任务）

### 1.1 现状——三张图全在插件根

| 文件 | 谁在用 | 位置 |
|:--|:--|:--|
| `cover.svg` | `README.md:3` 的 `![…](cover.svg)` → 市场**详情页说明区**的场景封面 | 🔴 插件根 |
| `icon.svg` | `plugin.json` 的 `"icon": "icon.svg"` → 图标栏 / 标签 / 市场身份图 | 🔴 插件根 |
| `cover-brutalist.svg` | **无人引用**（[实测 grep](#) 全仓零命中） | 🔴 插件根 |

**其余 14 只有 `resources/` 的插件，无一把图放在根。editor 是唯一的例外**——它比 `resources/` 惯例早，之后没回头。**M4（官方插件=教具）要求同形 ⇒ 归位。**

### 1.2 目标形态

```
plugins/editor/
├─ plugin.json        "icon": "resources/icon.svg"     ← 改
├─ README.md          ![…](resources/cover.svg)        ← 改
├─ resources/
│   ├─ icon.svg              ← git mv icon.svg
│   └─ cover.svg             ← git mv cover.svg
└─ src/
```

**三处必须同批改，漏一处即裂图/掉图标：**

1. `git mv icon.svg resources/icon.svg`
2. `git mv cover.svg resources/cover.svg`
3. `plugin.json`：`"icon": "resources/icon.svg"`
4. `README.md`：`](cover.svg)` → `](resources/cover.svg)`

**为什么第 4 条不能漏**：README 里的**相对路径就是随包依据**——SDK 打包时扫 README 的相对引用，把命中的文件拷进 zip（[vite-config.ts:553-555](../../../../packages/plugin-sdk/src/vite-config.ts)）。改了图的位置不改 README ⇒ **文件不在 zip 里 ⇒ 详情页说明区裂图**（正是 E6#70a 修的那个坑）。

**不动 `version` 的理由不成立**——`plugin.json` 的 `icon` 值变了，**打包产物内容变了** ⇒ **bump**（`1.0.7` → `1.0.8`，`fix:`）+ **zip 重建**。

### 1.3 🟢 `cover-brutalist.svg` —— **用户 2026-09-11 拍板：删**

- **来历**：E6#68 封面批里做了 A/B 两版，A 定稿（`cover.svg`），B「**留存**」（checklist 原话）。
- **现状**：**全仓零引用**（README 不引、`plugin.json` 不声明、文档不引）——SDK 也不随包（只随 README 引用 + 声明字段）⇒ **仓库里的一件纯占位资产**，踩的正是本层判据 **M2**（声明必须有消费者）。
- **✅ 决定**：`git rm`。**先拷一份到 `E:\linkdesk-build+scratch\` 留档再删**（A/B 设计过程可回溯），然后从仓里删除。
- **它为什么此前不能由我自行处理**：这是**上一次拍板「留存」的东西**——按 memory `rule-extrapolation-not-consent`，「M2 要求删死文件」这条规则**不能自动外推**到一件已经被人拍过「留存」的资产上。**用户 2026-09-11 明确答复「同意」后才动**，痕迹留在这里。

### 1.4 举一反三——还有没有别的「图不在 resources/」？

**全仓实测（2026-09-11）：根目录有图的插件只有 editor 一只**（`ls plugins/*/icon.svg` → 仅 editor；`cover*.svg` 同理）。**归位一次就够，没有第二家。**

---

## 二、资源随包规则审计（为什么要归位）

**这一节解释「位置」为什么有实际后果。**

### 2.1 打包器**只**随包这六类（[vite-config.ts:547-562](../../../../packages/plugin-sdk/src/vite-config.ts)）

| # | 随包对象 | 判据 |
|:--|:--|:--|
| 1 | `contributes.i18n` 声明的文件 | **声明** |
| 2 | `README.md` | 固定名 |
| 3 | `CHANGELOG.md` | 固定名 |
| 4 | **README 里相对引用的媒体** | **引用** |
| 5 | `manifest.icon` 指向的文件 | **声明** |
| 6 | `manifest.marketIcon` 指向的文件 | **声明** |

> 🔴 **读法**：`resources/` 这个**目录名本身没有任何魔法**——没有任何一行代码在扫 `resources/`。文件能进 zip，靠的是**被 README 引用**或**被字段声明**。`resources/` 是**约定**（放一起好找、好归位），不是**机制**。

### 2.2 反例：为什么不能「反正 resources/ 会自动随包」

见 [12 档案 §三 的 ℹ️](01-规范文档对齐.md)——规范收口到 `resources/` 是**规整性**要求。**一个放在 `resources/` 但从没被引用过的文件，照样不会随包，照样没人读。**

### 2.3 🔴 顺手抓到的冗余：`copyFileInto(root, pkgDir, "icon.svg")`

[vite-config.ts:548](../../../../packages/plugin-sdk/src/vite-config.ts) 硬写死拷一次**根目录的 `icon.svg`**，**紧接着** 558 行又按 `manifest.icon` 声明路径拷一次。

- **同一件事两条路**（M1 违规）：声明字段是唯一真源（硬约束 11），第 548 行是**惯例推断**——正是硬约束 11 明令禁止的「用文件位置推断插件属性」的写入侧版本。
- **实测它的实际作用 = 0**：全仓只有 editor 有根 `icon.svg`，而 editor **同时声明了** `"icon": "icon.svg"` ⇒ 第 558 行已覆盖。**删掉 548 行，全仓随包结果零变化。**
- **顺带**：删掉它还能防一个未来的坑——作者把图标挪进 `resources/` 但忘了改字段时，第 548 行会**静默把一张没人用的根 `icon.svg` 也塞进包里**，掩盖错误。
- 👉 **建议：删**（`@linkdesk/plugin-sdk` 轴 +1 patch）。若担心第三方作者依赖，可先加一条 `validate` 提示再删——**但别留着**。

### 2.4 数据型插件走另一条路（**不是漏洞，别误判**）

主题 / 语言包这类**没有 `src/index.tsx` 的数据型插件**（`theme-*` / `lang-*`）**不走上表**——它们**整目录拷贝**进 zip。

**实证**（2026-09-11 `unzip -l`）：`theme-panorama.linkdesk-plugin` **1,536,836 B，内含 `resources/sailor-moon.jpg`**——而该文件只被 `themes/panorama.json` 以 `linkdesk://theme-panorama/resources/sailor-moon.jpg` 引用，**不在上表六类里**。同理 theme-zones 的 `zones-bg.svg` / `paper-texture.svg`、theme-aurora-glass 的 `aurora-bg.svg` 都在包里。

> ⚠️ **审计记录**：初查时曾怀疑「主题的 `resources/` 不随包」是 bug——`unzip -l` 一照即证伪。**写进档案以免下一个人再疑一遍。**

---

## 三、图标 / 封面覆盖矩阵（顺盘，含缺口）

| 插件 | `icon` | `marketIcon` | `resources/` 里有什么 | 缺口 |
|:--|:--|:--|:--|:--|
| editor | `icon.svg`（根） | 无 → 回落 `icon` | **（无目录）** | 🔴 **§一 归位** |
| file-tree | `resources/icon-bar.svg` | `resources/icon.svg` | cover / icon / icon-bar | — |
| marketplace | `resources/icon.svg` | `resources/icon-brand.svg` | icon / icon-brand | 🔴 无 `cover.svg`（[03 §3.4](03-官方插件补齐.md)） |
| python | `resources/icon.svg` | 无 → 回落 | cover / icon | — |
| serial-monitor | `resources/icon-bar.svg` | `resources/icon.svg` | cover / icon / icon-bar | — |
| settings | `resources/icon-bar.svg` | `resources/icon.svg` | cover / icon / icon-bar | — |
| lang-defaults | `resources/icon.svg` | 无 → 回落 | cover / icon | — |
| theme-aurora-glass | `resources/icon.svg` | 无 → 回落 | aurora-bg / cover / icon | — |
| theme-terminal | `resources/icon.svg` | 无 → 回落 | cover / icon | — |
| **theme-mint-soda** | **❌ 缺** | 无 | **`icon.svg` 存在但没声明** | 🔴 **见 §3.1** |
| theme-zones | `resources/icon.svg` | 无 → 回落 | icon / paper-texture / zones-bg | — |
| theme-panorama | **❌ 缺** | 无 | 只有 `sailor-moon.jpg` | ⚪ 见 §3.2 |
| theme-defaults / theme-iconset-pastel / theme-pill / theme-songti / theme-twilight-forest | ❌ 缺 | 无 | （无目录） | ⚪ 见 §3.2 |
| lang-test-ja | ❌ 缺 | 无 | （无目录） | ⚪ 语言包，纯数据 |
| floating-panel-demo / panel-demo | codicon 名（`layout`） | 无 | （无目录） | ⚪ 演示插件 |

### 3.1 🔴 theme-mint-soda：**图在、字段没声明**（真缺口）

`resources/icon.svg` **存在且已随包**（zip 实测有它），但 `plugin.json` **没有 `icon` 字段** ⇒ 身份裁决 `pickIdentityArt(marketIcon ?? icon ?? 默认块)` **落到默认块** ⇒ **市场列表行 / 详情页显示的是默认「插头」块，不是它自己的薄荷图**。

**两条修法，选一（不许两条都不做）：**

- **(a) 补字段**（推荐）：`plugin.json` 加 `"icon": "resources/icon.svg"` —— 图已经在包里，加一行就生效，**零新资产**。
- **(b) 删文件**：`git rm`，认下「薄荷苏打没有专属身份图」。**但图都画好了，删是浪费。**

👉 **推荐 (a)**。用户可见变化 ⇒ bump `1.0.0` → `1.0.1`。

> 🔴 **注意**：这个缺口**不是本层引入的**，是历史遗留。**但它正是「声明必须有消费者」的反面**——一个资产躺着没人读。**用户问「为什么其他几个插件都有 resources」时看到的表面整齐，底下藏着这一处不对齐。**

### 3.2 ⚪ 其余缺 `icon` 的主题 / 语言包（**建议不动，但要有个说法**）

6 只主题 + `lang-test-ja` 没有身份图 ⇒ 市场里显示默认块。**这是可接受的**：

- 它们是**纯数据插件**（没有 UI、没有视图），默认块本身就是一个诚实的表达；
- 补 8 张图是**新增设计资产**，属封面铺画工作（E6#68 用户 2026-09-09 拍板「暂停铺剩余」），**不在本层范围**；
- 与 `theme-mint-soda` 的区别是：**mint-soda 的图已经存在了**，只是没接线——那是漏，不是欠。

👉 **不动。** 若将来铺画，归 [E6#68](../E6-执行清单.md)。**本条只是把「默认块是正常的」写下来，免得下次又当 bug 报一遍。**

---

## 四、settings 的 `views/hooks/`（小项）

### 4.1 现象

settings 里 hooks 有两种摆法：

| 摆法 | 例 |
|:--|:--|
| **与视图同夹**（多数插件都这样） | `views/SettingsView/useSettingsEvents.ts`、`views/SettingsView/SettingRow/useSettingRowValue.ts` |
| **集中在一个 `hooks/` 夹** | `views/hooks/useBaselineSeedsIpc.ts` / `useConfigurationValueIpc.ts` / `useEffectiveTokensIpc.ts` / `useUserOverridesIpc.ts` |

### 4.2 判据（memory `shared-hooks-placement`）

> **共享 hook → `@linkdesk/ui`；域专属 hook → 插件本地。**

实测这 4 个 IPC hook 是**settings 域专属**（全仓只在 settings 里被引用，`@linkdesk/ui` 里没有对应件）⇒ **不违「共享 hook 归位」**。

**但同一个插件里两套摆法 = 不归一**（M1）。且这 4 个是**配置 IPC 层**，按壳目录规范的语义更贴 `src/services/` 而不是 `src/views/hooks/`。

### 4.3 结论

- **本层只记录规则，不做搬迁**——理由：① 它不是死代码，无功能损害；② 搬迁会动 4 个文件的 import 路径，属「搬家」可做但收益小；③ **本轮已有 editor 归位这个真实搬家，别在一个低收益项上分心**（memory `reduce-parallel-subagents` 的同理：一次做透一件）。
- **规则留给下次**：**插件内跨视图共享的域逻辑（含 IPC hook）→ `src/services/`；单视图专属 → 与视图同夹。** 新增文件按这条走，存量不专门回填。
- 👉 若要回填，**单独立轮**，别塞进 3.7.4。

---

## 五、验证（本轮收工判据）✅ 2026-09-11 全过（提交 `1f1d323c7`）

1. **editor 归位到位**：`ls plugins/editor/` **根下无 `.svg`**；`resources/` 下有两张图；`grep -n '"icon"' plugins/editor/plugin.json` → `resources/icon.svg`；README 第 3 行 → `resources/cover.svg`。
2. **zip 里两张图都在**：重建 editor zip 后 `unzip -l` → `resources/icon.svg` + `resources/cover.svg` **同时在列**（**这是本轮的硬判据——位置改了但没随包 = 白改**）。
3. **实机看得见**：装 editor → 图标栏/标签页有图标（不是默认块）→ 市场 editor 详情页说明区**封面真显示**（不裂图）。
4. **`cover-brutalist.svg` 有结论**：删除 / 归位 / 不动——**三选一有记录**，不留「还在根上没说法」。
5. **theme-mint-soda 有结论**：补字段 / 删文件——**二选一有记录**。
6. **空目录数 = 0**：`find . -type d -empty -not -path '*/node_modules/*' -not -path '*/.git/*' -not -path '*/dist/*'` → **无输出**。
7. `npm run check` 全绿；**改动插件的 zip 全部重建**；打包版验收走一次性 `--user-data-dir`（memory `bundled-same-version-no-refresh`）。

### 收官结果（2026-09-11）

| 判据 | 实测 |
|:--|:--|
| ① editor 归位到位 | `ls plugins/editor/*.svg` → **无匹配**（根下零 `.svg`）；`resources/` 下两张图；`plugin.json` 的 `icon` = `resources/icon.svg`；README 第 3 行 = `](resources/cover.svg)` |
| ② **zip 两张图在列**（本轮硬判据） | `unzip -l editor.linkdesk-plugin` → `resources/icon.svg` 1336B + `resources/cover.svg` 9303B **在列**；zip 内 `plugin.json` 的 `icon` = `resources/icon.svg`；**无根 `icon.svg` 残留** |
| ③ 实机看得见 | 编辑器图标 → `linkdesk://editor/resources/icon.svg`（`naturalWidth=48`）；详情页说明区封面 → `linkdesk://editor/resources/cover.svg`（`naturalWidth=640`）**真加载不裂图** |
| ④ `cover-brutalist.svg` 有结论 | ✅ **删**（用户拍板）——删前已 `cp` 留档 `E:\linkdesk-build+scratch\l37-93a\cover-brutalist.svg`（3745B） |
| ⑤ `theme-mint-soda` 有结论 | ✅ **补字段**——实机实证：市场侧栏该行 DOM 已是 `img.plugin-icon` → `linkdesk://theme-mint-soda/resources/icon.svg`（`naturalWidth=150`），**不再是默认「插头」块** |
| ⑥ 空目录数 = 0 | `find . -type d -empty -not -path '*/node_modules/*' -not -path '*/.git/*' -not -path '*/dist/*'` → **无输出** |
| ⑦ `npm run check` | **REAL_EXIT=0**（152 文件 / 2060 测试；`check-bundled-version-bump` 绿 = **18 zip 全过，其中只有 editor 与 theme-mint-soda 是 `version-bumped`，其余 16 只 `identical`**——那 16 只在重打 bulk 时字节变了但内容指纹没变，已 `git checkout` 还原，不制造无意义二进制 diff） |

### 🔴 执行时逼出的三条（比任务本身值钱）

1. **清单里的版本号是「写清单那一刻」的快照，照抄必错。** 本文档 §1.2 / §3.1 写的 `1.0.7 → 1.0.8` / `1.0.0 → 1.0.1`，**早在上一轮（3.7.3）就被吃掉了**——editor 在那轮的 9 只搬家名单里、`theme-mint-soda` 在 10 只短 README 名单里，各已 bump 过一次。本轮**现算**为 editor `1.0.8 → 1.0.9`、`theme-mint-soda` `1.0.1 → 1.0.2`，并按 3.7.3 立下的**死规矩**同笔补了各自 `CHANGELOG.md` 的对应段。
   > **这是本次会话第 3 次撞上同一类坑**（3.7.3 撞两次：bump 与段的对应关系、`lang-test-ja` 已是 1.0.1）。**统一记法：`version` 类判据一律执行时现读现算，文档里的数字只当「预期形态」看，不当「操作数」用**（memory `snapshot-shadows-truth-bug-class` ①「快照遮蔽真值」）。
2. **删掉 548 行的实证方式是「新 zip 里图还在」**，不是「grep 不到那行」。**因为删除后我写的注释里也引用了那行代码**（解释为什么删）⇒ 宽松 grep 会命中注释、看起来像没删干净。**实证靠 `^\s*copyFileInto\(` 这种「作为语句」的严格模式，再加「产物侧证据」**（editor 包内两张图分别在列，证明 `manifest.icon` 分支 + README 引用两条路各自有效）。**验收要用产物说话，不用文本说话。**
3. **SDK 轴待发必须记账，但不能靠 `release:mark` 把灯关掉。** §2.3 建议的「删（SDK 轴 +1 patch）」在执行时判为**本轮不发**：93b 对既有插件是**零可观测变化**（删的是一条已在别处覆盖的冗余路径），单独为它发一版不值；但 `check-npm-release.mjs` 会因此**亮黄灯**（「`@linkdesk/plugin-sdk` 作者面内容自 npm v0.1.9 后已变」）。
   - **处置：不 bump、不发布、也不 `release:mark` 绕行**——绕行 = 把灯关掉，等于看不见（正是 [#95g](06-门禁扩域与验收.md) 那类「藏一个月也不出声」的反面教材）。
   - **记为 `0.1.9 → 0.1.10` 待发**，归入 3.7.6 的 npm 轮，与 `@linkdesk/ui@0.1.3`（#95g）**同批**走。**已当面报用户。**

> ⚠️ **顺带记一条 dev 形态观察（非缺陷，别当 bug 报）**：dev 模式下 `linkdesk://` 协议**有双根回落**——源树优先，源树找不到时回落到 `%APPDATA%/linkdesk/plugins/` 的**已安装副本**。实证：本轮移动 editor 图之后，`linkdesk://editor/icon.svg`（源树已无此文件）**仍返回 200**，因为它从安装副本（v1.0.6 旧布局）里取到了；而 `linkdesk://editor/abap-CrvW7_qN.js`（只存在于安装副本的 Monaco 语言块）同样 200。**判定「某资产在不在包里」必须看 zip / 看打包版，不能拿 dev 的协议探测当证据**（memory `bundled-same-version-no-refresh` 同源）。
