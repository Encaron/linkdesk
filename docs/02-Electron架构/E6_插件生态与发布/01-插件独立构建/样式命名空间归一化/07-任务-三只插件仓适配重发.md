# 任务 `#109g-7`：三只插件仓适配重发（marketplace / serial-monitor / settings）——第 1.11 轮

> **一句话**：把三只插件里**对共享组件类名的 scoped 调优**（全量 7 处）改到 `ldk-*`，升依赖到 `@linkdesk/ui@0.2.0`，各 bump 一个 PATCH，重打包重发。
> **前置**：第 1.9 轮已 ✅（**npm 上必须有 0.2.0**，否则本仓 lock 拿不到）。可与第 1.10 轮（作者面文档）并行。
> **规模**：3 个仓 · 7 行 CSS · 各一次 verify/build/publish。
> **提交轴**：插件仓 ×3（`E:\linkdesk-plugins\official\<id>`，**不是壳仓**）。

---

## 一、先记住三件事（顺序错了会白干）

1. 🔴 **依赖区间必须手动放宽**：三仓现在写的是 `^0.1.4` / `^0.1.7` ——**caret 在 0.x 上不允许 0.2.0**。所以**先**把 `package.json` 的 `@linkdesk/ui` 改成 **`^0.2.0`**，**再** `npm install`（不是 `npm update` 就能解决——区间不放开，它永远只在 0.1.x 里挑）。
2. 🔴 **改的是「插件自己 CSS 里的调优选择器」，不是「调用组件的地方」**：三只插件都通过 `<Combobox>` / `<Slider>` 这类**组件**消费，组件内部类名由 `@linkdesk/ui` 决定——插件侧的改动只有**scoped 调优**那几行。
3. 🔴 **推送**：本机代理（`127.0.0.1:7890`）**实测会握手失败**（上轮读数：经代理 curl=000、直连=200）⇒ **直连推送**；判据是 curl 两端对比，不是「以前都走代理」。

---

## 二、逐仓面清单（全量，实测；`E:\linkdesk-plugins\official\`）

| 仓 | 版本 | 要改的行 | 改成 |
|:--|:--|:--|:--|
| **marketplace** | 1.0.31 → **1.0.32** | `src/styles/detail/detail-changelog.css:84` `.mpd-chg .mdv` | `.mpd-chg .ldk-mdv` |
| | | `src/styles/detail/detail-changelog.css:96` `.mpd-title-row .badge .codicon` | `.mpd-title-row .ldk-badge .codicon` |
| **serial-monitor** | 1.0.10 → **1.0.11** | `src/styles/ControlPanel.css:46` `.control-bar .selectbox-trigger` | `.control-bar .ldk-selectbox-trigger` |
| | | `src/styles/ControlPanel.css:56` `.control-bar .combobox` | `.control-bar .ldk-combobox` |
| | | `src/styles/ControlPanel.css:60` `.control-bar .combobox-field` | `.control-bar .ldk-combobox-field` |
| | | `src/styles/ControlPanel.css:68` `.control-bar .combobox-input` | `.control-bar .ldk-combobox-input` |
| **settings** | 1.0.9 → **1.0.10** | `src/views/SettingsView-rows.css:146` `.settings-slider-control .slider` | `.settings-slider-control .ldk-slider` |

> ⚠️ 上表是**全量**（[00 §1.2](00-整理档案.md)），但**不排除**还有借用了**族名变体**（如 `.combobox-arrow`）的地方——**每仓先跑一次全量 grep 自查**：
> `grep -rnE "\.(badge|button|combobox|mdv|selectbox|sle|slider|toggle)[a-zA-Z0-9_-]*" <该仓>/src --include=*.css`
> 命中里凡**不是自有前缀**的都要一起改（上表之外发现的，**在交接.md 记一行**）。

---

## 三、逐仓步骤（三个仓各走一遍）

1. **改依赖区间** `^0.2.0` → `npm install` → 确认 `node_modules/@linkdesk/ui/package.json` 是 **0.2.0**（贴读数）。
2. **改 §二 那几行**（先 grep 自查，再逐行改）。
3. **bump**：`package.json` + `plugin.json` 的版本 + `CHANGELOG.md` 一条 + 该仓 `AGENTS.md` 里的版本事实（有的话）——照该仓既有规矩（L7 已给 18 仓铺过 `sync-plugin-agents`，`npm run sync:plugin-agents -- --check` 会验漂移）。
4. **门禁**：`npm run verify`（严格腿 = SDK lint 全腿 + 跨插件 import + 字典完整性 + 声明自洽）**必须全绿**。
5. **重打包 + 发布**：`npm run build` → `npm run publish`（GitHub Release + `marketplace.json` 目录条目）。
6. **提交 + 推送**（直连）。
7. **回壳仓**更新官方目录：`npm run catalog:official`（生成候选）→ 按既有流程收录（**版本与账逐条相等**才算收录成功）。

---

## 四、判据（做完逐条给读数，**逐仓**给）

> ✅ **2026-09-15 补勾（第 1.16 轮收尾清账——用户点出「前面 AI 做完却留着空方框」）**：以下各框是该轮判据，对应轮次 = 第 1.11 轮（`#109g-7`），**已在 [E6-执行清单](../../E6-执行清单.md) 宣告完成**（逐条读数见该文件「第 1 层头」的轮次进度行、以及 [交接.md](交接.md) 的该轮次段）。**本次只改勾选状态，未改任何判据条文与结论**；若日后发现某条实际未过 ⇒ 改回 `[ ]` 并写明原因。

- [x] 三仓的 `@linkdesk/ui` 依赖区间 = `^0.2.0`，且 `node_modules` 里的实际版本 = 0.2.0（贴三行）
- [x] 三仓 `grep` 自查：**没有任何非自有前缀**的共享组件类名引用残留（贴三行 grep 输出）
- [x] 三仓 `npm run verify` 全绿（贴三行摘要）
- [x] 三仓 GitHub Release 与目录条目版本 = 1.0.32 / 1.0.11 / 1.0.10
- [x] 三仓 `git status` 干净、与 `origin/main` 同步（贴 `git rev-list --count origin/main..HEAD` = 0 ×3）
- [x] 壳仓 `npm run catalog:official` 之后，官方目录里这三条版本已更新（贴读数）

---

## 五、提交与收尾

- 三仓提交信息各自带类别前缀（各仓有自己的门禁；L7 已铺 `commit-msg` 同款纪律），建议：
  `fix: @linkdesk/ui 0.2.0 适配——共享组件类名加 ldk- 前缀（scoped 调优同步）`（**英文仓按该仓既有语言习惯**）。
- **收尾两件**：交接.md 顶部追加 + 清单第 1 层头轮次进度（1.11 ✅）。
- 本任务**不发壳**（壳 bump 在第 1.12 轮）。

---

## 六、禁区

1. **不许改插件自己的类名前缀**（`settings-*` / `mpd-*` / `ms-*` / `control-*` 是**自有前缀**，正确得很）。
2. **不许改插件的功能代码**（只动 CSS 调优行 + 版本/依赖/CHANGELOG）。
3. **不许为了「让 verify 绿」放宽该仓门禁**（L7 立过的规矩：尺子不许为个案放宽）。
4. **不许只改一两个仓就收工**——三只插件是**同一批**（§1.3 的机制：漏掉的那只在它下次升级 ui 时会静默坏掉）。
