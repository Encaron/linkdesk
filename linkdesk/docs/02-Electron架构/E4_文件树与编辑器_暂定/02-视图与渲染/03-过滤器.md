# 02-03 — 过滤器

> 对标 VS Code `explorerViewer.ts` — `FilesFilter`（~200 行）。
> 对应执行清单：第 1 层 #9。

---

## 一、当前实现 ✅

| 组件 | 文件 | 行数 |
|:--|:--|:--:|
| `FileExcludeFilter` | `FileExcludeFilter.ts` | ~72 |
| glob 匹配 | `minimatch` 或等价实现 | |
| 接入点 | `FileTreeModel.getChildren()` 过滤 | |

**当前只支持 `files.exclude` glob 模式。** `.gitignore`、文件嵌套、只读检查均未实现。

## 二、VS Code 对标

```typescript
// VS Code: FilesFilter implements ITreeFilter<ExplorerItem>
class FilesFilter {
    filter(stat: ExplorerItem): boolean {
        // 1. files.exclude glob 匹配
        // 2. .gitignore 集成 (explorer.excludeGitIgnore)
        // 3. fileNesting 文件折叠
        // 4. 只读/锁定检查
    }
}
```

## 三、待建 ⬜

| 功能 | 说明 | 配置项 |
|:--|------|------|
| `.gitignore` 集成 | 读取 `.gitignore` → 过滤 | `explorer.excludeGitIgnore` |
| 文件嵌套 `fileNesting` | `file.ts` → `file.d.ts`、`file.js`、`file.js.map` 折叠 | `explorer.fileNesting.*` |
| 只读/锁定标记 | 文件只读时禁止重命名/删除 | #20 ContextKey |

## 四、AI 进场须知 🛡️

- **`FileExcludeFilter.matches(relPath)`——入参是相对路径，不是绝对路径**
- **全局 exclude 模式从 `ConfigurationService.get("files.exclude")` 读**
- **dimmed vs 隐藏——当前是隐藏。dimmed CSS 类已就绪，改为灰显需同时改 `FileTreeModel.getChildren` 逻辑**
