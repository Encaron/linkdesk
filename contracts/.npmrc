# E6#91g：@linkdesk 作用域只存在于官方 registry（npmmirror 不镜像新首发 scoped 包，2026-09-05 实测 404）。
# 仅 @linkdesk 指官方，其余依赖继续走默认 npmmirror（中国加速）——per-scope 精确路由，不全量迁 npmjs。
# 🔴 **必须放在包目录里**：npm 只读**当前目录**的 .npmrc，**不向上递归**——从本目录 `npm publish` 时
# 根工程 .npmrc 读不到，会静默回落到用户级默认源（实测报「need auth to https://registry.npmmirror.com」）。
@linkdesk:registry=https://registry.npmjs.org/
