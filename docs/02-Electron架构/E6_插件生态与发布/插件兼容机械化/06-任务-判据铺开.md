# 格 6 · 任务 `E6#119`：**判据铺开**——让作者在自己仓里就能跑这条判据

> **一句话**：前三格造的尺子都住在**壳仓**里，官方 18 仓的 CI 跑不到它们；本格把口径**搬进 SDK 随包下发**，18 仓 lock 接腿 —— 作者**在他自己的仓里**就能跑出红黄，不必来问维护者。**这是「让第三方插件作者自由制造插件」这句话的机械兑现。**
>
> **编号**：`E6#119` ｜ **轮次**：第 8.6 轮 ｜ **队列位置**：**第 6 棒（共 7 棒；⚠️ 原写「恒在最后」，2026-09-18 立格 7 `E6#120` 后**不再最后**）**
> **前置**：**格 1（`E6#114`，口径）＋ 格 2（`E6#115`，口径）**——⛔ 口径没定稿就搬 = 搬错
> **提交轴**：`@linkdesk/plugin-sdk`（PATCH）＋ `@linkdesk/plugin-docs`（PATCH，若作者面文档变）＋ **18 仓 lock** ＋ 壳 PATCH
> **规模**：SDK 一条/两条腿 ＋ 自测 ＋ 18 仓接腿 ＋ 发版。**预期零壳侧行为改动**
> **设计前置声明**（`check-design-flow` §8.4 ③）：本格对**壳运行时**不是新能力（不发 IPC、不碰 `window.linkdesk.`、不改壳行为），但**对作者侧是新面**（SDK 新导出 `runDanglingNameCheck` ＋ 随包 `schemas/host-reserved.json` 扩类名栏 ＋ 作者面 docs 一届）⇒ §二第一步已补**完整 8 维度设计前置**。文中出现的 `window.linkdesk.` / `contributes.` 只是**被校验的对象名**。

---

## 〇、本格在轴上的位置

| 件 | 射程 | **不射程** | 产出 |
|:--|:--|:--|:--|
| 判据铺开 | SDK 腿（作者侧能跑）＋ 随包下发宿主定义集 ＋ 18 仓 lock 升级 ＋ 发版 | ❌ **不造第二条口径**（只是搬运）❌ 不抢跑改插件源码（除 lock/CI 接线）❌ 不改 `fail-closed` 语义 ❌ 不改壳行为 | SDK 腿 ＋ 自测 ＋ 18 仓锁 ＋ 发版实录 ＋ 清单勾格 ＋ 交接段 |

## 一、事实与读数（**进场自己重跑**）

1. **SDK 腿今天已经有什么**：
   - **随包下发宿主定义集**：`packages/plugin-sdk/schemas/host-reserved.json`（E6#111d 起）。
   - **四条 ownership 腿**：`command-ownership` / `config-ownership` / `appearance-ownership` / `context-ownership`。
   - **关键帧引用腿**：`keyframe-refs`（E6#112——`runKeyframeRefCheck`，允许集 = 本仓被扫描 CSS 的 `@keyframes` ∪ 宿主保留账 8 条；报点并入**同一条** `check-css-namespace` 腿）。
   - **18 仓 CI 的 `strictLegs` fail-closed**（沿用 E6#102 起的形状）。
2. **口径同源是硬要求，且已有先例怎么钉**：E6#112 用 `scripts/check-css-namespace.mjs:850-870` 的 **`锚⑨`（4 句锚词）**把「SDK 抽取口径与壳侧同源」钉住 ⇒ 本格**照这个做法钉**（⛔ 不许只靠注释说「与壳一致」）。
3. 🔴 **铺开边界（今天的真实状态，不许说成「已铺开」）**：18 仓 lock **今天仍钉 `@linkdesk/plugin-sdk@0.1.34`**（E6#112 铺开时的读数）⇒ 那条腿要等**各自下次升 SDK 依赖**才进它们的 CI。**本格要如实复查这个读数并写进档**（#112 的残余边界共 6 条，见其 §八.4）。
4. **发版纪律**：SDK 内容变了 ⇒ 按「**凡更新就发**」必须发（`npm publish`）；`check:npm-release` 黄灯 = 基线未重记（#112 的先例：`0.1.36 → 0.1.38`、`plugin-docs 0.1.15 → 0.1.18`）。
5. **账的三处拷贝**：`scripts/host-reserved.json`（真源）→ 运行时副本 `src/core/registry/host-reserved.generated.ts` → SDK 副本 `packages/plugin-sdk/schemas/host-reserved.json`（`npm run audit:plugin-scope:regen` 生成；`--check` 对账）。

## 二、要做（**六步，缺一即半成品**）

### 第一步：8 维度设计前置（**未填完不许写代码**；本格是**新能力**：给作者加一条腿，所以八维必须逐条回答，⛔ 不许拿「就是搬个函数」当免答理由）

| 维度 | 本格要回答的 |
|:--|:--|
| **① 能力边界（含保底）** | 新增的是一条**只读、只报不拦**的静态判据（作者在自己仓里跑）：输入 = ① 作者本仓 CSS 的定义集 ② **随包下发的宿主定义集**（`schemas/host-reserved.json`，本格要给它加类名栏）③（条件）退役账 `retired[]`；输出 = 报点 `file:line` ＋ 计数（作者面说法：**N 处引用已失效的名字**）。🔴 **保底**：宿主定义集取不到（老 SDK / 离线 / 文件缺失）⇒ 输出「**未核验**」并计数，⛔ **不许当成 0 处通过**（假绿比假红更坏：作者会以为已经查过了）。⛔ 不判「该不该退役」、不判「老不老」（时间不设阈值，见 [00 §〇c](00-整理档案.md)）。 |
| **② API** | **零运行时 API**：⛔ 不碰 `window.linkdesk.*`、不发新 IPC、壳运行时一行不改。新增面 = **SDK 导出**（`runDanglingNameCheck`，照 `keyframe-refs.ts` 形状，并入既有 `check-css-namespace` 腿）＋ **随包 schema 文件扩栏**（`schemas/host-reserved.json` 加类名定义集）＋ **`@linkdesk/plugin-docs` 一届**（作者面文档，第五轴 PATCH）。⇒ **只加不删**，会被格 2 的门禁自证。 |
| **③ 通信** | **无 IPC、无广播、无握手**：这条腿跑在作者仓的 CI / 本地，与壳运行时不发生任何数据交换。唯一「跨进程」的东西是**一份文件**——宿主定义集由壳侧真源生成、随 SDK 包下发 ⇒ **契约 = 该文件的字段名与语义**（与壳侧逐字同源，用锚词断言钉住，照 #112 的 `锚⑨`）。 |
| **④ 壳侧代码** | 壳侧只动**定义集真源与再生成链**：`scripts/host-reserved.json`（真源）→ `src/core/registry/host-reserved.generated.ts`（运行时副本）→ `packages/plugin-sdk/schemas/host-reserved.json`（随包副本），由 `npm run audit:plugin-scope:regen` 生成、`--check` 对账 ＋ 壳侧**锚词自测**。⛔ 壳的加载器 / 注册表 / 主题引擎零改动。 |
| **⑤ 插件侧代码** | 18 仓**只改 lock（与必要 CI 接线）**，⛔ 源码一行不改（判据⑥ 机械核）；若格 3 决定下发退役账 ⇒ 提示进 `ci-verify`（仍是提示，⛔ 不升级成拒绝）。 |
| **⑥ 显示** | **无界面**（本格是三套词表里**插件作者面**的文字：`处数 ＋ 文件:行 ＋ 升依赖重打包`），⛔ 不许把这些字漏进用户面（用户面五词见 [00 §〇d](00-整理档案.md)）。作者面文案与格 5 的用户面文案**必须同一套说法**（[05 号档](05-任务-市场显示状态.md) §八「下游」那行）。 |
| **⑦ 配置壳给 or 插件给** | **壳给**（单点）：允许集全部来自壳侧真源 ＋ 作者本仓自动采集，作者侧**零配置**。⛔ 不许让作者自定义允许集 / 加豁免名单（自定义 = 第二条口径 ⇒ 迟早漂移）；⛔ 不加设置项、不加 flag（本批不设阈值、不吃配置）。 |
| **⑧ 验收** | 见 §三判据 ①–⑧：**锚词同源可证**（第一条）＋ 单测正负控（正控报、三条负控不报）＋ 18 仓真跑读数 ＋ SDK 真发 ＋ 铺开边界如实 ＋ `npm run check` EXIT=0。 |

### 第二步：把格 1 的判据搬进 SDK（**同源，不新造**）

- 在 `packages/plugin-sdk/src/eslint/checks/` 下**照 `keyframe-refs.ts` 的形状**加一条腿（例如 `dangling-names.ts` / `runDanglingNameCheck`）：作者仓里跑，判「**我喊的名字，我自己没定义、宿主定义集里也没有**」。
- **允许集来源**：① 作者本仓 CSS 的定义集 ② **随包下发的宿主定义集**（从 `schemas/host-reserved.json` 或其扩展取）。🔴 **已核实：现副本不含类名定义集**（`reserved-class-names.json` 只剩 8 条关键帧、`host-reserved.json` 无类名栏，见 [00 号档 §二第 15 条](00-整理档案.md)）⇒ **本格要把类名定义集加上**，并同笔更新三处拷贝与 `--check`（⛔ 不许只在 SDK 本地补、让壳与 SDK 两份口径分叉）。
- 🔴 **口径必须与格 1 同源，并用「锚词」钉住**（照 #112 的 `锚⑨` 做法）：抽取规则的关键句在**壳体与 SDK 两侧逐字一致**，由壳侧自测断言。
- ⛔ **不许在 SDK 里另写一套正则**（那就是第二条口径 ⇒ 迟早漂移）。

### 第三步：作者侧再加一条（**条件做，别硬凑**）

- 若格 3 已落地 `retired[]` **且**决定下发 ⇒ 作者跑 `ci-verify` 时，若其 `plugin.json` / 类名 / API 引用**踩到已退役的名字** ⇒ **报点**（不是拦死，是提示「这个名字已退役，建议改用 …」）。
- ⚠️ **若格 3 决定不下发**，本步**不做**，并在档里写明「作者侧拿不到退役账 ⇒ 提示能力不存在」——**理由必须写**（判「不做」要写明理由）。
- ⛔ 这一条**永远不许升级成「拒绝」**（拒绝墙是自选 2.0，不在本批）。

### 第四步：自测（正控 ≥ 负控）

- **正控**：桩仓里引用一个宿主没有、自己也没有的类名 ⇒ 报（且报点 `file:line`）。
- **负控 A**：桩仓里自己定义了的类名 ⇒ 不报。
- **负控 B**：含插值的动态拼接 ⇒ **跳过并计数**（⛔ 不许报——这是格 1 的误报控口径）。
- **负控 C**：`monaco-*` / `cm-*` 这类第三方自带（桩里模拟「包内 CSS 有定义」）⇒ 不报。
- **锚词断言**：壳侧自测能证明「SDK 抽取口径 == 壳侧口径」（照 `锚⑨` 的形态）。

### 第五步：18 仓接腿 ＋ 真跑读数

- 18 仓 lock 升到本格发的 SDK 版本 ⇒ 各自 CI 跑腿 ⇒ **逐仓读数**（悬空 / 退役名各多少）。
- ⚠️ **升 lock 是改插件仓** ⇒ 🔴 **推 18 仓前等用户点头**（memory `push-wait-for-user`）。
- 🔴 **如实写铺开边界**：哪些仓这次真升了、哪些没升、没升的什么时候会跟上（照 #112 的写法，⛔ 不许含混成「已铺开」）。

### 第六步：发版 ＋ 清单与交接（**同笔**）

- `@linkdesk/plugin-sdk` PATCH 真发 ＋ 重记 `check:npm-release` 基线；`@linkdesk/plugin-docs` PATCH（若作者面文档变）。
- 勾 `E6#119` 全行 ＋ 第 8 层轮次进度行 ＋ 交接段（顶部追加 ＋ 队列表整张复制）＋ **§8.7 收口素材交齐**（六条判据读数 ＋ 残余边界八条 ＋ 大白话四问草稿 ⇒ 格 7 直接汇总，⛔ 别再让它重跑）。

## 三、判据（**逐条给读数**）

- [ ] ① **同源可证**：锚词断言绿（壳侧自测证明两侧抽取口径一致）——**这是本格第一条判据**。
- [ ] ② **单测正负控**：正控报、三条负控不报（读数写进档）。
- [ ] ③ **真仓真跑**：18 仓逐仓读数（悬空/退役名），与格 1 的壳体读数**对得上**（不一致 ⇒ 逐条解释）。
- [ ] ④ **SDK 真发**：`npm view @linkdesk/plugin-sdk version` == 本地版本；`check:npm-release` 无黄灯。
- [ ] ⑤ **铺开边界如实**：逐仓列明「已升 lock / 未升」，⛔ 不许写「已铺开」。
- [ ] ⑥ **不抢跑**：`git status` 在插件仓里**只有 lock（与必要 CI 接线）**，⛔ 无源码改动。
- [ ] ⑦ **fail-closed 未改**：18 仓 CI 的严格腿语义未变（真红仍然红）。
- [ ] ⑧ **收尾**：`npm run check` **EXIT=0**。

## 四、禁区

1. ⛔ **不许造第二条口径**（SDK 腿只搬格 1 的尺子；口径变了就回格 1 改，⛔ 不许在 SDK 里改）。
2. ⛔ **不许把提示升级成拒绝**（无拒绝墙 —— 自选 2.0 才谈）。
3. ⛔ **不许抢跑改插件源码**（只升 lock ＋ CI 接线）。
4. ⛔ **不许把退役提示做成黑名单门禁**（`retired[]` 只提示）。
5. ⛔ **不许改 `strictLegs` 的 fail-closed 语义**（放宽 = 把已设防的腿废掉）。
6. ⛔ **不许未获点头就 `npm publish` / 推 18 仓**。

## 五、交棒要求（**本格 = 第 6 棒；🔴 本批最后一棒 = 格 7 `E6#120`**）

1. **收尾四件套**（交接段顶部追加 · 清单第 8 层轮次进度行 · 勾格全套 · **队列表整张复制**）。
2. 🔴 **本格不再出「全批收口报告」**——收口归**格 7**（[08-任务-通用悬停提示卡.md](08-任务-通用悬停提示卡.md) · 第 8.7 轮 · 本批最后一棒）。本格交出的是 **收口素材**（写进 §8.7，格 7 直接汇总，⛔ 不许让它重跑）：**本批 §五 六条判据逐条读数 ＋ §六 残余边界八条（一条不许省）＋ 大白话四问草稿**（照 E6#111l 形态：做了什么 / 读数是什么 / 什么没做 / 下一步最难的一条是什么）。
3. **判「这条不做」必须写明理由**（尤其是 §二第三步的退役提示）。
4. 🔴 **不许说「已铺开」**：逐仓列明，边界如实。
5. 队列收尾：**队列表整张复制、自己那格改 ✅**。⚠️ **本批还剩格 7** ⇒ ⛔ 不许写「剩余 = 无」（那句留给格 7 的交接段）。

## 六、🔴 进场第一步（复制即可跑）

```bash
# ① SDK 腿今天有什么（四条 ownership + keyframe-refs）
ls packages/plugin-sdk/src/eslint/checks/
grep -n "runKeyframeRefCheck\|strictLegs" packages/plugin-sdk/src/eslint/index.ts packages/plugin-sdk/src/eslint/lint.ts | head

# ② 随包下发的宿主定义集（本格可能要扩它）
node -e "const a=require('./packages/plugin-sdk/schemas/host-reserved.json');for(const k of Object.keys(a)){const v=a[k];console.log(k, Array.isArray(v)?v.length:typeof v)}"

# ③ 三处拷贝一致性（真源 / 运行时 / SDK）
node scripts/gen-host-reserved.mjs --check
npm run audit:plugin-scope:regen && git status --porcelain

# ④ 锚词先例（口径同源怎么钉）
sed -n '845,875p' scripts/check-css-namespace.mjs

# ⑤ 18 仓今天钉的 SDK 版本（铺开边界的读数）
grep -rn "@linkdesk/plugin-sdk" E:/linkdesk-plugins/official/*/package.json 2>/dev/null | head -20

# ⑥ 本批前五格的档（口径从这来）
ls docs/02-Electron架构/E6_插件生态与发布/插件兼容机械化/

# ⑦ 收尾（禁跳）
npm run check
```

## 七、与其它格的关系

| 格 | 关系 |
|:--|:--|
| 格 1 `E6#114` | **硬前置**：搬运它的口径。 |
| 格 2 `E6#115` | **硬前置**：作者侧的「只加不删」若要做，形状取自它。 |
| 格 3 `E6#116` | **条件前置**：`retired[]` 是否下发决定 §二第三步做不做。 |
| 格 7 `E6#120`（[08](08-任务-通用悬停提示卡.md)） | **下游＝本批最后一棒**：出的**全批收口报告**吃本格 §8.7 的素材；本格的 SDK / docs 版本与 18 仓读数要写清楚供它汇总。⚠️ 它**不回改本格任何产物**。 |
| 格 4 / 格 5 | **无关**（读数与显示不下发；作者看的是自己仓里的红黄）。 |
| E6#112 | **形状先例**：SDK 腿 ＋ 锚词 ＋ 铺开边界的三件套照它做。 |
| E6#107 | **形状先例**：lockfile 同步门禁（本格的 18 仓 lock 升级要过它）。 |

---

<!-- §八 起 = 本格执行记录 -->

## 八、执行记录（格 6 · 2026-09-19 填写）

### 8.1 落地表（`file:line` 可点验）

**新增**：

| 文件 | 是什么 |
|:--|:--|
| `packages/plugin-sdk/src/eslint/checks/dangling-names.ts` | **悬空名腿**——格 1 口径的作者侧移植（`runDanglingNameCheck` ＋ `loadHostCssNames`）；JS 词法段（scanCode/shoutSites）与格 4 运行时腿逐行同构（jscpd 不扫 packages/，口径由 `锚⑩` ＋ 对账先例钉住） |
| `packages/plugin-sdk/src/eslint/checks/retired-names.ts` | **退役名提示腿**（`runRetiredNameHint`）——`retired[]` 整词匹配，🟡 只提示、⛔ 永不拒绝（hints 不进 violations，CI 严格腿看不见） |
| `packages/plugin-sdk/src/eslint/checks/dangling-names.test.ts` | 单测 **17 例**（正控 3 ＋ 负控 A–G ＋ 保底 2 ＋ `锚⑩` ＋ 退役提示 4）——常驻壳 check（vitest `packages/**/*.test.ts`） |
| `packages/plugin-sdk/schemas/host-css-names.json` | **宿主 CSS 定义集随包下发**（classes 1076 ＋ keyframes 9 · 生成物勿手改） |

**改动**：

| 文件 | 改了什么 |
|:--|:--|
| `packages/plugin-sdk/src/eslint/lint.ts` | 命名空间腿并**第六条判据**（同点不重复报，以先出的为准）＋ `danglingNameCounts` / `retiredHints` / `retiredLedger` 报告字段 ＋ 打印两行 |
| `packages/plugin-sdk/src/eslint/index.ts` | 导出 `runDanglingNameCheck` / `runRetiredNameHint` 等六组 |
| `scripts/gen-host-css-manifest.mjs` | **同笔产出第三份投影**（SDK JSON）；`--check` / `--self-test` 两条都查——两份口径构造上不会分叉 |
| `scripts/plugin-dangling-name-audit.mjs` | selfTest 加 **`锚⑩`**（6 句锚词断言，14 例全绿） |
| `packages/plugin-sdk/package.json` | **0.1.39 → 0.1.40**（作者侧新面 ⇒ PATCH） |
| `docs/03-插件制造/16-命名规范.md` ＋ `docs/03-plugin-authoring/16-naming-conventions.md` | §7.5 第 2 条改句（「今天还没有」→「0.1.40 起你能跑出提示」——🔴 交接点②，作者面不成假话）＋ **新 §7.6**（悬空名自检的用法/修法/误报控/绕行/未核验保底），中英同笔 |
| `docs/03-插件制造/05-插件UI写法规约.md` §12.4 ＋ `docs/03-plugin-authoring/05-ui-conventions.md` §12.4 | 尾部各加一行指针（「这把尺子你也有」），中英同笔 |
| `packages/plugin-docs/**`（重生成 47 文件）· `packages/plugin-docs/package.json`（**0.1.20 → 0.1.21**）· `package-lock.json` | 作者面文档变 ⇒ docs:build ＋ PATCH ＋ lock 同步（`check-lockfile-sync` 绿） |

### 8.2 锚词（口径同源的机械证据）

**`锚⑩` = 6 句锚词**（壳尺子 `scripts/plugin-dangling-name-audit.mjs` 与 SDK 腿 `dangling-names.ts` **逐字同源**；断言位置两处——① 尺子 selfTest（`npm run audit:plugin-dangling:selftest`，14/14 含 `锚⑩`）② SDK 单测 `dangling-names.test.ts`（**常驻壳 check** 的 vitest，锚词漂移当场红，不必等 selftest））：

1. `只判「**不属于它自己**的名字」`（判定对象）
2. `在「自身定义集 ∪ 当前宿主定义集」里都没有`（悬空定义）
3. `动态拼接一律**跳过并计数**`（误报控①）
4. `（自有命名空间 / DOM 钩子 / 第三方内联）`（误报控②——非 `ldk-` 不计悬空）
5. `只被包内自己满足`（borrowedLdk——自定 `ldk-*` 归前缀腿判红）
6. `` `animation:` / `animation-name:` 引用的**关键帧名** ``（关键帧通道——抽取本身另由既有 `锚⑨` 钉住）

⛔ 本腿**没有另写一套正则**：CSS 侧抽取（`bareClassDefinitions` / `keyframeDefinitions` / `animationRefs`）直接复用 SDK `css-selectors.ts`（`锚⑧`/`锚⑨` 已钉）；JS 侧词法是格 1 尺子 / 格 4 运行时腿**同一段代码的第三份拷贝**（跨包无法 import——先例同款），由 `锚⑩` ＋ 格 4 的随包对账测试钉口径。

### 8.3 18 仓逐仓读数与 lock 状态（**铺开边界**）

**真跑读数**（`npx tsx scratch/dangling-18repo.mjs`——scratch 一次性驱动不进 git，照 E6#112 `scratch/kf-18repo.mjs` 先例；宿主定义集 classes 1076 / keyframes 9）：

| 仓 | 悬空 | ldk喊 | kf引 | 模板跳 | 动态跳 | 未判 | 借用 | 退役提示 |
|:--|:--:|:--:|:--:|:--:|:--:|:--:|:--:|:--:|
| editor / file-tree / lang-defaults / lang-test-ja / marketplace / python / serial-monitor / settings / theme-×9（**18 仓**） | **0** | 0/0/0/0/0/0/8/0/其余 0 | 0/0/0/0/1/0/2/0/其余 0 | 0/6/0/0/4/0/12/14/其余 0 | 2/2/0/0/9/0/2/0/其余 0 | 4/0/0/0/2/0/5/1/其余 0 | 0 | **0** |

**合计：18 仓 · 悬空 0 · 退役提示 0**——与格 1 产物域基线**对得上**（判据③）：插件产物侧悬空也是 0；格 1 报的合计 2 处 / 1 名（`ldk-sle-row--locked`）是**壳仓共享组件源码**的死引用，不在任何插件仓的作者侧射程内（它由格 1 尺子挂给壳维护者，已另行处置）。

**真仓负控**（照 E6#112 先例）：复制 `serial-monitor` 全仓 ＋ 只加一个 `ldk-ghost-negative-control` 引用 ⇒ SDK 腿报 `src/NegativeControl.tsx:1` 悬空 1 / 违规 1（**真红实证**；跑完即删，未动真仓）。

**lock 状态（🔴 铺开边界如实，不许写成「已铺开」）**：18 仓 `package.json` 声明仍 `^0.1.19`、**lockfile 全部 resolved `0.1.34`**（与 06 号档进场读数一致，逐仓复查过）。**本格零仓改动**（判据⑥ `git status` 只有本格档／清单／交接——插件仓一个文件都没碰）：SDK **0.1.40 尚未发布**（🔴 用户 2026-09-19 拍板：发版等 6、7 做完一起发），升 lock 会让 `npm ci` 当场找不到包 ⇒ **18 仓接腿 = SDK 发版后各自下次发版时升 lock**（CI 模板零改动——腿并进既有 `check-css-namespace` 严格腿，`ci-verify` 的 `strictLegs` 自动接管，fail-closed 语义未动）。

### 8.4 发版实录（SDK / docs / 基线）

- `@linkdesk/plugin-sdk` **0.1.39 → 0.1.40**（作者侧新面 ⇒ PATCH；🔴 **未 publish**——等用户统一点头）。
- `@linkdesk/plugin-docs` **0.1.20 → 0.1.21**（作者面文档 §7.5/§7.6/§12.4 变 ⇒ docs:build 重生成 47 文件；🔴 **未 publish**）。
- **黄灯合计（全部攒着，发版链一次走）**：SDK 0.1.40 · plugin-docs 0.1.19/0.1.20/0.1.21 · marketplace 1.0.36（格 5，`c5e8b8f` 未推送）。发版链 = publish → 官方目录收录 → `sync:bundled --latest`（**要代理**）→ 壳 tag；`check:npm-release` 基线发版后重记。
- **壳仓零代码改动** ⇒ 版本恒 **0.2.12**、无 CHANGELOG 变更；`@linkdesk/contracts` 本格零改动。

### 8.5 残余边界（诚实清单）

1. **18 仓 lock 未升**（0.1.34）：SDK 腿在官方仓 CI **尚未真生效**——今天的 18 仓读数是本地驱动跑的，CI 覆盖要等发版＋各自升 lock（见 8.3）。
2. **源码域 ≠ 产物域**：SDK 腿扫作者**源码**；产物里 inline 进来的共享组件名字（如 `settings` 产物里那条 `ldk-sle-row--locked`）作者侧看不见——产物轴仍由格 1 尺子（维护者跑）＋格 4 运行时腿（装机读数）管，**三份各守一段，不互替**。
3. **静态尺子的固有射程外**：动态拼接跳过并计数（报告里如实打印），不猜——这也是判定**可以判红**而不假红的原因（壳尺子因此只报不拦，作者侧自己的仓红得起）。
4. **退役提示按整词文本匹配**：注释里的出现也提示（多报无害，它不拦）；非代码面（用户 `settings.json`）不在作者仓 ⇒ 天然射程外。
5. **SDK / docs 均未发布**：作者实际拿到 0.1.40 要等发版链走完（全部等用户点头）；在那之前 `linkdesk-plugin-sdk lint` 线上跑的还是旧腿集。
6. **双零对账的含义**：判据③「对得上」今天 = 基线本来就是 0——它证的是**没有新的假红/漏报**，不是「判据被真悬空验证过」；真悬空的验证靠真仓负控（真红 1 处，见 8.3）。

### 8.6 `npm run check` 末尾三行

```
Test Files  178 passed (178)
     Tests  2502 passed (2502)
   Start at 01:03:43 …  Duration  36.29s
```

EXIT=0（较格 5 的 177 文件 / 2485 例 ＋1 文件 ＋17 例 = 本格 `dangling-names.test.ts`）。

### 8.7 交给格 7 的收口素材（六条判据读数 ＋ 残余边界 ＋ 大白话四问草稿——**报告本体由格 7 出**）

**判据六条逐条读数**（格 7 直接汇总，⛔ 不重跑）：

1. **悬空名**：`npm run audit:plugin-dangling` 基线 = 产物 24 个（随包 6 ＋ 官方 18）· 合计悬空 **2 处 / 1 名**（`ldk-sle-row--locked`——壳仓 `StringListEditor.tsx:102` 死引用，挂壳维护者）；插件侧 0；误报控 `editor` 74 个 Monaco 裸名 0 报（[01 §八](01-任务-悬空名核验.md)）。
2. **只加不删**：面快照进 `npm run check`（快照 **1057 条面**——格 4 additive +2）· 负控真造红 1 次（[02 §八](02-任务-只加不删门禁.md)）。
3. **退役账**：`retired[]` **1 条**（`app.themeColorMode`）过对账（`check-retired-ledger` 7 例）· 「有登记即放行」唯一出口 = `check-api-surface-additive.mjs` · 🟡 作者侧提示已落地（本格，只提示永不拒绝）（[03 §八](03-任务-退役登记.md)）。
4. **读数**：壳唯一只读面 `plugins.getCompatibility` 五态；真样本 `drifted`=settings / `compatible`=其余 5 只；`incompatible`/`current` 桩（标注）；运行时腿与格 1 尺子随包 6 zip 对账逐只相等（对账测试常驻 check）（[04 §八](04-任务-兼容读数.md)）。
5. **市场显示**：`marketplace` 1.0.36（`c5e8b8f`，未推送）详情页信息栏「兼容性」组**全页唯一状态标**＋悬停卡；用户点头 ✅ 2026-09-18（五词照抄 00 §〇d）；🔴 **实机四态截图未做**（发版链未走完——按 [05 §9.5](05-任务-市场显示状态.md) 先例如实登记「待发版后补」）；禁词 grep 唯一命中 =「这不是评分」句（点头范围内逐字原文，豁免登记 [05 §9.3](05-任务-市场显示状态.md)）。
6. **铺开**：SDK **0.1.40** / docs **0.1.21**（均 bump 未发）；SDK 腿单测 17 例绿 ＋ `锚⑩` 同源 ＋ 18 仓本地真跑悬空 0 ＋ 真仓负控真红 1；🔴 **铺开边界 = 18 仓 lock 仍 0.1.34，等发版后各自升 lock 接腿**（CI 模板零改动）——⛔ 不许写成「已铺开」。
7. **共享件（第 7 格本体，非第七判据）**：待格 7 自报。

**残余边界**：00 总纲 §六 八条原样有效（一条不许省），另加本档 §8.5 六条（尤其 lock 未升 / 源码域≠产物域 / SDK 未发）。

**大白话四问（草稿，格 7 润色）**：
- **做了什么**：把「旧插件在新版本上还能不能跑」从靠人记变成了六件常驻机械件——尺子（悬空审计）＋门禁（面快照只加不删）＋账（退役登记）＋读数（兼容五态）＋显示（市场状态标）＋铺开（作者在自己仓就能跑同一条判据）；另加一件看得见的共享件（格 7）。
- **读数是什么**：今天 24 个产物悬空基线 = 2 处 / 1 名（全是壳自己的死引用，插件 0）；18 仓作者侧真跑悬空 0；面快照 1057 条进 check；退役账 1 条；市场显示已按设计图落地（用户点头）。
- **什么没做**：**发版链整条没走**（SDK 0.1.40 / docs 0.1.21 / marketplace 1.0.36 全部 bump 未发、18 仓 lock 未升、种子未同步）——等用户统一点头；实机四态截图同因未做；第三方作者的私仓我们看不见（只有他自己能跑——这正是铺开的意义）。
- **下一步最难的一条**：走发版链（publish → 目录收录 → 种子同步（要代理）→ 壳 tag）＋ 18 仓 lock 升级——机械化判据要**真到作者手里**才算数，现在它们还都在本机。

### 8.8 交接（队列表整张复制、自己那格改 ✅；⚠️ **后面还有格 7** ⇒ 剩余 ≠ 无）

- 见 [交接.md](交接.md) 顶部「第 6 格」段（队列表已整张复制、格 6 改 ✅）。
- **提交 sha = `3c4fb68b1`**（24 个文件 · **未推送**；上一棒格 5 = 插件仓 `c5e8b8f` ＋ 壳仓 `7cd2af652`/`4f23a675f`）。
