# 任务 `#109k-a/b/c`：**收口批**——遗留账三笔 ＋ 系列收口（第 1.18 / 1.19 / 1.20 轮）

> **一句话**：本系列最后三棒——**① 把死规则裁决掉**（1.18：`.divider` 宿主自己的死规则 ＋ 宿主 `.search-input` 残规则 ＋ **1.14④ 新捡的 settings `text-muted` 死类** ＋ **1.14⑤ 新捡的两条**（宿主带连字符的全局工具类 `.setting-group`/`.setting-group-title`——**壳门禁判据③ 的启发式盲区**；serial-monitor 自己的 `serial-monitor-select-input` 死类），**五条一并裁**）· **② 把作者面 §11 那两处裸坐标清掉**（1.10 留的账）· **③ 四件套终态复核并宣布系列完成**。
> **本档怎么用**：三格各自独立，**每格一个 AI**（读自己那一节；1.20 必须最后做）。
> **前置**：1.18 只依赖 1.16 ✅；1.19 只依赖 1.10 ✅；**1.20 依赖其余全部**（含 [件 2 落地 ⑧](18-任务-件2落地⑧-发版与文档收口.md) 与 [1.17](13-任务-宿主input裁决与落地.md)）。
>
> **非新能力声明（`check-design-flow` §8.4 ③）：** 本档是**遗留账处置 ＋ 终态复核**，规则对象是既有类名表、既有文档与既有门禁，不新增能力。⇒ **无需新能力设计前置**。

---

## 〇、拍板记录（2026-09-16 用户）——本批五条 ＋ 一条改判

> **怎么发生的**：用户要求「把选项翻成大白话，我自己选」⇒ 本条逐条给事实与代价后，用户裁决。**原话**：「同意」（＝追认上一轮列出的全部倾向）＋「**选 A**」＋「最好给它开个新任务，让单独 ai 去做」＋「**最后一个任务就是 1.20，不管怎么样反正这个任务必须有**」。
> **性质**：①–⑥ 是**用户追认**（倾向由维护者提出、用户以「同意」明确认可；每条改动面都很小、可逆）；⑦ 是**用户自己选的方向**（原话「选 A」）。

| # | 事 | 裁决 | 落地归 |
|:--:|:--|:--|:--|
| ① | 宿主 `.ldk-divider`（零采用的基础细横线） | **删** | **本格**（1.18） |
| ② | 宿主 `.search-input:focus-visible` **半条**残规则 | **删**，**搭 1.17 的壳改同一笔** | [1.17](13-任务-宿主input裁决与落地.md)（本格记账，视为已收口） |
| ③ | 宿主 `.setting-group-title`（零消费方） | **删** | **本格** |
| ④ | 宿主 `.setting-group`（**有**消费方） | **前缀化成 `.ldk-setting-group`** | [1.17](13-任务-宿主input裁决与落地.md)（与 `.input` 一次裁决） |
| ⑤ | `settings` 的 `settings-text-muted`（空转类） | **摘掉 className**（**不**补规则、**不**升格） | **本格**记账，落在 settings 下一次插件发版里 |
| ⑥ | `serial-monitor` 的 `serial-monitor-select-input`（零渲染点） | **删**，**搭 1.17 的 serial-monitor 发版的车** | 1.17（本格记账） |
| ⑦ | 门禁 `scripts/check-css-namespace.mjs` 判据③ 那句 `if (name.includes("-")) continue;` | **不删**。用户对人判方向的裁决 = **「宿主自己定义的类名一律以 `ldk-` 开头」** | **新开 [第 1.21 轮 · `#109l`](21-任务-宿主类名统一前缀.md)** |
| ⑧ | `settings` 的 `text-muted` **视觉**（弱化未生效） | 同 ⑤ —— **不动视觉**（硬约束 16：要动先走设计 skill；本次不产生视觉改动） | — |

**支撑读数（本格实测，可复跑）**

- ① 的「零采用」证据：全仓（`src/` `electron/` `plugins/` `packages/`）搜 `.ldk-divider` **只有它自己那一行定义**；仓里**没有任何 `<hr>` 元素**；今天所有细横线都是各组件自己用 `border-top/bottom: 1px solid var(--separator)` 画的（发行说明页 / 插件详情页 / 欢迎页 / 快速选择框 / 下拉框 / 色板）。它的来历 = Tauri 期 [P1 实施记录](../../../../01-Tauri_P1至P5.5/P1-架构设计/P1-实施记录.md) 的一张**基础元素样式清单**里、紧挨 `.input` 的那一节「`.divider` — 分隔线」（3 行 CSS：1px 高 ＋ `--separator` ＋ 上下 4px）。**⇒ 它在界面上不存在，用户不可能指认出来**（用户曾两次把它认成「区域之间的拖动线 `.zone-resize-handle`」与「标签页内分屏线 `.split-handle`」，两者都活着、各有名字）。
- ⑤ 的「走不到」证据：`settings` 的兜底分支（`renderControl.tsx:264` 的 `default:`）只在配置项的 `type` 不属于**文字/数字/开关/对象/数组**五种时触发；而 `plugin.schema.json` 里 `type` 是**硬枚举**（正好这五种），实测 **18 只插件仓 ＋ 壳内夹具共 47 个配置项、壳内置设置 27 个声明，全数合规 ⇒ 今天零命中**。⚠️ 但宿主装载**不校验**清单（校验发生在作者用 SDK 构建时）⇒ 手写清单写错类型仍会走到那里。⇒ 结论：这是**给写错类型的插件兜底的容错路径**，为它补样式要付「一次插件发版 ＋ 一次设计流程」而用户看不见，故取「摘掉名字」。
- ⑦ 的「不能照删」证据（**只读探针**，`scratch/narrow-probe-109k-a.mjs` 复跑）：把那句跳过删掉后，判据③ **当场照出 271 处 / 260 个类名 / 18 个文件**，其中 **255 个非 `ldk-`**——绝大多数是**宿主池内界面自己的名字**（发行说明页 42 · 状态栏 37 · 插件详情 30 · 快速选择框 22 · 欢迎页 18 …），**从来不是给作者用的**；而且**已经带 `ldk-` 的那 5 个也被照出来了**，与「带前缀 ＝ 自带命名空间、不需登记」这条既有规矩**正好相反**。⇒ 让这个结果可接受只能靠白名单/基线（**本系列禁区**）⇒ **改判为结构性治本：宿主类名一律 `ldk-`**（新格 1.21）。

---

## 第 1.18 轮 · `#109k-a`：`.divider` 零渲染点死规则的裁决

**事实（1.16 实测，复跑一遍确认）**：
- `.ldk-divider` 在 `src/index.css` **有一条规则**，但**全仓零消费方**（零渲染点、零 CSS 后代引用），插件仓 21 只也零消费。
- `git log -S"divider" -- src/index.css` **只落到根提交**（2026-09-04 仓库结构上移那笔）——来源是 Tauri 期 [P1-实施记录](../../../../01-Tauri_P1至P5.5/P1-架构设计/P1-实施记录.md) 的「`.divider` — 分隔线」设计稿：**线画了、渲染点从没建**。
- 它现在**挂在保留名表里**（`reserved-class-names.json` 的 `classes.host`）⇒ 作者面 §12 告诉作者「`.ldk-*` 是宿主的」，而实际上这条**根本没有元素会用**。

**要做（一次裁决，不许静默）**：
| 选项 | 动作 | 代价 |
|:--|:--|:--|
| **(a) 删（推荐）** | 删 `src/index.css` 的规则 ＋ 从登记表 `classes.host` 摘掉 ＋ 两棵树 §12 同步 | **必须同笔改三处**（登记表 + 中英两棵树）——`check-reserved-names-doc-sync` 与 `check-css-namespace` 判据⑤ 会同时盯着；漏一处即红 |
| (b) 留 | 在登记表 `why` 里写明「设计稿遗留、暂零消费方」 | 保留名表继续挂一条死名字（作者照表避让一个不存在的名字） |

> 🔴 **裁决归用户一句话**（1.16 立的规矩：**删死规则要一次独立裁决，别夹在别的轮次里做**）——开工先问，拿到答复再动手；**拿到答复前不许改**。

**判据**：`check-css-namespace` 绿（含判据⑤ 反向核对 —— **删了规则不摘登记表 = 红**）· `check-reserved-names-doc-sync` 绿 · `npm run check` EXIT=0 · 全仓（含 21 仓）grep `.divider` / `divider` **零类名残留**。

### 🔴 同格的**第二条宿主死规则**（2026-09-15 由第 1.14③ 轮捡到，交本格与 [1.17](13-任务-宿主input裁决与落地.md) 二者之一收口）

> **它是 `.divider` 的同形态，但成因相反**：`.divider` 是**宿主自己的**规则没人用；这一条是**宿主去用插件的**类名，而那个类名已经（或即将）不复存在。

**事实（1.14③ 实测）**：壳 `src/index.css:274` 是

```css
.input:focus-visible,
.search-input:focus-visible {
  outline: 2px solid var(--accent);
  outline-offset: 1px;
}
```

- `.search-input` **不是宿主保留名**（`classes.host` 里只有 `.input`）——它是**插件自己定义的类**，`file-tree` 与 `serial-monitor` 两家各一份（正是 [1.13](14-件2评估交付.md) 记的那起活体撞车）。
- **1.14③ 之后**：`file-tree` 改名为 `file-tree-search-input` ⇒ 实测 `matches('.search-input')` = **false**；环本身还在（全局 `*:focus-visible` 兜底 2px accent solid），**唯一 delta = `outline-offset` 1px → 2px**。
- **1.14⑤ 之后**（`serial-monitor` 也改名）：这一行**零消费方** ⇒ 与 `.ldk-divider` 完全同形态。
- **先例**：同一选择器列表里的第三个成员 `.palette-input` 在 `E5.7#18` 就是作为旧浮层清理**一起删掉**的 ⇒ 「删」在本仓有先例。

**要做（与 `.divider` 一并裁，或由 1.17 选 (b) 时顺带做掉）**：

| 选项 | 动作 | 代价 |
|:--|:--|:--|
| **(a) 删（推荐）** | 摘掉 `.search-input:focus-visible` 那半（整条规则若只剩 `.input`，则 `.input` 那半仍保留——**`.input` 的存废归 [1.17](13-任务-宿主input裁决与落地.md)**） | 一行；**注意别把 `.input` 那半误删** |
| (b) 留 | 在规则旁注明「残规则、零消费方」 | 留一条永久骗人的联动 |

> ⚠️ **不要把它升格为公共面**：它没有登记、没有作者面契约、没人申请过——**不是** `.input` 那种「宿主给作者的工具类」。
> ⚠️ **1.17 若选 (b) 前缀化**（把 `.input` 改成 `.ldk-input`）：最自然是在那次壳改里**直接删掉这半**（那时两家插件都已是自有前缀、不需宿主代管焦点环）；此时本格这条**视为已收口**，别再重复裁。
> ⚠️ 判据随 1.18 那条一并写：删完 `grep -n "search-input" src/index.css` **应归零**；`check-css-namespace` / `check-reserved-names-doc-sync` 仍应绿（它**不在**保留名表里 ⇒ 摘它**不动**三份登记账）。

### 🔴 同格的**第三条死规则**（2026-09-15 由第 1.14④ 轮捡到）：settings 的 `text-muted` **死类**

> **这次是「插件去用一个并不存在的宿主类」**——与上两条成因又不同：不是宿主没人用，也不是宿主借插件的名，而是**借用方以为宿主有这个类，其实宿主只有同名的 CSS 变量**。

**事实（1.14④ 实测，读数都在）**：

| 读数 | 值 | 怎么测的 |
|:--|:--|:--|
| 宿主有没有 `.text-muted` **类** | **没有** | `grep -rn "\.text-muted" --include="*.css" --include="*.ts(x)" src/ electron/` ⇒ 命中全是 `var(--text-muted)`（**变量**），无一选择器 |
| 池文档里匹配 `.text-muted` 的规则数 | **0** | 实机池里遍历 `document.styleSheets` 逐条 `selectorText` 匹配（隔离 profile ＋ CDP） |
| 谁在渲染它 | **只有 settings** 一处 | 18 只插件仓全量 grep `className="text-muted"` ⇒ 仅 `settings/src/views/SettingsView/renderControl.tsx:264`（`default:` 分支，渲染**未知类型**的配置值） |
| 参照：`.input`（**真**宿主保留名） | 池里 **36 条**规则 | 同上遍历；且它在登记表 `classes.host` 里 |

**后果**：`renderControl` 的 `default` 分支（配置项类型不在开关/滑杆/色板/对象/分段里时）把值渲染成 `<span className="settings-text-muted">`——**拿不到任何弱化色**，一直是**普通文字色**。⚠️ 这是**改名前就有的老账**（本轮只把类名归位成 `settings-text-muted`，**没有动视觉**——硬约束 16 要求动视觉前先走设计 skill）。

**要做（与 `.divider` / 宿主 `.search-input` 一并裁）**：

| 选项 | 动作 | 代价 |
|:--|:--|:--|
| **(a) 补一条自有规则（推荐）** | 在 settings 自己的 CSS 里加 `.settings-text-muted { color: var(--text-muted) }` | 一行 ＋ 一次插件 PATCH；⚠️ **属视觉改动** ⇒ 按硬约束 16 **先调设计 skill** 再落 |
| (b) 删掉这个类 | 摘 `className`，值就按普通文字色渲染 | 零视觉变化，但**放弃了原意**（弱化未知类型值）；需说明为何不再弱化 |
| (c) 升格为宿主保留名 | 登记进 `classes.host` ＋ 在 `index.css` 定义 | 要说服「这是给所有插件的通用工具类」；当前**只有一家在用** ⇒ 理由不足 |

> ⚠️ **别顺手改**：这条归 **1.18**（本格是死规则裁决的唯一落点）。1.14④ 只做了「名字归位」并把它记在这里。

### 🔴 同格的**第四条**（2026-09-15 由第 1.14⑤ 轮捡到）：宿主**带连字符**的全局工具类 —— **壳体门禁的盲区**（两条）

> **这条与上面三条都不同**：它**不是**「谁死了」，而是**「门禁根本没看见它」**——`.setting-group` 有活消费方、`.setting-group-title` 零消费方，但**两条都不在 `classes.host` 登记表里，也从来没有任何腿拦过它们**。

**根因（读代码即知，一行）**：壳仓 `scripts/check-css-namespace.mjs` **判据③** 收集宿主全局工具类时有一句 `if (name.includes("-")) continue;` —— 「裸名」被启发式定义成**不含连字符**的名字（件 1 那批 `.badge` / `.toggle` 正是这一类）。⇒ **带连字符的宿主全局工具类（`.setting-group` / `.setting-group-title`）从出生起就在射程外**：既不必登记，改了/加新的也不红。（同一条启发式在 1.14① 的插件侧新腿里**不存在**——新腿按「无祖先的类名主体」判裸定义，所以插件侧看得见它们。）

**实测读数（1.14⑤）**：

| 名 | 宿主定义 | 消费方 | 性质 |
|:--|:--|:--|:--|
| `.setting-group` | `src/index.css:228`（bg `--bg-card` / `--radius-md` / `padding 12px` / `margin-bottom 8px`） | **只有 serial-monitor**（4 个设置组 div） | 插件**消费 + 调优**（`padding: 4px 12px`）——`.input` 的同族，**第二条** |
| `.setting-group-title` | `src/index.css:235` | **零消费方**（壳仓 ＋ 18 仓 ＋ 池文档实机全扫） | **死规则**——与 `.divider` 同形态 |

> 🔴 **serial-monitor 侧已在 1.14⑤ 收干净**：插件不再裸定义这个宿主名（调优移到自有类 `serial-monitor-setting-group`，宿主类照旧渲染）⇒ **剩下的全是宿主侧的问题**：`.setting-group` 该不该**登记为公共面**（像 `.input` 那样），还是随**件 4 下半**一起前缀化？`.setting-group-title` 直接**删**还是**留**？
> ⚠️ **别顺手改**（1.14⑤ 只做了「插件侧不再裸定义」这一半）：宿主侧的处置归 **1.17（件 4 下半，与 `.input` 同一次裁决）**或本格——**两处都在等一次明确的裁决**，不是缺陷修复。
> ⚠️ **判据**：无论走哪条，`check-css-namespace` 判据③ 那句 `if (name.includes("-")) continue` **本身要不要收窄是一件事**（收窄 = 一次门禁升级，会当场照出更多存量）——建议**与件 4 的裁决同笔决定**，别在这个格子里偷偷改门禁。

### 🔴 同格的**第五条**（2026-09-15 由第 1.14⑤ 轮捡到）：serial-monitor 的 `serial-monitor-select-input` **死类**

> **形态最轻**：插件自己定义、**零渲染点**——与 `.divider` 完全同形，只是这里是插件自有前缀（不跨方、不撞名）。

| 读数 | 值 |
|:--|:--|
| 全仓命中 | **1 处 = `src/styles/SerialMonitorSidebar.css` 里那条定义本身**（18 仓 ＋ 池文档实机全扫无渲染点） |
| 定义体 | `min-width: 80px; max-width: 100%; flex: 1; cursor: pointer; appearance: auto;`（一段通用「选择输入」样式） |
| 1.14⑤ 怎么处置的 | **照纪律只加前缀、不删**（怕顺手删掉后续有意的复用；且删它要一次独立裁决） |

> ⚠️ **归宿**：与上面四条**一并裁**（删 / 留 ＋ 理由），处置落在 **serial-monitor 下一次改动**里（一行 CSS ＋ 一次 PATCH；或搭 1.17 的车）。
> ⚠️ **注意**：它不是宿主规则 ⇒ **不属于 `src/index.css` 的裁决面**；本格若只裁宿主那几条，它要么同笔裁掉、要么明确再挂一棒——**别让它掉在缝里**。

### ⚠️ 一条**不属于 1.18** 的相邻账（1.14④ 记账、**没动**）：壳仓 `string-list-editor.css` 的 3 处注释点名 settings 旧裸名

- **位置**：`src/components/shared/string-list-editor/StringListEditor.css` —— 文件头注（「token 对齐 settings `.object-editor-*` / 壳 `.input` 惯例」）＋ 两处行内注（「对齐 object-editor 输入行高度」「对齐 object-editor-delete」）。
- **为什么没动**：该目录是 **`@linkdesk/ui` 的单一源码**（`packages/linkdesk-ui/package.json` 原文：「壳 `src/components/shared` 单一源码经构建编译」）⇒ 改它就是动作者轴 ⇒ **禁区 1「不许改 `@linkdesk/ui`」**。而且 1.14④ 的规格变化（旧名 → `settings-object-editor-*`）只让这 3 处**散文陈旧**、**零渲染影响**。
- **归宿**：**ui 轴的下一次发版**或 1.19（作者面/文档面清理）。⚠️ **不要**把它塞进 1.18（1.18 是宿主 `src/index.css` 的死规则裁决，两码事）。

---

## 第 1.19 轮 · `#109k-b`：作者面 §11 / §11.5 的裸坐标清理

**事实（1.10 的 F5 留的账，1.13 复核仍在）**：
- `docs/03-插件制造/05-插件UI写法规约.md` 的 **§11.5 表行与 §11 正文** 里各有一处 `54b` / `54c`（英文树同页同样两处）——它们是**内部任务号，但没有 `#`**。
- `scripts/check-author-docs-symbols.mjs` 的尺子**只认 `E6#57` / `E5.8#36.5` 这类带 `#` 的形态** ⇒ **扫不到它们**。
- 对陌生的第三方作者的 AI：**这是无法解析的坐标**（「见 54b」= 什么都没说）。

**要做**：
1. 两处改成**自解释的表述**（把「54b 那条」写成「当你想 X 时，做 Y」这类可执行的话），**中英两棵树同笔**（`check-author-docs-bilingual` 只管篇目 ⇒ 内容等价靠人）。
2. 决定**要不要给尺子加一条判据**（裸 `NNx` / `NN#` 形态）——若要加，**必须带正/负控且不误伤正文数字**（这条口径要写进脚本文件头）；若不加，在交接段写明「为什么容忍」（现状：两处 + 无扩展趋势）。
3. 顺手 grep 一遍**全作者面**还有没有同类裸坐标（`\b\d+[a-z]\b` 形态），一次清干净。
4. 🔴 **本格同时认领 1.15 留下的一笔账：宿主关键帧表扩到作者面 ＋ 让门禁也守它。**
   - **事实**：`reserved-class-names.json` 里躺着 **8 个宿主关键帧**（`selectbox-in` / `drop-zone-in` / `dropdown-card-in` / `floating-panel-in` / `group-tab-enter` / `group-tab-exit` / `notif-icon-spin` / `notif-progress-scan`），而作者面 §12 **一个字都没提**；1.15 的对账门禁（`check-reserved-names-doc-sync`）**只守「保留名」那一列（类名）**，不守关键帧。
   - **为什么要做**：插件给自己写一个裸 `@keyframes selectbox-in` 会**静默撞掉共享组件的下拉入场动画**，而作者无从知道（这正是件 2 的 Q3 把关键帧纳入判据的同一理由——**插件侧由 SDK 腿管，宿主侧这份「哪些名字动不得」还没进作者面**）。
   - **要做**：① 作者面 §12 补一行/一节，列出这 8 个宿主关键帧（或一句「宿主关键帧也在保留范围内，附清单」），**中英两棵树同笔**；② 把 1.15 的门禁**扩到关键帧这一列**（照它现有的三份双向口径：登记表 ↔ 两棵树；**扩它要带正/负控**——改一个关键帧名 ⇒ 红，还原 ⇒ 绿）；③ 若判断「扩门禁」属另一件事，就**在交接段点名回队列**，别默默只做 ①（**作者面写了名字而机器不守 = 又造一份手抄表**，正是 1.15 花一轮修掉的那种）。

**判据**：`check-author-docs-symbols` / `-links` / `-bilingual` + `docs:check` 四绿 · 人工读一遍改动处上下文通顺 · 「全作者面裸坐标 = 0」读数（或如实写明剩余与理由）。

---

## 第 1.20 轮 · `#109k-c`：🔴 **系列收口**（必须最后做）

**这是一棒的"报完成"轮**，不是新工作——把四件套的终态**逐件给读数**，并把系列状态收成一句话。

### 一、逐件终态复核（**每件给「终态读数」，不是「做过了」**）

| 件 | 轮次 | 终态判据（逐条跑） |
|:--|:--|:--|
| **件 1 裸名归一** | 1.5–1.12 | `check-css-namespace`：共享组件裸定义 **0** · `@linkdesk/ui` 货架版本 **0.2.0** · 三只插件仓依赖 `^0.2.0` · 出厂种子含新名 |
| **件 2 插件前缀不变量** | 1.13–1.14⑧ | **全量审计 21 仓违规 0** · SDK 货架版本 = 新号（前缀判据在内，**真装一次验证它会红**）· 4 只插件仓版本 ＋ 目录 ＋ 种子对账 · 作者面 §12 写的是 `<pluginId>-` 规则（含关键帧与 `ldk-` 禁忌） |
| **件 3 保留名清单单一真相源** | 1.15 | `check-reserved-names-doc-sync` 绿（三份双向）· 作者面 §12 无手抄表（只剩机器守的那份） |
| **件 4 宿主工具类定格** | 1.16–1.17 | `classes.host` 终态（`.input` 的去向由 1.17 定）· `check-css-namespace` 宿主工具类读数 · 两棵树 §12 同步 |

### 二、把「系列状态」收成一句话（**三处同笔**）
1. 本目录 [00-整理档案.md](00-整理档案.md) **§九 实况**：从「件 1 收口终态」改成「**四件套全收口终态**」（四件各一行读数 ＋ 全部发版轴版本表）。
2. [E6-执行清单.md](../../E6-执行清单.md) **第 1 层头**的「轮次进度」行：把长串收成一句「**样式命名空间归一化四件套 ✅（1.5–1.20）**＋ 终态读数」，并把本批全部格子勾掉。
3. [CLAUDE.md](../../../../../CLAUDE.md) **硬约束 23**：终态措辞（规则句 + 机械兜底 + 作者面指针），**史料不删**。

### 三、收尾产出：**给用户的"系列完成"报告**（写进交接段顶部）
- 四件各一句终态 + 读数；**发版清单**（软件 / npm / 插件各发了什么版本）；**还欠什么**（若 1.18 选保留、或某处有黄灯未发，如实列出）。
- 🔴 **给下一个 AI 的话**：本系列到此结束 ⇒ 下一棒**回到 E6 主干**（`E6-执行清单.md` 的剩余层：**L6 安全加固** 等，去向由用户拍板），**不再有本系列的格子**。

---

## 判据（三格共用一张，收尾勾全套）

- [ ] 1.18：`.divider` 裁决已拿到并落地（或如实写明「用户选择保留 ＋ 理由」）；`check-css-namespace` + `check-reserved-names-doc-sync` 绿
- [x] 1.19：两处裸坐标已清（中英同笔）＋ 全作者面同类残留读数；**宿主关键帧表已进 §12（两棵树）＋ 1.15 的对账门禁已扩到关键帧列**（或如实写明「扩门禁回队列」＋ 理由）；作者面四门禁绿
  - ✅ **2026-09-16 完成（读数）**：① **两处 `54b`/`54c` 已清**（§11 正文 ＋ §11.5 表行，中英同笔改成人话）；② **顺带清净全作者面同类残留**——两棵树 **13 篇 / 56 处**（裸 `#` 坐标 **30** · 裸 `NNx` **14** · 裸 `NN.N` **4**（`22.6`）· **`I8-x` 8**），⇒ **全作者面裸坐标 = 0**；③ **尺子加了一条判据**（不是「裸 `NNx`」那条——见理由）：`check-author-docs-symbols` **判据② = 剥掉行内代码 / 栅栏代码块 / 链接目标后，正文里 `#`＋数字 ⇒ 红**，`--self-test` **10 例**（判据① 2 ＋ 判据② 正控 3 ＋ 不误伤负控 4）；**不加裸 `NNx` 判据的理由**（详案要求「若不加入写明为什么容忍」）：与正文里的**计量单位**完全同形（`2s` 轮询 / `10s` 超时，清账前后都真实存在），没有一条不误伤的正则，而本系列禁区「不引白名单/基线/棘轮」⇒ **人工清 + 如实记账**（今天 **0 处**）。**另测**：裸 `NN.N` 形态同样不可机械判定（`\b\d+\.\d+\b` 在两棵树有 **187 处命中、绝大多数是 `§3.1`/`11.2` 这类节号与 `0.06`/`25.3` 这类数值**）⇒ 一并容忍；④ **关键帧账收官**：两棵树 §12 新增「**保留的关键帧名**」表（8 行，登记表 `keyframes` 逐条）＋ 维护者注扩到两张表；`check-reserved-names-doc-sync` **扩到关键帧列**（与登记表 `keyframes` 双向 ＋ 中英集合相等 ＋ 该列混进 `.foo` ⇒ 红 ＋ 表找不到 ⇒ 红），`--self-test` **17 例**（正控 4 / 负控 13），真跑 = 裸名 **1** / `ldk-` **9** / **关键帧 8**；⑤ **真仓负控** `scratch/prove-109k-b.mjs` **14 绿 / 0 红**（含反向证明「行内代码里的颜色不误伤」；每条还原 sha256 逐字节相同）；⑥ **四道作者面门禁全绿**（46 篇零坐标 / 460 链接 ＋ 20 报备 / 中 23 英 23 / `docs:check` **47 文件**）· `npm run check` **EXIT=0**（156 文件 / **2,115 例**）。⚠️ **如实留账（超本轮范围的第六族）**：作者面还有「**单字母决策标签**」约 30 处（`D1`/`D2`/`D3`/`D5` · `N1`/`N2`/`N3`/`N6` · `G4` · `B54`/`B62`）——与正文正经缩写**同形**（`E3`/`E5`/`E6` 阶段 · `F2` 按键 · `CM6` 库 · `T1`/`T2` 篇目 · `V3-部件命名规范.md` 文件名），**无正则可分**，且清它需**逐句重写** ⇒ 属**内容改写**、不属剥坐标 ⇒ **点名回队列、建议单开一拍板格**（详见 [交接.md 顶部](交接.md) 该轮「新事实 3」）。
- [ ] 1.20：四件终态读数逐条跑出（上表 4 行）；三处状态同笔更新；`npm run check` **EXIT=0**
- [ ] 三格各自：交接.md 顶部追加（**并把剩余队列里自己那一格划掉**）＋ 清单格子与判据框勾掉

## 禁区

1. **1.18 不许在没有用户答复时动手删**（删死规则要独立裁决）。
2. **1.19 不许只改中文树**；**不许在作者面留下新的无法解析的坐标**。
3. **1.20 不许"顺带补做"前面任何没做的格**——发现缺漏 ⇒ **在交接段点名，交回队列**（它是复核轮，不是救火轮）。
4. **三格都不许改视觉/语义**、不许改 `@linkdesk/ui`、不碰插件仓源码。
5. **不许把本批并进别的轮次**（遗留账的价值就在于「一次一件、账清即勾」）。
