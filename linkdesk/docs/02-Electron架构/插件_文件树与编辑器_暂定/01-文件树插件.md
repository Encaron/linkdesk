# 文件树插件

> 2026-07-24。从旧 P7b 迁移。**第一批消费者插件——验证 E2d 侧栏扩展位 + E2c FileService。**
> 依赖 E3 架构完工。纯插件实现——不碰壳代码。

---

## 一、定位

对标 VS Code Explorer。文件树注册为系统视图插件，和终端插件完全相同的注册路径：

```
plugins/file-tree/plugin.json → {"name":"文件树","sidebarRole":"persistent","sidebar":{"component":"sidebar.tsx"}}
  → viewRegistry 注册 → IconBar 出现 📁 图标
    → 单击 📁 → 侧栏 persistent 区域显示文件树
    → 双击 📁 → 文件树变成独立标签页
```

---

## 二、任务清单

### 任务 1：文件树视图（~200 行）

```
plugins/file-tree/
├── plugin.json          → sidebarRole: "persistent"
├── sidebar.tsx          → 树组件 + 文件列表
├── FileTreeView.tsx     → 标签页模式
└── index.tsx            → 导出
```

**功能：**
- 树形目录浏览：`window.linkdesk.filesystem.listDir()` → 递归渲染
- 点击打开文件 → 关联编辑器插件
- 右键菜单：打开/打开方式…/剪切/复制/粘贴/重命名/删除/新建文件/新建文件夹
- 文件图标（按扩展名显示不同图标）

### 任务 2：文件树键盘操作（~60 行）

对标 VS Code，全部带 when 条件：

| 操作 | 快捷键 | when 条件 |
|------|------|------|
| 重命名 | F2 | Explorer focus + !root + writable |
| 删除 | Delete | Explorer focus |
| 剪切 | Ctrl+X | Explorer focus |
| 复制 | Ctrl+C | Explorer focus |
| 粘贴 | Ctrl+V | Explorer focus |
| 新建文件 | Ctrl+N | Explorer focus |
| 新建文件夹 | Ctrl+Shift+N | Explorer focus |

### 任务 3：文件关联（~60 行）

```typescript
class FileAssociationService {
  private extToCommands = new Map<string, { pluginId: string; command: string }[]>();
  register(pluginId: string, associations: FileAssociation[]): void;
  getCommandFor(extension: string): { pluginId: string; command: string } | undefined;
}

// plugin.json
{ "contributes": { "fileAssociations": [
  { "extension": ".md", "command": "docReader.open", "label": "Markdown 阅读器" },
  { "extension": ".dxf", "command": "cad.openFile", "label": "CAD 查看器" }
] } }

// 文件树双击：
const match = FileAssociationService.getCommandFor(".md");
if (match) CommandRegistry.execute(match.command, { filePath });
```

### 任务 4：系统文件拖入（~50 行）

```typescript
// Electron 拖放监听
window.addEventListener('drop', (event) => {
  for (const file of event.dataTransfer.files) {
    const ext = file.name.substring(file.name.lastIndexOf('.'));
    const match = FileAssociationService.getCommandFor(ext);
    if (match) CommandRegistry.execute(match.command, { filePath: file.path });
  }
});
```

### 任务 5：Reopen Closed Tab（~30 行）

Ctrl+Shift+T 撤销关闭标签页。

```typescript
// useTabManager reducer
case 'closeTab':
  closedTabStack.push(action.tab);
  if (closedTabStack.length > 10) closedTabStack.shift();
case 'reopenClosedTab':
  const last = closedTabStack.pop();
  if (last) createTab(last);
```

### 任务 6：文件搜索（~100 行）

Ctrl+Shift+F 跨文件内容搜索。搜索框架 + 结果树 + 搜索结果高亮 + 编码检测。

### 任务 7：文件树多选/批量操作（~40 行）

Ctrl+Click / Shift+Click 多选 → 批量删除/移动/复制。

### 任务 8：文件编码检测/切换（~60 行）

EncodingService——检测文件编码（UTF-8/GBK/Shift-JIS）→ 编辑器自动选编码。状态栏显示当前编码，点击切换。

### 任务 9：拖拽文件树节点到编辑区（~30 行）

拖拽文件树节点（.md/.json/.dxf 等）到主区 → FileAssociationService 匹配 → 打开对应编辑器。

### 任务 10：Settings Editor JSON schema 提示/自动补全（~50 行）

Monaco 编辑器读取 `plugin.schema.json` → 编辑 `settings.json` / `keybindings.json` 时自动补全 + 悬停提示 + 验证。

### 任务 11：多工作区文件夹 Multi-root（~60 行）

WorkspaceService 升级——同时打开多个根文件夹。文件树显示多根节点。对标 VS Code 的 multi-root workspace。

### 任务 12：文件图标主题（~50 行）

```json
{ "contributes": { "iconThemes": [{ "id": "seti", "label": "Seti", "path": "seti.json" }] } }
```

### 任务 13：文件装饰器框架（~60 行）

```typescript
interface FileDecorationProvider {
  onDidChangeFileDecorations: Event<string[]>;
  provideDecoration(path: string): FileDecoration | undefined;
}
interface FileDecoration { badge?: string; color?: string; tooltip?: string; }
```

对标 VS Code FileDecorationProvider——Git 插件可用它给文件打状态标记。

---

## 三、汇总

| # | 任务 | 行数 |
|:--:|------|:--:|
| 1 | 文件树视图 | ~200 |
| 2 | 文件树键盘操作 | ~60 |
| 3 | 文件关联（FileAssociationService）| ~60 |
| 4 | 系统文件拖入 + Reopen Closed Tab | ~80 |
| 5 | Monaco JSON 编辑器标签页 | ~80 |
| 6 | 文件搜索（Ctrl+Shift+F）| ~100 |
| 7 | 文件树多选/批量操作 | ~40 |
| 8 | 文件编码检测/切换（EncodingService）| ~60 |
| 9 | 拖拽文件树节点到编辑区 | ~30 |
| 10 | Settings Editor JSON schema 自动补全 | ~50 |
| 11 | 多工作区文件夹（Multi-root）| ~60 |
| 12 | 文件图标主题（File Icon Theme）| ~50 |
| 13 | 文件装饰器框架 | ~60 |
| **合计** | | **~930 行** |

---

## 四、多 WebView 注意事项

- 文件树在独立 WebContentsView 中运行
- `FileService.listDir()` 通过 IPC 调用壳侧
- 文件树右键菜单走 IPC → 壳侧 MenuService 渲染
- DialogService 已就绪——删除确认不用 `window.confirm()`

---

> **← 依赖：** `../E3_多WebView与壳收尾_暂定/`（架构完工后才做）
> **插件索引：** `00-README.md`
