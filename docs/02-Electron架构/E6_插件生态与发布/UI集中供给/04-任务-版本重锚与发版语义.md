# 04-任务——版本重锚 + 发版语义机械断言（E6#124）

> **第 9.3 轮 · 前置：#123 ✅ · 发版轴：本格只定案 + 立断言，真发统一归 #127**
> **一句话**：落实总纲 §三版本策略——`@linkdesk/ui` 版本号从此 = 壳版本号，并把「锁步」做成机械断言，不再靠人记。

## 一、改动点

1. **重锚点定案（写进本档 + 总纲 §三回填）**：翻通道发版日的壳新版本号 X（照 version-bump skill 判类别——本批是作者面新能力，倾向 MINOR；0.x 特例以 skill 判定为准）→ `@linkdesk/ui` 发 X。**撞号核对（机械）**：`npm view @linkdesk/ui versions`——若 X 已在历史版本列（0.3.0 高概率被占用），壳 bump 到下一未占用号（X+0.0.1），本档记档原因。
2. **`packages/linkdesk-ui/package.json`**：`version` → 重锚号；`description` 改写新语义（「运行时由壳池统一供给；本包 = 类型契约 + dev 解析体」）；README 同笔。
3. **锁步断言（机械件）**：`scripts/check-npm-release.mjs`（或其断言清单）加一条——**壳发版时 `packages/linkdesk-ui/package.json` 的 version 必须等于壳新版本号**，不等判红。version-bump 档案/流程文档同笔加行（四包之外 ui 包的特殊性：跟壳同号，不跟作者轴小步）。
4. **`peerDependencies` 不动**（react 系照旧）；`dependencies` 里 `@linkdesk/contracts` 照旧——本格不动依赖图。

## 二、边界（⛔ 本格不做）

- ⛔ 不把包瘦身成纯 d.ts——dev 解析（壳 vite dev 的 `resolveUserDataBundles`、作者热测环）仍靠包内真实 JS；「瘦身」与语义无关，不做即无死代码风险。
- ⛔ 不实现钉线（总纲 §六④：设计位留档，届时刻格）。

## 三、验收

- 断言红测：临时把 ui 包 version 改歪 → 发版断言红且报文指到重锚规则 → 恢复。
- `npm view @linkdesk/ui versions` 撞号核对结论写进执行记录（重锚号最终定案）。
- `npm run check` 全绿。

## 四、回勾要求

- 本档执行记录（重锚号定案 + 断言红测读数）+ 总纲 §三回填 + 清单回勾 + 交接本一段。

## §执行记录（2026-09-19 · 会话 B · ✅ 落地）

- **重锚号定案 = `0.2.13`**（壳与 ui 同号）。判定链：version-bump skill 判类别 = **0.x 向后兼容**（任务书原判「倾向 MINOR」被 skill 的 0.x 特例否——0.x 里 minor 位承担破坏性职责，L9 翻通道纯新增不破坏：旧插件 bundle 自带拷贝照跑）⇒ patch 位 0.2.12 → 0.2.13；撞号核对 `npm view @linkdesk/ui versions` = 0.1.0–0.1.7 / 0.2.0 / 0.2.1 / 0.3.0 / 0.3.1，**0.2.13 未占用 ⇒ 壳不让位**。已回填总纲 §三.1a。
- **ui 包重锚落点**：`packages/linkdesk-ui/package.json` version 0.3.1 → **0.2.13** ＋ `description` 改写新语义（类型契约 + dev 解析体 / 运行时壳池供给 / 不 import css / 同号锁步）＋ README 同笔（删掉教作者 `import "@linkdesk/ui/index.css"` 的旧段落——与新语义相反的活证据）＋ `npm install` 同步 lock。
- **锁步断言落点 = `check-publish-gate.mjs` 判据⑤**（任务书给的候选之一是 check-npm-release——实测该脚本是**永不 fail 的黄灯闸**，锁步断言要「不等判红」，放黄灯里等于没拦；发布门禁才是发布那一下的硬闸，且自测基建现成）。`judgeUiVersionLockstep` + main 接线 + 自测 3 例（29 例全过）；**红测实录**：ui 版本歪回 0.3.1 → `check-publish-gate.mjs` 红「⑤ ui 锁步」拒绝发布 → 恢复 0.2.13。窗口期说明：壳尚在 0.2.12（#127 才 bump），此刻 ⑤ 对「重发 0.2.12」判红属**预期行为**（判据① 本也会拦重发）；`npm run check` 只跑 self-test 不受影响。
- **version-bump 档/流程文档同笔**：`.agents/skills/version-bump/SKILL.md` 多轴表 ui 行改锁步 ＋ 更新落点 Step 4 加「同笔必改 ui 同号」；`docs/06-发布管理/作者轴npm发版.md` §〇 加 ui 锁步条（五轴唯一与软件联动者 + 撞号规则 + 断言指针）。⚠️ SKILL.md 在 `.agents`（不进 git），以发版文档与发布门禁为持久真相源。
- `peerDependencies`/`dependencies` 未动（照 §一4）；⛔ 不瘦身、⛔ 不钉线（§二边界照办）。
- `npm run check` 全绿。本格零 publish（真发统一归 #127）。
