# 04-其余插件整理——file-tree / serial-monitor / editor / settings

> **对应任务：E6#87（第 3.6.4 轮）。**
> **四插件共 24 处超限**（file-tree 9 · serial-monitor 7 · settings 5 · editor 3）。其余插件（`python` / `floating-panel-demo` / `panel-demo` / 10 个 `theme-*` / 2 个 `lang-*`）**全部达标，本轮不动**。

---

## 〇、先读这个——三个插件专属的坑

**① `plugins/*/src/index.tsx` = entry，不是「视图之一」。**

`appearsIn.tabBar === true` + 有 `entry` 声明 → 该插件可创建为标签页，**入口的 default export 就是主视图组件**（`src/pluginLoader/contributions/viewRegistry.ts:145`）。`contributes.views` 是**侧栏/附加视图**。

→ 所以 `serial-monitor/src/index.tsx`（**1642 行**）里那坨 `SerialMonitorView` **是主视图本体**，不是入口胶水。**正解 = 视图搬去 `views/`，入口瘦成薄门面**（照 marketplace 的形状）。这是本轮最大的一个动作。

**② `hooks/` 与 `utils/` 是既有房间，只是门禁没登记。**

`serial-monitor/src/` 下有 `hooks/`（`useSerialSessions.ts`）与 `utils/`（`useSendData.ts` / `RingBuffer.ts`）——**09 规范认可这两类，但 `check-file-size.mjs` 的 `ROLE_LIMITS` 只登记了 views/components/services/styles**。第 5 轮扩域时必须补上（见 [05](05-门禁扩域与验收.md)），否则门禁会对它们**抛错**（`未知 role 夹`）而不是放行。

**③ 模块级 mutable 状态 = 拆分时的一号杀手。**

| 文件 | 模块级状态 | 拆错的表现 |
|:--|:--|:--|
| `serial-monitor/src/services/SerialContext.tsx` | `_openPorts` / `_listeners` / `_portListeners` | 串口开着但界面说没开 / 监听器重复注册 |
| `serial-monitor/src/index.tsx` | `_cmdMap` / `_receiveSnapshots` | 发送无回显 / 切换标签页后接收区空白 |
| `file-tree/src/components/FileTreeContextMenu.tsx` | `FileTreeHandle` 引用 / `OpenFileFn` | 右键菜单点了没反应 |
| `editor/src/services/lsp-bridge.ts` | `_clients` / `_states` / `_stateSubs` / `_pending` / `_blockedNoticed` | LSP 客户端重复起 / 状态不刷新 |

> 🔴 **铁律（照 0d.10-8）**：模块级 mutable 状态**各归单域属主**，跨域读走公开 accessor。**拆完 grep 一次变量名，确认全仓只有一处 `const _xxx =` 声明。**

---

## 一、file-tree（9 处超限）

| 文件 | 行 | 档/阈值 | 处置 |
|:--|--:|:--|:--|
| components/FileTreeContextMenu.tsx | 536 | components/150 | 门面 + 同名夹 |
| components/FileTree.tsx | 463 | components/150 | 门面 + 同名夹 |
| views/FoldersView.tsx | 447 | views/150 | 门面 + 同名夹 |
| services/FileTreeModel.ts | 436 | services/200 | 门面 + 同名夹 |
| styles/file-tree.css | 371 | styles/300 | 拆 2 件 |
| views/SearchView.tsx | 359 | views/150 | 门面 + 同名夹 |
| services/FileTreeKeyboard.ts | 268 | services/200 | 门面 + 同名夹 |
| services/FileTreeDnD.ts | 249 | services/200 | 门面 + 同名夹 |
| components/FileTreeNode.tsx | 215 | components/150 | 拆（树节点渲染 + 内联编辑） |

**`FileTreeContextMenu.tsx` 真相**（实测）：33–498 行是**模块级宿主桥**——`setFileTreeHandleRef` / `clearFileTreeHandle` / `setOpenFileFn` / `setSelectedUris` / `setFocusedUriBridge` / `activateFileTreeContextMenu`；**499–536 才是菜单组件本体**。文件名字骗人，**拆法是照职责不是照名字**：

```
components/
├─ FileTreeContextMenu.tsx      ← 门面：re-export 下面三件
└─ FileTreeContextMenu/
   ├─ host-bridge.ts            ← FileTreeHandle 引用 + setOpenFileFn（🔴 模块级 mutable 单一属主）
   ├─ commands.ts               ← activateFileTreeContextMenu（命令注册，独立于 UI）
   └─ Menu.tsx                  ← 菜单组件本体
```

**其余同名夹拆法**（单元按实测导出定）：

| 门面 | 子模块 |
|:--|:--|
| `FileTree.tsx` | `flattenTree.ts`（58–88）/ `findLeaf.ts`（89–110）/ `render-row.tsx` / 容器体 |
| `FoldersView.tsx` | 侧栏容器 / 空态 / 工具栏 / 事件接线（4 段） |
| `SearchView.tsx` | 查询输入 / 结果列表 / 结果项 / 键盘导航 |
| `FileTreeModel.ts` | `types.ts` / `load.ts`（读盘+监听）/ `mutate.ts`（增删改）/ `selectors.ts` |
| `FileTreeKeyboard.ts` | `typeAhead.ts`（`TYPE_WINDOW` 逻辑）/ `scrollToItem.ts` / `useFileTreeKeyboard.ts`（hook 本体） |
| `FileTreeDnD.ts` | `dropTarget.ts`（`getDropTargetIndex` / `resolveDropTarget` / `isAncestorOf`）/ `execute.ts`（`executeSafeDrop`）/ `useFileTreeDnD.ts` |
| `file-tree.css` | `file-tree-shell.css` / `file-tree-node.css`（按现有分节切，**只搬不改**） |

> ⚠️ **`FileTreeKeyboard` 依赖 `scrollToItem` + 1 秒 type-ahead 窗口**——拆 hook 与窗口逻辑时**别改时序**，否则出现「连按字母跳不对」。

---

## 二、serial-monitor（7 处超限）——**本轮最重的一个插件**

| 文件 | 行 | 档/阈值 | 处置 |
|:--|--:|:--|:--|
| **index.tsx** | 1642 | entry/120 | 🔴 **主视图搬出 entry**（见下） |
| styles/SerialMonitorView.css | 591 | styles/300 | 拆 2–3 件 |
| services/SerialContext.tsx | 477 | services/200 | 门面 + 同名夹 |
| views/SessionListView.tsx | 263 | views/150 | 门面 + 同名夹 |
| views/SerialSettingsView.tsx | 221 | views/150 | 门面 + 同名夹 |
| components/ControlPanel.tsx | 210 | components/150 | 拆 2 件 |
| hooks/useSerialSessions.ts | 180 | hooks/150 | 拆 |

### 2.1 `index.tsx` 拆分（1642 → entry ≤120 + `views/SerialMonitorView.tsx` 门面）

**实测顶层结构：**

| 区段 | 行 | 内容 | 去处 |
|:--|--:|:--|:--|
| 头注 + imports | 1–54 | — | 按去处分配 |
| `_saveFilePicker` + 句柄类型 | 55–96 | 保存文件能力 | `utils/saveFile.ts` |
| 常量群 | 98–118 | 缓存容量 / 模式 / 高度 | `constants.ts` |
| `_cmdMap` + `getActiveCmd` | 134–141 | 🔴 模块级 mutable | `services/commandBridge.ts`（**单一属主**） |
| CM6 StateEffect/StateField | 162–225 | 行装饰 + 时间戳标记 | `cm6/decorations.ts` |
| `_receiveSnapshots` + `snapshotCmLines` | 227–252 | 🔴 模块级 mutable | `services/receiveSnapshots.ts`（**单一属主**） |
| 搜索装饰 StateField | 253–304 | — | `cm6/search.ts` |
| `ScrollTracker` + `ViewPlugin` | 284–307 | — | `cm6/scroll.ts` |
| `escapeInvisible` / `sanitizeFileName` | 309–332 | 纯函数 | `utils/text.ts` |
| **`SerialMonitorView`** | 333–1642 | 主视图本体 | `views/SerialMonitorView.tsx`（门面 + 同名夹） |
| `export default SerialMonitorView` | 1642 | — | entry 改为 `export { default } from "./views/SerialMonitorView"` |

**`views/SerialMonitorView/` 子模块**（按 333–1642 内的功能段切）：工具栏（COM/波特率/开关）、接收区（CM6 挂载 + 装饰 + 滚动）、发送栏（CM6 + 历史 + HEX 模式）、导出/保存、搜索条接线。

> 🔴 **CM6 的生命周期**（00 §六 风险 ⑤）：`EditorView` 的创建/销毁**必须与 React 组件生命周期一致**。拆成子组件时，**CM6 实例的持有者只能是接收区/发送栏各一个组件**——**不要**把 `EditorView` 提到门面再往下传（off-by-one 帧 + StrictMode 双挂 = 双实例）。
> `compartment` 重配置（语言/只读/主题）**留在持有者内部**，跨组件传 `Compartment` 句柄 = 换一种方式踩同一个坑。

**这条拆完必须实机验**（不能只看 `npm run check`）：
- 打开一个串口 → 收到数据 → 接收区滚动正常
- 标签页切走再切回 → **接收区内容不丢**（keep-alive 依赖 CSS display，不是条件渲染）
- 关闭标签页再打开 → CM6 不重复实例化（控制台无重复警告）
- 发送一帧 → 有回显

### 2.2 其余

| 门面 | 子模块 |
|:--|:--|
| `services/SerialContext.tsx` | `store.ts`（🔴 `_openPorts`/`_listeners`/`_portListeners` 单一属主 + `_setState`/`_subscribe`）/ `ipc.ts`（`_initOnce` / `_registerIPCListeners` / `_unregisterIPCListeners`）/ `status.ts`（`mergeStatus`）/ `useSerialContext.ts` |
| `views/SessionListView.tsx` | 列表容器 / 会话项 / 新建·重命名内联输入 / 空态 |
| `views/SerialSettingsView.tsx` | 设置分组 / 表单控件 / 读写接线 |
| `components/ControlPanel.tsx` | 工具栏本体 / 下拉（COM·波特率）/ 开关按钮 |
| `hooks/useSerialSessions.ts` | 会话表 store / 快照读写 / 活动会话（`setActiveSessionId` 等） |
| `styles/SerialMonitorView.css` | 按现有分节切 2–3 件（工具栏 / 接收区 / 发送栏），**只搬不改** |

> ⚠️ **`services/SerialContext.tsx` 的 IPC 监听器**受硬约束 19/20 管：**模块级 `_initOnce` 数据拉取**可以，**监听器必须走引用计数**（mount 注册 / unmount 清理）。拆时**别把两者混进同一个 `_initialized` guard**——E3j #81 的僵尸回调就是这么来的。

---

## 三、editor（3 处超限）

| 文件 | 行 | 档/阈值 | 处置 |
|:--|--:|:--|:--|
| components/EditorTab.tsx | 443 | components/150 | 门面 + 同名夹 |
| views/EditorView.tsx | 431 | views/150 | 门面 + 同名夹 |
| services/lsp-bridge.ts | 250 | services/200 | 门面 + 同名夹 |

| 门面 | 子模块 |
|:--|:--|
| `components/EditorTab.tsx` | 标签页容器 / 面包屑接线 / 状态栏接线 / 自动保存 |
| `views/EditorView.tsx` | Monaco 挂载 / 模型同步 / 差异化编辑（`DiffEditor` 已独立）/ 事件接线 |
| `services/lsp-bridge.ts` | `registry.ts`（🔴 `_clients`/`_states`/`_stateSubs`/`_pending`/`_blockedNoticed` **单一属主**）/ `start.ts`（`startLspClient`）/ `ipc-reader.ts`（`createIpcReader`）/ `notice.ts`（`takeBlockedNotice`） |

> 🔴 **`lsp-bridge` 是真·重灾区**：五份模块级 `Map`/`Set` 互相耦合（`_pending` 防重复起客户端、`_blockedNoticed` 去重提示）。抽屉式搬法：**先把五个容器集中到 `registry.ts` 并导出 accessor，再搬逻辑**——顺序反了 = 中途编译不过 + 容易写出第二份 `_clients`。
> **`editor` 是 `core: true` 插件**——卸载按钮隐藏，但**代码上可卸可禁**（E6#18）。**不要**在拆分时顺手加特权分支。

---

## 四、settings（5 处超限）

| 文件 | 行 | 档/阈值 | 处置 |
|:--|--:|:--|:--|
| views/SettingsView.css | 630 | styles/300 | 拆 2–3 件 |
| views/SettingsView/renderControl.tsx | 438 | views/150 | 门面 + `renderControl/` |
| views/SettingsView.tsx | 410 | views/150 | **已是门面**（配 `SettingsView/`）→ 继续拆 |
| views/keybinding-settings/KeybindingSettingsView.tsx | 334 | views/150 | 门面 + 同名夹 |
| views/SettingsView/SettingRow.tsx | 220 | views/150 | 拆（行渲染 + 齿轮菜单 + 控件委派） |

**settings 已经是 feature-folder 形态**（`views/SettingsView/` 已存在 8 个子模块）——本轮是**在既有夹内继续加密**，不是新建结构。

| 门面/文件 | 拆法 |
|:--|:--|
| `SettingsView.tsx` | 门面 → 分组导航 / 搜索过滤 / 事件订阅（`useSettingsEvents` 已独立）留在夹内 |
| `renderControl.tsx` | 保留 `renderControl` 分发器 + `CONFIG_NONE_SENTINEL`；把 `BackgroundImagePicker` / `FontTonePreview` / `AccentSourceControl` / `FontToneControl` 搬去 `renderControl/controls.tsx` |
| `SettingRow.tsx` | 行渲染留本文件；齿轮菜单 → `SettingRow/gearMenu.ts`；`COLOR_PICKER_PRESETS` → `SettingRow/constants.ts` |
| `KeybindingSettingsView.tsx` | 容器 / 行渲染（`keybindingRowSort` 已独立）/ 冲突提示 / 搜索 |
| `SettingsView.css` | 按现有分节切 2–3 件（**只搬不改**） |
| **测试 6 个** | 🔴 **从共置迁入 `src/__tests__/`**（见下） |

> 🟡 **`views/SettingsView.css` 是 co-located CSS（摆 `views/` 不摆 `styles/`）**——这是 09 规范没写过的形态。**已拍板**（[00 §五](00-整理档案.md) 决策 ③）：允许组件自带样式，[05](05-门禁扩域与验收.md) 的门禁按 **扩展名**（`.css` → 300）而非按目录，这个文件才管得住。

> 🔴 **settings 测试要真迁**（2026-09-11 拍板，[00 §五](00-整理档案.md) 决策 ④）。现况：`views/SettingsView/deriveSourceBadge.test.ts` 等 **5 个**共置 + `views/keybinding-settings/keybindingRowSort.test.ts`（合 **6 个**）——走的是**壳**的做法，与另外 4 只插件的 `__tests__/` 不一致。**拍板 = 迁**，不是只改规范文本。
>
> **迁法（照 [09-插件目录规范](../../../03-插件制造/09-插件目录规范.md) 的相对路径镜像规则）：**
> ```
> src/views/SettingsView/deriveSourceBadge.test.ts        → src/__tests__/views/SettingsView/deriveSourceBadge.test.ts
> src/views/keybinding-settings/keybindingRowSort.test.ts → src/__tests__/views/keybinding-settings/keybindingRowSort.test.ts
> ```
> **迁完必须逐条改相对 import**（`./renderControl` → `../../../views/SettingsView/renderControl`）——**测试路径加深一层，所有 import 全变**。这是本档最容易漏的一处：`vitest` 找不到被测模块的表现是**测试文件被静默跳过**（`0 test`），不是报错——**迁完必须核对 `vitest run` 里 settings 的用例数没变**。
>
> ⚠️ **测试文件不受体积门禁约束**（`check-file-size.mjs` 的 `SKIP_DIRS` 含 `__tests__`）——迁完不可见任何行数收益，**收益纯粹是「五只插件一个样」**。若嫌这轮动作多，可把本项**单独摘出来做一步**（清单 E6#87d 内已单列）。

---

## 五、四个插件的统一验收

每个插件各自一轮，**逐插件走完下面 8 条再进下一个**（不要四个一起改一起验——出问题无法二分）：

- [ ] 超限文件全部 ≤ 各自档位阈值
- [ ] 门面保留原路径 → **消费方 import 零变更**（`git diff --stat` 核对）
- [ ] 🔴 **模块级 mutable 变量全仓只有一处声明**（逐变量 `grep -rn "const _xxx"`）
- [ ] `npm run check` 全绿（tsc 抓相对路径 + ESLint 硬约束 10/13/14 + 体积门禁）
- [ ] **实机（dev）**：见下「逐插件实机项」
- [ ] 打包版形态至少跑一次（`--real` 或安装版）
- [ ] **version-bump**：改 `plugins/<id>/` → **插件轴**，各自 `fix:` bump patch
- [ ] 清单回勾 + 本档案结果注记

**逐插件实机项：**

| 插件 | 必验 |
|:--|:--|
| **file-tree** | 展开/折叠目录 → 右键菜单 → 文件树拖拽移动 → 键盘导航 + 首字母跳转 → 搜索视图 |
| **serial-monitor** | 见 §2.1 尾注四条（**本轮唯一「不实机必翻车」的插件**） |
| **editor** | 打开文件 → Monaco 可编辑 → 保存 → LSP 若可用则补全/诊断 → 标签页切换不丢状态 |
| **settings** | 打开设置 → 分组导航 → 改一项设置立即生效 → 快捷键页 → 搜索 |

---

## 六、🔴 本轮风险（四插件共有）

| 风险 | 形态 | 规避 |
|:--|:--|:--|
| **① 模块级状态被拆散** | 🔴 **本层头号**——四插件都有，见 §〇③ | 拆前先 grep 全部模块级 `_xxx`，集中到单属主文件，再搬逻辑 |
| **② CM6 生命周期** | 🔴 仅 serial-monitor | §2.1 尾注；**必须实机验** |
| **③ IPC 监听器僵尸** | 🔴 serial-monitor `SerialContext` | 硬约束 19/20；`_initOnce` 与监听器**分开** |
| **④ 循环 import** | 🟡 editor `lsp-bridge`、serial `SerialContext` | 拆完核 import 方向 |
| **⑤ 漏搬样式** | 🔴 `.css` 拆分漏块 = **静默丢样式** | 拆完 grep 类名前缀，比对定义数 |
| **⑥ 相对路径层级** | 🟡 文件进子夹 → `../` 变 `../../` | tsc 拦得住；**逐文件过，禁批量 sed** |
| **⑦ 顺手改逻辑** | 🔴 拆 1642 行时手痒 | **只搬家**；发现的问题单独立案 |
| **⑧ 版本轴** | 🟡 改 `plugins/*` = 插件轴 | 逐插件 version-bump（**不是**软件轴） |

---

> **← 上一档案：** [03-市场插件整理.md](03-市场插件整理.md)
> **→ 下一档案：** [05-门禁扩域与验收.md](05-门禁扩域与验收.md)
