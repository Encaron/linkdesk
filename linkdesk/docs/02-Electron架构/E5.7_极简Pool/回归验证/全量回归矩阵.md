# 全量回归矩阵

> 极简Pool 迁移完成后的全量验证。每个场景列验证点 + 回归风险。

---

## 场景 1：侧栏交互

| 验证点 | 操作 | 期望 |
|:--|:--|:--|
| 图标栏切换 | 点文件树图标 → 点搜索图标 → 点市场图标 | 侧栏视图切换正确，图标高亮跟随 |
| 折叠/展开 | 侧栏 header ◀ 按钮 / 折叠态 ▶ 按钮 / header 右键「折叠」/ Ctrl+B | 150ms 动画，MainZone 自动扩展（E5.7 订正：壳侧「顶部分隔线」点击入口随 SidePanel 消失；Ctrl+B 由 E5.7#84 补注册） |
| 拖宽 | 拖侧栏右侧 4px 分隔线 | 实时 resize，鼠标移出分隔线不丢事件 |
| 视图切换 | 图标栏切到别的容器再切回 | keep-alive 状态保持（E5.7 订正：侧栏视图堆叠展示，无侧栏内 tab——原 E5.6 tab 切换手势随 SidePanel 消失） |
| 图标栏显隐 | 点侧栏图标（激活视图再点一次） | 侧栏隐藏/显示，与 Ctrl+B 等效 |
| 单容器 header | 侧栏只剩一个视图容器 | mergeHeaderWhenSingle 生效——header 不重复渲染 |
| sticky 吸顶 | 侧栏视图内容滚动 | header 吸顶，不随内容滚走 |
| 文件树快捷键 | 文件树内 F2 / Delete | 内联重命名框出现 / 删除确认——E5.6#11.5 修复不回归 |

**回归风险：** SidebarZone 从独立 WCV 变同 DOM 组件——插件组件必须还能 `import.meta.glob` 动态加载。防 E5.6#11.5 回归：IconBar 的 `window.linkdesk.*` 缺口（Phase 12 #62 `sidebar` 命名空间补全）——图标切换/显隐走 IPC 而非跨组件调用。

## 场景 2：编辑器

| 验证点 | 操作 | 期望 |
|:--|:--|:--|
| 打开文件 | 文件树双击 .c/.py/.ts 文件 | 编辑器打开，LSP 启动 |
| 切标签页 | 点多个 tab | 编辑器状态保持（Monaco 不重载） |
| 关闭标签页 | 点 tab × | 卸载正确，脏标记确认弹窗 |
| 脏标记 | 修改文件 | tab title 显示 ● |
| F12 跳转 / Ctrl+Click | 函数跳转 | LSP 正常 |
| 主题切换字体颜色 | 设置切暗色/亮色 → 打开代码文件 | Monaco token 颜色正确（无黑字黑底）、字体族生效——防 E5.6#2 静态 import 竞态 + 暗色字体竞态回归 |
| 同名标签标题 | 打开两个同名不同文件 | tab 标题显示各自文件名，不混淆 |
| 斜体/正体 | 已有 tabBehavior.singleton 插件标签时再开同插件 | 斜体 = 替换（单例），正体 = 新增标签——声明式行为不回归 |

## 场景 3：分屏

| 验证点 | 操作 | 期望 |
|:--|:--|:--|
| 左右分屏 | 拖 tab 到编辑器右半区 | Glassmorphism 预览，drop 后分屏 |
| 上下分屏 | 拖 tab 到底部 | 同上 |
| 拖拽分屏线 | 拖分隔线 | 实时 resize |
| 合屏 | 拖 tab 回原 group | 分屏合并 |
| 深度限制 | 4 层分屏后继续拖 | 拒绝第 5 层 |
| 设置 tab 拖拽 | 拖"设置"标签页到编辑器右半区 | 正常分屏——防"设置 tab 不可拖"回归（titlebar drag region 冲突） |
| 标签栏排序 | 标签栏内左右拖 tab | 顺序实时变化，不掉 tab |

## 场景 4：浮层

| 验证点 | 操作 | 期望 |
|:--|:--|:--|
| 右键菜单——文件树 | 右键文件 | 菜单显示在鼠标位置，不被裁切 |
| 右键菜单——编辑器 | 右键代码 | 同上 |
| 右键菜单——标签页 | 右键 tab | Close/Split 等命令 |
| 菜单边界 | 右键窗口右下角 | 菜单自动翻转，不出屏 |
| 菜单消失 | 点菜单外 / Escape | 关闭 |
| 命令面板 | Ctrl+Shift+P | 居中显示，输入过滤，防抖 150ms |
| Toast | 触发通知 | 右下角显示，5s 自动消失，新 Toast 在下方 |
| Dialog | 关未保存文件 | 模态居中，焦点陷阱，Escape/backdrop 关闭 |
| 菜单键盘导航 | 右键菜单后按 ↑↓ Enter | 高亮移动、执行正确——防 focusable:false 键盘全拦回归 |
| 语言纯净 | LanguagePicker 切英文 → 右键文件树/编辑器 | 菜单全英文无中文残留——防 E5.6#11.5 i18n 标签回归 |

**回归风险：** ContextMenu 从 OverlayWindow 独立窗口改同 DOM position:fixed——z-index 必须高于所有 zone（3000+），pointer-events 切换正确（"点不动" = pointer-events 或 z-index 被 zone 盖住）。**语言不是 CSS 能继承的**——菜单标签必须在壳解析时已 t() 本地化（聪慧→哑：壳解析、池哑渲染字符串），池侧不得二次翻译。

## 场景 5：底部面板

| 验证点 | 操作 | 期望 |
|:--|:--|:--|
| 打开 | Ctrl+J | PanelZone 显示，MainZone 缩小 |
| 切换视图 | 点 Terminal/Output/Problems | keep-alive 切换 |
| [+] 按钮 | 点 + | 新建终端 |
| 拖高 | 拖面板顶部分隔线 | 实时 resize |
| Toast 位置 | 面板打开时触发 Toast | Toast 仍在窗口右下角（30px from bottom——现网 ToastContainer.css:17，状态栏 22px + 8px gap），不被面板推走 |

> **✅ ①③ 改写为已实现（2026-08-21，E5.8#36 收官）**——本场景 ①打开（Ctrl+J）与 ③[+] 新建在 E5.7#88 时记为「已知未做项」：① 由 **E5.8#31** 补 `workbench.action.togglePanel` + shellKeybindings `ctrl+j`（commit `0fe467a9`）；③ 由 **E5.8#32** 补 `panel:createView` 桥（QuickPick 视图选择器，commit `0966eb99`）。五行自此全实现。面板全链（贡献→显隐→切换→新建→容器切换→reveal/moveToEditor）收口见 E5.8 回归验证矩阵 + E5.8#36 执行注。

## 场景 6：串口监视器

| 验证点 | 操作 | 期望 |
|:--|:--|:--|
| 多会话 | 打开 COM3 + COM5 | 两个 tab 独立 |
| 收发数据 | 发送/接收 | 实时滚动 |
| 右键菜单 | 右键数据区 | 菜单正常（clear/copy 等） |
| 协议下拉 | 打开协议选择 | 下拉不裁切，列表正确 |
| 关闭连接 | 点断开 | 状态更新 |

**回归风险：** 之前修复的 3 个串口 bug（右键空/无数据/Monaco 主题污染）在新架构下依然有效。

## 场景 7：插件热重载

| 验证点 | 操作 | 期望 |
|:--|:--|:--|
| 修改插件代码 | 改文件后保存 | import.meta.glob 更新 |
| 重载插件 | 市场里 Reload | 状态保持 |
| 新装插件 | 装新插件 | 无需重启壳 |
| 卸载→重启→重装 | 卸载插件 → 退出软件 → 重进 → 再安装同插件 | 已装列表扫盘重建正确；安装幂等；plugin:installed 通知到达市场 UI（#83） |

## 场景 8：崩溃恢复

| 验证点 | 操作 | 期望 |
|:--|:--|:--|
| renderer 崩溃 | 插件 while(true){} / kill renderer | 2-4s 自动重建 |
| layout 恢复 | 崩溃后 | 标签页/分屏/侧栏/面板全恢复 |
| Hot Exit | 编辑未保存内容 → 崩溃 | 重建后内容恢复 + dirty 标记 |
| 壳崩 | kill 壳 renderer（BrowserWindow webContents） | 全窗口重建 + tabState 持久化恢复 + lastLayout 兜底 |
| 连续崩溃 | 10s 内 3 次 | 停止重建，显示错误页 |
| 布局引擎复活实机验证（E5.8#37.6-#37.8，**实机验证 = 用户验收**） | ① 面板位置切顶/左/右/底逐一实机渲染；② 四对齐（居中/两端/左/右）各验；③ 侧栏换边 + 图标栏跟随（双栏互换）；④ 面板拖拽钳制；⑤ 重启恢复位置/对齐/换边；⑥ 换位后视图状态 | ① 顶/左/右/底各渲染正常（左/右 = 竖条 5 带排布）；② 对齐几何正确（覆盖侧栏缩短/贯穿）；③ 双栏互换不崩 + 图标栏恒贴主侧栏外缘；④ 拖拽钳制仍生效；⑤ 重启恢复位置/对齐/侧栏边；⑥ 换位（grid 零重挂）不丢视图状态 |

## 场景 9：脱出窗口

> 🔴 2026-08-13 审计：整个场景推迟 v1.3（Phase 8 已整体推迟——清单 #32-#35/#92 划掉）。E5.7 发布不验证，05-版本更新 v1.3 时启用本表。

| 验证点 | 操作 | 期望 |
|:--|:--|:--|
| 拖出 | 拖 tab 超出窗口 30px | 新窗口跟随鼠标 |
| 独立编辑 | 在脱出窗口编辑 | 功能正常 |
| 关闭回归 | 关脱出窗口 | tab 回主窗口，状态保持 |
| 漂移面板 | PanelZone ⤢ 按钮 | 面板独立窗口化 |

## 场景 10：收尾检查

- [ ] `npx tsc --noEmit` 零错误
- [ ] `npx eslint` 零新增 warning/error
- [ ] vitest 基线测试通过
- [ ] grep 零残留：`OverlayWindow` / `sidebarPoolView` / `pluginViews` / `instanceId` / `poolId` / `?zone=`（2026-08-13 审计补 poolId——API 变更清单 §1 删除项）
- [ ] 进程数 = 3（主进程 + 壳 + Pool）——任务管理器验证
- [ ] F5 刷新——会话恢复、布局恢复

---

> 📖 执行清单 → [E5.7-执行清单.md](../E5.7-执行清单.md) Phase 16
