# 03 · 官方插件补齐（L3.7 第 3.7.3 轮）

> **🔖 非新能力文档（2026-09-11 判定）：** 本文出现的 `contributes.*` 标记**是「引用既有贡献点做对照」**（如 §四 讲 `contributes.i18n` 的字典汇入全局 bundle）——本层**不新增任何壳 API、不新增贡献点**。判定理由见 [00 号档案](00-整理档案.md) 卷首的「非新能力文档」条。
>
> **本轮是「内容搬家 + 补缺」，零新逻辑。** 把 `plugin.json` 里没人读的 49 条记录**搬**成有人读的 `CHANGELOG.md`；把 marketplace 缺的 `README.md` 补上；把 settings 的两处 i18n 键错位修掉（**原判「缺 i18n 目录」已证伪，见 §四**）。
> 🔴 **判据 M3**（[00 §三](00-整理档案.md)）：**删字段必须给迁移出口**——49 条**内容零丢失**，是搬家不是重写。

---

## 一、任务总览

| # | 动作 | 对象 | 量 |
|:--|:--|:--|:--|
| ① | **49 条搬家** `plugin.json.changelog` → `CHANGELOG.md` | 9 只插件 | 9 个新文件 |
| ② | **补 README.md + 专属封面** | marketplace | 2 个新文件（`README.md` + `resources/cover.svg`） |
| ③ | **修两处 i18n 键错位**（原判「补 i18n」已证伪，见 §四） | settings | 2 行 |
| ④ | **补空插件的元数据**（建议，可单独砍） | **10 只**（2 demo + 7 theme + lang-test-ja；**第 11 只 `marketplace` 归 ②**） | README + CHANGELOG 各一行 |

**为什么必须现在做**：3.7.2 删掉 schema 字段后，**这 49 条会变成「写在没人看的地方」→「哪儿也没写」**。中间没有别的出口。

---

## 二、① 搬家——1:1 映射，零解释成本

### 2.1 映射规则（机械，不用判断）

```jsonc
// 搬前：plugin.json
"changelog": [
  { "version": "1.0.7", "date": "2026-09-11", "changes": ["内部整理（E6#87b…）：…"] },
  { "version": "1.0.6", "date": "2026-09-10", "changes": ["README 展示区封面随包…"] }
]
```

```markdown
<!-- 搬后：CHANGELOG.md（版本已倒序，与源同序，不重排） -->
# 更新日志

## v1.0.7（2026-09-11）

- 内部整理（E6#87b 文件整理层）：1642 行主视图按 12 档拆为 24 件同名夹 + …

## v1.0.6（2026-09-10）

- README 展示区封面随包（E6#70a）：说明区静态图经 assetBase 解析 linkdesk:// 包内资产真加载
```

- `version` → 段标题 `## v{version}`（**带 `v` 前缀**——[09 规范](01-规范文档对齐.md) 的示例形态）
- `date` → 段标题括号 `（{date}）`（**全角括号**，与既有 README 文风一致）
- `changes[]` → 段正文**逐条一行 bullet**（`- ` 前缀）。**多条 = 多行**，不合并、不改写、不缩写
- **数组顺序原样保留**（源已是最新在前）
- 文件头固定 `# 更新日志` 一行 + 空行

### 2.2 🔴 搬家对账表（**搬完必须逐只核对，条数对不上即红灯**）

| 插件 | 搬前条数 | 搬后 `CHANGELOG.md` 段数 | 校验命令 |
|:--|--:|--:|:--|
| **marketplace** | **27** | 27 | `grep -c '^## ' plugins/marketplace/CHANGELOG.md` |
| serial-monitor | 8 | 8 | 同上 |
| lang-defaults | 5 | 5 | 同上 |
| file-tree | 3 | 3 | 同上 |
| settings | 2 | 2 | 同上 |
| editor | 1 | 1 | 同上 |
| python | 1 | 1 | 同上 |
| theme-aurora-glass | 1 | 1 | 同上 |
| theme-terminal | 1 | 1 | 同上 |
| **合计** | **49** | **49** | — |

> **对账判据**：`grep -c '^## ' plugins/*/CHANGELOG.md` 逐只数字**必须等于上表**。
> **`changes` 条目总数也要核**——段数对不代表 bullet 没丢。搬完跑一次 `grep -rc '^- ' plugins/*/CHANGELOG.md`，与搬前 `plugin.json` 里 `changes` 数组的总元素数比对。（搬前先把总数记下来，别凭印象。）

### 2.3 同时做的事（同一笔提交）

- 删该插件 `plugin.json` 的 `changelog` 数组**整个键**（含尾随逗号/前一行的逗号——JSONC 尾逗号合法，但**保持文件整洁**，别留孤儿逗号）。
- 🔴 **`version` 不动**——本次是「内容换住处」不是「发新版」。**版本号表示「插件变了」，不是「文件动了」**（承 [12-README说明区契约 §五](../03-插件市场/12-插件目录与UI细节.md)：内容变更必 bump；但**本轮不动用户可见内容**，只换元数据存放位置 ⇒ 见 §五 版本判定）。

### 2.4 🔴 顺序：**必须在 3.7.2 之后**

见 [02 §七 顺序铁律](02-元数据链路归一.md)。**反序会在仓库里造出一个自相矛盾的窗口期。**

### 2.5 关于 `first-run-setup`（唯一「两边都写」的样板）

它的 `plugin.json` 里有 1 条 `changelog` **同时**有 `CHANGELOG.md`。**「两边都写」不是优点，是冗余**——本层按 M1 把它归一到「只有文件」。

**但它源码不在本仓**（在 `E:\linkdesk-build+scratch\first-run-setup` + 仓内只有 `dev-fixtures/first-run-setup.linkdesk-plugin` 成品）⇒ **本轮够不着**——[00 §五 ②](00-整理档案.md) **已拍「不搬」**（外部构建是有意的验证）⇒ 只能靠**对用户的口头交接**（E6#91h）让它自己删掉那个字段。

---

## 三、② marketplace 补 `README.md`

### 3.1 为什么它没有

时间线（[00 §一.2](00-整理档案.md)）：marketplace 目录首提交 **2026-07-22**（与 settings 并列最老）；README 惯例 **2026-09-09** 才成形。**它比惯例早一个半月出生，不在那批里，之后没回头补。**

### 3.2 后果（用户可见）

详情页「详情」页签的数据源就是包内 `README.md`（[useDetailPackage.ts](../../../../plugins/marketplace/src/views/detail/DetailView/useDetailPackage.ts) 三源回落的**第一源**）。没有它 → 已装态落到 `readmeUrl` 远端兜底（**marketplace 也没声明**）→ 再落到 description。

⇒ **打开 marketplace 自己的详情页，「详情」页签是空的**（只有一句 description）。**最会被人抄的那只插件，示范的是「怎么没有 README」。**

### 3.3 写什么（大纲，实现时逐节展开）

| 节 | 内容 | 依据 |
|:--|:--|:--|
| 开头 | 一句话定位 + `![marketplace 场景封面](resources/cover.svg)` | 🔴 **封面引用是必须的**——SDK 靠 README 里的相对引用决定哪些资产随包（[vite-code.vite-config.ts:549-556](../../../../packages/plugin-sdk/src/vite-config.ts)）；marketplace 现在**有 `resources/icon.svg` / `icon-brand.svg` 但没有 `cover.svg`** ⇒ **需先补一张封面图**（或复用 `icon-brand.svg`，见 §3.4） |
| 你会看到什么 | 侧栏分几块（探索 / 已安装 / 更新）、详情页三个页签、安装/更新/卸载的动作条 | 对齐 [03-市场交互设计](../03-插件市场/03-市场交互设计.md) |
| 怎么用 | 发现插件 → 装 → 更新（含「自动更新」开关语义）→ 卸载；装之前看什么（详情页三页签） | 对齐 [10-市场UI拥有权](../03-插件市场/10-市场UI拥有权.md) |
| 三条纪律 | 从源码里提炼 2-3 条作者/用户该知道的（如「装之前能看 README 和更新日志」「更新目标与显示版本是两个值」） | 对齐 [first-run-setup README](../../../03-插件制造/00-README.md) 的「三条能直接抄走的纪律」体例 |
| 怎么读这份源码 | 一张「想看什么 → 看哪」的表 | 同上体例 |

### 3.4 🔴 封面图从哪来（开工前定，别边写边找）

- marketplace 现在 `resources/` 里是 `icon.svg` + `icon-brand.svg`（[实测](../../../../plugins/marketplace/plugin.json)），**没有 `cover.svg`**。
- **三条路**：
  - **(a) 新建一张专属封面**（对齐其余 8 只「一插件一封面」的形态）——**推荐**（M4：官方插件同形）；
  - **(b) 复用 `icon-brand.svg` 当封面**——零新资产，但封面与身份图同图，说明区头部会显得重复；
  - **(c) 不画封面**，README 里不放 `![…]`——最省事，但破坏「8 只范本整齐一致」。
- 🔴 **画封面属 UI/视觉改动 ⇒ 触发硬约束 16**（任何配色/视觉改动前必须经 `Skill` 调设计 skill 拿设计系统）。**开工时先调 skill，再落图。**

---

## 四、③ settings 的 i18n——**初判是缺口，实测不是；真东西是两处键错位**

### 4.1 🔴 先纠正一个判断（**这是本档案里最容易抄错的一段**）

**初判**：settings 有 **54 处 `t()` 调用 / 46 条去重文案**，却**没有自己的 `i18n/` 目录** ⇒ 判「缺 46 条译文，要补 `i18n/en.json`」。

**这个判断是错的。** 错在**把 i18n 字典当成了「插件私有财产」**。

> **LinkDesk 的 `t()` 走的是壳提供的同一个 react-i18next 实例**——各插件 `contributes.i18n` 声明的字典**汇进同一个全局 bundle**。所以 **settings 的文案可以由任何插件的字典提供**，不需要自己有目录。

**实测（2026-09-11）**：settings 的 46 个去重 key 里，**44 个已在 [`plugins/lang-defaults/en.json`](../../../../plugins/lang-defaults/en.json)（443 key）**里——那是**随壳发货的默认语言包**（`lang-defaults` 是一只普通插件，`en.json` + `zh.json` 都在）。

**⇒ settings 不需要补 `i18n/`。** 佐证：`npm run check` 里的 `audit-i18n.mjs --strict` **本来就报「缺翻译 0」**——**门禁一直是绿的，是我一开始没用门禁判。**

> 🔴 **教训（写给下一个 AI）**：判「某插件缺不缺译文」**不要**数它有没有自己的 `i18n/` 目录——**跑 `node scripts/audit-i18n.mjs`，那是唯一权威**。
> 🔴 **python 也同理**：它**0 处 `t()`**（没有 UI 文案），没 i18n 目录是对的。**两只都不是缺口。**

### 4.2 但顺着这条线抓出**两个真 bug**

#### B-i18n-1｜`t("newPattern")`——把「数据标识符」错当「UI 文案」翻 🔴

[ObjectEditor.tsx:39](../../../../plugins/settings/src/views/SettingsView/ObjectEditor.tsx)：

```ts
const baseKey = t("newPattern");     // ← 这是「点 + 新增一行」时给新行起的默认键名
```

**三条问题，任一条都够立案：**

1. **它根本不是 UI 文案**，是**用户配置里的一个键名**（`newPattern` / `newPattern1` / `newPattern2`…）——**数据标识符不过翻译**。
2. **全仓任何字典都没有这个 key** ⇒ 靠 `parseMissingKeyHandler` 回落成字面量 `"newPattern"`。**它现在能用纯属巧合。**
3. 🔴 **陷阱**：**只要有人真的补一条 `"newPattern": "新条目"`，新增行的键名就变成「新条目」并写进用户配置**——**「补翻译」= 制造数据损坏。**

**修法**：`const baseKey = "newPattern";`（去 `t()`）+ 一行注释说明「这是数据键名，不是文案」。

**顺带暴露一个门禁盲区**：`audit-i18n.mjs` **只扫中文**（设计如此，作者可用任意语言当 key）⇒ 这种**标识符形态的 `t()` key 它完全看不见**。扩域候选见 [06 号档案](06-门禁扩域与验收.md)。

#### B-i18n-2｜`t("加载中...")`——3 个 ASCII 点 vs 1 个省略号字符 🟡

[SettingsView.tsx:195](../../../../plugins/settings/src/views/SettingsView.tsx)：`t("加载中...")` 用的是**三个英文句点**；而字典里的 key 是 **`加载中…`（U+2026 单字符省略号）**。**两者不是同一个字符串。**

- **现在之所以英文能显示**，是因为 `plugins/marketplace/i18n/en.json` 里**恰好有一条三点版本** ⇒ **settings 的这句文案在靠 marketplace 的字典活着**。
- **marketplace 若被卸载** ⇒ 英文模式下 settings 显示中文「加载中...」。
- `audit-i18n.mjs` **也没报**——它比对的是**全插件字典的并集**，marketplace 那条把它盖过去了。

**修法（推荐 (a)）**：**(a)** 调用方改成 `t("加载中…")`——字典已有，**改一处**；**(b)** 在 `lang-defaults/en.json` 再补一条三点 key（**不推荐**：三点写法本来就是笔误，补它 = 把笔误固化）。

### 4.3 结论

- **不补 `i18n/`**，**只修上面两个键**。
- **版本**：B-1 零行为变化（键名字面量不变），**B-2 是用户可见**（英文模式显示中文）⇒ 取最高 ⇒ settings **`1.0.4 → 1.0.5`（`fix:`）**。

---

## 五、④ 短 README 一并补（🟢 **用户 2026-09-11 拍板：补** ——「反正迟早要补」）

**对象（2026-09-11 实测 `ls plugins/*/README.md`——全仓 11 只没有 README）**：

| 类 | 插件 | 补什么 |
|:--|:--|:--|
| 代码·演示 | `floating-panel-demo` / `panel-demo` | 简版 README（能看懂它是干什么的）+ `CHANGELOG.md` 一行 `## v1.0.0（初始版本）` |
| 纯数据·主题 | `theme-defaults` / `theme-mint-soda` / `theme-panorama` / `theme-pill` / `theme-songti` / `theme-twilight-forest` / `theme-zones`（**7 只**） | 同上（说明偏色/风格取向，一句话即可） |
| 纯数据·语言包 | `lang-test-ja` | 同上（说明是测试语言包） |
| **代码·正式插件** | **`marketplace`** | **不在此子任务**——它走 [§3.4](#34-marketplace-的-readme--封面) / 任务 **92c** 的**完整** README + 专属封面；`CHANGELOG.md` 走 **92a**（27 条） |

> 🔴 **订正（2026-09-11 实测，本档案初稿错了三处）**：初稿写「11 只 = 2 demo + 8 theme + `lang-test-ja`」，凭**印象**列名单，实测后：① 主题实为 **7 只**不是 8 只；② 名单里的 **`theme-iconset-pastel` 本来就有 `README.md`**（还带 `LICENSE.md`），**不该在列**；③ 反而**漏了 `marketplace`**——它是全仓唯一没有 README 的**代码**插件，然而它另有大任务，**差点被当成「空插件」一笔带过**。
> **教训**：**凡是「哪些文件缺」的问题，先实测（`ls plugins/*/README.md`）再写名单**——印象列名单 = 必漏必错（承 memory `ai-pre-commit-checklist` 的「改 N 个漏 M 个」）。**门禁 G3 挡不住这类错**（它验的是脚手架生成物，不是仓内插件），只能靠「写名单前先跑一遍」这条纪律。

**为什么值得补**：M4 要求「任一官方插件的目录形态一致」——**缺 README 的插件本身就是 11 个不一致的范本**；且市场详情页的「详情」页签直接读 README（[12 档案](../../../03-插件制造/12-README说明区媒体契约.md)），**没有 README = 详情页空白**。

**独立子任务（任务 92e）**——不与 49 条搬家混在一起，**可单独砍而不影响 92a-92d**。

---

## 六、版本轴判定（🔴 逐只算，别一刀切）

**判据**：`version` 表示「插件对用户变了吗」。本轮的变更是**元数据存放位置 + 补缺失资产**——用户装上后**看到的东西变了**（更改日志页签从「未附带」变成有内容；marketplace 详情页从空变成有 README）。

⇒ **补了内容的插件 = 用户可见变化 = bump**；**只搬位置的**……

| 插件 | 用户可见变化 | 判定 |
|:--|:--|:--|
| **marketplace** | 详情页「详情」页签从空 → 有内容；「更改日志」页签 27 条从无 → 有 | **bump**（`1.0.27` → `1.0.28`，`fix:`） |
| **settings** | 英文模式下「加载中...」从中文 → 英文（B-i18n-2）；B-i18n-1 零行为变化 | **bump**（`1.0.4` → `1.0.5`，`fix:`） |
| editor / file-tree / python / serial-monitor / lang-defaults / theme-aurora-glass / theme-terminal | 更改日志页签从「未附带」→ 有内容 | **bump**（各 +1 patch，`fix:`） |
| 11 只空插件（若做 ④） | 同上 | **bump**（各 +1 patch） |
| **软件轴** | 无变化 | **不动**（0.1.47 保持） |

> ⚠️ **不打包上新版本来做这件事的例外**：若某只插件的 `CHANGELOG.md` 内容**完全等于**它 `plugin.json` 里原有的 49 条、且该插件**本来就是 bundled 随壳发货**——bump 的后果是「bundled 版本号变了但 zip 内容也变了」，**是应该 bump 的**（承 [12-插件目录与UI细节 §一·五](../03-插件市场/12-插件目录与UI细节.md)「plugin.json 同步铁律」）。
> **判定现算**：`node scripts/check-bundled-version-bump.mjs` 三档（`identical` / …）——**产物真值说了算，不是这条笔记说了算**。

---

## 七、验证（本轮收工判据）✅ 2026-09-11 全过

1. **搬家对账**：§2.2 表格**逐只数字全中**（段数 + bullet 总数），命令留档。
2. **`plugin.json` 零残留**：`grep -rn '"changelog"' plugins/*/plugin.json` → **空**（9 只全清）。
3. **`CHANGELOG.md` 可被现有读取方消费**：起一次实机（或直接 `readInstalledPackageFile` 路径复核），详情页「更改日志」页签**真的显示内容**，不是「该插件未附带更改日志」。
4. **marketplace 详情页**「详情」页签显示 README 正文 + 封面图**在 `linkdesk://` 协议下真加载**（承 [#70a 的资产随包纪律](../03-插件市场/12-插件目录与UI细节.md)）。
5. **settings 两处键错位已清**：`grep -rn 't("newPattern")\|t("加载中\.\.\.")' plugins/settings/` → **空**；切英文实机看一遍设置页（含对象编辑器点「+」新增一行，确认键名仍是 `newPattern`）；`node scripts/audit-i18n.mjs` 仍报 **0 缺**。
6. `npm run check` 全绿；各插件 zip **重新构建**（[12 §五 纪律](../03-插件市场/12-插件目录与UI细节.md)：zip 是一次构建产物，不自动重生）；**打包版验收走一次性 `--user-data-dir`**（memory `bundled-same-version-no-refresh`）。

### 收官结果（2026-09-11，✅ 全过）

六条判据**逐条实测通过**，提交 `b4f994c0d`。关键实测数（**均与本文档预估有出入的地方一律以实测为准**）：

| 判据 | 实测 |
|:--|:--|
| ① 搬家对账 **段数** | `27/8/5/3/2/1/1/1/1 = 49` ✅ 逐只全中 |
| ① 搬家对账 **bullet 总数** | `29/11/5/3/2/1/1/1/1 = **54**` ✅ ——🔴 **§2.2 表第二列写 49 是估数，真实 bullet 总数为 54**。**段数对不代表 bullet 没丢，两列都核过才作数**（本文档 §2.2 脚注早就这么要求，实测兑现了它的价值） |
| ② `plugin.json` 零残留 | `grep -rn '"changelog"' plugins/*/plugin.json` → **空** |
| ③ 更改日志页签真出内容 | 实机 CDP：`lang-test-ja` / `marketplace` 两页签**都真出内容**，`mpd-chg-empty` 为 null |
| ④ marketplace 详情页 | README 正文渲染 + `linkdesk://marketplace/resources/cover.svg` **640×640 真加载** |
| ⑤ settings 两键已清 | `grep -rn 't("newPattern")\|t("加载中\.\.\.")' plugins/settings/` → **空**；`audit-i18n.mjs --strict` **0 缺** |
| ⑥ `npm run check` | **REAL_EXIT=0**（152 文件 / **2060 测试**，`check-bundled-version-bump` 绿 = 18 个 zip「内容变了且 bump 了」） |

### 🔴 执行时逼出的四条订正 / 新知识（比任务本身更值钱）

1. **短 README 是 10 只，不是 11 只。** §一 ④ 与 §五 的「11」是**全仓缺 README 的总数**（含 `marketplace`），而 **92e 子任务只有 10 只**（marketplace 走 92c）。§一/§五 已按此改写。**根因是初稿凭印象列名单**——§五 的 🔴 订正框已记此教训，本轮**再次**验证：**名单类结论必须先实测再落笔。**
2. **`lang-test-ja` 的初始段是 `## v1.0.1（初始版本）`，不是 `v1.0.0`。** 该插件建包时 `version` 已经是 **1.0.1**——照 §五 表照抄 `## v1.0.0（初始版本）` **会凭空造一个仓里从来不存在的版本**。（这正是 memory `snapshot-shadows-truth-bug-class` 的①「快照遮蔽真值」：任务书写的是**写任务时**的快照，执行时真值已经漂了。）
3. 🔴 **新增一条本轮之后必须守的死规矩：bump 了版本，就必须在同笔写下对应的 `## v<新版本>` 段。** 理由不是文风——**`publish` 是按版本号严格匹配段标题来切 `versions[].changelog` 的**（[02 §91c](02-元数据链路归一.md)）。bump 而缺段 ⇒ 目录里该版本 changelog 为空 ⇒ 未装详情页显示「此版本未提供变更说明」——**正是本层立案要修的那个 bug，会被自己重新制造出来**。本轮 19 只每只都加了对应段，并用脚本逐只断言「`plugin.json.version` === `CHANGELOG.md` 首个 `## v…` 段头」，0 例外。**这条已写进 [06 门禁扩域](06-门禁扩域与验收.md) 的候选清单。**
4. **marketplace 的封面必须新画**（§3.4 的 (a) 路）：`icon-brand.svg` 是**身份图**不是**场景图**——两者的构图职责不同（身份图要能在 42px 图标栏里认出来，场景图要在详情页顶部铺开讲故事），复用会让说明区头重脚轻。已按 A 家族「货架的窗」形态画定，640×640，**颜色自含**（封面是美术资产，不受壳主题 token 约束）。

### 🔴 两条**越出本轮范围、只报不动**的发现（交用户裁定，不外推）

1. **`src/` 里还有三处三点省略号**：`hooks/usePoolSync/notif.ts:142` / `pool/shared/plugin-component/PluginComponent.tsx:208` / `pool/views/plugin-detail/PluginDetailViewHost.tsx:126`——**与 B-i18n-2 完全同病**（`t("加载中...")` 三点写法，字典里只有 U+2026 版本，靠 marketplace 的字典活着 ⇒ **卸载 marketplace 后英文模式露中文**）。**修它要动 `src/` = 软件轴**，与本层「零 `src/`（行为）改动」铁律冲突 ⇒ **不在本层修，报给用户**。
   > **对照（不是 bug）**：`marketplace` **自己源码里 7 处**三点省略号**自给自足**——实测 `加载中...`（三点）**全仓只存在于 `plugins/marketplace/i18n/en.json:27` 一处**，那正是它自己的字典；而 `加载中…`（U+2026）**全仓只存在于 `plugins/lang-defaults/en.json:141`**（随壳发货的默认语言包）。**判据是「这条 key 谁提供」，不是「字符串长什么样」**——两条 key 各归各家，谁都没缺。
2. **`theme-iconset-pastel` 有 `README.md` 但无 `CHANGELOG.md`**（§五 🔴 订正框里已经点名它「不在 92e 列」——因为**名单判据是「缺 README」**，它不缺）⇒ 它的「更改日志」页签**永远是「未附带」**。**要不要给它补一份 CHANGELOG，等用户点头**（memory `rule-extrapolation-not-consent`：名单判据是「缺 README」，把它外推成「也顺手补 CHANGELOG」是**换了一个判据**，必须重新征得同意）。
   > **本轮未改的诚实行为**：无 `CHANGELOG.md` 的插件在已装分支**仍显示「该插件未附带更改日志」**——这是**对的**，实机已复验（`theme-iconset-pastel` 即此例）。**「没有就说没有」比「编一句」好。**
