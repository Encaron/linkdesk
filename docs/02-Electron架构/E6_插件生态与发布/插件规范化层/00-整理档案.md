# 插件规范化层——总档案（L3.7）

> **立案：2026-09-11，用户四问（marketplace 更新日志为何在 plugin.json / 首次配置为何标准 / marketplace 是否历史遗留 / settings 空目录 + editor 图散落）→ 审计交付 → 用户拍板「同意你的想法，但是要立文档，加任务，同样放在第四层前面」。**
> **一句话：** 3.6 层把**文件**理干净了（谁多长、谁该拆）；这层理**元数据**——写的地方和读的地方**从来没对上**，中间还躺着三个「声明了、写了文档、填了类型、就是没人用」的死字段。**文件干净 ≠ 账目对上。**
>
> **定位：** 插在 L3.6（文件整理层）与 L4（端到端验证）之间。**L4 是拿真插件在安装版里跑通全链路——而「详情页读不到更新日志」「未装插件看不到说明」正是全链路上的一段。地基建完先对账，再跑车。**
>
> **🔖 非新能力文档（2026-09-11 判定）：** 本层**不新增任何 `window.linkdesk.*` 壳 API、不新增 contribute 贡献点**。改动只在三处：① `plugin.schema.json` **减两个字段**（changelog / readme）；② `@linkdesk/plugin-sdk` 的 `publish` **增加行为**（读工程内 `CHANGELOG.md` / `README.md` 自动写目录条目）——这是**作者工具链**，不是壳 API；③ 官方插件的**内容补齐**（补 `README.md` / `CHANGELOG.md` / 挪图）。故**无需 8 维度设计前置**，也**不需要在 03-插件制造 里新增 API 章节**（已在 [02 号档案 §六](02-元数据链路归一.md) 逐条核过「要不要新 API」，结论 = 不要）。

---

## 〇、本层做什么、为什么做

### 做七件事

| # | 动作 | 说明 |
|:--|:--|:--|
| ① | **规范先立** | 把「插件该不该有 README.md / CHANGELOG.md、放哪、和详情页什么关系」写进 [09-插件目录规范](../../03-插件制造/09-插件目录规范.md)——现在**一个字都没有** |
| ② | **元数据真源归一** | `plugin.json` 的 `changelog` / `readme` 两字段**删除**；`CHANGELOG.md` / `README.md` 是唯一真源 |
| ③ | **接通断链** | SDK `publish` 从工程内两个文件**自动写**目录条目的 `readmeUrl` / `versions[].changelog`——未安装的插件也有说明可看 |
| ④ | **官方插件补齐** | **9 只**插件补 `CHANGELOG.md`（**49 条记录搬家，不是重写**）；marketplace 补 `README.md`；settings 修两处 i18n 键错位 |
| ⑤ | **资源与目录归位** | editor 散在插件根的三张图归位 `resources/`；`theme-mint-soda` 补 `icon` 字段；资源随包规则盘清 |
| ⑥ | **脚手架换代** | `create-linkdesk-plugin` 模板补 README / CHANGELOG / resources / .gitignore / publish·lint——**生成出来的插件必须长得像官方插件** |
| ⑦ | **门禁扩域** | 空目录红灯 + 可疑 `t()` key 黄灯 + 脚手架形状红灯——**让这层修的东西下次不会再犯** |

### 为什么现在做——四条实打实的理由

**① 一条链路断成三截，每一截单独看都「没问题」。**

更改日志这条链路：

| 段 | 现状 | 单独看 |
|:--|:--|:--|
| **写** | 9 只插件把 **49 条**更新记录写进 `plugin.json` 的 `changelog` 数组（marketplace 一只 27 条） | 「有更新记录 ✅」 |
| **中间** | SDK 打包器**会**把 `CHANGELOG.md` 拷进 zip（[vite-config.ts:556](../../../packages/plugin-sdk/src/vite-config.ts)） | 「路修好了 ✅」 |
| **读** | 详情页「更改日志」页签**只读包内 `CHANGELOG.md`**（[ChangelogTab.tsx](../../../plugins/marketplace/src/views/detail/DetailView/ChangelogTab.tsx)） | 「有渲染逻辑 ✅」 |

**三段都「有」⇒ 净结果是一条都通不了。** 全仓 **0 个插件**带 `CHANGELOG.md` ⇒ 每只官方插件的「更改日志」页签都显示「**该插件未附带更改日志**」，**49 条记录一条都看不见**。

**② 三个死字段——「声明了、写了文档、填了类型、就是没人用」。**

| 字段 | 谁声明它有 | 谁读它 | 实况 |
|:--|:--|:--|:--|
| `plugin.json.readme` | [plugin.schema.json:85](../../03-插件制造/plugin.schema.json) + [06-plugin.json规范.md:225](../../03-插件制造/06-plugin.json规范.md) | **无人** | publish **读了又丢掉**——[publish.ts:251](../../../packages/plugin-sdk/src/publish.ts) 收进 `ManifestView.readme`，`buildCatalogEntry` 从不用它；**0 只插件填** |
| `plugin.json.changelog` | schema:219 + 06 规范:237 | **无人** | 49 条内容写进了一个没有任何读取方的数组 |
| catalog `versions[].changelog` | [select.ts:115](../../../plugins/marketplace/src/services/marketCatalog/select.ts)（读者在）+ publish.ts:45（类型在） | marketplace（**未装**分支） | publish **从不写**（[publish.ts:268](../../../packages/plugin-sdk/src/publish.ts) 只发 version/downloadUrl/publishedAt） |
| catalog `readmeUrl` | marketplace parse.ts:47（读者在） | marketplace（**未装**分支） | 只有官方目录手写才有，SDK publish 不产 |

**这直接踩用户原则第 4 条「没有死代码」。** 四个字段，四个都是「有类型、有文档、有注释、没读取方/没写入方」。

**③ 根因不是疏忽，是「两套机制从没对上」。**

一边设计成**文件**（`CHANGELOG.md` / `README.md`，对标 VS Code），一边实现成**清单字段**（`plugin.json.changelog` / `.readme`）。两边各通半截、各能自圆其说，**合起来一条也不通**。

判断「哪边对」的锚点不是偏好，是**一件事实**：详情页的读取代码**已经按文件写好了**（`readPackageFile(id, "CHANGELOG.md")`）。⇒ 文件是真源，字段是死路。**本层就是把实现往已经写好的那一侧收。**

**④ 唯一两边都对的样板，是手写的，脚手架造不出来。**

`first-run-setup`（「首次配置」，E6#73p）全仓**唯一**同时有 `plugin.json.changelog` **和** `CHANGELOG.md` 的插件——也是唯一「更改日志」页签真有内容可显示的。

但它**不是脚手架生成的**：它的 `resources/` / `CHANGELOG.md` / `src/{views,services,components,styles}/` / `publish` script **模板一个都没有**（见 [05 号档案 §一](05-脚手架换代.md) 对照表）。

⇒ **「上一个 AI 手写了一只标准插件」不等于「脚手架能造出标准插件」。** 这层的第 ⑤ 件事就是把这个差值补平。

---

## 一、现状体检（2026-09-11 实测，全部 `find`/`grep`/`git log` 回源）

### 1.1 元数据覆盖表

| 插件 | `plugin.json` 有 `changelog` | `README.md` | `CHANGELOG.md` | `resources/` | `icon` 指向 |
|:--|--:|:--:|:--:|:--:|:--|
| editor | 1 条 | ✅ | ❌ | ❌ | **`icon.svg`（根）** |
| file-tree | 3 条 | ✅ | ❌ | ✅ | `resources/icon-bar.svg` |
| **marketplace** | **27 条** | ❌ | ❌ | ✅ | `resources/icon.svg` |
| python | 1 条 | ✅ | ❌ | ✅ | `resources/icon.svg` |
| serial-monitor | 8 条 | ✅ | ❌ | ✅ | `resources/icon-bar.svg` |
| settings | 2 条 | ✅ | ❌ | ✅ | `resources/icon-bar.svg` |
| lang-defaults | 5 条 | ✅ | ❌ | ✅ | `resources/icon.svg` |
| theme-aurora-glass | 1 条 | ✅ | ❌ | ✅ | `resources/icon.svg` |
| theme-terminal | 1 条 | ✅ | ❌ | ✅ | `resources/icon.svg` |
| floating-panel-demo / panel-demo | 0 | ❌ | ❌ | ❌ | codicon 名 |
| 其余 8 只 theme-* | 0 | ❌ | ❌ | 部分 | 部分缺 |
| **合计** | **49 条** | 9 只有 | **0 只** | 15 只有 | — |

**读法**：`changelog` 一列 49 条**全部无效**（无读取方）；`CHANGELOG.md` 一列 0 —— **这一列才是详情页要的**。

### 1.2 时间线——「历史遗留」判据是日期不是感觉

| 插件 | 目录首提交 | 说明 |
|:--|:--|:--|
| **marketplace** | **2026-07-22** | 与 settings 并列最老 |
| **settings** | **2026-07-22** | 同上 |
| serial-monitor | 2026-07-28 | |
| editor / file-tree / python | 2026-09-05 | |
| **README.md 惯例成形** | **2026-09-09** | 7 只一起铺（E6#4a / #68 试水批） |

⇒ **marketplace 比 README 惯例早一个半月出生，不在那批里，之后没回头补——「历史遗留」成立。** 但**同一句不能推到 settings 的空目录上**（那是本轮 AI 自己的探针残留，见 [04 号档案 §〇](04-资源与目录归位.md)）。

### 1.3 与「唯一标准样板」的对照

| 维度 | first-run-setup | 其余官方插件 | 脚手架模板 |
|:--|:--|:--|:--|
| README.md | ✅ | 9/20 | ❌ |
| CHANGELOG.md | ✅ **全仓唯一** | **0/20** | ❌ |
| resources/ | ✅ `icon.svg` | 15/20 | ❌ |
| `plugin.json` 有 `changelog` | ✅（**两边都写 = 冗余但无害**） | 9/20 | ❌ |
| .gitignore | ✅ | 0/20 | ❌ |
| `publish` script | ✅ | 0/20（真插件走 monorepo 构建） | ❌ |

---

## 二、四条拍板（2026-09-11 用户逐条选定，全按推荐）

| # | 问题 | 拍板 | 落点 |
|:--|:--|:--|:--|
| ① | 更改日志以哪边为唯一真源 | **`CHANGELOG.md` 为准，删 `plugin.json.changelog` 字段** | [02](02-元数据链路归一.md) / [03](03-官方插件补齐.md) |
| ② | 未装插件的 README/日志怎么显示 | **接通——publish 自动写 `readmeUrl` + `versions[].changelog`** | [02 §四](02-元数据链路归一.md) |
| ③ | 脚手架 `src/` 预建文件夹吗 | **不预建**，改在生成的 README 里给目录契约表 | [05 §四](05-脚手架换代.md) |
| ④ | 模板的 `.vscode/settings.json` | **保留 + 加注释说明为什么** | [05 §三](05-脚手架换代.md) |

**判据一句话**：① 对标 VS Code（扩展的更新日志就是仓库里的 `CHANGELOG.md`）——GitHub 上直接能看、已装未装同一条路径、作者写 Markdown 不受 JSON 转义折磨；② 「装之前」恰恰是最需要看说明的时候，砍掉远端兜底等于把说明藏起来。

---

## 三、判据（本层拿它量东西）

**M1｜一个概念一处表达。** 同一件事（更改日志 / 说明文档）**只允许一个真源**——允许「文件 + 字段」并存即等于允许两套真相。

**M2｜声明必须有消费者。** schema 里出现一个字段 ⟺ 有代码读它**且**有代码写它。否则**删**（不是「留着以后可能有用」——那是 memory `no-deferral-future-excuse` 明令禁止的）。

**M3｜删字段必须给迁移出口。** 删 `plugin.json.changelog` 不能只说「已废弃」——49 条内容**必须搬进 `CHANGELOG.md`**（内容零丢失），且 SDK `validate` 对旧写法给**可读提示**（[02 §五](02-元数据链路归一.md)）。

**M4｜官方插件 = 教具。** [09 规范](../../03-插件制造/09-插件目录规范.md) 开篇原话「**官方插件模板——第三方开发者看任一官方插件，目录结构一致**」⇒ **脚手架生成物、官方插件、规范文档三者必须同形**。任何一处不一致都算本层的账。

**M5｜接通优先于砍掉。** 设计已存在、读取方已写好、只差写入方一段纯函数 ⇒ **接通**（②号拍板的判据）。只有「读也写也都没有、且不打算做」才砍。

---

## 四、风险清单（开工前过一遍）

| # | 风险 | 对策 |
|:--|:--|:--|
| R1 | **删 schema 字段后旧插件静默失效** —— 顶层 `additionalProperties: true`（实测），作者继续写 `changelog` **不会报错、只会被忽略** | [02 §五](02-元数据链路归一.md)：SDK `validate` 加**墓碑提示**（removed-fields 名单 + 替代写法）。名单**只有「已移除」一种语义**，不是第二份字段清单（必填/contributes 仍以 schema 为唯一真源——不违归一） |
| R2 | **`publish` 切段解析错版本** —— CHANGELOG.md 是自由 Markdown，切段判据必须稳 | [02 §四](02-元数据链路归一.md)：**只在 `## v<version>` 标题行切**（正则锚定行首 + 版本号严格等于 `manifest.version`）；切不到 → 该版本 `changelog` 留空（**诚实留空，不猜**） |
| R3 | **49 条搬家搬丢** | [03](03-官方插件补齐.md)：**逐插件对账**——搬完 `grep -c` 条数必须等于搬前（27/8/5/3/2/1/1/1），少一条即红灯 |
| R4 | **改 8 只插件 `plugin.json` = 插件轴 8 次 bump** | 各插件 bump 但不混账（memory `version-axes-separated`）；**软件轴不动**（本层软件侧只有 SDK publish 行为 + 文档） |
| R5 | **`first-run-setup` 是「两边都写」的样板，删字段会不会打脸样板？** | **不会**——它是**手写工程**且 `changelog` 是冗余无害的那一半；本层同步把它 `plugin.json.changelog` **删掉**（保留 `CHANGELOG.md`），让样板变成「只有文件」的干净形态。**它当前不在 `plugins/` 里**（只在 `E:\linkdesk-build+scratch\first-run-setup` + `dev-fixtures/*.linkdesk-plugin`），**源码不在本仓 ⇒ 改不到**，故列为**待办交接项**而非本层任务（**§五 ② 已拍「不搬」⇒ 交接是唯一路径**，见 [E6#91h](../E6-执行清单.md)） |
| R6 | **已装 / 未装的日志版式不对称**（已装 = 全文一个大块；未装 = 逐版本小块） | 是**既有设计**（04-详情页设计 §三），非本层引入。接通 `versions[].changelog` 后会**第一次变得可见** ⇒ **§五 ① 已拍：切法不动、只改标题**（[E6#92f](../E6-执行清单.md)） |

---

## 五、研究时冒出来的项 —— 拍板结果（2026-09-11 用户分三批答复，**10 项全部有结论，待答清零**）

### ① 已装 / 未装的「更改日志」版式要不要归一？

- **现状**：已装分支把包内 `CHANGELOG.md` **全文**渲染成一整个 Markdown 块（[ChangelogTab.tsx:86](../../../plugins/marketplace/src/views/detail/DetailView/ChangelogTab.tsx)），块头却写 `v{已装版本}` + 「已安装」——**底下其实是全部历史**；未装分支按 `versions[]` **逐版本**渲染。
- **为什么现在冒出来**：接通前未装分支基本是空的，**不对称看不出来**；接通后两个分支都有内容，一眼就能看出「同一个文件，两种切法」。
- **建议**：**先不动**。已装读全文是「本地权威」的设计（装的是哪个版本看哪个版本的说明），改它 = 改 UI 行为，要有自己的实机证据。**本层只把它记成已知不对称**，需要归一另立轮。

#### ① 说明（2026-09-11 用户反问「这是指要不要加 log.md，还是加了之后格式统不统一？」——**两个都不是**，请看这段）

**先澄清**：**「要不要给每个插件加 `CHANGELOG.md`」这件事本身已经定了**（四条拍板②：`CHANGELOG.md` 为唯一真源）——**不是**在问要不要加，也**不是**在问文件内容格式（内容格式由四条拍板②的搬运规则定死：`## v<版本>（日期）` + 逐条 bullet）。

**问的是第三件事：市场详情页「更改日志」页签，在两种状态下显示方式不一样。**

| 插件状态 | 现在怎么显示 |
|:--|:--|
| **已安装** | 把整份 `CHANGELOG.md` **全文一次铺出来**（[ChangelogTab.tsx:86](../../../plugins/marketplace/src/views/detail/DetailView/ChangelogTab.tsx)），**但块头写的是「v{当前版本}·已安装」——底下其实是全部历史** |
| **未安装** | 按 `versions[]` **一个版本一个块**地列出来 |

**同一个文件，两种切法。** 为什么会现在冒出来：**接通前未安装那一栏基本是空的，不对称看不出来**；等 3.7.2 把 publish 接通（未安装也有了内容），**一眼就能看出「上面写 v1.0.7、底下铺着 1.0.0 到 1.0.7 全部」这个错位**。

**所以问题的实质 = 要不要统一成同一种切法**（两选一）：
- **不动**（推荐）：已安装读全文是**有意的**设计（VS Code 也是这么干的：「你装的这版，说明就在你本地」）——**但要给它换个诚实的标题**（别写 v当前版本）；
- **归一**：两边都改成逐版本切。

### 🟢 **用户 2026-09-11 拍板：版式不动，但标题必须改**（选「不动 + 改标题」）

- **不动切法**：已安装照旧读全文、未安装照旧逐版本分段——**两边的渲染逻辑一行不改**。
- **改标题**：已安装分支那个 `v{已装版本}·已安装` 是**唯一必须修的**——它自称单一版本、底下却是全部历史，**标题在骗人**。换成诚实说法即可。
- **落点 = [E6#92f](../E6-执行清单.md)**（在 3.7.3 里做，marketplace 本就要动）；🔴 **动的是 UI 文案与版式 ⇒ 触发硬约束 16，先经 `Skill` 调设计 skill 再改**。
- **为什么这不算「归一」**：改的是一句话对不对，不是两种切法合不合并。**切法不对称本身被接受为已知事实**（记在这里，免得下次又当 bug 报）。

### ② `first-run-setup` 源码不在本仓 —— 🟢 **用户 2026-09-11 拍板：不搬（选 (b)）**

**用户原话**：「**当时就是为了尝试外部构建插件，不依赖软件主体，所以不要移入**」。

⇒ **这不是「不小心放在仓外」，是一次有意的验证**：`first-run-setup` 是**外部构建**（不依赖壳的仓库、不依赖 `npm run check`）跑通全链路的活证据。**它留在仓外是它的价值所在**，搬进来反而毁掉这个验证。（**与 memory `plugin-independence-iron-law` 同源**：第三方作者永不需要壳改代码——它就是这个承诺的实物。）

**由此产生的三条义务（写进来，别让「有意留外」变成「没人管」）**：

1. **它不受 `npm run check` 保护**——样板改坏了没人知道。**代价由用户手工维护承担，明确接受。**
2. **本层删 `plugin.json.changelog` 时够不着它** ⇒ **它会带着旧写法继续存在**。3.7.2 的墓碑警告会在**下次外部构建**时对它响一次——**届时由用户手工把它那几条搬进它自己的 `CHANGELOG.md`**（它本来就两边都写，所以是「删掉旧的、留新的」）。**列为 3.7.2 交付后要口头交代给用户的一条。**
3. `audit-i18n.mjs` 里 `plugins/first-run-setup/i18n/en.json` 这条路径**永远是死的**（插件不在 `plugins/` 下）⇒ **3.7.6 的 #95d 照删**，不是「等它搬进来就活了」。

### ③④ 研究过程中新冒出来的两项 —— 🟢 **用户 2026-09-11 均已拍板**

| # | 事项 | 决定 | 落点 |
|:--|:--|:--|:--|
| ③ | **editor 的 `cover-brutalist.svg`**——零引用，但上次拍板「留存」 | 🟢 **删**（先拷一份留档） | [04 §1.3](04-资源与目录归位.md) |
| ④ | **无 README 的插件要不要补** | 🟢 **补**（用户：「反正迟早要补」）——**注意实测后名单与本档初稿不同**（是 11 只、含 `marketplace`；`theme-iconset-pastel` 已有 README 不在列） | [03 §五](03-官方插件补齐.md) |

### ⑤ settings 的 hooks / CSS 摆法 —— 🟢 **用户 2026-09-11 拍板：不搬，只写规则**

**用户反问「是指摆成其他插件那样，有 utils / components 的格式吗？」——不是，见这段：**

**不是**要新建 `utils/` `components/` 这类文件夹。**问的是「同一种文件放的位置不一样」**，而且是两处独立的、**都很小**的差异：

| 差异 | settings（老） | 其余插件（新） | 影响 |
|:--|:--|:--|:--|
| **CSS 放哪** | `src/views/*.css`（和视图混在一个夹） | `src/styles/*.css`（样式单独一个夹） | 纯位置，**功能零影响** |
| **hook 放哪** | 4 个 IPC hook 集中在 `views/hooks/` 一个夹 | 跟各自视图**同夹**（如 `views/SettingsView/useSettingsEvents.ts`） | 纯位置，**功能零影响** |

**所以两个选项都很轻**：
- **不动**（推荐）：**只把规则写下来**（新文件按新摆法走，存量不回填）——因为**这两处差异不影响任何功能**，回填要改 4 个文件的 import 路径，**收益小、风险有**；
- **回填**：把 CSS 挪到 `src/styles/`、hook 分散回各视图夹，**单独立轮**（不塞进 3.7.4）。

### 🟢 **裁决：不搬，只写规则**（用户 2026-09-11）

- **存量不动**——4 个 hook 留在 `views/hooks/`、4 份 CSS 留在 `src/views/`，**一个文件都不挪**。
- **规则已经写下了，不必再补任务**：CSS 规则 → [05 §2.7 + §四 契约表](05-脚手架换代.md)（**多份 CSS → `src/styles/`；单份且与入口同夹也可**）；hook 规则 → [04 §4.3](04-资源与目录归位.md)（**跨视图共享的域逻辑（含 IPC hook）→ `src/services/`；单视图专属 → 与视图同夹**，新文件按这条走）。
- **为什么不做**：这两处差异**功能零影响**，回填要改 4 个文件 import 路径，**收益小、风险有**；且本轮 3.7.4 已有 editor 三图归位这个真搬家，别在低收益项上分心（memory `reduce-parallel-subagents` 同理：一次做透一件）。

### ✅ 已拍板汇总（用户 2026-09-11 答复）

| 四条已定（更早） | 本轮新增 |
|:--|:--|
| ① `CHANGELOG.md` 唯一真源、删 `plugin.json.changelog` 字段<br>② 接通 publish（写 `readmeUrl` + `versions[].changelog`）<br>③ 脚手架 `src/` 不预建文件夹，README 写目录契约<br>④ 模板 `.vscode/settings.json` 保留 + 加注释 | ⑤ `first-run-setup` **不搬进仓**（有意的外部构建验证）<br>⑥ editor 的 `cover-brutalist.svg` **删**<br>⑦ 无 README 的插件 **补**（11 只，可单独砍）<br>⑧ **npm 发布批准**（见下面 ⑥） | ⑨ 日志版式**不归一，但标题必须改**（已安装分支那个 `v{已装版本}` 在骗人 ⇒ E6#92f）<br>⑩ settings 摆法**不搬，只写规则**（规则已在 05 §2.7 / 04 §4.3）<br>🟢 **待答清零——10 项全部有结论** |

### ⑥ npm 发布（🟢 用户 2026-09-11：「记得到时候更新后顺便上传到 npm 上，这个是我同意的」）

**背景**：`@linkdesk/contracts` 0.1.3 / `@linkdesk/plugin-sdk` 0.1.6 / `@linkdesk/ui` 0.1.2 / `create-linkdesk-plugin` 0.1.0 **四个包都已真实发布在 npm 上**，且版本与本地一致（2026-09-11 实测 `npm view`）。

**本层要发的**：

| 包 | 版本 | 触发轮 | 发什么 |
|:--|:--|:--|:--|
| `@linkdesk/contracts` | 0.1.3 → **0.1.4** | **3.7.2** | 重生成的 `linkdesk.d.ts`（删掉 `changelog` 字段） |
| `@linkdesk/plugin-sdk` | 0.1.6 → **0.1.7** | **3.7.2**（接通 + 墓碑警告）／**3.7.4**（删冗余 icon 拷贝） | 打包器 + `validate` |
| `create-linkdesk-plugin` | 0.1.0 → **0.1.1** | **3.7.5** | 模板 v2 |
| `@linkdesk/ui` | **不动** | — | 本层不碰共享组件 |

**🔴 三条硬约束：**

1. **🔴 现在发不了——npm 未登录**（2026-09-11 实测 `npm whoami` → `ENEEDAUTH`）。**发布需要用户交互式 `npm login`（含 OTP）**，AI 无法代劳 ⇒ **到 3.7.2 时须用户在场配合**（或提供 token）。见 memory `user-friction-one-time-auth-setup`。
2. **发完必须跑 `npm run release:mark`**——把新版本+内容哈希写进 `scripts/npm-release-state.json` 消除黄灯。**不发就别动版本号**（黄灯判定 B：版本动了没 mark = 真发了没 mark，还是没打算发却动了版本号）。
3. **🔴 新发现的缺口 → 补进 3.7.6 门禁**：`scripts/check-npm-release.mjs` 的 `PACKAGES` **只盯 `@linkdesk/contracts` + `@linkdesk/plugin-sdk`**，**`create-linkdesk-plugin` 和 `@linkdesk/ui` 在 npm 上已发布却完全没有基线** ⇒ 脚手架模板改了不发版，**第三方作者拿到的还是旧模板，且没有任何灯会亮**。**脚手架恰恰是最典型的「作者面」**。⇒ 3.7.6 加一条：把这两个包纳入 `PACKAGES`。

---

## 六、本层档案

| 档案 | 管什么 |
|:--|:--|
| [00-整理档案.md](00-整理档案.md)（本文件） | 总则 / 诊断 / 判据 / 风险 / **10 项拍板结果** |
| [01-规范文档对齐.md](01-规范文档对齐.md) | 03-插件制造 三份文档要写什么、写到什么程度 |
| [02-元数据链路归一.md](02-元数据链路归一.md) | schema / 类型 / 契约 / SDK publish —— 含**可能的 bug** 逐条排查 |
| [03-官方插件补齐.md](03-官方插件补齐.md) | 49 条搬家对账 + marketplace README + **settings 两个 i18n 真 bug**（原判「缺 i18n 目录」已证伪） |
| [04-资源与目录归位.md](04-资源与目录归位.md) | editor 图归位 + **资源随包六类规则** + `theme-mint-soda` 缺 `icon` + 空目录事故复盘 |
| [05-脚手架换代.md](05-脚手架换代.md) | template v2 逐文件 + `.vscode` 理由 + 目录契约表 + CLI 文案 |
| [06-门禁扩域与验收.md](06-门禁扩域与验收.md) | 三条新门禁（各带自审三闸）+ 不加的四条 + 全层总账/验收 |

**← 上一层：** [文件整理层/00-整理档案.md](../文件整理层/00-整理档案.md) · **→ 下一层：** 第 4 层（端到端验证，memory `e6-l4-deferred` 用户暂缓）
