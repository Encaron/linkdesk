# 批次 6 处置档案 · 六大主题族归并 + 索引重写

> **执行日期：** 2026-09-12 起
> **范围：** `01-分批执行路线.md` 批次 6 —— 六个子步（6a–6f 归并 + 6g 索引重写 + 6h 校验），**每子步独立审议门 + 独立提交**
> **通用节奏（每子步）：** ① 通读全集 → ② 出「处置表 + 新文件目录大纲 + 原文件清单」→ 🚫 **审议门（报告 + 探讨 + 放行）** → ③ 写合并文件 → ④ 核对无遗漏 → ⑤ 删除 → ⑥ 校验 → ⑦ 档案 + 提交

---

# §6a · `bug-atlas.md`（跨时代 bug 图谱）

> **记忆仓提交：** `a350c5b`（消息含「批次6a」）· **本步前回滚点：** `bd2a595`（批次 5）
> **结果：** 折入 **16 份 + 附 2 份** · 删 **18** · 新增 **1** · 保 **1** · 修引用 **9 文件 / 9 行** · 全库 **200 → 183 文件**（净 −17）· 体积 **1012K → 954K**（本步净 **−58K**）

## 一、审议门结论

- 2026-09-12 本会话呈现 6a 报告（原文件清单 19 份 + 新文件目录大纲 + `v3-pitfalls` B1–B83 过筛法 + 三项待裁）→ **用户「开始」放行**（无推翻项，即采纳推荐：方案甲 / 保 `invisible-bugs` 独立 / `e4v44`·`e4-v46` 折入后删）。
- **用户三项裁定的采纳：**
  1. `bug-atlas.md` = 跨时代通用图谱；`multi-webview-bug-atlas.md` **保留独立**（多 WebView 时代专属，与 `multi-webview-era.md` 配对——**方案甲**）。
  2. `invisible-bugs-lesson-59c.md` **保留独立**（硬约束 13/14 原文出处，TF.5 要求可检索）。
  3. `e4v44-emptycontent-deadlock` / `e4-v46-viewcontainer-persistence` 折入后删。

## 二、原文件清单与处置（19 份通读）

| # | 文件 | 前 | 判定 | 去向 / 理由 |
|:--:|:--|--:|:--:|:--|
| 1 | `v3-pitfalls.md` | 50.3K | **拆+并** | §F（B1–B83 逐条过筛，见 §三） |
| 2 | `seam-bugs-are-real-bugs.md` | 4.6K | 并 | §A1（接缝通则 4 案例）+ §A2（Nullish vs Falsy） |
| 3 | `uninstall-bug-recurring.md` | 5.4K | 并 | §A3（9 轮历史 + `fs::rename` 根因） |
| 4 | `vite-glob-cache-seam-bug.md` | 3.9K | 并 | §A4（构建时 glob vs 运行时） |
| 5 | `phantom-transitive-browser-polyfill.md` | 2.3K | 并 | §A5（隐形传递垫片，E6#16） |
| 6 | `global-ipc-channel-isolation-pattern.md` | 4.3K | 并 | §C1（含 E4 文件树 10s 缩回完整追凶） |
| 7 | `monaco-addcommand-global-trap.md` | 2.4K | 并 | §C2（Monaco 全局键位陷阱） |
| 8 | `titlebar-drag-region-fixed-overlay-bug.md` | 1.4K | 并 | §C3（OS 级拖拽区） |
| 9 | `react-stale-closure-setstate-invoke.md` | 2.4K | 并 | §B1（B86） |
| 10 | `vite-hmr-boot-side-effect-staleness.md` | 2.0K | 并 | §B2 |
| 11 | `monaco-editor-options-not-applied.md` | 5.1K | 并 | §B3（诊断签名 + 4 层防线） |
| 12 | `monaco-dark-font-race-condition.md` | 2.3K | 并 | §B4 |
| 13 | `plugin-reinstall-tab-bar-disappear-bug.md` | 2.0K | 并 | §B5 |
| 14 | `editable-element-dom-check.md` | 1.7K | 并 | §D2 |
| 15 | `title-empty-string-no-remount.md` | 1.5K | 并 | §D1 |
| 16 | `babel-parser-caveats.md` | 2.6K | 并 | §E（Babel 解析 TSX） |
| 附1 | `invisible-bugs-lesson-59c.md` | 5.4K | 🟢 **保留独立** | 硬约束 13/14 原文出处 |
| 附2 | `e4v44-emptycontent-deadlock.md` | 1.8K | 并 → 删 | §D3（监听不该挂在会卸载的 UI 组件上，`bdd26d2`） |
| 附3 | `e4-v46-viewcontainer-persistence.md` | 2.2K | 拆 → 删 | §F 附（重置按钮三套系统互踩 12 commit 教训；其余死栈） |

## 三、`v3-pitfalls` B1–B83 逐条过筛结果

**筛法：** 逐条问「这条是否只在 Tauri / CM6 / WebView2 下成立？」是 → 死栈删；否 → 铁律留（压成一行进 §F 表）。**未整段砍。**

### 已废止（Tauri / WebView2 专属）

`B4–B9`（CM6 API 初学坑：`DecorationSet` 迁移 / 装饰位偏移 / `search()` 扩展依赖）· `B11`（Tauri `listen()` —— generation counter 已升华**硬约束 5**，见 [[hard-constraints]]）· `B36`（Tauri `capabilities` fs 权限）· `B43`（HTML5 DnD 在 Tauri/WebView2 不可靠；Electron/Chromium 下可用）· `B47`/`B48`（Tauri `_useLocalStorage` 竞态 / identifier 改名丢数据）· `B49` 的 Tauri 部分（`e.key` 不可靠；**"全局快捷键放 App.tsx capture + 用 `e.code`"保留**）· `B61`（Rust `read_loop` 硬编码 UTF-8；**"三处编码值必须对齐"保留**）· `B83`（Tauri `window.confirm` 静默返回 false；**`showConfirm` 已落地**）

### 跨时代铁律（已进 §F，按组）

| 组 | 条目 |
|:--|:--|
| 布局/CSS | B1–B3（`.form-row` 归一化）· B29/B30/B41（22px 状态栏 / `1fr auto` / 标签宽）· **B46（颜色硬编码→变量）** · **B56（`var(--x)` 必须有定义）** · B71（窄窗 label 竖排） |
| React 结构 | **B42（Rules of Hooks）** · **B22/B33（跨父节点移动必 unmount→绝对定位平铺）** · B12（不捏造调用方不产的数据）· B13（组变空路径遍历）· B14–B17（照抄 VS Code drop zone）· B21（方向信息全程保留）· B44（Activity Bar 无双击）· B80/B81（DOM 事件模型优先于 React 猜） |
| 注册/归一化 | B45（别硬编码 switch）· **B50（注册幂等）** · B51（动态 import `.then()` 晚于 mount）· B52–B54（浮层 portal/blur）· **B62（右键菜单归一化）** · **B79（壳级功能放壳里）** · B27–B32（出厂图标 / 删前建替代 / 不画图标） |
| 持久化/异步 | B55/B63/B64（统一配置应用管道）· B65（`enum` 约束）· **B57/B59/B60（存值 ≠ 显值）** · B66–B68（两服务写同文件 / 入口唯一）· B69/B72（兜底读死代码 / 唯一真源）· B70（卸载 `unregister*`）· B73（重构 grep 零残留）· B74/B75（Vite 冷构建 / watch.ignored）· **B76/B77（加载路径分流 / 同步写内存+异步落盘）** · B82（模块级计数器恢复后同步）· **B84（AI 重构留下的 magic value）** · 步5+6（端口自动填充 / 假 COM 口 / `<select>` 暗色） |
| 调试方法论 | B49 保留（capture handler + `e.code` + `stopImmediatePropagation`）· B70–B72 教训（**一个 bug = 一个最小修复**）· 持久化 4-grep 扫描 · **全层铁律**（改一层架构必须列出所有内层逐层检查） |

## 四、引用修复（9 文件 / 9 行）

| 文件 | 原文 | 改为 |
|:--|:--|:--|
| `appearance-audit-archive.md:154` | `[[v3-pitfalls]]` | `[[bug-atlas]]` |
| `configuration-applier-pattern.md:82` | `[[v3-pitfalls]] B62-B65` | `[[bug-atlas]] B62-B65` |
| `design-decisions.md:40` | `[[v3-pitfalls]] B66-B69 章` | `[[bug-atlas]] B66-B69 章` |
| `quality-commandments.md:214` | `[[v3-pitfalls]]` | `[[bug-atlas]]` |
| `vscode-source-reference.md:184` | `[[v3-pitfalls]]` | `[[bug-atlas]]` |
| `evolution-chronicle.md:51` | `[[global-ipc-channel-isolation-pattern]]` | `[[bug-atlas]] §C1` |
| `evolution-chronicle.md:52` | `[[monaco-dark-font-race-condition]]` | `[[bug-atlas]] §B4` |
| `plugin-lifecycle-reinstall-contract.md:22` | `[[vite-glob-cache-seam-bug]]` | `[[bug-atlas]] §A4` |
| `MEMORY.md` | 4 行（L46/L57/L58/L59）含 12 个死链 | 收敛为 3 处 `[Bug Atlas](bug-atlas.md)` |

> **被删文件内部的互链**（如 `vite-glob-cache-seam-bug → [[seam-bugs-are-real-bugs]]`）随文件删除自然消失，无需修。

## 五、新文件结构（`memory/bug-atlas.md`，378 行）

```
§0 三家族总纲（接缝/时序/全局）+ 判据三句 + git 二分
§A 接缝：A1 通则 4 案例 · A2 Nullish vs Falsy · A3 卸载 9 轮 · A4 Vite glob · A5 隐形垫片
§B 时序：B1 stale closure(B86) · B2 HMR · B3 Monaco options(4 层防线) · B4 Monaco dark · B5 重装标签栏
§C 全局：C1 全局 IPC（10s 缩回）· C2 Monaco addCommand（0.55 源码三处实锤）· C3 TitleBar drag region
§D 组件陷阱：D1 title="" remount · D2 isEditableElementFocused · D3 e4v44 死锁
§E 工具链：Babel 解析 plugins/ TSX（箭头+三元+JSX / 模板串+注释）
§F V3 铁律 B1–B83（已废止段 + 5 组铁律表 + E4 ViewContainer 附）
§G 方法论：git 二分 · 三类引用 grep · 持久化 4-grep · 接缝 checklist · 诊断签名
```

**保真核验：** 9 轮卸载 commit（`f9a2570`…`aec1564`）· B86 端口竞态 · Monaco `427217c`/`a129288`/`99784f2` + 4 层防线 · addCommand 源码行号（`standaloneCodeEditor.js:84-93/108/118/137`、`keybindingResolver.js:275-284`）· 10s 缩回三层追凶 · E6#16 `5802e3234` · Babel 4 类限制 **全部在**。

## 六、校验

| 项 | 结果 |
|:--|:--|
| 文件总数 | **183**（200 − 18 + 1） |
| 总体积 | **954K**（0.93MB；本步净 **−58K**） |
| `[[wiki]]` 断链 | **31**（批 5 为 34；**本批未新增**，减少 3 条系被删文件内的旧断链） |
| `(x.md)` 内部断链 | **1**（`e6-review-rhythm` → 旧仓库结构 `linkdesk\linkdesk\docs\…`，既存非本批） |
| 孤儿文件 | **17**（批 5 为 20；本步减少 3 = 被删的 3 份无入站文件） |
| `git diff --cached --stat` | `27 files changed, 391 insertions(+), 1362 deletions(-)` |

## 七、MEMORY.md 处理（blob 重建 + 双向重放）

- HEAD 版与工作树版**行折叠方式不同**（并行 E6 agent 重排过）→ 首次分段法在 HEAD 上**误伤 `Invisible Bugs` 行**（其 `）/ [Seam Bugs]` 分隔符缺空格，被整段吞）。**改用精确串替换**，并核验「残留死链 0 / `Invisible Bugs` 保留 true / `Bug Atlas` 出现 3」。
- 手法沿用批次 4/5：`git show HEAD:memory/MEMORY.md` → Node 替换 → `git hash-object -w` → `git update-index --cacheinfo`（blob `34492b51…`），**只纳入我方 hunk**；同时对工作树重放同款改动，防并行 agent 回退。

## 八、遗留 / 待用户裁

- **批次 5 三份超体积**（`snapshot` 15.4K / `e6-review` 9.7K / `design-skills` 10.7K，合计 +7.8K）仍待用户一句「接受超出」或点名删项。
- **31 条 `[[wiki]]` 断链 + 17 份孤儿** → 移交 **T6h** 归零（T6g 索引重写会覆盖大部分孤儿）。
- 并行 E6 agent 未提交改动原样保留（`MEMORY.md` 顶部、`cdp-ui-automation`、`dev-environment`、`e6-l4-deferred`、`e6-l5-handoff`、`flaky-full-suite-tests`、`linkdesk-is-universal-container` + 3 份未跟踪新文件）；本步提交只纳入我方 27 项。
- 临时脚本已清理（`.b6*.cjs` / `.b6*.txt` / `.b6mem_head.md`）。

---

# §6b · `plugin-authoring-manual.md`（插件制造手册）

> **记忆仓提交：** `818fed9`（消息含「批次6b」）· **本步前回滚点：** `a350c5b`（6a）
> **结果：** 折入 **9 份** · 删 **9** · 新增 **1** · 保 **3** · 修引用 **10 文件 / 10 行** · 补链 1 处 · 全库 **183 → 175 文件** · 体积 **954K → 947K**

## 一、审议门结论（2026-09-12，用户「开始」放行）

四项待裁全部采纳建议：

1. **`plugin-api-audit-checklist` 过时台账删** ✅——「仍在 import 裸奔 / 待迁移」表是 E5#61-#67 时点台账，`plugin-import-exceptions` 自述「E5#54c + E6#15h 已全部清干净——今天再照那张表去 grep 会一无所获」（历史版本 git `b6e5048`）。手册只留**当前 API 面 + 审计方法**。
2. **`api-preference-existing-first` 折入** ✅——两段用户原话**逐字保**进 §5。
3. **六类插件表的「新卡片类型」行标注** ✅——「原 CardRegistry 骨架已随 E5.7#45.7 整删；未来重建**不得进标签页系统**（硬约束 3）」。
4. **能力对比表压缩** ✅——Phase 4 时点的「vs VS Code」对比列删除，压成 §1.7 能力全景表。

## 二、原文件清单与处置（9 份 + 3 份 🟢 交叉核对）

| # | 文件 | 前 | 判定 | 去向 |
|:--:|:--|--:|:--:|:--|
| 1 | `plugin-system.md` | 4.0K | 并 → 删 | §0（插件性定义 / 六类插件 / 无 API 白名单 / 命名 / 生命周期） |
| 2 | `plugin-dev-flow.md` | 4.2K | 并 → 删 | §1（四样板 + 安装流转 + 能力全景） |
| 3 | `plugin-lifecycle-reinstall-contract.md` | 2.1K | 并 → 删 | §0.1（「可卸载 ⇒ 可重装」契约，用户原话逐字保） |
| 4 | `plugin-schema-json-canonical-location.md` | 1.7K | 并 → 删 | §2 |
| 5 | `contributes-schema-versioning.md` | 2.9K | 并 → 删 | §3（含迁移表 + 测试要求 + 「不做什么」） |
| 6 | `plugin-api-audit-checklist.md` | 6.6K | 并（**部分删**）→ 删 | §4（API 面 + 审计 grep）；**过时台账删** |
| 7 | `api-preference-existing-first.md` | 2.9K | 并 → 删 | §5（两段用户原话逐字保） |
| 8 | `api-naming-mandatory-read.md` | 1.5K | 并 → 删 | §6 |
| 9 | `non-view-plugin-normalization-lesson.md` | 4.2K | 并 → 删 | §7（三规则 + 9 bug 速查） |
| — | `plugin-communication-standard.md` | 7.8K | 🟢 **保** | 通信唯一铁律（手册 §4 只放指针） |
| — | `plugin-independence-iron-law.md` | 9.1K | 🟢 **保** | 壳侧独立铁律（手册 §5 指针；本次因引用修复而改动 1 行） |
| — | `plugin-import-exceptions.md` | 8.8K | 🟢 **保** | import 例外台账（手册 §4 指针；本次改动 1 行） |

## 三、新文件结构（`memory/plugin-authoring-manual.md`，383 行）

```
§0 插件性定义（0.1 可卸载⇒可重装契约 · 0.2 六类插件+协议两模式 · 0.3 无 API 白名单
   · 0.4 命名规范 · 0.5 生命周期状态）
§1 从零写插件（1.1 视图 / 1.2 协议 / 1.3 主题 / 1.4 语言 / 1.5 字段驱动检测
   / 1.6 安装流转 / 1.7 能力全景）
§2 plugin.json 与 schema（权威位置 public/schemas/）
§3 contributes 格式演进与向后兼容（3.1 minAppVersion / 3.2 loader 兼容 / 3.3 迁移表
   / 3.4 测试 / 3.5 与 $schema·PluginType 关系 / 3.6 不做什么）
§4 插件与壳的通信（4.1 linkdesk.* API 面 · 4.2 可 import 三类 · 4.3 审计方法 + 历史台账作废注记）
§5 API 设计铁律（5.1 已有优先 · 5.2 壳先行建设不等消费方 —— 两段用户原话逐字保）
§6 API 命名规范（FileService 当前导出名全集）
§7 非视图插件归一化三规则 + 9 bug 速查 + 归一化版图互补
```

**保真核验：** 契约用户原话（"如果说不可以被卸载的话？…"）· API 铁律两段用户原话（"如果有 API 的话那么自然而然…" / "VS Code 只有 1000 多个 API…"）· 破口 commit `a61736c3` · FileService 12 个导出名 · 9 bug 全表 · 迁移表 7 字段——**全在**。

## 四、引用修复（10 文件 / 10 行）

| 文件 | 原 | 改 |
|:--|:--|:--|
| `design-decisions.md:32` | `[[plugin-system]]` | `[[plugin-authoring-manual]]` |
| `dev-uninstall-relocates-source.md:31` | `[[plugin-lifecycle-reinstall-contract]]` | `[[plugin-authoring-manual]]` |
| `gear-menu-normalization.md:177` | `[[non-view-plugin-normalization-lesson]]` | `[[plugin-authoring-manual]]` |
| `directory-flat-exception-needs-user-approval.md:18` | `[[api-preference-existing-first]]` | `[[plugin-authoring-manual]]` |
| `e5.8-layout-tree-grid.md:25` | 同上 | 同上 |
| `new-capability-design-flow.md:25` | 同上 | 同上 |
| `panel-window-archive.md:62` | 同上 | 同上 |
| `plugin-independence-iron-law.md:84` | 同上 | 同上 |
| `plugin-communication-standard.md:159` | `[[plugin-api-audit-checklist]]` | `[[plugin-authoring-manual]]` |
| `plugin-import-exceptions.md:15` | 同上 | 同上 |
| `MEMORY.md` | 4 行 5 处死链（L37/L41/L46/L84） | 收敛为 `[Plugin Manual](plugin-authoring-manual.md)` |

**补链 1 处：** `ai-workflow-target.md` 原唯一入站来自 `plugin-system.md`，随删成为**新孤儿** → 在手册「相关」补 `[[ai-workflow-target]]`（孤儿 17→16→**15**）。

## 五、校验

| 项 | 结果 |
|:--|:--|
| 文件总数 | **175**（183 − 9 + 1） |
| 总体积 | **947K**（本步净 **−7K**） |
| `[[wiki]]` 断链 | **31**（与 6a 同；**本批未新增**） |
| `(x.md)` 内部断链 | **1**（`e6-review-rhythm` → 旧仓库结构，既存非本批） |
| 孤儿文件 | **15**（6a 末 17；本步 −2 = 被删的 2 份无入站文件，补链后净 −2） |
| `git diff --cached --stat` | `21 files changed, 396 insertions(+), 650 deletions(-)` |

## 六、遗留 / 待用户裁

- **批次 5 三份超体积**（`snapshot` 15.4K / `e6-review` 9.7K / `design-skills` 10.7K，合计 +7.8K）仍待裁定。
- **31 条 `[[wiki]]` 断链 + 15 份孤儿** → 移交 **T6h** 归零。
- **终检体积预警：** 按路线书批次 6 预估（~64 删），跑完 6c–6f 约落 **~125 文件**，**高于 TF.3 的 95–110 目标**——需在 T6h 前请用户重定口径（接受 125，或再指定一轮删除）。已记录，届时提请。
- 并行 E6 agent 未提交改动原样保留；本步提交只纳入我方 21 项。

---

# §6c · `toolbox-sop.md`（工具与操作手册）

> **记忆仓提交：** `802e4f4`（消息含「批次6c」）· **本步前回滚点：** `818fed9`（6b）
> **结果：** 折入 **11 份** · 删 **11** · 新增 **1** · **暂缓 2** · 修引用 **10 文件** · 全库 **175 → 165 文件** · 体积 **947K → 934K**

## 一、⚠️ 冲突处置（本步最重要的一件事）

**`dev-environment.md` 与 `flaky-full-suite-tests.md` 正被并行 E6 agent 实际编辑**（工作区 `M`，mtime 2026-09-12 07:22 / 06:51）：
- `dev-environment` 含**当天新增未提交内容**：GitHub 代理订正（「小请求直连能过、真传被掐」）、gh token 权限（缺 `workflow` scope 被拒推）、npm registry 段。
- `flaky-full-suite-tests` 含**当天新增**：「整条 suite 全红但零测试运行」更唬人形态。

**`git rm` 会连同并行 agent 的未提交改动一起删掉（不可逆）** ⇒ 用户 2026-09-12 裁「同意」→ **本步暂缓这 2 份**（保留独立），留待并行 agent 提交后或 T6h 统一处理。

**副产：对并行 agent 已改过的文件做引用修复，必须走 blob 重建**——本次 `cdp-ui-automation` / `e6-l5-handoff` 两个修复目标均在并行 agent 的 `M` 清单内 → 采用 **HEAD blob + 我方替换 → `update-index`；同时在写工作树重放**（与 MEMORY.md 同款手法）。另 `electron-builder-github-safe-artifact-name.md` 是并行 agent 的**未跟踪新文件** → **只改工作树、不暂存**（归他们提交）。

## 二、审议门结论（2026-09-12，用户「同意」）

三项待裁全部采纳：
1. §一 冲突 → **暂缓 2 份** ✅
2. `vscode-source-reference`（10.8K）→ **折入 §9 并压缩**（2026-09-10 用户拍板逐字保 + 路径速查表保 + curl 法保；删重复论证）✅
3. `git-workflow` → **折入时按实况订正** ✅

**结构微调（避免重复）：** 因 `dev-environment` 暂缓保留，手册**不设重复的「开发环境」章**，改为 **§0 入口指针**（指向 `dev-environment` / `e5.8-contracts-npm-publish-ready` / `cdp-ui-automation` 三份独立件）。原大纲的 §0 内容（装什么/空间/镜像/文件锁）**全部留在 `dev-environment`**，不抄一份。

## 三、原文件清单与处置

| # | 文件 | 前 | 判定 | 去向 |
|:--:|:--|--:|:--:|:--|
| 1 | `packaging-run-sop.md` | 3.6K | 并 → 删 | §1 一跑一清 SOP |
| 2 | `dev-artifact-hygiene.md` | 6.1K | 并 → 删 | §2（三处落位 / 十禁 / CWD holder 查法 / 收尾仪式） |
| 3 | `production-debug-log.md` | 3.4K | 并 → 删 | §3（+ LSP 关键标记表） |
| 4 | `electron-run-as-node-trap.md` | 3.1K | 并 → 删 | §4（三次实证 + 症状判据 + 启动姿势） |
| 5 | `cdp-verify-read-ui-first.md` | 1.2K | 并 → 删 | §5.1 |
| 6 | `mcp-cdp-ai-bridge.md` | 3.7K | 并 → 删 | §5.2（三层关系 + 「对一半」） |
| 7 | `engineering-config-notes.md` | 3.8K | 并 → 删 | §6（vitest 缺口 + Vite 缓存） |
| 8 | `eslint-mechanical-guard.md` | 1.8K | 并 → 删 | §7 |
| 9 | `git-workflow.md` | 1.2K | 并（**订正**）→ 删 | §8（+ §8.2 仓库现状订正表） |
| 10 | `deepseek-harness-reference.md` | 3.3K | 并 → 删 | §9.2（四路侦察 + A/B 档立案） |
| 11 | `vscode-source-reference.md` | 10.8K | 并（**压缩**）→ 删 | §9.1（拍板逐字 + 路径表 + curl 法 + 模式对照） |
| — | `dev-environment.md` | 8.2K | ⏸ **暂缓** | 并行 agent 编辑中 |
| — | `flaky-full-suite-tests.md` | 2.3K | ⏸ **暂缓** | 同上 |
| — | `e5.8-contracts-npm-publish-ready.md` | 13.9K | 🟢 **保** | npm 发布坑不重复收录，手册 §0 放指针 |
| — | `cdp-ui-automation.md` | 9.6K | 🟢 **保** | 手册 §5 放指针（本次因引用修复改动 1 行） |

## 四、新文件结构（`memory/toolbox-sop.md`，356 行）

```
§0 入口指针（→ dev-environment / e5.8-contracts-npm-publish-ready / cdp-ui-automation）
§1 打包运行 SOP（一跑一清 cycle：build 次数纪律 / 一 cycle 一 ud / 关实例清场 / dev 与打包隔离）
§2 产物卫生（三处落位 / 十禁 / 空目录删不掉查法 PEB 读 CWD / 收尾仪式按 mtime 不按 grep）
§3 诊断日志（protocol-debug.log + 4 条注意 + LSP 关键标记表）
§4 环境陷阱 ELECTRON_RUN_AS_NODE（三次实证 dev/后台/打包 exe + 症状判据 + 启动姿势）
§5 CDP 自验纪律（先读界面 / 莫名切换=用户误点）+ MCP/CDP/方向B 三层关系
§6 工程配置（vitest 覆盖缺口 + 6 文件补测计划 / Vite 缓存 postinstall 自清）
§7 机械门禁（三条 ESLint 规则 + 加规则触发条件 + ESLint vs vitest）
§8 Git 工作流（规则 + 8.2 仓库现状订正）
§9 外部源码参考（9.1 VS Code 阅读指南含 2026-09-10 拍板 / 9.2 DeepSeek Harness）
```

**保真核验：** `E:\linkdesk-build` 唯一家 · 十禁全条 · PEB x64 三个 offset · 三次 RUN_AS_NODE 实证（含 `v24.18.0` 假版本号判据）· LSP 12 条日志标记 · VS Code 2026-09-10 用户原话 · 7 行模式对照表 · 源码路径速查（18 条）· dsh 四路侦察表 · 不抄清单 5 项——**全在**。

## 五、引用修复（10 文件 + 1 未跟踪件）

| 文件 | 处理 | 处数 |
|:--|:--|:--:|
| `dev-uninstall-relocates-source.md` | 普通 | 2 |
| `e-testfiles-user-asset.md` | 普通 | 2 |
| `version-forms-user-terms.md` | 普通 | 3 |
| `hello-linkdesk-keep-installed.md` | 普通 | 1 |
| `lsp-self-contained-gaps.md` | 普通 | 1 |
| `pre-v1-bundled-plugin-curation.md` | 普通 | 1 |
| `bundled-same-version-no-refresh.md` | 普通 | 1 |
| `vscode-mirror-read-renderer-dom-order.md` | 普通 | 1 |
| `cdp-ui-automation.md` | **blob 重建 + 重放** | 1 |
| `e6-l5-handoff.md` | **blob 重建 + 重放** | 2 |
| `electron-builder-github-safe-artifact-name.md` | **未跟踪 → 只改工作树不暂存** | 1 |
| `MEMORY.md` | token 清理 + blob + 重放 | 4 行 |

**MEMORY.md 手法升级：** 本次改用 **md-link token 分段清理**（按 ` · ` / ` / ` 拆段、丢弃含死链的段、清悬空分隔符）——比 6a 的精确串替换更稳健（同时适配 HEAD/工作树两种排版）；核验「HEAD 残留死链 = 0」且 `CDP UI 自动化` 等活条目保留。

## 六、校验

| 项 | 结果 |
|:--|:--|
| 文件总数 | **165**（175 − 11 + 1） |
| 总体积 | **934K**（本步净 **−13K**） |
| `[[wiki]]` 断链 | **28**（6b 为 31；**本批未新增**——减少 3 条系被删文件内的旧断链：`eslint-mechanical-guard→vitest-coverage-gaps`、`git-workflow→progress-reporting-rule`、`vscode-source-reference→recurring-themes`） |
| `(x.md)` 内部断链 | **1**（`e6-review-rhythm` → 旧仓库结构，既存） |
| 孤儿文件 | **14**（6b 末 15；−1 = 被删的 `deepseek-harness-reference` 无入站；**无新增孤儿**） |
| `git diff --cached --stat` | `23 files changed, 374 insertions(+), 662 deletions(-)` |

## 七、遗留 / 待用户裁

- **⏸ 暂缓 2 份**（`dev-environment` / `flaky-full-suite-tests`）——待并行 E6 agent 提交后处理；**若届时仍无人认领**，建议在 T6h 前单独走一次小审议门折入 `toolbox-sop`。
- **批次 5 三份超体积**（+7.8K）仍待裁定。
- **终检体积预警：** 跑完 6d–6f 预计 ~115 文件，仍高于 TF.3 的 95–110——T6h 前请重定口径。
- 并行 agent 未提交改动原样保留；本步提交只纳入我方 23 项。

---

# §6d · `ui-normalization-atlas.md`（UI 归一化图谱）

> **记忆仓提交：** `24675a9`（消息含「批次6d」）· **本步前回滚点：** `802e4f4`（6c）
> **结果：** 折入 **15 份** · 删 **15** · 新增 **1** · 修引用 **10 文件 / 13 处** · 补链 1 + 连带修 1 · 全库 **165 → 152 文件** · 体积 **934K → 919K**

## 一、数量订正（对审议门报告）

审议门表把 `settings-appearance` **既列为 #9 又列为「附」**，报告写的「删 14 · 165 → 152」有误。
**实际删除 15 份**（路线书 14 份 + 前置判定件 `settings-appearance`）。
**算式：165 − 15 + 1（新增图谱）+ 1（并行 agent 本步期间新增未跟踪件 `clean-checkout-vs-local-env.md`）= 152** ✅

## 二、审议门结论（2026-09-12，用户「同意」）

四项待裁全部采纳：
1. **前置判定（路线书要求）** ✅——三份**全部折入**，路径/状态订正：

   | 文件 | 判定 | 证据 | 图谱处理 |
   |:--|:--:|:--|:--|
   | `settings-appearance` | 🔴 **考古** | 文中 `src/components/views/SettingsView.tsx` **已随 E5.8#41.14 设置插件化整删** | §2.3 只保留**仍活的两条机制**（动态派生导航 / `onClick` 只读输入框模式），并加 2026-09-12 订正注记 |
   | `theme-browser` | 🟡 **半考古** | 独立组件**已并入 QuickPickService**（E5.5#7） | §2.1 保留**双数据源 + 六入口 + 命令 ID** 设计 |
   | `color-picker` | 🟢 **组件活跃** | 路径已随 E6#54c 迁 `@linkdesk/ui` | §1.4 保留布局/接口/场景/强调色方案/卡片调色盘，**路径订正** |

2. **`ui-ux-audit-todos` 逐行明细删** ✅——只留 **10 类 + 目标值**（圆角 4 token / 阴影 `0 8px 16px rgba(0,0,0,0.24)` / z-index 分层 / 色彩结论）+ 优先级 P0–P7 + 已合规 7 条。
3. **`type: user` 语义标注** ✅——图谱 §6 保留完整内容，frontmatter `description` 显式标注「用户 UI 惯用语档案（原 type:user）」。
4. **`command-palette` 过时段删** ✅——WebView2 截获 `Ctrl+Shift+P` 为打印（Tauri 死栈）与三期接入点表删除，只留「是什么 + 现由 QuickPick 承载」。

## 三、原文件清单与处置（15 份）

| # | 文件 | 前 | 去向 |
|:--:|:--|--:|:--|
| 1 | `selectbox-replaces-native-select.md` | 2.0K | §1.1（原生 select 时序根因 + 替换范围） |
| 2 | `quickpick-normalization.md` | 1.8K | §1.2（四面板合并单例） |
| 3 | `inline-input-normalization.md` | 3.3K | §1.3（API + 五防线 + E5#19a bug） |
| 4 | `color-picker.md` | 6.2K | §1.4（布局 + 接口 + 场景 + 强调色方案 + 卡片调色盘） |
| 5 | `dialog-normalization-requirement.md` | 1.3K | §1.5（Encaron 原话逐字保） |
| 6 | `gear-menu-normalization.md` | 8.2K | §1.6（四型齿轮 / 四 MenuId / 五条反模式） |
| 7 | `theme-browser.md` | 3.5K | §2.1 |
| 8 | `command-palette.md` | 2.1K | §2.2（压缩） |
| 9 | `ui-ux-pro-max-rules.md` | 4.8K | §4（六表 + 自检 8 条） |
| 10 | `ui-ux-audit-todos.md` | 8.8K | §3.2（10 类 + 目标值；逐行明细删） |
| 11 | `linkdesk-ui-component-library.md` | 3.1K | §5（远期 + 三层 UI 模型 + createWindow 门槛） |
| 12 | `css-variable-normalization-safety-net.md` | 1.1K | §0.1（临时代码也有底线） |
| 13 | `sidebar-section-force-expand-limitation.md` | 1.5K | §3.1（+ forceExpand 治本方案） |
| 14 | `user-ui-design-phrases.md` | 2.9K | §6（惯用语档案，两批 + 交流要点） |
| 15 | `settings-appearance.md` | 4.3K | 见 §二·1 前置判定（🔴 考古 → §2.3 压缩订正） |

## 四、新文件结构（`memory/ui-normalization-atlas.md`，360 行）

```
§0 归一化总则（临时代码也有底线 + 提交前 UI 自检 8 条）
§1 归一化组件全集 1.1–1.6（SelectBox / QuickPick / InlineInput / ColorPicker+强调色 / 弹窗 / 齿轮四型）
§2 选择器与设置页 2.1–2.3（主题语言选择器 / 命令面板 / Appearance 组与 onClick readOnly）
§3 已知限制与待办 3.1–3.2（SidebarSection 限制 / UI 全量审计 10 类 + P0–P7）
§4 UI/UX 专业规则六表（图标 / 动画 / 间距 / 颜色 / 交互 / z-index）
§5 @linkdesk/ui 远期组件库 + 三层 UI 模型
§6 用户 UI 惯用语档案（原 type:user）
```

**保真核验：** `<select>` 时序链 4 步 · InlineInput 完整 API + 五防线表 + E5#19a · ColorPicker 布局 ASCII + 四场景 + 强调色五件事 · 齿轮四型全表 + 声明驱动链路 + 五条反模式 · Encaron 弹窗原话 · SidebarSection 三问（症状/影响/根因/治本）· 圆角 4 token 与阴影目标值 · z-index 分层 6 档 · 三层 UI 模型 + createWindow 门槛 · 用户惯用语 12 词条——**全在**。

## 五、引用修复（10 文件 / 13 处）

| 文件 | 处数 |
|:--|:--:|
| `theme-system.md` | 2 |
| `keybinding-system-complete.md` | 2 |
| `appearance-audit-archive.md` | 2 |
| `notification-surface-single-panel.md` | 1 |
| `language-system.md` | 1 |
| `content-vs-space-ownership.md` | 1 |
| `e6-10min-first-plugin.md` | 1 |
| `design-skills-inventory.md` | 1 |
| `update-failure-retry-inline-not-final.md` | 1 |
| `user-communication-map.md` | 1 |
| `MEMORY.md` | token 清理 + blob + 重放 |

**补链 1：** `clipboard-provider-normalization` 原唯一入站来自被删的 `inline-input-normalization` → 图谱「相关」补 `[[clipboard-provider-normalization]]`（孤儿 14→11→**10**）。

**连带修复 1（本次新情况）：** 并行 E6 agent 在**本步执行期间**新建未跟踪件 `clean-checkout-vs-local-env.md`，其中引用了 **6c 已折入**的 `[[dev-artifact-hygiene]]` → 工作树改指 `[[toolbox-sop]]`（**未跟踪件只改工作树、不暂存**，归其提交）。**教训：每步校验必须重跑——并行 agent 会在两步之间新建文件。**

## 六、校验

| 项 | 结果 |
|:--|:--|
| 文件总数 | **152**（165 − 15 + 1 新增 + 1 并行 agent 新件） |
| 总体积 | **919K**（本步净 **−15K**） |
| `[[wiki]]` 断链 | **27**（6c 为 28；**未新增**——`quickpick-normalization→[[quickpick-service]]` 既存断链随折入消除） |
| `(x.md)` 内部断链 | **1**（`e6-review-rhythm` → 旧仓库结构，既存） |
| 孤儿文件 | **10**（6c 末 14：−4 被删孤儿，+1 新孤儿 `clipboard-provider-normalization`，−1 补链归零） |
| `git diff --cached --stat` | `27 files changed, 376 insertions(+), 1233 deletions(-)` |

## 七、遗留 / 待用户裁

- **⏸ 6c 暂缓 2 份**（`dev-environment` / `flaky-full-suite-tests`）仍待并行 agent 提交后处理。
- **批次 5 三份超体积**（+7.8K）仍待裁定。
- **终检体积预警：** 跑完 6e–6f 预计 **~125 文件**，高于 TF.3 的 95–110——T6h 前请重定口径。
- 并行 agent 未提交改动原样保留（含新增未跟踪件 `clean-checkout-vs-local-env`）；本步提交只纳入我方 27 项。

---

# §6e · `version-and-release.md`（版本与发布）

> **记忆仓提交：** `ce91f93`（消息含「批次6e」）· **本步前回滚点：** `24675a9`（6d）
> **结果：** 折入 **7 份** · 删 **7** · 新增 **1** · 修引用 **7 文件** · 全库 **152 → 146 文件** · 体积 **919K → 922K**（+3K：新增图谱含四次实证全保，净值说明见 §六）

## 一、审议门结论（2026-09-12，用户「同意」）

三项待裁全部采纳：
1. **`version-forms-user-terms` 的 `type: user`** ✅——§2 保留完整内容 + frontmatter description 标注「原 type:user」。
2. **`pre-v1-bundled-plugin-curation` 是「未定案」件** ✅——折入 §3.3 并显式标注「v1 前待用户裁定，别自作主张改 build」，保留其「别折腾发货集」的纪律价值。
3. **`bundled-same-version-no-refresh` 全文保真折入** ✅——四次实证逐条留（#15d / #15e / #15o / 2026-09-11）、#15n 拍板原话留、`--force-rematerialize-bundled` 用法与两个 ⚠️ 留。

## 二、原文件清单与处置（7 份）

| # | 文件 | 前 | 去向 |
|:--:|:--|--:|:--|
| 1 | `version-axes-separated.md` | 6.0K | §0（五轴表 + 判定三问 + 案例库 4 例 + E6 恒 0.x + 拆焊 + 检测链双保险） |
| 2 | `version-major-additive-first.md` | 1.6K | §1（2.0 前置自检 + 四条判定 + VS Code 十年 1.x 根因） |
| 3 | `version-forms-user-terms.md` | 2.1K | §2（叫法对照表 + 「不必纠正」纪律） |
| 4 | `bundled-same-version-no-refresh.md` | 6.5K | §3.1（四次实证表 + #15n 拍板 + `--force-rematerialize-bundled` + 两个 ⚠️） |
| 5 | `stale-sdk-built-plugin-zip.md` | 2.3K | §3.2（+ 与 §3.1 的双根因判据） |
| 6 | `pre-v1-bundled-plugin-curation.md` | 1.8K | §3.3（v1 前发货集未定案 + demo×2 已拍板） |
| 7 | `update-failure-retry-inline-not-final.md` | 1.4K | §4（**非终局**须逐字保） |

## 三、新文件结构（`memory/version-and-release.md`，199 行）

```
§0 版本号是多轴模型（五轴表 · 判定三问 · 案例库 4 例 · E6 前恒 0.x · 拆焊 · 检测链双保险）
§1 版本判定纪律（硬约束 22 · MAJOR/2.0 前置自检「这为什么不能做成增量的？」四条）
§2 用户口中的软件形态（原 type:user —— 安装版/编译版对照 + 不纠正纪律）
§3 bundled 随包插件分发与刷新（3.1 boot 幂等只补缺失四次实证 + 拍板 + 开关 / 3.2 陈旧 SDK zip 双根因判据 / 3.3 v1 前发货集未定案）
§4 更新/安装失败的重试形态（非终局）
```

**保真核验：** 五轴检测机制表 · E6 恒 0.x 用户拍板（"记住 + 每次都用"）· 拆焊 `0.1.2`/`0.1.1` 实证 · 案例库四例含「设置页难道不是独立插件吗」反例 · 2.0 四条判定 · 用户叫法对照表与"不必提醒我"原话 · 四次 bundled 实证（含 09-06 05:23 物化日期、18 被重铺日志、16 个哈希名分块）· #15n 拍板三条防滞旧 · demo×2 不带 · §4 非终局逐字——**全在**。

## 四、引用修复（7 文件）

| 文件 | 处数 | 备注 |
|:--|:--:|:--|
| `lsp-self-contained-gaps.md` | 3 | |
| `toolbox-sop.md` | 3 | |
| `e5.8-contracts-npm-publish-ready.md` | 1 | |
| `bug-atlas.md` | 1 | |
| `e6-plugin-folder-flatten.md` | 1 | |
| `e-testfiles-user-asset.md` | 1 | |
| `e6-l5-handoff.md` | 3 | ⚠️ **并行 agent 的 `M` 清单内 → blob 重建（HEAD + 我方替换）+ 工作树重放** |
| `MEMORY.md` | 4 行 | token 清理 + wiki 替换 + blob + 重放 |

## 五、⚠️ 本步教训：MEMORY.md 清理漏了 `[[wiki]]` 形式

我沿用的 6c/6d **md-link token 分段清理**只处理 `](x.md)` 形式；本步首次校验发现**引入一条新断链**：`MEMORY.md -> [[bundled-same-version-no-refresh]]`（MEMORY.md 里有一条 wiki 形式的引用）。

- **修正：** 补 `.b6fix2.cjs`——`cleanLine` 在丢弃死链段之后，**再对存活文本跑一遍 wiki 替换**；HEAD blob 与工作树同步重做。
- **复验：** 工作树残留死链 = 0，全库断链回到 **27**（未新增）。
- **🔴 教训入库：MEMORY.md 的引用清理必须同时处理两种形式**——`](x.md)` 与 `[[x]]`。前几步没踩到只是运气（那些死链恰好只以 md 形式出现）。

## 六、校验

| 项 | 结果 |
|:--|:--|
| 文件总数 | **146**（152 − 7 + 1） |
| 总体积 | **922K**（+3K）——**说明：** 本步 7 份原 ~21.7K，新图谱 199 行；`bundled-same-version-no-refresh` 按裁定**全文保真折入**（四次实证 + 拍板 + 开关用法），压缩比低于前几步，故净值微增。**内容完整性优先于体积指标**（「压缩比删除更容易误伤」） |
| `[[wiki]]` 断链 | **27**（6d 末 27；**净变化 0**——中途因 wiki 形式遗漏曾 +1，已修正） |
| `(x.md)` 内部断链 | **1**（`e6-review-rhythm` → 旧仓库结构，既存） |
| 孤儿文件 | **10**（6d 末 10；**无新增**，被删 7 份均非孤儿） |
| `git diff --cached --stat` | `16 files changed, 210 insertions(+), 184 deletions(-)` |

## 七、遗留 / 待用户裁

- **⏸ 6c 暂缓 2 份**（`dev-environment` / `flaky-full-suite-tests`）仍待并行 agent 提交后处理。
- **批次 5 三份超体积**（+7.8K）仍待裁定。
- **终检体积预警：** 6f 是最后一步大归并（~22 份 → 1），跑完预计 **~125 文件**，仍高于 TF.3 的 95–110——T6h 前请重定口径。
- 并行 agent 未提交改动原样保留；本步提交只纳入我方 16 项。

---

# §6f · `ai-workflow-canon.md`（AI 工作法正典）

> **记忆仓提交：** `ab05389`（消息含「批次6f」）· **工程仓提交：** `5b06ec16c`（CLAUDE.md 订正）· **本步前回滚点：** `ce91f93`（6e）
> **结果：** 折入 **21 份** · 删 **21** · 新增 **1** · **保 1** · 修引用 **18 文件** · 补链 1 + 去链 1 · 全库 **146 → 126 文件** · 体积 **922K → 897K**

## 一、审议门结论（2026-09-12，用户「我认同你的建议」）

四项待裁全部采纳：
1. **硬约束级判定** ✅——**只保 `ai-pre-commit-checklist` 独立**（CLAUDE.md 逐字点名 + 提交门禁机械清单须独立可检索）；其余 21 份折入。
2. **`progress-tracking-rules` 旧文件名订正** ✅——「`05-执行清单.md`」订正为「**当前 Phase 的执行清单**」，保留「唯一真相源 + 不复制进度」的规则本体。
3. **并行 agent 产物处理** ✅——`flaky-full-suite-tests` / `e6-l4-deferred`（已跟踪 `M`）走 **HEAD blob + 工作树重放**；`clean-checkout-vs-local-env` / `e6-57-release-acceptance-two-sides`（未跟踪新件）**只改工作树、不暂存**。
4. **顺手订正 CLAUDE.md 陈旧引用** ✅——7 处，工程仓单独 commit `5b06ec16c`。

## 二、原文件清单与处置（22 份）

| # | 文件 | 前 | 去向 |
|:--:|:--|--:|:--|
| 1 | `discuss-before-editing.md` | 2.2K | **§0 第零条铁律·审议门**（用户原话逐字 + 五步） |
| 2 | `design-approval-vs-implementation-go.md` | 1.6K | §0.3 |
| 3 | `ai-workflow-discipline.md` | 5.3K | §1（全部八小节） |
| 4 | `progress-tracking-rules.md` | 3.9K | §2.1 + §2.2 + §2.3 |
| 5 | `task-list-first-real-machine-gate.md` | 2.2K | §2.4（#46.12 回归实证） |
| 6 | `checklist-concise-pointer-set.md` | 3.8K | §2.5 |
| 7 | `checklist-slim-preserve-links.md` | 1.4K | §2.6 |
| 8 | `bug-fix-protocol.md` | 1.4K | §2.7 |
| 9 | `new-capability-design-flow.md` | 3.7K | §3.1 + §3.2 |
| 10 | `add-feature-discipline.md` | 3.0K | §3.3 |
| 11 | `feature-folder-split-rule.md` | 2.4K | §3.4 |
| 12 | `directory-flat-exception-needs-user-approval.md` | 1.8K | §3.4 末 |
| 13 | `ai-communication-protocol.md` | 3.4K | §4（三段式 + 邻近区域表） |
| 14 | `rule-extrapolation-not-consent.md` | 2.6K | §5.1 |
| 15 | `opt-in-not-opt-out.md` | 1.9K | §5.2 |
| 16 | `no-deferral-future-excuse.md` | 1.5K | §5.3 |
| 17 | `reduce-parallel-subagents.md` | 2.4K | §5.4 |
| 18 | `code-self-documenting.md` | 2.9K | §6.1 |
| 19 | `architecture-side-effects.md` | 1.9K | §6.2 |
| 20 | `phase-review-meaning.md` | 2.8K | §7 |
| 21 | `ai-workflow-target.md` | 3.9K | §8 |
| 22 | `ai-pre-commit-checklist.md` | 6.0K | 🟢 **保留独立** |

## 三、新文件结构（`memory/ai-workflow-canon.md`，477 行）

```
§0 第零条铁律·审议门（0.1 五步节奏 · 0.2 讨论期不落笔 · 0.3 设计批准≠实现授权 · 0.4 与沟通协议关系）
§1 工作节奏（1.1–1.8：一步一验证 / 一 commit 一任务 / 线性插入 / 设计文档写全 / 沾边立案 /
   不心急 / 共享文档混入检查 / 修一验一）
§2 执行清单规范（2.1 唯一真相源 · 2.2 在途句回扫 · 2.3 攒批更新 · 2.4 任务先行+实机门禁 /
   2.5 指针集两行制 · 2.6 精简保链接 · 2.7 Bug 修复三件事）
§3 设计前置（3.1 8 维度+三层固化 · 3.2 核心理念 10 条 · 3.3 加功能戒律+功能位置解耦 /
   3.4 feature-folder 拆法+平铺例外须拍板）
§4 沟通协议（4.1 三段式 · 4.2 UI 验证点+邻近区域表 · 4.3 进度报告）
§5 判断纪律（5.1 规则外推≠同意 · 5.2 opt-IN · 5.3 禁未来再做 · 5.4 少用并行 agent）
§6 代码即说明书（6.1）+ 架构副作用（6.2）
§7 「审视」= 逐 Phase 规划审查
§8 AI 友好终极目标
```

**保真核验：** 审议门用户原话 · #160「我让你现在改了吗？」 · E2c#19「为什么分这么多小任务」 · #46.12 2800MB 卡死链 · 在途句白跑一轮实证 · e902bc0f 混入实证 · 8 维度全文 + 三层固化 commit `4cc0ed24`/`c3b08cf8` · 核心理念 10 条逐字 · 加功能用户原话 · feature-folder 先例三件 · floating/ 纠正 · 邻近区域表 8 行 · E6#75/E6#79 用户原话 · opt-OUT 代码对比 · 账单 388 元/101.94 元/5.39 亿 token · 三段式三例 · 审视五步 + 用户原话 · 四场景 + VS Code 对比表——**全在**。

## 四、引用修复（18 文件）

| 类别 | 文件 | 处理 |
|:--|:--|:--|
| 普通（14） | `plugin-authoring-manual` / `user-communication-map`(2) / `e6-review-rhythm-execution-batch`(3) / `lsp-self-contained-gaps` / `plain-language-for-client` / `report-file-paths-not-bare-names`(2) / `monaco-architecture-decision`(2) / `snapshot-shadows-truth-bug-class` / `file-split-thresholds`(2) / `core-ignorance-principle` / `quality-commandments` / `platform-readiness-gaps` / `e6-checklist-structure-keep-layers`(2) | 改 + `git add` |
| 并行已跟踪（2） | `flaky-full-suite-tests` · `e6-l4-deferred` | **blob 重建 + 工作树重放** |
| 并行未跟踪（2） | `clean-checkout-vs-local-env` · `e6-57-release-acceptance-two-sides` | **只改工作树，不暂存** |
| — | `MEMORY.md` | token 清理 + wiki 替换 + blob + 重放 |

> `hardcoded-path-constraint` 出现在引用扫描里，但它指向的是**保留**的 `ai-pre-commit-checklist`——无需修改（残留校验 0 处确认）。

## 五、补链 1 + 去链 1

- **补链：** `detached-window-main-only-paths` 原唯一入站来自 `add-feature-discipline` → 随删成为**新孤儿** → 正典「相关」补 `[[detached-window-main-only-paths]]`（+ 顺带补 `[[system-capability-linkdesk-api-only]]`）。
- **去链：** 我在正典 §3.3 写下的 `[[window-namespace]]` **无此文件**（系从 `add-feature-discipline` 继承的旧断链）→ 改为纯文本「壳 `window` 命名空间共享模块」。**教训：折入时继承的旧断链要顺手清，别原样搬运。**

## 六、工程仓改动（CLAUDE.md）

**`E:\linkdesk\CLAUDE.md` 7 处失效 memory 引用订正**（commit `5b06ec16c`，纯文档不 bump）：
`uninstall-bug-recurring` / `v3-pitfalls` / `phase3-drag-bugs` / `e1-flush-timeout-bug` → `bug-atlas`；`vite-cache-after-import-fix` → `toolbox-sop` §6.2；`[[e5-multi-webview-6-bugs]]` → `multi-webview-bug-atlas` §A；`phase4-design-decisions` → `design-decisions`。
**保留：** `ai-pre-commit-checklist.md`（6f 判定独立保留）· `invisible-bugs-lesson-59c.md` · `core-ignorance-principle.md` · `design-skills-inventory` · `theme-system.md`。

## 七、校验

| 项 | 结果 |
|:--|:--|
| 文件总数 | **126**（146 − 21 + 1） |
| 总体积 | **897K**（本步净 **−25K**） |
| `[[wiki]]` 断链 | **23**（6e 末 27；**未新增**——减少 4 条系被删文件内的旧断链：`add-feature-discipline→window-namespace`、`ai-workflow-discipline→communication-protocol-before-after`/`step-ui-verification`、`code-self-documenting→recurring-themes`） |
| `(x.md)` 内部断链 | **1**（`e6-review-rhythm` → 旧仓库结构，既存） |
| 孤儿文件 | **10**（6e 末 10；+1 新孤儿 `detached-window-main-only-paths`，−1 补链归零） |
| `git diff --cached --stat` | `36 files changed, 494 insertions(+), 832 deletions(-)` |

## 八、遗留 / 待用户裁

- **⏸ 6c 暂缓 2 份**（`dev-environment` / `flaky-full-suite-tests`）——后者本次已作为**引用修复目标**处理（blob 重建 + 重放），但**文件本身仍保留**未折入。待并行 agent 提交后或 T6h 处理。
- **批次 5 三份超体积**（+7.8K）仍待裁定。
- **`docs/06-记忆系统整理/` 目录仍未纳入工程仓 git**（`??` 状态）——整套路线书 / 任务清单 / 六份处置档案都在此处，**建议用户拍板是否提交入库**（防丢失）。
- **下一步 6g（MEMORY.md 全量重写）+ 6h（校验归零）** 是批次 6 最后两步。
- 并行 agent 未提交改动原样保留；本步记忆仓提交只纳入我方 36 项，工程仓提交只纳入 CLAUDE.md 一项。
