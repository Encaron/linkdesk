# E6 执行清单——插件生态与发布

> 2026-08-07。**E5 全部完成（E5-收尾已封站）。** 🔥 **E5.8 已收官（2026-08-29，332 任务全勾）——E6 正式开战。**
> **🔥 进度唯一真相源。** 做完一个勾一个。
> **E6 目标：** 三个角色各自有一条完整链路——插件作者开发→发布、普通用户下载→安装插件、你自己 build→发布软件。安装后支持 Windows 右键菜单 + 文件关联 + 多窗口（对标 VS Code）。
>
> **当前进度：0/72。**（2026-08-30 审计同步——72 活任务含小数，剔 #3/#14 划线删除，+6 第 0 层 #0.1-#0.6 +1 #9.1 pluginLoader 拆分）7 层（第 0-6 层）+ 展望。第 0 层 = E6 前置（目录卫生 + 壳树 feature-folder 审计 + 防膨胀门禁）；第 1 层 = 地基必须先做。含 SDK 类型对接（E5.8 契约）+ 动态加载改造 + glob 替换 + IPC handler + 共享组件分发 + 内置插件独立化 + PluginInstallService + 路径解析归一化 + 市场第一站体验（探索/详情三态/README/更新/信任/安装细节）+ Windows Shell 集成 + 多窗口。
> 🔴 **2026-08-16 E5.8 衔接审计**：#3 删（E5.8 Phase 4 吸收）/ #14 搬（E5.8 Phase 1.5）/ #23.5 补（dev 宿主重设计——plugin-shell 已删）/ #48-#52 补（dsh 第二梯队安全加固立案）。任务号留空不补位。
> 🔵 **2026-08-27 移入 #53**：装配清单 profile 设计（自 E5.8#50——用户确认要做，E6 生态地基展望）。
> 🔵 **2026-08-29 立案 #54**：共享组件独立分发 + token 契约（方向 A——@linkdesk/ui 包 + SDK eslint 门禁）。E6#15 硬前置：内置插件独立 build 后失去 @src，共享组件必须先打包分发。候选方案讨论后用户拍板 A。
> 🔵 **2026-08-29 清单结构定案**：**第 N 层 ≈ E5.8 的 Phase 容器；第 N.M 轮 ≈ E5.8 的一个 Phase 批次**（一个执行批次，做完等检查）。**编号 ≠ 执行序**——编号是稳定 ID（插入序），执行序一律看底部「执行顺序与依赖」段（30.10→30.14 搬迁系列已全覆盖）。

---

## 🔥 关键路径

```
第 0 层：E6 前置——代码库卫生（#0.1-#0.2）+ 壳树 feature-folder 审计（#0.3-#0.5）+ 防膨胀门禁（#0.6，L1 开工前收口）

第 1 层：插件独立构建（地基——必须先做，E5.8 完成后再开始）
  第 1.1 轮：plugin-sdk + 类型对接 E5.8 生成契约（linkdesk.d.ts 派生）
  第 1.2 轮：plugin-shell 动态加载 + loader glob 替换 + .linkdesk-plugin + IPC handler 新建
            + handler 审计走 IpcBridge + PluginInstallService
  第 1.3 轮：共享组件分发（@linkdesk/ui）→ 内置插件独立化 → 壳瘦身 → 协议重构

第 2 层：插件开发工具链（第 1 层后——依赖 SDK）
  第 2.1 轮：create-linkdesk-plugin 脚手架
  第 2.2 轮：dev + build 命令
  第 2.3 轮：Mock 自动生成 + dev 可选 Electron 弥合生产差距

第 3 层：插件市场（第一站体验）
  GitHub Releases 后端 + 壳内下载安装 + 探索/详情三态/README/更新/信任/安装细节
  交互设计 → [03-市场交互设计.md](03-插件市场/03-市场交互设计.md)（总览 + 04-09 分维度文档）

第 4 层：端到端验证（第 1-3 层后——全链路）
  测试插件 + 开发版验证 + 生产安装版验证 + 内置插件独立验证

第 5 层：文档与发布（全程穿插）
  开发者指南 + CI + 官网上线 + 内置插件迁移指南 + Shell 集成 + 多窗口

第 6 层：安全加固（第 1-3 层后——dsh 第二梯队，2026-08-16 立案）
  E6#48-#52：子进程沙箱缝 / 审批层审计对 / 崩溃补闭环 / 凭据隔离 / 用户 patch 层
```

---

## 第 0 层：E6 前置——代码库卫生与收官衔接

> 🔵 **2026-08-29 立项。** E5.8 已收官（332 任务全勾）、git 分支已到 e6，补 E6 前置层装「必须在 E6 壳层改造前收干净」的目录卫生。**E6 完成前全部收口**：L0（#0.1-#0.6）在 L1 开工前做完；pluginLoader 拆分 = **新增 E6#9.1**（随 #9 loader 重写同批做，一次到位不双份 churn）；preload-pool 核对 = **E6#15g**（已每命名空间一文件，不拆）；ThemeEngine 明确排除（2026-08-29 收官，等独立归一化专项）。背景：2026-08-29 目录审计——结论是七功能核心 = E5.8#0d.10-3（2026-08-19 拍板）定案结构不动，只有 0d.10-3 之后长出的增量平铺文件要过 feature-folder 判据。**2026-08-30 扩为全树审计**——判据对齐 codex 回应（单文件 >500-800 行，或同时管理多种生命周期/领域职责 → 拆模块；大但单职责 → 拆=返工），审计维度 = src/App 增量（#0.3）+ 全树大文件（#0.4）+ 目录核对（#0.5）。**同日再拍板 #0.6 防膨胀机械门禁**（V2 教训——MainWindow.xaml 2738 行无门禁涨成巨兽；阈值 800 行非测试源文件亮红灯；allowlist 例外须用户拍板）。

---

### 第 0.1 轮：代码库卫生

#### E6#0.1 🟢 死 `.git/` 空壳清理

- [ ] **E6#0.1a** `rm -rf linkdesk/.git`——该目录仅存空 `hooks/`（无 HEAD/objects/refs，2026-08-29 实测确认死壳，git 从未把它当独立仓库，真根一直是 E:/linkdesk）
**验证：** `git -C linkdesk rev-parse --show-toplevel` 仍返 `E:/linkdesk`，git status 零变化。

#### E6#0.2 🟢 构建产物浏览排除

- [ ] **E6#0.2a** `dist / dist-electron / release / node_modules` 进工作区 `files.exclude`（`.vscode/settings.json`）——产物不参与源码浏览
**验证：** VS Code 资源管理器不再显示四个产物目录。

### 第 0.2 轮：壳树 feature-folder 判据审计（src/App 增量 / 大文件 / 目录三维度）

#### E6#0.3 🟡 src/App 增量文件审计（主要工作）

- [ ] **E6#0.3a** 对象：0d.10-3（2026-08-19 定案）后长出的 ~10 平铺文件——window 族（windowHost/windows/windowRelocation）+ panel/floating 族（panelHost/panelReveal/panelDrift/panelCreatePicker/floatingPanelReveal）+ appearance 族（**appearanceApplier——`config/appearance.ts` 归 #0.4c 达标不拆（schema 单职责），0.4 已定案，0.3 不重审**）
- [ ] **E6#0.3b** 判据（照 0d.10-3 定案，壳目录规范 §2.5）：只被聚合器消费 → 留平铺与七功能同型；多处消费 → 下沉 `hooks/` 或 `core/`；同功能域簇且满足「功能域=文件夹」→ 分组成簇
- [ ] **E6#0.3c** 产出审计报告（每文件：消费方 + 判据命中）→ 与 #0.4/#0.5 结论**合并为一份审计报告，一次拍板**（三审计是同一审计的三切面，结论交织，一次拍板避免报告改三次前后矛盾）→ 执行（若动 = 改壳，纯代码组织非加业务，壳改动边界原则）
**验证：** 合并审计报告经用户一次拍板；改动的 `npm run check` 全绿 + 外部消费方 import 零变更。

#### E6#0.4 🟡 全树大文件多职责复核（codex 判据）

> 📖 **2026-08-30 扩 scope。** 判据对齐 codex 回应：单文件 >500-800 行，或同时管理多种生命周期/领域职责 → 拆模块；大但单职责 → 拆=返工。300 行一刀切作废。

- [ ] **E6#0.4a** 真候选（判据命中，**预判拆法草案**——执行时按草案对照，不凭空再定路径）：`electron/windows/window-manager.ts`(585——pool 生命周期 / IPC push / bounds 同步 / 内存监控 4 职责 → feature-folder `window-manager/` = index 聚合器 + pool-lifecycle / ipc-push / bounds-sync / memory-monitor 子模块) + `src/core/services/configuration/ConfigurationService.ts`(572——applier 注册 / 值读取检查 / 变更监听 / settings 读写 / 缓存 4+ 职责 → feature-folder `ConfigurationService/` = index 聚合器 + applier-registry / value-access / change-listener / settings-io / cache 子模块)
- [ ] **E6#0.4a.2** **preload-shell.ts(526) 折叠进第 1 层，第 0 层只记结论不拆**——判据命中（多域聚合一文件，与 preload-pool 每命名空间一文件不对称），但 L1 的 SDK 类型对接 + IPC handler 必动 preload，0 层拆完 L1 又改 = 双份 churn（仿 contributions→#9.1 先例折叠）
- [ ] **E6#0.4b** 弱候选：`src/core/commands/shell/coreCommands.ts`(444——聚合 6 命令组) ——**倾向不拆**（<500 行，门禁不拦非人工审区；6 命令组聚合 = 大但单职责，按 codex 判据拆=返工），审计确认即闭环
- [ ] **E6#0.4c** 达标确认不拆（大但单职责）：`pluginLoader/contributions.ts`(715，#9.1 覆盖) + `src/App/config/appearance.ts`(521，schema 单职责) + `tabCallbacks.ts`(510，纯工厂) + `ViewContainerService.ts`(492，单类) + `useTabManager/`(478，已 folder 结构)
- [ ] **E6#0.4d** 拆法铁律：拆完必须 feature-folder（聚合器 + 子模块），参照 preload-pool/linkdesk-api 先例——不许拆成更平铺
**验证：** 复核结论并入 #0.3 合并审计报告（一次拍板）；真候选拆法草案经用户拍板；改动的 `npm run check` 全绿 + 外部消费方 import 零变更。

#### E6#0.5 🟢 壳目录全树核对（目录维度）

- [ ] **E6#0.5a** 对象：壳树 ≥8 文件热点目录——`src/core/api/linkdesk-api` / `src/hooks/usePoolSync` / `electron/preload-pool` / `src/core/services/plugins/IpcBridgeHandler` / `electron/ipc/handlers` / `electron/services` / `src/core/services/ui` / `src/core/registry` / `src/pool/hooks`
- [ ] **E6#0.5b** 判据三态逐一核对（照 0d.10-3）：已「每命名空间/能力/服务一文件」= 达标确认（预期多数）；真混装 = 分组成簇；多消费者 = 下沉 hooks/core
**验证：** 核对结论并入 #0.3 合并审计报告（一次拍板）；纯核对即闭环则 0.5 无手术。

### 第 0.3 轮：防膨胀机械门禁

#### E6#0.6 🟡 防膨胀机械门禁（check-file-size）

> 📖 **2026-08-30 立项（用户拍板）。** V2 教训：MainWindow.xaml 2738 行 / code-behind 3866 / Display 3775 / Sensors 3569——无门禁持续膨胀成巨兽。一次性审计治标，机械门禁治本。判据对齐 codex：单文件 >800 行（单职责亦然）= 机械拦区；500-800 多职责 = #0.4 人工审区。

- [ ] **E6#0.6a** 新建 `scripts/check-file-size.mjs`——扫 `src/` + `electron/` 非测试源文件（跳过 `*.test.*` / `*.spec.*` / 生成物），>800 行 → exit 1；**阈值内建一份常量（注释指向 [12 档 §一·二](03-插件市场/12-插件目录与UI细节.md)——门禁=执行方、12 档=规范方，软链可接受，不造 md 解析器）；接进 `npm run check` 固定位置：audit-i18n 之后、eslint 之前**（文件尺寸是最廉价检查，放最前最快拦；现有 13 个 check 脚本同款）
- [ ] **E6#0.6b** 例外 allowlist 拍板制：合法单职责巨兽（概率极低）→ allowlist + 文件头注释注明理由，**须用户拍板**才放行（对齐 directory-flat-exception 拍板制，非「共识」）；放行例外仍须 feature-folder 不拆平铺（继承 #0.4d）
- [ ] **E6#0.6c** 市场插件门禁——check-file-size.mjs 支持 per-scope 阈值：`src/`+`electron/` = 800；`plugins/builtin/marketplace/` = [12 档 §一·二](03-插件市场/12-插件目录与UI细节.md)（views 150 / services 200 / components 150 / styles 300 / index 120）；12 档 §一·〇 对齐拆法铁律（删「不引入子目录层级」，超限走 feature-folder 聚合器+子模块——2026-08-30 用户拍板，纸面一票否决升级真门禁）；市场 scope 随 #30.11 激活（拆分 MarketplaceSidebar.css 377 后绿灯，L0-L2 不扫市场插件）
**验证：** `npm run check` 全绿（当前全树最大 737 index.css / 715 contributions，即日绿灯）；**#0.4 拆完后重跑确认仍绿灯**（拆出聚合器超 800 = 0.4 拆坏，门禁立即抓）；临时造一个 >800 文件验证红灯；marketplace scope 激活后 12 档各行超限亮红灯；allowlist 例外经用户拍板。

---

## 第 1 层：插件独立构建——每个插件独立出 .linkdesk-plugin

> 🔥 **地基。** 没有独立 build，脚手架没东西调，市场没东西发。

---

### 第 1.1 轮：@linkdesk/plugin-sdk —— plugin-sdk npm 包

#### E6#1 🔴 创建 plugin-sdk 目录与 package.json

- [ ] **E6#1a** 新建 `packages/plugin-sdk/` 目录 | 结构如下：
  ```
  packages/plugin-sdk/
    ├── package.json           # name: "@linkdesk/plugin-sdk", main: "./dist/index.js"
    ├── src/
    │   ├── index.ts           # barrel 导出
    │   ├── types.ts           # window.linkdesk.* 完整类型定义
    │   ├── vite-config.ts      # 插件 Vite build 配置（一键 build）
    │   └── validate.ts        # plugin.json 格式验证
    ├── tsconfig.json
    └── README.md
  ```
- [ ] **E6#1b** `package.json`——name/version/main/types/scripts/files
  - `"name": "@linkdesk/plugin-sdk"`
  - `"main": "./dist/index.js"`
  - `"types": "./dist/index.d.ts"`
  - `"scripts": { "build": "tsc" }`
**验证：** `cd packages/plugin-sdk && npm install && npm run build` 成功。

#### E6#2 🔴 window.linkdesk.* 类型——从 E5.8 生成契约派生

> 📖 **2026-08-16 E5.8 衔接改写。** 原方案"从 linkdesk-api.ts 手写一份干净类型"已废止——E5.8 Phase 4 生成 `linkdesk.d.ts`（编译期契约，漂移即编译错，含 sync/fire-and-forget/async 精确标注 + JSDoc）。本任务只做派生，不手写第二份真相源。**前置：E5.8 Phase 4 完成。**

- [ ] **E6#2a** `packages/plugin-sdk/src/types.ts`——`export * from "linkdesk.d.ts"`（或复制生成物）
  - [ ] 只暴露公开 API——生成器已收敛公开面
  - [ ] JSDoc 缺失处补在 E5.8 生成器标注侧（不回写生成物）
  - [ ] 命名空间真实数以 E5.8#17 摸底为准（原"26"为立项估计）
  - [ ] **2026-08-20 提示：** 现成 npm 包 `@linkdesk/contracts`（E5.8#22.6）已建好且 `types` 直指生成契约——plugin-sdk 可直接 `dependencies: "@linkdesk/contracts"` 转发，不必复制生成物（发布态见 E6#2.5）
  - [ ] **dev 过渡态（2026-08-30 审视补）**：#1-#2 期间 contracts 尚未真发布（#2.5 才发）——`package.json` 依赖先写 `"@linkdesk/contracts": "file:../../contracts"` 本地引用；#2.5 发布后改 `^0.1.0`（registry）。**防「dev 装不到包」踩空。**
- [ ] **E6#2b** `declare global { interface Window { linkdesk: LinkDeskAPI } }`——生成物自带，本任务只验证插件项目 tsc 提示
**验证：** 插件项目 `npm install @linkdesk/plugin-sdk` 后，`window.linkdesk.` 有智能提示。

#### E6#2.5 🟢 @linkdesk/contracts 真实 npm publish（E5.8#22.6 立案）

> 📖 **2026-08-20 E5.8#22.6 立案。** E5.8 Phase 4 已建独立 npm 包 `@linkdesk/contracts`（`types` 入口直指生成契约 `linkdesk.d.ts`，零构建，`files` 白名单只 d.ts）——本地 `npm pack` 出 tarball + 示例插件 tsc 验收已在 E5.8#22.6 闭环。**本任务 = 真实发布上 npm registry**（当时用户拍板：可以只 pack 不真发，发布到本任务点再做）。

- [ ] **E6#2.5a** 发布前检查——`npm run check` 全绿 + `contracts:check` 契约产物最新 + `npm pack --dry-run` 确认 tarball 只含 `README.md`/`linkdesk.d.ts`/`package.json`
- [ ] **E6#2.5b** `cd contracts && npm publish --access public`（@linkdesk/contracts——需 npm 账号 + 命名空间权限）
  - [ ] **🔥 必须显式 `--registry=https://registry.npmjs.org`**——2026-08-20 npm login + `--dry-run` 实测发现：本机全局 npm registry 配的是 npmmirror.com 镜像，`npm publish` 不带 registry 参数会去发镜像（登录态不匹配 → 认证失败/静默失败）。先 `npm whoami --registry=https://registry.npmjs.org` 确认 fengyili，再 `npm publish --registry=https://registry.npmjs.org --access public`
  - [ ] **🔥 前置：npm 登录态（2026-08-30 审视实测——当前 401 Unauthorized）**：`npm whoami --registry=https://registry.npmjs.org` 实测返回 **401**——本地登录态已失效。执行本任务前**必须先 `npm login --registry=https://registry.npmjs.org` 重新登录 fengyili**（用户名 fengyili，见 memory e5.8-contracts-npm-publish-ready），登录确认后再发布
- [ ] **E6#2.5c** 发布后验收——示例工程 `npm i -D @linkdesk/contracts@<ver>`（registry 真包）`npx tsc --noEmit` 零错误；壳内 tsconfig `paths` 别名改指 node_modules 版本（可选）
**前置：** E6#2（SDK 类型派生，决定对外命名/范围口径）。**版本联动：** 包版本 = 壳版本——生成器自动同步写入 `contracts/package.json`，漂移即 `contracts:check` 红。

#### E6#3 🔴 ~~SDK 类型从 preload-plugin.ts 自动生成——不手抄~~

> 🔴 **2026-08-16 已删——E5.8 Phase 4 吸收。** 原任务实质（preload 面类型审计 sync/fire-and-forget/async + 自动生成 + CI 门禁）= E5.8#17 摸底审计 + #19 生成器 + #21 contracts-ready 门禁（编译失败，强于原"CI warn"）。锚文件 preload-plugin.ts 已随 E5.7 清理删除（现为 preload-pool.ts / preload-shell.ts）。任务号留空不补位。

#### E6#4 🔴 Vite 插件构建配置

- [ ] **E6#4a** `packages/plugin-sdk/src/vite-config.ts`——`defineLinkdeskPluginConfig()`
  - [ ] 输入：插件入口文件路径（默认 `src/index.tsx`）
  - [ ] 输出：`dist/<pluginId>.linkdesk-plugin/` 目录
  - [ ] 内容：
    ```
    <pluginId>.linkdesk-plugin/
      ├── plugin.json          ← 原样复制
      ├── icon.svg             ← 原样复制（如果有）
      ├── i18n/
      │   └── en.json          ← 原样复制
      ├── index.bundle.js       ← Vite build——React+依赖+插件代码全打包
      └── assets/               ← Vite emit 静态资源——字体/精灵图/音效（import 即自动 emit）
    ```
  - [ ] React / react-dom / react-i18next / i18next 标记为 external（壳提供——插件不重复打包；**i18next 必须 external：插件自打 i18next 实例 → 翻译全空**，2026-08-29 审计 B3 补）
  - [ ] 其他依赖全部 inline——插件自包含
  - [ ] **静态资源随包（审计补 2026-08-29——目录→zip 回归风险）**：Vite build 的 assets/ 整目录进 zip；兜底 assetsInlineLimit 抬高可全部 base64 内联进 bundle（单文件自包含，音效稍大）
  - [ ] **README.md + CHANGELOG.md 原样复制（缝隙 K2，2026-08-29 审计补）**：作者包里有这两个文件就随包（详情页已装态数据源）；格式规范 `02-linkdesk-plugin格式规范.md` 补文件位 + `plugin.schema.json` 补 `readme` 字段（可选，作者填指向包内 README.md）
- [ ] **E6#4b** 自动读 `plugin.json` 获取 pluginId → 调 Vite build → zip
**验证：** 插件作者 `npm run build` → 产出 `.linkdesk-plugin` 文件。

#### E6#5 🟡 plugin.json 验证工具

- [ ] **E6#5a** `packages/plugin-sdk/src/validate.ts`——`validatePluginJson(path)`
  - [ ] 检查必填字段：name, version, entry
  - [ ] 检查 contributes 格式——**以 `plugin.schema.json` 为唯一真源**（读 schema 校验，不手写第二份字段清单——schema 一改 validate 即自动跟上，判据⑧归一性）
  - [ ] 检查 i18n 声明一致性
  - [ ] 输出友好的错误信息
- [ ] **E6#5b** 集成到 build 流程——`npm run build` 前自动跑 validate
**验证：** 故意写错 plugin.json → build 报清晰错误。

#### E6#6 🟢 plugin-sdk 发布到 npm

- [ ] **E6#6a** `"publishConfig": { "access": "public" }`
- [ ] **E6#6b** `npm publish`——发布到 npm registry（**🔥 显式 `--registry=https://registry.npmjs.org`，对齐 #2.5b——npmmirror 镜像陷阱同款；发布前 `npm whoami --registry=https://registry.npmjs.org` 确认 fengyili 已登录**）
- [ ] **E6#6c** plugin-sdk 独立 typecheck 进 `npm run check`（check 链加 `tsc -p packages/plugin-sdk --noEmit`——plugin-sdk 是仓库唯一不在门禁范围的代码区，坏了要等第三方装才知道，进 check 链及时拦；**2026-08-30 审视补**）
**验证：** `npm install @linkdesk/plugin-sdk` 从任意电脑可装。

---

### 第 1.2 轮：壳侧——支持加载 .linkdesk-plugin 文件

#### E6#7 🔴 loader.ts 改造——支持加载 .linkdesk-plugin 打包格式

> 📖 **承接 E5.6#84——** E5.6#84 已在 loader.ts 建立 dev/installed 双分支结构（源码 glob vs 已安装 plugin.js `import()`；原引 #81 系任务号误标，实锤见 [03-loader改造方案.md §八](01-插件独立构建/03-loader改造方案.md#八审视实锤)）。本任务在 installed 分支上扩展 `.linkdesk-plugin` zip 格式支持（解压→加载）。
> 📖 **2026-08-16 E5.8 衔接**：loader 生命周期状态机已在 E5.8#11 建立（PENDING→ACTIVE→DISPOSED）——本任务在其上扩展 zip 分支，不重造生命周期。

- [ ] **E6#7a** `loader.ts`——`loadPlugin()` 加 zip 格式分支
  - [ ] 检测：插件来源是源码目录（`plugins/builtin/` / `plugins/user/`）还是 `.linkdesk-plugin` 解压目录（`{userData}/plugins/<id>/`）
  - [ ] 打包文件：读 `plugin.json` → 读 `index.bundle.js` → 动态 import（基于 E5.6#81 的 installed 分支扩展）
  - [ ] **assets/ 整目录静态服务（审计补 2026-08-29）**：解压目录 assets/ 按相对路径静态服务——bundle 内相对 URL（字体/精灵图/音效）正确解析，不可只服务 index.bundle.js（否则 404）
  - [ ] 源码目录：保持 E5.6#81 的 dev glob 分支
  - [ ] 统一走 `loadPluginLifecycle`——不管来源，后续步骤完全相同
- [ ] **E6#7b** 打包格式解压——`.linkdesk-plugin` = zip 文件
  - [ ] 安装时：解压到 `{userData}/plugins/<pluginId>/`
  - [ ] 加载时：从解压目录读 `plugin.json` + `index.bundle.js`
**验证：** 手动放一个 `.linkdesk-plugin` → 重启 → 插件出现。

#### E6#8 🔴 运行时动态加载——替代 import.meta.glob（🔁 折叠进 E6#9，2026-08-30 第三批审视）

> 📖 **折叠注记（编号保留为稳定 ID，不执行）：** 原方案（`?entry=` URL 参数 + plugin-view:create）是 E5.6 多 WebView 时代话术，极简 Pool 下无消费点、handler 已消亡、文件指向错。意图 = #9 全部覆盖，折叠不双份 churn。**实锤依据见 [03-loader改造方案.md §七](01-插件独立构建/03-loader改造方案.md#七审视实锤)。**

#### E6#9 🔴 loader.ts + PluginComponent/state.ts import.meta.glob 全部替换——运行时文件系统扫描

> 📖 **🔥 E5.5 对照审计发现（2026-08-30 第三批审视修正文件指向）。** 打包插件不在源码树中——glob 发现不了。glob 真身分布、#8 折叠缘由、可行性实锤见 [03-loader改造方案.md §七](01-插件独立构建/03-loader改造方案.md#七审视实锤)。

- [ ] **E6#9a** `loader.ts`——`pluginModules` glob → 主进程 IPC `plugins:listAll`（🔧 新建 IPC 走 audit-api-contracts 四齐全）
  - [ ] 主进程扫描 `{userData}/plugins/builtin/` + `{userData}/plugins/user/` + (dev 模式) `plugins/builtin/` + `plugins/user/`
  - [ ] 返回 `[{ pluginId, entry, manifest }]`——统一格式
- [ ] **E6#9b** `loader.ts`——`pluginStatusBarModules` / `viewRenderModules` glob → 合并到 `plugins:listAll` 返回值（`manifest.statusBar` + `manifest.entry` 已含信息）
- [ ] **E6#9c** `loader.ts`——`pluginManifests` glob（eager）→ `plugins:readAllManifests` IPC（🔧 同样四齐全）——主进程读所有 `plugin.json`
- [ ] **E6#9d** `PluginComponent.tsx`——`pluginModules` / `viewModules` glob → 消费 `plugins:listAll` 入口 map（升级已有 `import(/* @vite-ignore */ url)` 兜底）
  - [ ] `React.lazy(() => import(/* @vite-ignore */ entryPath))`——不依赖 glob
- [ ] **E6#9e** dev 保留 Vite glob（即时代码分割），prod 走 IPC | 分支 ~5 行
- [ ] **E6#9f** 验证——dev 全插件加载（glob 路径）；build 内置 + 市场安装全加载（IPC 路径）——含 #8e 折叠的 Pool 渲染验证
**验证：** loader.ts / PluginComponent.tsx 的 glob 只在 DEV 分支保留；生产 build 零 glob，全走 IPC。

#### E6#9.1 🔴 pluginLoader feature-folder 拆分（随 #9 loader 重写同批做）

> 📖 **2026-08-29 目录审计 + 用户拍板。** pluginLoader（21 文件）混装——拆的判据 = feature-folder（聚合器 + 子模块，E5.8#0d.10-3）。**与 #9 同批做**——loader.ts 重写时一起动 import，一次到位不双份 churn。**执行序（2026-08-30 定）：先 #9 改写 glob 落 `discovery/` 目标夹，再 `git mv` 建夹，同一文件只改一遍**——详见 [03-loader改造方案.md §七·四](01-插件独立构建/03-loader改造方案.md#74-91-拆分执行序避免同一文件改两遍)。

- [ ] **E6#9.1a** 目标结构
  ```
  pluginLoader/
    loader.ts              ← 聚合器——编排入口（0d.10-3 同款，零业务逻辑）
    discovery/             ← 插件发现 + manifest：manifest.ts（+ loader 内 glob→IPC 发现逻辑随 #9 移入）
    lifecycle/             ← lifecycle.ts / lifecycle-events.ts / lifecycle-ops.ts
    contributions/         ← contributions.ts / viewRegistry.ts（含 floatingPanelDeclarers 逻辑）/ i18nResources.ts
    resolution/            ← dependencies.ts / state.ts / loadState.ts / runtime.ts（+ loader 内 revert 逻辑随 #9 移出）
  ```
- [ ] **E6#9.1b** 测试同夹跟随（colocated）——floatingPanelDeclarers.test→contributions/（测 viewRegistry）；revert.test→lifecycle/ 或 resolution/（测 loader 内 revert 逻辑，随归位）；activationEvents.test 随目标归位
- [ ] **E6#9.1c** `git mv` 保历史 + import 全量更新（pluginLoader 外部消费方路径同步）
- [ ] **E6#9.1d** 验证——`npm run check` 全绿（loader 相关 9 测试全过）；跨子模块 import 零残留
**验证：** pluginLoader 目录只剩聚合器 loader.ts + 4 功能夹；子模块零跨 import（只 import `../core/` 等壳依赖）；外部消费方 import 全量更新无残留。

#### E6#10 🟡 installed-plugins.json——已安装插件记录（🔁 折叠进 E6#12，2026-08-30 第 1.2 轮审视）

> 📖 **折叠注记（编号保留为稳定 ID，不执行）：** 与 E6#12 PluginInstallService 高度重叠——#10b 读写 == #12a 内部持久化、#10c 市场判断 == #12b isInstalled。格式契约（#10a）并入 #12a，读写实现全归唯一 owner（设计决策#20）；StorageService 仅通用 key-value 通道，不算第二个 owner。实锤见 [03-loader改造方案.md §八](01-插件独立构建/03-loader改造方案.md#八审视实锤)。

#### E6#11 🟡 安装/卸载/更新单一路径

> 📖 **2026-08-16 E5.8 衔接**：卸载残留由 E5.8#12 机械保障（disposer 逆序回滚）——本任务只管市场侧流程，不再手动验证残留。

- [ ] **E6#11a** 安装流程——下载 → 解压 → 写 installed-plugins.json → loadPlugin → UI 刷新
- [ ] **E6#11b** 卸载流程——确认 → 关标签页 → unloadPlugin → 删目录 → 删记录
- [ ] **E6#11c** 更新流程——卸载旧版 + 安装新版
- [ ] **E6#11d** 🔥 路由表验证——装/卸插件后确认规则自动满足
  - [ ] 规则 1（登记注销对称）：E5.8 Phase 2 已机械保障（disposer 逆序回滚）+ E5.8 回归矩阵场景 2 覆盖——此处只做安装/卸载实机抽查
  - [ ] 规则 2（路由感知变化）：安装/卸载后 FactorySlots 重扫——`Ctrl+,` 永远打开当前有效的设置插件
  - [ ] 规则 3（关联桌子同步）：installed-plugins.json 与实际文件系统一致（删文件=删记录，装文件=加记录）
- [ ] **E6#11e** 🔥 `.disabled/` 退役——市场时代真删除
  - [ ] 市场安装的插件：卸载 = `fs.remove()` 真删除（市场可重新下载）
  - [ ] bundled 插件（`source: "builtin"`）：卸载 = 真删除。壳检测缺失 → 从 `bundled-plugins/` 自动恢复 + toast 通知（`bundled-plugins/` 机制与第 1.3 轮 E6#15 内置插件独立 build 的目录衔接待 #15 对齐，2026-08-30 第 1.2 轮审视轻标）
  - [ ] 旧 `.disabled/` 目录：E6 迁移时清理——已禁用的插件如果市场有 → 提示"从市场重新安装或彻底删除"
**验证：** 卸载市场插件 → 目录消失。卸载 builtin 插件 → 目录消失 → 重启 → 自动恢复 + toast "已恢复系统插件 settings"。
**验证：** 安装→卸载→不残留。再安装→正常。路由表不指死 ID。

#### E6#12 🔴 PluginInstallService——installed-plugins.json 唯一 owner（🔁 吸收 E6#10）

> 📖 **承接 E5.6#83——** E5.6#83 已纸面设计 `plugin-install-handlers.ts` 基本 install/uninstall/enable/disable IPC 骨架，但**该文件从未落地（E5.6 封存 51% 纸面完成）**——E6#13 从零建。本任务创建 PluginInstallService 作为 `installed-plugins.json` 的**唯一读写入口**，防止 loader / marketplace / 安装流程三处直读 JSON。
> 🔥 **设计决策#20：每个持久化文件只有一个 owner service。** 当前 E6 方案中 loader / marketplace / 安装流程三处读写 `installed-plugins.json`——没有明确谁拥有这份数据。必须建一个单一 owner。

- [ ] **E6#12a** 新建 `src/core/services/PluginInstallService.ts`（**吸收 E6#10**——格式契约见下，读写实现全归本 owner）
  - [ ] `getInstalled(): Record<string, InstalledPlugin>` ——读取全量
  - [ ] `add(pluginId, info)` ——安装时写入
  - [ ] `remove(pluginId)` ——卸载时删除
  - [ ] `isInstalled(pluginId): boolean`
  - [ ] `getSource(pluginId): "builtin" | "user" | "marketplace"`
  - [ ] 内部走 StorageService 持久化——`installed-plugins.json`（独立文件，不混入 settings.json；StorageService 按 key 独立文件已支持——`_filePath` Map + `getFilePath`；**命名统一——全清单只此一个名**，2026-08-29 审计 B4 修）
  - [ ] 格式契约（E6#10a 原样并入）：
    ```json
    {
      "serial-monitor": {
        "version": "1.0.0",
        "installedAt": "2026-08-07T12:00:00Z",
        "source": "marketplace"
      }
    }
    ```
- [ ] **E6#12b** 所有其他模块改为调 PluginInstallService——不直接读 JSON
  - [ ] loader.ts → `PluginInstallService.getInstalled()`
  - [ ] marketplace UI → `PluginInstallService.isInstalled(id)`（吸收 E6#10c——市场"已安装"判断）
  - [ ] 安装/卸载流程 → `PluginInstallService.add()` / `PluginInstallService.remove()`
**验证：** 安装一个插件 → 记录存在。卸载 → 记录删除。`grep -r "installed-plugins" src/ plugins/` → 仅 PluginInstallService.ts 一处。

#### E6#13 🔴 E6 IPC handler 扩展——下载/解压/更新/进度（基于 E5.6#83）

> 📖 **承接 E5.6#83——** E5.6#83 已纸面设计 `plugin-install-handlers.ts` 基本 install/uninstall/enable/disable IPC 骨架，但**该文件从未落地（纸面完成）**——本任务**从零新建**并直接扩展 download/extract/update/progress handler。
> 📖 **🔥 2026-08-07 E5.5 对照审计发现。** 当前 `electron/ipc/` 下 8 个 handler 文件，没有一个覆盖 E6 需要的核心操作。E6#11 安装流程和 E6#31 下载安装 UI 都假设这些 handler 已存在。

- [ ] **E6#13a** `electron/ipc/handlers/plugin-install-handlers.ts`——新建（与现有 11 个 handler 同目录）
  - [ ] `plugins:download(url)`——`fetch` + 进度回调 → ArrayBuffer 写入临时目录 | ~20 行
  - [ ] `plugins:extract(pluginId, zipPath)`——解压到 `{userData}/plugins/user/<pluginId>/` | ~15 行
  - [ ] `plugins:install(pluginId)`——读 `plugin.json` → 验证 → `PluginInstallService.add`（E6#12 已建）→ loadPlugin → IpcBridge.broadcast `plugin:installed` | ~20 行
  - [ ] `plugins:uninstall(pluginId)`——E6#11b 流程 | ~15 行
- [ ] **E6#13b** `plugins:update-check`——对比本地版本 vs marketplace.json → 返回更新列表 | ~15 行
- [ ] **E6#13c** `plugins:update-download(pluginId)`——下载新版本 → 替换旧目录 → 重新 loadPlugin | ~20 行
- [ ] **E6#13d** `plugins:install-progress`——`ipcMain.on` + 下载进度 `event.sender.send` 回调 | ~15 行
- [ ] **E6#13e** `preload-shell.ts` + `preload-plugin.ts`——暴露 `window.linkdesk.pluginManager.install/installWithProgress/uninstall/checkUpdates/update` | +10 行
- [ ] **E6#13f** `linkdesk-api.ts`——PluginManager API 类型声明 | +15 行
- [ ] **E6#13g** invoke 统一日志（2026-08-17 用户拍板立案——记忆欠账"留待 E3 后做"）
  - `electron/ipc/` 统一 invoke 包装：warn 日志含 channel + args 摘要 + 耗时；错误带堆栈——**E6 新 handler（13a-13f）从第一天走包装，存量 handler 逐个接入**
  - 与 E5.7 诊断面合并不另起炉灶：主进程 protocol-debug.log 已有 console.error 速查表——统一包装汇入该通道
  - 验收：故意让 handler 抛错 → protocol-debug.log 可见 channel + 原因；E6#13 新 handler 全走包装
**验证：** 从壳侧 `window.linkdesk.pluginManager.install(url)` → 下载 → 解压 → 安装 → 图标栏出现新插件。从插件 WebView 同调同结果。

#### E6#13.5 🟡 notifications.show 主动作按钮 API——toast 内 [重试]/[查看依赖] 统一通道（缝隙 K1，2026-08-29 审计定稿）

> 📖 **审计坐实**：mockup 帧 3/7 的 toast 主动作按钮（[重试]/[查看依赖]）当前 `notifications.show` **无 actions 参数弹不出**；但 ToastHost 渲染面 + IPC 回传已实现（lifecycle.ts:83 [撤销] 走壳侧 pushToast 先例实证）。本任务给**插件面**补同一能力——mockup 验收契约（09 §五「插件自调 notifications.show」）成立。
> 📖 **设计前置（8 维度）：**
> - ① 能力边界：**壳能力**（notifications 命名空间扩展，任意插件可用——多消费方）。保底：不传 actions → 现状无按钮零变化。
> - ② API：`notifications.show(msg, { type, progress, actions? })`——`actions: [{ id, label, isPrimary?, command?, args? }]`；点击 → 壳执行 `command(args)`（命令系统 = 现成跨层动作通道）。契约位置：contracts/linkdesk.d.ts + preload-pool/namespaces-workspace.ts + IpcBridgeHandler/ui.ts + 03-制造文档。
> - ③ 通信：插件 show（带 actions）→ 壳建 toast（存 actions）；点击 → 壳 `runToastAction` → **对带 command 的 action 执行 `executeCommand(command, args)`**（复用现成命令执行，壳零新事件通道）；无 command → 现状 closure 兜底。
> - ④ 壳侧：`IpcBridgeHandler/ui.ts`（showNotification 透传 actions）+ toast 服务（TTL 按 severity 对齐——插件 error 类 8000，mockup 帧 3）+ `runToastAction` 分流 command/closure。
> - ⑤ 插件侧：市场插件失败 toast 传 `[重试]`（command = 重试安装）、缺依赖 toast 传 `[查看依赖]`（command = 跳依赖详情页）；命令 handler 插件自注册。
> - ⑥ 显示：toast 底部 action 按钮（primary/secondary，ToastHost 已具，mockup 帧 3/7）。
> - ⑦ 配置：无。
> - ⑧ 验收：`npm run check` 全绿 + CDP 实机弹 [重试] 点它真的重试、[查看依赖] 真的跳转。

- [ ] **E6#13.5a** 契约 + 类型——`contracts/linkdesk.d.ts` + `linkdesk-api/ui.ts` 加 `actions?: ToastAction[]`（`{id,label,isPrimary?,command?,args?}`）；`NotificationHandle` 不变
- [ ] **E6#13.5b** preload-pool `namespaces-workspace.ts`——`show()` 透传 options.actions（序列化 `{id,label,isPrimary,command,args}`）
- [ ] **E6#13.5c** `IpcBridgeHandler/ui.ts`——showNotification 建 toast 时存 actions（引用壳侧既有 serializeToasts 形态）；`runToastAction` 分流：action.command → `executeCommand(command, args)`；无 command → 现状 onClick
- [ ] **E6#13.5d** TTL 对齐——插件 error 类 toast 落 `TOAST_TTL_ERROR`（8000），mockup 帧 3 契约（非 progress 时当前落 6000）
- [ ] **E6#13.5e** 03-插件制造文档——notifications.show actions 说明 + 契约四齐全审计（audit-api-contracts 入 check）
- [ ] **E6#13.5f** 市场消费先例——失败 toast [重试] / 缺依赖 toast [查看依赖] 各一实现点（消费 30.9b/30.5e，落迁移后 DetailView）
**验证：** 插件 `notifications.show("安装失败", { type:"error", actions:[{id:"retry",label:"重试",isPrimary:true,command:"xxx.retry"}] })` → toast 带按钮 → 点击执行命令。不传 actions → 现状无按钮。

#### E6#14 🔴 ~~handler 审计——统一走 IpcBridge.broadcast 不直发 mainWindow~~

> 🔴 **2026-08-16 已搬 E5.8 Phase 1.5 #6.5**（IPC 广播归一化——serial/lsp/file 三 handler 改走 IpcBridge.broadcast + ipc-audit 脚本入 check）。任务号留空不补位。

---

### 第 1.3 轮：共享组件分发 + 内置插件独立化——打破特权阶级

> 🔥 **核心无知原则。** 当前内置插件（file-tree/editor/settings/marketplace/serial-monitor）和壳一起 Vite build，依赖从全局 node_modules 读。这意味着壳必须带着 monaco-editor、serialport 等"插件专属"依赖——壳被污染了。
> 内置插件本质也是插件——`core: true` 只是不让卸载的 UI 限制，去掉就能卸。它们也应该走 pre-bundle。
> **组件特权（同轮第一桩特权，E6#54）。** 壳共享组件（右键菜单/下拉/开关/取色器）在 `src/components/shared/`，内置插件靠 Vite `@src` 别名编译期引用；第三方插件没有 `@src`——想用壳组件只能来让壳改代码，违反插件独立铁律。E6#15 把内置插件也独立 build 后，连内置插件都失去 `@src` → 共享组件必须先打包分发（E6#54 是 E6#15 硬前置）。

#### E6#54 🔴 共享组件独立分发 + token 契约（方向 A——2026-08-29 用户拍板）

> 🔥 **方向 A 裁决（候选讨论后拍板）。** 用户三处右键实机证据（文件树右键 / 串口接收区右键 / 设置齿轮右键 引同一个 ContextMenu）证明：共享机制对、分发路径错——只有能 import `@src` 的特权插件能用。方案 A = 打包 `@linkdesk/ui` npm 包 + SDK 内置 eslint token 门禁；否决方案 B（命令式 API——盖不住嵌入控件：齿轮/下拉要摆进插件布局的 React 组件；且两套契约违反归一化）；A 是现状延续 = VS Code `webview + @vscode/webview-ui-toolkit` 模型。
> **设计前置 8 维度 →** [07-共享组件独立分发设计.md](01-插件独立构建/07-共享组件独立分发设计.md)。

- [ ] **E6#54a** 组件解耦审计——全量盘点 `src/components/shared/` 每组件三依赖轴：① 纯 props 契约（数据经 props 传入）② var(--*) token 禁 hex ③ window.linkdesk.* API 禁 @src/core import；列出带专属假设的组件（@src/core 依赖 / 非 token 色值 / 非 props 数据源）→ 解耦改造清单
  - [ ] **type-only import 重定向（审计补 2026-08-29）**：3 处类型 import 出包后 d.ts 挂 @src 别名不可解析——ContextMenu.tsx:26（MenuItemDescriptor）+ DynamicSelect.tsx:16 + ThemePicker.tsx:14（RecipeMeta）→ `@src/core/api/linkdesk-api(types)` → **`@linkdesk/contracts`**（类型已在 :211/:382，一行改）
  - [ ] **组件清单修齐（审计补 2026-08-29）**：实盘 16 个含 ColorPicker/PluginIcon（settings 色板、marketplace 图标），ConfirmDialog/OverlayPortal 标「在范围未消费」
- [ ] **E6#54b** `@linkdesk/ui` 包——`packages/linkdesk-ui/`；**壳 workspace 单一源码防漂移**（组件仍居 `src/components/shared/`，npm 包是构建产物/引用非拷贝）；React 组件 + 零 @src/core 依赖 + 全 token；npm 发布（账号 fengyili；npmmirror 不能发布 → 显式 `--registry=https://registry.npmjs.org`）
  - [ ] **repo 侧接线（#54c 硬前置，审计补 2026-08-29）**——workspaces 或 file: 依赖让内置插件能 `import "@linkdesk/ui"`（现仓库无 packages/、无 @linkdesk 依赖，29 处切换编不过）
  - [ ] **sideEffects: ["**/*.css"] + ESM 具名导出（审计补 2026-08-29）**——防 tree-shaking 失效（只用 Toggle 的插件不背全部组件）
- [ ] **E6#54c** 消费方切换——现有 4 插件 13 文件 33 处 import 全量 `@src/components/shared/*` → `@linkdesk/ui`（**E6#15 独立化的前置，先切后独立**；2026-08-30 第 1.3 轮审视实测计数 13 文件 33 处）
  - [ ] settings（4 文件）——`views/SettingsView/SettingRow.tsx`（ContextMenu/ColorPicker）+ `views/SettingsView/renderControl.tsx`（Toggle/SelectBox/DynamicSelect/FontFamilySelect/FilePathInput/NumberInput/Slider/sliderStep/ThemePicker/SegmentedRadio/Button）+ `views/SettingsView.tsx`（InlineInput）+ `index.tsx`
  - [ ] serial-monitor（9 处）——index.tsx:41,45（SelectBox/ContextMenu）+ ControlPanel.tsx:20,22（SelectBox/Combobox）+ SerialSettingsView.tsx:14-16（Toggle/SelectBox/FormRow）+ SessionListItem.tsx:9-10（InlineInput）
  - [ ] file-tree（2 处）——FileTreeContextMenu.tsx:10（ContextMenu）+ FileTreeNode.tsx:12（InlineInput）
  - [ ] marketplace（4 处）——ExtensionItem.tsx:9-10（PluginIcon/ContextMenu）+ UninstalledListView.tsx:8 + DisabledListView.tsx:8（PluginIcon）
  - [ ] 第三方插件直接 `npm i @linkdesk/ui`
  - [ ] **白名单定死（审计补 2026-08-29）**：清零后 PLUGIN_IMPORT_WHITELIST 的 @src/components/shared/* **收紧为 error** 防内置插件滑回双入口（第三方零影响——@src 别名只在壳构建，物理不可达）
- [ ] **E6#54d** 门禁机制定案——plugin-sdk 新增 `eslint/` 预设 + 知情声明机制（07 设计 §六；**推荐/警告/知情绕行三档**，15 条规则，**永不 fail build / 上传**）：
  - [ ] 推荐：脚手架模板默认正路（var()/t()/@linkdesk/ui），照抄即对
  - [ ] 警告·React 通用陷阱 6——no-async-init-guard-only / no-effect-callback-without-active-guard / no-dynamic-import-in-effect-cleanup / no-ref-current-in-jsx / react-hooks/rules-of-hooks / react-hooks/exhaustive-deps（任何 React 插件通用，WARN 自解释）
  - [ ] 警告·视觉统一 5——no-hardcoded-zh（→t()）/ **check-css-hardcode.mjs 移植**（hex+rgb/hsl 全拦——强化版 hex 门禁）/ **check-font-scale-audit.mjs 移植**（字号裸 px，现成脚本直接搬）/ no-hardcoded-radius（border-radius 裸 px，新写）/ import-x/no-unresolved
  - [ ] 警告·审美 4——no-quickpick-render-item / **check-spacing-grid.mjs 移植**（4px 节奏）/ @typescript-eslint/no-explicit-any / jscpd
  - [ ] 知情绕行：红灯须**标准 eslint-disable 格式**（`// eslint-disable-next-line <rule> -- 理由` 或文件级 `/* eslint-disable <rule> -- 内容画布 */`，非新语法——壳 no-hardcoded-hex 现有报错即此格式）声明；构建/上传不硬拦，无声明=「未处理偏离」、有声明=「知情偏离」
  - [ ] 市场标签：上传不拒收，偏离统计（N 处知情硬编码）随包可见
- [ ] **E6#54d.5** 第三方纪律文档——`docs/03-插件制造/05-UI写法规约.md` 新增「token 契约」节五块（作者知道有哪些变量才写得出动态 + 内容/数据纪律 + 壳纪律转移落点）
  - [ ] ① token 目录**六域矩阵**——每域标 var() 键 + 门禁覆盖：颜色（--text/--bg/--accent；fontTone 控 --text-*）/ 表面（--surface-*）/ 圆角（--radius-*）/ 玻璃（--glass-*）/ 字号（--font-size-*；--ui-scale 缩放轴）/ 字体族（--font-ui）；var(--*) = 自动跟随主题 + 玻璃 + 全局缩放
  - [ ] ② 诚实边界/知情绕行语义——作者硬写死品牌色壳管不住（任何生态都管不住）；门禁把做对变默认、做错变显式红但可**知情绕行**（标准 eslint-disable 声明带理由 = 浏览器危险网站「仍要访问」）；豁免类别明确：var() 回退 hex / 取色器色板 / canvas 绘图 / 引擎区 / **内容画布视图级豁免**——**内容 vs chrome 边界**（游戏/数据可视化/canvas 主区 = 内容世界非 UI chrome，UI chrome 跟主题走、内容有自己的天空）；视图级一次声明豁免，门禁对内容区不逐条亮灯（愤怒的小鸟考核实证：游戏背景/红心/表扬语全走内容豁免）
  - [ ] ③ 非 CSS 引擎适配模式——引擎自渲染不走 CSS 变量，用翻译桥：订阅 theme:changed → 翻译成引擎 theme API（theme-sync.ts 先例）；monaco 现状背景不透明 = 过渡态绕开，不落死边界
  - [ ] ④ 插件私有数据通道——作者自建设置界面/自管存档（不走壳设置页声明）时数据落地按语义选：**linkdesk.pluginState 首选**（E5#71 正牌 API 集中缓存+文件持久化，审计补 2026-08-29）/ config.set 私有命名空间（跟设置语义走）/ linkdesk.filesystem（大文件/二进制）/ localStorage（补充临时缓存）；铁律：**不声明 contributes.configuration 的私有键不进壳设置页 UI**（无 schema 键照写持久化，作者要壳设置页就声明贡献、要自建界面就私有键直存）
  - [ ] ⑤ 壳纪律转移清单——按落点沉对应规范文档：插件独立唯一入口 window.linkdesk.* 禁 import 壳 / 配置订阅不读一次 / linkdesk.events 订阅时序（顶层或守卫）/ 不写死其他插件 ID / 配置键必给 default / 只用文档化 API 命名空间（01-API契约 / 03-contributes / 06-plugin.json 各就各位）
- [ ] **E6#54e** 脚手架示范 + 端到端验证——create-linkdesk-plugin 模板 `src/index.tsx` 用 var(--*) 示范（玻璃/主题自动跟随）；第三方独立构建插件用 @linkdesk/ui 的右键/下拉/开关与内置插件一致 + 玻璃切换自动跟随（@linkdesk/ui 控件层零思考跟随 = 玻璃达标下限）
**验证：** 内置插件独立 build（E6#15）后共享组件仍工作（走 @linkdesk/ui）；第三方插件不用 `@src`、`npm i @linkdesk/ui` 写出与内置同款右键/下拉/开关，玻璃切换自动跟随（var(--*) 天然继承）；门禁 15 条 WARN 级自解释——硬编码（hex/中文字面量/字号裸 px/圆角裸 px）即红但可知情绕行（标准 eslint-disable 声明），var(--*)/t() 零警告，永不 fail build / 上传。

#### E6#15 🔴 内置插件迁移为 pre-bundle

> ✅ **serialport 定性方案①（2026-08-30 第 1.3 轮审视拍板）**：serialport 留壳里——electron-builder npmRebuild 自动保证 ABI，serial-monitor 走 `window.linkdesk.serial.*` 通道（数据源后门通道语义：UI 独立 build + 原生设备层壳托管），**package.json 不列 serialport**。否决方案②（native .node 绑 Electron ABI，每次升级 Electron 大版本都要插件侧 rebuild，永久维护债）。实锤见 [03-loader改造方案.md §九](01-插件独立构建/03-loader改造方案.md#九审视实锤)。

- [ ] **E6#15a** 插件清单全量盘点（2026-08-30 第 1.3 轮审视补全——现状 user/ 17 个，原列 6 个严重过时，实锤见 [03-loader改造方案.md §九](01-插件独立构建/03-loader改造方案.md#九审视实锤)）
  - [ ] **React 独立 build（8 个，加 package.json + `npm run build` 产 index.bundle.js）**
    - [ ] `plugins/builtin/`：file-tree / editor / settings / marketplace——依赖 `@linkdesk/plugin-sdk` + `@linkdesk/ui`
    - [ ] `plugins/user/`：serial-monitor（**不列 serialport**——方案①留壳，走 `window.linkdesk.serial.*`）/ python / floating-panel-demo / panel-demo（demo 不例外——#15f「壳零插件代码」要求全量分发，dev/prod 一致）
  - [ ] **JSON 纯打包（12 个，无 build 步骤——zip 含 plugin.json + JSON 资源，无 index.bundle.js；loader 只注册贡献不加载入口 → #7 路径 C 需支持无 entry 分支，实现时补）**
    - [ ] `plugins/user/`：lang-defaults / lang-test-ja / theme-aurora-glass / theme-defaults / theme-iconset-pastel / theme-mint-soda / theme-panorama / theme-pill / theme-songti / theme-terminal / theme-twilight-forest / theme-zones
- [ ] **E6#15b** 每个内置插件独立 `npm run build`（React 8 build + JSON 12 纯打包）
  - [ ] 产出 `builtin/` 下：file-tree / editor / settings / marketplace 各一个 `.linkdesk-plugin`
  - [ ] 产出 `user/` 下：React 插件（serial-monitor / python / floating-panel-demo / panel-demo）各一个 `.linkdesk-plugin`
  - [ ] 产出 `user/` 下：JSON 插件（lang-defaults / lang-test-ja / theme×10）各一个 `.linkdesk-plugin`（纯打包无 build）
- [ ] **E6#15c** `electron-builder.yml`——插件文件放到 `extraResources/bundled-plugins/`，**保留 `builtin/`/`user/` 双层结构**（builtin/user/core 语义 → [01-插件独立构建/06-builtin-user-语义规范.md](01-插件独立构建/06-builtin-user-语义规范.md)，2026-08-29 审计补引用）
  - [ ] `bundled-plugins/builtin/`——file-tree / editor / settings / marketplace 的 `.linkdesk-plugin`
  - [ ] `bundled-plugins/user/`——serial-monitor / lang-defaults / theme-* / python 的 `.linkdesk-plugin`
  - [ ] 壳首次启动时自动安装 `bundled-plugins/` 下所有文件 → 解压到 `{userData}/plugins/builtin/` 和 `{userData}/plugins/user/`（保持子目录）
  - [ ] 🔥 自动恢复通知——如果检测到 bundled 插件被删后重新恢复，给 toast："已恢复系统插件 <name>"（Opt-IN 原则——用户删了又被恢复，必须告知）
  - [ ] 🔥 **故意删除不恢复**——读取 `installed-plugins.json` 的 `removed` 标记。用户通过卸载 API 删除的 `core:true` 插件 → 写 `removed: true` → 重启时跳过自动恢复。手动删文件夹（无标记）→ 壳认为是意外丢失 → 自动恢复。防止：高手卸载旧 settings 装自己的 → 壳悄悄恢复老的 → FactorySlots 还是老的
- [ ] **E6#15d** 验证——`npm run electron:build` → 安装 → 启动 → 内置插件从 bundled-plugins 自动安装 → 全功能正常
- [ ] **E6#15e** 🔴 LSP 路径解析——插件自带的 LSP 二进制，壳能正确定位
  > **根因：** E5#114d 的 pyright 放 `extraResources` 是临时方案。E6 插件自包含后，pyright 在 `{userData}/plugins/user/python/node_modules/pyright/`，不在 `resources/`。`lsp-handlers.ts` 的 `resolveLspArg` 需要知道去插件目录找。
  > **同样的 bug 会发生在每个自带 LSP 的插件：** C 语言（clangd）、Java（jdtls）、Rust（rust-analyzer）……每个插件的 LSP 二进制路径都不一样。
  - [ ] `lsp-handlers.ts` `resolveLspArg` 加 fallback——`process.resourcesPath` 找不到 → 去插件目录查：`{userData}/plugins/{pluginId}/` + 相对路径
  - [ ] `plugin.json` 的 `lsp.args` 中相对路径的基准明确为**插件根目录**（文档化）
  - [ ] C 语言插件带 `clangd` 验证——`lsp.args: ["node_modules/.bin/clangd", "--stdio"]` → 壳正确解析到 `{userData}/plugins/user/c/node_modules/.bin/clangd`
  - [ ] 从 `electron-builder.yml` 删除 `- from: node_modules/pyright`（临时方案的清理——见 [[lsp-packages-in-extraresources-temporary]]）
  - [ ] 验证——python 插件独立构建后 Ctrl+Click 跳转仍工作
- [ ] **E6#15f** vite.config.ts `scanPluginEntries` 清理——插件 entries 不再作为 rollup input（壳 build 不再包含任何插件代码；E6#9（#8 折叠）只覆盖 loader/PluginComponent/state glob，rollup input 是漏网——2026-08-29 审计 C2 补）
- [ ] **E6#15g** preload-pool 结构核对（2026-08-29 目录审计结论）——**不随 #15 做目录手术**：preload-pool 已是「每命名空间一文件」feature 粒度（commands/configuration/contextkey/decorations/events/factory-slots/language/layout/panel/quickpick/settings/surface-zones/toast-dialog/viewcontainer 单 namespace 桥 + namespaces-data/plugin/workspace 三聚合文件），对齐 feature-folder 理想态；安全敏感 preload + 纯外观移动零收益。**内置插件迁出后 API 面不收缩**——第三方插件仍需全 `window.linkdesk.*`，只核对 namespaces-* 聚合文件是否覆盖迁出插件所需
**验证：** 内置插件和第三方插件走同一条加载路径（`loadPlugin({ source: "bundled" })`）。壳 Vite build 不再包含任何插件代码。LSP 路径解析与插件目录无关——每个插件自带 LSP，壳只知道去插件目录找。

#### E6#16 🔴 壳 node_modules 瘦身

- [ ] **E6#16a** 审计——壳自己真正需要哪些依赖？（2026-08-30 第 1.3 轮审视实测：@codingame 已不在 deps/devDeps 删；serialport/iconv-lite 方案①留壳，删移出项）
  - [ ] 壳需要：react / react-dom / react-i18next / i18next / electron / vite / typescript / eslint / vitest / lucide-react / @vscode/codicons + **serialport + iconv-lite（方案①留壳，串口通道壳托管）**
  - [ ] 插件需要（应移除）：monaco-editor / monaco-languageclient / @codemirror/* / canvas
  - [ ] 待确认：**@codemirror/*（editor 还是 serial-monitor 的依赖？确认归谁后 16b 才移——2026-08-29 审计 C4 补）** + canvas（FontFamilySelect 字体检测？壳功能→保留 / 插件功能→移出）
- [ ] **E6#16b** 从壳的 `package.json` 移除插件专属依赖
  - [ ] 移出 `monaco-editor`、`monaco-languageclient`（→ editor 插件）
  - [ ] 移出 `@codemirror/*`（**待确认归谁后移，见 16a**）
  - [ ] 移出 `canvas`（如果确认只有插件在用）
- [ ] **E6#16c** 这些依赖移到各自插件的 `package.json` 中
  - [ ] editor 插件 → monaco-editor + monaco-languageclient（+ @codemirror 如归 editor）
  - [ ] serial-monitor 插件 → **不迁依赖**（serialport/iconv-lite 方案①留壳）
- [ ] **E6#16d** `npm install`——验证壳自己的依赖完整，能启动
**验证：** 壳 `package.json` 的 `dependencies` 只包含壳自己需要的包（含留壳的 serialport/iconv-lite）。插件依赖在各自插件的 `package.json` 中。

#### E6#17 🔴 electron-builder.yml 改造

- [ ] **E6#17a** `files` 字段——不再打包 `plugins/` 源码目录（内置插件已 pre-bundle）
  - [ ] 移除或缩小 `extraResources: plugins`——只保留必要的 JSON 文件（如 lang-defaults 的语言包 JSON）
- [ ] **E6#17b** `node_modules` 过滤——只打包壳真正需要的依赖
  - [ ] 移除 monaco-editor、@codingame、serialport 等插件专属包的 shipping
  - [ ] 安装包体积显著缩小
- [ ] **E6#17c** 验证——`npm run electron:build` 成功 → 安装 → 启动 → 内置插件自动安装 → 全功能正常
**验证：** 安装包体积明显变小。所有功能正常。

#### E6#18 🟡 core:true 可卸载化 + 重新安装路径

- [ ] **E6#18a** `lifecycle-ops.ts`（E5.8#1c 去重后的卸载守卫位置，非 loader.ts；[lifecycle-ops.ts:252](src/pluginLoader/lifecycle-ops.ts#L252)）——`core: true` 插件允许卸载（去掉 API 硬阻止）
  - [ ] 卸载时给警告 toast："这是系统插件，卸载可能影响基本功能"
  - [ ] 不阻止——高手可以换掉
  - [ ] 🔥 **故意删除标记**——卸载 API 删除时在 `installed-plugins.json` 写 `removed: true`。重启时 E6#15c 自动恢复读此标记 → 跳过恢复。手动删文件夹（无标记）→ 视为意外丢失 → 照常自动恢复 + toast
- [ ] **E6#18b** 重新安装来源
  - [ ] 从 `bundled-plugins/builtin/` 恢复——壳自带备份，秒恢复（前提：无 `removed` 标记）
  - [ ] 从插件市场重新下载——下载到 `user/` 目录（市场和 builtin/ 无关）。下载时清除 `removed` 标记
  - [ ] 手动丢 `.linkdesk-plugin` 到 `builtin/` 文件夹
- [ ] **E6#18c** 场景验证——替换 builtin settings 为自己的
  - [ ] 通过卸载 API 删 `{userData}/plugins/builtin/settings/` → `removed: true` 写入 → 设置消失
  - [ ] 装自己的 settings 插件（声明 `factoryRole: "settings"`）→ FactorySlots 自动接管
  - [ ] 重启 → 壳检测 bundled settings 缺失但 `removed: true` → **不恢复** ✅
  - [ ] `Ctrl+,` → 打开的是新 settings ✅
  - [ ] 从市场搜 "settings" → 下载 → 清除 `removed` 标记 → 恢复正常
**验证：** 卸载 file-tree → 警告 → 消失 → 重启不自动恢复。从市场重新安装 → 进 user/ → 重启仍在。

#### E6#19 🟢 内置插件独立构建文档

- [ ] **E6#19a** 新建 `01-插件独立构建/05-内置插件迁移指南.md`——记录迁移步骤、原理、验证清单
**验证：** 新 AI 按文档能把一个内置插件从"和壳一起 build"迁移到"独立 pre-bundle"。

#### E6#20 🟡 协议统一路径解析——扫描 → Map 直查（2026-08-30 第 1.3 轮审视现状重写，实锤见 [03-loader改造方案.md §九·四](01-插件独立构建/03-loader改造方案.md#94-20-现状重写依据)）

> 🔥 **硬约束#18 + 大厅模型 规则 2。** 现状（E5.7#82 已抽纯函数）：`electron/plugins/protocol.ts` 调 `resolveLinkdeskPath(pluginsDir, scanPluginSubdirs(pluginsDir), urlPath)`（`src/core/utils/path/linkdeskProtocolPath.ts`）——每次请求动态扫描子目录（builtin > user > 字母序），已非硬编码回退链。E6 加 `{userData}/plugins/` 后扫描面更大。**改为 Map 直查**（插件位置由 #9 `plugins:listAll` 一次性构建，不每次扫目录）——消位置推断，对齐硬约束#11。

- [ ] **E6#20a** 新建 `src/core/services/PluginPathResolver.ts`
  - [ ] 维护 `Map<pluginId, resolvedPath>`——**吃 #9 `plugins:listAll` 返回值**构建（含 builtin/user/bundled 全部来源）
  - [ ] `resolve(pluginId): string | null`——返回插件根目录路径
  - [ ] 订阅 PluginInstallService 变化（安装/卸载）→ 增量更新映射
- [ ] **E6#20b** `electron/plugins/protocol.ts` 改为调 `PluginPathResolver.resolve(pluginId)`——不再扫描子目录
- [ ] **E6#20c** 验证——`linkdesk://` 协议在 dev 和 production 下都能正确解析内置插件、用户安装插件、市场下载插件
**验证：** `src/core/utils/path/linkdeskProtocolPath.ts` 无 `builtin > user` 硬编码排序；协议命中全走 Map。

---

## 第 2 层：插件开发工具链——脚手架 + 预览 + build + 发布

> 🔥 第 1 层 SDK 就绪后才能做——脚手架生成的模板依赖 `@linkdesk/plugin-sdk`。
> 第 3 层（市场）可与本层并行。

---

### 第 2.1 轮：create-linkdesk-plugin 脚手架

#### E6#21 🔴 脚手架目录与 package.json

- [ ] **E6#21a** 脚手架设计稿——**吸收 [02-插件开发工具链/01-create-linkdesk-plugin脚手架.md](02-插件开发工具链/01-create-linkdesk-plugin脚手架.md)（已是设计稿，不重复新建）**；缺失处（插件作者工作流/模板结构/CLI 行为）就地补进该档案。**模板 plugin.json 按 E5.8 schema 重写（第 2.1 轮审视实锤：原设计稿模板是 E5.6 schema——`views` 对象无 render / `viewsContainers.sidePanel` 无 location / `pluginRole` 应为 `factoryRole`，照原样生成的插件 E5.8 加载不了，见档案 §八）** | ~80 行（2026-08-29 审计 C6）
- [ ] **E6#21b** 新建 `packages/create-linkdesk-plugin/` | 结构：
  ```
  packages/create-linkdesk-plugin/
    ├── package.json       # name: "create-linkdesk-plugin", bin: "./index.js"
    ├── index.js           # CLI 入口（纯 Node.js）
    └── template/          # 模板文件
        ├── plugin.json
        ├── package.json
        ├── tsconfig.json   # jsx: "react-jsx" + types 引 @linkdesk/plugin-sdk
        ├── src/index.tsx
        └── i18n/en.json
  ```
- [ ] **E6#21c** `package.json`——`"bin": { "create-linkdesk-plugin": "./index.js" }`
**验证：** `node packages/create-linkdesk-plugin/index.js my-plugin` → 生成目录。

#### E6#22 🔴 CLI 入口——生成模板

- [ ] **E6#22a** `index.js`——命令行参数读取
  - [ ] 插件名作为参数，未提供 → 交互式询问
  - [ ] 创建目录、复制模板、替换占位符
  - [ ] 输出成功信息
- [ ] **E6#22b** 模板 `plugin.json`——最小可用，所有字段有注释；**按 E5.8 schema**：`$schema`+`factoryRole`+`appearsIn`+`viewsContainers(location)`+`views` 数组含 `render`+`entry`+`contributes.i18n`（参照 `plugins/user/panel-demo`——第 2.1 轮审视 E5.6→E5.8 换代）
- [ ] **E6#22c** 模板 `src/index.tsx`——最小可渲染组件；**契约对齐 E5.8 `{ isActive?, tabId?, sourceId? }`**（editor/index.tsx 实锤）+ 样式示范 CSS 变量（禁内联硬编码 padding）
- [ ] **E6#22d** 模板 `package.json`——依赖 `@linkdesk/plugin-sdk`；devDependencies 补 `@types/react` + `typescript`（模板 tsx import React 需类型，第 2.1 轮审视补）
**验证：** `npm create linkdesk-plugin hello-world` → 生成项目 → `npm install` → `npm run dev`。

#### E6#23 🟢 脚手架发布到 npm

- [ ] **E6#23a** `npm publish`——发布 create-linkdesk-plugin（**显式 `--registry=https://registry.npmjs.org`——npmmirror 不能发布，同 E6#2.5b**，2026-08-29 审计 C7 补；**🔥 发布前 `npm whoami --registry=https://registry.npmjs.org` 确认 fengyili——2026-08-30 实测 401，401 则先 `npm login`，同 #2.5b 前置**）
**验证：** 在空目录 `npm create linkdesk-plugin test123` → 生成项目。

#### E6#23.5 🔴 dev 宿主重设计——plugin-shell 已删（E5.8 衔接补充）

> 📖 **2026-08-16 补充。** E5.7 清理已整删 plugin-shell.html / plugin-shell-main.tsx（原 E6#24/#27/#28 的 dev 宿主 + 手动 mock 载体）。第 2.2/2.3 轮 dev 命令需在极简Pool 架构（1 BrowserWindow + 1 WCV）下重设计宿主。

- [ ] **E6#23.5a** 设计 dev 宿主新形态——最小壳只加载开发中的插件（候选：Pool 内临时 zone / 独立轻量窗口——开工时拍板）
- [ ] **E6#23.5b** 本地插件入口经 `/@fs/` 加载 + HMR 通道
- [ ] **E6#23.5c** mock 注入位置随宿主重定（mock 内容由 E6#27 生成——E5.8 生成器第二输出目标）
**验证：** 插件项目 `npm run dev` → 宿主打开 → 改代码 → 热更新。

---

### 第 2.2 轮：dev + build 命令

#### E6#24 🟡 dev 命令——最小壳只加载开发中的插件

> E5#110 已做初版——`plugin-shell.html` + `?pluginId=` mock。现在加强：支持本地插件项目路径。

- [ ] **E6#24a** `plugin-sdk` 加 `dev` CLI 命令——`linkdesk-plugin-sdk dev`
  - [ ] 读当前目录 `plugin.json` 获取 pluginId
  - [ ] 启动 Vite dev server
  - [ ] 自动打开 dev 宿主（形态见 #23.5——plugin-shell.html 已删）
- [ ] **E6#24b** #23.5 宿主——`dev=true` 模式
  - [ ] 本地插件入口通过 `/@fs/` 加载
  - [ ] HMR 热更新
**验证：** 在插件项目目录 `npm run dev` → 浏览器打开 → 改代码 → 热更新。

#### E6#25 🟡 build 命令——`linkdesk-plugin-sdk build`

- [ ] **E6#25a** CLI `build` 命令
  - [ ] 调 validate → E6#4 Vite build → zip → `.linkdesk-plugin`
  - [ ] 输出文件大小 + "Ready to publish!"
**验证：** `npm run build` → 产出 `.linkdesk-plugin`。

#### E6#26 🟡 插件市场中"发布"入口

> 📖 **2026-08-17 多市场源（用户拍板）：发布到作者自己的仓库**——Releases + 根目录 marketplace.json，别人添加该仓库为市场源即可被发现，无需进入中心目录。设计 → [01-GitHub-Releases方案.md §六 多市场源](03-插件市场/01-GitHub-Releases方案.md)。

- [ ] **E6#26a** 插件市场 UI——"发布插件"按钮
- [ ] **E6#26b** 选择本地 `.linkdesk-plugin` → **发布前预览+确认（解压读 plugin.json → 预览作者/版本/图标 → 确认）** → 上传到 GitHub Releases → 更新**自己仓库的** marketplace.json（2026-08-29 审计 C5 补）
**验证：** 作者 build → 打开 LinkDesk → 市场 → 发布 → 文件上传成功；别人添加该仓库为市场源 → 插件被发现可安装。

---

### 第 2.3 轮：Dev 体验与生产对齐

#### E6#27 🔴 Mock API 从真实 preload 自动生成——不做两份维护

> 📖 **2026-08-16 E5.8 衔接**：宿主 plugin-shell-main.tsx 已删（见 #23.5）；生成脚本复用 E5.8 契约生成器（同一标注源，第二输出目标）——见 #27a 改写。
> 📖 **🔥 2026-08-07 E5.5 对照审计发现 + 2026-08-09 E5.6 更新。** 原 [plugin-shell-main.tsx:48-171] 手动维护 ~120 行 mock `window.linkdesk`。E5.5#16-#24 + E5.6 Phase 10 API 补全后，mock 和真实 preload 的差距进一步拉大——mock 缺 ~24 个新方法（E5.5 14 + E5.6 10），但凭空多出 `pluginManager.install` 等 preload 根本没暴露的方法。
> **后果：** 插件作者 dev 下调 `window.linkdesk.workspace.openFolder()` → mock 无此方法 → `TypeError`。或 mock 有但生产 preload 没有 → dev 正常、用户装完炸。

- [ ] **E6#27a** E5.8 契约生成器加第二输出目标 mock（同一标注源，读 preload-pool.ts / preload-shell.ts 的 exposeInMainWorld 面）
  - [ ] 生成 `injectMockApi()` 内容——每个方法按返回类型决定 mock 行为
  - [ ] sync 方法 → 返回类型默认值（`path.join` → `""`, `contextKey._getValue` → `undefined`）
  - [ ] async 方法 → 返回 `Promise.resolve(默认值)`（如 `configuration.get` → `Promise.resolve(undefined)`）
  - [ ] fire-and-forget 方法 → 空函数 `() => {}`
- [ ] **E6#27b** mock 在调用不存在的方法时 **抛错** "该 API 仅在生产环境可用"——不静默返回 `undefined`
  - [ ] 使用 Proxy 拦截未定义的方法 → `throw new Error(...)` | +10 行
- [ ] **E6#27c** #23.5 新宿主——`injectMockApi()` 改为 import 生成的 mock 文件（plugin-shell-main.tsx 已删）
- [ ] **E6#27d** preload 改了 → CI 跑生成脚本 → mock 自动更新 | 注册到 `npm run check`
**验证：** `npm run check` 后 mock 和 preload 方法列表完全一致。插件作者 dev 下调 `linkdesk.workspace.openFolder()` → 抛 "该 API 仅在生产环境可用" 而非 `undefined is not a function`。

#### E6#28 🟡 Dev 模式可选 Electron——弥合浏览器 vs 生产的鸿沟

> 📖 **🔥 2026-08-07 E5.5 对照审计发现。** 当前 E6#24 dev 命令在纯浏览器中跑（`localhost:1420/plugin-shell.html`），没有 Electron、没有 IPC、没有 WebContentsView。插件作者整个开发周期看不到真实的 `window.linkdesk.*` 行为。Dev 和生产之间的差异 = 经典的"缝 bug"——dev 通过但生产炸。

- [ ] **E6#28a** `npm run dev:plugin`——保留纯浏览器模式（轻量 HMR，不改）
- [ ] **E6#28b** `npm run dev:plugin -- --electron`——可选 Electron 模式
  - [ ] 启动完整 Electron → 壳加载开发中的插件（`?path=<cwd>&dev=true`）→ 真实 IPC + 真实 WebContentsView
  - [ ] 仍支持 HMR——插件代码改动 → Vite dev server 推送 → 插件 WebView 热更新
  - [ ] 壳不加载其他插件（对标 E6#24 "最小壳只加载开发中的插件"）
- [ ] **E6#28c** 文档——`03-插件制造/11-插件开发指南.md` 明确两种模式的使用场景
  - [ ] 纯浏览器：UI/布局/组件快速迭代（10 秒启动）
  - [ ] Electron：IPC/文件系统/多 WebView 真实环境验证（发布前必跑）
**验证：** `npm run dev:plugin -- --electron` → Electron 窗口打开 → 只有开发中的插件 → 调 `linkdesk.configuration.get()` 走真实 IPC → 返回真实值。

---

## 第 3 层：插件市场——存储 + 分发 + 安装 + 第一站体验

> 和第 2 层可并行。**第一站交互设计** → [03-市场交互设计.md](03-插件市场/03-市场交互设计.md)。

---

### 第 3.1 轮：GitHub Releases 后端

#### E6#29 🔴 marketplace.json 设计

- [ ] **E6#29a** 格式——插件列表 JSON，含 id/name/version/description/author/icon/downloadUrl/size/publishedAt
- [ ] **E6#29b** 存放位置——GitHub 仓库 `linkdesk-marketplace` 根目录
- [ ] **E6#29c** 格式扩展（2026-08-29 第一站数据前提——纯增量，旧插件不填不崩）→ [03-市场交互设计.md](03-插件市场/03-市场交互设计.md) §三
  - [ ] `versions[]`（版本历史 `{version,downloadUrl,publishedAt,changelog?}` 最新在前——版本下拉源）+ `readmeUrl` + `screenshots[]` + `license` + `categories[]`
  - [ ] `icon` 支持 url 形态（作者自制彩色图标）
  - [ ] 配套：E6#4a 打包结构补 `README.md` 原样复制（已装插件详情数据源）
**验证：** 壳请求 `https://raw.githubusercontent.com/.../marketplace.json` → 返回 JSON。

#### E6#30 🟡 壳侧 marketplace 插件改造——读 marketplace.json（多源）

> 📖 **2026-08-17 用户拍板：单中心目录 → 多市场源。** 第三方作者发布到自己的仓库，用户/AI 添加任意 GitHub 仓库为市场源即可发现安装——对标 VS Code 多 marketplace / npm 多 registry。设计 → [01-GitHub-Releases方案.md §六 多市场源](03-插件市场/01-GitHub-Releases方案.md)。

- [ ] **E6#30a** marketplace 插件启动时 fetch `marketplace.json`
  - [ ] 从 GitHub raw URL 拉取
  - [ ] 缓存到本地——5 分钟过期
- [ ] **E6#30b** 交叉比对 installed-plugins.json
  - [ ] 本地有 → "已安装"（或"更新"）
  - [ ] 本地没有 → "安装"
- [ ] **E6#30c** 多市场源（2026-08-17 用户拍板）
  - [ ] 源列表配置化——`marketplaceSources`：`{ id, url, default? }`（默认源 = encaron/linkdesk-marketplace）
  - [ ] 设置里「添加市场源」——填 GitHub 仓库 URL → 校验根目录存在 marketplace.json → 加入源列表
  - [ ] 启动 fetch 全部源 → 合并去重（同 id 取 semver 高者）→ 统一展示
  - [ ] 单源失败 → 跳过不阻塞其他源，用该源缓存兜底
  - [ ] 来源标注——商店卡片/列表显示来源仓库名（用户知道装的是谁的）
- [ ] **E6#30d** 探索插件视图——「待安装」改名 + 目录驱动（点1）→ [03-市场交互设计.md](03-插件市场/03-市场交互设计.md) §一
  - [ ] `UninstalledListView` 数据源从 `getUninstalled()`（E5.8 卸载缓存）改为 market catalog，未装条目绿色「安装」按钮
  - [ ] i18n 标题 + badge 语义同步
- [ ] **E6#30e** 目录图标接线（点5）→ §六
  - [ ] PluginIcon 支持显式 icon descriptor 入参（不走 viewRegistry——未装无 registry 条目）
  - [ ] 探索行/详情页图标 = catalog icon（url 直接 `<img>`）；作者改图标 → 重新 fetch（5min 缓存）即新
- [ ] **E6#30f** 目录防御（缝隙 G12/G8——01 档 §四）→ [01 §四](03-插件市场/01-GitHub-Releases方案.md)
  - [ ] catalog parse 失败 → 用缓存；无缓存 → 空态「目录损坏」+ [重试]
  - [ ] 全源不可达 + 无缓存（首次离线）→ 探索组空态「无法加载市场，请联网重试」+ [重试]
  - [ ] 市场插件订阅壳语言变更事件 → 切换语言即时刷新（F3，11 档 §三·五）
**验证：** 市场列表显示正确安装状态；添加第二源 → 两源插件合并展示；删源 → 只剩默认源；断网无缓存 → 空态可重试；坏 catalog → 用缓存不崩。

---

### 第 3.2 轮：详情页体验（第一站核心）

> 交互设计 → [03-市场交互设计.md](03-插件市场/03-市场交互设计.md) 总览 + [04-详情页设计.md](03-插件市场/04-详情页设计.md)（详情页）/ [05-更新与版本.md](03-插件市场/05-更新与版本.md)（更新）/ [07-标签页标题.md](03-插件市场/07-标签页标题.md)（标签）/ [08-信任与安全.md](03-插件市场/08-信任与安全.md)（信任）/ [09-安装细节.md](03-插件市场/09-安装细节.md)（安装细节）/ [11-API与反馈.md](03-插件市场/11-API与反馈.md)（API 契约 + 反馈闭环）/ [12-插件目录与UI细节.md](03-插件市场/12-插件目录与UI细节.md)（目录树 + UI 交互 + 变量化）。
> 🔧 **详情页渲染载体 = 市场插件主区详情视图**——两系列前置先行：**系列一·市场搬迁**（[第 3.2.0 轮](#) E6#30.10 机制 / E6#30.11 迁移）→ **系列二·UI 组件**（[第 3.2.1 轮](#) E6#30.12 token / E6#30.14 Button+Badge）→ **E6#30.13 联合验收**。壳 PluginDetailPoolView 降级保底宿主。内容任务（E6#30.5-#30.9）统一落在搬迁后的市场插件 DetailView 上做（2026-08-29 用户定方向「换市场=换样式」）。

---

### 第 3.2.0 轮：市场搬迁——详情页渲染面归市场插件（系列一·前置）

> 📖 **2026-08-29 用户定方向「换市场插件 = 换全新样式」。** 壳 = 宿主面 + 基础件 + 数据；市场插件 = 业务组合 + 皮肤。当前唯一违背 = 详情页五层全在壳（git 时间线实锤：E3.6 只搬侧栏，详情页归属悬置四代）。设计 → [10-市场UI拥有权.md](03-插件市场/10-市场UI拥有权.md)。
> 🔗 **两个任务系列（2026-08-29 用户指出）**：本轮 = **系列一·市场搬迁**（结构性——UI 住哪）；下一轮 [3.2.1](#) = **系列二·UI 组件**（组件化——UI 用什么）。**系列一是系列二的前置**——谁画 UI 由「UI 住哪」决定，搬迁先到位，组件消费才落对 owner（先做组件消费在壳 → 搬迁后重做 = 双份工）。
> 📐 **搬迁操作细则**（搬什么/怎么搬/动哪些代码/归一化·死代码·硬编码·AI 友好四问）→ [10-市场UI拥有权.md](03-插件市场/10-市场UI拥有权.md) §六·五。本轮任务条目 = 可执行指针，细节沉档案。

#### E6#30.10 🔴 详情页渲染面贡献机制——contributes.views 主区 location + tab 解析路由

> 📖 设计前置（8 维度）：
> ① 能力边界：壳能力（多提供方=任何声明 `factoryRole:marketplace` 的插件；多消费方=tab 系统渲染 plugin-detail）。保底：无市场插件/无详情贡献 → 壳 PluginDetailPoolView（现状不崩）。
> ② API：**优先零新 API**——tab 解析走现有 `factorySlots.getActive("marketplace")`（E5.8#41.12）+ manifest 读取；`contributes.views` 新 location 值 = schema 字段扩展非 API；tab 参数随 props 传入（确认无缺口则显式记录零新 API）。
> ③ 通信：壳 tab 系统 → 解析活跃市场插件 manifest → 动态 import 详情视图（props: pluginId/marketEntry/pinned）。数据流：marketEntry 由市场视图 tabs.create 带入 + pluginManager.list() 实时状态。零新通道。
> ④ 壳侧：schema（plugin.schema.json 加 location:"main"）+ ShellViewRenderer 分支 + FactorySlots 消费。单一权威：contributes.views location 枚举。**多主区 id 可扩展（2026-08-29 拍板）**：location:"main" 不焊死 plugin-detail 单 id——ShellViewRenderer 从活跃市场插件 contributes.views main[] 按 tab id 查（plugin-detail / 未来 plugin-store…），大屏商店 = 第二个主区 tab，同机制零新宿主（落位见 [03 §五](03-插件市场/03-市场交互设计.md)）。
> ⑤ 插件侧：内置市场插件贡献 plugin-detail 主区视图。插件独立铁律：第三方市场插件照同样式声明即可换样式（E6#30.13 验收）。
> ⑥ 显示：主区 tab 内容，详情页布局由市场插件自绘（消费 E6#30.5/#30.6 目标）。
> ⑦ 配置：无新配置。factoryRole active 已有（E5.8#41.12）。
> ⑧ 验收：npm run check 全绿 + 换市场插件实机（E6#30.13）。
> 🔧 **动哪些代码（机制侧）**：`plugin.schema.json`（contributes.views items 加 location:"main" 枚举）· `src/pool/views/shell-renderer/ShellViewRenderer.tsx`（plugin-detail case 解析分支）· `FactorySlots`（E5.8#41.12 已有，零改动）· `docs/03-插件制造/03-contributes.md`（location 文档说明）。**归一化：主区详情视图 = 单一机制（contributes.views "main" + factorySlots.getActive），保底走同一解析链 fallback 分支，不做第二套。**

- [ ] **E6#30.10a** schema——`contributes.views` 支持主区 location（"main"）：plugin.schema.json + 03-插件制造 contributes 文档（审计-contributes 三件套）
- [ ] **E6#30.10b** 壳 tab 解析路由——ShellViewRenderer 渲染 "plugin-detail"：`getActive("marketplace")` → 主区视图含 id="plugin-detail" → 动态 import；缺 → PluginDetailPoolView 保底
- [ ] **E6#30.10c** 零新 API 审计——tab 参数 props 传递闭环确认（无新 API 显式记录；有缺 → 按契约四齐全补）
**验证：** 内置市场插件声明详情贡献 → 详情页走插件渲染；删贡献 → 壳保底不崩。

#### E6#30.11 🟡 内置市场插件提供详情渲染面——详情页 UI 归市场插件（搬迁操作）

> 📖 **搬什么**：① 布局——现壳 `PluginDetailPoolView.tsx` 的 header/action bar/navbar/body/元数据侧栏结构 → 插件 `plugins/builtin/marketplace/src/views/DetailView.tsx`；② 样式——`PluginDetailView.css` → `plugins/builtin/marketplace/src/MarketplaceDetail.css`；③ 动作——enable/disable/uninstall → `window.linkdesk.pluginManager.*`（market.commands 已有同语义命令可复用）；④ 数据——`pluginManager.list()`（仅已装）→ marketEntry（catalog 完整条目，未装可显示）+ list() 实时状态合并。
> 🗂 **搬迁后目录树终态**（DetailView/MarketplaceDetail.css 落位 + MarketplaceSidebar.css 377 超限拆分 + 视图/服务/组件/样式四类 + 行数上限；文件级规则 = [12 §一·〇](03-插件市场/12-插件目录与UI细节.md) 五步仪式/命名/边界）→ [12-插件目录与UI细节.md](03-插件市场/12-插件目录与UI细节.md) §一。
> **怎么搬**：复制结构 → 逐处 `@src/core` import 换 `window.linkdesk.*`（对照 plugin-import-exceptions 例外表，非白名单不得直 import）→ CSS token 化 → plugin.json 声明主区视图（30.10 机制已就位）。
> **四问（用户 2026-08-29 要求逐项过）**：归一化——主区详情视图 = 单一机制（contributes.views "main" + getActive），保底走同一解析链 fallback，不做两套；无死代码——搬迁后壳 `PluginDetailView.css` 若零消费 → 删，过渡临时文件当场清不留尸体；无硬编码——按钮色走 `var(--success)`（30.12 就位后）/ 圆角尺寸走 `var(--surface-radius)` `var(--radius-*)` `var(--space-*)` / 图标走 codicon / 插件 CSS 不写死 hex；AI 友好——保底存在（无市场插件/无贡献 → 壳兜底不崩）+ 第三方只需 factoryRole + 主区详情贡献即可换样式 + 数据走壳 API 不变。
> **UI 目标 = E6#30.5/#30.6**：三态 action 由本任务交付基础（动作 + 状态合并）；README/截图画廊/元数据侧栏扩展由 [E6#30.6](03-插件市场/04-详情页设计.md) 在搬迁后的插件视图上续做。

- [ ] **E6#30.11a** DetailView 布局迁入——现壳详情页结构复制进插件视图，`@src/core` 依赖逐处换 `window.linkdesk.*`（对照 plugin-import-exceptions 例外表）
- [ ] **E6#30.11b** CSS 迁入——`PluginDetailView.css` → `MarketplaceDetail.css`（本地类前缀 + token 化，mockup 死色消除）
- [ ] **E6#30.11c** 数据源切换——list()（已装）→ marketEntry（catalog，未装可显示）+ list() 实时状态合并；`tabs.create` 传 marketEntry 完整条目
- [ ] **E6#30.11d** 动作走 linkdesk.*——enable/disable/uninstall 调 `window.linkdesk.pluginManager.*` / 复用 market 命令，三态 action bar 基础落插件视图（消费 E6#30.5b/c 目标）
- [ ] **E6#30.11e** 壳 PluginDetailPoolView → 保底宿主（无详情贡献时兜底，注释「保底宿主」，现状已装管理面不崩）
- [ ] **E6#30.11f** 清理 + 验证——壳侧零残留死代码 grep + `npm run check` 全绿 + 实机未装/已装/禁用三态
**验证：** 详情页从市场插件渲染；未装显示 catalog 信息；三态 action 正常；删市场插件 → 壳保底不崩。README/截图 = E6#30.6 续做。

---

### 第 3.2.1 轮：UI 组件——壳基础件就位 + 市场插件消费（系列二·依赖搬迁）

> 🔗 **依赖：系列一（E6#30.10/#30.11 搬迁）为前置**（用户 2026-08-29 定：两任务系列，搬迁必为前置）。壳侧基础件（`--success` / Button variant / Badge）本身可并行就位，但**消费**必须等搬迁落定——谁画 UI 由「UI 住哪」决定，消费落对 owner 才不做双份工。
> 📐 按钮/胶囊/弹窗归属细则 → [10-市场UI拥有权.md](03-插件市场/10-市场UI拥有权.md) §四。实机缺口已坐实：壳 Button 无 variant 体系（仅 ghost 轨 = SegmentedRadio 注释）、无 Badge/Capsule 基础件（市场用自家 ms-item-badge CSS）。

#### E6#30.12 🟢 壳补 `--success` 语义 token——安装绿主题语义化

> 📖 实机缺口：壳有 --error/--warning/--danger/--warning-text，grep 不到 --success——「安装绿」目前是 mockup 画死的色。补语义 token 进主题配色域（颜色轴）——主题能换整套语义色，市场插件代码不能写死绿（10 档 §四·一）。

- [ ] **E6#30.12a** 主题域加 `--success` token（暗/亮两套）+ 颜色轴 token 集同步（theme.ts / index.css）
- [ ] **E6#30.12b** 内置市场插件侧栏安装按钮改用 `var(--success)`（ms-item-install-btn），mockup 死色消除；详情页按钮 = 30.14 走 Button variant（不重复改）
**验证：** 侧栏安装按钮走 var(--success)；换主题 → 语义色随整套换。

#### E6#30.14 🟡 壳 Button variant 体系 + Badge/Capsule 基础件——市场按钮/胶囊组件化

> 📖 设计前置（8 维度）：
> ① 能力边界：壳基础件（多提供方=任何插件消费；多消费方=市场/设置/文件树）。保底：现有 Button 加 variant 后缺省 = 现状外观，未声明 variant 的存量调用零变化。
> ② API：**零新 API**——组件是壳共享组件（`@src/components/shared`），不新增 `window.linkdesk.*`；插件经共享组件例外表消费（plugin-import-exceptions 补记录）。
> ③ 通信：无——纯 UI 组件，props 驱动（variant/children/onClick）。
> ④ 壳侧：`src/components/shared/button/Button.tsx` 加 `variant` prop（success/danger/ghost，缺省 primary=现状）+ 语义 token 消费；新建 `src/components/shared/badge/Badge.tsx`（圆角/内边距/文字走 `var(--radius-*)`/`var(--space-*)`/`var(--text-*)`）。单一权威：variant 枚举 + 语义色 token。
> ⑤ 插件侧：内置市场插件详情按钮换 variant（安装=success 填充 / 卸载=danger / 禁用=ghost）+ 胶囊换 Badge（数据仍自 catalog，消费方 = 搬迁后的详情视图 + 侧栏）。
> ⑥ 显示：按钮形状主题可控（走 var），语义色不可随手改（token）；胶囊统一圆角/内边距。
> ⑦ 配置：无新配置。variant = 组件 props 非配置项。
> ⑧ 验收：npm run check 全绿 + 实机安装绿/卸载红/禁用灰 + 换主题形状随变。
> 📐 归属细则 → [10-市场UI拥有权.md](03-插件市场/10-市场UI拥有权.md) §四·一/§四·二。**归一化：variant = 组件级单一枚举，市场只声明 variant 不碰色值（10 档定案：不选「全部透明」——丢主操作可发现性）。**

- [ ] **E6#30.14a** Button variant 体系——`Button.tsx` 加 `variant` prop（success/danger/ghost，缺省=现状）+ 语义 token 消费 + 测试（fixture 用虚构名，硬约束 21）
- [ ] **E6#30.14b** Badge/Capsule 基础件——新建共享 Badge（token 化圆角/内边距/文字），marketplace 胶囊改走 Badge（数据仍自 catalog）
- [ ] **E6#30.14c** 市场消费——详情视图按钮走 Button variant（安装=success / 卸载=danger / 禁用=ghost），壳共享组件经例外表消费（plugin-import-exceptions 补记录）
**验证：** 安装=绿 success / 卸载=danger / 禁用=ghost；胶囊 = 壳 Badge 渲染；换主题形状随变。

#### E6#30.13 🟢 换市场插件验收契约——两系列联合验收（实机换样式 + 数据继承）

> 📖 **联合验收（依赖系列一 + 系列二全就位）**：第三方市场插件声明 `factoryRole:"marketplace"` + 主区详情贡献 → 实机验证「换市场=换样式 + 继承旧的」——已装标记/来源配置/下载数缓存不丢（数据继承契约 = [10-市场UI拥有权.md](03-插件市场/10-市场UI拥有权.md) §五）。fixture 用虚构 id（demo-marketplace，硬约束 21），对标 settings-demo 先例。

- [ ] **E6#30.13a** 造一个第三方市场插件 fixture（虚构 id demo-marketplace，声明 factoryRole + 主区详情贡献 + 不同样式/按钮 variant 组合）→ 实机切换验收（侧栏 + 详情全换新样式）
- [ ] **E6#30.13b** 数据继承核对——切换后已装标记/禁用列表/来源配置/下载数缓存仍一致（数据走壳 API 验证）
**验证：** 切市场插件 → 侧栏+详情全新样式；已装/禁用/来源数据原样继承；回切内置市场 → 同样成立。

---

### 第 3.2.2 轮：详情页内容——三态/富展示/标签/信任/安装细节（搬迁后的插件视图上做）

> 🔗 **依赖：两系列前置已就位**（系列一搬迁 E6#30.10/#30.11 + 系列二组件 E6#30.12/#30.14）。以下原 3.2 轮内容任务，统一落在市场插件 `DetailView.tsx` 上做——数据走 marketEntry + `linkdesk.*`，UI 走壳基础件 + 语义 token。
> 🧭 **制作顺序逻辑：** 谁画 UI 先定（系列一·搬迁）→ 用什么画后定（系列二·组件）→ 画什么最后做（本段·内容）。每步消费方已就位，无回头路、无双份工。

#### E6#30.5 🔴 详情页目录数据源 + 三态 action bar（点2/3）

> 📖 详情页现读 `pluginManager.list()`（仅已装）→ 未装 = 空态。改为：市场视图开详情传 catalog 条目（marketEntry tab 参数），详情页合并目录信息 + 实时安装状态。壳侧零新 API。设计 → [04-详情页设计.md](03-插件市场/04-详情页设计.md) §二。

- [ ] **E6#30.5a** 目录/状态合并展示——marketEntry（catalog 信息）+ `pluginManager.list()`（实时状态）合并（tabs.create 传 marketEntry 已由 30.11c 就位），未装不再空态；**本地插件（无 marketEntry）显示本地 plugin.json 信息 + 市场数据区隐藏**（A2，04 档 §二）
- [ ] **E6#30.5b** 三态 action bar——未装→🟢安装（带进度）/ 已装→禁用+卸载 / 禁用→启用+卸载；marketEntry 缺失兜底走现有 installed 逻辑；**内置插件（core:true）卸载灰 + tooltip「内置插件不可卸载」**（C3，04 档 §二）；**卸载二次确认走 `window.linkdesk.ui.confirm`（M7，mockup 帧 4 备注「复用现有 confirm」，DialogService 现成）**；侧栏该行「已安装」徽标（M4 一）
- [ ] **E6#30.5c** lifecycle 订阅自动切换——安装完成自动变「禁用/卸载」，卸载完成变「安装」
- [ ] **E6#30.5d** catalog 字段渲染 sanitize——插件名/描述/分类走纯文本转义（E1，04 档 §三，与 README 同机制）
- [ ] **E6#30.5e** 挂起·缺依赖拦截态（缝隙 M1，mockup 帧 7 三通道）——依赖缺失（dependencies.ts pendingReason）→ action bar 显示「🚫 安装不可用（缺依赖）」（非禁用/卸载，**不拦不装**）+ 主区「依赖未满足」块列出缺失依赖 + 依赖行缺失标黄 ✕ 点击跳其详情页；侧栏「等待依赖」徽标已由 E5.8#15.5 实现（不重复）；缺依赖 toast [查看依赖] 消费 E6#13.5（09 §五）
**验证：** 未装插件详情页看目录信息 + 绿色安装 → 进度 → 完成自动切「禁用/卸载」；内置插件卸载灰 + 提示；本地插件详情无市场数据区不崩；缺依赖插件详情显示拦截态 + 点缺失依赖跳详情。

#### E6#30.6 🟡 详情页富展示——README markdown + 截图画廊（点6）

> 📖 VS Code 对标：详情 = 渲染插件包内 README.md（就是 md 文档）。已装读包内 README / 未装 fetch `readmeUrl`；markdown 渲染 = 壳共享 MarkdownView（E6#54 分发，@linkdesk/ui 可复用）。设计 → [04-详情页设计.md](03-插件市场/04-详情页设计.md) §三。

- [ ] **E6#30.6a** 壳共享 MarkdownView 组件（markdown→React，sanitize；壳零依赖需加渲染库——开工拍板）
- [ ] **E6#30.6b** `pd-readme` 升级 MarkdownView——已装读包内 README / 未装 fetch readmeUrl / 无 → 降级 description；**已装读包通道（K2）**：包内 README.md 经 `workspace.readTextFile`（已装插件目录路径，plugins 面 listDirs/readManifest 可派生）或 scoped 读取 API——实现时定，新 API 走设计前置
- [ ] **E6#30.6c** 截图画廊（`screenshots[]` 横向滚动）+ 元数据侧栏扩展（大小/分类/许可证/来源/发布时间）+ **依赖/被依赖行**（M5，mockup 帧 1/7——requires 数据在 dependencies.ts，缺失标黄 ✕ 点击跳详情）+ **侧栏追加（2026-08-29 对标 VS Code 拍板）**：大小 = catalog `size`（已装另显安装目录）；仓库/问题 = 来源 URL 派生（`/issues` 约定，零新字段零服务器，来源目录保留）；首次发布 = `versions[最后].publishedAt`；更新时间 = `versions[0].publishedAt`（最近发布）；评分/评论/趋势/Star 不做（无服务器不伪造，04 §三）
- [ ] **E6#30.6d** 功能 tab 渲染面（M2，mockup 帧 9）——contributes 四组自动列出（commands/configuration/keybindings/menus，数据 = `list().manifest.contributes` 现成，壳零新 API）；点击命令复制命令 ID、点击配置项跳设置页（04 §三）
- [ ] **E6#30.6e** 更改日志 tab 渲染面（M3，mockup 帧 10）——版本倒序最新在前 + 「已安装/最新」徽标；数据源分装态：未装 = catalog `versions[].changelog`，已装 = 包内 CHANGELOG.md（E6#4a 打包补）；有更新时合并并排两版 = E6#33b（不重复）
**验证：** 详情页显示 README markdown（代码块/图片正常）+ 截图；无 README 插件不崩；功能 tab 列 contributes；更改日志 tab 显示版本历史；元数据侧栏含依赖/被依赖。

#### E6#30.7 🟢 标签页标题修复——市场图标 + 插件名（点7）

> 📖 根因已定位：`getDefaultLabel`（tabIdentity.ts:280）只读 `detailPluginId`，调用方全传 `pluginId` → 全部兜底「插件详情」。设计 → [07-标签页标题.md](03-插件市场/07-标签页标题.md)。

- [ ] **E6#30.7a** `getDefaultLabel` pluginId 兜底——`opts?.detailPluginId ?? opts?.pluginId`（对齐 :187/:255）
- [ ] **E6#30.7b** 标签形态——市场视图开详情传自己图标（tabs.create `icon` → Tab.icon）+ 标签文本 = 插件名（不带「介绍」后缀）
**验证：** 点任何插件 → 标签 [市场图标] + 插件名，不再是「插件详情」。

#### E6#30.8 🟡 信任与安全——第一站敢不敢装（安装确认 + 下载数）

> 📖 多源市场 = 谁都能发布，第一站必须回答「凭什么信」。来源标注（列表/详情仓库名）+ 许可证已由 E6#30c + E6#30.6c 覆盖；本任务补**安装确认** + **下载数**。设计 → [08-信任与安全.md](03-插件市场/08-信任与安全.md)。

- [ ] **E6#30.8a** 安装确认——点🟢安装先弹轻量确认（来源仓库 + 一句话描述 + 许可证 + 版本）
- [ ] **E6#30.8b** 下载数——GitHub Releases API `download_count`（启动拉 + 缓存；Gitee/无 API 源不显示不伪造）
- [ ] **E6#30.8c** minAppVersion 校验（缝隙 G1 🔴——字段设计了零消费）——装/更新前 semver 比对 `marketEntry.minAppVersion` vs 壳版本，不足 → 拒绝 + error toast「需升级 LinkDesk 至 x.x.x」+ 详情元数据「需 LinkDesk x.x.x」；已装插件版本高于本地壳不崩（08 §五）
- [ ] **E6#30.8d** 恶意 id 抢占（缝隙 G11/A3）——同 id 多源冲突维持「取版本高者」；来源标注做显眼（详情页完整仓库 URL 可点 + 列表卡片来源名可见），安装确认含来源仓库（08 §六）
- [ ] **E6#30.8e** 更新后权限变化（缝隙 E3）——插件更新新增权限 → 重新走审批（E6#49），**不清已有 remember 授权，只补新授权询问**（08 §七）
- [ ] **E6#30.8f** 「官方发布」徽标（缝隙 G1，2026-08-29 定稿——mockup 全帧 header + 确认弹窗发布者行 + 侧栏「内置·官方」）——判定 = **来源是默认源（source.id === 'official'）派生**（08 §八），零新 catalog 字段；侧栏「内置」= core:true 已有；第三方源无官方徽标
**验证：** 详情页显示来源/下载数/许可证；点安装先确认；无下载数的源不崩；旧版壳装新插件提示需升级；同 id 假插件来源可见可点；官方源条目带「官方发布」、第三方源不带。

#### E6#30.9 🟡 安装细节体验——绿色按钮背后（进度/失败/多窗一致）

> 📖 安装不只在成功时是按钮——进度、失败、断网、多窗一致都是第一站体验。设计 → [09-安装细节.md](03-插件市场/09-安装细节.md)（粗粒度）+ [11-API与反馈.md](03-插件市场/11-API与反馈.md)（**反馈提示映射表——情况→API→toast 文案全量**，§三；进度/失败/断网/缺依赖 toast 文案 + i18n key 逐条定稿）。

- [ ] **E6#30.9a** 下载进度——详情页安装按钮内进度（依赖 E6#13 进度事件 + E6#31 下载）；**安装中版本下拉 + 自动更新勾选置灰**（M6，mockup 帧 2）；**侧栏该行「安装中 62%」瞬时徽标**（M4 二，09 §一）
- [ ] **E6#30.9b** 失败态全览（缝隙 G2/B2/D1/D2/D3）——七种失败逐一归因 + toast 文案（网络/超时→网络、checksum 不符→校验、size 异常→大小、磁盘不足→环境、包损坏、未知）+ [重试]（消费 E6#13.5 actions）；**重试=手动触发无自动风暴**；断网/超时统一归「网络」；checksum 旧条目无字段跳过（09 §二 失败表）；**侧栏该行「安装失败」+ 重试按钮**（M4 三）；**断网离线态**（G3）——`navigator.onLine === false` 归「离线」非「失败」：按钮置灰 + 提示「联网后重试」，无 [重试]（09 §二）
- [ ] **E6#30.9c** 多窗口即时一致——安装/卸载/更新后所有窗口侧栏/详情同步（E5.8 跨窗事件联动现成，零新 API，验收项）
- [ ] **E6#30.9d** 覆盖安装（缝隙 G3）——已装同版本/再装 → 确认或提示，不静默覆盖（11 §三·五 定案）
- [ ] **E6#30.9e** 卸载使用中 + 挂起中（缝隙 G4/B4）——有标签页卸载/禁用 → **静默关标签页 + 执行 + toast 照常（不弹窗，用户拍板）**；依赖缺失挂起的插件可卸载，连带清依赖引用无孤儿残留（09 §三·五）
**验证：** 安装 → 按钮进度 → 完成 toast + 状态切「禁用/卸载」；断网 → 明确失败态可重试不风暴；多窗口同步；卸载在用插件标签页静默关闭；挂起插件卸载无残留。

---

### 第 3.3 轮：下载与安装 UI

#### E6#31 🔴 壳侧下载与安装

> 📐 **市场消费侧 API 契约**（installWithProgress 回调形状 + 进度通道 plugins:install-progress）→ [11-API与反馈.md](03-插件市场/11-API与反馈.md) §一·一；进度/失败 toast 文案 → §三 映射表。

- [ ] **E6#31a** 下载——`fetch(downloadUrl)` → ArrayBuffer → 进度条
  - [ ] 下载写 `.part` 临时文件 + 启动扫描清理残留（缝隙 B1，「下载中关软件」不留垃圾；01 §四·五）
- [ ] **E6#31b** 安装——调 E6#11a 安装流程
  - [ ] 解压到 `{userData}/plugins/user/<pluginId>/`——**插件市场下载的只放 user/ 目录**（builtin/user 语义 → [01-插件独立构建/06-builtin-user-语义规范.md](01-插件独立构建/06-builtin-user-语义规范.md)）
  - [ ] 写记录 → loadPlugin → UI 刷新
- [ ] **E6#31c** 卸载——调 E6#11b 卸载流程
**验证：** 市场点"安装" → 进度条 → 图标栏出现新图标。点"卸载" → 消失。

#### E6#32 🟡 搜索与分类——vscode式（2026-08-17 用户拍板）

> 📖 **用户拍板「vscode式」。** 搜索框 → 单一搜索结果列表，不是各组各自过滤。改动只在 marketplace 插件内部，不改壳。行为定义 → [出厂制造 05 档案 §搜索行为](../../04-出厂制造/05-Marketplace-Store-插件市场/02-Marketplace增强.md)。

- [ ] **E6#32a** 搜索——本地过滤 marketplace.json 缓存，**vscode式**：
  - [ ] 搜索非空 → 隐藏「已安装/内置/已禁用/探索插件」四组 section 头，合并为**单一搜索结果列表**——所有匹配项（无论本地状态）平铺一处
  - [ ] 已安装的匹配项**仍然出现**（带 [卸载]/[禁用] 按钮）——不是只显示未下载的（对标 VS Code：搜到已装的直接给卸载入口）
  - [ ] 清空搜索 → 恢复四组正常显示
  - [ ] 匹配规则不变：按 name / pluginId / description（现有 `matchSearch` 逻辑）
  - [ ] **侧栏组名/顺序对齐 mockup（M8，2026-08-29 审计补）**——现代码「待安装」组改名「探索插件」；顺序对齐 已安装/探索插件/已禁用/内置（现 内置/已禁用 互换），group 顺序随 i18n + plugin.json 声明改
- [ ] **E6#32b** 分类——按 plugin.json 中的 category 字段
**验证：** 搜索 "hello" → 四组标题消失，只剩单一搜索结果；已安装的 hello 匹配项仍出现带 [已安装]。清空 → 四组恢复。

#### E6#33 🟡 更新通知 + 版本管理（点4）

> 📖 交互设计 → [05-更新与版本.md](03-插件市场/05-更新与版本.md)。**发现机制**（拉目录+比版本/触发时机/幂等 lastNotifiedVersion/多源取高/内置不参与/beta 忽略）→ §一/§二。

- [ ] **E6#33a** 启动时检查已安装插件是否有更新
  - [ ] 比较本地版本 vs marketplace.json 版本（唯一判定 `semver.gt`）
  - [ ] 有更新 → 状态栏铃铛 + 市场内"更新"按钮
  - [ ] 幂等 `lastNotifiedVersion`——已提醒过该版本不重复响铃，更新成功清（05 §二·一）
  - [ ] 多源冲突取高者；源变化导致 remoteLatest 回退 → 按现 catalog 比，不制造额外提示（05 §二·二）
  - [ ] 内置（core:true）不参与更新提示；beta 默认忽略只看稳定版（05 §二·三/二·四）
  - [ ] 条目消失/下架 → 不提示不动本地：未装从探索消失、已装保留、详情页「已下架」历史标识（缝隙 A1/G9，05 §二·五）
- [ ] **E6#33b** 更新徽标——「禁用/卸载」旁升级入口（点4 第一段，E6#33a 检测结果消费）；**禁用插件点更新 → 完成后保持禁用态**（缝隙 F1，05 §二·十）
  - [ ] 有更新时详情页按**第四维展示规则**（04 §二·五 / mockup 帧 6）——header 显本地版本 + 「有新版本可用」块 + 更改日志 tab 亮 dot + 合并并排两版 changelog（v1.3.0 最新/远程 + v1.2.0 已安装/本地）
- [ ] **E6#33c** 版本下拉——`versions.length > 1` 时可选历史版本（点4 第二段，装到 user/）；**选中哪条拉哪条的 `downloadUrl`**（05 §四）；**选旧版（目标 < 当前）→ 确认弹窗「配置可能不兼容」，不拦只提示**（缝隙 F2，05 §二·十一）；beta 手动可选（05 §二·四）
- [ ] **E6#33d** 自动更新勾选——插件级 Opt-IN 默认关（点4 第三段）
  - [ ] `installed-plugins.json` 条目 `autoUpdate: boolean`（PluginInstallService owner）
  - [ ] **勾选只在已装态显示（G2，2026-08-29 定稿）**——未装/挂起态不显示（autoUpdate 是已装条目属性），装完进入已装态才显示且默认不勾；mockup 帧 1「未安装」行同步修订
  - [ ] 开启后更新检查自动下载替换 + toast；只看稳定版不自动装 beta（05 §二·四）
  - [ ] 插件有标签页开着 → 本次跳过自动更新，下次启动补做（缝隙 G6，05 §二·七）
  - [ ] 版本下拉手动选非最新 → 记 `pinnedVersion` 暂停自动更新；选最新/点更新清除（缝隙 B3，05 §二·九）
  - [ ] 配置/用户数据由壳管天然不丢，更新替换目录不碰配置（缝隙 G7 契约，05 §二·八）
- [ ] **E6#33e** 更新执行原子切换（缝隙 G5）——下载 temp → 校验 → 原子 rename 替换 → 失败旧版保留 + error toast [重试]，不存在「替换到一半崩了插件损坏」（05 §二·六；执行落 E6#13 update）
**验证：** 已装 v1.0.0，市场有 v1.1.0 → 显示更新；提醒过一次不重复响铃；禁用插件更新后仍禁用；自动更新开着但标签页开着 → 跳过；更新失败 → 旧版还在；选旧版弹确认。

---

## 第 4 层：端到端验证——造一个真插件，在安装版里跑通

> 🔥 **Encaron 的最终需求。** 自己造插件 → 直接用到安装版软件中。
> 第 1-3 层全部完成后才能做。

---

### 第 4.1 轮：hello-world 测试插件

#### E6#34 🔴 插件设计与实现

- [ ] **E6#34a** `plugin.json`——覆盖所有 contributes 字段
  - [ ] viewsContainers.sidePanel——侧栏视图
  - [ ] contributes.views main——主区标签页视图（E6#30.10 已把 location:"main" 扶正，测试插件应覆盖——2026-08-29 审计 C3 补）
  - [ ] contributes.commands——hello.sayHi + hello.openTab
  - [ ] contributes.menus.EditorContext——右键菜单
  - [ ] contributes.keybindings——Ctrl+Shift+H
  - [ ] contributes.configuration——greeting 配置项
  - [ ] contributes.i18n——英文翻译
- [ ] **E6#34b** `src/index.tsx`——React 组件
  - [ ] 侧栏视图——欢迎语 + 配置值 + "打开新标签页"按钮
  - [ ] 标签页视图——随机数证明每次新建不同
  - [ ] 读配置 / 响应命令 / 打开标签页 / 状态栏显示问候语
- [ ] **E6#34c** `i18n/en.json`——英文翻译
**验证：** `npm run dev` → 浏览器看到 hello-world 插件。

#### E6#35 🔴 开发模式下全功能验证

- [ ] **E6#35a** 图标栏——Hello 图标出现（Lucide Smile）
- [ ] **E6#35b** 侧栏——点击图标 → 侧栏展开 → 显示欢迎语 + 配置值
- [ ] **E6#35c** 标签页——"打开新标签页" → 新标签出现 → 内容不同
- [ ] **E6#35d** 右键菜单——编辑器右键 → "Hello: Say Hi" → 点击 → toast
- [ ] **E6#35e** 快捷键——`Ctrl+Shift+H` → toast
- [ ] **E6#35f** 配置——设置 → 修改 greeting → 侧栏文字变化
- [ ] **E6#35g** 语言切换——切英文 → 所有 UI 文字变英文
**验证：** 全部 7 项通过。npm run check 零错误。

---

### 第 4.2 轮：独立构建 + 生产安装验证

#### E6#36 🔴 用 SDK 构建 hello-world

- [ ] **E6#36a** 在 hello-world 项目目录 `npm run build`
- [ ] **E6#36b** 验证产出——`hello-world.linkdesk-plugin` 存在
- [ ] **E6#36c** 解压验证——plugin.json / index.bundle.js / i18n/en.json 齐全
**验证：** 产出文件可解压，内容齐全。

#### E6#37 🔴 构建安装包——含市场后端就绪

- [ ] **E6#37a** `npm run electron:build`——构建安装包成功
- [ ] **E6#37b** 安装到 Windows——桌面快捷方式 / 开始菜单 / 卸载程序
- [ ] **E6#37c** 启动验证——无白屏 / 任务栏图标正确 / E5.8 回归矩阵场景正常（`../E5.8_归一化基建/回归验证/全量回归矩阵.md`）

#### E6#38 🔴 从市场安装 hello-world 到安装版

- [ ] **E6#38a** 上传 hello-world.linkdesk-plugin 到 GitHub Releases
- [ ] **E6#38b** 更新 marketplace.json
- [ ] **E6#38c** 启动安装版 LinkDesk → 打开插件市场 → 看到 hello-world
- [ ] **E6#38d** 点"安装" → 下载进度条 → 安装完成 → 图标栏出现 Smile
- [ ] **E6#38e** 完整功能回归——E6#35 七项验证全部在安装版中通过
- [ ] **E6#38f** 关掉重开 → hello-world 仍在（持久化）
- [ ] **E6#38g** 卸载 → 图标消失 → 记录清除

#### E6#39 🔴 Encaron 的终极验证

- [ ] **E6#39a** 你自己打开安装版 LinkDesk
- [ ] **E6#39b** 打开插件市场 → 安装 hello-world
- [ ] **E6#39c** 使用它——侧栏/标签页/右键/快捷键/配置/语言切换
- [ ] **E6#39d** **不用任何命令行、不看任何代码——纯用户操作。**
**验证：** Encaron 亲口说"行了"。E6 封站。

---

## 第 5 层：文档与发布

> 全程穿插——不阻塞前 4 层。

---

### 第 5.1 轮：插件开发文档

#### E6#40 🟢 插件开发指南

- [ ] **E6#40a** 新建 `docs/03-插件制造/11-插件开发指南.md`
  - [ ] 快速开始——`npm create linkdesk-plugin` → `npm run dev` → 10 分钟上手
  - [ ] 项目结构——plugin.json / src / i18n / 每个文件的角色
  - [ ] plugin.json 字段详解——每个字段的含义、类型、示例
  - [ ] 视图插件——`isActive` + React 组件约定
  - [ ] 命令/菜单/快捷键/配置/标签页/i18n——完整示例
  - [ ] 发布——`npm run build` → 上传到市场
- [ ] **E6#40b** API 参考——指向 E5.8 生成的 linkdesk.d.ts（2026-08-16 衔接：文档面已收敛，不手写第二份）
**验证：** 新 AI 读此文档能独立开发一个插件。

#### E6#41 🟢 API 类型文档

- [ ] **E6#41a** `@linkdesk/plugin-sdk` README——安装 + 使用 + API 速查表（速查表指向生成 d.ts）
**验证：** npm 页面上文档清晰。

---

### 第 5.2 轮：发布流水线

#### E6#42 🟡 CI 自动构建（GitHub Actions）

- [ ] **E6#42a** 逐项审计——auto-update / crash report / CI / E2E / 代码签名 / NSIS 升级测试——每项写结论："已做 / 不需要 / v1.1做" | 记录
- [ ] **E6#42b** `.github/workflows/build.yml`
  - [ ] push 到 `electron` 分支 → 触发
  - [ ] `npm ci` → `npm run check` → `npm run electron:build`
  - [ ] 🔴 代码签名坑位（TODO 注释，v1.0 发布前启用）——Azure Trusted Signing / SignPath / 正规证书三种方案
  - [ ] 上传安装包到 GitHub Releases
- [ ] **E6#42c** 版本号自动打 tag → Release
**验证：** push 代码 → Actions 跑 → Release 页面出现安装包。

#### E6#43 🟡 官网下载页

- [ ] **E6#43a** `linkdesk.io/download`——下载页
  - [ ] Windows 下载按钮——指向最新 GitHub Release
  - [ ] 版本号 + 发布日期 + 系统需求
**验证：** 浏览器打开 → 看到下载按钮 → 点击下载 `.exe`。

#### E6#44 🟢 发布清单

- [ ] **E6#44a** 新建 `docs/06-发布管理/v1.0-发布清单.md`
  - [ ] 发布前检查（`npm run check` 零错误 / 七场景回归 / 安装包验证）
  - [ ] 发布步骤（打 tag → CI → 检查 Release → 更新官网）
  - [ ] 发布后检查（下载安装包 → 安装 → 跑一遍）
**验证：** 每次发布照着清单勾。

---

### 第 5.3 轮：Windows Shell 集成与多窗口

> 📖 **2026-08-07 Encaron 提出。** 对标 VS Code——安装后右键文件/文件夹能直接"以 LinkDesk 打开"、右键菜单有固定位、不同文件夹各自开独立窗口。

#### E6#45 🔴 文件关联与右键菜单

**目标：** 安装 LinkDesk 后，任意文本文件（`.txt`/`.py`/`.js`/`.json`/`.md`/`.html`/`.css`/`.ts`/`.tsx` 等）右键菜单出现"Open with LinkDesk"，且出现在根级（不展开"打开方式"子菜单）。文件夹和文件夹空白处同样。

- [ ] **E6#45a** `electron-builder.yml`——注册文件类型关联
  - [ ] `fileAssociations` 数组——`.txt` `.py` `.js` `.json` `.md` `.html` `.css` `.ts` `.tsx` `.yaml` `.xml` `.csv` `.log`
  - [ ] `ext` + `name` + `description` + `role: "Editor"` | +15 行
- [ ] **E6#45b** NSIS 安装脚本——右键根级菜单项
  - [ ] 新建 `build/installer.nsh`——写注册表
  - [ ] `HKCR\*\shell\LinkDesk`——任意文件右键 → "Open with LinkDesk"
  - [ ] `HKCR\Directory\shell\LinkDesk`——文件夹右键
  - [ ] `HKCR\Directory\Background\shell\LinkDesk`——文件夹空白处右键
  - [ ] icon 指向安装目录下的 `icon.ico`
  - [ ] `electron-builder.yml` → `nsis.include: build/installer.nsh` | +25 行
- [ ] **E6#45c** 验证——安装后右键 `.txt` → 看到 "Open with LinkDesk" → 点击启动

#### E6#46 🔴 命令行文件/文件夹 intake——接收并路由

**目标：** 应用收到文件路径后（来自右键打开 / 命令行 / 双击关联文件），正确路由：文件 → editor 插件打开，文件夹 → workspace 添加。

- [ ] **E6#46a** `electron/main.ts`——`app.on('open-file', ...)` 事件处理器
  - [ ] macOS: `open-file` 事件在 `app.whenReady()` 前可能触发——需要缓存
  - [ ] Windows: 第二个实例通过 `second-instance` 事件 + `process.argv` 获取路径
  - [ ] 路径传给已有窗口（IPC `shell:openPath`）
- [ ] **E6#46b** 渲染进程——`shell:openPath` IPC handler
  - [ ] `fs.statSync` 判断文件 vs 文件夹
  - [ ] 文件 → 从路径提取扩展名 → `FileAssociationService.getPluginFor(ext)` 查注册的插件
  - [ ] 如果无匹配 → fallback 到 editor 插件（E5#99 未知文件路由）
  - [ ] 文件夹 → `workspaceService.addFolder(folderPath)` + 如果已存在则 `setActiveWorkspace`
  - [ ] 🔥 **留口子：不硬编码 `pluginId: "editor"`。** 未来第二编辑器注册 `contributes.fileAssociations` 后，`getPluginFor()` 自动命中。FileAssociationService 已有 `getPluginsFor()`（多编辑器列表）和 "默认用第一个注册的" 语义——多编辑器并存时只需加用户偏好层，不返工路由代码。
- [ ] **E6#46c** 验证——`linkdesk.exe C:\some\file.txt` → editor 打开；`linkdesk.exe C:\some\folder` → workspace 添加

#### E6#47 🔴 多窗口支持——不同文件夹各自开独立窗口

**目标：** 对标 VS Code——右键两个不同文件夹各选"Open with LinkDesk"→ 出现两个 LinkDesk 窗口，各自独立 workspace，各自编译/运行。关键技术决策：一个 Electron 进程 + 多个 BrowserWindow（非多个 app 实例）。

- [ ] **E6#47a** `electron/main.ts`——移除 `!gotLock → app.quit()` 杀死逻辑
  - [ ] `second-instance` 解析 `process.argv`，提取文件夹路径
  - [ ] 创建新 BrowserWindow——调 `createWindow(workspacePath?)`，同进程新窗口
- [ ] **E6#47b** `electron/window-manager.ts`——多窗口管理
  - [ ] `createMainWindow(workspacePath?: string)` 方法
  - [ ] 每窗口维护独立的 workspace 状态
  - [ ] 窗口关闭时清理对应 workspace
  - [ ] 所有窗口关闭 ≠ app quit（macOS 惯例；Windows 最后一个窗口关闭 = quit）
- [ ] **E6#47c** WorkspaceService——多 workspace 隔离
  - [ ] 每窗口的 workspace 数据通过 URL 参数或 IPC 区分
  - [ ] 不同窗口的 `workspace.getFolders()` 返回各自 workspace，不串扰
- [ ] **E6#47d** 验证
  - [ ] 右键文件夹 A → "Open with LinkDesk"→ 窗口 1 打开 projectA
  - [ ] 右键文件夹 B → "Open with LinkDesk"→ 窗口 2 打开 projectB（不替换窗口 1）
  - [ ] 两个窗口各自独立——文件树/编辑器/终端互不干扰
  - [ ] 关闭窗口 2 → 窗口 1 不受影响

**验证：** 行为对标 `code C:\projectA` + `code C:\projectB`——两个窗口各自独立。

## 第 6 层：安全加固——dsh 第二梯队（E5.8 借鉴产生，2026-08-16 立案）

> 📖 来源 → [../E5.8_归一化基建/00-dsh借鉴档案.md](../E5.8_归一化基建/00-dsh借鉴档案.md) 第二梯队 + memory `deepseek-harness-reference` B 档。用户拍板立案。
> 前置：第 1-3 层完成后。

#### E6#48 🔴 子进程沙箱缝——confine(argv, policy)

- [ ] **E6#48a** 子进程启动统一走 `confine(argv, policy) → 替换 argv` 沙箱缝（dsh：Linux bwrap/Landlock、Windows WRITE_RESTRICTED token）
- [ ] **E6#48b** 无沙箱退化路径 = 显式抛错 SANDBOX_UNAVAILABLE（fail-closed）
**验证：** 插件子进程被限制在声明 policy 内；无沙箱环境启动子进程 = 报错而非裸奔。

#### E6#49 🟡 审批层审计对——保留 remember 模式

- [ ] **E6#49a** 审批结果闭集 + approval/asked+decided 审计对（先审批后执行 escalation）——保留 LinkDesk 自己的 remember 权限模式（dsh 没有，是身份）
**验证：** 审批记录可审计；remember 模式行为不回归。

#### E6#50 🟡 崩溃补闭环——append-only 日志

- [ ] **E6#50a** 关键日志 JSONL append-only + 崩溃补闭环不截断（dsh：synthetic closers + Windows MoveFileExW write-through）
**验证：** 崩溃瞬间日志完整可回放，无截断尾。

#### E6#51 🟡 凭据隔离四件套

- [ ] **E6#51a** 配置只存引用不存秘密 / 每操作解析 / describe 不返回值 / env scrub（`*KEY*` / `*TOKEN*`）
**验证：** 配置导出无秘密；凭据按操作解析；环境变量清理由壳统一。

#### E6#52 🟢 用户 patch 层——按稳定 id 覆盖插件配置

- [ ] **E6#52a** `linkdesk.patch.yml`——用户按稳定 id 覆盖任何插件配置（dsh cordis.patch.yml 缩水版：后写覆盖，不写死字段）
**验证：** 用户 patch 不改插件源码即可改行为；重装后 patch 仍生效。

---

## 展望：装配清单 profile 设计（自 E5.8#50 移入，2026-08-27 用户确认要做）

> 2026-08-27 从 E5.8 Phase 10.5 移入。**不是清残留，是 E6 生态的地基展望**——「一台 LinkDesk 装什么」声明化。拍板记录/移入依据 → [E5.8#50 条目](../../02-Electron架构/E5.8_归一化基建/E5.8-执行清单.md) + [03-外观factoryRole-拍板记录.md](../../02-Electron架构/E5.8_归一化基建/壳半业务外推/03-外观factoryRole-拍板记录.md)。

#### E6#53 🟢 装配清单 profile 设计

- [ ] **E6#53a** profile 概念设计——「这台机器装了什么」声明化，对标 dsh cordis.yml：差距（已实锤）= LinkDesk 无「组合声明」——壳固定装配 zones，插件靠 contributes 挤入；workspace.json 是**状态快照**非**组合声明**；dsh 应用 = cordis.yml 插件组合列表
- [ ] **E6#53b** 设计文档——「这套环境 = base + serial + terminal + 主题 X」按声明组装；含与 workspace.json / plugin.json contributes 边界 + 排期建议；服务 AI 一站式（agent 按声明组装环境）
**验证：** 用户过目拍板；排期建议明确（E6 插件生态落地点）。

---

## 执行顺序与依赖

```
第 0 层（E6 前置——#0.1-#0.6 目录卫生 + 壳树 feature-folder 审计 + 防膨胀门禁，L1 开工前先做）

第 1 层（E5.8 完成后开始——串行）
  第 1.1 轮：E6#1-#2、#2.5、#4-#6   plugin-sdk + 类型对接 E5.8 契约 + contracts 真发布 ← 依赖 E5.8 Phase 4（linkdesk.d.ts 生成；#2.5 并入此轮，2026-08-29 审计 A2 补）
  第 1.2 轮：E6#9、#9.1（#8 折叠）   动态加载 + glob 替换 + pluginLoader 拆分 ← 依赖 E6#1-#6；#8 已折叠进 #9（2026-08-30）；#9.1 随 #9 同批做
  第 1.2 轮：E6#7                  loader 改造（.linkdesk-plugin 打包格式） ← 依赖 E6#9（#8 折叠；plugin-shell 已支持动态入口）
  第 1.2 轮：E6#12（吸收 #10）      PluginInstallService 唯一 owner ← 依赖 E6#7（loader 支持打包格式后记录才有意义）；#10 折叠进 #12（2026-08-30 第 1.2 轮审视，实锤见 03 §八）
  第 1.2 轮：E6#11                  安装/卸载/更新单一路径 ← 依赖 E6#12（数据 owner 已建）
  第 1.2 轮：E6#13                  新 IPC handler（下载/解压/更新/进度） ← 依赖 E6#12 + E6#11（install 消费 #12.add、uninstall 走 #11b）；广播审计已在 E5.8#6.5
  第 1.2 轮：E6#13.5               toast 主动作按钮 API（notifications.show actions，缝隙 K1） ← 无前置（基于 E3j#76 现有桥）；消费在 L3（30.5e 缺依赖 [查看依赖] / 30.9b 失败 [重试]）
  第 1.3 轮：E6#54、#15-#20        共享组件分发 + 内置插件 + 壳瘦身 + 路径解析 ← 依赖 E6#12 + E6#9（#8 折叠）；#54 是 #15 硬前置（内置插件独立 build 前先切 @linkdesk/ui）

第 2 层（第 1 层后——串行）
  第 2.1 轮：E6#21-#23             脚手架                       ← 依赖 E6#1-#2、#4-#6
  第 2.2 轮：E6#23.5、#24-#26      dev 宿主重设计 + dev/build/发布入口 ← 依赖 E6#21-#23
  第 2.3 轮：E6#27-#28             Mock 自动生成 + dev 弥合     ← 依赖 E5.8 Phase 4 生成器 + #23.5

第 3 层（第 1-2 层后）
  E6#29-#30: marketplace.json（含扩展）+ 改造 + 探索插件 + 图标    ← 依赖 E6#26
  E6#30.10-#30.11: 系列一·市场搬迁（详情页渲染面归市场插件——机制 + 迁移，壳保底降级） ← 依赖 E6#30 + E5.8#41.12（factorySlots active）
  E6#30.12/#30.14: 系列二·UI 组件（--success token + Button variant + Badge） ← 依赖系列一搬迁（消费落点）
  E6#30.13: 换市场插件联合验收（实机换样式 + 数据继承） ← 依赖系列一 + 系列二全就位
  E6#31: 壳侧下载与安装（下载/安装/卸载机制——详情页三态按钮与安装细节的地基） ← 依赖 E6#12 + E6#13 + E6#29（2026-08-29 审计 A3：先于 30.5-9，三态/下载进度吃它）
  E6#30.5-#30.9: 详情页内容（三态 + README + 标签标题 + 信任 + 安装细节，落在搬迁后 DetailView 上做） ← 依赖两系列前置 + E6#31（安装机制）+ E6#13 + E6#30
  E6#32-#33: 搜索 + 更新（徽标/版本/自动更新）       ← 依赖 E6#12 + E6#13 + E6#29

第 4 层（第 1-3 层后）
  E6#34-#35: 测试插件设计+开发版验证                             ← 依赖 E6#6 + E6#23 + E5.8 Phase 4
  E6#36: 独立构建                                               ← 依赖 E6#6
  E6#37-#39: 生产安装全链路                                      ← 依赖 E6#36 + E6#31 + E6#13

第 5 层（全程穿插）
  E6#40-#41: 文档（随时）
  E6#42-#44: CI + 官网 + 发布清单                               ← 第 1 层后
  第 5.3 轮：E6#45-#47             Shell 集成 + 多窗口           ← 依赖 E6#37（安装包构建完成）

第 6 层（第 1-3 层后）
  E6#48-#52: 安全加固——沙箱缝 / 审批审计对 / 崩溃补闭环 / 凭据隔离 / 用户 patch 层

展望（E6 生态地基——排期建议设计时定）
  E6#53: 装配清单 profile 设计（自 E5.8#50 移入）   ← 依赖 E6 生态成熟（市场/下载/安装落地后价值最大）
```

**最长串行链：** E5.8 完成 → 第 0 层（#0.1-#0.6 卫生）→ SDK 类型对接 → 动态加载 + glob 替换 → loader 改造 → IPC handler 新建 → PluginInstallService → 共享组件分发 → 内置插件独立化 → 壳瘦身 → 协议重构 → 脚手架 → dev + build（宿主重设计）→ Mock 自动生成 → marketplace（搬迁+UI 组件+内容）→ 测试插件 → 生产验证 → Shell 集成 + 多窗口 = 18 步。

## 总结

| 层 | 内容 | 任务数 | 估时 |
|:--|------|:--:|:--|
| L0 | E6 前置——目录卫生（死 .git 清理 / 产物排除）+ 壳树 feature-folder 审计（src/App 增量 / 大文件 / 目录）+ 防膨胀门禁 | 6 | 0.5-1 天 |
| L1 | 插件独立构建——SDK + 类型对接 + 动态加载 + glob 替换 + pluginLoader 拆分 + IPC handler + PluginInstallService + 共享组件分发 + 内置插件 + 壳瘦身 + 路径解析 | 22 | 5.5-8 天 |
| L2 | 插件开发工具链——脚手架 + dev 宿主重设计 + dev + build + Mock 自动生成 + dev Electron 弥合 | 9 | 2-3 天 |
| L3 | 插件市场——GitHub Releases + 安装管理 + 第一站体验（探索/详情三态/README/更新/信任/安装细节/标签页）+ 市场搬迁（系列一）+ UI 组件（系列二） | 15 | 2-3 天 |
| L4 | 端到端验证——测试插件 + 生产安装全链路 | 6 | 1-2 天 |
| L5 | 文档与发布——开发指南 + CI + 官网 + Shell 集成 + 多窗口 | 8 | 2-3 天 |
| L6 | 安全加固——dsh 第二梯队（沙箱缝 / 审批审计对 / 崩溃补闭环 / 凭据隔离 / 用户 patch 层） | 5 | 3-5 天 |
| 展望 | 装配清单 profile 设计（E6#53，自 E5.8#50 移入） | 1 | 排期建议设计时定 |
| **合计** | | **72** | **17-23 天** |

---

> **→ 下一文档：** `00-AI执行守则-必读.md`
> **← 上一 Phase：** `../E5_核心归一化与壳重构_待执行/`
