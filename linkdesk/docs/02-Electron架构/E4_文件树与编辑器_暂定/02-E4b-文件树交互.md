# E4b — 文件树交互操作

> 2026-07-25。**7 任务，~420 行。** 对标 VS Code Explorer 交互——右键菜单 / 键盘导航 / 快捷键 / 拖放 / 多选 / 行内重命名 / 剪贴板。
> **性质：** 纯插件代码。不碰 `src/`。

---

## 一、定位

E4a 做完有树了——但用户不能操作。E4b 补交互层。对标 VS Code Explorer 的全部鼠标和键盘操作。

**核心原则：右键之前先选中。** 对标 VS Code：如果右键的项不在当前选中集合里 → 自动切换为单选中 → 菜单作用于该项。这是 VS Code explorerViewer.ts 的行为——LinkDesk 照抄。

---

## 二、任务清单

### #72 右键菜单（~140 行）——🔥 对标 VS Code Explorer 全量菜单

VS Code Explorer 右键菜单有约 20 项，按文件/目录/空白处分组：

```typescript
// FileTreeContextMenu.tsx
// 消费现有 ContextMenu 组件，不手写右键菜单

const MENU_GROUPS = {
  // 第 1 组：导航/打开
  navigation: [
    { id: 'explorer.openFile',       label: '打开',                      when: 'isFile' },
    { id: 'explorer.openToSide',     label: '在侧边打开',                 when: 'isFile' },
    { id: 'explorer.openWith',       label: '打开方式…',                 when: 'isFile' },
    { id: 'explorer.revealInExplorer',label: '在文件管理器中显示',         when: 'always' },
    { id: 'explorer.openInTerminal', label: '在终端中打开',               when: 'isDirectory' },
  ],
  // 第 2 组：编辑
  editing: [
    { id: 'explorer.cut',            label: '剪切',     shortcut: 'Ctrl+X', when: '!isRoot' },
    { id: 'explorer.copy',           label: '复制',     shortcut: 'Ctrl+C', when: '!isRoot' },
    { id: 'explorer.copyPath',       label: '复制路径',                    when: 'always' },
    { id: 'explorer.copyRelativePath',label:'复制相对路径',                 when: 'always' },
    { id: 'explorer.paste',          label: '粘贴',     shortcut: 'Ctrl+V', when: 'clipboard' },
  ],
  // 第 3 组：新建
  creation: [
    { id: 'explorer.newFile',        label: '新建文件',     when: 'isDirectory || isRoot' },
    { id: 'explorer.newFolder',      label: '新建文件夹',   when: 'isDirectory || isRoot' },
  ],
  // 第 4 组：重命名/删除
  modify: [
    { id: 'explorer.rename',         label: '重命名',   shortcut: 'F2',    when: '!isRoot' },
    { id: 'explorer.delete',         label: '删除',     shortcut: 'Delete', when: '!isRoot' },
  ],
  // 第 5 组：搜索
  search: [
    { id: 'explorer.findInFolder',   label: '在文件夹中查找…',              when: 'isDirectory' },
  ],
  // 第 6 组：比较（为 Git 预留——文件树不实现，由 Git 插件注入菜单）
  compare: [],
};
```

**所有文件操作走 FileService 唯一入口**——右键菜单不直接调 `window.linkdesk.*`。

### #73 键盘导航（~80 行）——对标 VS Code List Widget

```typescript
// FileTreeKeyboard.ts
// 对标 VS Code List Widget 键盘导航
// 在 FileTree.tsx 的 onKeyDown 中消费

const KEYBOARD_NAVIGATION = {
  ArrowUp:    'focusPrevious',       // 聚焦上一个
  ArrowDown:  'focusNext',           // 聚焦下一个
  ArrowLeft:  'collapseOrFocusParent', // 折叠/跳转到父节点
  ArrowRight: 'expandOrFocusFirstChild', // 展开/跳转到第一个子节点
  Home:       'focusFirst',
  End:        'focusLast',
  Enter:      'openFocused',         // 打开聚焦项（目录展开/折叠，文件打开）
  Space:      'toggleExpand',        // 展开/折叠目录
  // 🔥 Type-ahead：按键 1s 内累积字符 → 跳到匹配的文件名
  // 对标 VS Code listWidget.ts 的 typeController
};
```

**type-ahead 实现（对标 VS Code）：**
- 用户在文件树上快速输入 "pac" → 1s 窗口内累积 → 跳到 "package.json"
- 每个按键重置 1s 计时器
- 大小写不敏感

### #74 快捷键注册（~60 行）

```json
// plugin.json contributes.keybindings
{
  "keybindings": [
    { "key": "F2",              "command": "explorer.rename",         "when": "explorerFocus && !inputFocus" },
    { "key": "Delete",          "command": "explorer.delete",         "when": "explorerFocus && !inputFocus" },
    { "key": "Ctrl+X",         "command": "explorer.cut",            "when": "explorerFocus && !inputFocus" },
    { "key": "Ctrl+C",         "command": "explorer.copy",           "when": "explorerFocus && !inputFocus" },
    { "key": "Ctrl+V",         "command": "explorer.paste",          "when": "explorerFocus && !inputFocus" },
    { "key": "Ctrl+N",         "command": "explorer.newFile",        "when": "explorerFocus" },
    { "key": "Ctrl+Shift+N",   "command": "explorer.newFolder",      "when": "explorerFocus" },
    { "key": "Ctrl+Shift+F",   "command": "workbench.view.search",   "when": "!searchFocus" },
    { "key": "Enter",           "command": "explorer.openFocused",    "when": "explorerFocus && !inputFocus" },
    { "key": "Ctrl+Enter",     "command": "explorer.openToSide",     "when": "explorerFocus && isFile" },
  ]
}
```

所有快捷键走 KeybindingRegistry（E2c #16-#17a），支持冲突检测 + when 条件。

### #75 拖放（~50 行）——对标 VS Code Explorer Drag & Drop

```typescript
// FileTreeDnD.ts
// 四种拖放场景：

// 1. OS → 文件树（文件拖入窗口）
window.addEventListener('drop', (e) => {
  for (const file of e.dataTransfer.files) {
    const destDir = getDropTargetDir(e); // 拖到哪个目录
    FileService.copy(file.path, destDir + '/' + file.name);
  }
  model.refresh(destDir);
});

// 2. 文件树内拖拽（移动文件/目录）
// onDragStart: 设置 dataTransfer（text/uri-list + ExplorerItem[]）
// onDragOver: 计算 drop indicator（插入线 vs 移入目录）
// onDrop: FileService.copy + FileService.deleteEntry（move = copy + delete）

// 3. 文件树 → 编辑区（拖文件到标签栏/主区打开）
// dataTransfer 携带 filePath → 编辑区 drop zone 消费：
//   const pluginId = FileAssociationService.getPluginFor(ext);
//   tabActions.createTab(pluginId, { filePath });

// 4. 🔥 拖放确认（对标 VS Code explorer.confirmDragAndDrop）
// 配置项控制：explorer.confirmDragAndDrop = true → DialogService.confirm("确定移动…")
```

**对标 VS Code 的细节：**
- 拖到节点之间 → 插入线（蓝色横线）
- 拖到目录节点上 → 目录高亮（移入该目录）
- 不能拖到文件上（文件不是容器）
- 不能拖祖先到其后代（循环引用）

### #76 多选操作（~40 行）

```typescript
// 对标 VS Code 多选
// Ctrl+Click → 切换该项的选中状态
// Shift+Click → 选中从 anchor 到该索引的所有项
// Ctrl+A → 全选（当前展开的可见项）

// 多选后批量操作：
// - 批量删除：DialogService.confirm("确定删除 N 个文件？") → 逐个 deleteEntry
// - 批量复制/剪切：ClipboardService 存储 ExplorerItem[]
// - 批量拖放：所有选中项一起移动

// 🔥 实现注意：选择状态用 Set<string>(uri)，不放在 ExplorerItem 上
// 对标 VS Code——选择是视图状态，不是数据状态
```

### #77 行内重命名（~50 行）——🔥 对标 VS Code Inline Rename

```typescript
// 对标 VS Code setEditable() / IEditableData
// F2 → 输入框替换文件名文本（inline，不是弹窗）
// Enter → 确认 → FileService.writeFile(newPath) + FileService.deleteEntry(oldPath)
// Esc → 取消 → 恢复原名
// Tab → 确认当前 + 自动跳到下一个可重命名的文件（VS Code 行为）

// 🔥 边界条件：
// - 文件名冲突：FileService.exists(newPath) → 提示 "已存在同名文件，是否替换？"
// - 文件被锁定：FileService.writeFile 抛错 → toast "文件被其他程序占用"
// - 重命名期间外部删除：watchFile 回调 → 自动取消重命名模式
// - 多选时重命名：只重命名聚焦的那个（不是批量重命名）

async function renameFile(oldPath: string, newName: string): Promise<void> {
  const newPath = dirname(oldPath) + '/' + newName;
  if (await FileService.exists(newPath)) {
    const ok = await DialogService.confirm(`"${newName}" 已存在。\n是否替换？`);
    if (!ok) return;
  }
  await FileService.writeFile(newPath, await FileService.readFile(oldPath));
  await FileService.deleteEntry(oldPath);
  model.refresh(dirname(oldPath));
}
```

**🔥 对标 VS Code 的粘贴重名处理：**
- 同一目录粘贴 → 自动加 " - Copy" 后缀
- 不同目录粘贴 → 保持原名
- 已存在 → " - Copy 2" 递增

---

### #78 剪贴板服务（~40 行）——🔥 对标 VS Code IClipboardService

```typescript
// FileTreeClipboard.ts
// 🔥 对标 VS Code IClipboardService——区分剪切和复制
// 复制 → 粘贴 = 创建副本（" - Copy" 后缀）
// 剪切 → 粘贴 = 移动（源文件消失，等价于拖放）

class FileTreeClipboard {
  private _items: ExplorerItem[] = [];
  private _isCut = false;

  /** 复制到剪贴板 */
  copy(items: ExplorerItem[]): void { this._items = [...items]; this._isCut = false; }

  /** 剪切到剪贴板 */
  cut(items: ExplorerItem[]): void { this._items = [...items]; this._isCut = true; }

  /** 粘贴到目标目录 */
  async paste(targetDir: string): Promise<void> {
    for (const item of this._items) {
      const destPath = this._resolveName(targetDir, item.name);
      await FileService.copy(item.uri, destPath);   // 先 copy
      if (this._isCut) {
        await FileService.deleteEntry(item.uri);     // 剪切 → 再删源文件
      }
    }
    if (this._isCut) this.clear(); // 剪切只能粘贴一次（对标 VS Code）
    model.refresh(targetDir);
  }

  /** 文件名冲突时加后缀 */
  private async _resolveName(dir: string, name: string): Promise<string> {
    let dest = dir + '/' + name;
    if (!(await FileService.exists(dest))) return dest;
    // "file.txt" → "file - Copy.txt"
    const ext = name.lastIndexOf('.');
    const base = ext > 0 ? name.slice(0, ext) : name;
    const suffix = ext > 0 ? name.slice(ext) : '';
    let n = 2;
    while (await FileService.exists(`${dir}/${base} - Copy${n > 2 ? ` ${n}` : ''}${suffix}`)) {
      n++;
    }
    return `${dir}/${base} - Copy${n > 2 ? ` ${n}` : ''}${suffix}`;
  }

  get items(): ExplorerItem[] { return this._items; }
  get isCut(): boolean { return this._isCut; }
  get isEmpty(): boolean { return this._items.length === 0; }
  clear(): void { this._items = []; this._isCut = false; }
}
```

---

## 三、汇总

| # | 任务 | 标杆 | 行数 |
|:--:|------|:--:|:--:|
| #72 | 右键菜单——全量 18 项 + when 条件 + 分组分隔线 | VS Code Explorer context menu | ~140 |
| #73 | 键盘导航——↑↓←→/Home/End/Enter/Space + type-ahead | VS Code List Widget | ~80 |
| #74 | 快捷键注册——F2/Delete/Ctrl+XCV/Ctrl+N/Ctrl+Shift+N/Enter + when | VS Code keybindings | ~60 |
| #75 | 拖放——OS 拖入 + 树内拖拽 + 拖到编辑区 + 拖放确认 | VS Code Explorer DnD | ~50 |
| #76 | 多选——Ctrl/Shift+Click + Ctrl+A + 批量操作 | VS Code multi-select | ~40 |
| #77 | 行内重命名 + 粘贴重名处理 | VS Code setEditable | ~50 |
| #78 | 剪贴板服务——复制/剪切/粘贴 + "Copy" 后缀 + 剪切一次性 | VS Code IClipboardService | ~40 |
| **合计 7 任务** | | | **~460 行** |

---

## 四、对标 VS Code 源码速查

| 你要做的 | VS Code 源码文件 |
|---|---|
| 右键菜单 | `explorerViewer.ts` — `explorerContextMenu` |
| 键盘导航 | `listWidget.ts` — `onKeyDown` + `typeController` |
| 拖放 | `explorerViewer.ts` — `DragAndDrop` + `onDragOver` + `onDrop` |
| 多选 | `listWidget.ts` — `selection` + `onMouseDown` (ctrl/shift) |
| 行内重命名 | `explorerViewer.ts` — `setEditable()` + `IEditableData` |

源码阅读方法：curl `raw.githubusercontent.com/microsoft/vscode/main/src/vs/...` → CSS 先看后 TS。

---

> **← 上一文档：** `01-E4a-文件树核心.md`
> **→ 下一文档：** `03-E4c-文件树集成.md`
> **索引：** `00-README.md`
> **进度：** `07-执行清单.md`（唯一真相源）
