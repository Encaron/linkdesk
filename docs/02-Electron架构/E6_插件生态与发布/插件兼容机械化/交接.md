# 交接.md ——「插件兼容机械化」批次接力本

> **读法**：**倒序，顶部 = 最新**。**只追加，不改旧段**（旧段是历史，改了就丢证据）。
> **一次可做多格**（🔴 **2026-09-18 用户取消原「一 AI 一格」**；建议编排 = **3 会话 × 2 格**，见顶部「编排变更」段）：一个会话**可**做一格或多格，**但每格各自收尾**——做完**每一格**都在**顶部追加**一段「给下一个 AI 的话」（**两格 = 两段**，⛔ 不许揉成一笔），并把**剩余任务队列表整张复制**进新段、把自己那格改 ✅；**做完一格就提交一格**。
> **批次总纲**（权威方案 / 6 格全貌 / 禁区 / 复量命令）：[00-整理档案.md](00-整理档案.md)
> **上位清单**：[E6-执行清单.md](../E6-执行清单.md) 第 8 层（勾格与轮次进度行落在那里）。

---

## ← 编排变更（**非格子 · 交棒纪律变更**）· 2026-09-18：用户取消「一 AI 一格」⇒ **建议 3 会话 × 2 格**

> 本段不是某一棒的交接口，是**纪律变更**的告知（按「只追加不改旧段」补记）——免得下一棒拿着旧读法**以为只能做一格**，或反过来把两格**揉成一笔**。
> 🔴 **射程只在本批（L8 插件兼容机械化）**：`样式命名空间归一化` / `非样式命名空间归一化` / `插件源码外移层` 各自的接力纪律**一字未动**（那些系列的「一格一账」照旧）。

### ⓪ 用户改了什么、什么没改

- **用户原话（2026-09-18）**：「我也取消必须一次一格的要求了，所以让你看看怎么让ai做，几个任务一个ai这样？」
- **作废**：「一个 AI 会话只执行一格」。
- **保留**（这四条不是仪式，是**账目可核对的前提**）：
  1. **一格一次收尾**：每格做完**各自** ① 顶部追加一段 ② 剩余队列表整张复制 ③ 勾格全套 ④ 轮次进度行——**两格 = 两段、两套勾格**，⛔ 不许揉成一笔（先例：样式归一化系列 `1.17` ＋ `1.19b` 交给同一个 AI，那次的写死要求就是「**不要揉成一笔**」）。
  2. **做完一格就提交一格**（⛔ 不攒着提交）——中途换会话 / 出错时损失最小，账也随时可核。
  3. **同一时刻只推进一格**；**跨仓 / 发版 / 用户点头闸门处串行**，⛔ 不并行（格 5 与格 7 都动 `marketplace`；格 4 的读数就是格 5 的输入；格 5 段 2 的用户点头见下面「通知段」）。
  4. **格数仍是上限不是配额**：**7 格一格不能省**——取消的是「几格一个会话」，**不是**「格能不能省」。

### ⑴ 建议分组：**3 会话 × 2 格**（顺着依赖链切，不是按均匀切）

| 会话 | 格 | 为什么这两格一起 | 预计调用 | 非高峰估算 |
|:--:|:--|:--|:--|:--|
| **A** | 格 2 `E6#115`（先）→ 格 3 `E6#116` | **格 3 的前置就是格 2**，且格 2 的**红报文就是格 3 的登记入口**；同一族文件（面快照 / 退役账 / `host-reserved` / SDK schemas）**摸一遍就够** | 240–310 | ¥1.7–2.4 |
| **B** | 格 4 `E6#117`（先）→ 格 5 `E6#118` | **同一条数据链的两半**：格 4 算出状态、格 5 把它显示出来（格 5 前置 = 格 4 ＋ 用户点头 ✅ 已解锁）；两格都动壳 ＋ `marketplace` | 300–430 | ¥2.3–3.3 |
| **C** | 格 6 `E6#119`（先）→ 格 7 `E6#120` | **格 6 交 §8.7 素材、格 7 出全批收口报告**（第 0 格写死的分工）⇒ 连做天然衔接；两格都在壳 ＋ SDK / `@linkdesk/ui` 发版轴上 | 330–470 | ¥2.5–3.6 |

- **依赖（定顺序，别背成口诀）**：**A 与 B 互不依赖**（谁先都行）；**C 必须最后**——格 6 等格 2（在 A），格 7 要等全部读数（A ＋ B）。
- ⚠️ **这是建议、不是死规**：任一会话**做到 ~250 调用、或压缩到 ≥3 次**就该收尾换会话（账本里「三格一会话」那段 = **269 调用 / 4 次压缩 ＝ 已知成功上限**；格 1 = **140 调用 / 1 次压缩**）。**A/B/C 之间的边界随时可切**——每格本来就是天然断点。
- **变体（要一句话质量最稳时）**：格 5 是全批**唯一用户可见 ＋ 有禁词口径**的格，可把它**单独一棒**（4 会话 ≈ ＋1 次进场 ¥0.1）。默认仍建议 3 会话。

### ⑵ 诚实成本账：分批省的**不是大头**

- 实测单价 ≈ **¥0.0076 / 次调用（非高峰）**，且**与会话长短几乎无关**：格 1（单格）= 140 调用 ¥1.06（均 ctx 104k）；账本里「三格一会话」那段 = 269 调用 ¥1.96（均 ctx 102k）⇒ 单次均 **0.0076 vs 0.0073**，基本持平（长会话靠压缩反复把上下文压回 ~100k）。
- ⇒ **分批真正省的**：① 少 3 次进场重读（≈30–45 次调用）；② **同一工作面不重复探索**（格 2/3 同一族文件、格 5/7 同一 `marketplace`——第二次做时地图已在上下文里）。合计约 **10–15%**。
- ⇒ **分批的代价**：单会话更长 ⇒ 压缩更多（每次 ¥0.04–0.365，且**压缩会重写账本里的旧记录**）＋ 判据串味风险 ＋ 中途出事损失更大——所以 ⓪ 里那四条**不是可选项**。
- 🔴 **省钱大头不在这**：**非高峰（工作日 18:00 后 / 周末）跑 ⇒ 单价直接减半**，比任何编排都划算；其次**不返工**（进场一次读齐、照各格 §⑷/§⑸ 的坑单走）。

### ⑶ 用户派工话术（照抄，只改 N/X/Y）

「你是本批第 N 棒。**本会话做两格：格 X（先）＋ 格 Y（后）**。① 格 X 做完**先收尾并提交**（勾格全套 ＋ 轮次进度行 ＋ 交接段追加 **X 段**），**再**开工格 Y；② 两格读数各写各的表，**⛔ 不许合表、不许揉成一笔提交**；③ 同一时刻只推进一格，跨仓 / 发版处串行；④ 🔴 不许 `git push`，推送等用户点头。」

---

## ← 第 1 格（格 1 · `E6#114` **悬空名核验**）· ✅ 已完成（2026-09-18）：**给下一个 AI ＝ 格 2 · `E6#115` 只加不删门禁**

### ⑩ 🔴 先说你是谁（⛔ 别猜）

你是**第 2 棒**，任务是 [02-任务-只加不删门禁.md](02-任务-只加不删门禁.md)（`E6#115`）。
- **以本段为准**：本段与你读到的任何别的说法冲突，**用本段**。拿不到本段 ⇒ 先问用户「我是不是格 2」。
- **你不是复核者**（本批没有「复核轮」这种格），**也不是派工者**（派工在第 0 格就做完了）。
- ⚠️ **上一棒的「通知段」（用户对设计图点头 ⇒ 格 5 段 2 解锁）仍在下面，照旧生效**——它管的是格 5，与你无关但别当成你的任务。

### ⑴ 上一棒（格 1）做完了什么（一句话 ＋ 读数）

- **一句话**：造出**只读尺子** `scripts/plugin-dangling-name-audit.mjs`——解开插件产物（zip 或目录）取「它喊的名字」，判**在不在宿主定义集里**；**只报不拦、退出码恒 0、⛔ 不进 `npm run check`**。
- **读数（产物 24 个：随包 6 ＋ 官方 18 · 未构建 0）**：**合计悬空 = 2 处 / 1 个名字**。
  - ✅ **判据①（误报控）达标**：`editor` 单独量 **悬空 0**（19 关键帧引全部被兜住／9 模板跳过／137 动态跳过／1128 CSS 提及），**74 个 Monaco·VS Code 裸名一条没报**。
  - 🔴 **唯一那条悬空 ＝ 宿主自己的死引用**：`ldk-sle-row--locked`（渲染点 `StringListEditor.tsx:102`，宿主 CSS **从未定义过**）⇒ 按「只报不修」**挂给宿主维护者**，本格**未改任何插件仓**。
  - 📌 **附带一条真读数（与你有关，见 §⑶ 末条）**：`ctx-item-chevron`（宿主 `ContextMenu.tsx:404,438` 渲染、宿主 CSS **从未定义过该族名**）——按本格口径（非 `ldk-` 只计数）**未判**，**交给你判「算不算面」**。
- **执行记录全文**（口径／逐仓表／残余边界 9 条／check 读数）：[01 §八](01-任务-悬空名核验.md)。**别重写它**，要补就补 §八.4 或自己那格的档。

### ⑵ 剩余任务队列（**自己那格已改 ✅ → 你做完改你那格，再整张复制进新段**）

| 序 | 轮次 | 编号 | 任务 | 详案 | 前置 | 状态 |
|:--:|:--|:--|:--|:--|:--|:--:|
| 1 | 第 8.1 轮 | `E6#114` | 悬空名核验（产物层只读审计） | [01](01-任务-悬空名核验.md) | 无 | ✅ |
| 2 | 第 8.2 轮 | `E6#115` | 只加不删门禁（面快照 ＋ diff ＋ 判红） | [02](02-任务-只加不删门禁.md) | 无（与格 1 互不依赖） | ⬜ |
| 3 | 第 8.3 轮 | `E6#116` | 退役登记（账 ＋ 流程 ＋ 机械对账） | [03](03-任务-退役登记.md) | 格 2 | ⬜ |
| 4 | 第 8.4 轮 | `E6#117` | 兼容读数 | [04](04-任务-兼容读数.md) | 格 1 ✅ | ⬜ |
| 5 | 第 8.5 轮 | `E6#118` | 市场显示状态（**含 HTML 设计图**） | [05](05-任务-市场显示状态.md) | 格 4 ＋ **用户点头 ✅ 2026-09-18** | ⬜ |
| 6 | 第 8.6 轮 | `E6#119` | 判据铺开（SDK 随包 ＋ 18 仓接腿 ＋ 发版） | [06](06-任务-判据铺开.md) | 格 1 ✅ ＋ 格 2 | ⬜ |
| 7 | 第 8.7 轮 | `E6#120` | 通用悬停提示卡（**壳的共享件**）＝ **本批最后一棒**（＋ 全批收口报告） | [08](08-任务-通用悬停提示卡.md) | 格 5（⚠️ **可换序、⛔ 不并行**） | ⬜ |

> **执行序 = 自上而下**。🔴 **格数是上限不是配额**（**7 格**一格不能省；判「不做」要写理由，写进本交接段的「不做」节）。
> ⚠️ **别与 `E6#113`（插件侧死类清账）并行**——那格要动 3 个插件仓，格 5 与格 7 也动 `marketplace`。
> ✅ **格 1 与格 2 互不依赖**（第 0 格已写死）⇒ 格 2 可立即开工，**不必等任何前置**。

### ⑶ 你这一棒（格 2）具体做什么——**以 [02 号档](02-任务-只加不删门禁.md) 为准**，这里只点三条最容易做错的

1. **面快照第 ③ 栏「宿主 `ldk-*` 类名 ＋ 保留关键帧」的生成口径，直接抄格 1**：[01 §8.2](01-任务-悬空名核验.md)①③⑤——扫 `src/**/*.css`（含共享组件单一源码）用 `scripts/lib/css-selectors.mjs` 的 `bareClassDefinitions` 取**裸定义**（今天 **348** 个，全部 `ldk-*`）＋ `reserved-class-names.json` 的 **8 条**关键帧；🔴 **两处必须是同一个生成器**（否则「快照说缺、尺子说没悬空」，就是本系列还了两轮的那种债）。
2. **拿不到基线 ⇒ 拒绝判定并红**（02 号档硬边界②）——⛔ 不许静默放行、⛔ 不许比工作区。
3. 📌 **格 1 交给你的一条真读数**：`ctx-item-chevron`（宿主 `ContextMenu.tsx:404` ＋ `:438` 渲染，宿主 CSS 该族名**任何拼写都不存在**，`git log -S` 证明**从未定义过**）。格 1 按口径（非 `ldk-` 不是宿主契约）**只计数不判**——**它落在你的面快照第 ③ 栏边界上**：它是「渲染方」不是「定义」，**本格如何处置（计不计面 / 记不记账）请写进你的 §八 与交接段**。⚠️ 顺带的同类：`ldk-sle-row--locked`（这条带 `ldk-`，是格 1 报出来的真悬空）。

### ⑷ 格 1 踩出来的（**别重踩**）

1. 🔴 **改作者面文档 ⇒ 必须重生成随包文档**：改了 `docs/03-插件制造/**` 或 `docs/03-plugin-authoring/**` 就跑 `npm run docs:build`，否则 `generate-plugin-docs --check` **当场红**（本格实撞）；内容变 ⇒ `@linkdesk/plugin-docs` **版本要动**（0.1.18 → **0.1.19**）＋ `package-lock.json` 同步（`check-lockfile-sync` 会指出「manifest = 0.1.19，lock = 0.1.18」）。
2. ⚠️ **产物是 zip**：必须 `unzip -p <zip> <entry>` 读条目；**grep zip 得全 0，那不是读数**。
3. ⚠️ **静态尺子必须做 JS 词法**：不做就会命中**字符串里**的 `className=`（生成式代码 `'… className="' + x`）⇒ 本格 v1 一版误报 **2145** 处（editor 一只 ~1029 处 Monaco 名）。做法 = 跳过字符串/模板/注释/正则，只认**代码上下文**的站点。
4. ⚠️ **跨产物聚合别在「条目数组」上误用 `.keys()`**：`r.notJudged` 是 `[[名字, 出处], …]` 数组，`.keys()` 给的是**下标** ⇒ 把「去重名字数」算成「最长那份的条数」（本格实测踩到：74 被当成并集，真并集 99）。**已提成 `uniqueNotJudged()` ＋ 自测「负控F-并集去重」钉住**。
5. ⚠️ **读数相等 ≠ 字节相等**：随包 6 只与官方同名 6 仓读数逐格相等，但 `settings` 一对 **zip 字节不同**（逐条目 sha256 相同 ⇒ 差在 zip 容器元数据）——对账时别用 sha256 下结论。
6. ⚠️ **`packages/linkdesk-ui/dist/index.css` 是派生物**（gitignored）：本格**实测**不读它**不丢定义**（它里面 85 个 `ldk-*` 名逐个都能在壳 `src/**/*.css` 找到，未覆盖 = 0）——你要用「宿主面」时照此口径，别把派生物和真源重复计。
7. ⚠️ **文档门禁会抓自己**：`check-doc-links`（断链 = 红）＋ `check-design-flow` §8.4③（含 `window.linkdesk.` / `contributes.` 的 md 必须带「设计前置」或「非新能力」声明）——续写时**保持这两个词在**。

### ⑸ 不做（**边界，明写理由**）

| 不做 | 理由 |
|:--|:--|
| 不推 `git push` / 不发 npm / 不推 18 仓 | 🔴 用户红线：**等用户本人点头**（memory `push-wait-for-user`）。格 1 因此**留了一盏 npm 黄灯**（见 §⑻） |
| 不并格（**格 1 当时**的纪律） | 该纪律 2026-09-18 已被用户取消 ⇒ 下一棒起按顶部「**编排变更**」段的分组做（**格数仍是上限不是配额：7 格一格不能省**） |
| 不改机制（加载器/注册表/主题引擎/`minAppVersion` 拒载） | 本批只加判据、账、读数、显示 |
| 格 1 不改任何插件源码 / 不改 SDK | 格 1 是**只读审计**（发现悬空只**报**，挂给作者 / 宿主维护者） |
| 格 1 不引黑名单 / 不引白名单 | 禁区③（退役名黑名单 ＝ 要人养名单 ＋ Monaco 当场假红）；红线段② |
| 格 1 不做门禁 | 静态尺子读不出动态拼接（今天 500 处跳过）⇒ 拦就假红；照 `runtime-style-audit.mjs` 的定位 |

### ⑹ 提交清单（格 1）

- **新增**：`scripts/plugin-dangling-name-audit.mjs`。
- **改动**：`package.json`（3 条 `audit:plugin-dangling*`）· `docs/02-Electron架构/E6_插件生态与发布/E6-执行清单.md`（第 8 层 `E6#114` 勾格全套 ＋ 轮次进度行 8.1 ✅）· `docs/02-Electron架构/E6_插件生态与发布/插件兼容机械化/01-任务-悬空名核验.md`（§八 执行记录）· `docs/03-插件制造/05-插件UI写法规约.md` ＋ `docs/03-plugin-authoring/05-ui-conventions.md`（§12.4 作者面一句）· `packages/plugin-docs/docs/**`（重生成 2 文件）· `packages/plugin-docs/package.json`（0.1.18 → 0.1.19）· `package-lock.json`（1 行同步）。
- **零 `src/` 改动 ⇒ 软件版本不 bump**（恒 **0.2.11**）。
- ⚠️ 提交信息带类别前缀（本格用 `feat:`／`docs:`）；`git add` 按**显式路径**（⛔ 禁 `git add -A`）。**提交 sha = `219672986`**（11 个文件 · **未推送**）。

### ⑺ 给下一棒的输入（**可直接抄的东西**）

- **宿主定义集口径**（哪些算 / 哪些不算 / 为什么 ＋ **清单怎么生成随包下发**）：[01 §8.2](01-任务-悬空名核验.md)——**格 6 要把它搬进 SDK，口径必须与壳体一致**。
- **`--self-test` 的形态**（正控 / 负控 / 计数的写法）：`scripts/plugin-dangling-name-audit.mjs` 的 `selfTest()`——**格 2 照这个形状写自测**（含「跨产物并集去重」这种**只会在真数据上咬人**的负控）。
- **今天这条基线读数**：[01 §8.3](01-任务-悬空名核验.md)（24 产物 · 悬空 2 处 / 1 名 · 未构建 0）。
- **复跑命令**：`npm run audit:plugin-dangling`（人读）／`:json`（机读）／`:selftest`（13 例）。

### ⑻ 发布实录

- **软件（壳）零发版**：无 `src/` 改动 ⇒ 版本恒 **0.2.11**。
- 🔴 **作者面 `@linkdesk/plugin-docs` 0.1.18 → 0.1.19（内容变 ⇒ 版本动了）· 尚未 `npm publish`、未 `release:mark`** ——红线「`npm publish` 等用户点头」⇒ `npm run check:npm-release` **亮黄灯**（该闸永不拦，check 照绿 EXIT=0）。
  - **收尾动作（等用户点头）**：`npm publish`（`packages/plugin-docs`）＋ `npm run release:mark`。
  - ⚠️ **格 3 与格 7 也要动作者面文档**（各自要 plugin-docs PATCH）⇒ 三格的作者面改动**可以攒着一次发**；发完记得 `release:mark` 把灯关掉。
- 本格 `npm run check` **EXIT=0**（175 文件 / 2461 例）——末尾三行见 [01 §8.5](01-任务-悬空名核验.md)。

---

## ← 通知（**非格子 · 闸门状态变更**）· 2026-09-18：用户对设计图点头 ⇒ **格 5 段 2 解锁**

> 本段不是某一棒的交接口，是**闸门状态变更**的告知（按「只追加不改旧段」补记）——避免下一棒**误以为还要再等一次点头**而空转。

- 🟢 **用户原话（逐字）**：「**注意，我同意按设计图走**」。
- **效力**：格 5（`E6#118`）**段 1 的出口判据① 已达成**（[05 §四①](05-任务-市场显示状态.md) 已勾 ✅）⇒ **段 2（动插件代码）直接开工**，⛔ **不要再向用户要一次点头**。
- **点头对象 = 现行** [mockups/01-市场兼容状态-细节详解.html](mockups/01-市场兼容状态-细节详解.html)——**当天已按用户要求改过的那一版**（插件详情页大标题旁的「部分不适配」标砍掉，状态词**全页只留信息栏那一处**）。
- **覆盖范围（6 条，逐条照办、偏离写 §9.3）**：① 五词（正常/兼容/部分不适配/不适配/—）② **状态标全页唯一**（信息栏那一行；标题旁不挂、侧栏不标）③ 悬停帮助卡（停标出卡、移开消失）④ 信息栏**不写**「鼠标停上来看」提示行 ⑤ 状态标**不挂 `title`** ⑥ **不进侧栏列表**。全文见 [05 §9.2](05-任务-市场显示状态.md)。
- ⚠️ **不覆盖**（各自**单独**等用户点头）：`git push` 壳仓 / 推插件仓 `marketplace` / `npm publish` / 官方目录收录。**本批红线未变**。
- 📌 队列与前置**其余不变**（下面这张表照旧；格 5 与格 7 仍是「**可换序、⛔ 不并行**」）。

| 序 | 轮次 | 编号 | 任务 | 详案 | 前置 | 状态 |
|:--:|:--|:--|:--|:--|:--|:--:|
| 1 | 第 8.1 轮 | `E6#114` | 悬空名核验（产物层只读审计） | [01](01-任务-悬空名核验.md) | 无 | ⬜ |
| 2 | 第 8.2 轮 | `E6#115` | 只加不删门禁（面快照 ＋ diff ＋ 判红） | [02](02-任务-只加不删门禁.md) | 无（与格 1 互不依赖） | ⬜ |
| 3 | 第 8.3 轮 | `E6#116` | 退役登记（账 ＋ 流程 ＋ 机械对账） | [03](03-任务-退役登记.md) | 格 2 | ⬜ |
| 4 | 第 8.4 轮 | `E6#117` | 兼容读数 | [04](04-任务-兼容读数.md) | 格 1 | ⬜ |
| 5 | 第 8.5 轮 | `E6#118` | 市场显示状态（**含 HTML 设计图**） | [05](05-任务-市场显示状态.md) | 格 4 ＋ **用户点头 ✅ 2026-09-18** | ⬜ |
| 6 | 第 8.6 轮 | `E6#119` | 判据铺开（SDK 随包 ＋ 18 仓接腿 ＋ 发版） | [06](06-任务-判据铺开.md) | 格 1 ＋ 格 2 | ⬜ |
| 7 | 第 8.7 轮 | `E6#120` | 通用悬停提示卡（**壳的共享件**）＝ **本批最后一棒**（＋ 全批收口报告） | [08](08-任务-通用悬停提示卡.md) | 格 5（⚠️ **可换序、⛔ 不并行**） | ⬜ |

---

## ← 第 0 格（派工）· 已完成（2026-09-18）：**给下一个 AI ＝ 格 1 · `E6#114`**

### ⑩ 🔴 先说你是谁（⛔ 别猜）

你是**第 1 棒**，任务是 [01-任务-悬空名核验.md](01-任务-悬空名核验.md)（`E6#114`）。
- **产物 = 一把只读尺子 ＋ 自测 ＋ 逐仓读数 ＋ 作者面一句 ＋ 本交接段**。
- **你不是复核者**（本批没有「复核轮」这种格），**也不是派工者**（派工在本段就做完了：第 0 格写了 6 份任务书）。
- **以本段为准**：本段说的与你读到的任何别的说法冲突，**用本段**。拿不到本段 ⇒ 先问用户「我是不是格 1」。

### ⑴ 本批是干什么的（一句话 ＋ 判据 ＋ 根因 ＋ 收口口径）

- **一句话**：平台承诺「旧插件在新版本上仍然能跑」（memory `plugin-authoring-manual.md:179-180`），但**没有门禁拦「把插件能用的名字拿走」**，也**没有尺子量「插件喊的名字宿主还有没有」** —— 本批 **6 格**把这两件事变成机械件；**第 7 格**（`E6#120`，[08](08-任务-通用悬停提示卡.md)）把格 5 那张悬停提示卡抽成**壳的共享件**。
- **判据**：产出的每一件都必须是**机械化、自动化、常驻**的（不靠记性）——这是用户对前面几轮反复说的「我讨厌『下次小心』」的正面答案。
- **根因**（每棒收尾都要能复述）：**宿主手里有一堆插件正在引用的名字，拿走它们没有任何机械阻力，也没有任何尺子量得出拿走之后谁的产物悬空了。**
- **收口口径**：本批结束时该读到的**判据六条 ＋ 共享件一条**（见总纲 §五）；残余边界八条**一条不许省**。🔴 **报告由格 7 出**（格 6 只交 §8.7 素材）。

### ⑵ 剩余任务队列（**每棒收尾：自己那格改 ✅，然后整张复制进新段**）

| 序 | 轮次 | 编号 | 任务 | 详案 | 前置 | 状态 |
|:--:|:--|:--|:--|:--|:--|:--:|
| 1 | 第 8.1 轮 | `E6#114` | 悬空名核验（产物层只读审计） | [01](01-任务-悬空名核验.md) | 无 | ⬜ |
| 2 | 第 8.2 轮 | `E6#115` | 只加不删门禁（面快照 ＋ diff ＋ 判红） | [02](02-任务-只加不删门禁.md) | 无（与格 1 互不依赖） | ⬜ |
| 3 | 第 8.3 轮 | `E6#116` | 退役登记（账 ＋ 流程 ＋ 机械对账） | [03](03-任务-退役登记.md) | 格 2 | ⬜ |
| 4 | 第 8.4 轮 | `E6#117` | 兼容读数 | [04](04-任务-兼容读数.md) | 格 1 | ⬜ |
| 5 | 第 8.5 轮 | `E6#118` | 市场显示状态（**含 HTML 设计图**） | [05](05-任务-市场显示状态.md) | 格 4 ＋ **用户点头** | ⬜ |
| 6 | 第 8.6 轮 | `E6#119` | 判据铺开（SDK 随包 ＋ 18 仓接腿 ＋ 发版） | [06](06-任务-判据铺开.md) | 格 1 ＋ 格 2 | ⬜ |
| 7 | 第 8.7 轮 | `E6#120` | 通用悬停提示卡（**壳的共享件**）＝ **本批最后一棒**（＋ 全批收口报告） | [08](08-任务-通用悬停提示卡.md) | 格 5（⚠️ **可换序、⛔ 不并行**） | ⬜ |

> **执行序 = 自上而下**。🔴 **格数是上限不是配额**：**7 格**一格不能省（⚠️ 2026-09-18 立格 7 后由「6 格」改为 7 格；判「不做」要写理由，写进本交接段的「不做」节）。
> ⚠️ **别与 `E6#113`（插件侧死类清账）并行**——那格要动 3 个插件仓，格 5 与格 7 也动 `marketplace`。
> 🔴 **格 6 原写的「恒最后」已作废**（2026-09-18 立格 7）⇒ **全批收口报告归格 7**；格 6 只交 §8.7 的收口素材。

### ⑵b 🔴 文案口径（用户 2026-09-18 两次纠偏后定稿——**格 5 那一棒必须照办**）

用户原话（逐字）：①「我感觉你很多东西写的都很口语化，比如"**老但兼容**"，直接写个**兼容**不好吗？」②「安装版软件更多是给用户看的，你写个 **n 处待修**，这个"待修"是给谁看的？给用户看的吗？指望用户修完后放到源码中吗？**你要清楚这是给谁的软件**。」③「**我从没见过手机厂给用户发的版本更新里写他们改的什么源码**。」④「在用户视角看，有个**正常，兼容，不适配**等提示才是合理的」。

- **三套词表按受众分家**（**唯一真相源 = [00 总纲 §〇d](00-整理档案.md)**）：**用户**面只许 **正常 / 兼容 / 部分不适配 / 不适配 / —** ＋ 用户自己能做的动作；**插件作者**面才写「N 处引用已失效的名字 ＋ `文件:行` ＋ 升依赖重打包」；**维护者**面用「悬空名 / 面快照 / 只加不删 / 退役账」。
- ⛔ **已作废的说法**（谁都不许再用）：「老但兼容」「N 处待修」「部分异常」「契合度/契合」。**禁词机械核** = [05 §四⑥](05-任务-市场显示状态.md)。
- ⚠️ 本批文档里凡是给用户看的字，写的时候先问一句：**这句话是给用户看，还是给作者看？**

### ⑶ 你这一棒（格 1）具体做什么

- **要做的四步**：① 脚本 `scripts/plugin-dangling-name-audit.mjs`（只读、只报不拦、**不进 `npm run check`**）② npm script ＋ `--self-test`（正控 ＋ 三条负控）③ 逐仓读数（随包 6 只 ＋ 官方 18 仓产物，合计预期 **0**）④ 作者面一句 ＋ 清单勾格 ＋ 交接段。详案见 [01](01-任务-悬空名核验.md) §二。
- 🔴 **最要紧的一条判据 = 误报控**：`editor` 产物里 **60 处裸名是 Monaco / VS Code 自带的**（`monaco-workbench` 978 处等）⇒ **一条都不许报**。假红比漏报更坏。
- ⛔ **别把尺子做成门禁**（静态尺子读不出动态拼接，拦就假红）；⛔ **别放 `scratch/`**（那里不进 git）。

### ⑷ 第 0 格踩出来的（**别重踩**）

1. 🔴 **「版本印记」方案已否**：插件 zip 里**没有 `package.json`**（`packages/plugin-sdk/src/pack.ts:56-62` 的排除表）、bundle 文本里**没有任何版本字符串**（grep `0.2.x` 命中 0）⇒ 判据**物理上取不到输入**。
2. 🔴 **「扫退役名黑名单」已否**：要人养名单（禁区），而且 Monaco 的 60 处裸名当场假红。
3. ⚠️ **产物是 zip，不是文本**：必须 `unzip -p <zip> <entry>` 读条目；**grep zip 得到全 0，那不是读数**（第 0 格踩过）。
4. ⚠️ **中文长文本别用 shell heredoc**（反斜杠会被吃掉）；写文档用编辑器/工具。
5. ⚠️ **两道文档门禁会抓自己**：`check-doc-links`（断链 = 红）＋ `check-design-flow` §8.4③（含 `window.linkdesk.`/`contributes.` 的 md 必须带「设计前置」或「非新能力」声明）——本批 7 份档已按此写好，你续写时**保持这两个词在**。

### ⑸ 不做（**边界，明写理由**）

| 不做 | 理由 |
|:--|:--|
| 不推 `git push` / 不发 npm / 不推 18 仓 | 🔴 用户红线：**等用户本人点头**（memory `push-wait-for-user`） |
| 不并格（一格做完就交棒） | 纪律：一 AI 一格，格数是上限不是配额 |
| 不改机制（加载器/注册表/主题引擎） | 本批只加判据、账、读数、显示 |
| 本格不改任何插件源码 / 不改 SDK | 格 1 是**只读审计**（发现悬空只**报**，挂给作者） |
| 不设「已 bump 未 tag」哨兵 | 用户 2026-09-18 明确不要（接受跳版本、攒着发） |

### ⑹ 提交清单（第 0 格 ＝ 本批文档，**未提交**）

- 新增：`docs/02-Electron架构/E6_插件生态与发布/插件兼容机械化/`（`00-整理档案.md` · `01`–`06` 六份任务书 · `交接.md` · `mockups/01-市场兼容状态-细节详解.html`）。
- 改动：`docs/02-Electron架构/E6_插件生态与发布/E6-执行清单.md`（新增第 8 层块 ＋ 执行顺序段一行 ＋ 总结表 L8 行与合计 ＋ 📌 追加说明）。
- **提交 sha 事后追记**（本段写于派工时，尚未提交）。
- ⚠️ 提交信息带类别前缀（`docs:`）；`git add` 按**显式路径**（⛔ 禁 `git add -A`）。

### ⑺ 给下一棒的输入（**可直接抄的东西**）

- **口径**：宿主定义集 = 壳 `src/**/*.css` 的类名定义 ＋ `packages/plugin-sdk/schemas/reserved-class-names.json` 的 8 条保留关键帧；自身定义集 = 产物包内 CSS。
- **形状先例**：`scripts/plugin-css-prefix-audit.mjs`（只读审计）／`scripts/runtime-style-audit.mjs`（不进 check 的定位）／`scripts/lib/css-selectors.mjs`（抽取库，⛔ 别新造）。
- **今天的基线**：随包 6 只 ＋ 四只 ui-dependent 产物**悬空 = 0**（开跑前自己复量一遍对账）。

### ⑻ 发布实录

- 本批**零发版**（第 0 格只动文档）；后续每格的发版记在自己那段里。🔴 **凡更新就发**（SDK/ui/docs 内容变 ⇒ 必须发）＋ **插件轴先发、壳最后**。

---

<!-- ↑ 新的交接段追加在这条线的上方（越靠上越新） -->
