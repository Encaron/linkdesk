# Serial Monitor V3 当前状态

> 最后更新：2026-07-18  
> 当前阶段：Phase 2.5 ✅ 完成  
> 下一阶段：Phase 3 — 主区标签页 + 分屏系统（→ [V3-Phase3-标签页分屏设计.md](V3-Phase3-标签页分屏设计.md)）

---

## 一、项目概况

V3 是 V2 的重架构——保持 V2 全部功能，底盘从 WPF .NET 8 换成 Tauri + React。

| | V2 | V3 |
|------|------|------|
| 框架 | WPF .NET 8 | Tauri v2 + React 18 + TypeScript |
| 接收区 | AvalonEdit | CodeMirror 6 |
| 发送栏 | 多行 TextBox | Monaco Editor 单行 |
| 主题 | DynamicResource | CSS 变量 21 色 |
| 双语 | 自研 T() 函数 | react-i18next `t()` |
| 串口 | C# SerialPort | Rust serialport + tokio |
| 协议 | `[type, subType, ...]` | `[卡片ID, 字段...]` |

---

## 二、已完成功能

### 终端视图

- CM6 只读接收区，等宽字体，行号显示
- 三色行：接收白色 / 发送蓝色 / 系统消息灰色
- Monaco 单行发送栏，`>` 命令前缀，Enter 发送
- 快捷发送药丸按钮（AT / AT+CWLAP / AT+CWJAP）
- 侧栏设置：时间戳格式 / 消息回显 / 行号 / 系统消息独立显示 / 换行符 / 定时发送 / 发送后清空 / 编码
- 所有设置**真实生效**（走 React Context 共享状态）
- 搜索（CM6 原生面板，按钮 toggle + 蓝色高亮同步）
- 暂停（实时计数，缓冲 2000 条上限，恢复回放）
- 导出日志（`showSaveFilePicker` API）
- 行数上限裁剪（2000 行 → 砍 500）
- "↓ 回到底部"浮动按钮

### 布局

- 3 行 × 3 列骨架（和 V2 一致）
- 顶栏：串口选择 / 波特率 / 打开按钮（绿点）+ 中/EN / ☀ / ?
- 图标栏：📟 终端 / 📊 工作台 / ⚙ 设置，选中蓝条 + 光晕
- 侧栏：220px 默认，160-520px 可拖拽，28px 折叠
- 状态栏：连接绿点 + TX/RX 计数
- 点 ⚙ 设置不改变上一次内容视图（和 V2 `_previousContentTab` 同逻辑）

### 基础设施（V2.6 教训）

- **CSS 变量 21 色**：所有颜色走 `var(--xxx)`，禁止硬编码 hex
- **react-i18next `t()`**：全部 UI 文字走翻译函数，`en.json` 已就绪（45 条映射）
- **归一化 `.form-row`**：标签+控件行强制对齐，`space-between` 错位从根源消灭
- **组件不从 JS 设颜色**：切主题 = 改 `data-theme` 一个属性

### 核心服务（14/14 单测通过）

| 模块 | 功能 |
|------|------|
| DataConverter | Bytes↔Hex↔Text，V2 算法照搬 |
| ProtocolParser | 方括号协议解析，V2 算法照搬，V3 协议适配 |
| RingBuffer | 512 容量，单生产者单消费者 |
| PreferenceService | 唯一配置入口，Phase 1 用 localStorage，Phase 2 切文件 |

### Rust 串口层

| 命令 | 功能 |
|------|------|
| `list_ports` | 枚举 COM 口 |
| `open_port` | 打开 + 启动 tokio 读线程 |
| `close_port` | 关闭 + 冲刷残留 |
| `send_data` | 发送字节 |
| `set_dtr` / `set_rts` | 硬件流控 |
| 读线程 | 100ms 超时 → `\n` 拆行 → emit `serial-data` |

### 数据管道

```
STM32 → Rust serialport 读线程 → emit("serial-data")
→ React listen → RingBuffer.write() → rAF drainAll()
→ CM6 appendLine(三色) → 暂停/搜索/导出/裁剪
```

---

## 三、待完成

### Phase 2 — 终端完善 ✅ 完成

**P0 5/5 + P1 7/7 全部实现。** P2 6 项完成 4 项，2 项不阻塞日常使用。🌟 新架构红利 4 项完成 3 项。

| 优先级 | 完成 | 项目 | 最终 commit |
|------|:--:|------|------|
| P0 | 5/5 | 发送历史 / COM 枚举+热插拔 / HEX / 定时发送 / 波特率运行时切换 | `ec92a14` |
| P1 | 7/7 | 行号 toggle / 编码切换 / 系统消息 / TX/RX 计数 / 右键菜单 / 快捷发送 / 搜索 | |
| P2 | 4/6 | 连接过渡 / 协议筛选 ✅ — 新数据闪烁 / DTR/RTS UI 未做（P2 打磨项，不阻塞） | |
| 🌟 | 3/4 | 实时过滤 / Command Palette / 语法高亮 ✅ — 多视图 tab 延后（Phase 3 或独立迭代） | `260dc97` |

**未做的 P2 项：**
- #13 新数据闪烁（50ms 半透明蓝底→消失）— 视觉打磨，不影响功能
- #15 DTR/RTS UI — Rust 命令 `set_dtr`/`set_rts` 已就绪，TopBar 没有对应的 toggle；Phase 5 设置视图加
- #16 数据位/停止位/校验 — 串口打开时未提供这些参数；Rust `serialport` crate 支持，需要时加

> 这些是 V2 也有的功能——V2 的高级串口设置里有数据位/停止位/校验，DTR/RTS 在流控里。Phase 2 定位是"达到日常可用"，串口助手的日常场景 115200-8N1 无流控就够了。**不阻塞进 Phase 3。**

### Phase 2.5 — 结构整理 ✅ 完成

> 2026-07-18 架构审查。不改逻辑，只整理结构。详见 git commit `588cf2a` / `ad2a935` / `260dc97`。

#### 实际完成 vs 计划

| 2.5.x | 计划 | 实际 | 行数 |
|------|------|------|:--:|
| TerminalView 拆分 | 7 个子组件，主文件→~200 行 | 4 个子组件，主文件 933 行 | — |
| `terminal/SearchBar.tsx` | ~80 行 | ✅ 84 行 | 84 |
| `terminal/FilterMenu.tsx` | ~50 行 | ✅ 55 行 | 55 |
| `terminal/CommandPalette.tsx` | ~60 行 | ✅ 68 行 | 68 |
| `terminal/ReceiveContextMenu.tsx` | ~40 行 | ✅ 31 行 | 31 |
| `terminal/SendBar.tsx` | ~100 行 | ❌ **放弃**——发送逻辑耦合 CM6 + 串口 IPC + prefs，线程 props 得不偿失 | — |
| `terminal/SendHistory.tsx` | ~40 行 | ❌ **放弃**——同上，和 SendBar 是同一块逻辑 | — |
| `terminal/QuickSendBar.tsx` | ~80 行 | ❌ **放弃**——快捷发送管理深度耦合 handleQuickSend + prefs 持久化 | — |
| 共享 UI 组件 | Toggle / Select / FormRow | ✅ 全部完成，TerminalSidebar 已接入 | 17+23+17 |
| ErrorBoundary | 包裹 TerminalView | ✅ MainContent.tsx 中包裹 | 37 |
| Tauri hook | `useTauriEvent(data)` | ✅ 升级为 callback 模式，TerminalView 已替换手写 gen counter | 51 |
| 测试 | 3 文件 | ✅ DataConverter + ProtocolParser + RingBuffer，30 用例全通过 | 76+68+37 |

**SendBar/SendHistory/QuickSendBar 为什么放弃：**

```
SendBar 不是纯 UI 浮层——它是"发送动作中枢"：
  用户敲 Enter
    → invoke("send_data")          ← Tauri IPC
    → appendLine("sent")           ← 写 CM6 接收区
    → recordHistory()             ← 更新发送历史
    → setSendValue("")            ← 清空输入框
  同时读 prefs 的 5 个字段（sendMode/sendCoding/lineEnding/autoClear/timestampFormat）
```

和拆成功的 SearchBar/FilterMenu 不同——那些是单向消费（读 state → 渲染 UI → 回调）。SendBar 是双向交汇点（Monaco 编辑器 + 串口 IPC + CM6 接收区 + prefs）。**拆 = 把所有核心依赖穿成 props，比留在原位更乱。**

如果未来要拆，正确路径不是拆组件，是抽一个 `useSend` hook（封装发送逻辑 + 历史 + HEX 格式化），Monaco JSX 留在原位。但这超过了"不改逻辑"的范围。

#### TerminalView 当前 933 行的构成

| 区域 | 行数 | 说明 |
|------|:--:|------|
| CM6 初始化 + 装饰系统 + ScrollTracker | ~100 | CM6 特定，无需拆 |
| 数据管道（Tauri 事件→RingBuffer→rAF） | ~45 | `useTauriEvent` 已接入 |
| 工具栏（暂停/清空/导出） | ~55 | 简单操作，自成一体 |
| 搜索逻辑（runSearch/navigateSearch） | ~55 | SearchBar 是 UI，逻辑留主文件 |
| 发送逻辑（handleSend/autoFormatHex/handleSendChange） | ~90 | 耦合最深，SendBar 弃拆 |
| 快捷发送管理 | ~70 | 同上 |
| 定时发送 | ~25 | 一个 useEffect |
| 右键菜单 + Command Palette | ~30 | ReceiveContextMenu/CommandPalette 已拆 UI |
| Monaco 挂载 + 键盘绑定 | ~40 | beforeMount/onMount |
| JSX 渲染 | ~230 | 工具栏 + CM6 容器 + 快捷发送条 + 发送区 |
| 其他（state 声明/import/formatTimestamp） | ~233 | — |

---

### Phase 3 — 主区标签页 + 分屏系统（← 当前）

→ [V3-Phase3-标签页分屏设计.md](V3-Phase3-标签页分屏设计.md)

卡片架构的前置基础设施。VS Code 风格标签页 + 拖拽分屏，支持左右/上下 split。
技术选型：allotment + 自己写标签栏（~390 行）。

### Phase 4 — 卡片架构 + 工作台视图（原 Phase 3）

- [ ] Card Shell + Component 架构
- [ ] CardRegistry（ID → Card 字典，O(1) 路由）
- [ ] 卡片网格布局 + 拖拽重排（react-grid-layout）
- [ ] Gauge / Switch / Slider / Plot / KeyPad 组件
- [ ] 编辑模式（属性面板 + 插入/删除）
- [ ] ProtocolParser 路由接通
- [ ] workspace.json 读写 + 多 workspace 切换
- [ ] 未知 ID 自动建 generic 卡
- [ ] V2 旧协议兼容翻译层

### Phase 5 — OLED 视图（原 Phase 4，待定）

### Phase 6 — 设置视图 + 打磨（原 Phase 5）

- [ ] 串口默认配置
- [ ] 主题切换（CSS 变量全量切换——底层已就绪）
- [ ] 双语切换（react-i18next——底层已就绪）
- [ ] V2 配置导入
- [ ] 快捷键系统

---

## 四、技术债务

| # | 问题 | 状态 |
|:--|------|:--|
| 1 | CM6 搜索面板 | ✅ 已替换为自建 React 搜索条 + 装饰系统高亮 |
| 2 | PreferenceService：localStorage → Tauri 文件系统 | ⬜ Phase 3 做（需 Tauri fs API） |
| 3 | CM6 行号动态开关 | ✅ Compartment reconfigure 已实现 |
| 4 | Monaco 打包体积 ~150KB | ⬜ 如果后续只做单行可换 textarea |
| 5 | TerminalView 单体膨胀 ~850 行 | ✅ Phase 2.5 拆分 4 个子组件 + useTauriEvent 接入 → 933 行 |
| 6 | 共享组件（Toggle/Select）未提取 | ✅ Phase 2.5 提取至 shared/ |
| 7 | 无单元测试 | ✅ Phase 2.5 补 3 文件 30 用例 |
| 8 | 无 ErrorBoundary | ✅ Phase 2.5 加，已包裹 TerminalView |
| 9 | AI 不可编程发送 | ⬜ 设计方案已有，Phase 3 后实现 |
| 10 | 多视图 tab（RingBuffer Pub/Sub） | ⬜ Phase 3 或独立迭代 |
| 11 | 新数据 50ms 蓝色闪烁 | ⬜ P2 打磨项，不阻塞 |
| 12 | DTR/RTS UI | ⬜ Rust 命令就绪，TopBar 未加 toggle |
| 13 | 数据位/停止位/校验 | ⬜ 串口打开时未提供这些参数 |

**Phase 2.5 清理：** 修复 5 个技术债务（#1/3/5/6/7/8），新增 3 个低优先级项（#11/12/13）。剩余 4 项（#2/4/9/10）不阻塞 Phase 3。

---

## 五、V3 踩坑记录

见记忆系统 `v3-pitfalls.md`。当前已记录 B1-B3（CSS 布局错位问题），根因+修法已归档。

---

## 六、开发环境

| 工具 | 版本 |
|------|------|
| Node.js | v24.16.0 |
| npm | 11.13.0 |
| Rust | 1.97.1 |
| Cargo | 1.97.1 |
| MSVC | VS 2022 |
| Tauri CLI | v2（npx） |

日常开发命令见 [V3开发操作手册.md](V3开发操作手册.md)。

---

*文档随工程进展持续更新。每次完成一个 Phase 追加记录。*
