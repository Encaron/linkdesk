# 03-市场插件整理——`plugins/marketplace/`

> **对应任务：E6#86（第 3.6.3 轮）。本层最重的一轮——marketplace 占超限总数的 10/34。**
> **12 档是唯一权威**（[12-插件目录与UI细节.md §一](../03-插件市场/12-插件目录与UI细节.md)），本档案是它的**执行版**：把「超限表」变成「怎么拆」。
> **🔖 非新能力文档**：本档案是**纯文件搬家**的作业单——不新增、不修改任何 API / 贡献点 / schema。文中出现的 `contributes.*` 只是**引用既有字段**当判据（如 §〇② 引 `contributes.views[].render` 说明 bundle 表面怎么定），**不构成新能力设计**，故无需 8 维度设计前置。

---

## 〇、先读这个——三条硬事实（决定整个拆法）

**① SDK bundle key = basename，与子目录无关。**

```
packages/plugin-sdk/src/vite-config.ts:113
let key = basename(rel, extname(rel)).replace(/[^A-Za-z0-9_.-]/g, "_");
while (usedKeys.has(key)) key += "_";
surfaces.push({ key, finalName: `views/${key}.bundle.js`, abs });
```

→ **把 `views/SearchView.tsx` 移进 `views/sidebar/SearchView.tsx`，产物名不变**（仍是 `views/SearchView.bundle.js`）。
→ 🔴 **但 basename 必须保持唯一**：**门面文件绝不能改叫 `index.tsx`**（`index` 已被 entry 占用；第二个 index 会退化成 `index_`，语义混乱）。**门面保留原名。**

**② 只有 `contributes.views[].render` 指向的文件才是 bundle 表面。**

私有子组件（`DetailFeaturesTab` / `DetailChangelogTab`）**不在 plugin.json 里 → 不是表面**，随宿主 chunk 打包。移动它们**零构建影响**。

**③ 视图文件移动 → 必须同步 plugin.json `render`。**

dev 走源码路径；dist 走 `rewriteDistManifest` 改写为 `views/<Key>.bundle.js`。**少改一条 = dev 白屏 + 打包版白屏**（换插件后壳拿不到组件）。

---

## 一、现状（2026-09-11 实测）

`plugins/marketplace/src/` 27 个源文件（不含 `__tests__/`），**10 个超 12 档**：

| 文件 | 行 | 档 | 阈值 |
|:--|--:|:--|--:|
| views/DetailView.tsx | 1258 | views | 150 |
| styles/MarketplaceDetail.css | 861 | styles | 300 |
| services/marketplaceShared.ts | 779 | services | 200 |
| styles/MarketplaceSidebar.css | 680 | styles | 300 |
| services/updateDiscovery.ts | 519 | services | 200 |
| services/marketCatalog.ts | 390 | services | 200 |
| views/ExploreView.tsx | 325 | views | 150 |
| services/marketSources.ts | 259 | services | 200 |
| views/DetailFeaturesTab.tsx | 210 | views | 150 |
| views/SearchView.tsx | 203 | views | 150 |

**另有两个结构性问题（行数看不出来的）：**

- **`views/` 里 9 个文件、只有 7 个是真视图**——`DetailFeaturesTab.tsx` / `DetailChangelogTab.tsx` **在 plugin.json 里出现 0 次**（`grep -c` 实测），它们是 DetailView 的私有子组件，**摆错了房间**。
- **`styles/` 的命名已经编码了分组**——`MarketplaceSidebar.css` 被 5 个侧栏视图 import（Search/Installed/Explore/Disabled/Builtin），`MarketplaceDetail.css` 只被 DetailView 用，`MarketplaceConfirm.css` 只被 ConfirmInstall 用。**这是现成的三域地图**，本层只是把它显式化。

**超限根因（给下一个 AI 的上下文）**：E6#0.6c 在 2026-09-04 就设计好了 per-scope 门禁，但翻转条件写成「#30.11 拆完 MarketplaceSidebar.css 后翻 true」——#30.11 全部 ✅ 打勾，**却从未产出那个拆分**（当年 377 行的文件，今天 680）。开关就这么休眠了一个月。**这正是 00 号档案说的「判定挂在别人的一次性产物上」——本层第 5 轮要用「翻开关自己是一个带判据的任务」堵死它。**

---

## 二、views/ 目标结构

**分组轴 = 按 plugin.json 容器**（sidebar / detail / confirm）——与 CSS 已有分组同轴，与「谁被谁 import」同轴。

```
src/views/
├─ sidebar/
│   ├─ SearchView.tsx            203 → ≤150
│   ├─ InstalledListView.tsx      51  ✓
│   ├─ ExploreView.tsx           325 → ≤150
│   ├─ DisabledListView.tsx       91  ✓
│   └─ BuiltinListView.tsx        39  ✓
├─ detail/
│   ├─ DetailView.tsx           1258 → ≤300（门面/宿主）
│   └─ DetailView/                     ← 同名字夹（0d.10 先例）
│       ├─ info-bits.tsx               ← `fmtCount` / `InfoItem` / `Dash` / `InfoGroup`
│       ├─ useDetailState.ts           ← 状态 + 派生（useState/useEffect/useMemo 群）
│       ├─ useDetailActions.ts         ← 启/禁/自动更新/升降级/卸载
│       ├─ useInstallAction.ts         ← 安装门禁 + runInstall + 点击入口
│       ├─ DetailHeader.tsx            ← 头图/标题/作者/统计
│       ├─ DetailActionBar.tsx         ← 版本下拉 + 主动作钮 + 自动更新勾选 + pinned 说明
│       ├─ DetailInfoSidebar.tsx       ← 元数据侧栏分组
│       ├─ FeaturesTab.tsx             ← **由 `views/DetailFeaturesTab.tsx` 迁入改名**
│       └─ ChangelogTab.tsx            ← **由 `views/DetailChangelogTab.tsx` 迁入改名**
└─ confirm/
    └─ ConfirmInstall.tsx        123  ✓
```

**DetailView.tsx 拆法（实测结构 → 提炼单元）：**

| 现区段 | 行 | 去处 |
|:--|--:|:--|
| imports + 模块常量 | 1–102 | 留门面 |
| `fmtCount` / `InfoItem` / `Dash` / `InfoGroup` | 103–148 | → `info-bits.tsx` |
| 状态 + 派生（state/effect/memo，含磁盘位置、缺依赖、changelog、版本选择） | 151–425 | → `useDetailState.ts` |
| 启/禁/自动更新/升降级/降级确认/卸载 | 426–570 | → `useDetailActions.ts` |
| 安装门禁 + `runInstall` + `handleInstallClick` | 571–649 | → `useInstallAction.ts` |
| 展示派生（字段合并、分类、版本选择器、勾选、侧栏分组内容） | 650–1016 | 拆入 `DetailHeader` / `DetailActionBar` / `DetailInfoSidebar` |
| 主 return JSX | 1017–1258 | 门面组合三段 |

> **门面判据**：拆完 `DetailView.tsx` = **调三个 hook + 组合三个展示件**。若还剩 >300，说明有逻辑没搬干净。

> ⚠️ **不要动 `views/` 下 5 个侧栏视图的文件名**——`InstalledListView` / `DisabledListView` / `BuiltinListView` 已达标，**只搬家不改名**（basename 一变，产物名就变，多一次无谓风险）。

---

## 三、services/ 目标结构

**拆法：超限文件 → 同名夹 + 门面保留原路径**（0d.10 先例，仓库已有 10 例）。**不重排目录、不改文件名 → 消费方 import 路径零变更。**

```
src/services/
├─ marketplaceShared.ts      → 门面（779 → ≤200 门面体）
├─ marketplaceShared/
│   ├─ commands.ts                ← `ensureMarketplaceCommands`（E6#71g 命令组注册）
│   ├─ useMarketplacePlugins.ts   ← 目录/已装列表 hook + 刷新
│   └─ types.ts
├─ marketCatalog.ts          → 门面（390 → ≤200）
├─ marketCatalog/
│   ├─ types.ts                   ← MarketEntry / versions[] / 版本比较判据（#81 单源判据在此）
│   ├─ normalize.ts               ← 远端/本地条目归一
│   └─ select.ts                  ← `selectableVersions` 等选择器
├─ updateDiscovery.ts        → 门面（519 → ≤200）
├─ updateDiscovery/
│   ├─ discover.ts
│   └─ pin.ts                     ← #82/#83 的 pinnedVersion / autoUpdate 记账
├─ marketSources.ts          259 → ≤200（拆为 `marketSources.ts` 门面 + `marketSources/`）
├─ installGate.ts             96 ✓
├─ installConfirmPayload.ts   99 ✓
├─ installJobs.ts            153 ✓
├─ packageFiles.ts            27 ✓
├─ marketCategories.ts        44 ✓
├─ installedUpdateMeta.ts    179 ✓
├─ downloadCounts.ts         160 ✓
└─ marketSourceAdd.ts         31 ✓
```

**🔴 依赖单向（实测 import 图，拆完必须仍是这个方向）：**

```
marketCatalog（零内部依赖）
   ↑ marketSources / installConfirmPayload / marketSourceAdd / installGate
   ↑ updateDiscovery（→ installedUpdateMeta / marketSources / marketCatalog）
   ↑ installGate
   ↑ marketplaceShared（→ installGate / updateDiscovery）
   ↑ installJobs（→ marketplaceShared）
```

**`marketplaceShared` 是顶层门面——它 import 别人，别人不 import 它**（除 `installJobs`）。拆它时**别把 `installJobs` 的依赖反向拉进来**。

**`marketplaceShared` 的 12 处消费者**（移动/改名 = 全炸）：`index.tsx`、`installJobs.ts`、`updateDiscovery.ts`、5 个侧栏视图、`DetailView.tsx`、2 个测试。**门面保持 `services/marketplaceShared.ts` 原路径 = 零改动。**

---

## 四、styles/ 目标结构

> 🔴 **CSS 不搬出 `styles/`。** 门禁按 **`src/` 后首段目录**定 role（`check-file-size.mjs:129`）；把 `.css` 挪进 `views/` 会落进 `views` role，而该档只登记 `.tsx` → **逃出 styles 档 300 的覆盖**。**留原地，按域改名。**

**`MarketplaceDetail.css`（861 → 5 件，实测分节行号）：**

| 新文件 | 迁入区段 | 约行 |
|:--|:--|--:|
| `detail-header.css` | 36–295（Header + 缺依赖三通道 + 新版本块） | ~260 |
| `detail-actions.css` | 296–400（右上动作列 + NavBar） | ~105 |
| `detail-body.css` | 401–633（Body 布局 + 侧栏分组 + 画廊 + 元数据侧栏） | ~233 |
| `detail-features.css` | 634–776（功能 tab） | ~143 |
| `detail-changelog.css` | 777–861（更改日志 tab + warn 值） | ~85 |

→ 与 `DetailView/` 的子模块一一对应，**谁用谁 import**。

**`MarketplaceSidebar.css`（680 → 3 件）：**

| 新文件 | 迁入区段 | 约行 |
|:--|:--|--:|
| `sidebar-shell.css` | 1–244（头部/搜索/市场源按钮/添加浮层/`.extensions` 容器） | ~244 |
| `sidebar-list.css` | 245–503（Section header + 扩展项 + 禁用项 + 安装钮） | ~259 |
| `sidebar-explore.css` | 504–680（探索项 + 空态重试 + 顶注 + 齿轮） | ~177 |

**`MarketplaceConfirm.css`（107）/ `MarketplaceView.css`（49）** → 达标，**不动**。

> **拆 CSS 的纪律**：只**搬**选择器块，**不改任何一条声明**。拆完 grep 一遍 `.mpd-` / `.mps-` 前缀类名，确认在 `src/` 里**全部仍被某个 CSS 文件定义**（漏搬 = 静默丢样式，不报错）。

---

## 五、`__tests__/` 处置

现 10 个测试平铺在 `plugins/marketplace/src/__tests__/`。**09-插件目录规范 §111 要求「与被测文件同目录，`__tests__/` 内保持相同相对路径」。**

**推荐：本轮不动**——测试文件不受行数门禁约束（`SKIP_DIRS` 含 `__tests__`），且平铺虽不规范但**不产生任何运行时/构建影响**。若要动，按被测域分 `__tests__/services/`、`__tests__/views/` 两个子夹，与 09 规范对齐。

> 🔴 **决策已拍板（[00 §五](00-整理档案.md) 第 4 条，2026-09-11）**：settings 用**同目录共置**、其余 4 插件用 `__tests__/`——**两套惯例并存，判为不合规**。拍板结果 = **settings 迁到 `__tests__/`**，走 3.6.4 轮（E6#87d）。**本档案（marketplace）自己的 10 个测试本轮仍不动**（见上）。

---

## 六、🔴 plugin.json 同步清单

**本拆分方案下，需要同步的 `render` 路径数量 = 取决于是否做 views 分组：**

| 方案 | plugin.json 改动 | 风险 |
|:--|:--|:--|
| **A：views 不分组**（只建同名字夹） | **0 条**（7 个视图全部原地不动） | 最低 |
| **B：views 按容器分组**（本档案 §二） | **7 条**（5 sidebar + 1 detail + 1 confirm） | 中——每条都要手改 + 验 |

**两方案都完整解决 10 个超限**（分组不解决行数，同名夹才解决）。**分组只换来「目录可见性」。**

> 🔴 **决策点（[00 §五](00-整理档案.md) 第 2 条）**：**推荐 B**——理由：9 个文件混 3 类角色本身就是「该建子文件夹」的信号；且 CSS 已按同轴命名，目录不跟上则两处地图对不上。**若选 A，本轮风险显著更低。**

**改 render 的机械流程（方案 B 时逐条走）：**

1. 改 plugin.json 的 `render`（如 `src/views/SearchView.tsx` → `src/views/sidebar/SearchView.tsx`）
2. **同步改源码里所有相对 import**（`../services/...` → `../../services/...`）——**层级加深一层，全部相对路径 +1 级**
3. `npm run check`（tsc 抓漏）
4. **实机验**：dev 起 Electron → 逐个打开 7 个视图（5 侧栏 + 详情 + 安装确认）→ 无白屏/无 console 报错

> ⚠️ **第 2 步最易漏**：`views/` 下每个文件都有 `../services/` `../components/` `../styles/` 的 import。**漏一个 = tsc 红**（这是好事，机械拦得住）。**但 CSS 的 `import "../styles/x.css"` 若写成动态或字符串拼接 → tsc 抓不到**（本项目无此写法，此处仅作提醒）。

---

## 七、🔴 本轮风险

| 风险 | 形态 | 规避 |
|:--|:--|:--|
| **① plugin.json 不同步** | 🔴 本层**头号风险**——7 条 render 改错/改漏 → 视图白屏 | 逐条核 + 实机逐视图验（§六） |
| **② basename 撞车** | 🔴 门面若改名 `index.tsx` → 退化 `index_` | **门面恒保留原名**（§〇①） |
| **③ CSS 逃门禁** | 🔴 `.css` 搬到哪 | ~~CSS 恒留 `styles/`（§四）~~ → 🔴 **本行原判据已作废（2026-09-11 订正）**：门禁**按扩展名管，`.css` 恒 300 不看目录**，co-located `.css` 合法（现成实例 `views/SettingsView.css` 631）。真正要防的是「拆 `.css` 时漏搬块 = 静默丢样式」（[04 §六⑤](04-其余插件整理.md)）。口径见 [12 档 §一·三](../03-插件市场/12-插件目录与UI细节.md) |
| **④ 循环 import** | 🟡 services 拆散后方向反转 | 拆完按 §三 import 图核一遍 |
| **⑤ 漏搬样式** | 🔴 CSS 拆分漏一块 → **静默丢样式，编译不报错** | 拆完 grep 类名前缀（§四尾注） |
| **⑥ 相对路径层级** | 🟡 视图进子夹 → `../` 变 `../../` | tsc 拦得住；**逐文件过一遍，别批量 sed** |
| **⑦ 顺手改逻辑** | 🔴 拆 1258 行时「这儿好像有 bug」→ 混进 commit | **只搬家**；发现的问题单独立案（照 [02 §三](02-主软件整理.md) 同名条款） |
| **⑧ 版本轴** | 🟡 改 `plugins/marketplace/` = **插件轴**，不是软件轴 | 走 version-bump：纯重构 → `fix:`，bump `1.0.26 → 1.0.27` |

---

## 八、本轮验收

> ✅ **全部通过（2026-09-11 收官）。** 逐条判据与实测证据见下。

- [x] 10 个超限文件**全部 ≤ 12 档阈值** —— 12 档复算 marketplace **87 件，超限 0**（views 7 / services 4 组同名夹 / styles 11 / 其余达标件）
- [x] `DetailView.tsx` ≤300 且**只剩「调 hook + 组三件」** —— **1258 → 267**（门面 = 调 hook + 组 Header/ActionBar/Body 三件）
- [x] `DetailFeaturesTab` / `DetailChangelogTab` **不再出现在 `views/` 根** —— 全仓**代码引用残留 0**（仅存于新文件的出处注释）
- [x] `services/` **消费方 import 路径零变更** —— `git diff --stat` 里 **12 个消费者 import 零出现**（门面守恒原路径原导出面）
- [x] CSS 拆分后 **`.mpd-*` / `.mps-*` 类名在 `src/` 内定义数 = 拆分前** —— 三路机械验证：去注释逐行多重集相等（详情 687 / 侧栏 556）· 跨文件重复选择器 0 · 🔥 **产物对账 `index.bundle.css` 逐字节相同（204,639 B）**，类定义集合逐一相等
- [x] **实机（dev）**：5 个侧栏视图 + 详情页 + 安装确认页**逐个打开** —— 5 侧栏全渲（`.ms-extension-item` 有内容）· `.mpd-detail` 609px（头 + `.mpd-acts` + 三 tab navbar + body + 信息侧栏）· `.mpd-confirm`（「确认安装」与「确认更新」两态）
- [x] **实机（dev）**：详情页核心动作走一遍——装/卸/启/禁/升降级/自动更新勾选 —— 全绿（4.0.0 ⇄ 2.0.0 升降级带「此版本较旧，配置可能不兼容」确认 · 自动更新勾选往返）
- [x] **打包版（安装版）**：至少打开一次详情页 —— ✅ 见下「打包版验收」
- [x] `npm run check` 全绿 —— 150 测试文件 / 2030 用例 / 0 clone
- [x] **version-bump**：`plugins/marketplace` → **fix，1.0.26 → 1.0.27**
- [x] 清单回勾 + 本档案结果注记 —— 提交 `99b0ff8cc` + 本注记

### 8.1 打包版验收（E6#86f 最后一项）

`npm run build && npx tsc -p electron/tsconfig.json && node scripts/electron-commonjs-fix.cjs && npx electron-builder --dir` → `E:\linkdesk-build\win-unpacked`（12:38），`resources/bundled-plugins/marketplace.linkdesk-plugin` = **1.0.27**。

> 🔴 **验收方法偏离（必须记）**：直接开打包版验的是**旧代码**——真实 profile（`%APPDATA%/linkdesk/plugins/marketplace`）里装的 marketplace 是 **1.0.17**，boot **不覆盖已存在目录**（memory `bundled-same-version-no-refresh` 的既有语义）⇒ 详情页会是 1.0.17 的老实现，验不到本轮的拆分。**改用一次性 `--user-data-dir=E:\linkdesk-build+scratch\86f\ud` 空白 profile**，boot 从 bundled zip **全新物化 18 个插件，marketplace = 1.0.27** ⇒ 才是本轮代码。验完即删该 profile（一跑一清）。
>
> 🔴 **原「另立一案」口径已作废（2026-09-11 同日订正，见 [06 §五](06-目录结构二次收口.md)）**：上面那条「bundled 升版不会自动覆盖」**不是未修 bug，是 E6#15n（2026-09-06 用户拍板）的主要内容**——「已装用户拿 seed 内容更新 = 市场真实条目（E6#26b）按版本发现/更新，与第三方同一路，**boot 不伸手**；出厂批未上架/断网期不随壳同步——**用户拍板接受**」。E6#15o 的实证补强（boot 连**异版本**也不覆盖）是同一结论的第二块证据。**不该另立一案，也没有东西要修。**
> 真实欠账只有一条且已立案：**18 只 bundled 插件尚未上架官方目录**（2026-09-11 实测官方 catalog 仅 2 条：`hello-linkdesk@0.1.1` / `first-run-setup@1.0.0`）⇒ E6#15n 拍板的「走市场更新」这条路**对它们还不存在**，归属 **E6#26b**。本轮真正新增的只有**验收纪律**：**打包版验收必须用一次性空白 profile**（直开安装版验的是旧物化代码，会验出假结论——上面那段 🔴 偏离记录的正是这件事）。

**实测（pool 目标 CDP）**：

| 项 | 结果 |
|:--|:--|
| 架构确认 | 壳渲染进程（`index.html`）body 仅 `<div class="app-shell">` 空壳；**整个 UI 由 pool 渲染**（E5.7 极简 Pool 语义）——初看像白屏，实为设计 |
| 图标栏 | 4 个 `.icon-btn` 全部渲染，图标走 `linkdesk://{id}/resources/*.svg`（**自定义协议在打包态可用**） |
| 详情页 | 点侧栏行 → 新标签「插件详情」→ `.mpd-detail` **1078×813**，标题「极光玻璃」，动作列 `禁用 \| 卸载`，navbar `详情 \| 功能 \| 更改日志` |
| 三 tab | 详情 / 功能 / 更改日志 **各自渲出不同内容**（证明 `DetailView/` 子模块在打包产物里完整解析） |

### 8.2 结果注记——偏离与发现（均为「只搬家不改逻辑」而原样保留）

| # | 类别 | 内容 | 处置 |
|:--|:--|:--|:--|
| **a** | **偏离档案粒度**（#86a） | 档案给 9 件；`useDetailState` 合并写会到 ~275 行（超 150）→ **七分**（身份/版本/数据/包文件/动作/版本动作/安装腿），另加 `info-top` / `useActionBits` / `features-groups` | 记此；**结果更细不更粗**，总件数 22（档案量级一致） |
| **b** | **偏离档案行表**（#86d） | §四 行号表只盖 **36–861**；**1–35 行**（头注 + 容器/空态）无处安放 → 多出第 **6** 件 `detail-shell.css` | 记此；拆完总量守恒（多重集比对已证） |
| **c** | **偏离「谁用谁 import」**（#86d） | §四 要求 CSS 按使用者分散 import → 实际**单一 import 点全量 import** | 理由（已写进 `detail-shell.css` 头注）：① surface 的样式需求要按 module 图算传递闭包，分散 import 在 SDK 逐 surface 打包下易漏；② 跨文件重复选择器 0 ⇒ 级联顺序与本拆分无关；③ SDK 产出单根 `index.bundle.css`，import 位置不影响产物；④ 单点便于「清单式」查漏 |
| **d** | **既有欠账·未定义的类** | `.ms-section-items`（**4 个侧栏视图在用，全仓无定义**）· `.mpd-version-select`（`DetailView/actionBits.tsx` 在用，全仓无定义） | **未动**（只搬家）；⚠️ 待单独立案——**不报错、静默无样式** |
| **e** | **既有重复**（#86b 发现） | `catalogRowText.authorLabel` 与 `installConfirmPayload.authorLabel` **逐字节同** · `formatSize` 与 `fmtSize` **取整不同**（同量两面显示可能不一致） | **未归一**；待单独立案 |
| **f** | **fixture 缺口**（#86f 发现） | 实机用的 demo-greet fixture 目录 `downloadUrl: null` ⇒ **侧栏行内安装静默无反应**（`useCatalogInstall` 要 `entry.downloadUrl`） | **fixture 缺口，非产品回归**（详情页走 `versionDownloadUrl(entry, ver) ?? entry.downloadUrl` 命中）；补 fixture 后装成功。⚠️ 但「**行内安装无入口时报错也不出声**」这一点值得单独立案 |
| **g** | **既有构建失败**（非本轮） | `npm run build:plugins` 报 `editor` 插件 `worker.format "iife"` 不支持 code-splitting | **stash 单验复现 ⇒ 先于本轮存在**；按纪律未动 |

---

> **← 上一档案：** [02-主软件整理.md](02-主软件整理.md)
> **→ 下一档案：** [04-其余插件整理.md](04-其余插件整理.md)
